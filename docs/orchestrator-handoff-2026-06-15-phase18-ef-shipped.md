# Orchestrator Handoff — 2026-06-15 (Phase 18 mid; 18-E + 18-F SHIPPED+CLOSED; 18-D + trailing remain)

**Audience:** a fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). **Read [`docs/orchestrator-context.md`](orchestrator-context.md) end-to-end first** (durable role framing), then this doc (the delta since the 2026-06-14 Phase-18-mid handoff). Bootstrap via `scripts/orchestrator_bootstrap.md`.

## 1. Clean boundary (verify: `git log --oneline -8` + `git status -sb` + `git worktree list`)
- main HEAD **`ec8f2d68`**; **origin/main IN SYNC** (pushed). **Single `main` worktree** (all 18-E/18-F worktrees merged ff + removed).
- **~8380 fast tests green** on `main` (re-run on the merged head at the 18-F close; docs commits since). **Schema v30** (unchanged since 18-C). Ruff clean. **ZERO `Co-Authored-By`** holds.
- Only untracked drift: the `exports/research/shadow-expectancy-*` dirs (research-lane artifacts; not the orchestrator's lane).
- Merge discipline unchanged: rebase the branch onto `main` → `merge --ff-only` → re-run the suite ON the merged head (never carry a branch count forward).

## 2. What this generation shipped (two arcs + a discipline)
- **18-E — operational tool-health monitor** (`65de5a7f`): new `swing/monitoring/` package — `compute_tool_health(conn, *, cfg, prices_cache_dir, now) -> ToolHealthStatus` (the §3 monitor-status envelope) + `scripts/tool_health.py` operator probe; aggregate-don't-instrument over pipeline-run / Schwab-TTL / data-freshness; the ratified additive `sessions_behind` helper in `swing/evaluation/dates.py`. NO schema. Gate: QA + CHARC C-E + operator CLI witness. **The operator CLI witness caught a false-RED weather-ticker defect the 8326 byte-tests + 4 Codex rounds both missed** (`get_latest(conn)` defaulted to ticker `QQQ`; production records weather under `cfg.rs.benchmark_ticker`=SPY) — fixed; banked as CLAUDE.md §Gotchas (read-path default-arg vs config).
- **18-F — GUI health stoplights** (`f075e7b5`): TWO topbar stoplights on EVERY page (tool-health live; research GREY-until-18-D) + two read-only drill-downs (`/health/tool`, `/health/research`), via a **Starlette context processor** registered in `swing/web/app.py:_build_templates` (the per-VM-field fallback SIDESTEPPED → the every-base-VM-or-500 gotcha + the D15 ~15-VM duplication avoided). DEFENSIVE-never-raises→grey (triply-guarded). `review-strong` converged R1-R5 (FOUR research false-green vectors closed). NO schema/dep. Gate: QA + CHARC C-F1..C-F5 + the BINDING operator browser gate (5/5).
- **Discipline banked** (`ec8f2d68`, orchestrator-context §Process hygiene): executing dispatches run the FULL fast suite **BEFORE the Codex review AND after convergence** (universal, operator-directed; the 18-F latent-invariant-regression lesson — the VM-manifest + theme-token tests are global invariants per-task TDD doesn't run). Apply it in EVERY executing-dispatch prompt's verification steps. **CHARC recipe-mirror request is PENDING** (flagged 2026-06-15 to add the before-run to `docs/implementer-dispatch-recipe.md` §verification, the implementer SPOF).

## 3. Codex review tiering + the codex-auto-review A/B (banked this generation)
- **Review tiers (recipe §3, `8e78e768`-era):** `-p review-fast` (writing-plans/docs — gpt-5.4-mini/low) vs `-p review-strong` (executing/ANY production code — gpt-5.5/high; NEVER tier down). They are Codex profiles (`$CODEX_HOME/<tier>.config.toml`); both exist. Omit `-p` + report if a profile is absent.
- **codex-auto-review A/B (FIRST run, at 18-F) — NOT ADOPTED.** `codex-auto-review` = the built-in `codex exec review` subcommand (model + codex's packaged review prompt), distinct from our hand-rolled `codex exec -` manual loop. It runs git itself → CANNOT run in a worktree (the worktree `.git` is WSL-unresolvable); the orchestrator ran it on the converged diff from a detached-HEAD checkout in the MAIN repo (real `.git`). Result: found NO escaped major (review-strong's convergence confirmed) BUT defaulted to gpt-5.5/**effort=none**, emitted 1.37MB unstructured prose with NO parseable verdict token. **review-strong (gpt-5.5/high manual loop) STAYS the binding gate.** CHARC's promotion call: settled, no-promote. A FUTURE experiment if pursued: head-to-head on a PRE-fix diff with the built-in forced to `-c model_reasoning_effort=high`.

## 4. Phase 18 state — 18-A/B/C/E/F SHIPPED; 18-D + trailing remain
Live tracker: [`docs/phase18-todo.md`](phase18-todo.md). **CLOSED:** 18-A (`c45d8752`), 18-B (`5cce72db`), 18-C (`2b381461`, schema v30), 18-E (`65de5a7f`), 18-F (`f075e7b5`).
**QUEUED (operator sequences; do NOT commission until "go"):**
- **18-D — RD research data-collection-health monitor.** The natural next arc: it CONSUMES `yfinance_calls` + the integrity data AND **WRITES `exports/research/health/latest.json` (the §3 envelope)** — so building it **flips 18-F's grey research stoplight to live** (the read contract + the `RESEARCH_MONITOR_ID="research_measurement"` + the ≤7d-fresh `generated_ts` requirement are already in place; RD-flagged, on record). RD-lane; **CHARC owes its architecture pass at commissioning** (new standing process). Brief: `docs/data-collection-health-monitor-commissioning-brief.md` (architecture-passed; C4-C5).
- **18-G** (broad dead-brief sweep, D10) · **R1** (declare `requests` in pyproject — its own tiny dispatch) · **18-H.1** (Schwab configured-but-tokens-absent → yellow) · **18-H.2** (404 error-surface unification — route 404s through `page_error.html.j2`) · **18-H.3** (stoplight ICON, not the word, in the health drill-down status column — operator-requested at the 18-F browser gate).
- Organic Phase-16 trailers: TROX age-off, the dividend/interest marker, the monthly cadence witness + RD QAs.

## 5. The two directors + comms (unchanged)
CHARC (tool/architecture) + RD (research/measurement). POST status/return_report/query via `python scripts/role_mail.py post --from orchestrator --to charc,rd[,operator] --type … --body-file …` FROM the main repo dir; there is NO orchestrator inbox — direction comes FROM the operator in chat (the operator relays director rulings). Measurement-core arcs: RD measurement-integrity merge-blocking; CHARC architecture on the diff. The IMPLEMENTER never posts to the mailbox (you post post-QA). **Open with directors:** the CHARC recipe-mirror request (§2) is pending CHARC action.

## 6. Binding disciplines + the dispatch model (unchanged; full detail in orchestrator-context + memory/)
The orchestrator spawns the implementer as a **sub-agent via the Agent tool** (the `.claude/agents/implementer-<model>-<effort>` cells; the SPOF [`docs/implementer-dispatch-recipe.md`](implementer-dispatch-recipe.md)) on the operator's "go": select the cell by TASK (writing-plans ~`opus-xhigh`, executing ~`opus-high`), **announce it (vetoable) before spawn**, `Agent(run_in_background=true)` (NO `model` override — the cell carries it; NO `isolation` — the cell makes its own `.worktrees/<name>`). Await the return; QA-against-disk (the diff, trailers `[]`, convergence from the REAL `.copowers-findings.md`, data shapes vs live code); post the QA'd return to directors; the gate; operator authorizes; rebase + `merge --ff-only` + suite on the merged head. You (orchestrator) OWN the codex-auto-review A/B + any git-resolvable-checkout Codex work (the worktree `.git` is WSL-unresolvable). Commit the brief BEFORE dispatch. Always announce the cell (= the model/effort rec). **Web/UI work: the operator browser gate is BINDING** (TestClient can't catch a base-render 500). Push only when the operator asks (origin currently in sync).

## 7. Operational gotchas worth re-noting
- **cwd drift:** run worktree pytest in a SUBSHELL `( cd .worktrees/<wt> && python -m pytest … )`; `cd /c/Users/rwsmy/swing-trading` to reset. Run `role_mail` + write its `--body-file` FROM the main repo.
- **Detached `swing web` survives TaskStop** + keeps 8080 bound — kill by port (`Get-NetTCPConnection -LocalPort 8080` → `Stop-Process -Force`), verify the port is free + no `*swing.cli web*` straggler.
- **Worktree CLI verification:** the editable install resolves the MAIN tree — run the worktree's code via `PYTHONPATH=. python -m swing.cli <cmd>` (PowerShell `$env:PYTHONPATH="."`) from inside the worktree.
- **Codex usage cap:** the WSL Codex CLI hit an account usage cap mid-18-E (reset ~6/17 16:41; operator raised it, back now). If it blocks again, the implementer flags NOT-CONVERGED (never fabricate); resume post-reset. `codex --version` (= codex-cli 0.135.0) is the liveness probe.

## 8. FIRST ACTION
Greet the operator; confirm the clean boundary (main `ec8f2d68`, origin in sync, ~8380 green, schema v30, single worktree, Phase 18 mid with 18-A/B/C/E/F closed). Announce the new generation to both directors (`role_mail … --to charc,rd --type status`). NO active dispatch, NO in-flight work — 18-E/18-F are merged + closed. Surface §4's queued arcs (18-D is the natural next) + the §7 gotchas. **Do NOT commission 18-D / trailing / anything until the operator says go.**

*End of handoff. Phase 18: the data is trustworthy + observable (18-A/B/C) and the operator + GUI now SEE the collection apparatus's health (18-E + 18-F); 18-D will light the research half of the GUI it just shipped.*
