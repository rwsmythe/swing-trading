Bugs:
1. In Dashboard.  When I click the "refresh now" button at the bottom of the page, the "Watchlist - near trigger" label disappears and the watchlist table butts up against the bottom of the "Open Positions" table.  Hard refresh on the page restores the layout.
2. I adjusted a stop value in accordance with an advisory.  The advisory remains after page refresh. It should go away when the conditions are met.
3. The "Rationale" boxes in adjust stop and Enter buttons should be drop downs with defined statements.  I believe this is in accordance with the CLI.  Additionally, Adjust Stop should have a notes box as well for free-form text as needed.
4. When expanding watchlist entries, if the chart is unavailable then there should be a reason stated (not enough data, etc...)
5. A+ entry says 'Stop Limit' but only provides the stop value, not the limit value.
6. Is total risk exposure displayed anywhere?
7. [2026-04-24] Dashboard "Today's decisions" shows a ticker that is not A+. Observed: SLDB listed under "Today's decisions" ("Buy-stop limit $8.87 · 15 sh · $37 risk / A+ setup, 7.0% ADR, 155% prior trend"), but expanded watchlist for the same ticker renders "Chart unavailable — this ticker isn't in today's charting scope (A+ names + top near-trigger watchlist)". These two surfaces must agree: today_decisions is populated only for A+ tickers (recommendations/build.py), and chart-scope's A+ set reads from the same candidate data — so they should never disagree for a given session. Preliminary diagnosis: "mixed as-of anchors" — same failure class that Session 2 Round 3 Major 1 fixed for the criteria panel. today_decisions may be reading from a different evaluation run or snapshot than the chart-scope resolver. Also observed (NOT a bug, persistence artifact only): "Buy-stop limit" text in the action line — the format was corrected in Session 2 (commit 32d4ba6) but existing daily_recommendations rows carry the old text; fresh pipeline run regenerates with "Buy-stop".