# Orchestrator Handoff -- 2026-06-04 (Phase 15 mid-flight)

**Audience:** A fresh Claude Code instance taking on the ORCHESTRATOR role for the Swing Trading project (`c:\Users\rwsmy\swing-trading`). This replaces an outgoing orchestrator at ~5% context. NOT an implementation prompt -- you drive the copowers cycle + QA + merge; you do not implement.

## 1. Clean boundary (verified)
- **main = origin/main = `65d6fc8d`** (0/0 with origin; working tree clean; only `main` exists local + remote). NOTHING in flight; NO open worktrees; NO servers running.
- **~7105 fast tests green** on main (re-run-confirmed 2026-06-04; the earlier 1-fail was a co-residency flake -- see §6). **Schema v24.** ruff clean. Production ZERO open discrepancies.
- **Streaks:** ~700+ commits **ZERO `Co-Authored-By`**; **L2 LOCK re-anchored ONCE** (`bf7e071`->`9d05a8f`, the schwabdev-v3 arc -- the only sanctioned baseline move; `docs/schwab-v3-endpoint-diff.md` proves zero new endpoints).

## 2. What shipped this session (Phase 15 -- three arcs, all CLOSED)
Each a full copowers cycle (brainstorm -> writing-plans -> executing-plans), Codex-converged, operator-browser/live-gate passed. Per-arc detail in `docs/phase3e-todo.md` `#20`/`#21`/`#22`.
- **`#20` schwabdev v2.5.1->3.0.5 + Fernet** (`7ee04c3`) -- token storage JSON->v3 SQLite, comprehensive preflight + non-interactive guard, logout/revoke rewrite, Fernet-at-rest, deleted P14.N7 + F-1 badge, the FIRST-EVER L2 re-anchor (operator GATE-A sign-off), operator GATE-B live-OAuth cutover DONE. Done in an ISOLATED VENV (operator's live env protected) -- memory `feedback_isolated_venv_for_shared_dependency_migration`.
- **`#21` B-7 operator failure-mode classification** (`d6092c7d`) -- the FIRST v24 migration (nullable `trades.failure_mode` 7-token CHECK), capture-only, orthogonal to grade+mistake-tags, web form + CLI `--failure-mode` + chronology read-back. Operator browser gate migrated the live DB v23->v24 (auto-backup-gated).
- **`#22` process-grade-trend chart redesign + reviews nav-date fix** (`e9b0292e`) -- presentation-only; small-multiples (3 inline-SVG panels), NO schema. Operator browser gate PASSED incl. an inline dark-mode gate-fix (SVG `<text>` is painted by `fill`, NOT `.muted`'s `color` -> defaults black -> invisible on dark; fix = `.process-grade-trend-chart text { fill: var(--fg-muted) }`).

## 3. BANKED + fully scoped -- ready to commission NOW
**Pattern-observation pool widening (aplus -> aplus+watch).** Widen the nightly `_step_pattern_detect`/`_step_pattern_observe` pool so the Phase-14 temporal log accumulates the ~83x-larger watch population as forward-walk data at ZERO capital risk + no Finviz change. Commissioning brief + the operator D1-D6 triage LOCKS are at `docs/pattern-observation-pool-widening-commissioning-brief.md` (Sec 10). **Locked:** D1 commission-standalone-after-PGT (now eligible -- PGT closed); D2 Phase-15 strategic-backlog (B-family); D3 pool=`aplus+watch` (NOT skip); D4 `finviz_screen_state` JSON provenance, **NO schema change**; D5 accept-and-measure (the brainstorm MEASURES the ~80x observe-load; cap ONLY past an operator threshold + ONLY with a #27 `warnings_json` audit -- NO silent cap); D6 single Codex chain. **Substance = the observe-load bound + bucket provenance + test rework, NOT the one-line predicate (`runner.py:1531`).** Out of scope: any ruleset/sizing/bucket/recommendation/Finviz-screen change; the beyond-Finviz universe net (separate future commissioning). To commission: author its `copowers:brainstorming` dispatch brief (Sec 4 Q1/Q2/Q3 are pre-resolved by D3/D4/D5; Q4 idempotency + Q5 tile-isolation remain brainstorm work), commit it, then provide the inline prompt.

## 4. Also open (Phase 15 remaining)
- **Phase-14 cross-sub-bundle integration review** (Sec 9.1 Q6) -- operator browser-witnessed coherence pass across charts + review/journal + metrics overview + Schwab; a retrospective.
- **B-1..B-8 strategic set** -- temporal-log-gated; resume once the v22 observation log accumulates forward-walk data over months (the pool-widening above feeds this).

## 5. READ FIRST (in order)
1. `CLAUDE.md` line-3 (Phase-15 state) + the §Gotchas (38+ BINDING) + Invariants/Conventions.
2. `docs/phase3e-todo.md` top: `#22`/`#21`/`#20` (the shipped arcs) + the Standing Open Backlog §A + `#5` (the punch-list / "Phase 15+").
3. `docs/orchestrator-context.md` -- orchestration framing, the "Pre-Codex review + brief-authoring disciplines" (Expansion #N catalog), in-flight section, lessons.
4. Memory at `C:\Users\rwsmy\.claude\projects\c--Users-rwsmy-swing-trading\memory\` -- esp. `feedback_orchestrator_performs_merge`, `feedback_orchestrator_qa_implementer_product`, `feedback_no_false_green_claim`, `feedback_await_return_before_qa`, `feedback_commit_brief_before_inline_prompt`, `feedback_always_provide_inline_dispatch_prompt`, `feedback_codex_round_limit_suspended`, `feedback_wsl_native_codex_invocation` (+ the 2026-06-03 prefix-required + stdin-pipe corrections), `feedback_implementer_persist_codex_responses`, `feedback_isolated_venv_for_shared_dependency_migration`, `feedback_seeded_gate_masks_default_state`, `feedback_visual_gate_both_render_and_browser`, `feedback_taskstop_does_not_kill_detached_server`, `feedback_commit_message_trailer_parse_hazard`, `feedback_orchestrator_vs_implementer_execution`.

## 6. SESSION-SPECIFIC operational learnings (carry forward)
- **WSL Codex transport (VERIFIED 2026-06-02/03):** `wsl.exe bash -ilc 'export PATH="$HOME/.local/node22/bin:$PATH"; codex exec ...'` -- the **PATH prefix is REQUIRED** (bare `-ilc` resolves the DEAD `/mnt/c/.../npm/codex` shim). Prove liveness with `codex --version` -> `codex-cli 0.135.0` (NOT `command -v codex`). **Pass the prompt via STDIN** (`cat prompt.txt | codex exec -s read-only --skip-git-repo-check -`), NOT `"$(cat ...)"` (breaks on parens). Pre-generate the diff on Windows; tell Codex NOT to run git; persist each round to `.copowers-findings.md`.
- **xdist co-residency flakes (THREE seen this session):** `test_ohlcv_reader_re_export_identity`, `test_read_cohort_csv_against_committed_v2trf`, `test_prices_refresh_uses_pipeline_eval_anchor` -- all PASS 1/1 in isolation (`-n0`), flake only under xdist co-residency. When a post-merge run shows 1 fail, re-run the failing test with `-n0`; if it passes, it's this family (not a regression). Real test-isolation debt -- a candidate small cleanup cycle (`@pytest.mark.xdist_group`/`forked`).
- **Branch divergence -> rebase-then-ff:** if main moved ahead of a dispatched branch's base (e.g. a doc commit landed mid-flight), `git -C <worktree> rebase main` then `git merge --ff-only` preserves linear history (the project avoids merge commits). Disjoint files -> clean rebase.
- **SVG `<text>` dark-mode gotcha:** a `color`-only CSS class (`.muted`) does NOT color SVG text -- SVG `<text>` needs a theme-aware `fill` or it defaults to black + vanishes in dark mode. (Candidate CLAUDE.md gotcha line; operator deferred -- ask if recurs.)
- **Shell trap:** `grep -c` exits 1 on zero matches -> breaks an `&&` chain (silently skipped a commit once). Don't chain `grep -c ... && git commit`.
- **Inline gate-fixes:** small browser-gate-caught defects (e.g. the dark-mode SVG-text fix) are orchestrator-inline on the branch (TDD: failing test -> fix -> re-witness), matching the Phase-14 inline-gate-fix precedent -- not a re-dispatch.

## 7. BINDING orchestration disciplines (inherited)
- **Merge is an ORCHESTRATOR action** at QA-pass/Codex-convergence/operator-gate-pass -- do NOT ask "shall I merge"; the next-phase dispatch brief + inline prompt are orchestrator actions (`feedback_orchestrator_performs_merge`). Merge + push + housekeeping.
- **QA every implementer product against disk** before merge (`feedback_orchestrator_qa_implementer_product`): branch base/merge-base; `%(trailers)` ZERO Co-Authored-By on every commit; diff scope; schema verdict; LOCKs preserved; convergence read from `.copowers-findings.md`; the deliverable's substance read (not the self-report). **AWAIT the implementer's RETURN before QA** -- touch nothing on an in-flight worktree (`feedback_await_return_before_qa`).
- **NEVER false-green** (`feedback_no_false_green_claim`): after EVERY merge, run `python -m pytest -m "not slow" -q` on the MERGED HEAD and READ it before claiming green; isolate the known co-residency flakes (§6) from genuine failures. For a SHARED-dependency-version migration, the post-merge SHARED-env suite is a false-red -- re-run in the isolated venv (`feedback_isolated_venv_for_shared_dependency_migration`).
- **copowers cycle** (brainstorm -> writing-plans -> executing-plans), each with a single adversarial Codex chain RUN TO CONVERGENCE (`NO_NEW_CRITICAL_MAJOR`; the ~5-round cap is SUSPENDED). Codex MCP is DEAD -- use the WSL prefix+stdin form (§6).
- **Operator gates are BINDING for UI/live work** and must witness the UNSEEDED/default state (`feedback_seeded_gate_masks_default_state`). For UI: stand up the branch server from the worktree on `--port 8081` (presentation-only -> no live-DB mutation; a schema-touching arc needs care), operator drives the browser, merge BLOCKED until confirmed, then kill the port by PID (`Get-NetTCPConnection -LocalPort 8081` -> `Stop-Process -Force`) + verify free (`feedback_taskstop_does_not_kill_detached_server`).
- **Worktree discipline:** `python -m swing.cli` (NOT bare `swing`) in worktrees; prefix git/test with `cd <worktree> &&` + re-verify `git branch --show-current` before each commit. After an executing-plans merge that changed installed deps, reinstall from main.
- **NO `Co-Authored-By`; NO `--no-verify`;** final `-m` paragraph PLAIN PROSE; verify `git log -1 --format='%(trailers)'` is `[]` before every push (a leading `Word:` line parses as a trailer -- `feedback_commit_message_trailer_parse_hazard`). **Commit the dispatch brief BEFORE the inline prompt** (`feedback_commit_brief_before_inline_prompt`); ALWAYS provide a paste-ready inline implementer prompt in chat for every brief (`feedback_always_provide_inline_dispatch_prompt`).
- **Pace:** the operator is highly engaged + paces deliberately at boundaries; present milestones + the forward path + let them set pace; "pause" means stop all forward motion. Default to implementer-dispatch over orchestrator-inline to minimize orchestrator context growth (`feedback_orchestrator_vs_implementer_execution`); small gate-fixes go inline (§6).

## 8. FIRST ACTION
Greet the operator, confirm the clean boundary (`65d6fc8d`, ~7105 green, v24, three Phase-15 arcs closed), and ask which item to commission next -- the **pattern-observation pool widening** is banked + fully scoped (D1-D6 locked) and is the natural next pickup; the **Phase-14 cross-sub-bundle integration review** is the other open near-term item. Do NOT commission unprompted. When commissioned, author the brainstorming dispatch brief (commit BEFORE the inline prompt), then provide the paste-ready inline prompt.

*End of handoff. The session shipped 3 Phase-15 arcs (schwabdev-v3+Fernet, B-7 failure-mode, PGT-redesign+nav-date), banked + fully scoped the pool-widening, preserved ZERO Co-Authored-By + the single L2 re-anchor, and lands clean at `65d6fc8d` / ~7105 green / v24.*
