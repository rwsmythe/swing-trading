# 18-H.1 — Schwab token health (configured-but-tokens-absent → YELLOW) — orchestrator return report

**To:** CHARC · **From:** orchestrator · **Date:** 2026-06-16
**Arc:** Phase 18 §18-H.1 (FIX-DIRECT, no tripwire). **Brief:** `docs/phase18-arc-h1-dispatch-brief.md` (`7c1ed8d3`).

## 1. Outcome — SHIPPED + CLOSED
Merged to `main` at **`1a2a774d`** (rebased onto the advancing main — the harness arc moved it `7c1ed8d3`→`64b498ad`; clean rebase, no overlap). Merged-head no-false-green: **8567 passed, 5 skipped** (268s), `ruff check swing/` clean. Trailers empty. Cell: implementer-sonnet-high (the overall-rollup audit stayed within the 2 flipped cases → no opus-high bump needed).

## 2. The change
`swing/monitoring/tool_health.py::_check_schwab_token` — two configured-but-tokens-absent returns flipped GREEN→YELLOW (summary `"Schwab configured but not authenticated; run swing schwab setup"`):
- `not tokens_path.exists()` (:319) → yellow, detail "no tokens DB on disk".
- `meta is None and error_message is None` (:324) → yellow, detail "tokens DB present but empty".
KEPT: `cfg is None or client_id == ""` → GREEN "Schwab not configured (n/a)" (LOCK #4, n/a-before-`cli_schwab`-import preserved). Everything else unchanged (unreadable→yellow, empty-refresh→red, expired/≤2h→red, ≤1day→yellow, valid→green). ASCII-only; read-only aggregator posture preserved.

## 3. Tests
Updated the 18-E test (green→yellow assertion); added a `client_id==""` boundary regression (still GREEN, passes both pre/post — pins the boundary); confirmed unaffected (valid→green, empty-refresh→red, cfg=None→green). **`compute_tool_health` overall-rollup audit: ZERO fixtures changed** — the aggregate tests all use `client_id==""`/`cfg=None` (short-circuit green), so the rollup assertions don't shift. Stayed within the 2 flipped cases.

## 4. Reviewer A (review-strong, repo-access, effort=HIGH — verified)
Converged **R1** (`NO_NEW_CRITICAL_MAJOR`). `reasoning effort: high` confirmed in the raw transcript header (the `da22b9d8` fix held). One MINOR fixed: a stale docstring line ("no tokens DB is green n/a") → updated (`7d1f3f6a` pre-rebase). Repo-access read the rollup + the tests. Artifacts: `.worktrees/18-H.1-schwab-token-yellow/.copowers-findings.md` + `.codex-review-r1.txt`.

## 5. Reviewer B (codex-auto-review, GATING, repo-access, effort=HIGH) — clean
Verdict verbatim: *"The change consistently reclassifies configured-but-unauthenticated Schwab token states from green/n/a to yellow, with tests covering the new behavior and the unchanged unconfigured boundary. I did not identify any introduced correctness issues in the diff."* No [P1]/major. (No WSL-CRLF phantom this time — the diff was small + Python-only.)

## 6. §5.10 BINDING operator live-witness — PASS (3/3)
Run on the worktree server (the 18-H.1 code) against the operator's live system:
1. **GREEN with valid tokens** (~6 days) — no false-yellow regression (the failure mode that mattered). ✓
2. **YELLOW after `swing schwab logout`** — confirmed at BOTH the topbar ("Tool health: yellow") AND the `schwab_token_ttl` drill-down row ("Schwab configured but not authenticated; run swing schwab setup", detail "no tokens DB on disk"); every other check green → the rollup went yellow *because* of the Schwab check. ✓
3. **GREEN after re-auth** (`swing schwab setup`) — tokens restored, row + topbar back to green. ✓
**Operational note (expected, not a bug):** the CLI `swing schwab logout` hit WinError-32 self-lock while the worktree web server held the tokens DB — the documented 18-H.4 condition (the 18-H.4.1 web fix only covers the `/schwab/setup` web path, not an external CLI process). Resolved per the standing workaround (stop server → logout → restart). No 18-H.1 implication.

## 7. Locks / notes
No schema/dep/module/process/carve-out. LOCK #4 preserved. Conventional commits, ZERO `Co-Authored-By`, no `--no-verify`. The implementer reported to the orchestrator in chat (no mailbox); this report is the orchestrator's post-QA post.
