# Phase 14 Close-Out Polish Batch -- Executing-Plans Dispatch Brief

**Audience:** Fresh Claude Code instance dispatched as the Phase 14 close-out polish-batch executing-plans implementer. No prior conversation context.

**Mission:** Execute the LOCKed close-out polish-batch plan to ship production code + tests across **5 serial slices** on already-shipped surfaces. Read-mostly UX/wiring/cosmetic + test-hardening:
- **Slice A -- A-7** Schwab web health badge: make the existing-but-dead `UNKNOWN` (`Schwab?`, warn) badge branch reachable, gated on `_is_ladder_active(cfg)` (a pure config read), instead of hiding; refine the misleading reason text. Pure web-VM change.
- **Slice B -- P14.N1** lazy candlestick thumbnails on **BOTH** dashboard tables: open-positions (reuse the journal `/journal/trades/{id}/thumbnail` route + `render_trade_window_thumbnail_svg`) AND hyp-rec (NEW render-direct `GET /hyp-recs/{ticker}/thumbnail` in `recommendations.py` calling the EXISTING `render_watchlist_thumbnail_svg`), both under a shared `_THUMBNAIL_RENDER_SEMAPHORE` extracted to `swing/web/thumbnail_render.py`. Includes the **colspan alignment** (Task B.6) across all full-row-span surfaces.
- **Slice C -- A-1** widen the market_weather OHLCV fetch window to >=200 trading bars (~300 calendar days) via a named constant `MIN_CALENDAR_DAYS_FOR_MA200 = 300` at THREE live sites (`runner.py:2763` `_bars_or_none`, `runner.py:2694` `_step_charts` [Task C.1b, own revertible commit], `dashboard.py:94`) + the JIT path (`chart_jit.py:117`).
- **Slice D -- cosmetics** A-2 (VCP label reposition) + A-4 (`_bulz_*`->`_rr_*` rename, NOT the `ticker="BULZ"` fixtures) + A-6 (process-grade-trend dark-mode CSS via `var(--accent)`).
- **Slice E -- group-(a)** FIVE items (C-1/C-2/C-3/C-5/C-19; **C-6 DEFERRED** -- it reopens a deliberate `BEGIN IMMEDIATE` TOCTOU; record the deferral, do NOT implement it).

**Read-mostly: ZERO swing-domain writes, NO `chart_renders` write on the dashboard thumbnail path; NO swing schema change (v23 held); L2-LOCK stays green (A-7 adds ZERO new `schwabdev.Client.*` call sites -- `_is_ladder_active` is a pure config read).**

**Plan (AUTHORITATIVE):** `docs/superpowers/plans/2026-06-01-phase14-close-out-polish-batch-plan.md` (1322 lines; merged `30ce9d4`; genuine single WSL Codex chain CONVERGED R3). The spec is `docs/superpowers/specs/2026-06-01-phase14-close-out-polish-batch-design.md` -- **AUTHORITATIVE EXCEPT the OQ-5 hyp-rec deferral, which the plan REVERSES** (see the plan's "SPEC SECTIONS SUPERSEDED" callout: hyp-rec thumbnails ARE in scope via `render_watchlist_thumbnail_svg`).

**Brief:** `docs/phase14-close-out-polish-batch-executing-plans-dispatch-brief.md` (this file).

**Context:** ALL Phase 14 feature sub-bundles SHIPPED (SB1-SB5.5); close-out batch brainstorm `83043ba` + writing-plans `30ce9d4` SHIPPED. **Main HEAD at this dispatch: `8561d3d`** (the orchestrator states the exact branch-from SHA in the inline prompt). This batch is the FIRST close-out-tail item; after it: B-7 (final touch) -> Sec 9.1 Q6 close-out review -> "Phase 14 CLOSED" at v23.

**Cumulative discipline:** the CLAUDE.md **Web / HTMX** + **Schwab / schwabdev** + **Windows / test-discipline** gotcha blocks are the implementation checklist; ~700+ cumulative ZERO Co-Authored-By; **Schema v23 LOCKED (NO migration)**; **L2 LOCK** (zero new `schwabdev.Client.*` call sites vs `bf7e071`; `tests/integration/test_l2_lock_source_grep.py` MUST stay green; NO re-anchor).

**Expected duration:** ~4-7 hours implementation + 1 Codex chain. Plan §G enumerates the 5 slices (A -> B -> C -> D -> E, serial). **Capture the exact fast-suite baseline at branch creation** (prior merged-main baseline was **6976**; trust `pytest -m "not slow" -q` over any estimate). SHIPS production code + tests under `swing/` + `tests/` with **ZERO new migration**.

**Skill posture:**
- Invoke `copowers:executing-plans` against this brief + the plan.
- **Codex chain count: ONE chain** (OQ-8). **Run to CONVERGENCE** (zero new criticals AND zero new majors; the ~5-round cap is suspended -- `feedback_codex_round_limit_suspended`).
- **Codex transport -- copowers v2.0.3 WSL fallback (MCP DEAD in the VS Code extension):** `wsl bash -ilc` (INTERACTIVE login) OR prefix `export PATH="$HOME/.local/node22/bin:$PATH"`; **VERIFY `command -v codex` -> `/home/<wsluser>/.local/node22/bin/codex`** (NOT the `/mnt/c/.../npm/codex` shim) before the chain. `codex exec -s read-only --skip-git-repo-check -C /mnt/c/.../<worktree> - < <prompt>` (R1) / `codex exec resume --last -c sandbox_mode="read-only" --skip-git-repo-check -` (R2+; `resume` REJECTS `-s`/`-C`). Pre-generate the diff on Windows; tell Codex NOT to run git. **PERSIST each round's PROMPT AND RESPONSE to `.copowers-findings.md`** (v2.0.3 by construction; the final `### Verdict` must be readable on disk for orchestrator QA). Memory `feedback_wsl_native_codex_invocation` + `feedback_implementer_persist_codex_responses`.
- Output: production code + tests + return report at `docs/phase14-close-out-polish-batch-executing-plans-return-report.md`.

---

## §0 Read first (in this order)
1. **THIS BRIEF end-to-end.**
2. **The PLAN** (1322 lines) -- AUTHORITATIVE. The "SPEC SECTIONS SUPERSEDED" callout (hyp-rec REVERSED); §B file map (re-grep EVERY anchor at STEP 0 per #2); §E LOCK reverification + the 3 re-grep findings (E.4 `runner.py:2694`, E.5 BULZ ticker, E.6 C-6 deferral, E.7 L2-LOCK); §G the 5 slices (step-checkbox TDD); §H test surface; §I the operator gate (S1-S8); §K schema (NO change); §L fixtures (esp. the A-7 UNSEEDED fixture); §M forward-binding lessons.
3. **CLAUDE.md** -- the **Web / HTMX** block (the HTMX `<tr>`-at-fragment-root synthetic-table-wrap hazard; `hx-headers HX-Request`; the shared-`base.html.j2` VM-field-default gotcha; matplotlib mathtext `$`/`^`/`_`) + the **Windows/test-discipline** block (the `USERPROFILE`+`HOME` monkeypatch for the A-7 UNSEEDED fixture; #15 production-path tests; cp1252 ASCII; TestClient lifespan) + the **Schwab** block (the `setLogRecordFactory` redaction; `_is_ladder_active` semantics). + `docs/orchestrator-context.md` §"Pre-Codex review + brief-authoring disciplines" (#2, #10a, #15).
4. **The spec** (reference for the A-7 design-vs-wiring rationale §4 + the gate §10).
5. **Memory:** `feedback_seeded_gate_masks_default_state` (the A-7 S7 gate witnesses the UNSEEDED state), WSL Codex transport + persist-responses + round-limit-suspended + trailer-hazard + visual-gate + `feedback_no_false_green_claim` + `feedback_taskstop_does_not_kill_detached_server` (the badge/thumbnail browser gate server).

---

## §1 Slices (plan §G is AUTHORITATIVE; serial; A first; cascade-audit + `%(trailers)` `[]` after each)
- **Slice A -- A-7 badge:** `swing/web/view_models/schwab_checker_badge.py:30-49` -- replace the early `if data is None: return None` with: keep `if cfg is None: return None`; add `if not _is_ladder_active(cfg): return None`; else route `data` (which is `None` when no sidecar) through `evaluate_liveness_state` -> `_BADGE_MAP` (the UNKNOWN branch already exists). Refine the no-sidecar-but-expected `title` (the hardcoded "web server not running" is now misleading; ASCII only, L7). VM test + a TestClient topbar-render test (the bug is the `base.html.j2:81` `{% if %}` guard -- a VM-only test misses it; #15).
- **Slice B -- P14.N1 dual-table lazy thumbnails:** extract `swing/web/thumbnail_render.py` (the shared `_THUMBNAIL_RENDER_SEMAPHORE`/timeout/cache-control from `routes/journal.py:34-39`; journal imports them back -- behavior-identical). Open-positions: add the lazy `<td>` to `open_positions_row.html.j2` (reuse the journal route) + `<th>Chart</th>` + colspan 10->11. Hyp-rec: NEW render-direct `GET /hyp-recs/{ticker}/thumbnail` in `recommendations.py` (a `_TICKER_RE` pre-fetch guard; calls `render_watchlist_thumbnail_svg`; render-direct -- NO `chart_renders` write) + `hyprec_thumbnail.html.j2` (fragment root `<svg>`/`<span>`, NEVER bare `<tr>`) + the lazy `<td>` (`<tr>` stays trigger-free) + `<th>Chart</th>` + colspan 9->10. **Task B.6 (load-bearing): colspan alignment** -- `_row_error_colspan` (`app.py`) hyp-rec 9->10 + open-position 10->11, the hyp-rec expanded/unavailable/entry-form/soft-warn full-row colspans (hyp-rec-origin branches only; leave the `else 8`), + a column-count regression for BOTH tables. (Pre-existing exit/stop-form under-spans are OUT of scope -- documented.)
- **Slice C -- A-1 fetch window:** add `MIN_CALENDAR_DAYS_FOR_MA200 = 300` (`ohlcv_cache.py`); apply at `runner.py:2763` (`_bars_or_none`), `runner.py:2694` (`_step_charts` -- **Task C.1b, own revertible commit**), `dashboard.py:94` (pass it explicitly; today inherits the 180 default), `chart_jit.py:117` (OQ-6). Regression: spy `render_market_weather_svg`, assert `len(bars) >= 200` at the call boundary (production path; not just `window_days` capture).
- **Slice D -- cosmetics:** A-2 reposition the `_annotate_vcp` contraction labels off the price-tick column (`charts.py:836-841`; no mathtext metachars). A-4 rename `_bulz_target_price`->`_rr_target_price` + `_draw_bulz_zones`->`_draw_risk_reward_zones` + comments/WARN text + test names; **DO NOT rename `ticker="BULZ"` fixtures** (real ETF symbol). A-6 add `.process-grade-rolling-line { stroke: var(--accent); }` + `.process-grade-marker { fill: var(--accent); }` to `app.css` (the polyline currently has NO `stroke=` -> invisible in BOTH themes; the circle no `fill=` -> dark-mode-invisible).
- **Slice E -- group-(a) (FIVE items):** C-1 (`trades.py:2153` provisional default `True`->`False`), C-2 (daily-management tooltip wording), C-3 (backfill CLI artifact-write `OSError`->`click.ClickException`), C-5 (strengthen the `BEGIN IMMEDIATE` ordering regression test), C-19 (`tests/research/test_pattern_cohort_evaluator_reader.py::test_ohlcv_reader_re_export_identity` xdist flake -> `@pytest.mark.xdist_group`). **C-6 DEFERRED** (record in Task E.0; do NOT implement -- it reopens the backfill `BEGIN IMMEDIATE` TOCTOU).

---

## §2 LOCKs (BINDING; plan §E + the OQ dispositions)
- **L1** scope = the 5 slices ONLY (NO B-7, NO close-out review, NO Phase 15+, NO A-5). **L2** NO swing schema change (v23; `EXPECTED_SCHEMA_VERSION = 23` unchanged; assert no `0024_*.sql`). **L3 (L2-LOCK)** A-7 ZERO new `schwabdev.Client.*` call sites (`_is_ladder_active` is a pure config read); `test_l2_lock_source_grep.py` (baseline `bf7e071`) stays green; NO re-anchor. **L4** REUSE not re-implement (both existing renderers; the `thumbnail_render.py` extraction is DRY, not a new renderer; A-6 reuses `var(--accent)`; A-7 reuses `evaluate_liveness_state` + `_BADGE_MAP`). **L5** read-mostly (ZERO swing-domain writes; NO `chart_renders` write on the dashboard thumbnail path -- assert it). **L6** production-path tests (#15) -- real route/VM/cache/topbar render, NOT stubs (only the external OHLCV-fetch boundary is stubbed). **L7** ASCII (the A-7 reason text + C-2/C-3 CLI text); the `setLogRecordFactory` redaction unchanged.
- **OQ recap:** OQ-1 (A-7 `_is_ladder_active` gate + reason-text) · OQ-2 (one slice, no split) · OQ-3 (one bundle, 5 serial slices) · OQ-4 (5 group-(a); C-6 deferred) · **OQ-5 (BOTH tables; hyp-rec via `render_watchlist_thumbnail_svg`)** · OQ-6 (JIT widened) · OQ-7 (A-6 CSS) · OQ-8 (single chain).

## §3 Production anchors (re-grep at STEP 0 per #2; the plan §B embeds these)
A-7: `build_schwab_checker_badge:30`, `_BADGE_MAP["UNKNOWN"]:26`, `_is_ladder_active` (`marketdata_ladder.py:221`), `evaluate_liveness_state(None)->UNKNOWN` (`checker_resilience.py:228-229`), `base.html.j2:81-84`. P14.N1: `render_trade_window_thumbnail_svg` (`trade_charts.py:88`), the journal route (`journal.py:80`) + semaphore (`:34`), `render_watchlist_thumbnail_svg` (`charts.py:514`), the `/hyp-recs/` routes in `recommendations.py`, the colspan surfaces (`open_positions_expanded.html.j2:27` =10, `hypothesis_recommendations_expanded.html.j2:10` =9, `_row_error_colspan` `app.py:52`). A-1: `get_or_fetch(*, ticker, window_days=180)` (`ohlcv_cache.py:131`), `runner.py:2763/2694/2884`, `dashboard.py:94`, `chart_jit.py:117`, `render_market_weather_svg` (`charts.py:777`). A-4: `_bulz_target_price` (`charts.py:632`), `_draw_bulz_zones` (`:725`). A-6: `process_grade_trend.html.j2:39-60`, `--accent` in `app.css`. Group-(a): plan §B + spec §5.

## §4 Codex SINGLE chain (OQ-8; run to convergence; persist prompt+response)
**Watch items:** NO schema (all 5 slices); L2 green (A-7 zero new `schwabdev.Client.*` sites); the A-7 UNKNOWN-gate semantics + reason-text + the UNSEEDED-witness test (production+ladder+isolated HOME+no constructible client -- the regression the SB5.5 seeded gate missed); P14.N1 dual-renderer LAZY delivery (the HTMX `<tr>`-at-fragment-root hazard; `hx-headers HX-Request`; the BOTH-tables column-count regression; read-only reuse of the watchlist renderer; the `thumbnail_render.py` extraction is behavior-identical); A-1 calendar-vs-trading-bar arithmetic + the `len(bars)>=200` production-path regression; A-2 mathtext-free labels; A-4 rename completeness (every `_bulz`/`bulz` feature token; NOT the ticker fixtures; behavior-neutral); A-6 CSS resolves in BOTH themes; group-(a) the `USERPROFILE`+`HOME` monkeypatch on the A-7 fixture; C-6 stays deferred; ASCII; production-path tests (L6). **If a finding needs a schema change or a new `schwabdev.Client.*` call site, STOP + escalate.**

## §5 Operator-witnessed gate (plan §I; BINDING; UNSEEDED/default-state witness)
- **S1** fast suite + ruff green (baseline `6976` + the new tests -- READ the actual numbers; no false-green; the lone `test_ohlcv_reader_re_export_identity` xdist flake passes in isolation -- C-19 hardens it). **S2** schema unchanged (v23; no `0024`; assert no new `chart_renders`/domain writes). **S3** L2 source-grep green (A-7 zero new `schwabdev.Client.` sites).
- **S4 (P14.N1, BINDING browser)** open-positions AND hyp-rec table thumbnails render with real rows present; no-coverage rows degrade cleanly; both tables' column counts align.
- **S5 (A-1)** the `>=200`-bar regression + operator confirms the market-weather 200-MA renders (full line).
- **S6 (A-2/A-4/A-6, BINDING browser)** VCP labels uncrowded; rename behavior-neutral (tests green); the process-grade-trend chart visible in DARK mode.
- **S7 (A-7, BINDING browser -- THE default-state witness)** under the UNSEEDED precondition (production + ladder enabled, NO sidecar, no constructible client): the topbar shows the `Schwab?` UNKNOWN (warn) badge instead of nothing. (The operator's currently-degraded production tokens reproduce this NATURALLY.)
- **S8** trailers `[]`; ZERO Co-Authored-By.
- **If a `swing web` server is launched for S4/S6/S7:** run the BRANCH server from the worktree (`python -m swing.cli web --port 8081`); kill by PID via `Get-NetTCPConnection -LocalPort 8081` -> `Stop-Process -Force` + verify the port is free (`feedback_taskstop_does_not_kill_detached_server`). **After merge: re-run the fast suite ON THE MERGED HEAD + READ it** (`feedback_no_false_green_claim`); reinstall `swing` from main (`pip install -e . --no-deps`).

## §6 Done criteria
All 5 slices shipped (Slice E = 5 items; C-6 recorded-deferred); Codex single chain CONVERGED (`.copowers-findings.md` evidences it); fast suite green (baseline + new; READ it); ruff clean; ZERO Co-Authored-By (`%(trailers)`); NO migration (v23) + ZERO new `schwabdev.Client.*` call sites (L2 green) + ZERO `chart_renders`/domain writes on the thumbnail path (L5); the colspan alignment regression green on BOTH tables; return report complete; branch pushed; ready for orchestrator QA + the operator S1-S8 gate.

## §7 Return report shape
Final HEAD + per-commit Codex-round attribution; the single chain + convergent shape (cite `.copowers-findings.md` incl. the final `### Verdict`); per-slice completion (incl. the C-6 deferral record + the `runner.py:2694` C.1b commit); test surface (baseline `6976` + actual new count); LOCK + OQ verbatim verification; Codex Majors accepted (ZERO preferred); production-anchor re-grep results; schema verdict (NO change); the L2 verification (source-grep green); the P14.N1 dual-table column-count + read-only verification; the A-7 UNSEEDED-test result; the gate readiness (S1-S8 incl. the browser legs); cumulative gotcha application; worktree teardown status; ZERO Co-Authored-By confirmation; CLAUDE.md status-line refresh draft; operator-gate handback.

## §8 OUT OF SCOPE
B-7 (the NEXT cycle -- may add a v24 column); the Phase 14 close-out review; A-5 (CLOSED); C-6 (deferred -- the backfill TOCTOU); any new schema (v24); any new `schwabdev.Client.*` call site / new Schwab feature / the Phase-15 v3 upgrade; a new chart renderer (reuse the two existing); the v22/v23 substrate; Phase 15+.

## §9 Dispatch metadata
- **Subagent type:** `general-purpose`. **Foreground.** **Model:** harness default.
- **Worktree:** YES -- branch `phase14-close-out-polish-batch-executing-plans`. Dir `.worktrees/phase14-close-out-polish-batch-executing-plans/`. **Branch from main HEAD `8561d3d`** (or later if the orchestrator states a newer HEAD in the inline prompt).
- **CLI in worktree:** `python -m swing.cli` (NOT bare `swing`).
- **Codex chain count:** ONE chain to convergence via the WSL fallback (verify `command -v codex`; transcript -> `.copowers-findings.md`).
- **Schema:** NO change (v23 held); read-mostly.

---

*End of brief. Phase 14 close-out polish-batch executing-plans dispatch -- execute the LOCKed 1322-line plan across 5 serial slices: A-7 (render the UNKNOWN Schwab badge gated on `_is_ladder_active` instead of hiding; reuse `evaluate_liveness_state` + `_BADGE_MAP`; reason-text) · P14.N1 (BOTH dashboard tables -- open-positions via the journal route + `render_trade_window_thumbnail_svg`, hyp-rec via a NEW render-direct route + the EXISTING `render_watchlist_thumbnail_svg`; shared `thumbnail_render.py` cap; both LAZY; the Task B.6 colspan alignment) · A-1 (>=200-trading-bar fetch window at THREE sites + JIT; named ~300-day constant) · cosmetics A-2/A-4/A-6 · group-(a) FIVE items (C-6 deferred). NO schema (v23 held); L2-LOCK green (A-7 zero new schwabdev.Client.* sites); reuse not re-implement; read-mostly. ONE Codex chain to convergence (persist prompt+response). The gate is BINDING browser legs (S4 both thumbnail tables, S6 cosmetics, S7 the A-7 UNSEEDED Schwab? badge) + S1-S3/S8 mechanical; after merge re-run the suite on the merged HEAD + reinstall swing. OUTPUT: production code + tests + return report.*
