# Phase 14 Close-Out FOLLOW-ON Bundle -- Executing-Plans Dispatch Brief

**Audience:** Fresh Claude Code instance dispatched as the Phase 14 close-out follow-on-bundle executing-plans implementer. No prior conversation context.

**Mission:** Execute the LOCKed plan to ship production code + tests across **4 serial slices** (F-1 first) -- corrections to the close-out polish batch (SHIPPED `f2cd376`):
- **F-1 -- P14.N7 web-checker liveness in normal operation.** The A-7 badge shows `Schwab?` UNKNOWN under HEALTHY tokens. **Root cause PINNED + orchestrator-verified on disk:** `web_cmd` ([cli.py:3324](swing/cli.py)) -> `run_server(cfg=ctx.obj["config"])` passes the main group's RAW cfg ([cli.py:185](swing/cli.py), no group-level `apply_overrides`) WITHOUT applying overrides, so the web cfg lacks the user-config-only Schwab CLIENT_ID/SECRET -> `_construct_web_schwab_client` returns None -> no checker -> no sidecar (the badge stays VISIBLE because `marketdata_ladder_enabled` is TRACKED, so `_is_ladder_active` is True). **Ship the §3.3 startup diagnostic FIRST** (confirms Class A vs B at the gate without pre-assuming), then the **Class-A fix** (`apply_overrides` on the web path -- NO new `schwabdev.Client.*` site) + the **Class-B fix** (install-anchored STARTING-write readback-verify + WARNING). Validate with an **UNSEEDED real-token gate**.
- **F-2 -- market-weather trend "undefined".** `current_stage` reads PERSISTED criteria for a benchmark not in the evaluated set. Compute the regime LIVE from a >=250-bar fetch via a **two-tier shared helper** (`structural_checks` per-check + `structural_stage` wrapper; `evaluate()` refactors to REUSE `structural_checks` with a **MANDATORY byte-identical** full-tuple `Result` golden across 4 fixtures), decoupling compute-window from display-window (frame-anchored slice).
- **F-3 -- segmented rolling-line polylines.** Multiple `<polyline>` (one per contiguous non-None run; drop 1-point segments); migrate the 4 existing `svg_polyline_points` test sites before the grep gate.
- **F-4 -- hyp-rec thumbnail spine borders.** Hide the matplotlib axes spines in `render_watchlist_thumbnail_svg`.

**Read-mostly; NO swing schema change (v23 held); L2-LOCK stays green (F-1 ZERO new `schwabdev.Client.*` call sites -- the Class-A fix plumbs cfg; construction stays in `auth.py:construct_authenticated_client`). Preserve the close-out A-7 badge fix.**

**Plan (AUTHORITATIVE):** `docs/superpowers/plans/2026-06-02-phase14-close-out-follow-on-bundle-plan.md` (1768 lines, §A-§O; merged `48fcbfe`; genuine single WSL Codex chain CONVERGED R3). The spec is `docs/superpowers/specs/2026-06-02-phase14-close-out-follow-on-bundle-design.md`.

**Brief:** `docs/phase14-close-out-follow-on-bundle-executing-plans-dispatch-brief.md` (this file).

**Context:** ALL Phase 14 feature sub-bundles + the close-out polish batch SHIPPED; the follow-on brainstorm `6836aa5` + writing-plans `48fcbfe` SHIPPED. **Main HEAD at this dispatch: `b1c74f3`.** After this bundle: B-7 (final touch) -> Sec 9.1 Q6 close-out review -> "Phase 14 CLOSED" at v23.

**Cumulative discipline:** the CLAUDE.md **Schwab/schwabdev** + **Web/HTMX** + **yfinance** + **Windows/test-discipline** gotcha blocks are the checklist; ~700+ ZERO Co-Authored-By; **Schema v23 LOCKED (NO migration)**; **L2 LOCK** (`tests/integration/test_l2_lock_source_grep.py` stays green; baseline `bf7e071`; F-1 adds NO new `schwabdev.Client.*` sites -- grep count stays 3, all comments/docstrings).

**Expected duration:** ~4-7 hours implementation + 1 Codex chain. Plan §G enumerates the 4 slices (F-1 -> F-2 -> F-3 -> F-4); **~30 new tests** projected (trust `pytest` over the estimate; **capture the exact baseline at branch creation** -- prior merged-main baseline was **7005**). SHIPS production code + tests under `swing/` + `tests/` with ZERO new migration.

**Skill posture:**
- Invoke `copowers:executing-plans` against this brief + the plan.
- **Codex chain count: ONE chain**, **run to CONVERGENCE** (`NO_NEW_CRITICAL_MAJOR`; the ~5-round cap is suspended -- `feedback_codex_round_limit_suspended`). **Persist each round's prompt AND the FULL response (incl. the literal `### Verdict` line) to `.copowers-findings.md`** -- the writing-plans chain's R3 verdict string was not captured; do NOT repeat that (the orchestrator reads the verdict on disk at QA).
- **Codex transport -- copowers v2.0.3 WSL fallback (MCP DEAD in the VS Code extension):** `wsl bash -ilc` OR `export PATH="$HOME/.local/node22/bin:$PATH"`; **VERIFY `command -v codex` -> `/home/<wsluser>/.local/node22/bin/codex`** before the chain. `codex exec -s read-only --skip-git-repo-check -C /mnt/c/.../<worktree> - < <prompt>` (R1) / `codex exec resume --last -c sandbox_mode="read-only" --skip-git-repo-check -` (R2+; `resume` REJECTS `-s`/`-C`). Pre-generate the diff on Windows; tell Codex NOT to run git. See `feedback_wsl_native_codex_invocation` + `feedback_implementer_persist_codex_responses`.
- **Worktree-cwd corollary (`feedback_degraded_harness_sequential_tool_calls`):** prefix EVERY git/test command with an explicit `cd <worktree> &&` (or `git -C <worktree>`); re-verify `git branch --show-current` + `git rev-parse --short HEAD` BEFORE every commit (the foreground cwd can silently revert to the primary repo on main; every `run_in_background` shell starts in the primary repo).
- Output: production code + tests + return report at `docs/phase14-close-out-follow-on-bundle-executing-plans-return-report.md`.

---

## §0 Read first (in this order)
1. **THIS BRIEF end-to-end.**
2. **The PLAN** (1768 lines) -- AUTHORITATIVE. §B file map (re-grep every anchor at STEP 0 per #2); §E LOCK reverification; §G the 4 slices (step-checkbox TDD -- **F-1 = the §3.3 startup diagnostic FIRST, then the Class-A `apply_overrides` fix + the Class-B install-anchored readback-verify write, then the UNSEEDED real-token test**; F-2 = the two-tier helper + the byte-identical `evaluate()` golden + the live-compute + the frame-anchored display slice + the `MIN_CALENDAR_DAYS_FOR_TREND_TEMPLATE` constant + the mandatory route/pipeline call-site tests; F-3 = the existing-test migration BEFORE the grep gate; F-4); §H test surface; §I the operator gate (S1-S7); §K schema (NO change); §L fixtures; §M forward-binding lessons.
3. **CLAUDE.md** -- the **Schwab/schwabdev** block (esp. `update_tokens()` does NOT raise on auth failure; the `"Schwabdev"` `setLogRecordFactory` redaction; the CLIENT_ID/SECRET resolution cascade env>cfg>prompt + `SchwabConfigMissingError`; the `apply_overrides` discipline at Schwab entry points) + the **Windows/test-discipline** block (`USERPROFILE`+`HOME` monkeypatch; #15 production-path tests; cp1252 ASCII; TestClient lifespan) + the **yfinance** "return the FULL archive; consumers slice" (F-2). AND `docs/orchestrator-context.md` §"Pre-Codex review + brief-authoring disciplines" (#2 anchor re-grep, #15 production-path).
4. **The spec** (reference for the F-1 Class A/B framing + the F-2 two-tier design).
5. **Memory:** `feedback_seeded_gate_masks_default_state` (the F-1 gate witnesses the UNSEEDED real-token state -- do NOT hand-seed a sidecar), the WSL Codex transport + persist-responses + round-limit-suspended + trailer-hazard + visual-gate + `feedback_no_false_green_claim` + `feedback_taskstop_does_not_kill_detached_server` entries.

---

## §1 Slices (plan §G is AUTHORITATIVE; serial; cascade-audit + `%(trailers)` `[]` after each)
- **F-1 -- P14.N7 web checker:** ship the §3.3 startup diagnostic FIRST (a one-shot log of which class fired: client constructed? sidecar written? -- so Class A vs B is confirmed at the gate, NOT pre-assumed). Then the Class-A fix (`apply_overrides` on the web path so `_construct_web_schwab_client` gets the user-config creds -- NO new `schwabdev.Client.*` site; construction stays in `auth.py:construct_authenticated_client`) + the Class-B fix (install-anchored STARTING-write readback-verify + WARNING). **The F-1 test exercises the REAL `create_app` -> `_install_web_marketdata_caches` seed path with a fake-client monkeypatch -- NOT a hand-written sidecar** + a parameterized construction-path test for the Class-A creds fix + the `test_create_app_startup_with_overrides` regression. Preserve the A-7 badge contract + the redaction. **`apply_overrides` reaches `create_app`'s risk-policy divergence hook -- a startup regression pins tolerance (Codex R1-M1).**
- **F-2 -- market-weather trend live-compute:** the two-tier `structural_checks`/`structural_stage` helper; `evaluate()` refactors to REUSE `structural_checks` with a **MANDATORY byte-identical full-tuple `Result` golden** (4 fixtures: all-pass / fail / TT3-NA at 200-220 bars / <200; exact frozen-dataclass equality). The live-compute from a >=250-bar fetch (`MIN_CALENDAR_DAYS_FOR_TREND_TEMPLATE`); the **frame-anchored display slice** (anchor on the frame's own last bar, cache-lag-safe -- NOT `now()`); the TT1-TT5-only `stage_2` semantic guardrail (the OQ-3a divergence from `current_stage`'s 8/8). **Mandatory** route + pipeline call-site tests (the refresh route returns 204 + HX-Redirect -- spy `render_market_weather_svg` + assert 204; the pipeline spy targets `swing.pipeline.runner.structural_stage` imported at module load).
- **F-3 -- segmented polylines:** multiple `<polyline>` (one per contiguous non-None run; drop 1-point segments); **migrate the 4 existing `svg_polyline_points` test sites** (`tests/web/test_view_models/test_process_grade_trend_vm.py:136/193/214/278`) BEFORE the grep gate; do NOT regress the close-out A-6 dark-mode CSS.
- **F-4 -- thumbnail spines:** hide the matplotlib axes spines in `render_watchlist_thumbnail_svg`; re-check the watchlist surface at the gate.

## §2 LOCKs (BINDING; plan §E + the OQ dispositions)
- **L1** scope = F-1 + F-2 + F-3 + F-4 ONLY (NO B-7, NO close-out review, NO Phase 15). **L2** NO swing schema change (v23; `EXPECTED_SCHEMA_VERSION = 23`; assert no `0024_*.sql`; the liveness stays the ephemeral sidecar). **L3 (L2-LOCK)** F-1 ZERO new `schwabdev.Client.*` call sites (grep count stays 3, all comments; `test_l2_lock_source_grep.py` baseline `bf7e071` green); the `"Schwabdev"` `setLogRecordFactory` redaction intact. **L4** REUSE not re-implement (the seed/sidecar/checker path; the TT1-TT5 logic via the two-tier helper; `_polyline_x`/`_polyline_y`; the existing renderer). **L5** read-mostly (ZERO swing-domain writes; the sidecar is ephemeral; render-direct; SELECT/compute-only). **L6** production-path tests (#15) -- F-1 fake-client monkeypatch on the REAL install/seed path (NOT hand-seeded); F-2 mandatory route + pipeline call-site tests. **L7** ASCII; the close-out A-7 badge fix preserved.
- **OQ recap:** OQ-1 diagnostic-then-fix · OQ-2 daemon heartbeats (resolved) · OQ-3a two-tier helper + byte-identical `evaluate()` · OQ-4 keep (50,200) · OQ-5 multiple `<polyline>` + drop 1-point · OQ-6 one bundle/4 slices · OQ-7 single chain · OQ-8 Class A/B pinned by the §3.3 diagnostic · OQ-9 confirm SPY has no persisted criteria (DB check at the gate).

## §3 Production anchors (re-grep at STEP 0 per #2; the plan §B embeds these)
F-1: `web_cmd` (`cli.py:3324`) -> `run_server(ctx.obj["config"])`; the main group (`cli.py:185`, no group-level `apply_overrides`); `apply_overrides` (`config_overrides.py`); `_install_web_marketdata_caches` (`app.py:251`, seed `:274`) / `_construct_web_schwab_client` (`app.py:148`); `checker_resilience.py:130/135` (`record_tick` before `original()`); `construct_authenticated_client` (`auth.py`); the A-7 badge VM (`schwab_checker_badge.py`); `test_l2_lock_source_grep.py` (`bf7e071`). F-2: `current_stage` (`foundation.py:745`); `trend_template.evaluate` (`:22`, `<200` at `:24`, TT3 at `:58-71`); the market-weather sites (`runner.py:2890-2931`, `dashboard.py:93-146`); `MIN_CALENDAR_DAYS_FOR_MA200=300` + `get_or_fetch` slice (`ohlcv_cache.py:259-261`); `rising_ma_period_days=21`; `render_market_weather_svg` (`charts.py:777`). F-3: `_format_polyline_points` (`process_grade_trend.py:262-301`) + the template (`:52-62`) + the 4 test sites. F-4: `render_watchlist_thumbnail_svg` (`charts.py:514-552`).

## §4 Codex SINGLE chain (run to convergence; persist prompt AND the full response incl. `### Verdict`)
**Watch items:** NO schema (all 4 slices); L2 green (F-1 zero new `schwabdev.Client.*` sites); the F-1 diagnostic-then-fix covers BOTH classes without pre-assuming + the test exercises the REAL install/seed path (NOT a hand-seeded sidecar); the F-2 `evaluate()` byte-identical golden (8-criterion output unchanged) + the >=250-bar live-compute + the frame-anchored slice + the mandatory route/pipeline call-site tests; F-3 multiple-`<polyline>` + drop-1-point + the existing-test migration; F-4 spines + the watchlist re-check; the A-7 badge fix preserved; the redaction intact; ASCII; trailer-parse (final `-m` paragraph plain prose). **If a finding needs a schema change or a new `schwabdev.Client.*` call site, STOP + escalate.**

## §5 Operator-witnessed gate (plan §I; BINDING; UNSEEDED real-token for F-1)
- **S1** suite + ruff green (READ the actual numbers; baseline 7005; no false-green; the `test_ohlcv_reader_re_export_identity` xdist flake passes in isolation). **S2** schema v23 / no migration / no new domain writes. **S3** L2 source-grep green (F-1 grep count stays 3). **S4 (F-1, BINDING, UNSEEDED real-token)** delete any existing sidecar -> operator runs `swing web` with HEALTHY production tokens (NO hand-seeded sidecar) -> the sidecar FILE appears + the badge shows STARTING->ALIVE (the §3.3 diagnostic logs which class fired + the fix resolved it). **S5 (F-2, browser)** the market-weather trend is DEFINED (not "undefined") via the live compute. **S6 (F-3/F-4, browser)** the process-grade-trend lines render gaps-as-gaps; the hyp-rec thumbnails have NO spine borders (+ the watchlist still looks right). **S7** trailers `[]`.
- **If a `swing web` server is launched for S4-S6:** orchestrator-run the BRANCH server (`python -m swing.cli web --port 8081`); kill by PID (`Get-NetTCPConnection -LocalPort 8081` -> `Stop-Process -Force`; verify free) per `feedback_taskstop_does_not_kill_detached_server`. **After merge re-run the suite on the MERGED head + READ it** (`feedback_no_false_green_claim`); reinstall `swing` from main (`pip install -e . --no-deps`).

## §6 Done criteria
All 4 slices shipped (F-1 = diagnostic + both class fixes; F-2 = two-tier helper + byte-identical golden + call-site tests); Codex single chain CONVERGED (`.copowers-findings.md` evidences it incl. the literal final `### Verdict`); fast suite green (baseline + new; READ it); ruff clean; ZERO Co-Authored-By (`%(trailers)`); NO migration (v23) + ZERO new `schwabdev.Client.*` call sites (L2 green) + ZERO swing-domain writes (L5); the close-out A-7 badge fix preserved; return report complete; branch pushed; ready for orchestrator QA + the operator S1-S7 gate.

## §7 Return report shape
Final HEAD + per-commit Codex-round attribution; the single chain + convergent shape (cite `.copowers-findings.md` incl. the literal final `### Verdict`); per-slice completion; test surface (baseline 7005 + actual new count); LOCK + OQ verbatim verification; Codex Majors accepted (ZERO preferred); production-anchor re-grep results; schema verdict (NO change); the L2 verification (grep count 3); **the §3.3 diagnostic outcome (which class -- A or B -- fired at the gate) + how the fix resolved it**; the F-2 byte-identical golden result + the live-compute; the gate readiness (S1-S7; UNSEEDED real-token for F-1); cumulative gotcha application; worktree teardown status; ZERO Co-Authored-By; CLAUDE.md status-line refresh draft; operator-gate handback.

## §8 OUT OF SCOPE
B-7 (the NEXT cycle); the Phase 14 close-out review; the schwabdev v3 upgrade (Phase 15 `#9`); any new schema (v24); any new `schwabdev.Client.*` call site / new Schwab feature; a persisted liveness table (keep the ephemeral sidecar); the v22/v23 substrate.

## §9 Dispatch metadata
- **Subagent type:** `general-purpose`. **Foreground.** **Model:** harness default.
- **Worktree:** YES -- branch `phase14-close-out-follow-on-bundle-executing-plans`. Dir `.worktrees/phase14-close-out-follow-on-bundle-executing-plans/`. **Branch from main HEAD `b1c74f3`** (or later if the orchestrator states a newer HEAD in the inline prompt).
- **CLI in worktree:** `python -m swing.cli` (NOT bare `swing`). Prefix git/test commands with `cd <worktree> &&`; re-verify the branch before each commit.
- **Codex chain count:** ONE chain to convergence via the WSL fallback (verify `command -v codex`; transcript -> `.copowers-findings.md`, incl. the literal final `### Verdict`).

---

*End of brief. Phase 14 close-out FOLLOW-ON bundle executing-plans dispatch -- execute the LOCKed 1768-line plan across 4 serial slices: F-1 (P14.N7 web checker; ship the §3.3 startup diagnostic FIRST to confirm Class A [web_cmd never applies overrides -> apply_overrides fix, zero new schwabdev.Client.* sites] vs Class B [silent write-fail -> install-anchored STARTING readback-verify], then validate with an UNSEEDED real-token gate using the operator's HEALTHY tokens) + F-2 (market-weather trend live-compute via a two-tier structural_checks/structural_stage helper from a >=250-bar fetch, frame-anchored display slice, with a MANDATORY byte-identical evaluate() golden + mandatory route/pipeline call-site tests) + F-3 (segmented rolling-line polylines + migrate the 4 existing test sites before the grep gate) + F-4 (hide the hyp-rec thumbnail axes spines). NO schema (v23 held); L2-LOCK green (F-1 zero new schwabdev.Client.* sites); reuse not re-implement; read-mostly. ONE Codex chain to convergence (persist prompt AND the full response incl. the literal final Verdict). The gate is BINDING browser legs (S4 the F-1 UNSEEDED real-token STARTING->ALIVE; S5 F-2 trend defined; S6 F-3/F-4) + S1-S3/S7 mechanical; after merge re-run the suite on the merged HEAD + reinstall swing. OUTPUT: production code + tests + return report.*
