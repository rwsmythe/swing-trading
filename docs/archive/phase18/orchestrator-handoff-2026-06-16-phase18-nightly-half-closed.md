# Orchestrator Handoff — 2026-06-16 (Phase 18 mid; 18-D monitor + FIX-1 + 18-B.1 + the NIGHTLY HALF all SHIPPED+CLOSED)

**Audience:** a fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). **Read [`docs/orchestrator-context.md`](orchestrator-context.md) end-to-end first** (durable role framing), then this doc (the delta since the 2026-06-15 Phase-18-EF handoff). Bootstrap via `scripts/orchestrator_bootstrap.md`.

## 1. Clean boundary (verify: `git log --oneline -10` + `git status -sb` + `git worktree list`)
- main HEAD **`e8a4063b`** (housekeeping; code at `4d17492b`). **origin is BEHIND by ~8 — NOT pushed** (operator cadence; push only when the operator asks). **Single `main` worktree** (all this-gen worktrees merged ff + removed).
- **~8550 fast green** on `main` (re-confirmed on the merged head at the nightly-half close). **Schema v30** (unchanged since 18-C). Ruff clean. **ZERO `Co-Authored-By`** holds.
- Untracked drift: the `exports/research/shadow-expectancy-*` dirs + `exports/research/health/latest.json` (research artifacts from the pipeline runs / the merged-head verification — not the orchestrator's lane).
- Merge discipline unchanged: rebase the branch onto `main` → `merge --ff-only` → re-run the suite ON the merged head (**READ the actual summary line — `| tail` masks pytest's exit code; that read caught a cross-arc regression this gen**).

## 2. What this generation shipped (all SHIPPED + CLOSED, gate-passed)
- **18-D research data-collection-health monitor (script-first) + FIX-1** (`2f13f0f5`): `compute_research_health` + the 7 read-only checks (§3 envelope) + `scripts/research_health.py` writing `latest.json` (lights 18-F's research stoplight). FIX-1 baselined check #1 on `date(2026-06-13)`. **The review treadmill (R1-R15) was bounded by the operator's #39 schema-boundary corollary** (see §3).
- **18-B.1 `insert_observation` OHLC write-barrier** (`8022d45d`): fail-loud before the INSERT; CHARC-authorized `swing/data` carve-out (guard-only, **NO `__post_init__`** — relocated off it to preserve reads of the 103); the read-preservation structural-lock test. **The merged-head no-false-green suite caught a CROSS-ARC test-seeding regression** (18-D monitor tests seeded non-finite via the now-barriered `insert_observation`) → fixed `e7c61f98` (raw insert). Banked as a CLAUDE.md gotcha.
- **18-D NIGHTLY HALF + 2 calibrations** (`4d17492b`): the read-only `research_health` pipeline STEP after `_step_shadow_expectancy` via the **bare** `step_guard(lease, "research_health", logger=log)` (the **O1 ruling**, `283ef878`: the spec's `status_key="research_health_status"` was an over-spec — it would trip `update_status_columns`' allowed-set raise + need a schema column; CHARC ruled (A) bare B-shape; the grounding catch happened at writing-plans, recipe §7) + a separate `mode=ro` conn + the single-sourced `write_research_health_artifact` (the script's local writer deleted; #24-#26 guard) + write-nothing-on-failure. **Calibration A** = #3 `coverage_gaps` trailing-≤1-session grace (kills the post-close session-rollover false-red). **Calibration B** = #2 `invalid_ohlc` named baseline `_INVALID_OHLC_BASELINE_COUNT=23` (SCOPE LOCK: only `invalid_ohlc`). **The operator pipeline-run gate (real `swing pipeline run` 104) witnessed both calibrations live.** The 2 RD calibration notes are now RESOLVED (folded into this arc).

## 3. Disciplines banked THIS generation (durable — apply them)
- **#39 schema-boundary corollary** (memory `feedback_schema_boundary_defensive_scope` · orchestrator-context §Pre-Codex disciplines **#39** · recipe §3 `94ac2905`, CHARC). For a READ-ONLY consumer/monitor, a Codex finding premised SOLELY on a value the write boundary VERIFIABLY prevents (CHECK/UNIQUE/NOT-NULL/FK/migration) is OUT-OF-SCOPE-for-V1 / non-blocking. Bounds the defensive-hardening treadmill; does NOT reinstate the round cap.
- **The orchestrator QA citation-verification check (CHARC-mandated, on #39):** at convergence-verification, EVERY "schema-prevented / out-of-scope" dismissal in `.copowers-findings.md` MUST cite the exact constraint AND you must READ the migration to confirm it actually prevents the value (never assume the defense — the 18-E lesson). An uncited/unverified dismissal → re-open it (stays in-scope).
- **The cross-arc write-barrier-vs-test-seeding gotcha** (CLAUDE.md §Gotchas, Windows/test-discipline): a repo write-barrier breaks tests seeding bad data via that function → plant via RAW insert; divergent-base cross-arc interactions are caught ONLY by the merged-head no-false-green re-run.

## 4. Phase 18 state — ALL dispatched arcs CLOSED; trailing remain
Live tracker: [`docs/phase18-todo.md`](phase18-todo.md). **CLOSED:** 18-A/B/C/D/E/F + FIX-1 + 18-B.1 + the nightly half.
**REMAINING (operator sequences; do NOT commission until "go"):**
- **The RD watch-standard amendment** (RD's own action — reference the monitor in `docs/research-director-watch-standard.md` §3.1; NOT an orchestrator dispatch).
- **`role_mail`-on-ATTENTION → 18-H.7** (deferred from the nightly half).
- **18-G** (dead-brief sweep) · **R1** (declare `requests` in pyproject) · **18-H.1** (Schwab tokens-absent→yellow) · **18-H.2** (404 unification) · **18-H.3** (stoplight ICON not word in the drill-down — operator-re-confirmed at the 18-D gate) · **18-H.4** (Schwab web re-auth self-lock, briefed `d353783e`) · **18-H.5** (dead-code-hardening audit — the retroactive twin of #39; HOLD; brief `f3b95d3e`).
- **The codex-auto-review A/B** was PULLED OFF 18-D + RE-HOMED to ride a future production-code `review-strong` arc (18-H.4 nominee; **CHARC owns the arc pick**; it is NOT the orchestrator's to run on a docs/already-converged arc). Design: both reviewers single-pass on the same pre-fix tree at matched HIGH effort.

## 5. Directors + comms (unchanged)
CHARC (tool/architecture) + RD (research/measurement). POST status/return_report/query via `python scripts/role_mail.py post --from orchestrator --to charc,rd[,operator] --type … [--body / --body-file]` FROM the main repo dir. **No orchestrator inbox** — direction comes FROM the operator (who relays director rulings; directors sometimes ALSO commit a ruling to main, e.g. CHARC's O1 `283ef878`). Measurement-core arcs: RD measurement-integrity merge-blocking; CHARC architecture/C-conditions on the diff. The IMPLEMENTER never posts to the mailbox (you post post-QA).

## 6. The dispatch model (unchanged; full detail in orchestrator-context + memory/ + the recipe)
Orchestrator spawns the implementer as a **sub-agent via the Agent tool** (the `.claude/agents/implementer-<model>-<effort>` cells; SPOF [`docs/implementer-dispatch-recipe.md`](implementer-dispatch-recipe.md)) on the operator's "go": select the cell by TASK (writing-plans ~`opus-xhigh`, executing ~`opus-high`; measurement-chain write-path → consider `opus-xhigh`), **announce it (vetoable) before spawn**, `Agent(run_in_background=true)` (NO `model`/`isolation` override — the cell carries model+effort + makes its own `.worktrees/<name>`). Commit the brief to main BEFORE dispatch. Await the return; QA-against-disk (the diff, trailers `[]`, **convergence from the REAL `.copowers-findings.md`** — never the agent's claim; the #39 citation check; data shapes vs live code). Post the QA'd return to directors; the director gates; operator authorizes; rebase + `merge --ff-only` + no-false-green merged-head + ruff. You OWN the codex-auto-review A/B + any git-resolvable-checkout Codex work. Web/UI → operator browser gate; a pipeline-step → operator real-`swing pipeline run` gate.

## 7. Operational gotchas worth re-noting
- **`| tail` masks pytest's exit code** — READ the actual `N passed, M skipped` summary before claiming green (the no-false-green discipline; it caught the 18-B.1×18-D regression).
- **Worktree CLI verification:** `PYTHONPATH=. python -m swing.cli <cmd>` from inside the worktree (the editable install resolves the MAIN tree). The operator pipeline-run gate ran from the worktree this way.
- **Detached `swing web` / `swing pipeline run`** survive TaskStop — kill by port (`Get-NetTCPConnection -LocalPort 8080` → `Stop-Process -Force`) + verify free + no straggler.
- **cwd drift:** run worktree git via `git -C .worktrees/<wt>`; write `role_mail --body-file` from the main repo.
- **A pipeline-step operator gate is HEAVY** (a real `swing pipeline run` writes the live DB + hits finviz/yfinance, ~minutes) — flag the difference vs a read-only script gate + get the operator's "drive it / run it solo" preference.

## 8. FIRST ACTION
Greet the operator; confirm the clean boundary (main `e8a4063b`, ~8550 green, v30, single worktree, **Phase 18 with ALL dispatched arcs closed** — only the RD watch-standard amendment + 18-G/R1/18-H.* + the re-homed A/B remain). Announce the new generation to both directors (`role_mail … --to charc,rd --type status`). NO active dispatch, NO in-flight work. Surface §4's remaining queue. **Do NOT commission anything until the operator says go.**

*End of handoff. The 2026-06-13 data-collection audit is fully closed AND automated: 18-A writer fix → 18-B consolidation → 18-C observability → 18-D monitor (+FIX-1, +18-B.1 structural prevention) → the nightly half + calibrations refresh the artifact every pipeline run; the GUI research stoplight reads the calibrated state.*
