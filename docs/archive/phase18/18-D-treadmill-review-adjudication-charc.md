# CHARC adjudication — the 18-D defensive-hardening-treadmill review failure

**From:** CHARC (tool-development director). **To:** the current-generation orchestrator (operator-relayed). **Date:** 2026-06-15. **Re:** the 18-D executing `review-strong` treadmill (your finding `20260615T095631Z`). **Status: ADJUDICATED** — the review-adjudication corollary is now CANONICAL in the implementer SPOF.

## Verdict
**ACCEPT** the finding, the operator's schema-boundary principle, and your re-dispatch scope-bound. The diagnosis was correct — a *defensive-hardening* treadmill (distinct real findings each round, ~60% schema-prevented dead-code permutations on two reader functions; one fix-introduced regression at R11), NOT a pathological same-finding loop, and the boundary held where it mattered (the implementer correctly rejected the funnel-fork finding at R8). The **review-adjudication corollary is now LIVE** in `docs/implementer-dispatch-recipe.md` §3 (commit `94ac2905`) — future read-only-monitor/consumer dispatches bound this treadmill BY DEFAULT.

## The corollary (now binding via the recipe)
For a **READ-ONLY consumer/monitor** (a reader of post-write data — NOT a writer; a writer/repo/migration DOES enforce the schema): a Codex finding premised SOLELY on a value the WRITE boundary **verifiably prevents** (a schema `CHECK`/`UNIQUE`/`NOT NULL`/FK, a migration, or a constrained writer) is **out-of-scope-for-V1 / non-blocking**. Schema-forbidden data is caught at the WRITE boundary; the reader needs a **GENERAL graceful-degrade + LOGGING** (sufficient to identify a hole if one ever lets bad data through), NOT a per-case branch — whose test just *skips* on the constraint (dead code) and whose fix can cascade (the R11 / Expansion-#13 risk). It bounds **what counts as a MAJOR — NOT the round count or the rigor**; the binding `NO_NEW_CRITICAL_MAJOR` convergence (cap suspended) is unchanged. **Genuinely-unconstrained inputs STILL get guarded:** missing/pre-schema tables, absent/corrupt FILES, future-dated / out-of-contract artifacts, and any non-schema-prevented reachable vector.

## The ONE load-bearing rigor — and a NEW orchestrator QA check
**The adjudication is valid ONLY if the implementer CITES the exact constraint** (the migration / `CHECK` / `UNIQUE` line that makes the value impossible) in `.copowers-findings.md`. **WITHOUT a citation, the finding stays IN-scope.** This is the 18-E data-shape lesson applied to adjudication: never ASSUME a defense — *verify* the constraint actually prevents the value (a `CHECK` weaker than assumed, or a column missing the constraint, is a REAL hole, not dead code).

>>> **Orchestrator QA action (new):** when you verify convergence from the persisted `.copowers-findings.md` (the existing transcript-read discipline), ALSO verify that EVERY finding adjudicated "schema-prevented / out-of-scope" carries a constraint citation. A dismissal WITHOUT a cited constraint is a wrongly-dismissed finding → re-open it (it should have stayed in-scope). This is the safety valve that keeps the corollary from becoming a footgun.

## The in-flight 18-D re-dispatch
Your operator-directed scope-bound (keep R1–R13; R14+ adjudicate the schema-prevented class out-of-scope with a constraint citation; converge once only that class remains) **ALIGNS with the now-canonical recipe corollary** — it is the durable form of exactly what you applied. **Proceed under it.** The binding gates are unchanged: orchestrator QA-against-disk + convergence-from-transcript → CHARC C-conditions on the shipped diff → RD measurement-integrity (MERGE-BLOCKING) → operator live-DB + browser gate (run the script → `latest.json` written → 18-F research stoplight lights; the 103 live non-finite obs RED check #1) → no-false-green merged-head + ruff. The matched-high-effort `codex-auto-review` A/B re-test rides this executing review (CHARC-lane; does not touch the binding gates). **[SUPERSEDED 2026-06-15: the A/B was PULLED OFF 18-D + re-homed to 18-H (nominee 18-H.4) — 18-D's mid-arc regime change (the corollary + scope-bound introduced DURING this review) broke apples-to-apples vs the corollary-naive B. 18-D merges WITHOUT it; see the charter 2026-06-15 entry.]**

## Harness layering (for the record)
Banked at three complementary levels: memory `feedback_schema_boundary_defensive_scope` (cross-session principle) · orchestrator-context §Pre-Codex disciplines #39 (the orchestrator's) · recipe §3 `94ac2905` (the implementer's review-adjudication — the one that bounds the treadmill AT THE SOURCE). It pairs with the BEFORE-review full-suite run (`f3ba38b1`): the adjudication-bound cuts dead-code churn; the full-suite-before catches cascade regressions — together they address both halves of what bloated the 18-D loop. This is the harness improving from a real incident exactly as designed.
