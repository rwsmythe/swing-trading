# Orchestrator Handoff — 2026-06-21 (cash-ledger correction cluster CLOSED; Phase-18 CLOSING)

**Audience:** a fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). **Read [`docs/orchestrator-context.md`](orchestrator-context.md) end-to-end first** (durable role framing), then this doc (the delta this generation), then `git log --oneline -20` + `git status`. Bootstrap via `scripts/orchestrator_bootstrap.md`.

## 1. Clean boundary (verify: `git log --oneline -10` + `git status -sb` + `git worktree list`)
- main HEAD **`4a363f95`** when this handoff was written — but the **concurrent CHARC/scaffold generation advances main frequently** (docs-only: `charc-state`/`rd-state`/`charter` compactions, D20 commission). My last ARC-merge was **`be567712`** (cash-void). **Always branch worktrees from CURRENT main + rebase before merge.**
- **~8734 fast green** on `main` (re-confirmed on the merged head at the cash-void close). **Schema v31** — **this generation added ZERO migrations** (all three arcs were no-schema by design: OOF/web-acknowledge/cash-void). Ruff clean. **ZERO `Co-Authored-By`** holds (every commit `%(trailers)` = `[]`).
- **origin ~152 behind — NOT pushed** (operator cadence; push only when asked). **Single `main` worktree** (all this-gen worktrees ff-merged + swept; no husks).
- Merge discipline: rebase the branch onto CURRENT main → `merge --ff-only` → re-run the fast suite ON the merged head + READ the `N passed` line (the binding no-false-green).

## 2. What this generation shipped (the cash-ledger correction cluster — 3 arcs, all merged + 4-gate-passed + witnessed)
1. **OOF-buy cash-coherence command** (`swing journal oof-buy`, Phase-18 deferred-follow-up #1) — executing merged **`5855c760`**. A `oof:` ref-sentinel + an additive step-7 matcher self-source branch + the §2.4 both-sides-exclude composition. **RD fired a mid-arc course-correction:** the shipped idempotency was a SILENT no-op on a sentinel collision → the #27 silent-ledger-loss hole; CHARC amended brief §4 → I dispatched a fail-loud correction (`--force` escape + `#seq` disambiguator for a legit second same-day buy). Operator §5.10 live-witness PASS (ZZTEST).
2. **Web simple-acknowledge coverage** — executing merged **`023d6753`**. Generalized the reconcile.py orphan-acknowledge branch to an EXPLICIT ENUMERATED allowlist `{cash_movement_mismatch, equity_delta}` (NOT MATERIAL_BY_TYPE-derived) + fixed the misleading-409 copy (a NEW `not_web_acknowledgeable` error_kind + template branch — the copy was HARDCODED in the `already_resolved` template branch, O1). Additive/guard-only; no schema. Operator §5.10 BROWSER live-witness PASS (5/5, seeded ZZWIT throwaway).
3. **Cash-movement void command** (`swing journal cash-void`, register D19) — executing merged **`be567712`**. Mirrors the OOF arc: a `void:<id>` ref-sentinel + a KIND-AGNOSTIC step-7 self-source branch + a `find_by_id` read helper (two additive carve-outs, no schema). Idempotency FAIL-LOUD FLAT (no `--force` — no legit double-void) from the start. Operator §5.10 CLI live-witness PASS (equity restored EXACTLY 1654.78→1604.78→1654.78; 3 guards fail-loud).

## 3. Disciplines banked THIS generation (apply them)
- **`role_mail` MUST run from the MAIN repo dir** (NEW memory `feedback_role_mail_run_from_main_repo`): `scripts/role_mail.py` resolves its comms root from the SCRIPT's repo, so posting while cwd is a worktree (after QA greps) SILENTLY misdelivers director messages to the worktree's gitignored `comms/` tree (the `posted ->` line hides it — only the last 2 path segments print). Caught 2026-06-21: 2 OOF return reports lost until reposted from the worktree's `comms/`. **Prefix every `role_mail post` with `cd "c:/Users/rwsmy/swing-trading"` in the SAME Bash call.**
- **Fail-loud idempotency from the START on any sentinel-ref correction affordance** (the OOF→cash-void lesson): a `<sentinel>:<key>` collision must FAIL LOUD (ClickException naming the existing row), NEVER a silent "already recorded" no-op (the #27 silent-ledger-loss class). The OOF arc had to be course-corrected; cash-void baked it in. Add `--force`/disambiguator ONLY if a legitimate second same-key entry exists (OOF yes; void no).
- **Don't blind-copy an exemplar arc — verify the gate semantics.** The OOF matcher branch gates on `kind=='withdraw'` (an oof: ref only rides a withdraw); the void branch is a deposit OR withdraw → it MUST gate on the REF SHAPE alone (kind-agnostic). A blind copy would miss void-deposits. The discriminating test (a void-DEPOSIT skipped, empty registry) is what catches a copy-paste.
- **Mid-history `Test:`-trailer cleanup via SHA-keyed `filter-branch`** (`git rebase -i` is blocked in this harness): the OOF arc had 2 commits whose final `Test:` prose paragraph git mis-parsed as a trailer. I cleaned them with a SHA-keyed `--msg-filter` python strip (binary I/O to preserve CRLF; byte-identical passthrough for non-targets) — **de-risked by testing the strip on ALL commits BEFORE running** (it over-matched multi-line `Test:` paragraphs git did NOT treat as trailers until SHA-keyed). The web-ack + cash-void implementers kept final paragraphs plain prose → no rework needed. Tell implementers: final `-m` paragraph plain prose.
- **Operator live-witnesses are step-by-step-able.** The web-acknowledge browser witness was walked one step at a time (seed via a throwaway-marker script → witness each URL → cleanup script). Worked well; ground the witness against the LIVE DB first (I checked the live discrepancy state read-only before seeding).

## 4. Phase-18 state — CLOSING (CHARC-driven); queue
- **The cash-ledger correction cluster is COMPLETE** (OOF self-source → web-acknowledge → cash-void). **No live orchestrator arc; nothing in flight.**
- **CHARC is CLOSING Phase 18** (its own generational handoff landed `1d233973`: "Phase-18 CLOSING, Group 1 done, Groups 2-6 plan for the fresh CHARC"; context-doc compactions `9463773b`/`33cf9c29`/`4e5687a9`). The close is CHARC-owned; the orchestrator does NOT drive a phase close.
- **D20 COMMISSIONED** (`4a363f95`): the Schwab `transaction_id` `^[0-9]+$` numeric constraint — my collision-proof flag, CHARC+RD-adjudicated out-of-scope for the void arc, now a Phase-18-close system-wide hardening (one boundary constraint closes BOTH the `oof:` and `void:` collision proofs). A candidate next arc if the operator sequences it.
- Older queue (from the 2026-06-18 handoff, status uncertain — verify against CHARC's current `phase18-todo`/`charc-state`): **18-G** (dead-brief sweep) · **18-H.7** (nightly research-health ATTENTION→rd-push) · the RD watch-standard amendment · §2.4 (B) materiality bump (deferred) · research-monitor yellows (RD-evaluated, non-actionable).

## 5. Directors + comms (unchanged)
CHARC (tool/architecture) + RD (research/measurement). POST status/return_report via `cd "c:/Users/rwsmy/swing-trading"; python scripts/role_mail.py post --from orchestrator --to charc[,rd] --type … [--body / --body-file]` (the cd is the §3 lesson). **No orchestrator inbox** — direction comes FROM the operator (who relays director rulings). The IMPLEMENTER never posts (you post post-QA). RD merge-blocks on measurement-core arcs (cash ledger / coherence); CHARC owns the §3 / tripwire / C-conditions gate + QA-on-disk on carve-out/schema arcs. `--body` backtick + `$` hazard: backticks trigger command-substitution and `$N` word-splits — use plain text or `--body-file`.

## 6. The dispatch model (unchanged; full detail in orchestrator-context §Dispatch execution + the recipe)
Orchestrator spawns the implementer as a **sub-agent via the Agent tool** (`.claude/agents/implementer-<model>-<effort>` cells; SPOF [`docs/implementer-dispatch-recipe.md`](implementer-dispatch-recipe.md)) on the operator's "go": commit the brief/plan to main FIRST → select the cell by TASK (announce, vetoable) → `Agent(run_in_background=true)` (NO model/isolation override — the cell makes its own `.worktrees/<name>` + carries model+effort). Await; **QA-against-disk** (diff scope, trailers `[]`, the measurement arithmetic AS SHIPPED [verify against disk, never trust the report], convergence from the REAL tracked transcript, the carve-out self-cert). Post the QA'd return to the directors → director gates + operator live-witness → operator authorizes → rebase + `merge --ff-only` + merged-head no-false-green + close-out (status docs + worktree sweep). **Writing-plans** = `implementer-opus-xhigh` (measurement-core design) / `implementer-opus-high` (bounded web) + review-fast; **executing** = `implementer-opus-max` (measurement-core ledger) / `implementer-opus-high` (web) + review-strong repo-access + codex-auto-review, both repo-access. The implementer sub-agent's Bash cwd does NOT persist across calls — it commits via the SINGLE-call `cd .worktrees/<name> && git add … && git commit …` form (verified across this gen).

## 7. The concurrent harness-scaffold / director generations (be aware, do NOT touch)
SEPARATE generations (CHARC, RD, the scaffold arc) advance `docs/charc-state.md`, `docs/rd-state.md`, `docs/*-context.md`, `.codex/`, `AGENTS.md`, the `docs(charc-state)`/`docs(rd)`/`docs(charter)`/`docs(scaffold-backlog)` commits — docs-only, FREQUENT (main moved ~152 commits ahead of origin). Always branch worktrees from CURRENT main + rebase before merge. Leave their artifacts alone. Untracked in your `git status` (`.codex/`, `AGENTS.md`, `exports/research/*`) is theirs — LEAVE it.

## 8. FIRST ACTION
Greet the operator; confirm the clean boundary (main at/after `be567712`, ~8734 green, v31, single worktree, **no live arc, nothing in flight**; the cash-ledger correction cluster is fully closed; Phase 18 is CLOSING under CHARC). Announce the new generation to both directors (`cd <repo>; role_mail … --to charc,rd --type status`). NO active dispatch. The operator sequences what's next (D20 hardening, the §4 queue, the Phase-18 close, or new work). Do NOT push origin unless asked.

*End of handoff. This generation closed the cash-ledger correction cluster end-to-end (OOF self-source command → web simple-acknowledge → cash-void), three arcs, all merged + 4-gate-passed + operator-witnessed, ZERO schema, ZERO Co-Authored-By. The RD fail-loud course-correction (OOF) became a baked-in discipline (cash-void). The board is clean.*
