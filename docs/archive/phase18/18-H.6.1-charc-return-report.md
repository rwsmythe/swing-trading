# 18-H.6.1 — orphan attribution (banner + clear + state) — orchestrator QA'd return (NOT yet merged)

**To:** CHARC (QA-on-disk requested) · **From:** orchestrator · **Date:** 2026-06-16
**Arc:** Phase 18 §18-H.6.1 (TRIPWIRE: `swing/trades`+`swing/data` carve-out; NO new schema). **Brief:** `docs/phase18-arc-h6-1-dispatch-brief.md` (`986e0690`).

## 0. Status — code QA PASS; merge PENDING (CHARC QA-on-disk → §5.10 operator live-witness)
Branch tip `ebf1443d` (7 commits on `e17727f4`; main now `5b0b08d2` — advance is docs-only harness-backlog, ZERO carve-out overlap → clean rebase). Suite **8618 passed, 5 skipped** (pre-review 8610), `ruff check swing/` clean, trailers `[]`. NOT merged.

## 1. The three parts (verified on disk)
- **Part 1 (banner/count):** `list_unresolved_material_orphans(conn)` added at `repos/reconciliation.py:483` (`discrepancy_type='untracked_broker_position' AND trade_id IS NULL AND material_to_review=1 AND resolution IN ('unresolved','pending_ambiguity_resolution')`); UNIONed into `count_unresolved_material` + `list_pending_ambiguities_in_banner_set` in `metrics/discrepancies.py`. The trade-JOINed helpers are byte-unchanged; arms are mutually exclusive (JOIN arms require a trade_id, orphan arm requires trade_id IS NULL → no double-count). **R1-M1 fix:** the reader is scoped to the type, so a non-orphan `equity_delta` `trade_id NULL` row does NOT leak (B independently re-verified).
- **Part 2 (FK-null-safe resolver, C2 HELD — no schema):** the service `resolve_discrepancy` + repo + CLI already clear an all-FK-null orphan (no FK access); the web `/reconcile/discrepancy/{id}/resolve` gained an orphan branch (a no-FK-safe acknowledge form/VM/template) → `resolve_discrepancy(resolution="acknowledged_immaterial")`. **The existing `acknowledged_immaterial` value sufficed → C2 STOP-and-flag did NOT fire; no new RESOLUTION_TYPES value, no migration 0032.**
- **Part 3 (state):** a single `if disc.discrepancy_type == "untracked_broker_position": continue` in `_pivot_classify_and_dispatch_for_run` (schwab_reconciliation.py:575) → the orphan stays `unresolved`, never stamped `pending_ambiguity_resolution`. Every other type's classify/dispatch path is byte-identical (C1).

## 2. C1-adjacent note for ratification — the R2 `resolve_discrepancy` generalization
Clearing the **legacy ID-68 orphan** (swept into `pending_ambiguity_resolution` by 18-H.6, before Part 3) required the manual resolver to satisfy the migration-0031 cross-column CHECK (non-NULL `ambiguity_kind` ⇒ resolution ∈ the two pending states). R2's fix: `resolve_discrepancy` now sets `clear_ambiguity_kind = existing.ambiguity_kind is not None` and clears it in the same UPDATE. **C1 assessment (verified):** the clear is GATED on the existing row carrying a non-NULL ambiguity_kind, so the COMMON resolve path (an `unresolved` row, ambiguity_kind already NULL) is BYTE-IDENTICAL (`clear_ambiguity_kind=False` → no-op). The ONLY behavior change: manually resolving a row CURRENTLY in a pending-ambiguity state to a non-pending terminal (e.g. `acknowledged_immaterial`) now SUCCEEDS where it previously RAISED on the CHECK — additive (enables a previously-impossible-but-sensible op), not a regression of any working flow; 88 tier-2 + ambiguity-roundtrip regressions green. Necessary for the in-scope ID-68 clear (§1/§5.10). **My read: C1-compliant in spirit (common-case byte-identical; additive-only). Flagging for your ratification.** (Also R1-M2: a `require_current_resolution` TOCTOU-close param, default None → every existing caller verbatim.)

## 3. C1–C4 discharge
C1 verified (trade-attributed count/banner/resolve/classify byte-identical in the common case; the R2 generalization is additive per §2); C2 HELD (no new schema; `acknowledged_immaterial` reused — B re-verified migration 0031 preserves the resolution CHECK); C3 (orphan end-state `unresolved` → banners → clearable → resolves cleanly, ambiguity_kind→NULL for the legacy case, run counter decremented); C4 (read-path + manual resolver are not new sandbox-gated domain mutations; resolver mirrors the existing `resolve_discrepancy` posture).

## 4. Reviewer A / B / mechanics
A: review-strong, `-p review-strong`, repo-access, **`reasoning effort: high` confirmed in every raw header**, converged R3 `NO_NEW_CRITICAL_MAJOR` (R1: over-broad reader scope + concurrent-POST TOCTOU; R2: legacy ID-68 cross-column CHECK — all fixed in-carve-out). B (codex-auto-review, repo-access, effort=high): `NO_NEW_CRITICAL_MAJOR`, independently confirmed the orphan-arm scope + C2. 8618 green; trailers `[]`. Pre-review `03025c89`; A/B artifacts: `.worktrees/18-H.6.1-orphan-attribution/.copowers-findings.md` + `.codex-review-r1..r3.txt`; B output `.codex-B-18-H.6.1.txt`.

## 5. Gate remaining
CHARC QA-on-disk (ratify the §2 C1-adjacent generalization; confirm C1–C4) → §5.10 BINDING operator live-witness (the SPCX orphan now banners + is `unresolved` + clearable; the legacy ID-68 clears with ambiguity_kind→NULL; resolves cleanly after journaling SPCX — no lingering cruft) → merge + no-false-green merged head. No RD merge-block.
