# SQLite Lock-Contention (WAL + busy_timeout) Arc -- Writing-Plans Dispatch Brief

**Audience:** Fresh Claude Code instance dispatched as the writing-plans implementer for the SQLite lock-contention fix. No prior conversation context.

**Mission:** Turn the LOCKed, Codex-converged brainstorm spec into an executing-plans-ready, TDD-task-decomposed implementation plan.

**Spec (AUTHORITATIVE):** `docs/superpowers/specs/2026-06-05-sqlite-lock-contention-wal-design.md` (545 lines; merged to main `5e135103`; Codex-converged R1[5maj/3min]->R2[4maj/3min]->R3 `NO_NEW_CRITICAL_MAJOR`). Execute its design verbatim EXCEPT where the operator OQ resolutions below override it; **re-grep every cited file:line at writing-plans STEP 0** (discipline #2).

**Brief lineage:** brainstorm dispatch `docs/sqlite-lock-contention-arc-brainstorming-dispatch-brief.md`; this file is the writing-plans dispatch.

**Context:** main HEAD at this dispatch: see §6 (branch from it). ~7175 fast tests green; **schema v24 (this arc adds NO schema -- runtime PRAGMA + connection plumbing only)**. The root cause is operator-witnessed-live + orchestrator-verified: `OperationalError: database is locked` -- WAL is ALREADY ON (live `swing.db` header bytes[18,19]=2,2; `ensure_schema` db.py:1132), so `busy_timeout` is the lever, NOT WAL-enable.

**Skill posture:** `copowers:writing-plans`. **SINGLE Codex chain** to convergence (`NO_NEW_CRITICAL_MAJOR`; ~5-round cap suspended -- `feedback_codex_round_limit_suspended`). **Codex transport (MCP DEAD):** `wsl.exe bash -ilc 'export PATH="$HOME/.local/node22/bin:$PATH"; cat prompt.txt | codex exec -s read-only --skip-git-repo-check -'` (PATH prefix REQUIRED; `codex --version` -> codex-cli 0.135.0 liveness; pre-generate any diff on Windows -- the worktree `.git` is unreachable from WSL; tell Codex NOT to run git). **Persist BOTH prompts AND responses** (incl. the final `NO_NEW_CRITICAL_MAJOR`) to `.copowers-findings.md` (`feedback_implementer_persist_codex_responses`). Output: plan at `docs/superpowers/plans/2026-06-05-sqlite-lock-contention-wal-plan.md`.

---

## §1 Operator OQ resolutions (LOCKed 2026-06-06 -- propagate into the plan, do NOT re-open)
- **OQ-0 ACCEPT R1.** WAL is already on; the operative lever is `busy_timeout`. WAL-enable stays as defense-in-depth -- but **do NOT reaffirm `journal_mode=WAL` on the hot `connect()` path** (Codex R1 major #1: reissuing it can itself hit `database is locked`). WAL is set only by `ensure_schema`.
- **OQ-A busy_timeout = 30000 ms, AND a cfg knob.** Plumb a module-level `DEFAULT_BUSY_TIMEOUT_MS = 30000` constant + a keyword override on the opener, AND a `cfg.web.db_busy_timeout_ms` (or equivalent namespace) config knob that feeds the override at the pipeline/web callsites for runtime tuning WITHOUT importing cfg inside `db.py` (no import cycle). Set `busy_timeout` (via `PRAGMA busy_timeout` and/or `sqlite3.connect(timeout=)`) BEFORE any other pragma.
- **OQ-B route ALL swing.db opens through the centralized opener.** `connect`, `ensure_schema`, the web-route/CLI/view-model direct opens, AND the backup SOURCE open -- the backup source MUST retain its fail-closed `mode=rw` URI semantics (Codex R2 major #1) while gaining the busy_timeout. The `schwab-tokens.db` opens are a SEPARATE file -> excluded. Produce the explicit **raw-open inventory** (Codex R2 major #2: classify every `sqlite3.connect` as live-swing.db / backup-dest / migration-source / probe / temp / tokens) so "one opener, uniform busy_timeout" is enforceable.
- **OQ-C INCLUDE write-pressure reduction in V1** (operator override of the spec's data-driven-defer). **THIS IS A DESIGN-BEARING WRITING-PLANS:** the brainstorm deferred the mechanism, so the plan CARRIES it. SELECT + design the concrete mechanism from the spec's menu -- a serialized/shared audit-writer connection (single connection guarded by a lock so audit writes serialize cheaply off the contended path) vs batched/deferred start+finish writes -- GROUNDED in the real `audit_service` concurrency model (audit_service.py:104/151 `BEGIN IMMEDIATE`; the start+finish pair; 8+8 workers config.py:381/386). **BINDING constraints the mechanism MUST preserve (or consciously change WITH justification + a test):** (1) the **single-transaction discipline** (each audit write owns `BEGIN IMMEDIATE`/COMMIT/ROLLBACK; rejects caller-held tx); (2) the **in-flight-row visibility contract** (the `start` row commits BEFORE the HTTP call so a mid-call crash leaves an `in_flight` row -- `record_call_start` at marketdata.py:551); (3) **thread-safety** of any shared connection (`check_same_thread`, an explicit lock). Keep the G2' lock-wait telemetry (it validates the fix at the first live run). If the mechanism choice proves genuinely under-determined, SURFACE it (don't wing it) -- the Codex chain must converge on the concurrency design.
- **OQ-D fold the catch-all observability logging into this arc.** Both ladder catch-alls (`fetch_window_via_ladder` ~456 + `fetch_quote_via_ladder` ~330) log the exception class+message, redaction-safe (the message routes through the `setLogRecordFactory`; **NOT `exc_info`** -- traceback frames can bypass message redaction).
- **OQ-E the OhlcvBar bad-bar issue is OUT of this arc but NOT banked** -- it is a SEPARATE active fix-target (its own arc; distinct root cause = Schwab regular-session data quality). Do NOT address it here; do NOT mark it accepted/banked.

## §2 Design surfaces (spec §4/§5 -- ground + plan)
The centralized `open_connection` opener (busy_timeout-first; WAL NOT reaffirmed on the hot path); the raw-open inventory + routing; backup fail-closed preservation; the two-path deadline regimes (OHLCV `get_or_fetch` no deadline -- 30 s busy_timeout fully effective; quote `get_many` 6 s caller deadline -- busy_timeout reduces but the write-pressure reduction is what protects it); G2' lock-wait telemetry (measure around `BEGIN IMMEDIATE`, log AFTER lock release -- Codex R2 major #3 -- incl. FAILED acquisitions).

## §3 Test strategy (spec §6 -- 5 layers; `feedback_regression_test_arithmetic`)
Preserve the spec's 5-layer strategy, esp. the **production-path stress test split by failure point** (Codex R2 major #4: forced-start-lock -> no row; forced-finish-lock -> in-flight row remains -- assert the correct row state per failure point, NOT a blanket "no audit row"). Each behavioral test must distinguish the pre-fix (default ~5 s, contention -> lock) from post-fix (30 s + write-pressure reduction -> no lock) paths. Include a test that the write-pressure-reduction mechanism preserves the single-tx discipline + the in-flight-row contract.

## §4 Locks / invariants (propagate)
NO schema (runtime PRAGMA + plumbing; v24 holds); DB-outside-Drive (WAL sidecars stay in `~/swing-data`); backup integrity + the migration backup-gate (`pre_version == target-1` strict); the data-integrity arc's barriers/lock-guard/typed-error/topbar intact; **the audit-service single-tx discipline** (the OQ-C design's hard constraint); L3 append-only; ZERO `Co-Authored-By`; ASCII (Windows cp1252).

## §5 Gate (binding)
1. fast suite green on the MERGED HEAD (isolate the 3 known xdist date-flakes per `feedback_no_false_green_claim`). 2. the concurrency stress test green (the production-path layer). 3. **operator-witnessed FIRST instrumented live run** (post-merge): confirm the ~13-22 tickers/run `database is locked` fallback collapses (Schwab becomes the primary source for them) AND read the G2' lock-wait telemetry; the first run also DECIDES whether the write-pressure reduction sufficed (if the quote path still starves, that surfaces here). UNSEEDED normal run (`feedback_seeded_gate_masks_default_state`).

## §6 Dispatch metadata
- **Subagent:** `general-purpose`, foreground, harness-default model. **Worktree:** branch `sqlite-lock-contention-arc-writing-plans` from main HEAD = the commit that ADDS this brief (orchestrator states the SHA in the inline prompt). Writing-plans writes a PLAN (no code); you MAY read live tables `mode=ro` + read the live DB header to re-confirm R1. SINGLE Codex chain to convergence. Leave the worktree INTACT at return.

## §7 Return report (mirror prior writing-plans returns)
Final HEAD + commits; Codex convergent verdict (cite `.copowers-findings.md`); plan line/task count; the LOCKed OQ resolutions reflected (esp. OQ-C's selected write-pressure-reduction mechanism + its preservation of the single-tx + in-flight contracts); the raw-open inventory (OQ-B); the schema verdict (NONE); the gate enumerated (incl. the operator live-run); per-axis test-arithmetic; ZERO `Co-Authored-By`; worktree intact; executing-plans readiness.

---

*End of brief. Writing-plans for the SQLite lock-contention fix: busy_timeout=30000 (module-const + cfg knob) + a centralized opener routing ALL swing.db opens (backup fail-closed preserved) + write-pressure reduction IN V1 (design-bearing -- preserve the audit single-tx + in-flight-row contracts) + the catch-all observability fold-in + G2' lock-wait telemetry + the 5-layer tests. WAL already on (do NOT reaffirm on the hot path). NO schema (v24). The bad-bar issue is OUT (its own arc, NOT banked). OUTPUT: an executing-ready plan.*
