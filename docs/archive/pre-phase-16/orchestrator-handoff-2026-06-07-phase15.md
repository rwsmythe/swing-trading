# Orchestrator Handoff -- 2026-06-07 (Phase 15, post-deadlock-fix)

**Audience:** A fresh Claude Code instance taking on the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). You drive the copowers cycle + QA + merge; you do NOT implement. **Read the durable role framing in `docs/orchestrator-handoff-2026-06-04-phase15.md` + `docs/orchestrator-handoff-2026-06-05-phase15.md`** (disciplines, the WSL Codex form, the merge mechanics) -- this doc is the DELTA since 2026-06-05. **Your mandate (operator-set): drive the top 3 non-research Phase-15 bullets to completion (§4).**

## 1. Clean boundary (verify: `git log --oneline -8` + `git status -sb` + `git worktree list`)
- **main HEAD = `94b67756`** ("CLOSE the bad-bar arc as accept-and-document"). **origin in sync** (pushes were authorized at QA-pass throughout the 2026-06-06/07 session -- NOT held). Working tree clean; only the `main` worktree registered.
- **~7223 fast tests green** on main; **schema v24**; ZERO `Co-Authored-By` (~720+ commits); ruff clean; busy_timeout restored to 30000.
- 3 known xdist co-residency flakes (`test_ohlcv_reader_re_export_identity`, `test_read_cohort_csv_against_committed_v2trf`, `test_prices_refresh_uses_pipeline_eval_anchor`) pass `-n0`; they did NOT fire on the last two full runs.
- **Windows note:** `git worktree remove --force` intermittently hits a "Permission denied" on the physical dir (a file lock) -- it still de-registers from git (`git worktree list` shows only main); the orphaned plain dir is harmless. `git worktree prune` after.

## 2. Shipped this session (2026-06-06/07) -- all CLOSED + pushed
- **The Schwab `database is locked` deadlock FIXED (the headline).** Root-caused = an OHLCV `get_or_fetch` INSIDE a held `lease.fenced_write` (the audit conn deadlocked). Fixed across two arcs: **lock-contention `ffb5fdc6`** (WAL was already on; `busy_timeout` 5s->30s + a serialized shared audit-writer connection + G2' lock-wait telemetry + catch-all class+message logging -- the telemetry DIAGNOSED the true cause after its own headline gate "failed") then the **fetch-vs-write-ordering reorder `4f0b4010`** (pre-fetch OHLCV OUTSIDE the fence at detect Pass-2 + observe; snapshot-authoritative exemplar membership + a #27 divergence guard). **Operator RE-RUN live gate PASSED Run 93** -- the deadlock collapsed at the restored 30000ms; Schwab is now primary for the ~13-22 liquid tickers. The banked **T-C.1 "per-ticker wrapper error" investigation is RESOLVED** (it WAS this deadlock).
- **Data-integrity arc SHIPPED** (`b237412b` exec): the ext-hours hypothesis was FALSIFIED at the live gate (the OhlcvBar-invariant errors are bad REGULAR-session bars, not ext-hours) but it delivered the typed `SchwabBarConsistencyError`+clean fallback + the completed-day write-barrier/date-only lock-guard + uniform `topbar_session_date(PageKind)`. **One pending sub-gate -> bullet (c) below.**
- **Bad-bar issue ACCEPTED-AND-DOCUMENTED** (`9070fe3d` spec, operator OQ-A/C=d): characterized as irreducible Schwab source data (a small stable set of ~10 candles, ~2-4/run; verified live -- `low (4.65)` recurs 23x); the typed->yfinance fallback already delivers correct bars; the coverage-guarantee fix was judged not worth the hot-path engineering. NO code; the spec stands as the rationale if revisited.
- Phase-14 integration review + the Finviz signature-provenance fix: CLOSED.
- **Process miss logged:** a config-only stopgap commit (`dd3d932e`) broke a brittle finviz TOML test undetected (no suite re-run after a config edit) -> memory `[[feedback_orchestrator_inline_edits_need_suite_run]]`. Re-run the suite after ANY tracked non-doc inline commit.

## 3. The data-driven-gate lesson (cite when commissioning #16 -- same bug class)
The deadlock arc's first live gate "failed its headline" but its telemetry revealed the TRUE root cause, which a second arc fixed. **#16 below is the SAME bug class** (network I/O inside a held `lease.fenced_write`) -- the fetch-vs-write arc's pattern (pre-fetch OUTSIDE the fence) is the template. Reuse spec `docs/superpowers/specs/2026-06-06-fetch-vs-write-ordering-design.md` + plan as precedent.

## 4. THE PLAN -- the top 3 non-research bullets (operator mandate; full records in `docs/phase3e-todo.md` §A)
- **(a) daily-management yfinance-under-fence #16** -- a CODE arc. `compute_daily_approximate_snapshot` (`daily_management.py:~510`) calls the fetch-capable `read_or_fetch_archive` INSIDE the per-trade `lease.fenced_write()` (`runner.py:~3709`), so a warm-miss / transient-empty / gap-fill does yfinance NETWORK I/O while holding the SQLite write lock. NOT the Schwab-audit deadlock (no separate audit conn) -- a lower-severity latent lock-hold (can block a concurrent web writer). **Codex-identified robust fix = a read-only-vs-fetch archive API split** (a spec-locked-signature refactor of `ohlcv_archive.py` + `daily_management.py` + consumers: warm/fetch OUTSIDE the fence, read-only INSIDE) -- OR the fetch-vs-write pre-fetch pattern. Full copowers cycle (the archive-API split has real blast radius). Re-ground at STEP 0 (the fetch-vs-write arc just changed runner.py).
- **(b) Issue #3 `_count_open_at_run` metrics predicate** -- a small metrics-fix arc. `capital.py:418` keys "still open at run time" on `last_fill_at >= started_ts`, but `last_fill_at` is the most-recent fill of ANY action (entry/trim/exit/stop), so a trade filled BEFORE the run but still open is wrongly excluded (surfaced at the Phase-14 integration review: capital-friction position count=0 for Run#89 despite SKYT open since 5/28). **Root cause (data-integrity spec §8 / OQ-5):** the `trades` table has NO terminal-timestamp column; the correct fix derives `exited_at` from `fills WHERE action='exit'`. Reads ONLY `trades` (NOT `account_equity_snapshots` -- the integration-review hypothesis was wrong). Small -- likely a tight brainstorm->writing->executing or a focused executing-with-Codex.
- **(c) data-integrity Slice-B quote cassette (Gate 4)** -- NOT a copowers cycle; the data-integrity arc's last pending gate. The operator records `tests/integrations/schwab/cassettes/quote_regular_fields.yaml` against a LIVE Schwab quote (regular market hours + live tokens) and confirms `regularMarket*` (Last/TradeTime/Bid/Ask) appear; then the slow `test_quote_fields_live.py` passes (it asserts the cassette exists + carries the 4 fields). `swing schwab fetch --verify-marketdata --symbols AAPL` exercises the live quote path. **The VCR record mechanism is NOT a turnkey CLI flag** -- you may need to wire the recording; flagged at the writing-plans. Closing this fully closes the data-integrity arc. Low urgency (B2 is L1-correct either way: it drops to yfinance if the fields are absent).

**Sequencing:** operator's call. (b) + (c) are small; (a) is the bigger one. Confirm sequencing/parallelism with the operator at session start (they chose "one implementer in flight at a time" earlier).

## 5. Binding disciplines (unchanged; full detail in the 2026-06-04/05 handoffs)
Merge is an orchestrator action at QA-pass/convergence/gate-pass (don't ask). **QA every product against disk + AWAIT the operator's return signal before QA.** NEVER false-green (re-run the suite ON the merged head; isolate the known flakes; docs-only merges are suite-exempt -- state it; **config/code inline commits are NOT suite-exempt** -- `[[feedback_orchestrator_inline_edits_need_suite_run]]`). copowers Codex runs to convergence via the WSL prefix+stdin form (MCP dead); implementer persists prompts AND responses to `.copowers-findings.md`. Operator gates are binding + witness the UNSEEDED default. NO Co-Authored-By / NO --no-verify / final `-m` paragraph plain prose / verify `%(trailers)` is `[]` before every push. Commit the dispatch brief BEFORE the inline prompt + always provide a paste-ready inline prompt. **Divergence (operator/orchestrator commits land mid-flight): rebase the branch onto main then `merge --ff-only`** (happened repeatedly this session). Scrutinize implementer REBUTTALS of Codex findings against disk (the use-after-close rebuttal this session was sound + verified). The operator paces deliberately; "pause" = stop.

## 6. READ FIRST
1. `CLAUDE.md` line-3 (the Phase-15 status, just updated) + §Gotchas (esp. yfinance partial-bar + session-anchor + the SQLite/transaction families) + Invariants.
2. `docs/phase3e-todo.md` §A (the 3 bullets have full records: the deadlock-fix closes, the bad-bar accept, #16, Issue #3, Gate 4).
3. The fetch-vs-write spec+plan (`docs/superpowers/specs/2026-06-06-fetch-vs-write-ordering-design.md` + the plan) -- the precedent for #16.
4. `docs/orchestrator-handoff-2026-06-04/05-phase15.md` (durable framing) + `docs/orchestrator-context.md` + the `memory/` directory.

## 7. FIRST ACTION
Stand by. Greet the operator; confirm the clean boundary (main `94b67756`, origin in sync, 7223 green, v24). State the plan: drive the top 3 non-research bullets (§4). Ask the operator how to sequence them ((b)+(c) are small; (a) is the bigger archive-API-split code arc), then commission the first per the copowers cycle (brainstorm dispatch brief -> commit -> inline prompt). Do NOT commission unprompted beyond the operator's go.

*End of handoff. Phase 15: the Schwab market-data deadlock is FIXED + validated (the session's headline); data-integrity + bad-bar closed. The forward plan is the 3 non-research bullets -- #16 (daily-mgmt fence, same bug class as the just-fixed deadlock), Issue #3 (the `_count_open_at_run` metrics predicate), Gate 4 (the data-integrity quote cassette). Land clean at `94b67756`.*
