# Harness-template scaffold — germination backlog

**Owner:** CHARC (swing), as the scaffold architect retaining the design record (the scaffold itself is accepted + static at harness-template `master @ d8ad5c9`; there is no active scaffold orchestrator).
**Purpose:** findings surfaced AFTER accept — chiefly during the **first real germination (coa-chess, started 2026-06-16)** — that improve the SCAFFOLD so future germinations don't repeat them. **Deferred-correction:** the operator reviews + sequences a fix pass later; coa-chess's own CHARC may apply the fixes to its copy in the meantime.
**How fixes land:** a future swing-commissioned scaffold-revision pass edits harness-template (the template); coa-chess pulls or re-applies as its CHARC sees fit. Append items as germination surfaces them.

---

## Status (G4 scaffold-revision — the status source for shipped items)

**SHIPPED 2026-06-22 (CHARC-direct, all gate-green [168 tests] in harness-template `master`)** — the doc/charter/config items:
- **B-1 / B-2 / B-4** (README: concrete CHARC launch, `.\scripts\` form, fresh git history) — `2a10019`
- **B-5 / B-6 / B-15** (charter + role-model: unprefaced dissent, QA-at-every-boundary, route-through-CHARC + dispatch-prose) — `950f084`
- **B-10 / B-2-bootstrap / B-4-bootstrap / B-7a** (registry explicit-addressing convention; bootstrap `.\scripts\`/git-history-note/complete-config-rec) — `e2c1356`
- **B-3 / B-7b-c** (launcher per-role seam-4: CHARC FIXED opus/max, orchestrator a no-baked-value placeholder; preflight verifies --model/--effort) — `7f3f7c0` (AST-parse + DryRun verified)

**CODE arcs — status (2026-06-22):** **B-9 + B-12 COMMISSIONED + EXECUTING** (brief `9bfddd45`; swing orchestrator → harness-template; genericity-guard instance-aware REDESIGN [carries the operator stdlib refinement; supersedes **B-8**] + peer-director-add code [single-source `SINGULAR_INBOX_ROLES` + instance-surface registration + §8 wording]; **writing-plans CONVERGED @plan `14f4062c`, executing dispatched** opus-max). **Brief §2-Gap-2 grounding CORRECTED 2026-06-22:** `INSTANCE_SURFACE_RELPATHS` / `is_instance_surface()` do NOT pre-exist on the template — B-9 CREATES them (the plan: `is_instance_surface()` + `CORE_DOC_RELPATHS`/`INSTANCE_STUB_RELPATHS`); CHARC's brief "`charc-state.md` already is" transcribed coa-chess's POST-fix state as the template's — a grounding miss the writing-plans gate caught. **CHARC architecture QA on disk = PASS** (2026-06-22, executing return @`f0da3aa`, 16 commits): the instance-surface model is SOUND (registered-core-always-wins + disjoint registries + future-safe exempt-until-registered + single-segment/markdown-only → executable teeth via `test_core_files_are_not_instance`/`test_pattern_vs_registry_boundary`/`test_registered_core_always_wins`); CORE contamination still FAILS; NO project term in CORE (spot-grep clean + `test_whole_tracked_tree_is_clean`); stdlib refinement landed instance-aware; **202 OK** re-run on disk; trailers [] ×16; B-12 single-source confirmed (no drift). **MERGED + CLOSED @ harness-template `f0da3aa`** (2026-06-22; clean ff-merge, merged-head **202 OK**, trailers [] ×16, convergence transcript persisted @swing `c7318972`; CHARC verified the merged head on disk). **B-16 now UNLOCKED** (the queue gate clears — dispatchable off master @`f0da3aa`). **B-17 residual** (candidate, OUT-OF-SCOPE for B-9 — both review tracks + CHARC CONCUR): a future CORE doc named to match an instance pattern + forgotten in BOTH the manifest AND the registry could be mis-classified; the registries + the 2 manifest cross-checks close the TRACKED-surface drift; fully closing needs a namespace redesign the brief did NOT authorize. **B-16 COMMISSIONED + QUEUED** (brief `84d06644`; role-identity-lost-on-resume; sequence AFTER B-9+B-12 — both touch the notice hook). **B-11 + B-13** (comms-UI re-sync) + **B-14** (Stop-hook template-banking; swing-live at `e8686e0e`) → ride **G6** (comms-sync). **Cross-repo dispatch mechanics RESOLVED:** the swing orchestrator dispatches INTO harness-template (operator-decided 2026-06-22).

*(The table below is the design-detail reference; for the items above, THIS ledger is the status of record.)*

---

| # | Item | Detail / fix | Where (in the scaffold) | Status |
|---|---|---|---|---|
| **B-1** | README CHARC-launch is abstract while the orchestrator command is concrete | The instantiate section spells out a copy-pasteable `launch_role.ps1 -Role orchestrator` (step 5) but states the FIRST step ("launch CHARC", step 3) abstractly with no concrete command → a human grabs the concrete command and launches the orchestrator **prematurely** (into an undefined app — no `APPLICATION.md`, no cells, no review-gate, no commissioned arc). **Fix:** put the concrete copy-paste command on the step you do FIRST (CHARC launch); de-emphasize the orchestrator command to step 5 where it belongs. | `README.md` (§How to instantiate) | OPEN |
| **B-2** | Docs print `launch_role.ps1 ...` without the PowerShell `.\scripts\` form | PowerShell won't run a current-dir script by bare name, and the launcher is in `scripts/` → `The term 'launch_role.ps1' is not recognized`. Every doc showing the invocation should use `.\scripts\launch_role.ps1 ...` (the working PS form). | `README.md`, `docs/charc-bootstrap.md`, any launcher-invocation doc | OPEN |
| **B-3** | Scaffold ships CHARC at `xhigh`; CHARC MUST be **opus/max** (the one fixed role config) | The seam-4 launcher default starts the scaffold's CHARC at `xhigh`. **CHARC MUST be opus/max in ANY harness** (operator, 2026-06-16) — the architect/bootstrap role needs top capability regardless of project. This is the ONE EXCEPTION to B-7's per-project principle: canonical, NOT tunable, NOT "negotiable" like other directors, NOT per-project like orchestrators. **Fix:** scaffold CHARC seam-4 default = `--model opus --effort max`. (Swing's CHARC is already opus/max at `start_directors.ps1:102`, so the *requirement* coincides with swing — but the *reason* is the role, NOT "match swing".) | `scripts/launch_role.ps1` (`$LaunchArgs`, seam 4) | OPEN |
| **B-4** | Germination clone carries the scaffold's build history → new-CHARC confusion | coa-chess was cloned WITH harness-template's full ~41-commit build log (scaffold arcs A4/A5, registry fixes, the state-pointer convention at `d8ad5c9`). The new CHARC, correctly applying "verify on reality," saw substantial dev history alongside a bare-clone state pointer and had to reconcile cold-bootstrap-vs-resume (it concluded correctly: a true germination, interview not yet run — but the friction is real + avoidable). **Fix:** (a) the instantiate step starts a FRESH git history (copy files + `git init`, OR clone + orphan) so commit-zero is the project's; and/or (b) `charc-bootstrap.md` notes that a git log of scaffold-build commits is the TEMPLATE's history, not this project's → proceed with the interview. | `README.md` (instantiate) + `docs/charc-bootstrap.md` | OPEN |
| **B-5** | Director-as-peer clause allows an initial permission-seeking-dissent lapse | A fresh director (coa-chess CHARC, 2026-06-16) read the §2 director-as-peer clause but initially framed dissent as INVITED ("here's where I'll push, since you invited it") — treating disagreement as permissioned rather than obligated. It self-corrected against the charter (so the clause WORKS), but the base assistant posture (deferential, permission-seeking) defaults to the lapse until actively corrected. **Fix (a SHARPENING — the clause is correct):** add an explicit anti-pattern line — *"state disagreement + its basis plainly and UNPREFACED; do NOT ask permission to dissent or frame it as invited; equal weight on the argument, the operator's call on the decision; no manufactured objections."* | `docs/charc-charter.md` §2 (+ swing `tool-director-context.md` §5.1, the genericized source) | OPEN |
| **B-6** | Scaffold under-expresses "the superior agent QAs the subordinate's report at each boundary" | A fresh CHARC (coa-chess, 2026-06-16) filling seam 3 did NOT catch that **every superior→subordinate boundary includes a QA review performed BY the superior** — it wrote the reviewer as a disembodied "tool + review pass" rather than the responsive role *performing* the QA with the review tool as its **instrument**. `orchestrator-context.md` says orchestrator-owns-QA, but the generic role-relationship model + the review-gate-seam contract don't make the QA-at-every-boundary principle explicit enough for a fresh CHARC to build it in. **Fix (GENERIC ONLY):** state in the role-model docs (+ the review-gate-seam contract) that each superior→subordinate boundary includes a QA review BY the superior, the review tool as instrument. **The QA's specific CONTENT / gating / cadence is per-project — do NOT encode swing's tripwire/per-arc configuration** (the contamination guard; the principle THAT it happens is firm, WHAT it checks is the project's). | role-model docs (`charc-charter.md` / `orchestrator-context.md`) + `docs/review-gate-seam.md` | OPEN |
| **B-7** | Scaffold treats role model/effort as a canonical value, not a per-project CHARC recommendation | At orchestrator bring-up (coa-chess, 2026-06-16) the germinated CHARC recommended the MODEL (Opus) but OMITTED effort (operator wanted xhigh). Operator principle: **a specific model/effort (opus/xhigh) being explicit is incorrect for a generic framework** — it's project-specific. Right model: the germinated CHARC recommends a **COMPLETE per-role config (model AND effort AND permission-mode)** as a per-project choice; the operator applies it (the seam-4 launcher default for launcher-spawned roles, or a manual launch — the operator launches orchestrators by hand in the CC extension). **Fix (GENERIC ONLY):** (a) the scaffold prompts the germinated CHARC to give a COMPLETE model+effort+permission recommendation per role at bring-up (not model alone); (b) seam-4 defaults are clearly-marked tunable placeholders, never canonical values; (c) do NOT bake a specific model/effort into the generic bring-up prompt / orchestrator-context. **EXCEPTION: CHARC is fixed opus/max in any harness (B-3) — the one non-negotiable role config; other directors are negotiable; orchestrators + implementers are per-project.** | `docs/charc-bootstrap.md` (step-5 bring-up) + `orchestrator-context.md` + seam 4 | OPEN |
| **B-8** | **BLOCKER** — template-integrity gates ship into the germinated project + trip on the intended seam-fills → RED baseline blocks the orchestrator | The scaffold's template-SELF-validation gates — `genericity_guard` (`FORBIDDEN_TERMS` bans chess/coa/swing/finviz/ticker tree-wide), `doc_acceptance` (APPLICATION.md "empty by design"; seam doc no-substrate-token), `seam3_defaults` (seam doc names no concrete mechanism), `manifest_accounting` (tracked surface == the 19-file template manifest) — exist to keep the **TEMPLATE** pristine. They ship into the germinated project + run in ITS suite, where filling the seams (APPLICATION.md, the seam-3 fill naming Codex, the domain cell, charc-state) NECESSARILY violates them → **6 failures on a fresh germination (coa-chess, 2026-06-16)**, blocking the orchestrator (accept gate = "tests green"). The bare-clone dry-run MASKED it (empty seams → guard passes); only a FILLED germination surfaces it. **Fix:** SUPERSEDED by B-9 — coa-chess's deeper critique + reference impl (`ec9856d`/`9aee81d`) showed wholesale-retirement is too blunt; the right design is **instance-aware exemption** (scope genericity to the reusable CORE — structurally, via the dependency-posture test — + a designed instance-surface seam → green-by-construction). Same family as B-4 — germination must TRANSFORM template-artifacts, not inherit them raw. | the instantiate process (README + `docs/charc-bootstrap.md`) + the template-integrity test files | OPEN |
| **B-9** | **Genericity-guard REDESIGN** — scope to the CORE + a designed instance-surface seam (supersedes B-8's fix) | The purity gates are legitimate in PURPOSE (keep the reusable thing reusable) but BLUNT, LEAKY, PERMANENT in realization. coa-chess (first live instance) critique — RIGHT on all four; `ec9856d`/`9aee81d` = reference impl: **(1) instance-exemption gap** — ship an `is_instance_surface()` predicate + scan/accounting scoping so instances are green-by-construction, not reverse-engineering the guards (the B-8 blocker, properly fixed). **(2) the vocab guard bans NAMING, not COUPLING** — a word-ban has false negatives (app-coupling without the word) AND false positives (innocent/meta/legit-domain mentions — coa-chess hit 'trade'=a chess move, 'ticker'=describing the guard in a state doc); the guarantee with TEETH is STRUCTURAL (the dependency-posture test: core stdlib-only); the word-ban's lasting value is for DOCS. Scope vocab/ticker bans to the CORE; exempt the instance surface; keep CORE contamination failing. **(3) the residue/ticker bans (swing/finviz/SPY/QQQ) are a one-shot EXTRACTION artifact** — an authoring-time clean-room-leakage gate, no ongoing value once verified clean, yet shipped permanently + snagging plain English forever; a 'generic' template hard-coding a prior project's tickers in its denylist IS carrying that project's ghost (**the scaffold's own guard is swing-contaminated**). Run once at authoring, or core-only. **(4) the concrete seam-3 FILL needs its own doc** — the mechanism-agnostic-seam guard correctly forbids a concrete reviewer in `review-gate-seam.md`, so the fill can't live there; ship a `docs/review-gate-<app>.md` pointer convention so instances don't trip the guard mid-fill. **NET:** scope genericity to the CORE (structurally where possible) + a designed instance-surface seam → green-by-construction, not red-by-default. **CHARC owns this** — I ratified the MAJOR-4 broad-guard model; this is the deeper structural critique that pass missed. **(Operator refinement 2026-06-22 — fold into B-9's design):** the stdlib-only constraint is a property of the TEMPLATE ITSELF (keeps the reusable scaffold dependency-light); it MUST NOT persist into the germinated IMPLEMENTATION — a consumed/germinated project is free to add runtime dependencies. So `test_dependency_posture.py` must be **instance-aware / CORE-scoped** exactly like the vocab guard: prove the reusable CORE stays stdlib-only, but EXEMPT the instance surface (the germinated project's own code imports third-party freely). Add `test_dependency_posture` to B-9's scope (the structural-guarantee guard the redesign must instance-scope, alongside `genericity_guard`/`manifest_accounting`). | `tests/genericity_guard`+`genericity_lists` + `manifest_accounting` + `doc_acceptance`/`seam3_defaults` + the instantiate convention + a `review-gate-<app>.md` convention | OPEN |
| **B-10** | Registry-doc lacks the explicit-under-concurrency addressing convention | `comms-orchestrator-registry.md` documents both forms (`:<session_id>` explicit + bare newest-live) but not the operator-banked convention: **under concurrency prefer explicit `:<session_id>` (deterministic); bare is a `newest_live` heuristic** (from the two-orchestrator anomaly that resolved-as-artifact, 2026-06-17). Low-priority doc clarity. | `docs/comms-orchestrator-registry.md` | OPEN |
| **B-11** | comms GUI missing the dark-theme toggle | The scaffold's Stage-1.5 comms GUI has no dark-theme toggle; swing's matured comms GUI carries one (reference impl: swing **17-D.2** `991430c8` + icon-parity `5df335d4`). **Fix (GENERIC ONLY):** add a dark-theme toggle to the scaffold's comms GUI — the toggle control + persisted preference + themed CSS variables, transcribing swing's MECHANISM, NOT its styling/branding/content (the §5.1 scaffold-contamination guard). A reusable theme toggle is generic UI infrastructure, not project config. **(Superseded by B-13's RE-SYNC** — the dark-theme toggle is one of the post-snapshot fixes the stale comms-UI snapshot misses; re-syncing the comms UI from swing's current version per B-13 delivers this + the 5-sec-collapse fix in one pass, so do NOT patch this in isolation.) | the scaffold's comms-GUI templates + static (the Stage-1.5 GUI surface) | OPEN |
| **B-12** | §8 peer-director-add checklist is INCOMPLETE vs the code (3 gaps) | coa-chess CHARC ran the §8 peer-director-add against the actual code (4th feedback, 2026-06-17). The checklist (`charc-charter.md` §8 + the mirrored bottom comment in `scripts/role_mail.py`) enumerates only the comms role tuples + charter + launcher + state pointer — incomplete on 3 counts a real peer-director-add hits. **Gap 1 (SILENT runtime):** the unread-notice hook `.claude/hooks/user_prompt_submit.py` has its OWN hardcoded role set (`COMMS_ROLES` ~:60 + the `_inbox_for_role` singular-role branch ~:83). Adding the new director to the mail role tuples makes it a valid sender/recipient + auto-creates its inbox, but the UserPromptSubmit notice NEVER fires for it → the new director is silently never told it has mail (same failure class as role-lost-on-resume notice-goes-dark, but structural to adding a role). **Preferred fix (coa-chess #2):** single-source — the hook imports `SINGULAR_INBOX_ROLES` from `role_mail` (it already does a guarded sibling import from `session_start`), so ONE mail-core edit covers delivery AND the notice + 'role-agnostic' becomes true of the whole comms surface (fallback #1: a checklist line to edit `COMMS_ROLES` + the singular branch). **Gap 2 (BUILD-GATE):** a new director ships its own `<role>-context.md` + `<role>-state.md` carrying domain vocab → fails `test_genericity_guard` (whole-tree vocab scan) AND `test_manifest_accounting` unless registered as instance surface in `genericity_lists.py::INSTANCE_SURFACE_RELPATHS` (`charc-state.md` already is; `<role>-state.md` is its exact parallel). **Fix:** checklist line to register BOTH instance docs in `INSTANCE_SURFACE_RELPATHS`. **Gap 3 (WORDING):** §8 item 2 ('add the new director's authority to this charter') is a tension — `charc-charter.md` is CORE (manifest + genericity-guarded), so it CANNOT carry instance-specific authority vocab; the generic peer-director contract already lives in the kernel (§6 routing/custody + §5.1 state), and the new director's instance authority belongs in its own `<role>-context.md`. **Fix:** reword item 2 to split 'generic contract already in the kernel' vs 'instance authority in the instance doc' (keeps the kernel charter generic/upstreamable). **Net:** codify all 3 + single-source the director role set → §8 becomes followable straight to a green build with a working notice. | `.claude/hooks/user_prompt_submit.py` + `scripts/role_mail.py` (single-source `SINGULAR_INBOX_ROLES` + the checklist comment) + `genericity_lists.py` (`INSTANCE_SURFACE_RELPATHS`) + `charc-charter.md` §8 | OPEN |
| **B-13** | comms GUI: expanded message COLLAPSES on the 5-sec auto-refresh — a STALE pre-fix snapshot was used | The scaffold's comms GUI collapses an expanded message when the 5-second auto-refresh fires (operator, 2026-06-17). **This was fixed in swing WELL BEFORE the template was generated** → the scaffold's comms UI is a STALE pre-fix snapshot of swing's. **SYSTEMIC ROOT (the headline, not just this bug):** B-11 (missing dark-mode toggle) + B-13 (this 5-sec collapse) are BOTH symptoms of the SAME stale snapshot — the template's comms UI predates at least these two swing fixes and may miss OTHER post-snapshot comms-UI fixes (unknown unknowns). **Recommended fix (supersedes patching B-11/B-13 one-by-one):** RE-SYNC the scaffold's comms GUI from swing's CURRENT comms UI as the source-of-truth — genericized (transcribe the markup + the JS auto-refresh-preserves-expanded logic + the dark-theme toggle + whatever else diverged, the MECHANISM only, NOT swing styling/branding/content per the §5.1 contamination guard). A single re-sync catches the 5-sec-collapse fix, the dark-mode toggle (B-11), AND any other post-snapshot fix in one pass; the executing scaffold-revision CHARC diffs the scaffold comms UI vs swing's current comms UI to find the full delta + cites the swing fix commits. | the scaffold's comms-GUI templates + static (the Stage-1.5 GUI surface); source-of-truth = swing's CURRENT comms UI | OPEN |
| **B-14** | Inbox-unread check fires ONLY on `UserPromptSubmit` — add it to the `Stop`/close hook too (a continuous-ops bridge, absent a true idle-wake) | The Stage-1 comms unread notice (the `UserPromptSubmit` hook running the comms-unread script) fires ONLY when the operator submits a prompt → a message that lands WHILE the agent is working (an orchestrator `return_report`, an RD `query`) doesn't surface until the operator NEXT prompts. **Operator template-feedback (2026-06-21):** add the inbox check ALSO at the **`Stop`/close hook**, so a message that arrived during the turn surfaces at turn-close → as continuous ops as possible, absent an idle-wake feature for agents. **Design (CHARC):** a `Stop` hook running the same role_mail unread check; on unread>0 it BLOCKS the stop (`decision=block`) + injects the unread so the agent CONTINUES processing (a continue-on-unread); on unread==0 it allows the stop (idle). **Loop-safety (load-bearing):** the agent must `read`/ack (DRAIN) the inbox each cycle so the loop self-terminates when the inbox is empty; use the `stop_hook_active` guard to prevent runaway re-blocking. **Scope:** a Stage-1 unread-hook ENHANCEMENT — NOT Stage-2 push, NOT full Stage-3 timer idle-wake; it stays operator-SESSION-bounded + consistent with the comms taxonomy (autonomously processing `fyi`/`status`/`return_report` is in-lane; dispatch authority stays hand-carried). It is also a data point toward the Stage-3 gating (the operator wants continuous ops). **LIVE-NOW option:** the same change applies to swing's LIVE hook set (`scripts/comms_unread_hook.py` + a `Stop` entry in `.claude/settings.json`) — a small swing-hooks change NOT under the scaffold HOLD, with immediate value for the active director comms loop; operator may opt to enable it live now, separately from the template banking. | the scaffold's `.claude` hook set (`Stop` hook + the comms-unread script + role_mail) + the §8 peer-director-add checklist (the notice now fires on TWO events — fold into B-12's single-source) | OPEN |
| **B-15** | Comms routing does not reflect the role hierarchy (orchestrator routes co-equal to the operator, not UP through CHARC) | Kernel hierarchy is correct (operator→CHARC→orchestrators→implementers) but the COMMS model treats CHARC + orchestrators as co-equal operator-facing (both post `--to operator`; nothing routes the orchestrator's reports/permissions UP through its director) → an orchestrator reaches past CHARC to the operator, routing around the architecture owner (observed coa-chess arc-1). **Model (operator-stated):** the orchestrator's DEFAULT upward channel is CHARC (permissions + reports + ask-path through CHARC); it still gives info-updates to the operator + the operator may interact directly during execution; the boundary is NOT hard (orchestrator-judged direct escalation is fine). **Fix:** reframe `charc-charter.md` §6 + `orchestrator-context.md` "operator-relayed"/`decision_request` wording as "to the operator VIA CHARC" (transport via the director; the operator's authority unchanged). **+ dispatch-prose fix:** orchestrator-context step 4 "the operator dispatches an implementer" is substrate-wrong — the implementer is ORCHESTRATOR-spawned (the sub-agent return-report contract IS the mechanism); reword to "the orchestrator spawns the seam-2 cell as a sub-agent; the cell's final message IS its return report." | `charc-charter.md` §6 + `orchestrator-context.md` | OPEN (coa-chess 2nd feedback, `template-feedback-comms-hierarchy.md`; DOC-only → rides the G4 CHARC-direct charter batch) |
| **B-16** | Role identity lost on session RESUME (`HARNESS_ROLE` is launch-time-only) | A session's role lives ONLY in the launch-time `HARNESS_ROLE` env var (set by `launch_role.ps1`); a RESUMED session (`claude --resume` / crash-recovery) bypasses the launcher → `HARNESS_ROLE` unset → SILENTLY: the `UserPromptSubmit` unread-notice SKIPS (role-gated → the director stops being told about mail) + an orchestrator's registry heartbeat / per-generation inbox stop (role-gated). Bit coa-chess (a resumed CHARC silently missed an orchestrator message). **Fix:** do not hinge role identity on a volatile launch-time env var; persist it recoverably — a `.harness-role` file OR a session_id→role map the hooks fall back to when `HARNESS_ROLE` is absent — + document a resume command (`$env:HARNESS_ROLE='<role>'; claude --resume <id>`). Same failure CLASS as B-12 Gap-1 (notice goes dark) but a distinct CAUSE (resume drops the env var vs adding a role). | `scripts/launch_role.ps1` + `.claude/hooks/user_prompt_submit.py` (+ the registry hooks) + `docs/charc-bootstrap.md` | OPEN (coa-chess 2nd feedback, `template-feedback-comms-hierarchy.md`; CODE arc → bundles with the comms-code work, NOT the G4 doc batch) |

---

*Provenance: coa-chess germination, 2026-06-16..17. B-1/B-2 = orchestrator-before-CHARC launch + `.\scripts\` PATH; B-3 = CHARC fixed-opus/max (the one role-config exception to B-7); B-4 = git-history reconciliation; B-5 = peer-dissent self-correction; B-6 = superior-QAs-subordinate principle; B-7 = orchestrator model/effort genericity; B-8 = **BLOCKER** template-integrity gates trip on the seam-fills; B-9 = the **genericity-guard REDESIGN** (coa-chess's 4-point critique + reference impl `ec9856d`/`9aee81d`; scope to CORE + instance-surface seam; supersedes B-8's retire-fix); B-10 = registry-doc explicit-under-concurrency convention. B-11 = comms GUI dark-theme toggle (operator template-feedback, 2026-06-17 — not coa-chess); B-12 = §8 peer-director-add checklist incomplete vs code (coa-chess 4th feedback, 2026-06-17 — silent notice-hook role-set gap, instance-doc genericity registration, instance-authority-not-in-kernel wording); B-13 = comms GUI 5-sec-refresh-collapse + the SYSTEMIC stale-comms-UI-snapshot root (operator, 2026-06-17 — recommend RE-SYNC from swing's current comms UI; subsumes B-11). B-14 = inbox-unread check ALSO at the Stop/close hook (a continuous-ops bridge absent an idle-wake; operator template-feedback 2026-06-21; carries a live-now swing-hooks option). **B-15/B-16 = coa-chess 2nd feedback (`template-feedback-comms-hierarchy.md`, 2026-06-17):** B-15 = comms-routing-through-CHARC hierarchy + the orchestrator-spawns-the-cell dispatch-prose fix; B-16 = role-identity-lost-on-resume robustness. **The 5 coa-chess CHARC source docs (`coa-chess/docs/template-feedback-*.md`) consumed + dispositioned 2026-06-22:** `genericity`→B-9, `comms-ui`→B-11, `peer-director-checklist`→B-12, `comms-ui-expand-collapse`→B-13 (those 4 already captured — source docs banked, cross-ref only), `comms-hierarchy`→B-15+B-16 (NEW). All GENERIC-only fixes. Append-as-found.*
