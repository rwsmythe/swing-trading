# Research Director — Role Charter & Working Memory

**Audience:** A fresh instance of the strategic-evaluator role, bootstrapping with no prior conversation context (we spin up fresh instances as context fills).
**Companion docs:** [`docs/orchestrator-context.md`](orchestrator-context.md) is the parallel charter for the *orchestrator/implementer* role. This file is the charter for the *evaluator/advisor* role. They are different jobs — see §1.
**Created:** 2026-06-08. **Owner/operator:** Reid Smythe (the Principal/PM). Keep §5 (Session Log) append-only.

---

## 1. Role definition

**My role:** Director of Research / Head of Research, wearing a CIO / Chief Investment Strategist hat.
**The operator's role (Reid):** Portfolio Manager / Principal — holds the capital, the trigger, and the final decision.

**Mandate:** Evaluate the project's basic research, applied research, and operational (live-trading) results from a big-picture perspective, and recommend (a) future research directions — basic *and* applied — and (b) operational deployment of completed research. The north star is **how quickly the portfolio gains value** (risk-adjusted, compounding edge — not vanity P&L on a tiny base).

**Boundary (respect it):** I evaluate, judge deployability, and recommend. The operator decides and executes. I challenge both the research desk *and* the trader; I do not hold the trigger.

**In scope:** strategy assessment, research-yield judgment, expectancy/edge reasoning, capital-allocation logic, instrumentation critique, surfacing operator process errors.
**Out of scope:** writing production code myself by default (that routes through the orchestrator → implementer copowers workflow), and re-deriving the project's infra history (it's in CLAUDE.md + orchestrator-context.md).

---

## 2. Behavioral directives (the "behavior" to preserve)

These are why this instance is useful. Hold them.

1. **Blunt over sycophantic. Always.** The operator explicitly prefers direct, non-flattering feedback and asked to be called out on his own perceived mistakes. No hedging, no praise padding, no "great question." State the uncomfortable thing plainly. (Memory: `feedback_blunt_no_sycophancy`.)
2. **Evidence before assertion.** Ground every claim in the actual data — the live DB (`~/swing-data/swing.db`), the studies under `research/studies/`, the methodology refs. Cite numbers, dates, `file:line`. Distinguish "the system *can measure* X" from "X *is true*."
3. **Check intent before labeling a mistake — then be blunt.** Hard lesson from session 1 (2026-06-08): I read the operator's designed-loss hypothesis trades as undisciplined "chasing." They were a *pre-registered experiment* (§4). Verify a behavior wasn't a deliberate, documented choice before calling it an error. Once verified either way, say it straight.
4. **Separate the two unknowns.** "Does the strategy have edge?" and "does the operator execute it?" are different questions and are routinely entangled in the live record. Always disentangle them.
5. **Think in expectancy and edge, not dollars.** On a ~$1.5–2k base where deposits dominate, optimizing dollar P&L is premature. Optimize *demonstrated, statistically-credible edge* — that's the asset that compounds when capital scales.
6. **Big-picture first.** Don't get pulled into infra/plumbing detail. Ask "does this move portfolio performance?" and say so when the answer is no.
7. **Respect honest negative results.** "Rejection is a first-class outcome" (V2.1 governance). A clean null is a win, not a gap to paper over.

---

## 3. Established facts — the trading reality (as of 2026-06-08)

Sourced from a read-only query of the live `~/swing-data/swing.db` (schema v24). The real track record is **not in the git repo** (DB lives outside Drive per hard invariant); these numbers must be re-pulled to refresh.

- **16 trades** total (1 closed + 15 reviewed, 0 open), entries 2026-04-20 → 2026-06-01.
- **Win rate 12.5%** (2 of 16). **Expectancy −0.083R/trade** (sum −1.33R).
- **Realized P&L ≈ −$46** (fills/fees) to **−$73** (legacy ex-fees).
- **Visible balance: $1,200 start → $2,027.44 NLV.** Decomposes to `$1,200 + (−$72.74 trading) + $900 deposits`. **The portfolio is growing on deposits, not trading.** Operator adds ~$100/mo (memory `project_recurring_monthly_deposit`).
- Only **2 winners**: DHC +1.08R, VSAT +1.22R. Worst: SATL −0.88R. Mean hold ~7.4 days.
- **Only 1 of 16 trades was a pipeline A+ signal** (YOU, −0.07R scratch). 12 of 16 came from the watch pool / manual.
- Capital: ~$1.3–2k real vs a **$7,500 sizing floor** (`max(real, $7500)`, memory `project_capital_risk_floor`) → tiny positions (2–39 shares).

**Instrumentation note / open finding:** ~13 of 16 trades self-graded process "A" while expectancy is negative. The `mistake_tags`/`process_grade` surfaces **conflate designed-suboptimal hypothesis-test entries with genuine execution slips** (§4). Until that's fixed, the dashboards mislead — they misled me. Genuine slips do exist in the record (e.g. VIR carried `STOP_NOT_PLACED`); they need isolating from the by-design losses.

---

## 4. Established facts — the experiment that produced the record

**Critical context I got wrong on first pass.** The live trades were not the operator failing to follow a strategy. They were a **pre-registered hypothesis investigation plan v0.1**, frozen in migration 0008 on 2026-04-25, with target samples, decision criteria, and tripwire safeguards. Two purposes: (a) generate framework-evaluation evidence, and (b) bootstrap the operator's own learning curve (he started with no trading experience). The ~$70 loss was an acknowledged, well-justified cost of learning. Docs: [`docs/hypothesis-recommendation-backend-brief.md`](hypothesis-recommendation-backend-brief.md), [`docs/trade-hypothesis-label-brief.md`](trade-hypothesis-label-brief.md). Engine: `hypothesis_registry` table + `swing/recommendations/hypothesis.py` matcher/prioritizer/tripwire.

**The 4 frozen hypotheses:**

| ID | Name | Statement | Target | Decision criterion | Status (my read) |
|---|---|---|---|---|---|
| H1 | A+ baseline | A+ candidates produce positive expectancy | 20 closed | Mean R>0; Wilson-LB win rate >30% | **~1/20 — the money question, barely started** |
| H2 | Near-A+ extension | Watch failing ONLY `proximity_20ma` ≈ within 25% of A+ | 10 closed | Mean R within 25% of A+ mean | far from sample |
| H3 | Sub-A+ VCP-not-formed | Watch failing `tightness`/`vcp_volume_contraction` → **reliable losses validating discipline** | 5 closed | **Confirm NEGATIVE mean R** | most live trades; likely near/at sample — **bank it** |
| H4 | Capital-blocked | A+ except `risk_feasibility`, smaller position | 5–10 closed | Mean R positive | far from sample |

**Takeaway:** the negative R is mostly **H3 succeeding by design**, not execution failure. But H1 — the only hypothesis whose positive result would prove the project can make money — is starved (~1 of 20) and will not reach its decision criterion by hand at the operator's A+ signal rate. That gap is the central strategic problem.

---

## 5. Established facts — research yield & strategy

- **Net deployable research yield to date: ZERO rulesets, zero signal changes, zero criterion adjustments.** The W-pattern applied-research arc (9 rulesets × 6 substrates) closed 2026-05-27 with no robust positive expectancy; the near-win (Ruleset E, +1.22R N=71) collapsed to −0.80R on substrate-freshness re-eval. Study: `research/studies/2026-05-27-applied-research-arc-closure.md`. (Memory `project_applied_research_arc_2026-05-27`.)
- **Minervini exemplar-recall study (2026-06-09, diagnostic-only):** screening recall **0.90 of screenable exemplars** when timed by window-sweep (vs 0.25 single-session, by design). 7/27 exemplars un-screenable (young names <~221 bars). The Stage-2 8/8-TT detector gate is the binding constraint *and* the source of specificity. **This earns trust in the A+/watch funnel as a sensitivity-validated filter.** Study: `research/studies/2026-06-08-minervini-exemplar-recall.md`.
- **Minervini primary-base (young-name) recall arc (2026-06-09, CLOSED, n≈3 proof-of-concept):** the Ch.11 primary-base screen **mechanism-validates and is specific** — fired on 3/3 evaluable sub-floor documented primary bases (AMZN-1997, BODY, DKS) at their documented emergences with **0% same-ticker control fire**. But the load-bearing result is a **negative**: the YHOO positive control (a known sufficient-history primary base) was **rejected on depth** (its correction exceeded the graduated depth cap), so the **depth ladder is miscalibrated** and would miss real bases. **Not deployable** (promotion gate N/A) — works-in-principle + selective, thresholds need tuning; forward = **corpus expansion to calibrate the depth ladder** (YHOO is the first calibration signal). Full copowers cycle, one `swing/` change (CLI reg `66fcd7c9`), 7373 tests green; study committed `429e780a` (`research/studies/`). QA'd PASS by this role.
- **Basic research:** trend-template + VCP structure the A+ gate; **A+ supply is genuinely sparse and capital-gated** (~5 A+ on SPX+NDX at operator capital; trend-template binds 34–46%). Universe-shape effects (2–5×) are real but confounded. Earnings-proximity exclusion was a clean null.
- **Strategy (governing):** Minervini/Qullamaggie/DST trend-following swing method — bifurcated research vs operational branches per `reference/Future Work/2026-04-23-bifurcated-strategic-implementation-proposal-v2.1.md`. Research *validates/rejects* methodology; it doesn't invent edge. Promotion gate = method record + parity + **shadow mode (≥30 signals / ≥6 months)** before production (V2.1 §VII). Performance is treated as a *lagging* indicator.
- **Forward roadmap:** Phase 16 (Observability & Logging — pure plumbing, won't move performance). B-1..B-8 applied-research backlog is **temporal-log-gated** (needs N≥100 patterns; months away at current pace). The v22 temporal log accrues detector predictions nightly (#23 pool-widening feeds ~83× watch population).

---

## 6. Standing recommendations / open theses (continue this line)

Prioritized by impact on portfolio growth. Re-validate against fresh data each session.

- **P1-NOW — ✅ FULLY LIVE 2026-06-10.** Shipped (merge `54bd8b94`, migration 0026, v26) AND both operator gates PASSED: the live DB migrated v25→v26 (backup `swing-20260609T234259.db`; 5 rows, history seed = 1) and the Arc-5 5b nightly-drumbeat gate passed on run #98 — **funnel 255 detections → 51 signals → 0 unattributed (`matched_no_hypothesis` 0), 51/51 attributed to `Broad-watch baseline`**, scorecard honestly zero (trigger rate 0/36; no breakouts yet). The spec's acceptance criterion (attribution-routes-correctly, not numbers) is MET in production. **Measurement is now autonomous; the standing posture is STOP ENGINEERING + market time.** Read cadence: weekly funnel glance (unattributed stays 0; trigger rate off 0), monthly evaluator read; the pre-registered decision point is N≥30 priced shadow signals. Original decision record follows. The shadow-expectancy engine is **SHIPPED + corrected** (merged `31e7441c`, runs turnkey) but prices ≈zero: **100% `matched_no_hypothesis`** — the active narrow cohorts (H2 watch∩proximity_20ma-only / H4 watch∩risk_feasibility-only) don't cover the broad #23-widened watch pool the temporal log contains; plus zero aplus and no breakouts yet. Adding a `bucket==watch`, any-miss-set hypothesis (governance amendment via a new migration) lets the engine price the population the log ACTUALLY contains — which is also the operator's actual trading practice (12/16 live trades were watch). Highest-leverage next step; **necessary-not-sufficient** (still needs the log to mature for breakouts). The engine is DONE; the measurement now awaits this registry decision + market time, not more engineering. (Forensics: §7, 2026-06-09 entries.)
- **P1 — Shadow-expectancy engine (highest leverage; the centerpiece).** Apply the *full operational ruleset* (Minervini entry timing → Day-3–5 partial → 10/20/50-MA trail → LoD stop) to **every emitted A+/watch signal**, forward-walk to a realized R using the existing OHLCV archive. This is the *only* way to drive H1 to its 20-sample decision criterion in human-reasonable time (signal-pace, not trade-pace), it decouples strategy-edge from operator execution, and it plugs into the hypothesis matcher/registry the operator already built. It *is* the "shadow mode" the governing strategy already endorses. **This is the next arc I'd commission, ahead of all of B-1..B-8.** **STATUS (2026-06-09):** spec brainstormed + Codex-converged through 6 rounds (3 crit / 20 maj / 13 min, all resolved, zero accepts), spec `03bc562f`. **SHIPPED to `main` 2026-06-09** (merge `e8d03036`; plan `2ee03c1b` — writing-plans converged 6 Codex rounds, executing-plans converged 3 Codex rounds; orchestrator QA PASS; 7504 fast tests green; L2 held — one CLI change, no schema). **NOT YET RUN against the live temporal log** — the operator running `swing diagnose shadow-expectancy --db <live>` produces the first H1 expectancy scorecard; until it runs + accumulates signal-pace samples, H1 stays unanswered (the engine is the instrument, not yet the measurement). Key design locks: read-only consumer of the immutable v22 temporal log (no mutable-archive drift); **single fixed entry + exit-only `[realistic, favorable_reprice]` bracket**; core + Day-3–5 partial ruleset; four survival-aware censoring scenarios (no lower-bound claim); two-level denominator funnel; one `swing diagnose shadow-expectancy` CLI, no schema. Transcript: `.copowers-findings.md`.
- **P0 — Fix the tuition-vs-error instrumentation.** Add a field/flag distinguishing "deliberate hypothesis-test entry (sub-optimal by design)" from "genuine discipline violation." Without it, `process_grade` + `mistake_tag_frequency` conflate the two and every future analyst (human or AI) misreads the record — as I did.
- **P2 — Let the temporal log mature; pursue only bounded funnel-wideners.** Set a calendar checkpoint (~6 months) for B-1..B-8 rather than forcing it. The young-name primary-base screen now **exists** (SHIPPED + CLOSED 2026-06-09, §5): mechanism-validated + specific, but the **depth ladder is uncalibrated** (the YHOO false-negative) — so it needs **corpus expansion to calibrate** before any recall claim generalizes, and remains *unvalidated for edge* until the P1 engine can price it. Do not treat "shipped" as "deployable." Also queued: **Stage-2 detector-gate ratification** (small, scoped) and **P0** below.
- **Do NOT:** widen VCP/criteria thresholds for more signals (W-arc proved classification gains ≠ closed-trade gains); increase position size or trade frequency while expectancy is unproven/negative; let Phase 16 plumbing absorb the cycles P0/P1 deserve.

**Capital framing to keep repeating to the operator:** at this base, dollar growth is a deposit story. The near-term objective is not "make money faster" — it's "establish a statistically credible positive expectancy on clean, rule-followed signals (real or shadow)." That edge is what compounds once capital scales.

---

## 7. Session log (append-only)

### 2026-06-08 — Session 1 (role established)
- Operator commissioned the strategic-evaluator role and this charter.
- Delivered the big-picture evaluation (§3–§6). **Key miss, corrected by operator:** initially framed the 16 live trades as undisciplined "chasing"/execution failure. Operator corrected — they were the pre-registered 4-hypothesis program (§4); negative R on H3 is success-by-design; cost-of-learning was acknowledged and worth it. Retracted and recalibrated; the correction *strengthened* the P1 shadow-engine thesis (it's the way to answer H1, which is starved).
- Operator confirmed strong preference for blunt, non-sycophantic feedback and explicit call-outs of his own mistakes (saved as memory).
- Open thread handed forward: brainstorm the P1 shadow-expectancy engine; spec the P0 tuition-vs-error instrumentation fix.

### 2026-06-09 — Session 1 (cont.): P1 brainstorm converged; young-name closed
- **P1 shadow-expectancy engine brainstormed end-to-end + Codex-converged** (6 rounds, 3 crit / 20 maj / 13 min, all resolved, zero accepts; spec `03bc562f`). Two structural wins from the review: the original `[realistic, optimistic]` bracket was *unsound* (negative R denominator on gap-up entries) → redesigned to a fixed single entry + exit-only `favorable_reprice` bracket; the multi-detection collapse hardened (canonical-detection rule + identical OHLC/`triggered_open` + input validation + two-level funnel + `unattributed` bucket). **Parked at the writing-plans gate** per operator (young-name WIP first). Transcript `.copowers-findings.md`.
- **Young-name primary-base recall arc CLOSED** (a separate orchestrator the operator ran in parallel; I QA'd the return — **PASS**: zero co-author trailers across the arc, the single `swing/` change is the CLI reg `66fcd7c9`, 7373 fast tests live-verified by operator). Finding in §5. **My evaluator read:** mechanism-validated + specific but **depth-uncalibrated → zero near-term performance impact**; an unvalidated funnel-widener that routes into P1 for eventual edge-pricing — consistent with the program's pattern (rigorous + honest + no deployable edge yet). The cumulative signal is unchanged: the bottleneck is *measuring edge* (P1), not *adding screens*.
- Phase 16 (Observability) Arc-1 brainstorm was commissioned in parallel (`2160bc92`); pure plumbing, **no shadow-engine impact** (verified: Phase 16 touches none of the temporal log / candidates / matcher / engine read-path).

### 2026-06-09 — Session 1 (cont.): P1 SHADOW-EXPECTANCY ENGINE SHIPPED
- **Writing-plans converged** (6 Codex rounds; 4 crit / 17 maj / 8 min, all resolved; plan + spec reconciliations `2ee03c1b`). Round 1 caught **four real correctness bugs** — wrong initial-stop basis, per-leg R double-counting partials (3.2R vs 1.6R), four-scenario means dropping closed trades, pivot-only collapse consistency; rounds 2–6 hardened the funnel contract to airtight (reconciliation invariant + raise-on-malformed both sides).
- **Executing-plans dispatched to an independent implementer** (brief `2348be57`, committed before the inline prompt). Ran copowers:executing-plans in an isolated worktree off v24; converged 3 Codex rounds (0 crit / 5 maj / 1 accepted minor). Notable catch: `validate` still gated eligibility on `candidate.initial_stop` (a field the simulator never uses) → removed, *strengthening* the lock.
- **Orchestrator QA: PASS + MERGED** (`e8d03036`). L2 held (only `swing/cli.py`), zero co-author trailers across 19 commits, no schema from the arc. `main` had advanced to **v25 (Phase 16 Arc 1)** off the v24 branch — the 3-way merge auto-resolved `swing/cli.py` cleanly. Binding suite on the merged head: 7503 passed + the **1 pre-existing** minervini em-dash failure (independently confirmed failing on clean `main` pre-merge); zero new failures, all 95 harness tests green. Operator-authorized in-scope fix (ASCII-transliterated the young-name study doc, `df402810`) → **7504 passed / 0 failed**. Worktree removed, branch deleted.
- **P1 is SHIPPED but NOT YET RUN.** Next move belongs to the operator: run `swing diagnose shadow-expectancy --db <live>` to produce the first H1 scorecard, then let it accumulate signal-pace samples. That run is the measurement the whole research program has been routing toward.

### 2026-06-09 — first live run of P1: BLOCKING join bug + H1-substrate reality
- Pushed the session's work (origin/main synced) and ran the engine against the live DB. **Invocation gap:** bare `swing.exe` fails `ModuleNotFoundError: research` (research/ isn't an installed package + repo root not on sys.path); ran with `PYTHONPATH=<repo root>`. Affects ALL research diagnostics (pre-existing pattern), worth a fix.
- **Result: 42 unique signals, 100% → `no_canonical_detection`, ZERO priced trades, empty scorecard.** The funnel honesty surfaced it cleanly (no fabricated result).
- **Root cause (blocking design bug):** the canonical-detection rule `detection.pivot == candidate.pivot` is a FALSE PREMISE. `detection.pivot` (structural_anchors.evidence.pivot_price) is the PATTERN-geometric pivot — per pattern class, often `0.0` (double_bottom_w / cup_with_handle); `candidate.pivot` is the SCREENING pivot. Different quantities (WULF 25.86/0.0 vs 27.47), never coincide. Synthetic fixtures forced them equal → passed 6+3 Codex rounds; only the live run falsified it (the **synthetic-vs-real-emitter drift** gotcha — already in CLAUDE.md). **Fix:** re-adopt the R3 direction — recompute entry from `candidate.pivot` over the shared frozen forward bars (all (run,ticker) detections share the OHLC; no look-ahead since forward obs are post-detection). A focused correction arc.
- **Two substrate realities the run exposed:** (1) all 42 signals are `watch` — ZERO aplus — so even fixed, the engine prices H2/H3/H4, NOT H1; **H1 stays gated on rare aplus fires.** (2) The log is tiny/young (4 runs, 2026-06-05→09, 219 forward obs) → ~5-session walks → nearly everything open-at-horizon; needs months. **Refined thesis:** P1 accelerates the WATCH hypotheses fast; H1 accrues only as aplus fires (still faster than hand-trading, but NOT signal-pace as I'd framed it). Correcting my own earlier overstatement.

### 2026-06-09 — entry/join correction spec: Codex-converged + QA PASS
- Dispatched the correction brainstorm to a fresh instance (brief `c84369cc`); it returned a Codex-converged spec (`6fd664f7`; 3 rounds, 7 major resolved, `NO_NEW_CRITICAL_MAJOR`). **QA PASS** — independently re-verified the load-bearing claim against the live DB: no-look-ahead holds (**0/219** obs violate `observation_date > data_asof_date`; first obs is ON `detection_date`, which correctly corrects my brief's wrong "strictly after detection_date" framing). Trailers clean.
- **Design (surgical, sound):** entry recomputed as first forward bar `high >= candidate.pivot`; canonical detection demoted to bar-source-only (longest chain, prefix-invariant-checked); removed the falsified `detection.pivot==candidate.pivot` match + `no_canonical_detection` + a SECOND latent bug (`inconsistent_trigger_state` — per-pattern pivots legitimately differ, 8/42 groups); new `no_candidate_pivot` reason. Honest V1 limitation: no close-confirmation admits intraday-touch entries → realistic arm slightly optimistic, mitigated by a reported `entry_bar_weak_close` diagnostic. Invocation-gap fix folded in → ALL research diagnostics now turnkey from the installed entry point. Spec: `2026-06-09-shadow-expectancy-entry-join-correction-design.md`.
- **Substrate reality (restated honestly in the spec):** even fixed, the current log prices ≈zero — every `candidate.pivot` sits above its forward highs; nothing has broken out in the thin 2–3 session windows. The fix unblocks the MECHANISM; priced samples accrue as the log matures + breakouts occur. Success criterion correctly reset to *funnel-routes-correctly + a breakout golden-fixture prices end-to-end*, NOT immediate H1 numbers. **Next: writing-plans.**

### 2026-06-09 — correction writing-plans: Codex-converged + QA PASS
- Dispatched (brief `67a4fe30`); returned a Codex-converged TDD plan (`b59541c7`; 4 rounds, `NO_NEW_CRITICAL_MAJOR`). **QA PASS** — verified on disk: trailers clean; L2 held (only `swing/cli.py`); the plan's edits target the REAL shipped signatures (`collapse_detections`, `_DetView`/`_entry_and_forward`, `validate_candidate_levels(*, pivot)`, the three constants tuples + the to-be-deleted `no_canonical_detection`/`inconsistent_trigger_state` branches all confirmed present). Codex verified against the live DB + shipped code (the discipline the original chain lacked) and caught real defects (a look-ahead test JOIN on a non-existent column; the BULZ fixture's invalid OHLC). 7 tasks; honest caveats accepted (Task-3 collapse/run signature coupling; testkit stays v24). **Next: executing-plans.**

### 2026-06-09 — correction SHIPPED + the attribution-coverage finding
- Executing-plans returned (brief `5a5e3b8c`), Codex-converged round 2, **QA PASS + MERGED `31e7441c`** (L2 held — only `swing/cli.py`; clean 3-way merge onto Phase-16-Arc-2 main; zero co-author trailers; **7516 fast tests green** on the merged head). Codex caught a real honesty bug (attributed zero-observation signals mis-routed to `never_triggered` → `missing_observations`). Worktree removed. Re-ran the engine **turnkey** — the invocation fix is confirmed (runs from the installed entry point without `PYTHONPATH`).
- **The fix WORKED + surfaced the next binding constraint.** Live funnel: 210 detections → 42 unique signals, **`no_canonical_detection` wall GONE**, all 42 join+collapse+reach attribution — but **100% `matched_no_hypothesis`**. Bottleneck moved from the collapse bug (fixed) to **attribution coverage**: all 42 are `watch` (zero aplus); the active hypotheses are NARROW cohorts (H1 aplus / H2 watch∩proximity_20ma-only / H4 watch∩risk_feasibility-only) that don't cover the BROAD #23-widened watch pool. Honest output, not a defect.
- **Three independent reasons the engine prices ≈zero today** (all substrate/coverage, none a defect): (1) attribution coverage gap (above); (2) no breakouts — every `candidate.pivot` sits above its forward highs in the thin 2–3 session windows; (3) no aplus → H1 starved.
- **My recommendation (research-director call):** add a **broad-watch-baseline hypothesis** (bucket==watch, any miss-set) so the engine prices the population the log ACTUALLY contains — which is also the operator's actual trading practice (12/16 live trades were watch). Highest-value next step (the narrow cohorts almost never occur); answers "does the widened watch pool have mechanical edge?" without risking capital. Routes through V2.1 §VII governance (a registry amendment / new migration). Caveat: necessary-not-sufficient — even with coverage, prices ≈zero until the log matures (breakouts). **The instrument is DONE; the measurement now awaits a registry decision + market time, not more engineering.**

### 2026-06-09 — Session 2 (fresh instance): operator GO — both arcs commissioned
- Operator approved P1-NOW and added a second commission: the engine must run **automatically in the operational drumbeat**. Division of responsibility (operator-set): research director finishes tool development (the hypothesis amendment arc) + recommends placement; the **Phase 16 orchestrator implements** the operational integration as a new Phase 16 arc.
- **Pre-dispatch grounding surfaced two load-bearing design hazards** (verified in code, not carried from the handoff): (1) the engine's attribution is a thin wrapper over the PRODUCTION matcher (`research/harness/shadow_expectancy/attribution.py` → `swing/recommendations/hypothesis.py:match_candidate_to_hypotheses`), and the engine routes **>1 match → `multi_match` → unattributed** (`run.py` ~L121) — a naive `bucket==watch` rule would cannibalize H2/H4 attribution the moment their signals appear → the brief mandates **fallback semantics** (baseline = watch not otherwise hypothesized); (2) the same matcher feeds the LIVE recommendation surfaces (`hypothesis_prefill.py`, `dashboard.py` ×2) — an always-matching hypothesis would flood them and outrank H2/H4 via distance-to-target → the brief proposes opt-in containment (`include_baseline=False` default; shadow engine passes True). Also verified live: registry v25 has H1/H2/H4 active, H3 `closed-target-met`; matcher's documented fifth-hypothesis extension point at `hypothesis.py` ~L124; latest export (`20260610T063533Z`) re-confirms 42/42 `matched_no_hypothesis`.
- **Briefs authored + committed:** `docs/broad-watch-baseline-hypothesis-brainstorming-dispatch-brief.md` (migration 0026 v25→v26; proposed pre-registration: name 'Broad-watch baseline', target 30 shadow-priced, baseline criteria with both outcomes pre-committed; tripwires 5/5.0 schema-mandated) and `docs/phase16-shadow-expectancy-drumbeat-integration-commissioning-brief.md` (Phase 16 **Arc 5**). **Placement recommendation:** new best-effort step `shadow_expectancy` AFTER `_step_export`, BEFORE `complete` (data final after `pattern_observe`; last slot protects operator-facing outputs; lease serialization + Arc-1 step-timing/pipeline.log observability for free); **subprocess** of the installed CLI, not an in-process `research/` import (bifurcation boundary + crash isolation); rejected: pre-run (one session stale) and external scheduler (no lease coordination, drift). Arcs are independent — land in either order.
- Next: operator dispatches both inline prompts; research director QAs the brainstorm returns.

### 2026-06-09 — broad-watch brainstorm returned: QA PASS (one process flag) → writing-plans dispatched
- **QA PASS** on the brainstorm return (spec `99e0608f`, trailers clean). Independently verified — not carried from the self-report: (1) the live-DB cohort composition **reproduces exactly** (my own read-only query: 400 watch rows, H2-exact=1, H4-exact=0, dominant sets 110/61/60/36/15/15/10 matching the spec §3.1 table); (2) `tier.py` IS allowlist-locked (`TAXONOMY_COHORTS` 4-tuple + `__post_init__` raises on ≠4 — the R1-C1 inversion is real, the no-touch decision sound); (3) `PHASE16_PRE_MIGRATION_EXPECTED_TABLES` aliases the v24/B7 set → the spec's R2-M2 expected-tables correction (`| {"pipeline_step_timings"}`) is correct; (4) testkit pins `target_version=24` and `test_no_match_returns_empty` (watch+`{orderliness}`→`[]`) WILL flip under the wrapper change — the R2-M3 catch is real; (5) `closed-target-met` appears in migrations only as a CHECK enum value → the spec's fixture-vs-live H3-status distinction (fresh DB = 5 active, live DB = 4 active) is correct and load-bearing for the #11 sweep; (6) `hypothesis_status_history` + the open-interval partial-unique index exist in 0017 → the §2 history-seed requirement is real.
- **Design verdict:** the spec is STRONGER than my commissioning brief — it found the registry-reading surfaces my containment framing missed and split them correctly (progress/process accept the 5th cohort coherently because live-labeling is permitted; tier/deviation self-contain via the allowlist), and rejected the `measurement_only` column with sound YAGNI reasoning (operator-confirmed). Fallback semantics implemented as a structural gate on `not matches` (order-robust), exactly the §2.2 protection.
- **One process flag:** `.copowers-findings.md` persisted Rounds 2–3 + the convergence verdict but left **Round 1's response section empty** (prompt only) — violates `feedback_implementer_persist_codex_responses`. Convergence itself is evidenced (R3 `NO_NEW_CRITICAL_MAJOR` persisted; R1 findings reconstructable from the spec's inline R1-C1/R2-M* citations), so non-blocking — but the writing-plans brief explicitly mandates all-rounds persistence.
- **Writing-plans dispatched:** brief `docs/broad-watch-baseline-hypothesis-writing-plans-dispatch-brief.md` committed; spec is BINDING; locks restated (tier.py untouched; frozen rows; no funnel changes; no column). Phase 16 Arc 5 (drumbeat) accepted by its orchestrator in parallel (`36a7a955`) — independent, no coordination needed.

### 2026-06-09 — writing-plans returned: QA PASS (no flags) → executing-plans dispatched
- **QA PASS** on the plan (`75b6d291`, 821 lines, trailers clean; Codex R1 0-crit/0-maj/2-min → R2 clean, BOTH rounds' full responses persisted including R1 — the prior process flag was addressed). Independently re-verified every material implementer claim: (1) `test_db_v8.py` DOES migrate to head via `ensure_schema` and pins `EXPECTED_SCHEMA_VERSION == 25` at `:112` — the spec's STAY-4 premise was wrong, the implementer's escape-clause bump is correct; (2) the version-pin family is real (~40 grep hits across 15+ migration-test files) — the plan's expansion beyond the spec's §7.1 table is justified and necessary for a green landing; (3) `run_migrations` reads `_current_version` ONCE (`db.py:1153`) and passes it to every gate → the 0025-clamp inversion analysis (`:107` 25→26, broad-watch gate inert on a v24 walk) is correct; (4) `test_repos_hypothesis.py` `== 4` (rows + active) → 5 under fresh-DB-H3-active; (5) Task-3 fixture logic sound (the §9.1.1 fixture correctly moves to the real `{adr}` shape because the v26 testkit seeds H3 ACTIVE — a sharp catch the spec missed; H2-exact anti-cannibalization fixture asserts `[H2]` exactly). Task ordering verified safe (Task-2 testkit bump can't fire the baseline pre-Task-3: attribution still defaults `include_baseline=False`).
- **Executing-plans dispatched:** brief `docs/broad-watch-baseline-hypothesis-executing-plans-dispatch-brief.md` committed. Worktree off main HEAD; migration-number collision check at branch time (Arc 2/Arc 5 in flight); live DB read-only (the v25→v26 live migration fires post-merge on the operator's first `ensure_schema` touch, backup-gated); implementer returns WITHOUT merging — research director QAs the worktree, merges, owns the post-merge suite run.

### 2026-06-09 — executing-plans returned: QA PASS → MERGED `54bd8b94`; P1-NOW SHIPPED
- **QA PASS on the worktree** (branch `bwb-impl`, base `599a0444`, 4 commits): trailers all `[]` (my own audit); production diff = EXACTLY the four planned files (lock-grep verified); migration 0026 text matches the frozen spec §2 verbatim; the two-phase gate (`if include_baseline and not matches:` + active+present check) and the single opt-in caller verified in the diff. Codex evidence on disk (`.codex-review/`, both rounds incl. the 9.4k-line R1; converged `NO_NEW_CRITICAL_MAJOR`); notable: the implementer REJECTED Codex's terse R1 clean verdict and forced a deeper R2 trace — the right instinct. All three plan deviations independently verified sound (the `test_no_schema_change_v3` `<=26` guard; the index-overview headline "5" — `_extract_hypothesis_progress` at `index.py:251-256` counts the registry-driven progress card, NOT the tier allowlist, so the old "4 TAXONOMY_COHORTS" comment was stale; the 5th name appended to the ordered names list).
- **MERGED `54bd8b94`** (clean ort merge onto `ca671850` — Arc 5 had landed underneath; zero file overlap as the implementer reported). **Binding suite on the merged head: 7599 passed / 0 failed / 3 pre-existing skips** (read off the actual run); `ruff check swing/` clean; pushed to origin. Worktree removed (Codex evidence preserved at `.codex-review-bwb-executing/`), branch deleted. CLAUDE.md updated to schema v26 (line 3 + baseline).
- **The arc is complete: instrument + hypothesis + drumbeat all SHIPPED.** Live state: the live DB is still v25/4-rows (verified untouched, read-only probe) — the v26 migration + backup fires on the operator's next write-path touch; Arc 5's nightly step then attributes the watch pool to the baseline each session. Two operator gates remain (the v26 live migration witness + the Arc-5 5b live-run go), then it's market time, not engineering.

### 2026-06-10 — both operator gates PASSED: the measurement program is FULLY LIVE
- Operator reported the Arc-5 5b live-run go complete. **Independently verified** (read-only probes, not the report): live DB at **v26**, 5 registry rows (`Broad-watch baseline` id=5 active), exactly 1 status-history row (the migrate-twice invariant holding in production), backup snapshot `swing-20260609T234259.db` landed. Run #98 artifact (`shadow-expectancy-20260610T095430Z`): **255 detections → 51 unique signals → 0 unattributed** — the `matched_no_hypothesis` wall (100% two sessions ago) is GONE; all 51 attribute to the baseline. Scorecard honestly zero: trigger rate **0/36**, all four censoring scenarios n=0, suppression floors annotating correctly, `entry_bar_weak_close=0`. Exactly the spec's predicted first-live shape.
- **Program state:** temporal log → engine → hypothesis → nightly drumbeat, all live and autonomous. Engineering on the measurement chain is COMPLETE. The remaining research-director-lane engineering item is **P0 (tuition-vs-error instrumentation)** — still open, still worth commissioning when the operator wants it. The run-#98 perf finding (`evaluate=522s` ≈91% of wall) is Phase 16's lane (commissionable on data per CLAUDE.md); it does not affect measurement correctness.
- **Posture:** stop engineering; weekly funnel glance + monthly evaluator read; decision point N≥30 priced shadow signals; ~6-month calendar checkpoint for B-1..B-8 stands (P2).

### 2026-06-10 — P0 commissioned; the labeling question answered
- **Operator commissioned P0** (tuition-vs-error instrumentation). Brief authored + committed: `docs/p0-tuition-vs-error-instrumentation-brainstorming-dispatch-brief.md`. Core design direction: a trade-level `entry_intent` nullable enum via migration 0027 (v27), set-at-entry/correctable-at-review, operator-driven backfill of the 16 historical trades, surfaces FACETED not regraded (the semantic contract: `process_grade` measures execution-given-intent, so the historical "A on designed losers" grades are correct — the fix is display conflation). Orthogonality lock: a designed trade with a slip (VIR `STOP_NOT_PLACED`) still counts as a violation. Measurement chain untouched. The Phase 16 orchestrator is running the run-#98 perf arc in parallel — not my lane.
- **Labeling answer (operator Q):** yes, `Broad-watch baseline` on live watch trades is MANUAL — the entry-form prefill (`lookup_active_recommendation_label` → matcher default `include_baseline=False`) will never suggest it, by the containment design (the frozen 0026 notes: "live recommendation surfaces never surface it"). Operational rule given: take the narrow prefill when one fires (H1/H2/H4 — narrower attribution wins, matching the fallback complement), else hand-type `Broad-watch baseline` (exact name, then optional ` ...`/`; ...` suffix per the 3-rule prefix contract). If manual proves annoying, opting the ENTRY-FORM PREFILL ONLY into the baseline is a one-line + tests mini-amendment — but it touches the frozen notes' "never surface it" language, so it routes as a deliberate governance edit; recommended living with manual at the current ~trade-per-week rate.
- **CORRECTION (2026-06-10, web-GUI mechanics):** my "hand-type the label" guidance was WRONG for the web — the entry form's `hypothesis_label` is a display-only span + HIDDEN input, server-resolved at form-render via `lookup_active_recommendation_label` (`trade_entry_form.html.j2:147-148`, `view_models/trades.py:542`; the V1 server-stamp discipline). No free-text hypothesis field and no post-hoc label editor exist in the web. With the prefill contained (`include_baseline=False`), web-entered watch trades persist `hypothesis_label=NULL`. The ONLY manual labeling path is the CLI `swing trade entry --hypothesis "Broad-watch baseline"`. Identification tell in the GUI: a watch name absent from the hyp-recs panel / entry form showing "Hypothesis: (none)". Consequently the prefill mini-amendment (opt `lookup_active_recommendation_label` ONLY into the baseline; fallback gate gives narrow-first for free) is now RECOMMENDED rather than optional — it is the only way the operator's web-first workflow yields labeled broad-watch trades; routes as a documented micro-arc separate from P0 (it amends the 0026 spec's prefill-containment letter; argument: the entry-form prefill is an ATTRIBUTION surface, not a recommendation surface). Awaiting operator go.
- **Labeling-effectiveness requirements elicited + Phase 16 arc commissioned (2026-06-10):** operator proposed pairing the missing watchlist-tagging feature with the labeling fix. Four operator decisions captured via structured Q&A: (1) tag shape = **pin flag + note** (not free-form tags, not an enum); (2) label source = **matcher-driven** (the prefill amendment; tags do NOT drive labels — labels always mirror shadow attribution); (3) pin semantics = **blocks removal only** (streaks keep counting; unpin → accumulated state applies); (4) the prefill amendment **folds into the Phase 16 arc** (not a separate micro-arc), with the research-director governance authorization granted in the brief (the 0026 spec §5.1 letter is amended by a dated addendum re-classifying the single-ticker entry-form/CLI prefill as an ATTRIBUTION surface; dashboard recommendation surfaces stay contained, regression-tested). Brief: `docs/phase16-watchlist-pin-and-labeling-effectiveness-commissioning-brief.md` (binding R1–R6; ground truth verified in code: no tag columns exist on `watchlist`; the absent-from-candidates skip at `service.py:70-71` flagged for the stale-pinned-row design). Coordination noted: P0 (in flight) and this arc both touch the entry form; migration numbers taken at branch time.
- **P0 brainstorm returned: QA PASS → writing-plans dispatched (2026-06-10).** Spec `0c1efe71` (520 lines, trailers clean; Codex 4 rounds — R1 6 maj / R2 1 / R3 1 / R4 clean — ALL rounds persisted incl. R1). Independently verified: the §1 live-record table matches my own read-only query row-for-row (16 trades; 13 A / 2 B / 1 unreviewed; VIR carries `NO_STOP`+`STOP_NOT_PLACED` at grade B; `disqualifying_process_violation=0` on ALL 15 reviewed rows — the existing flag does no separating work, load-bearing for the column's justification); the `MISTAKE_TAGS` category premise behind the §7.1 always-on execution-discipline panel holds on disk (`NO_STOP` in `risk`, `STOP_NOT_PLACED` in `reconciliation`, `CHASED`/`NO_SETUP` in `entry` — the panel includes real slips, excludes probe-noise); `FAILURE_MODES`-in-models.py colocation + the 0024 ALTER template + all §7 file:line anchors land exactly. Design judgment: the spec's probe-vs-practice reframe (D1) is RIGHT and better than my brief's framing; the Codex-driven execution-discipline panel (separate unfiltered denominator) is the real orthogonality guarantee — accepted as V1 load-bearing. One reviewed-and-accepted boundary: the panel covers risk∪reconciliation only (management-category violations like `MOVED_STOP_AWAY` stay facet-visible but not panel-pinned) — defensible, matches the record's actual failure family, V2-widenable. Writing-plans brief: `docs/p0-tuition-vs-error-writing-plans-dispatch-brief.md` (spec BINDING; D1–D6/L1–L7 closed; migration-number check at executing time — Phase 16 perf + Arc 7 may contend for 0027).
- **P0 writing-plans returned: QA PASS → executing-plans dispatched (2026-06-10).** Plan `92ccf13d` (1763 lines, trailers clean; Codex R1 2 maj [the soft-warn `is not None` discriminator — a real truthiness bug that would have masked an explicit "Unclassified"; the normalized PGT CSS map] → R2 clean; BOTH rounds persisted incl. the 1.5MB raw R1). Independently verified the implementer's beyond-spec claims: (1) `failure_mode` is absent from BOTH `INSERT INTO trades` column lists (`repos/trades.py:216,280`) → the new v27 INSERT branch + four-projection-era resolution is correct (failure_mode is review-only; entry_intent is set-at-entry — a genuine structural difference from the 0024 template); (2) three projection eras confirmed on disk, failure_mode at `_row_to_trade` index 54; (3) the no-hypothesis-count-sweep simplification is sound (0027 adds a column, not a registry row). Judgment calls ruled: Task-1 size accepted (forced by #11 atomicity + the pin sweep — the 0026 precedent); the two-call execution-discipline fetch accepted as primary (≤16-row cohort, exercises the spec-mandated predicate); precedent-derived form-VM internals accepted with executing-time re-anchoring mandated. Executing brief: `docs/p0-tuition-vs-error-executing-plans-dispatch-brief.md` (worktree; migration-number preflight — Phase 16 perf/Arc-7 may contend; live DB read-only; implementer returns WITHOUT merging).
- **Arc-7 commission corrected + pre-Codex spec QA'd (2026-06-10).** Operator surfaced a miscommunication in MY commissioning brief: R2's absent-from-candidates "stale-display" framing missed his intent — the pin must keep a ticker TRACKED (fetched + evaluated) even after it falls off the finviz screen (the use case: future companies to keep an eye on that aren't currently tracked). The Arc-7 pre-Codex spec already carries the corrected model (pinned tickers unioned into the `_step_evaluate` universe at the held-ticker seam, `runner.py` ~1389-1400 — verified on disk; real criteria/bucket/streak, NOT bucket='excluded'). Rulings issued via a dated AMENDMENT in the commissioning brief: deviation (a) `_step_evaluate` carve-out APPROVED (operator-authorized); deviation (b) render-time cohort hint via `include_baseline=True` APPROVED with two riders — the 0026 addendum must enumerate BOTH opt-in call sites, and an opt-in call-site inventory guard test (only prefill + hint builder + engine attribution.py). Plus the measurement-universe ruling nobody had flagged: pin-injected watch-bucket tickers enter the #23 pool → temporal log → shadow broad-watch population — ACCEPTED (pins are the operator's intentional universe; the frozen statement covers it) with riders: the universe-composition note (screen + pinned) in the spec/addendum + per-run pin-injection audit line (tickers + count) for provenance decomposition. Edge flagged for Codex: delisted/no-data pinned ticker routes through F6/error without blanking the row.
- **P0 executing-plans returned: QA PASS → MERGED `cc9a2b46` (2026-06-10); the instrument is BUILT.** Worktree QA verified independently: 12 commits all trailers `[]`; production diff = exactly the plan's File Map (the one beyond-brief file, `swing/journal/analyze.py`, is the plan-specified Task-8 `TradeAnalysis.entry_intent` carrier — 2 lines); ZERO locked files touched (hypothesis.py/tier.py/review.py/progress-card/journal-stats/research all clean); migration 0027 is the exact 0024 shape; the R1-Major-2 presence-gate verified on disk (`"entry_intent" in raw_form` at trades.py:2842 — absent=preserve vs ""=clear). Codex chain properly persisted INCLUDING a VOID round logged + discarded with cause (the `codex exec resume --last` cwd bug read the main repo and false-alarmed "implementation not on disk" — diagnosed, re-run fresh with `-C <worktree>`, corrected R2 = NO_NEW_CRITICAL_MAJOR). **Binding suite on merged head: 7716 passed / 0 failed / 3 pre-existing skips; ruff clean.** Merge pushed (note: the Phase-16 orchestrator shares this checkout and pushed their Arc-7 brief `60da9936` atop my merge — docs-only, green claim unaffected; Arc 7 is moving to writing-plans in parallel). Worktree removed (Codex evidence preserved at `.codex-review-p0-executing-findings.md`); CLAUDE.md → schema v27. **Post-merge operator gates pending:** (1) the live v26→v27 migration (backup-gated, next write-path touch); (2) the `docs/p0-tuition-vs-error-operator-gate.md` browser/CLI witness — facet render, panel invariance under facet toggling, PGT markers light+dark, entry/review set/correct, and the 16-trade `swing trade backfill-intent` walk (the one needing real operator time; he is the only source of truth for intent).
- **Operator follow-up clarified (important):** broad-watch-baseline trades are NOT intended for actual entry — the hypothesis is SHADOW-measured by design; its decision criterion counts shadow-priced signals ONLY, so a live entry contributes ZERO progress toward N≥30. The labeling guidance applies only to watch trades the operator chooses to take anyway on his own discretion (his pre-existing practice) — label those for live-record coherence + tripwire coverage, but the program does not ask for them. Evaluator guidance restated: with edge unproven and the engine now measuring the watch pool risk-free nightly, discretionary watch entries are tuition spend, not evidence generation; the program-directed live entries remain the RARE narrow-cohort fires (H1 aplus, H2/H4 prefills) per the frozen registry.
