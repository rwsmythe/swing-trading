# Phase 22 — Arc 22-B: Demand A, `unintended_execution` (the third `entry_intent` value + the evidence-bearing assignment surface) — commissioning brief

**STATUS: COMMISSIONED — DISPATCHED 2026-09-23 (CHARC, on the operator's GO).** The operator ruled GO on 22-B in the RD session, 2026-09-23, his words verbatim (RD as courier, `docs/phase22-arc-b-rd-rulings-f1-f3.md` header): *"We have 7 days before 9-30, plenty of time. Let's go for 22-B"*. The §1 live state was RE-MEASURED at this seat on the post-22-A2 live DB (v39, `mode=ro`, 2026-09-23 ~17:05Z) — every value stands (§1). **F1–F3 are RULED by RD in ONE packet (`docs/phase22-arc-b-rd-rulings-f1-f3.md` @ `e5feec2a`) and clause (4) is RATIFIED by the operator (his words, 2026-09-23 ~17:15Z: *"clause (4) is ratified."*, recorded there §1) — the F1 pre-loop gate is CLEARED.** F4–F6 were ruled by CHARC in this brief. The plan cell's base is `main` at or above `e5feec2a`. This file was amended BY REPLACEMENT from the 2026-09-23 DRAFT (`74c15b77`); the change record is the commit message.

**From:** CHARC. **To:** the orchestrator. **Stage:** WRITING-PLANS under the PLAN-STAGE PROTOCOL (harness §5.1): premise verification + fork census FIRST (no Codex), ONE packet, directors rule in ONE pass each, no round while a ruling is outstanding, plan ≤ ~1,000 lines, ≤ 3 `fast` rounds then a self-sweep. Every fork this brief could foresee is RULED (§3); the census packet therefore carries ONLY forks the cell newly finds (both branches, ruler named) plus the premise re-verification — if it finds none, it says so in one line and the loop opens on that statement. **Plan cell:** `implementer-opus-high` — the design is small and every known fork is closed; xhigh only if the census re-opens a fork, with the reason stated. **Executing cell:** `implementer-opus-high` (the recipe's schema-migration exception: a `trades` rebuild on the journal of record). **Reviewer A** at `strong` to convergence; **Reviewer B REQUIRED** at the orchestrator's gate on the finished tree (charter §2.9; `swing/data` + `swing/trades` + `swing/cli`). **§3 tripwires crossed:** new schema (migration `0040`, a `trades` REBUILD + one new append-only table), a `swing/data` + `swing/trades` carve-out, one new module (F4, approved in advance on its conditions) — CHARC's architecture pass rides the census packet and the plan. **The RD gate** rides the evidence semantics (his F1–F3 rulings, encoded) and the witness (H1/H-cohort re-read). **Every director ruling is transcribed into the 22-B plan ledger as a block quote when the ledger exists** (a ruling's durable home is the committed ledger, harness §3): RD's F1–F3 doc, the ratification, and this brief's §2/§3 CHARC rulings.

**THE SERIAL RULE (RD-raised, CHARC-ruled 2026-09-22) — its first half is DISCHARGED:** 22-A2 is MERGED (`57187164`) and WITNESSED (live v39, correction 2 ADMIT, confirmed off-box), CLOSED at CHARC 2026-09-23 ~16:54Z. So 22-B's plan cell runs now, and 22-B's EXECUTING cell may dispatch on the plan's acceptance with its base at or above `57187164`. Both arcs touch the entry record and the correction machinery; the composition class (21-B × 21-G; D53) has no rung, and this time both halves are the record H1 is counted from — **22-A2's acceptance cases stay GREEN byte-unchanged on 22-B's tree (§4.8).** **Two rebuild migrations in sequence** (22-A2's `provenance_corrections`, 22-B's `trades`): each carries its own gate row and its own D51 diff; neither is folded into the other. **THE CALENDAR (RD's position, `rd-rulings-f1-f3.md` §0, adopted as the arc's STOP LINE — readable, not inferred):** the witness needs the operator's hand and no witness rung exists 10-03..10-20 (declared trading pause); the read-week freeze (`phase22-scope-charc.md:152`) forbids a `trades` rebuild landing during the October read week. **If Reviewer B is not clean on the finished tree by 2026-09-29 end of day HST, the witness waits for 10-21**, the arc parks at "plan on `main`, executing branch WIP-committed", and the October read runs on v39 with trade 20 NULL and NAMED. No merge over the read week; no witness during the pause; nobody merges over the witness.

## §0 READ FIRST (pointers — the cell LIFTS, it does not re-derive)

1. **The authority:** `docs/demand-a-entry-intent-charc-section3-pass.md` (CHARC's §3 pass, GO on the tripwire; the naming argument — the value names the CLASS, an execution nobody intended, not the mechanism; the mirror manifest; the finding that the structural-citation rule is UNREACHABLE for trade 20 because the latch instrument post-dates the event). **RD's ruling, `comms/charc/read/20260812T164026Z-rd-demand-c-your-contemporaneous-comparison.md` §DEMAND A** and its record in `docs/research-director-context-archive.md` ~line 342 (**THE ADMISSIBILITY TEST**: evidence is admissible iff (a) RECORDED BEFORE THE OUTCOME WAS KNOWN and (b) IMMUTABLE SINCE; two-tier admission ENDORSED with the tier and the reason recorded on the row; tier 2 admissible ONLY where the instrument PROVABLY post-dates the event; **(b) is verified against the audit trail — no correction row has touched the cited field — never inferred from writer-absence** (the D36 correction, CHARC's, adopted); **Demand B's annotation rows are PERMANENTLY INADMISSIBLE as evidence** — the guard that must appear in every Demand brief). `docs/phase22-scope-charc.md` §TIER 1 (22-B: name operator-concurred; §3 pass committed; §VII.F ratification of SPECIFIC AMENDED TEXT in the 0034 shape is a DISTINCT GATE inside the arc — **now CLEARED, see F1**; applies to trade 20 at a witnessed gate). **RD's F1–F3 rulings: `docs/phase22-arc-b-rd-rulings-f1-f3.md` @ `e5feec2a`** — read WHOLE; §3 below encodes them and points, it does not paraphrase them.
2. **The trade-20 doctrine:** `docs/research-director-context-archive.md` ~line 349 (`entry_intent` NULL is DELIBERATE, operator-ruled 2026-08-10; stays NULL until Demand A completes; anyone setting it to `standard` contaminates the denominators) and ~line 347 (trade 20 CLOSED 2026-08-11 @ 33.73, ≈ −0.996R; a closed trade with a realized result in NO cohort — "the vocabulary gap has stopped costing us a LABEL and started costing us a RESULT"). **The forward intent contract's ONLY home today is the same archive, line 84 (the 2026-06-10 declaration, clauses (1)–(3))** — F1 moves it to a live doc.
3. **The code the plan builds ON:** migration `0027` (`entry_intent TEXT CHECK (entry_intent IS NULL OR entry_intent IN ('standard','hypothesis_test_by_design'))` — a COLUMN-LEVEL CHECK inside the `trades` DDL, so the widening is a REBUILD by construction, the 22-A2 F12 finding one table over); `swing/data/models.py:243` `ENTRY_INTENTS` + the `__post_init__` mirror at `:356`; `swing/metrics/cohort_intent.py` (H1's predicate is `entry_intent = 'standard'` by its OWN criterion text, pinned as `H1_COHORT_CLAUSE` against the live registry row; NULL is a distinct third facet, never coerced — **so the new value lands in NO cohort automatically; no cohort code changes**; RD re-verified this before ruling F1); `swing/trades/intent.py` (display tuple; the `by_design_keywords` inference at `:54` is NOT extended — the new value is never INFERRED, only asserted with evidence); `swing/web/view_models/metrics/process_grade_trend.py:78` (`_INTENT_CSS_CLASS`, needs a class for the new token or it inherits the unclassified style); `trade_process_card.py:52` (filter options); `swing/cli.py:602`, `:1558` (`click.Choice` lists), `:1699-1706` (review-time `update_entry_intent`), `swing/web/routes/trades.py:711/:838` (the entry form's early validation against `ENTRY_INTENTS`). `swing/data/repos/trades.py:update_entry_intent` (the existing WRITER of the column — the plan reads it). **`swing/latches/service.py:54-74` (the terminal-state projection: `declined` and `criteria_lapsed` REUSE `horizon_expired` with the REASON first-class) and `:1134` (the fill pass)** — F3's precondition read starts here.
4. **The rebuild precedents and the gotchas that bound them:** `0014` (the last `trades` rebuild: `trades_new` + INSERT-SELECT + DROP + RENAME; the UNIQUE partial index dropped first and re-created after); 22-A2's `0039` as ruled (F12 conditions 1–6, ledger R0.12 — the three-edit textual proof, sequence carry by statement, dependants re-created VERBATIM after RENAME, the D51 diff as the D30 instrument); the D30 class (`0033` nearly lost a paired CHECK); `PRAGMA foreign_key_check` after RENAME is MANDATORY under the runner's `foreign_keys=OFF`; gotcha #9 explicit BEGIN/COMMIT; the migration backup gate `pre_version == 39 AND target >= 40` (`swing/data/db.py:612` `BackupGateSpec`; the 22a2 row is the shape to copy, and it wrote ONE image into `backups_dir` at the 22-A2 witness — the D32 production proof, read on disk 2026-09-23); **D51's manifest test now ENFORCES the 0038 header's item (B) AND hashes under the D51b line-start-comment normalization (`2f56235c`)** — a rebuild that forgets `ux_trades_attempt_id` or `trg_trades_attempt_id_immutable` goes RED in this arc, and the fixture is regenerated with `scripts/schema_manifest.py --write` from the arc's checkout, never hand-edited; the version-mirror naming rule (HEAD tests are `_head`; no rename sweep in the version commit).
5. **The Phase-22 doctrine that binds the evidence surface:** cohort bookkeeping NEVER blocks a money-bearing operation (0036:26-38); the ENTRY-vs-LABEL test (harness §5.1); AUTHORIZE-THEN-ABORT (the trigger's predicate set ⊆ the service's admission predicates, closure-checked); a stored grade is an attestation, a verdict is computed at read (S12.1 #5); gotcha #30 (a run-level stamp is not per-row provenance — `pre_trade_locked_at` equals `entry_date + 'T16:00:00'` on every trade and is NOT corroboration; **and `fills.fill_datetime` on every ENTRY fill is the same synthetic `T16:00:00` — F2 Sharpening 1 follows from it**).

## §1 LIVE STATE — MEASURED 2026-09-23 at v38 (`74c15b77`), RE-MEASURED 2026-09-23 ~17:05Z at v39 by CHARC (`mode=ro`, post-22-A2): EVERY VALUE UNCHANGED

The 22-A2 merge moved `provenance_corrections`'s DDL (0039 rebuilt it and its two triggers) but none of the facts below: the FK→`trades` set still has five members, `trades` itself is untouched, and the arc's only row (trade 20) is as it was. RD's independent re-read (`rd-rulings-f1-f3.md`, "Live re-reads behind the packet") agrees on every line. The cell lifts these; it does NOT re-derive them, except where a task's own discriminator needs the live shape.

| fact | value | method |
|---|---|---|
| `trades` columns / CHECKs / DDL chars | **58 / 14 / 3,419** (the §3 pass measured 57/13 on 2026-08-12; `0038` added `attempt_id` and its CHECK since — the count moved, the doc did not) | `PRAGMA table_info`; `\bCHECK\b` count over `sqlite_master.sql` |
| `trades` dependants | **5**: `idx_trades_candidate_id`, `idx_trades_pattern_evaluation_id`, `ux_trades_attempt_id`, `ux_trades_one_open_per_ticker` (UNIQUE partial), `trg_trades_attempt_id_immutable` | `sqlite_master WHERE tbl_name='trades'` |
| tables with an FK → `trades` | **5**: `fills`, `daily_management_records`, `trade_events`, `reconciliation_discrepancies`, `provenance_corrections` (the §3 pass counted 4; 0036 added the fifth; 0039 REBUILT it — same name, new DDL) | `sql LIKE '%REFERENCES trades%'` |
| `entry_intent` value set | `NULL` 1 (trade 20) · `hypothesis_test_by_design` 20 · `standard` 7 — 28 trades, 0 open | `GROUP BY` |
| trade 20 | AMN, entry 2026-08-07, `entry_intent` NULL, keys NULL, `trade_origin manual_off_pipeline`, `attempt_id` NULL (pre-0038); `why_now` = "Accidental entry while working on latch/unlatch mechanism"; `notes` = "Stale A+ latch order fired after A+ condition disappeared.  This happened as we were working on the latch removal code." (two spaces after the first period — cite bytes, not a retyping); `thesis` = "Old A+ setup, high likelihood of loss" | row read |
| the instrument PROVABLY post-dates the event | earliest `latch_order_intents.recorded_ts` = **2026-08-08T20:01:08** (13 rows) vs entry 2026-08-07; AMN intents = **0** — at SESSION-DATE grain (F2 S1): `2026-08-08 > 2026-08-07`, strictly later → ADMIT | `MIN(recorded_ts)`; `WHERE ticker='AMN'` |
| immutability against the audit trail | `reconciliation_corrections` rows with `affected_table='trades' AND affected_row_id=20`: **0**; on ANY `trades` text field ever: **0** (the only `trades` fields ever corrected are `current_stop` ×4, `entry_date` ×1); `provenance_corrections`: **2 rows** (trades 23, 25 — cohort keys, never text fields) | `GROUP BY field_name`; `COUNT(*)` |
| entry-captured text columns on `trades` | `notes`, `why_now`, `thesis`, `emotional_state_pre_trade` (+ `pre_trade_locked_at`, a synthetic restatement — NEVER cited) | `PRAGMA table_info` |
| Python mirrors of the enum (lower bound, grep `hypothesis_test_by_design` over `swing/`) | 12 lines / 7 files: `cli.py` ×3 (`:603`, `:1559`, `:1768`), `models.py:243`, `cohort_intent.py` ×3 (`:14`, `:33`, `:75`, prose + clause), `trades/intent.py` ×2 (`:19`, `:56`), `process_grade_trend.py` ×2 (`:76`, `:80`), `trade_process_card.py:54`; templates: `process_grade_trend.html.j2:58` (legend), `partials/review_form.html.j2:136` (prose). Two further hits are SQL, not mirrors: `0027_entry_intent.sql:13` (the CHECK being widened) and `0034:145` (H1's criteria text — UNCHANGED by this arc, per F1) | token grep — a LOWER BOUND; the plan's #11 task reads each member separately |

## §2 SCOPE SHAPE — CHARC's rulings (mine; stated so the census does not re-open them), with RD's F1–F3 encoded where they bind

**IN:**
- **Migration `0040`: the `trades` REBUILD widening ONE enum token** — under the 22-A2 F12 conditions transposed: (1) the new DDL is the STORED v39 `trades` DDL with EXACTLY ONE textual edit (the IN-list gains `'unintended_execution'`), proven bytewise by a test that applies that one edit to the v39 text; (2) ONE transaction, explicit column list on the INSERT…SELECT (58 columns + none added), row counts equal, every row's tuple identical, `PRAGMA foreign_key_check` empty, the five child tables' row counts unchanged; (3) order: CREATE new → copy → DROP old (takes the five dependants) → **RENAME inside a two-statement bracket `PRAGMA legacy_alter_table=ON;` / `ALTER TABLE trades_new RENAME TO trades;` / `PRAGMA legacy_alter_table=OFF;` — nothing else inside the window** → re-create the FOUR indexes and the ONE trigger VERBATIM from their source migrations (`0014`/`0027`-era for the partial UNIQUE; `0038` for the attempt-id pair). **R0.J (CHARC-ruled 2026-09-23, branch (a); the bare RENAME FAILS BY EXECUTION on v39):** SQLite 3.50.4's modern RENAME re-parses every trigger, and 0039's `trg_provenance_corrections_citation_graph` (ON `provenance_corrections`, 103,406 chars, six textual `trades` references, the ONLY foreign trigger/view naming `trades` — census method: `sqlite_master WHERE sql LIKE '%trades%' AND tbl_name <> 'trades' AND type IN ('trigger','view')`, a textual SUPERSET, 1 row; 0 views on the DB; 0 objects name `trades_new`, so legacy mode has nothing to skip rewriting) raises `no such table: main.trades` after the DROP. Reproduced by the plan cell on a live copy, by the orchestrator in memory, and by CHARC in memory before ruling: under the bracket every foreign object is byte-identical to v39, `trades` equals the one-edit text, `integrity_check` ok, and the trigger still RAISES its own message against the rebuilt table. **Conditions:** (i) the pragma is connection-scoped and NOT transactional (CHARC measured: it survives a rollback), so `_apply_migration` (`swing/data/db.py:457`) reads the PRIOR value before `executescript` and restores it in the existing `finally` beside `foreign_keys` — restore-prior, never hard-OFF; **b22_45** pins the failure path (a script that sets ON then fails leaves the connection reading the prior value); (ii) **b22_44** as the cell proposed, with the set clause CORRECTED at G1 (the orchestrator's measurement, CHARC-accepted 2026-09-23 — "EQUAL at v39 and v40" was false by construction once 0040 added its own cross-table readers): every v39 foreign object referencing `trades` is byte-identical at v40, AND the v40 additions are EXACTLY the named set 0040 creates after the RENAME (`trg_eia_trade_binding`, `trg_eia_cited_fields`, `trg_eia_audit_trail`, `trg_eia_tier2` — the v40 `trades` census reads 5), AND the citation-graph trigger FIRES against the rebuilt `trades` — a 22-A2 fully-cited fixture row inserts, its one-mutation twin ABORTS with the trigger's OWN message (a `no such table` error is a FAIL, not an abort); **G6 (CHARC, 2026-09-24): on the LIVE COPY only the ABORT half of b22_44 runs — ACCEPTED as the Task 13 step-3 result, both halves stay in the fixture suite.** The positive half's fully-cited 22-A2 fixture cannot insert on live state (trade 25's real citation graph and two real `provenance_corrections` rows make the helper's `COUNT == 1` unsatisfiable), and no live-shape positive is needed: SQLite resolves a trigger's body when the triggering statement is PREPARED, so an INSERT that reaches the citation-graph trigger's OWN `RAISE` text (never `no such table`) proves the rebuilt `trades` resolves inside it — the R0.J property — measured at this seat before R0.J was ruled (`INSERT … DEFAULT VALUES` → the trigger's `IntegrityError` on the rebuilt table). A positive cloned from live correction 2's own row was considered and DECLINED: it proves the admit direction on one row, which the fixture suite already proves, at cell spend. The live-copy assertion is sharpened by one clause: the abort's message MUST be one of that trigger's `RAISE` literals (a table-CHECK or FK abort does not count). (iii) 0040's header states the census (method + count) as the precondition that makes the bracket sufficient — a later foreign object referencing `trades` is covered by the same bracket, but the header says which objects the bracket was proven against. **Branch (b) REJECTED** (drop + verbatim re-create of the 0039 trigger): 103 KB of the evidence grammar's SQL binding duplicated into 0040 moves `head_create_statement` resolution (`tests/data/_migration_text.py:47`) to 0040 for every 22-A/22-A2 test — the composition class on the object §4.8 pins — and under D60 gives a future grammar edit TWO homes. Forward rule for ANY rebuild in this repo: run the cross-table census BEFORE ruling the order; a non-empty census means the RENAME is bracketed. Gotcha candidate → the orchestrator's Gotchas content. **Owned by CHARC:** item (3) as first written carried the mechanism without its admissibility precondition against the v39 schema (harness §5.1's rule, met on my own brief); a D51 manifest cannot see a runtime-order defect — the cell's self-check found what only execution finds. (4) the D51 diff reads EXACTLY TWO changed pairs (the header, the table) PLUS, as ADDITIONS: the new table's objects, the one new `trades` trigger `trg_trades_entry_intent_attested_terminal` (N4, below), and **the TWO `latch_view_events` evidence belts adopted at census N1 (RD, `rd-rulings-census-n1-n5.md` @ `c33bd6e7`, on CHARC's schema finding): `trg_lve_actionable_ever_viewed_monotonic` (`BEFORE UPDATE OF actionable_ever_viewed … WHEN NOT COALESCE(NEW.actionable_ever_viewed >= OLD.actionable_ever_viewed, 0)` → `RAISE(ABORT)`, the SQL twin of the writer's `MAX` at `repos/latch_view_events.py:167`) and `trg_lve_no_delete` (`BEFORE DELETE` → `RAISE(ABORT)`), PLUS — R0.F-1 (a'), RD @ `8644affa`, completing the 22-I TRIPLE — `trg_lve_no_replace` (`BEFORE INSERT WHEN EXISTS` a row on the table's UNIQUE key **OR on the PK — `(NEW.view_event_id != -1 AND view_event_id = NEW.view_event_id)`, the FULL `trg_loml_no_replace` shape it was ruled to copy, CONFIRMED by CHARC at G1 (2026-09-23) on the cell's execution-level completion: it closes an `INSERT OR REPLACE` carrying an EXISTING `view_event_id` under a fresh UNIQUE key, which would otherwise DELETE the old row past `trg_lve_no_delete`. Inert for the writer, whose INSERT omits the PK (`view_event_id INTEGER PRIMARY KEY AUTOINCREMENT`, `0033:37`; an omitted PK presents as -1 in a BEFORE INSERT trigger). DECLARED LIMITATION, in the 0040 header: 0037 pairs this clause with `CHECK (link_id > 0)` (`0037:340`) and `latch_view_events` has NO such CHECK — adding one is a rebuild of that table, OUT of 0040 — so an explicit PK of -1 is storable and a later REPLACE on it bypasses the clause; fabrication class (R0.K's threat model). Discriminator: `INSERT OR REPLACE` with an existing `view_event_id` and a fresh UNIQUE key ABORTS with the belt's message; on a control with the PK clause dropped the old row VANISHES; the 0037 `_NO_REPLACE_PK` roster carries `trg_lve_no_replace` and `trg_eia_no_replace`** → `RAISE(ABORT)`; the writer's INSERT at `repos/latch_view_events.py:179-190` is plain, no `ON CONFLICT`, so the DO-NOTHING facet of the REPLACE gotcha does not bite and its `IntegrityError` handler at `:191` already routes the ABORT to the UPDATE path — writer UNCHANGED, proven by execution), PLUS — **R0.K (a), RD @ `226eb6da`, the FOURTH belt — `trg_lve_view_window_immutable` (`BEFORE UPDATE OF first_viewed_ts, ticker` → `RAISE(ABORT)`): leg 1's INPUTS are the window (`first_viewed_ts`) and the identity block; the triple guarded the evidence column and the row's existence, not the window leg 1 reads, and under R0.F-2 (a) leg 1 speaks only where leg 2 refuses, so an unguarded `first_viewed_ts` is a manufacturable admit the snapshot and the drift reader run too late to see. The sole writer's UPDATE (`repos/latch_view_events.py:157-169`) never sets either column, so no production path aborts. RD's rule, encoded in the plan so 22-C does not meet this fork: EVERY COLUMN A LEG READS IS A COLUMN THE SCHEMA GUARDS — the plan lists leg 1's read set beside the belt set and pins their equality by test; the identity triggers stay** — and ZERO deletions; the five pre-existing `trades` dependants hash identical. The belts' discriminators (§4.4): a raw `UPDATE … SET actionable_ever_viewed = 0` on AMN's row 5 ABORTS; a raw `DELETE` ABORTS; `INSERT OR REPLACE` on AMN's key with 0 ABORTS; a raw `UPDATE … SET first_viewed_ts = '2026-08-03T00:00:00'` on row 5 ABORTS; the writer's plain INSERT on an existing key takes the UPDATE path; the writer's last-view UPDATE passes; the writer's 0 → 1 passes. **Leg 1's WINDOW — R0.F-2 (a), RD, same commit: a `latch_view_events` row speaks ONLY if recorded under the actionability instrument, `date(first_viewed_ts) >= 2026-08-03` (the same constant as leg 2; 0033 backfilled every earlier row to 0 = "no actionable presentation RECORDED", a deployment fact; live rows 1–5 ARE that backfill); window by VIEW TIMESTAMP at date grain (local naive HST), never `view_session_date`. Trade 20: row 5 first viewed 08-01 → backfilled → leg 1 SILENT → ADMITS on LEG 2, `admitted_leg = deployment`.** Why the table earns barriers now: leg 1 makes it EVIDENCE, and "monotonic + no-vanish" was a WRITER property only — the inference the admissibility standard forbids. (5) the reversibility header names the edit and the gate row `BackupGateSpec(39, "22b", …)`; (6) `EXPECTED_SCHEMA_VERSION` 39 → 40. `trades` has NO AUTOINCREMENT (`id INTEGER PRIMARY KEY`, the rowid-reuse gotcha) — no sequence carry; the plan says so rather than copying 0039's step. **(7) THE F1 PIN (CHARC's ruling, the shape RD expected):** `0040`'s header carries the sha256 of clause (4)'s ratified text; a test derives that hash INDEPENDENTLY from the committed doc `docs/training-epoch-intent-contract.md` (the block-quoted clause (4) EXACTLY as it stands at `rd-rulings-f1-f3.md` @ `e5feec2a` — byte-for-byte, the plan COPIES it, never retypes it) and asserts equality; the migration NEVER reads the docs tree at runtime. Any later edit to the clause is a NEW amendment with a new hash and a new ratification.
- **The `entry_intent_attestations` table (NEW, append-only, its SCHEMA IS THE EVIDENCE RULE — the 0036 pattern), encoding RD's F2/F3:** one row per assignment of `unintended_execution`; columns: `trade_id` (FK, UNIQUE — one head per trade), `assigned_value` (CHECK = `'unintended_execution'` only — this table records ONLY the evidence-bearing value; the other two are entry-time facts, not assignments), `admission_tier` (CHECK IN `('structural','contemporaneous_record')`); **tier-2 columns — P1 as RULED at census N1 (RD, `docs/phase22-arc-b-rd-rulings-census-n1-n5.md` @ `7a6f7539`, NEITHER offered branch; "the structural record could NOT have existed", proven from the instrument's OWN record, three-valued):** `placement_session` (the fill envelope's Schwab order-ENTERED date, the D31 value; fallback `trades.entry_date`, conservative) + `admitted_leg`; **LEG 1 (primary, telemetry):** the mandate's `latch_view_events` rows with `view_session_date` on or before the placement session — ANY with `actionable_ever_viewed = 1` → REFUSE ("the instrument offered the order and did not fire"); rows exist and ALL 0 → admissible; **LEG 2 (fallback, NO telemetry rows):** `placement_session < '2026-08-03'` (the 21-B deployment session, a pinned constant whose derivation the docstring states: merge `d5d03bb9` 01:04 HST as lower bound, the w32 image 17:30 HST v33/0 rows as upper bound — determined, not estimated); **LEG 3:** neither speaks → UNPROVABLE → REFUSE. The draft's first-row grain is REJECTED as a proxy (it errs toward admission); the SQL bind on the row is over the leg's recorded evidence (leg 2: `placement_session < '2026-08-03'`; leg 1: the cited telemetry row id with `actionable_ever_viewed = 0` at read, snapshotted). `fill_datetime` is synthetic `T16:00:00`, not a clock — never compared against; **structural column (F3 (a) as narrowed by census N5 (b), RD): `cited_latch_link_id` (FK `latch_order_mandate_links`) ONLY — the LINK is the structural citation; `cited_latch_intent_id` is DROPPED from the schema; an unlinked order-naming intent REFUSES, typed, naming 22-A's link path as the recovery** (a backfilled link under 22-A's rules is a follow-on primitive, not a 22-B heuristic); NULL iff tier 2; **tier-2 leg record (census N1, RD): `admitted_leg` (CHECK IN `('telemetry','deployment')`) + the leg's evidence in the tier-reason JSON — the `latch_view_events` row id read, or the pinned deployment constant `2026-08-03` and the placement session — so a replay re-derives the admission;** **the DEATH-BEFORE-FILL predicate:** `cited_latch_terminal_rung` (CHECK IN `('invalidation','criteria_lapsed','horizon','declined')` — the death rungs ONLY; `fill`/`superseded` never admit) + `cited_latch_terminal_session` with the bind **`cited_latch_terminal_session < trade_entry_date`** (strictly before; same-session is a MANDATE fill under R6 and refuses); `cited_fields_json` (a CLOSED list drawn from the allowlist `notes|why_now|thesis|emotional_state_pre_trade` — F2 edge (ii) puts all four IN; NEVER `pre_trade_locked_at`; NEVER an annotation row) with **F2 S4's bind: at least one of `notes`/`why_now` is cited** (a tag-only citation admits on nothing); `cited_text_snapshot_json` (the cited fields' text AT ASSIGNMENT, so a later edit is visible against the snapshot); `audit_trail_checked_at` + `corrections_touching_cited_fields` (must be 0 — the D36 check the code performs, AND its SQL twin over **BOTH audit tables** (F2 S2): `NOT EXISTS` on `reconciliation_corrections (affected_table='trades', affected_row_id=NEW.trade_id, field_name IN cited)` AND the equivalent over `provenance_corrections`'s columns — the plan reads 0039's DDL for the exact names); `outcome_known_at` (the trade's first exit fill datetime, or NULL if open — **F2 edge (i): an OPEN trade is ADMISSIBLE NOW, `outcome_known_at` written ONCE at assignment, NEVER back-filled; the row carries NO P&L / MFE / MAE — running state is not evidence**) with the SQL bind `trade_entry_date < date(outcome_known_at) OR outcome_known_at IS NULL` (test (a)); `reason`, `applied_at`, `applied_by`. Append-only TRIPLE (`no_update` + `no_delete` + conflict-scoped `no_replace`, the 22-I convention) + every `WHEN` clause `NOT COALESCE(...,0)`. **Both tiers' predicate sets live in the service FIRST and in the triggers as the SQL twin** (AUTHORIZE-THEN-ABORT: trigger set ⊆ service set, closure-checked by a test that enumerates both). **G1b FORK — PERSIST-CANONICAL (22-A, CHARC + RD 2026-08-26) × 22-B's envelope reads. RULED (CHARC, 2026-09-23): branch (b), SHARPENED — ZERO SQL reads of a fill envelope anywhere in 22-B, NO exemption.** Found by execution: `tests/trades/test_22a_envelope_canonicality_sweep.py::test_no_sql_anywhere_in_swing_reads_a_fill_envelope` (a whole-tree walk for `json_*(… schwab_source_value_json`) went RED on the branch at 0040's `trg_eia_trade_binding` (`$.schwab_order_id`), `trg_eia_tier2` (`$.entry_date`) and the service's mirror read — the plan's R3-06/SS-3 prescription re-introduced the Python-mirrored-in-SQL shape that ten 22-A rounds retired. **ORDER ID:** the service calls `ensure_entry_fill_identities(conn)` inside its `BEGIN IMMEDIATE` before any read (rung 6's idiom, `latched_origin.py:1827`; it RE-VERIFIES and covers exactly the population 22-B reads — entry fills), then takes `stored_identity` for the authoritative entry fill against its CURRENT envelope (`fei.fill_id = f.fill_id AND fei.envelope_raw = f.schwab_source_value_json`, the permitted form): state `refused` → typed refusal `envelope_refused` (never admits); a drift raise from `record_identity` → contained to a typed refusal `identity_drift` (fail-closed, as the ladder does); no envelope → no reading → `order_id` None → the E9 path; an envelope with NO reading after `ensure` → invariant raise. The trigger twin binds `NEW.entry_broker_order_id IS (SELECT fei.broker_order_id FROM fill_envelope_identity fei JOIN fills f ON f.fill_id = fei.fill_id AND fei.envelope_raw = f.schwab_source_value_json WHERE f.fill_id = NEW.entry_fill_id_at_assignment AND fei.envelope_state = 'canonical')` — stored values only. **PLUS the NULL-side close, RULED IN at G1d (the cell's candidate, CHARC 2026-09-23; the residual was in CHARC's own SQL: "NULL on both sides" admitted a raw attestation with a NULL id over a REFUSED reading, the fabrication class):** a NULL `entry_broker_order_id` is admissible ONLY IF the fill has NO envelope (`f.schwab_source_value_json IS NULL`, the same predicate `ensure_entry_fill_identities` uses to decide what gets a reading) OR a `canonical` reading EXISTS for the fill's CURRENT document — so a refused reading ABORTS a NULL id, and a non-NULL envelope with NO reading ABORTS (fail-closed; the service always `ensure`s first, so by construction the reading is present at assignment). Whole predicate inside `WHEN NOT COALESCE(…, 0)`; a presence test on the column is the permitted form (the sweep forbids only `json_*` over it). Measured at this seat: the authority reads NULL, empty, whitespace-only and `{}` documents ALL as `canonical` with no order id, so a blank envelope gets a canonical reading and the service's blank-means-absent path (b22_193) AGREES with the stored reading — no second opinion exists. Discriminators, trigger alone on plain sqlite3: a raw row with NULL id over a refused (padded) reading ABORTS; NULL id over a fill with NULL envelope PASSES; NULL id over a canonical no-key reading PASSES; NULL id over a non-NULL envelope with its reading row DELETED (raw) ABORTS.** `drift_report` (vi) compares `entry_broker_order_id` to the CURRENT stored reading. **ENTRY DATE:** the SQL bind `placement_session = json_extract(…'$.entry_date')` is DROPPED; the service reads `entry_date` in Python (ONE engine). **CORRECTED at G1d (orchestrator-measured, CHARC-verified by execution 2026-09-23): the stored reading does NOT refuse a doubled `entry_date` — the canonicaliser's duplicate/padding checks cover only `ENVELOPE_KEYS_READ_BY_BOTH_DOMAINS` = (order id, symbol), `latched_origin.py:729-732`, and a document carrying `entry_date` twice is stored `canonical`. RULED (a): the SERVICE counts the root's `entry_date` keys with the canonicaliser's own `object_pairs_hook` idiom and refuses when there is more than one, BEFORE `json.loads` can keep the last key — a 22-B predicate on the one key 22-B alone reads, with its OWN typed code `entry_date_ambiguous` (not `envelope_refused`, which now means the stored reading's state; the refusal-legibility rule) naming the fact; a present value not exactly `YYYY-MM-DD` stays the `unprovable` refusal (covers padding). (b), widening 22-A's canonicaliser to `entry_date`, REJECTED: that key set is named and justified by the TWO-ENGINE read, and `entry_date` is read by one engine now — the widening would make the set's name false and bump 22-A's version for a hazard that is not the two-engine one; (c) REJECTED. Discriminator: `entry_date` twice (08-01 then 08-02) → `entry_date_ambiguous`, nothing written; the twin under a bare `json.loads` would ADMIT on 08-02.** The service persists `placement_session` + `placement_session_source`; SQL keeps the stored value's date CHECK, the source pairing, the deployment bound and the leg predicates over `NEW.placement_session` as stored. In Task 11 `placement_session`'s derivation moves to `SERVICE_ONLY` with the reason "persist-canonical: SQL never reads the envelope; provenance is replay-verified in Python by drift (vi)", beside death-before-fill. Consequences: the `<ENV>` `CASE WHEN json_valid` guard leaves 0040 entirely (R3-06's hazard class exits the schema; b22_193 keeps its service half, the sweep test is its SQL discriminator); b22_78 (duplicate `entry_date` key) is REWRITTEN as a refusal case (`envelope_refused`); the D51 SET is unchanged and only 22-B-added trigger bodies move; `trg_eia_trade_binding` now names a THIRD table (`fill_envelope_identity`) — the R0.J census for any future rebuild of that table gains it (the 0040 header says so). 0040 is edited in place on the branch BEFORE it has reached any DB — not the D59 class. **Branch (a) REJECTED:** an exemption re-admits the mirror for the very key 22-A closed, and the sweep's own docstring says a roster is what it exists to replace; **(b)'s by-name exemption for `entry_date` REJECTED for the same reason** — one exemption is how a closed rule reopens; (c) OUT, agreed. Precondition, existing today (orchestrator-measured on live v39): 14 of 14 envelope-bearing entry fills carry a canonical reading; the 13 unread fills are all non-entry.
- **F2 SHARPENING 3 — THE PRECONDITION WALK (a plan task, its output a FINDING, never a channel):** the plan walks EVERY `UPDATE trades` site in `swing/` (the repo's own count was 21 at `reconciliation_auto_correct.py:190`; the plan counts again, by READ, gotcha #36 — a dynamic-SQL writer does not show a column name) and asserts by test that NO site writes `notes|why_now|thesis|emotional_state_pre_trade` outside the audited corrector. If one exists, P2 is fail-open for that field and the cell REPORTS it (the plan ledger names the site) — it does not add an audit channel in this arc.
- **F3's PRECONDITION READ (a plan task, fail-closed):** whether the latch ledger PRESERVES a death terminal when a fill lands on a later session (the fill visible separately) or OVERWRITES it with `filled` is read from the resolution code (`swing/latches/service.py:54-74`, `:1134`, the fill pass), not from the ruling. If death-then-fill is NOT expressible, the structural tier ships REFUSING (typed message: "the ledger cannot yet prove death-before-fill; tier 2 or a follow-on primitive") and the gap is a register candidate for CHARC — never a weaker tier-1. Trade 20 is tier 2 and unaffected either way.
- **The assignment surface, CLI ONLY:** `swing trade assign-intent TRADE_ID --value unintended_execution --cite notes,why_now --reason "<r>" [--dry-run]` — the service (a new `swing/trades/entry_intent_assignment.py`, F4-shaped: preflight outside the transaction; the write is ONE `BEGIN IMMEDIATE` inserting the attestation row THEN updating `trades.entry_intent`, both or neither). **The tier is DETECTED, never chosen (three-valued, as RULED at F3 + census N5 (b) + census N1):** a `latch_order_mandate_links` row for the trade's filled order → `structural`, citing the LINK (the ONLY structural citation); no link but an order-naming `latch_order_intents` row → REFUSE, typed, naming 22-A's link path as the recovery (N5 (b) — no intent-row citation exists); no link and no order-naming intent → the tier-2 legs of N1 in order: leg 1 (telemetry) speaks → admissible or REFUSE ("the instrument offered the order and did not fire"); leg 1 silent → leg 2 (`placement_session < 2026-08-03`) → admissible or REFUSE; neither → UNPROVABLE → REFUSE naming the missing fact. A structural citation then ADMITS only under death-before-fill; a live latch or a same/later-session death REFUSES naming the rung and both dates ("a mandate fill is `standard` under clause (1)"). Refusals are typed and name the recovery. The seam obligation: `update_entry_intent` (the existing review-time writer) and both `click.Choice` lists and the web entry form's validation REFUSE `unintended_execution` with a typed message naming `assign-intent` — the value has exactly ONE writer, pinned by a test. The surface refuses when `entry_intent IS NOT NULL` (no relabel path — OUT below).
- **The #11 mirror sweep** (the §3 pass's manifest, now 7 files + 2 templates, §1's file:line list), classified per site by READ as widened / tight-by-design / value-agnostic; the SQL-vs-Python drift test over `trades.entry_intent`'s CHECK (the census found NONE exists today — the plan ADDS it, the mandatory mirror member); a CSS class for the token; the display maps.
- **COHORT CODE CHANGES — census N2 (a), RD; the draft's "no cohort code changes" is WITHDRAWN (RD's premise, owned by him):** the H2–H5 readers do not filter intent (`cohort_intent.py:171-176`, `metrics/cohort.py:78-83`), so an `unintended_execution` trade carrying an H2–H5 label WOULD count. Both halves gain the exclusion IN CODE, grounded in clause (4) as a NAMED authority: a `CONTRACT_EXCLUSION_CLAUSE` constant pinned against the committed contract doc (the `H1_COHORT_CLAUSE` shape), and for the epoch-contract cohorts the predicate gains `entry_intent IS NOT 'unintended_execution'` — NULL still counts for H2–H5 as today; NOT a blanket filter (D29's design honoured). `cohort_intent.py`'s docstring also re-points from the archive to `docs/training-epoch-intent-contract.md`. **This is inside `swing/metrics/` — no new tripwire; CHARC's §3 pass absorbs the scope.**
- **NAMING — census N3 (a), RD: per-cohort with N2 (a); the draft's "20 NAMED in each reader's exclusion line" is FALSIFIED and RESTATED.** The existing exclusion lines name LABEL-matched trades dropped by 22-A2's tier-2 replay; intent-filtered trades drop silently (`recommendations/hypothesis.py:547-552`) and trade 20's label is NULL, so 20 cannot appear there. §4.3 and §5 step 4 are restated below; the naming discriminator runs on SYNTHETIC H1-/H2-labelled `unintended_execution` trades.
- **The §VII.F record (F1, RATIFIED):** the NEW doc `docs/training-epoch-intent-contract.md` — (i) clauses (1)–(3) quoted VERBATIM from `research-director-context-archive.md` line 84 (preserve the quote), (ii) clause (4) copied byte-for-byte from `rd-rulings-f1-f3.md` @ `e5feec2a`, dated, with the operator's ratification quoted (*"clause (4) is ratified."*, 2026-09-23); RD's charter §7 gets a dated pointer entry (RD's content — the plan PROPOSES the line, RD lands it; the cell does not edit RD's charter). The doc is a docs-only deliverable of the plan cell, committed BEFORE the migration task so the hash test has its input.
- **Trade 20's assignment at a witnessed gate** (§5).
- **The 22-A2 acceptance cases stay GREEN byte-unchanged** (A2-09-style pin on 22-A2's own case set) and the four cohort readers' outputs + `swing hypothesis list` are asserted UNCHANGED before/after 20's assignment except that 20 is now NAMED as `unintended_execution` in the excluded-with-reason line (F10's "never silently uncounted", one more reason string).

**OUT:** 22-C (abandonment intent) and 22-E (Demand B annotations) — RD: one schema family, one arc or strictly sequential, NEVER parallel with 22-B; the era model / 22-A2i; trades 19/24/28 (named-pending); any UI (the entry form does not offer the value; the assignment is CLI); the `initial_stop` correction surface; inference of the value from text (`by_design_keywords` untouched); a relabel path for a trade that already carries `standard`/`by_design` (a NON-EMPTY relabel is a NEW evidence class — the 22-A2 F1.1 ruling applies verbatim, and clause (4) says so in its own text); a death-then-fill PRIMITIVE for the latch ledger if F3's precondition read finds it absent (a follow-on, banked); D58's snapshot fence (its own arc); any `trades` change beyond the one enum edit (F6).

## §3 FORK CENSUS — ALL SIX RULED; the census adds only what it newly finds

- **F1 — WHAT TEXT §VII.F RATIFIES, and how is it pinned. RULED: RD branch (a), clause (4) text at `rd-rulings-f1-f3.md` §1 @ `e5feec2a`; RATIFIED by the operator 2026-09-23 (*"clause (4) is ratified."*); the PIN is CHARC's, ruled in §2 item (7): test-pinned sha256 in `0040`'s header, derived independently from the committed doc, NEVER checked at migration runtime.** RD verified before ruling that H1's registry row needs NO amendment (`H1_COHORT_CLAUSE` already reads STANDARD-intent only) — the amendment is to the CONTRACT, not the criteria; `0034` is untouched. The home is the new live doc (an archive is not a home — harness §2's archive discriminator).
- **F2 — THE TIER-2 ADMISSIBILITY PREDICATES. RULED: the three CONFIRMED with FOUR sharpenings; both edges ruled** (`rd-rulings-f1-f3.md` §2): S1 P1 at session-date grain, strictly later, same-session refuses; S2 P2 over BOTH audit tables; S3 P2's precondition VERIFIED by the plan's `UPDATE trades` walk, a negative reported as a finding; S4 a DESCRIPTIVE field (`notes`/`why_now`) must be cited; edge (i) open trade ADMISSIBLE now, `outcome_known_at` NULL written once, no running state on the row; edge (ii) `thesis` + `emotional_state_pre_trade` IN the allowlist. Encoded in §2's table bullet.
- **F3 — THE STRUCTURAL TIER's citation. RULED: branch (a) (link, else intent) PLUS DEATH-BEFORE-FILL** (`rd-rulings-f1-f3.md` §3): terminal rung ∈ the four death rungs with terminal session STRICTLY before `entry_date`; `fill`/`superseded`/live/same-session REFUSE as mandate fills; the ledger's death-then-fill expressibility is a PRECONDITION the plan READS, fail-closed if absent. **The AMN geometry is the discriminator fixture** (fire 08-03, `skip` and close 30.80 below invalidation 30.85 on 08-07, fill 08-07 at 36.43): synthetic latch with invalidation terminal 08-06 + fill 08-07 → ADMIT structural; terminal 08-07 → REFUSE; live latch → REFUSE as a mandate fill. Encoded in §2.
- **F4 — MODULE + SERVICE SHAPE. RULED (CHARC):** new module `swing/trades/entry_intent_assignment.py` (a §3 new-module tripwire, approved in advance on the F4 conditions: preflight outside the transaction; ONE `BEGIN IMMEDIATE` for the two-row write; callers pinned by test). NOT inside `cohort_provenance_correction.py` (3,414 lines) and NOT a second writer of the cohort keys — this surface writes `entry_intent` and its attestation ONLY. **§3 PASS CONDITION (CHARC, 2026-09-23, on the converged plan @ `351b2e60`): the attestation table's SQL lives in `swing/data/repos/entry_intent_attestations.py` (insert + `get_attestation` + the row mapper), a SECOND new module GRANTED under the same tripwire; the service imports it and holds no table SQL.** The plan's Task 5 read "one new module" as a CAP and put the INSERT in the service to honour it; a tripwire ROUTES review, it does not cap module count, and the repo's precedent is repo-per-table for every evidence table (`repos/provenance_corrections.py`, `repos/latch_order_intents.py`, `repos/latch_order_mandate_links.py`, `repos/latch_view_events.py` — the only `INSERT INTO` sites for those tables under `swing/` are their repo modules and their migrations, grep stated). The change is file placement inside the already-granted `swing/data` carve-out, behaviour identical, minor scope — no counted round; b22_115's writer inventory names the repo module as the INSERT site, b22_114 (the sole caller of `assign`) is unchanged.
- **F5 — THE SINGLE-WRITER SEAM. RULED (CHARC):** every generic writer of `entry_intent` REFUSES `unintended_execution` (tight-by-design mirrors, each with a typed message naming `assign-intent`); the model validator ACCEPTS it (widened); the drift test covers the three-valued CHECK. **The surface set is EIGHT by the census's read (C.4), not the draft's five:** `update_entry_intent`, both CLI `click.Choice` lists, the `backfill-intent` free-text prompt (`cli.py:1723-1778`), the web entry form, the review form, `EntryRequest.__post_init__` (`entry.py:569`), and the corrector's dynamic writer (`reconciliation_auto_correct.py:2309`, via `_RESERVED_JOURNAL_FIELDS` `:200`). Pinned by a test that greps the eight surfaces' literals AND by execution (each refuses with the message).
- **N4 — THE SEAM IN REVERSE: the value is TERMINAL for every generic writer. RULED (CHARC, census round 0, 2026-09-23): branch (a).** The census's defect is real at the sites: `review_form.html.j2:129-133` always submits `entry_intent`; `routes/trades.py:3618-3621` is presence-gated, so an attested trade's first web review submits `""` and `update_entry_intent(None)` CLEARS the attestation — at exactly the moment F2 edge (i) says to assign. **The guard has THREE layers, each named to the writer it catches, and the SQL layer is the twin of the service layer (AUTHORIZE-THEN-ABORT):** (1) `update_entry_intent` reads the CURRENT value inside the caller's transaction and raises a typed `AttestedIntentError` when it is `unintended_execution` and the new value differs — this one check covers the web review route, `trade review --entry-intent`, and `backfill-intent` (all three call it; `--force` and `--trade-id` re-prompts SKIP an attested row with the message, never prompt over it); (2) the corrector adds `("trades", "entry_intent")` to `_RESERVED_JOURNAL_FIELDS` — the EXISTING coupled-column mechanism, its message routing to `assign-intent` — which is the only layer that reaches the dynamic `UPDATE trades SET {field}` builder; (3) **the SQL twin, part of `0040`: `trg_trades_entry_intent_attested_terminal`, `BEFORE UPDATE OF entry_intent ON trades WHEN COALESCE(OLD.entry_intent,'') = 'unintended_execution' AND NEW.entry_intent IS NOT OLD.entry_intent` → `RAISE(ABORT, …)`** — a NEW `trades` dependant created after the RENAME (F6 is untouched: the table DDL still carries exactly one edit; the trigger is a separate `sqlite_master` object and the D51 diff shows it as an addition). A same-value UPDATE is a no-op and passes (idempotent); NULL → the value passes (the assignment path). **The review form renders the attested value READ-ONLY with no `<select>` and OMITS the field from the POST**, so the presence gate preserves it; a handcrafted POST carrying `entry_intent=""` for an attested trade gets the typed refusal as a 4xx fragment, value preserved. **The typed message states the truth of this arc:** "entry_intent is attested (`entry_intent_attestations` row N); no reversal surface exists — a reversal is a NEW evidence class with its own record" (it does NOT name `assign-intent` as the recovery, because `assign-intent` refuses a non-NULL row by F5; a mistaken attestation is a register item, banked for CHARC, not a channel in 22-B). Branch (b) REJECTED: an overwritable attested column makes the attestation row and the journal disagree by design, and the drift reader's "attested, column now X" is a report about a defect the schema could have refused. **Discriminators (§4.2 gains them):** TestClient review POST for an attested trade WITHOUT the field → preserved; WITH `""` → 4xx typed, preserved; CLI `trade review --entry-intent standard` on an attested trade → `ClickException` with the message; `backfill-intent --force` → the row skipped, message printed, value preserved; the corrector on `("trades","entry_intent")` → `ReservedJournalFieldError`; a raw `UPDATE trades SET entry_intent='standard'` on plain sqlite3 → the trigger ABORTS (the layer-3 twin, tested alone); the same-value UPDATE → passes. **N4 EXTENSION — RD's recommendation at his plan read, ADOPTED by CHARC at the §3 pass (2026-09-23, on `351b2e60`): the value is UNWRITABLE WITHOUT ITS ATTESTATION, at the schema.** AL-1 rested on writer-absence, the argument declined three times for `latch_view_events`; the same standard applies to the journal's own column. Shape — TWO NEW `trades` triggers, siblings of N4 (N4's ruled text is UNTOUCHED; one trigger, one refusal, one message — the refusal-legibility rule), created in the SAME 0040 section as `entry_intent_attestations` (after the table's CREATE, since their bodies name it): `trg_trades_entry_intent_unattested_update` — `BEFORE UPDATE OF entry_intent ON trades FOR EACH ROW WHEN COALESCE(NEW.entry_intent,'') = 'unintended_execution' AND NOT EXISTS (SELECT 1 FROM entry_intent_attestations WHERE trade_id = NEW.id)` → `RAISE(ABORT, 'entry_intent unintended_execution requires its attestation row; use swing trade assign-intent')`; and `trg_trades_entry_intent_unattested_insert` — `BEFORE INSERT ON trades FOR EACH ROW WHEN COALESCE(NEW.entry_intent,'') = 'unintended_execution'` → the same ABORT (an attestation cannot precede its trade — the FK — so an INSERT carrying the value is always unattested; this twin also fires on the INSERT half of `INSERT OR REPLACE`, which narrows AL-3's `entry_intent` facet without closing AL-3). Both `WHEN`s are TOTAL (`COALESCE` + `NOT EXISTS`) and join `TOTAL_WHEN`. **AUTHORIZE-THEN-ABORT holds by construction:** every generic writer already refuses the value at the seam (F5, typed), and the sole writer `assign` INSERTs the attestation THEN UPDATEs `trades` inside one `BEGIN IMMEDIATE` — so the SQL now enforces that order in BOTH directions (`trg_eia_trade_binding` requires `trades.entry_intent IS NULL` at the attestation's insert; the update twin requires the row at the trades update). **Discriminators (Task 4 / Task 11 `SCHEMA_ONLY`, raw on plain sqlite3):** `UPDATE trades SET entry_intent='unintended_execution'` on a row with NO attestation → ABORTS with the twin's message (names `assign-intent`); the same UPDATE with a raw-planted attestation row → passes; `INSERT INTO trades (…, entry_intent) VALUES (…, 'unintended_execution')` → ABORTS; the service's own write (b22_80) passes through the real triggers; the REVERSED service order (trades UPDATE before the attestation INSERT, monkeypatched) → ABORTS — the order is pinned by execution, not by reading the source. **Consequences:** AL-1 is REWRITTEN (schema-enforced on UPDATE and INSERT; residual = a fabricated attestation row, outside the threat model per R0.K, and the REPLACE facet AL-3 already declares); `drift_report` (iii) stays as a check; RD's UNATTESTED renderer token stands as ruled (the detector for what the schema cannot see); the D51 additions gain the two triggers (b22_25's constant); the cross-table census for any FUTURE rebuild of `entry_intent_attestations` reads TWO rows TEXTUALLY and ONE real reference (corrected at G1, orchestrator-measured, CHARC-accepted: the UPDATE twin's `NOT EXISTS` is the real reference; N4's row is its RAISE message text only; the INSERT twin does not name the table — the textual census is a SUPERSET, as R0.J stated, and the 0040 header records both counts; the R0.J class, covered by the forward rule). Minor scope at a director gate, encoded by replacement in the executing dispatch; Reviewer A `strong` reviews the code; no counted plan round. **FORK R1-1 (Reviewer A round 1; CHARC-ruled 2026-09-24): (a) — a DECLARED LIMITATION with its reason, plus two in-scope fixes.** The review surfaces pre-check the attestation with a plain read, `complete_trade_review` commits in its OWN `with conn:` (`review.py:632`), and the intent write runs in a SECOND `with conn:` — so an `assign-intent` committing between the pre-check and the review commit leaves the REVIEW committed and the INTENT refused. **Why (a):** the composed end state is CORRECT — the review's fields persist exactly as submitted, the intent column is never written over an attestation (the service re-check R1-2 + the trigger), and the refusal is typed; the window needs a concurrent CLI assign during a review submit by the same single operator. **(b)/(c) REJECTED on three grounds:** `swing/trades/review.py` is OUTSIDE the carve-out (a §3 widening at the executing stage for a race whose outcome is correct); the route's own comment records a LANDED LOCK — "entry_intent is independent of review state, so it is NOT folded into complete_trade_review (L2/L5 lock)" — which (b) and (c) would break; and under (b) a refused intent change would ROLL BACK the operator's review text, converting a legible refusal into a lost review — the worse composition. **Fixes, in scope:** the web's second call (`routes/trades.py:3655`) is wrapped so `AttestedIntentError` renders as the route's 409 refusal fragment, never a 500 (legibility) — **RE-RULED at Reviewer A R3-1 (2026-09-24; the reviewer took AL-6's invitation and found the MESSAGE half unsound, CHARC concurs): on the CONCURRENT path, where the review has already committed, the refusal on BOTH surfaces names the committed review FIRST and the refused intent change SECOND — never a message that implies the review was refused (D39's message rule: a message says what was observed, and what was observed is a committed review plus a refused intent write).** CLI (`cli.py:1760-1769`): the EXISTING "Review recorded for trade #N (TICKER). Process grade: G." echo is emitted BEFORE the intent write, so it precedes any refusal (RULED at R4 item 1, option (b): the ruling's substance is the ORDER and that the committed review is named first; the "(state reviewed)" wording in the first text was illustrative, and replacing the grade / failure-mode confirmation the operator reads on EVERY review for a message ruling would be a regression — the pre-existing echo, ASCII-safe and written through the 22-A3 post-durability sink after R4-1, stands; a ruling that places an I/O between two writes carries the precondition that the I/O cannot fail, which R4-1 supplied); the refusal is then a `ClickException` reading "Intent change REFUSED: <attested message>" — the NONZERO exit stands, because the part the operator asked for did not happen and a zero exit would be a false all-clear on it (RD's direction rule). Web (`routes/trades.py:3674`): the concurrent call's 409 body reads "Review for trade #N was recorded (state reviewed). The intent change was refused: <attested message>."; the PRE-CHECK path's message is UNCHANGED (nothing was recorded there) — so `_attested_refusal` composes the message from whether the review committed, ONE fragment, two texts. AL-6's declared reason is amended by replacement to "a legible refusal that NAMES THE COMMITTED REVIEW"; the two-transaction window itself stays declared as ruled. Discriminators: b22_228 asserts BOTH sentences in the 409 body (the committed-review sentence AND the attested message) with state `reviewed` and the intent unwritten; a CLI twin plants the same race through `swing trade review --entry-intent` and asserts the recorded line in the output, the refusal line, a nonzero exit, and the same row state; the pre-check path's message asserted UNCHANGED (no "recorded" sentence on a refusal that recorded nothing); Task 7's "NOTHING written" is corrected by replacement to "the intent is never written; the review's own fields persist as submitted", with the window DECLARED in the plan's accepted-limitations list WITH this reason (the A prompt carries it and invites the challenge). b22_121/123's byte-identical assertions stay for the non-concurrent path; the concurrent window IS tested — b22_228 (the web, since G7a) and the R3-1 CLI twin plant the race by execution (CORRECTED by replacement 2026-09-24: the earlier "a window test is not required" sentence was falsified the moment G7a planted the race and stood in normative voice until the orchestrator flagged it at the R3-1 landing — the discharged-deferral class, in my own text). **FORK R1-3 SHAPE (RD ruled the IF — one render must be internally consistent on the four decision readers, a declared limitation REFUSED; CHARC rules the HOW, 2026-09-24; RE-RULED BY REPLACEMENT at FORK R1-3-SHAPE-EXEC the same day — the snapshot bracket is WITHDRAWN, the assert stands alone): THE DISJOINTNESS ASSERT WITH NO BRACKET, one shared helper in `swing/metrics/cohort.py`, name-closed over the four readers.** (0) Why the bracket is withdrawn: every governed reader runs the tier-2 replay BETWEEN its counted read and its naming read (the R2-04 tier-(1) ordering, 22-A2 A-R2 item 4: `stats.py` loads → tier-2 → naming; `tier.py:724 → :736 → :776`; `hypothesis.py` `compute_tripwire_status` `:569`/`:571 → :610`; the card `:500 → :502 → :462`), and the replay REFUSES under any caller-held transaction by a landed 22-A2 rule (S12.1 #9, G-T7 Q1: `frozen_value_evidence.py:1330-1331` returns `VERDICT_UNVERIFIABLE` / `caller_holds_transaction`; `:1510-1511` skips the ref resolution — no git, no network inside a transaction). A deferred `BEGIN` sets `in_transaction` at once, so the ruled bracket converts every admitted tier-2 trade into a FALSE EXCLUSION in the authority position (cell 11's probe on one reader, reverted: 18 of 111 red, `admit` → `tier2_unverifiable`), and its SHARED lock blocks the rollback-journal test worlds' same-thread commits (`database is locked`). The bracket was ruled without measuring its precondition against the readers it brackets — the ruling-states-its-preconditions rule, CHARC's own, owned. (1) The instrument, RD's constraint (i): `assert_intent_exclusion_disjoint(counted_ids, named)` in `cohort.py`, called by each reader AFTER its naming read and BEFORE it populates `intent_excluded`; `counted_ids & named_ids` non-empty → a typed `CohortReadRacedError(ValueError)` ("cohort read raced an intent write; re-run"), rendered as each surface's refusal (CLI: `ClickException` at the boundary; web: the route's degraded message) — NEVER the contradictory row, never a partial row. (2) Why the assert alone is SUFFICIENT, not merely cheaper — the argument the helper's docstring carries, its preconditions named at the schema: the counted predicate and the naming predicate (`list_intent_excluded_for_cohort`, `cohort.py:134`) differ ONLY in the intent test, so the intersection is reachable ONLY by an intent value moving between the two reads; and that move is MONOTONE, counted → excluded, never back: `unintended_execution` is TERMINAL on UPDATE (`0040:325` `trg_trades_entry_intent_attested_terminal`), unwritable without its attestation row (`:831` / `:838`, the unattested twins), and the attestation table is append-only (`:808` no-delete, `:816` no-replace, `:776` no-update save the fill-id null-out). So every interleaving is one of three: the write lands before the counted read → excluded AND named, consistent; after the naming read → counted and NOT named, consistent (it was not excluded when read); BETWEEN the reads → counted AND named, which is exactly the intersection the assert detects. No fourth state is reachable. PRECONDITION NAMED FOR THE FUTURE: any writer that moves a trade from excluded back to counted — a reversal surface, register candidate (i), which N4 says does not exist — invalidates this argument; the helper's docstring says so, and the arc that builds a reversal re-rules this fork. (3) RD's constraint (ii) BY CONSTRUCTION, unchanged: names and the UNATTESTED token come from the ONE naming statement; a stale flag can only false-alarm (the comment states the asymmetry). (4) The cost, stated: a live race yields a typed re-run refusal instead of a silently consistent read — loud, single-operator in incidence, and the re-run reads clean. REJECTED at this fork: (a) open the bracket AFTER the tier-2 read and re-read the counted rows inside it (touches R2-04's ordering and N2 (a)'s read, and manufactures a second transient — a tier-2 set computed over rows the re-read no longer matches); (b) the tier-2 read before the bracket (reverses R2-04 tier (1), a landed 22-A2 ruling, for a transient). Standing rejections unchanged: derive-from-loaded with a Python-side filter; the A-R2 recheck loop. **Closure:** b22_130's static walk gains ONE obligation — every GOVERNED reader calls `assert_intent_exclusion_disjoint` (a new governed reader FAILS until it does); `cohort_read_snapshot` is NOT built. **Tests, the race planted BY EXECUTION per RD:** the naming read is monkeypatched to first commit a REAL `assign(...)` on a counted trade over a SECOND connection, then run the original — one case for the in-memory shape (`stats.py`) and one for the SQL shape (`tier.py`); `hypothesis.py` and the card are covered by the shared helper plus the name closure. Discriminators: pre-fix (no assert) the render CONTRADICTS itself → RED on the rendered row; post-fix the same planted race → the typed error, never the contradiction; the boundary twin — the same `assign(...)` committed BEFORE the first cohort read → the consistent excluded-and-named row and NO error, so the assert is pinned to the intersection and not to the write's existence. The "bracket no-op" third test is retired with the bracket. The unit asserts the tier-2 admission of every fixture trade UNCHANGED (the false exclusion the bracket produced is the regression this fork exists to refuse). **FORK R1-3-SURFACES (CHARC-ruled 2026-09-24 on cell 12's census of the readers' CONSUMERS — a census my R1-3 rulings never asked for: "each surface renders its refusal" named the four readers' own pages and not the pages that consume them, existence ≠ completeness in my own ruling, owned): CONTAIN ON EVERY SECONDARY PANEL; THE GOVERNED PAGE DEGRADES, NEVER 500s; THE PREFILL DEGRADES.** The asymmetry that decides every item is R1-1's: cohort bookkeeping never blocks, fails or mislabels a money-bearing operation; and a 500 is never a refusal (the D34 class — an exception escaping every handler on an operator path reaches the operator as a 500 wearing the right words). ONE idiom, applied by the ROLE of the read on the page: (i) where the cohort read is a SECONDARY panel of a page doing something else, the panel CONTAINS `CohortReadRacedError` ONLY (never a bare `except`; the degraded state renders VISIBLY with the ruled text and is logged at WARNING — the silent-skip-without-audit gotcha #27), and the page renders: `POST /trades/{id}/exit` — NO handler of its own (RE-RULED at Reviewer A R2-2, 2026-09-24: the exit's post-commit rebuild calls `build_dashboard`, which contains the race at its only governed read per the dashboard clause below, so a handler at the exit route is UNREACHABLE by construction — a dead guard reading as coverage; the original clause was ruled without tracing the one call between the two sites I ruled, the composition class in my own packet, owned). In a real race the exit returns its ORDINARY success fragment (the row hidden, the status strip refreshed from the same `dashboard_vm`), because the exit response does not render the hyp-recs panel and a notice about a panel not on screen would name what nobody observed (D39's message rule; branch (A) REJECTED on that ground). A durable exit is NEVER reported as a failure — delivered where the read lives, not at each consumer (a per-consumer handler is a roster of `build_dashboard`'s callers); `GET /`, `POST /prices/refresh`, `/hyp-recs/refresh` — the hyp-recs panel is isolated at its builder (`dashboard.py:578`, the `build_recommendation_progress` call) the way `/metrics` isolates cards: the panel reads "hypothesis progress unavailable: cohort read raced an intent write; re-run", every other panel renders. (ii) Where the page IS the governed read (`/metrics/hypothesis-progress`, `/metrics/tier-comparison`, `/metrics/deviation-outcome`), the route renders its page with the governed region carrying the refusal text — the overview's card-suppression idiom, status 200 — never the app-wide 500 (the alternative the packet offered, "accept the 500 carrying the ruled text", is REJECTED: a 500 is the D34 shape whatever its body says). (iii) The ENTRY PREFILL (CLI `swing trade entry` without `--hypothesis`, `cli.py:828`; the web entry-form GET, `view_models/trades.py:580`) DEGRADES: the suggestion is skipped and the entry PROCEEDS; the CLI prints one ASCII line — "hypothesis suggestion unavailable: cohort read raced an intent write; pass --hypothesis or re-run" — and the web form renders WITHOUT the suggestion and WITH that line visible beside the field (the suggestion's NEGATIVE state renders as a statement, never as a silent blank — the asymmetric-surface question). The prefill SUGGESTS a label from counts and renders no cohort row: "never a contradictory row" does not reach it, and a `ClickException` there refuses a REAL TRADE for a bookkeeping transient — the exact inversion R1-1 named; `cli.py:828`'s refusal is removed. MESSAGE TEXT (the packet's item 4, ruled because the text is mine — a message says what was OBSERVED, never a cause it did not observe; D39's banked message rule): `CohortReadRacedError` reads "cohort read counted and named the same trade(s) {ids}: a concurrent intent write, or the counting and naming predicates disagree; re-run" — the ids named, BOTH causes named, so a code defect (b22_136's coercing mutant) is loud AND correctly labelled; this supersedes "cohort read raced an intent write; re-run" wherever the ERROR is rendered; the panel and prefill lines above keep their short form because they render a degraded state, not the error. BANKED, not this arc's (pre-existing; the encoding commit states its `git log` provenance): the exit route's post-commit rebuild is uncontained for ANY exception while 22-A3 contained the entry route's — the register carries it as the exit-side twin of 22-A3; this arc adds NO handler at the exit route — the typed error is contained where the read lives. CENSUS STATED: the encoding commit records the consumer census BY METHOD (every caller of the four governed readers and of `lookup_active_recommendation_label`, one hop, the `grep` stated), so the next reader re-runs it rather than inherits it. DISCRIMINATORS, the race planted by execution as in R1-3: exit (b22_237 REPOINTED at the real path — never a wholesale monkeypatch of `build_dashboard`, which tests a stand-in) — the race planted BY EXECUTION inside the real builder (the naming read under `build_recommendation_progress` commits a real `assign(...)` over a second connection, R1-3's technique) → the trade is CLOSED in the DB, the response is the ORDINARY success fragment at 200, AND the dashboard builder's WARNING record ("hyp-recs panel degraded") is captured via `caplog`, proving the planted race REACHED the containment on the operator's path (the reachability claim lives in a test; a 500 FAILS; an unclosed trade FAILS; no WARNING FAILS — the race was not reached); dashboard — the planted race → 200, the hyp-recs panel carries the text, every other panel renders; the three metrics pages — 200 with the refusal text in the governed region (b22_236 flips from MEASURING the 500 to REFUSING it); prefill CLI — the planted race → the trade is RECORDED and the warning line is in the output (BOTH asserted); prefill web GET — 200, no suggestion, the line rendered; boundary twins — no race → the suggestion present and every panel populated; the `/metrics` overview's existing per-card isolation asserted UNCHANGED. **FORK R5-1 (Reviewer A R5, CHARC-ruled 2026-09-24): FIX IN 0040, never a declaration.** `trg_eia_tier2`'s inner `json_each(..., '$.telemetry_rows') e` reads `json_extract(e.value, …)` with NO `e.type = 'object'` guard (`0040:712-720`, read), so a non-object MEMBER reaches `json_extract` raw: a plain string member raises `OperationalError: malformed JSON` (cell 15, by execution) — which the plan's OWN R2-05 rule (plan:185) forbids: "a malformed blob is then the trigger's own ABORT, never an `OperationalError`". R2-05 was applied to the top-level columns and not to array MEMBERS; the member is the same class one level down. Declaring under R0.K is REJECTED: R0.K is the THREAT MODEL for forged rows; this is the barrier's LEGIBILITY contract (authorize-then-abort's illegible-refusal facet, SS-4), which the plan states as its own and which 0040, UNAPPLIED on live, can still honour at file-edit cost (no D60 replay question arises — no row exists under the old text). Severity: MAJOR, in-plan-contract, outside the threat model — the printed CRITICAL is overstated. Shape, the reviewer's: every member read is guarded — reject any member whose `e.type <> 'object'` as the trigger's own ABORT, and extract under a `CASE WHEN e.type = 'object'` guard (a serialized-object STRING member is thereby REFUSED too: right values in the wrong encoding, which the service never writes). Discriminators (b22_48 extended to members): a plain `"x"` member and a serialized-object string member each → the trigger's own `IntegrityError`, never `OperationalError`; an object member → the control passes. THE SWEEP RIDES AS ONE UNIT, uncounted (the orchestrator's SS form): all 22 `json_each` sites in 0040 read for the same untyped-member class (any `json_extract(e.value, …)` or `json_each(e.value)` over a member without a type test), fixed together, the count and the method stated in the encoding commit; the manifest fixture moves for exactly the triggers touched and the gate's D51 diff names them as changed pairs. Then round 6 as ONE confirming round on the settled tree. **FORK R6-1 ITEM 1b — THE SHAPE (CHARC-ruled 2026-09-24, on RD's 1a: a placement session strictly after the trade's entry date REFUSES under the new typed code `placement_after_entry`; equality passes): THE ARC'S THREE-LAYER PATTERN, one bound in four places, each a twin of a site that already exists.** (1) SERVICE FIRST, typed: in `entry_intent_assignment.py` at the ISO-shape step (`:475-481`), immediately after `_is_iso_date(placement)` establishes source `schwab_envelope` and BEFORE either leg runs — `if placement > entry: raise _RefusalError("placement_after_entry", …)` with RD's message (ASCII, both dates through `ascii()`, the recovery named: the supported entry-date correction path if the trade's date is wrong; 22-A's identity path if the envelope is another order's document; NOTHING substitutes one date for the other). The comparison is BYTEWISE over two CANONICAL ISO dates and is admissible ONLY because both are shape-checked first — `trades.entry_date` by `source_date_malformed` at preflight (`:219`, the G4 ruling) and the envelope value by `_is_iso_date` at `:475` — which is what keeps this outside D38's lexical-predicate class; the comment at the site says so. The fallback source needs no branch: `placement = entry` by construction, and the bound holds trivially. (2) THE SQL TWIN, in `trg_eia_tier2` (`0040`, the `contemporaneous_record` trigger) as ONE added conjunct inside the existing placement clause at `:676-678` — `AND NEW.placement_session <= NEW.trade_entry_date` — binding BOTH sources in the same predicate that already pairs the fallback (the exact shape RD named as G1b-permitted: two STORED columns, no envelope read); the WHEN stays inside `NOT COALESCE(…, 0)` (CORRECTED at R7 item 1(a) on the orchestrator's measurement: `placement_session` is NOT NULL on this tier by the composite CHECK (`0040:474`) and `trade_entry_date` by its COLUMN (`0040:393`); and because the BEFORE trigger runs ahead of the CHECKs, a NULL placement would fail CLOSED inside `NOT COALESCE` — the SQL comment cites both). NOT a table CHECK: a CHECK's message is generic and a CHECK can PRE-EMPT the trigger's own message on a discriminator (SS-13, D38's fourth facet), whereas the trigger's RAISE names the refusal; no NEW trigger, so SS-12's newest-fires-first ordering is untouched. The trigger's predicate stays a SUBSET of the service's (authorize-then-abort): the service refuses first with the typed code, the trigger is the backstop for a raw write. (3) THE MODEL MIRROR: `EntryIntentAttestation.__post_init__` gains the same bound (`placement_session <= trade_entry_date` when placement is non-NULL) — the #11 mirror family, so the SQL-vs-Python drift comparator remains the instrument that defends the set. (4) THE ADMISSION MAP: a new predicate row for `trg_eia_tier2`, `placement_not_after_entry`, citing the service check and its tests, so the closure asserts the twin in BOTH directions. TIER SCOPE, answered from the schema and RD's fact (3): `placement_session` is NULL on `structural` by CHECK (`0040:465`), so the bound lives on tier 2 only — there is no other tier on which placement exists. D51: the manifest line moves for `trg_eia_tier2` ONLY (again; the gate reads it as the same changed object); D60: 0040 is unapplied, no replay question. DISCRIMINATORS (RD's five, plus the twin's own): envelope `entry_date` = trade entry + 1 session → `placement_after_entry`, nothing written, over the SERVICE (typed) AND over the SQL twin (a raw INSERT on plain sqlite3 with `placement_session` one day after `trade_entry_date` → the trigger's OWN ABORT with its message; the same row at equality → passes to the legs); the BOUNDARY twin at equality passes and the legs decide; the three live EARLIER cases (19, 20, 22) keep their verdicts, trade 20 admitting on leg 2 exactly as before; the model mirror refuses the same one-mutation row and admits equality; the drift comparator UNCHANGED; and cell 16's own probe (entry 08-07, envelope 08-09, view 08-08) flips ADMIT → `placement_after_entry` — the RED the fix produces first. R6-2 rides with it (`ascii(order_id)` at `:440-442` and `:447-452`, one non-ASCII test). Round 7 as the next confirming round in a fresh cell. **R7 ITEMS (CHARC-ruled 2026-09-24):** (1) the three encoded differences CONFIRMED — the NOT NULL fact above; `trg_eia_tier2`'s RAISE text naming the new reason with the `TIER2_RAISE` prefix unchanged (one trigger, one message, now complete); the service recovery naming `swing journal correct-entry-date` (exists, `cli.py:2652`). (2) R7-1's DRY-RUN echoes under the 22-A3 sink CONFIRMED: the exit code is the assignment's verdict, never stdout's; the dry run writes nothing, so the sink costs nothing. (3) R7-1's SIBLING — `conn.close()` in `assign-intent`'s `finally` raising AFTER the commit — is FIXED IN THIS ARC with the 22-A3 shape the entry command already carries (`cli.py:969`: the durable row is reported, the close failure is a WARNING, exit 0): `assign-intent` is 22-B's own command, so DEFAULT TO INTRODUCED decides it — "pre-existing-shape twin" is a class label, not provenance. Discriminator: `close()` monkeypatched to raise after a real commit → the attestation row is durable, the warning names the close failure, exit 0; the same raise BEFORE the commit → the ordinary refusal path (CLARIFIED at R8 item 1, CHARC 2026-09-24, option (b): "the ordinary refusal path" means the OPERATOR SEES THE REFUSAL — the typed REFUSED text survives a close failure, raised with the close error CHAINED and NAMED in the message ("…; the connection close also failed: <ascii error>"), exit nonzero, nothing written. A close raise that MASKS the refusal reports the wrong failure — D39's own template (report the cleanup failure CHAINED from the original, never instead of it) — and the fact that the masking pre-dated the fix does not make it the ordinary path; it makes it a pre-existing instance of the class on a command that is 22-B's own. The entry command's c2 pre-boundary re-raise (22-A3) is NOT touched: that is 22-A3's shape, banked as a sibling for the D39 sweep. b22_252 flips from asserting NO output to asserting the refusal text present, the close failure named, the exception typed, nothing written on a fresh connection. SCOPE (R8 follow-up, CHARC 2026-09-24, option (i)): EVERY refused result, DRY RUN INCLUDED — the branch condition is `result is not None and not result.admitted`; "non-dry-run" in the R8 text was copied from the DURABILITY boundary's definition, where it belongs, and is not a scope choice here: the reason (the operator sees the refusal) applies to a refused dry run identically, and a dry run writes nothing either way. b22_253 (c), the admitted-dry-run control, unchanged; a refused-dry-run twin added.) (4) OUTSIDE THE ARC, BANKED: the pre-existing `backfill-intent` prompt line echoes the raw ticker without `ascii_safe` — a register row at CHARC's next charter touch (a compaction touch) and a post-merge witness-free rider on the orchestrator's queue beside D67. Round 8 as the next confirming round in a fresh cell, on the item-3 encoding. (5) REVIEWER B (0/4/0 on branch 16becf0c, code a868f58b), RULED (CHARC, 2026-09-24, ONE packet, four items; the orchestrator's proposed dispositions taken on items 1-4 with the precisions below). ITEM 1, B-01 (RULING-CHALLENGE to R7): option (a) -- DECLARED as AL-7 with its REASON: `assign`'s COMMIT can commit and then lose its return, leaving the caller's `result` unbound, so `assign-intent` exits nonzero over a durable attestation; unlike the entry command (22-A4), a RETRY CANNOT DUPLICATE -- `entry_intent_attestations.trade_id` is UNIQUE (0040:389) and the value is terminal -- so the retry refuses `already_set` and names the value it finds (b22_112 pins the exact line); the window's whole cost is one wrong exit code and one legible retry, and 22-A4's token existed because an entry retry duplicates a money-bearing row. This is NOT a contradiction of R7 item 3: R7 moved the read-back before the COMMIT and named the COMMIT as the boundary; B-01 is the residual R7 left on the COMMIT's OWN return, which no ordering inside the transaction removes. Two riders: (i) the `assign-intent` durability-boundary comment ("as does any exception out of `assign` (`result` stays None)") is FALSE for a COMMIT that commits and then raises -- corrected BY REPLACEMENT to state the residual and name AL-7 (gotcha #31: a comment describes what the code does today; this one described what it does not do); (ii) AL-7's entry carries the reason above and the invitation to challenge it (the accepted-limitations rule); its evidence is the existing retry line, NO new test -- nothing plants "COMMIT commits then raises" without monkeypatching the connection, and a monkeypatched COMMIT tests the mock. Option (b) REJECTED on proportion: a schema change to an unapplied migration plus a fresh-connection settle, against the 09-29 stop line, for a window whose worst outcome is one legible retry. REOPEN PRECONDITION: a second writer of attestations (none exists -- acceptance test 5, `assign-intent`'s service is the only writer). ITEM 2, B-02: FIX IN ARC with the two shapes already ruled, the handler stated whole so it is written once: `trade review` binds TWO durability facts, `review_committed` after `complete_trade_review` returns and `intent_committed` after `update_entry_intent`'s transaction exits, each bound at its writer's return, never cached earlier; the close handler RE-EVALUATES: (i) a refusal in flight -- the pre-commit ValueError from `complete_trade_review`, the AL-6 race, or the ValueError intent refusal -- raises that refusal with the close error NAMED and CHAINED (R8's shape; message order per R3-1: committed review first, refused intent second, the close failure third), exit nonzero; (ii) nothing refused and every write durable -- the 22-A3 post-commit close shape: the close failure logged contained and a WARN on stderr through the contained sink, exit 0. The pre-commit refusal is IN (R8-SCOPE's reason -- the operator sees the refusal -- applies identically, and the command is one 22-B changed: DEFAULT TO INTRODUCED covers the whole `finally`). Discriminators, a close that raises after really closing: (a) a review alone -> exit 0, WARN present, the review durable; (b) a review plus an intent change -> exit 0, both durable; (c) the AL-6 race -> exit nonzero, the R3-1 text PRESENT, the close failure named, chained; (d) a pre-commit refusal -> exit nonzero, the refusal text present, nothing written; plus the no-close-failure CONTROL: b22_240/241 byte-unchanged. One sonnet-high cell, no Codex; every other branch byte-unchanged and the encoding says so; B (the finder) RE-READS the hunk and the full suite runs on the head -- no A round (A examined no close-failure path in `trade review`; the change lives only there). ITEM 3, B-03: CONFIRMED BANKED, no re-rule -- my predecessor's R8 mail and charc-state.md #2 row (ii) already carry it: `backfill-intent`'s per-row commits followed by uncontained output and a bare close (cli.py ~:2025-2039; reachable on every interactive run), with its raw-ticker prompt line; a register row at CHARC's compaction touch, a post-merge witness-free rider beside R7 item 4 and D67. ITEM 4, B-04: BANKED for the D39 sweep as proposed, two precisions. (i) PROVENANCE, stated honestly in the ledger: the `legacy_alter_table` restore is INTRODUCED (R0.J) and is one more raiser on an EXISTING post-COMMIT path (the `foreign_keys` restore beside it, `db-migrate`'s close and echo, cli.py ~:419; 0038:124 and 0039:2311 COMMIT in-script the same way) -- the arc extended a class, it did not create one, and the fix is runner-wide, which is the sweep's shape; label it INTRODUCED-BY-EXTENSION, BANKED WITH A COMPENSATING CONTROL, never PRE-EXISTING. (ii) THE CONTROL IS BINDING at BOTH gates: the witness script's post-`db-migrate` step and CHARC's merge-gate read `SELECT version FROM schema_version` (mode=ro) and `schema_manifest.py --db` INDEPENDENTLY and never read the exit code as the verdict; a nonzero exit with v40 present and the manifest clean is REPORTED as B-04's shape by name, not as a failed migration. Noted without action: claim 7 was the orchestrator's synthesis, owned; the gate reads brief section 4, and B-01/B-02 are dispositioned on their merits. THE MERGE PACKET stands as listed in the R8 mail, on the head that carries the item-2 encoding and B's re-read of it, plus the item-4 post-migrate reads. (6) B RE-READ (0/0/1 on 27b7f04f, code 3e9085e0), B2-01 RULED (CHARC, 2026-09-24): option (a) -- KEEP the behaviour, correct the comment BY REPLACEMENT (the same three lines; no census pin moves). The case: a review COMMITS, a NON-refusal exception is then raised (an OperationalError from the intent write, a KeyboardInterrupt), and close() raises too; with `review_committed` true the wrapped handler WARNS the close failure truthfully and lets the ORIGINAL exception resume -- nonzero exit, both errors observable. That is D39's own direction (a cleanup failure never replaces the original) and exactly what RULING B (ii) asked for on a durable path; RULING B's "every other branch byte-unchanged" is READ AS SCOPED to paths with NOTHING DURABLE -- my sentence understated the fix's reach, and the comment copied my sentence rather than the code (gotcha #31, on my own text: the comment claimed what the code no longer does). Option (b) REJECTED: routing an in-flight non-refusal exception back through the bare close to restore byte-parity would re-create, on a path where a review IS durable, the masking class R8 just closed -- parity with a defect is not a property worth a cell. No test: the comment change is prose; the behaviour is already pinned by b02_a..d and B's checks 2-5. Then the trial merge, the merged-head suite, and the merge packet on that head. (7) THE MERGE GATE, RULED (CHARC, 2026-09-24 ~16:10Z): CLEAR for the real merge at the witness sitting, on 22-b-exec @ 635e1096 (code e353b097), trial merged head 863329aa, tree 2c1b4be6 -- every item of the packet RE-MEASURED at this seat, by method: (1) REVIEWER B ON THE TREE: `reviewer-b-response.txt` 4,120,007 B, exit file 0, banner `gpt-5.6-sol`, `^ERROR` 0, `^tokens used` at line 35284, `^NEW_CRITICAL_MAJOR_FOUND` 2 / `^NO_NEW_CRITICAL_MAJOR` 0 -- 0/4/0, all four dispositioned under RULING B (AL-7; fixed at 3e9085e0; banked; banked-with-control); the re-read `reviewer-b2-response.txt` 789,533 B, exit 0, `^ERROR` 0, footer at line 5235, CLEAN 2 / FOUND 0 -- 0/0/1, B2-01 ruled (a), the comment fixed at e353b097; the two passes cover the code delta e61dad27..e353b097, the trial tree is BYTE-IDENTICAL to 22-b-exec's (`rev-parse HEAD^{tree}` on both = 2c1b4be6), so B ran on the tree being merged. (2) THE MERGED-HEAD SUITE: the evidence file's line 1 names 863329aa; 13413 passed / 13 skipped / 0 failed (operator-run, -n 8); trailers over e61dad27..HEAD: none. (3) THE D51 DIFF, regenerated here main vs 863329aa parsed by key: CHANGED 2 (the header 39->40, table `trades`), ADDED 18, DELETED 0, `trg_eia_tier2` AMONG THE ADDITIONS -- the expected shape exactly; `schema_manifest.py --check` on the trial head: clean; ruff clean. (4) THE MANIFEST COMPOSITION PROVEN BY EXECUTION (the 22-A2 lesson): a backup-API copy of the LIVE DB (mode=ro source, WAL-consistent, 1,673,039,872 B, 28 trades) migrated 39->40 by `run_migrations` under the TRIAL code -- `swing.__file__` asserted inside `.worktrees/22-b-trial`, `EXPECTED_SCHEMA_VERSION` asserted 40 (the editable install resolves MAIN otherwise) -- in 11.8 s: `SELECT version FROM schema_version` = 40; `PRAGMA foreign_key_check` = []; `quick_check` ok; `legacy_alter_table` restored to 0; all 19 promised objects present; `schema_manifest.py --db <copy>` under the trial code: "schema_version 40 / clean". Copies deleted after the ruling; the LIVE DB was never opened for write (its version read 39 on mode=ro before and after). (5) THE 22-A SWEEP: `tests/trades/test_22a_envelope_canonicality_sweep.py` on the trial head, 80 passed / 7 skipped / 0 failed -- and OWNED: my predecessor's term "the 22-A sweep at zero offenders" has NO definition on disk (charc-state and the ledgers name no instrument); the orchestrator's reading is accepted and IS the definition from here -- a handoff names a term of art with a pointer (harness-architecture 5.1's sibling rule), and mine did not. (6) THE RUNTIME COMPOSITION READ is the witness sitting's, BY CONSTRUCTION (the guard makes a live run between the merge and the migration unrunnable), and its W0-W4 are BINDING CONDITIONS of this clearance, one step per operator result: W0 `swing web` STOPPED -- pid 48408 is STILL LISTENING on 8080 at this reading (started 2026-09-23 06:51, loaded pre-merge, `EXPECTED_SCHEMA_VERSION` 39 in its process; it MUST be stopped before, restarted after -- the CLAUDE.md long-running-holder gotcha); W1 plain `sqlite3` `BEGIN EXCLUSIVE; ROLLBACK` on the live file, the RESULT stated (a refusal names a holder the process list did not); W2 the real `--no-ff` merge onto whatever main then is, `EXPECTED_SCHEMA_VERSION` = 40 read from the merged code; W3 `swing db-migrate` -- expect ONE backup image, `swing-pre-22b-migration-<UTC>Z.db`, in `backups_dir` (CORRECTED BY REPLACEMENT 2026-09-24 ~16:15Z on the orchestrator's flag, verified at this seat on 863329aa: `cli.py:283-289` D32/D50 F2 branch (b) -- a gate covers the transition, so the gate writes its integrity-verified image into `backups_dir` via `ensure_schema(db_path, backup_dir=backups_dir)` at `:354` and the CLI takes NO copy, then asserts exactly one new image appeared at `:357-368`; `BackupGateSpec(39, "22b", ...)` at `db.py:767`. My original "TWO backups" lifted the CLAUDE.md gotcha that has been STALE since D32/D50 merged, and my proof run called `run_migrations` directly with `backup_dir=None`, which is why its image landed beside the copy -- the proof did not exercise the CLI branch, and I stated the gotcha as a fact; owned. Two images, or one beside the DB, is REPORTED as a discrepancy, never assumed), and per RULING B item 4 its EXIT CODE IS NOT THE VERDICT; W4 `SELECT version FROM schema_version` (mode=ro) = 40, `schema_manifest.py --db` clean, `PRAGMA foreign_key_check` = [] -- a nonzero W3 exit with W4 clean is REPORTED as B-04's shape by name; then section 5's witness and RD's step 5. Timing as the orchestrator stated: not across the 17:30 HST pipeline; before 09-30 EOD HST. The operator chooses the sitting; CHARC closes 22-B on the witness result.
- **F6 — REBUILD SCOPE. RULED (CHARC):** ONE edit to the `trades` DDL. NO other `trades` change rides this rebuild (no column added, no CHECK strengthened, no D58 fence) — a rebuild is the riskiest migration shape and it carries exactly the change it exists for. D58 stays its own arc.

Any further fork the census finds is ADDED to the packet with both branches and a named ruler, not decided in the plan. **A census that finds none says so in one line; the loop opens on that line (the F1 pre-loop gate is already cleared).**

## §4 THE ACCEPTANCE TEST (RD holds the plan to it — his §4 additions folded in)

1. **Trade 20 ADMITS on `contemporaneous_record`** citing `why_now` + `notes`: tier detected (no AMN link, no AMN intent; the N1 legs decide — WHICH leg admits is R0.F-2, RD's, and the witness READS `admitted_leg` back rather than pinning it); zero corrections on the cited fields in BOTH audit tables; outcome (exit fill 2026-08-11) post-dates the record; the attestation row carries the text snapshot byte-for-byte (the two-space `notes`); `trades.entry_intent = 'unintended_execution'`; BOTH rows land or NEITHER (a forced failure between the two statements leaves both absent).
2. **Per-clause discriminators, each a one-mutation synthetic in the real row shape, boundary twins with the inequality direction pinned:** a trade whose filled order HAS a latch link/intent row → tier `structural` REQUIRED, tier-2 REFUSED (the null-implementation of "provably post-dates" admits it); **P1 as the N1 legs (RD's discriminators, `rd-rulings-census-n1-n5.md` N1, carried as he wrote them):** a telemetry row in the leg-1 window with `actionable_ever_viewed` flipped to 1 → REFUSE naming the offer; no telemetry with placement 08-01 → ADMIT on leg 2; no telemetry with placement 08-03 → REFUSE (boundary twin 08-02 → ADMIT); no telemetry and no envelope with `entry_date` 08-03 → REFUSE; the OII shape (actionable 1, a validity row) → routes to the structural tier, not tier 2; the leg-1 WINDOW is R0.F-2's to fix (RD); **the AMN structural fixture (F3):** death terminal 08-06 + fill 08-07 → ADMIT, terminal 08-07 → REFUSE naming the rung and both dates, live latch → REFUSE as a mandate fill, terminal `fill`/`superseded` → REFUSE; a planted `reconciliation_corrections` row on `notes` (raw insert, the 18-B.1 technique) → REFUSE naming the field, and the SAME with a planted `provenance_corrections` row (F2 S2 — an implementation checking one table FAILS); `--cite pre_trade_locked_at` → REFUSE (not citable); `--cite emotional_state_pre_trade` ALONE → REFUSE (F2 S4: no descriptive field); an OPEN trade → ADMIT with `outcome_known_at` NULL, and a later exit does NOT back-fill it (the row is byte-identical after the exit); a trade with `entry_intent` already set → REFUSE (no relabel path); a cited field whose live text differs from the snapshot at replay → the drift reader names it.
3. **The value lands in NO cohort read, COMPUTED (restated at census N3 (a) with N2 (a)):** the four `cohort_intent.py` readers + the H2–H5 readers + `swing hypothesis list` + the H1 count BEFORE and AFTER 20's assignment: every N unchanged AND 20 in NO candidate set (asserted, not inferred from N). The NAMING discriminator runs on SYNTHETIC trades: an `unintended_execution` trade carrying an H1 label and one carrying an H2 label are each EXCLUDED by the `CONTRACT_EXCLUSION_CLAUSE` predicate and each NAMED in that cohort's exclusion output with reason `unintended_execution`; a NULL-intent trade with an H2 label still COUNTS (N2's "NULL counts as today" twin). A refactor to an `else`-branch coercing unknown → `standard` FAILS this test.
4. **Migration:** the one-edit bytewise proof; 58 columns in order; every row tuple identical; child row counts unchanged; `foreign_key_check` empty; `ux_trades_one_open_per_ticker` still UNIQUE-partial (a second open trade on a ticker REFUSED post-migration — the D30 discriminator for the partial index); `trg_trades_attempt_id_immutable` still fires (an UPDATE of `attempt_id` ABORTS post-migration); run twice = no-op; the D51 diff = two changed pairs + the new table's additions, zero deletions, regenerated with `--write` under the D51b normalization; the model rejects a fourth value; `test_expected_schema_version_is_head` → 40; **the F1 hash test: header sha256 == the hash derived from the committed clause (4)**.
5. **The single-writer seam:** each of the EIGHT refusing surfaces (F5 as corrected by the census) refuses `unintended_execution` by execution with the typed message; the AST caller walk shows `assign-intent`'s service is the only path that writes the value.
6. **The precondition walk (F2 S3):** the test enumerates every `UPDATE trades` site by READ and asserts none writes the four allowlisted fields outside the audited corrector — or the plan ledger names the site as a finding and the test pins the finding.
7. **Display:** every surface renders the token with its OWN class/label, never the `standard` or `unclassified` styling (TestClient on the trend + card; the CLI label); the trade-process card's per-tab N is quoted WITH the tier page's N until D61 lands (RD's interim over-count note carries over).
8. **22-A2's acceptance cases (A2-09 pin) stay green byte-unchanged on 22-B's tree, with EXACTLY THREE named exemptions — R0.F-3, RULED (CHARC, 2026-09-23): branch (a), re-scope ONCE to their own migration.** Found by the plan cell reading all 106 `CASES_22A2` bodies: A2-18 (migrates to HEAD and asserts exactly 0039's diff), A2-19 (asserts the literal `# schema_version 39` in the committed fixture + fixture == HEAD), A2-22 (the gate table holds exactly one post-base row) cannot survive ANY v40 migration — they are HEAD claims wearing 0039's name, the stale-`_is_NN` class the version-mirror rule already names (CHARC-ruled 2026-09-07; rule text at `tests/data/test_migration_0035_fills_trades_price_divergence.py:62`). **The re-scope:** A2-18 → image at 38, `run_migrations(c, target_version=39)`, diff == 0039's four changed pairs (stable forever); A2-22 → the gate roster asserted over a `target_version=39` run from a 38 image (ONE gate fires — and, corrected at G1 by the orchestrator's measurement, CHARC-accepted: `run_migrations` evaluates the gate ONCE on the STARTING version, so a 38→40 HEAD run ALSO fires only the 22a2 gate, never two; the re-scope to its own migration stands on the version-mirror rule alone, not on a two-gate count; R0.I's "four changed pairs" executes as THREE object pairs plus the header line); A2-19 → its literal is RETIRED (the "committed fixture matches HEAD" clause is ALREADY the `_head` tests' claim — `test_schema_manifest_head.py` + `test_expected_schema_version_is_head` — and is NOT duplicated) and the test becomes: migrate to 39, the migrated connection's `schema_version` == 39 and the three 0039 objects hash to the values A2-18 pins. Case ids RETAINED in `CASES_22A2` (the closure test walks tokens, not bodies). The composition pin names the three as "re-scoped 22-B, reason: HEAD-claim in a version-named case", all other 103 byte-unchanged. Branch (b) (bump the literals to 40) REJECTED: it re-opens at every later migration and is exactly the drift the rule was written against. **Forward rule for THIS arc's own cases:** every 22-B migration case pins `target_version=40`, never HEAD, so 22-C does not meet this fork.
9. **Death-then-fill precondition (F3):** the plan ledger states, with file:line, whether the ledger preserves a death terminal across a later fill; if not, the structural tier's REFUSAL is itself a pinned case.

## §5 THE WITNESS (post-merge; OPERATOR-EXECUTED, orchestrator-scripted; one step per result; BEFORE 09-30 EOD HST or AFTER 10-20 — the STOP LINE above)

1. Stop `swing web` (port 8080 free, no `swing.cli web` python survives); plain sqlite3 `BEGIN EXCLUSIVE; ROLLBACK` succeeds — stated WITH its result, never a process list. The merge-time composition read asks the RUNTIME question (code × live v39 at the second of merge: `connect()` refuses every run between merge and `db-migrate`, so the two land in ONE sitting; the last pre-merge pipeline run is the baseline).
2. `swing db-migrate`: the `22b` gate image path ECHOED (ONE backup, in `backups_dir`, none in the swing-data root — the D32 production shape, as at 22-A2); `python scripts/schema_manifest.py --db <live>` clean at v40; `PRAGMA foreign_key_check` = [] on live.
3. `swing trade assign-intent 20 --value unintended_execution --cite why_now,notes --reason "<r>" --dry-run` → ADMIT, tier `contemporaneous_record`, the predicates printed with their VALUES (the admitting N1 leg and its evidence — telemetry row id and its 0, or placement 2026-08-01 vs the 2026-08-03 constant; corrections 0/0 over both tables; outcome = the earliest non-entry fill, 2026-08-11 STOP), nothing written. The witness QUOTES the leg the reader prints; it does not pin one.
4. The same without `--dry-run`; `swing trade analyze 20` prints trade 20's OWN label `Unintended execution` (**G3a, CHARC 2026-09-24, replacing "the journal / trade view renders…" — that surface DOES NOT EXIST: neither `journal_trade_detail.html.j2` nor `trades/detail.html.j2` renders `entry_intent` for ANY value, measured by the cell, the orchestrator and CHARC; the premise was this brief's own**); `swing hypothesis list` UNCHANGED line-for-line against step 3's pre-image (20 was in no cohort before and is in none after — N3 (a)); the attestation row read back on `mode=ro` (tier, `admitted_leg` + its evidence, cited fields, snapshot, `outcome_known_at`).
5. RD re-reads the H-cohort counts on the live DB and states them beside the reader's, method named; the witness QUOTES the readers' numbers (no expected count pinned).

**G3a RULINGS (CHARC, 2026-09-24, on 22-b-exec @ `8f989bfa`):** **(i)** branch C — witness step 4 re-pointed to `swing trade analyze 20` (b22_142 pins it; it survives review), the b22_143 id RETIRED (plan text by replacement, orchestrator-made); branch A DECLINED as a DUPLICATE of b22_122 (the review form's read-only label is already pinned there); branch B OUT under §2 (any UI) — **BANKED as a post-merge display-gap follow-on, RD/operator's demand call:** no stored-trade page renders `entry_intent` for ANY value (pre-existing; 22-B exposed it), so the only durable operator surfaces for a trade's intent are the CLI and the metrics cards. **(ii)** the trend legend (`process_grade_trend.html.j2:58`, `intent: standard / by-design / unclassified / unintended`, now ending x≈385 against the first swatch at x=360, cell-measured by font metrics) moves to ITS OWN LINE — never shortened: a shortened label is fragile against the next value and the collision is a property of the row, not the text; within Task 9's display scope, no new UI; **the operator's browser look at the trend page is added to witness step 4** (presentation is visible only at the witness rung). **(iii)** the trade-process card's facet labels come from ONE source, `entry_intent_label`, for EVERY value (`trade_process_card.py:58` hard-codes "Standard" beside `:60`'s function call — the #11 mirror family in a label); the pre-existing "Standard" becomes the label home's "Standard entry" on the card, a text change b22_141 pins and the witness sees; a second label source is the drift this arc's constants table forbids ("every other site imports them"). **G3c follow-up (CHARC, 2026-09-24):** (iii) reaches every VALUE facet, as the cell read it — `All` and `Unclassified` are facet SENTINELS, not stored values, and `entry_intent_label(None)` returns `None` by contract, so the label home has no label for them; SETTLED. But the NULL sentinel's text has TWO rendered homes (`cli.py:1386` `or 'Unclassified'`; `trade_process_card.py:66` — the only two under `swing/`, grep stated, every other hit a comment): FOLD IN as a rider on Task 9's own files — `NULL_ENTRY_INTENT_LABEL = "Unclassified"` in `swing/trades/intent.py` (the label home; `entry_intent_label`'s `None` contract UNCHANGED), imported at both sites; `All` stays local to the card (a facet, not an intent label); a static pin in b22_141's pattern: no `'Unclassified'` string literal under `swing/` outside `intent.py` (comments excluded). Banking a two-line mirror is how a known mirror becomes a third home. **G4b addendum (CHARC, 2026-09-24): the ONE docstring copy of the label — `cli.py:1840`, `backfill-intent`'s `--help` text "(renders 'Unclassified')", operator-visible and skipped by b22_217 by construction — RULED (b): REWORD so the help does not quote the label's text ("leaves the row unclassified"); a copy removed beats a copy fenced ((c) REJECTED: a docstring walk with a named exclusion is a roster for one line). Lowercase `unclassified` in form options, the legend and CSS tokens is a different string, outside the ruling.** **G4 — copied_date_shapes (CHARC, 2026-09-24): RULED (b).** The attestation's date-shape CHECKs bind values the service COPIES from `trades.entry_date` and `fills.fill_datetime`, both bare `TEXT NOT NULL` with NO shape CHECK at v40 (read on live, `mode=ro`); a malformed source value today reaches the operator as an untyped `ValueError` from `__post_init__`, or WORSE, as a MISLABELED refusal produced by a lexical comparison on a non-canonical date (`outcome_not_after_record`, `instrument_existed`) — the D38 class (a lexical predicate on an unconstrained TEXT timestamp fails toward permissive, and here also toward the wrong reason), and the "one authority: TEXT compared BYTEWISE" convention is sound ONLY over canonical shapes. So the service gains ONE typed refusal, `source_date_malformed`, naming the column and the value, checked at preflight step 1 BEFORE any string comparison and before `__post_init__` — the refusal-legibility rule applied at G1d, same shape; service-only, a handful of tests, inside the stop line. Discriminators: `entry_date` `2026-8-07` → `source_date_malformed` (NOT `outcome_not_after_record`); `fill_datetime` `2026-08-11 16:00:00` → `source_date_malformed` (NOT a raise); the existing raise-or-refusal property test TIGHTENED to assert the code. Incidence today ZERO (orchestrator-measured: 28/28 entry dates and 59/59 fill datetimes canonical). **(c) OUT** — a CHECK on the source columns is a second `trades` edit (F6) and a `fills` rebuild; it belongs to the D37/D38 repo-wide sweep (CHARC's register), which this instance now motivates; (a) REJECTED — a mislabeled refusal is a legibility defect on its own regardless of incidence.

## §6 RETURN

The plan cell returns the census packet FIRST (the premise re-verification + any NEW forks with both branches and a named ruler; or the one-line "none found"); the orchestrator posts it to CHARC and RD; the loop opens on the packet's ruling (F1's gate is already cleared). The ORCHESTRATOR posts the return report to `charc` and `rd` after its QA. Executing dispatches on the plan's acceptance with its base ≥ `57187164` (the serial rule's first half is discharged). CHARC's architecture pass rides the plan (0040's rebuild shape, the new table, the new module, the gate row); RD's gate rides the encodings of F1–F3 and the §4 cases. CHARC closes 22-B on the §5 witness; the STOP LINE is read at Reviewer B's verdict on 2026-09-29 EOD HST — clean → witness before 09-30; not clean → park to 10-21, and the orchestrator posts the park as its own status.
