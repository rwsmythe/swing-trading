# Phase 22 — Arc 22-B: Demand A, `unintended_execution` (the third `entry_intent` value + the evidence-bearing assignment surface) — commissioning brief

**STATUS: DRAFT, NOT DISPATCHED (CHARC, 2026-09-23, on the operator's word "draft the 22-B brief now so it's ready when 22-A2 lands").** The go/no-go is the OPERATOR's, made when 22-A2 lands (operator 2026-09-22: "I am not convinced we cannot execute 22-B and witness before I leave. Leave that as a call to make once 22-A2 lands."). Nothing in this file dispatches. When the go is given, CHARC re-verifies §1 against the then-live DB (post-22-A2 schema), amends by replacement, and posts the dispatch.

**From:** CHARC. **To:** the orchestrator. **Stage:** WRITING-PLANS under the PLAN-STAGE PROTOCOL (harness §5.1): premise + fork census FIRST (no Codex), ONE packet, directors rule in ONE pass each, no round while a ruling is outstanding, plan ≤ ~1,000 lines, ≤ 3 `fast` rounds then a self-sweep. **Plan cell:** `implementer-opus-high` — the design is small and every fork is pre-specified below; xhigh only if the census re-opens a fork, with the reason stated. **Executing cell:** `implementer-opus-high` (the recipe's schema-migration exception: a `trades` rebuild on the journal of record). **Reviewer A** at `strong` to convergence; **Reviewer B REQUIRED** at the orchestrator's gate on the finished tree (charter §2.9; `swing/data` + `swing/trades` + `swing/cli`). **§3 tripwires crossed:** new schema (migration `0040`, a `trades` REBUILD + one new append-only table), a `swing/data` + `swing/trades` carve-out, possibly one new module — CHARC's architecture pass rides the census packet and the plan. **The RD gate** rides the evidence semantics (§3 F1–F3) and the witness (H1/H-cohort re-read).

**THE SERIAL RULE (RD-raised, CHARC-ruled 2026-09-22, a RULE not a timing accident):** 22-B's PLAN cell may run once 22-A2's plan is on `main` (it is, `23313918`); **22-B's EXECUTING cell does NOT dispatch until 22-A2 is MERGED and its witness has run**, and 22-B's base is at or above that merge. Both arcs touch the entry record and the correction machinery; the composition class (21-B × 21-G; D53) has no rung, and this time both halves are the record H1 is counted from. **Two rebuild migrations in sequence** (22-A2's `provenance_corrections`, 22-B's `trades`): each carries its own gate row and its own D51 diff; neither is folded into the other. **Witness calendar:** the live migration + trade 20's assignment need the operator's hand; no witness rung exists 10-03..10-20 (declared trading pause). So the witness lands BEFORE 10-03 or AFTER 10-20 — the operator's call at the go/no-go; nobody merges over it.

## §0 READ FIRST (pointers — the cell LIFTS, it does not re-derive)

1. **The authority:** `docs/demand-a-entry-intent-charc-section3-pass.md` (CHARC's §3 pass, GO on the tripwire; the naming argument — the value names the CLASS, an execution nobody intended, not the mechanism; the mirror manifest; the finding that the structural-citation rule is UNREACHABLE for trade 20 because the latch instrument post-dates the event). **RD's ruling, `comms/charc/read/20260812T164026Z-rd-demand-c-your-contemporaneous-comparison.md` §DEMAND A** and its record in `docs/research-director-context-archive.md` ~line 342 (**THE ADMISSIBILITY TEST**: evidence is admissible iff (a) RECORDED BEFORE THE OUTCOME WAS KNOWN and (b) IMMUTABLE SINCE; two-tier admission ENDORSED with the tier and the reason recorded on the row; tier 2 admissible ONLY where the instrument PROVABLY post-dates the event; **(b) is verified against the audit trail — no `reconciliation_corrections` row has touched the cited field — never inferred from writer-absence** (the D36 correction, CHARC's, adopted); **Demand B's annotation rows are PERMANENTLY INADMISSIBLE as evidence** — the guard that must appear in every Demand brief). `docs/phase22-scope-charc.md` §TIER 1 (22-B: name operator-concurred; §3 pass committed; §VII.F ratification of SPECIFIC AMENDED TEXT in the 0034 shape is a DISTINCT GATE inside the arc; applies to trade 20 at a witnessed gate).
2. **The trade-20 doctrine:** `docs/research-director-context-archive.md` ~line 349 (`entry_intent` NULL is DELIBERATE, operator-ruled 2026-08-10; stays NULL until Demand A completes; anyone setting it to `standard` contaminates the denominators) and ~line 347 (trade 20 CLOSED 2026-08-11 @ 33.73, ≈ −0.996R; a closed trade with a realized result in NO cohort — "the vocabulary gap has stopped costing us a LABEL and started costing us a RESULT").
3. **The code the plan builds ON:** migration `0027` (`entry_intent TEXT CHECK (entry_intent IS NULL OR entry_intent IN ('standard','hypothesis_test_by_design'))` — a COLUMN-LEVEL CHECK inside the `trades` DDL, so the widening is a REBUILD by construction, the 22-A2 F12 finding one table over); `swing/data/models.py:243` `ENTRY_INTENTS` + the `__post_init__` mirror at `:356`; `swing/metrics/cohort_intent.py` (H1's predicate is `entry_intent = 'standard'` by its OWN criterion text, pinned as `H1_COHORT_CLAUSE` against the live registry row; NULL is a distinct third facet, never coerced — **so the new value lands in NO cohort automatically; no cohort code changes**); `swing/trades/intent.py` (display tuple; the `by_design_keywords` inference at `:54` is NOT extended — the new value is never INFERRED, only asserted with evidence); `swing/web/view_models/metrics/process_grade_trend.py:78` (`_INTENT_CSS_CLASS`, needs a class for the new token or it inherits the unclassified style); `trade_process_card.py:52` (filter options); `swing/cli.py:602`, `:1558` (`click.Choice` lists), `:1699-1706` (review-time `update_entry_intent`), `swing/web/routes/trades.py:711/:838` (the entry form's early validation against `ENTRY_INTENTS`). `swing/data/repos/trades.py:update_entry_intent` (the existing WRITER of the column — the plan reads it).
4. **The rebuild precedents and the gotchas that bound them:** `0014` (the last `trades` rebuild: `trades_new` + INSERT-SELECT + DROP + RENAME; the UNIQUE partial index dropped first and re-created after); 22-A2's `0039` as ruled (F12 conditions 1–6, ledger R0.12 — the three-edit textual proof, sequence carry by statement, dependants re-created VERBATIM after RENAME, the D51 diff as the D30 instrument); the D30 class (`0033` nearly lost a paired CHECK); `PRAGMA foreign_key_check` after RENAME is MANDATORY under the runner's `foreign_keys=OFF`; gotcha #9 explicit BEGIN/COMMIT; the migration backup gate `pre_version == 39 AND target >= 40`; **D51's manifest test now ENFORCES the 0038 header's item (B)** — a rebuild that forgets `ux_trades_attempt_id` or `trg_trades_attempt_id_immutable` goes RED in this arc; the version-mirror naming rule (HEAD tests are `_head`; no rename sweep in the version commit).
5. **The Phase-22 doctrine that binds the evidence surface:** cohort bookkeeping NEVER blocks a money-bearing operation (0036:26-38); the ENTRY-vs-LABEL test (harness §5.1); AUTHORIZE-THEN-ABORT (the trigger's predicate set ⊆ the service's admission predicates, closure-checked); a stored grade is an attestation, a verdict is computed at read (S12.1 #5); gotcha #30 (a run-level stamp is not per-row provenance — `pre_trade_locked_at` equals `entry_date + 'T16:00:00'` on every trade and is NOT corroboration).

## §1 LIVE STATE, MEASURED 2026-09-23 (read-only, `mode=ro`, schema v38 — RE-MEASURE at the go, post-22-A2 the table set and the manifest differ)

| fact | value | method |
|---|---|---|
| `trades` columns / CHECKs / DDL chars | **58 / 14 / 3,419** (the §3 pass measured 57/13 on 2026-08-12; `0038` added `attempt_id` and its CHECK since — the count moved, the doc did not) | `PRAGMA table_info`; `\bCHECK\b` count over `sqlite_master.sql` |
| `trades` dependants | **5**: `idx_trades_candidate_id`, `idx_trades_pattern_evaluation_id`, `ux_trades_attempt_id`, `ux_trades_one_open_per_ticker` (UNIQUE partial), `trg_trades_attempt_id_immutable` | `sqlite_master WHERE tbl_name='trades'` |
| tables with an FK → `trades` | **5**: `fills`, `daily_management_records`, `trade_events`, `reconciliation_discrepancies`, `provenance_corrections` (the §3 pass counted 4; 0036 added the fifth) | `sql LIKE '%REFERENCES trades%'` |
| `entry_intent` value set | `NULL` 1 (trade 20) · `hypothesis_test_by_design` 20 · `standard` 7 | `GROUP BY` |
| trade 20 | AMN, entry 2026-08-07, `entry_intent` NULL, keys NULL, `trade_origin manual_off_pipeline`, `attempt_id` NULL (pre-0038); `why_now` = "Accidental entry while working on latch/unlatch mechanism"; `notes` = "Stale A+ latch order fired after A+ condition disappeared. This happened as we were working on the latch removal code."; `thesis` = "Old A+ setup, high likelihood of loss" | row read |
| the instrument PROVABLY post-dates the event | earliest `latch_order_intents.recorded_ts` = **2026-08-08T20:01:08** vs entry 2026-08-07; AMN intents = **0** | `MIN(recorded_ts)`; `WHERE ticker='AMN'` |
| immutability against the audit trail | `reconciliation_corrections` rows with `affected_table='trades' AND affected_row_id=20`: **0**; on ANY `trades` text field ever: **0** (the only `trades` fields ever corrected are `current_stop` ×4, `entry_date` ×1) | `GROUP BY field_name` |
| entry-captured text columns on `trades` | `notes`, `why_now`, `thesis`, `emotional_state_pre_trade` (+ `pre_trade_locked_at`, a synthetic restatement — NEVER cited) | `PRAGMA table_info` |
| Python mirrors of the enum (lower bound, grep `hypothesis_test_by_design` over `swing/`) | 12 lines / 7 files: `cli.py` ×3, `models.py`, `cohort_intent.py` (prose), `trades/intent.py` ×2, `process_grade_trend.py` ×2, `trade_process_card.py`; templates: `process_grade_trend.html.j2:58` (legend), `partials/review_form.html.j2:136` (prose) | token grep — a LOWER BOUND; the plan's #11 task reads each member separately |

## §2 SCOPE SHAPE — CHARC's rulings (mine; stated so the census does not re-open them)

**IN:**
- **Migration `0040`: the `trades` REBUILD widening ONE enum token** — under the 22-A2 F12 conditions transposed: (1) the new DDL is the STORED v39 `trades` DDL with EXACTLY ONE textual edit (the IN-list gains `'unintended_execution'`), proven bytewise by a test that applies that one edit to the v39 text; (2) ONE transaction, explicit column list on the INSERT…SELECT (58 columns + none added), row counts equal, every row's tuple identical, `PRAGMA foreign_key_check` empty, the five child tables' row counts unchanged; (3) order: CREATE new → copy → DROP old (takes the five dependants) → RENAME → re-create the FOUR indexes and the ONE trigger VERBATIM from their source migrations (`0014`/`0027`-era for the partial UNIQUE; `0038` for the attempt-id pair); (4) the D51 diff reads EXACTLY TWO changed pairs (the header, the table) and ZERO deletions — the five dependants hash identical; (5) the reversibility header names the edit and the gate row `BackupGateSpec(39, "22b", …)`; (6) `EXPECTED_SCHEMA_VERSION` 39 → 40. `trades` has NO AUTOINCREMENT (`id INTEGER PRIMARY KEY`, the rowid-reuse gotcha) — no sequence carry; the plan says so rather than copying 0039's step.
- **The `entry_intent_attestations` table (NEW, append-only, its SCHEMA IS THE EVIDENCE RULE — the 0036 pattern):** one row per assignment of `unintended_execution`; columns: `trade_id` (FK, UNIQUE — one head per trade), `assigned_value` (CHECK = `'unintended_execution'` only — this table records ONLY the evidence-bearing value; the other two are entry-time facts, not assignments), `admission_tier` (CHECK IN `('structural','contemporaneous_record')`), `cited_latch_intent_id` (FK `latch_order_intents`, NULL iff tier 2), `instrument_earliest_recorded_ts` + `trade_entry_ts` (tier 2 REQUIRES `trade_entry_ts < instrument_earliest_recorded_ts` — "provably post-dates", bound in SQL to its source), `cited_fields_json` (a CLOSED list drawn from the entry-captured allowlist `notes|why_now|thesis|emotional_state_pre_trade`; NEVER `pre_trade_locked_at`; NEVER an annotation row), `cited_text_snapshot_json` (the cited fields' text AT ASSIGNMENT, so a later edit is visible against the snapshot), `audit_trail_checked_at` + `corrections_touching_cited_fields` (must be 0 — the D36 check the code performs, AND its SQL twin: `NOT EXISTS (SELECT 1 FROM reconciliation_corrections WHERE affected_table='trades' AND affected_row_id=NEW.trade_id AND field_name IN (<cited fields>))`), `outcome_known_at` (the trade's first exit fill datetime, or NULL if open) with the SQL bind `trade_entry_ts < outcome_known_at OR outcome_known_at IS NULL` (test (a)), `reason`, `applied_at`, `applied_by`. Append-only TRIPLE (`no_update` + `no_delete` + conflict-scoped `no_replace`, the 22-I convention) + every `WHEN` clause `NOT COALESCE(...,0)`.
- **The assignment surface, CLI ONLY:** `swing trade assign-intent TRADE_ID --value unintended_execution --cite notes,why_now --reason "<r>" [--dry-run]` — the service (a new `swing/trades/entry_intent_assignment.py`, F4-shaped: preflight outside the transaction; the write is ONE `BEGIN IMMEDIATE` inserting the attestation row THEN updating `trades.entry_intent`, both or neither). The tier is DETECTED, never chosen: a `latch_order_intents` row for the trade's order → `structural` (cites it, REQUIRED); none, and the instrument provably post-dates the entry → `contemporaneous_record`; none, and the instrument PRE-dates the entry → REFUSE ("the instrument existed and did not fire; this is not an unintended execution the record can prove"). Refusals are typed and name the recovery. The seam obligation: `update_entry_intent` (the existing review-time writer) and both `click.Choice` lists and the web entry form's validation REFUSE `unintended_execution` with a typed message naming `assign-intent` — the value has exactly ONE writer, pinned by a test.
- **The #11 mirror sweep** (the §3 pass's manifest, now 7 files + 2 templates), classified per site by READ as widened / tight-by-design / value-agnostic; the SQL-vs-Python drift test over `trades.entry_intent`'s CHECK (does one exist today? the plan measures; if not, it is added — the mandatory mirror member); a CSS class for the token; the display maps.
- **The §VII.F gate** (§3 F1) and **trade 20's assignment at a witnessed gate**.
- **The 22-A2 acceptance cases stay GREEN byte-unchanged** (A2-09-style pin on 22-A2's own case set) and the four cohort readers' outputs are asserted UNCHANGED before/after 20's assignment except that 20 is now NAMED as `unintended_execution` in the excluded-with-reason line (F10's "never silently uncounted", one more reason string).

**OUT:** 22-C (abandonment intent) and 22-E (Demand B annotations) — RD: one schema family, one arc or strictly sequential, NEVER parallel with 22-B; the era model / 22-A2i; trades 19/24/28 (named-pending); any UI (the entry form does not offer the value; the assignment is CLI); the `initial_stop` correction surface; inference of the value from text (`by_design_keywords` untouched); a relabel path for a trade that already carries `standard`/`by_design` (a NON-EMPTY relabel is a NEW evidence class — the 22-A2 F1.1 ruling applies verbatim; the surface refuses when `entry_intent IS NOT NULL`).

## §3 FORK CENSUS — pre-specified (both branches; ruler named)

- **F1 — WHAT TEXT DOES §VII.F RATIFY, and how is it pinned? Ruler: RD names the text; CHARC rules the pin.** The scope doc says ratification of SPECIFIC AMENDED TEXT in the 0034 shape is a distinct gate. Measured: H1's `decision_criteria` needs NO amendment (it says "STANDARD-intent trades only"; an `unintended_execution` trade is not STANDARD-intent and is excluded by the existing clause — `cohort_intent.py` proves it). So the amended text is DOCTRINE, not a registry row: the training-epoch intent contract (RD's charter §7 / the 2026-06-10 declaration) gains a third value with its definition and its admissibility rule. Branch (a): RD authors the amended clause; the operator ratifies the EXACT text; `0040`'s header carries its sha256; a test derives the hash independently from the committed doc (the 0034 shape, in a doc rather than a row); the migration REFUSES to run if the header hash and the doc disagree? — NO: a migration cannot read the docs tree at the operator's machine reliably; the hash is pinned by TEST, not by the migration's runtime. Branch (b): no doctrine text is amended; the value's definition lives only in `models.py`'s docstring and the §3 pass. **CHARC rules the pin shape if (a): test-pinned, never migration-runtime-checked.** RD rules (a) vs (b) and, under (a), writes the clause. The operator's ratification of the exact text is a gate inside the arc; the plan does not open its loop until the text is ratified (a fork discovered mid-loop stops it).
- **F2 — THE TIER-2 ADMISSIBILITY PREDICATES, exactly. Ruler: RD.** The brief encodes RD's test as three SQL-bound predicates (instrument post-dates entry; no correction touched the cited fields; the record pre-dates the outcome). RD confirms the set, and rules the two edges: (i) a trade still OPEN (no outcome) — admissible now, or only at close? (the record pre-dates an outcome that does not yet exist; CHARC's lean: admissible, `outcome_known_at` NULL recorded); (ii) `thesis` / `emotional_state_pre_trade` as citable fields alongside `notes`/`why_now` — in the allowlist or not.
- **F3 — STRUCTURAL TIER's citation when the instrument existed and DID fire: which `latch_order_intents` row kinds count? Ruler: RD.** For a future unintended execution WITH latch rows (the AMN mechanism repeating post-instrument): the citation is the latch's `place`/`validity` row on the order that filled; and `latch_order_mandate_links` (22-A) gives the order↔mandate link. Branch (a): cite the LINK (`cited_latch_link_id`, 22-A's authority) when one exists, else the intent row; (b) the intent row only. **CHARC's shape stake: (a)** — the link is the durable order↔mandate binding 22-A built for exactly this.
- **F4 — MODULE + SERVICE SHAPE. Ruler: CHARC, RULED HERE:** new module `swing/trades/entry_intent_assignment.py` (a §3 new-module tripwire, approved in advance on the F4 conditions: preflight outside the transaction; ONE `BEGIN IMMEDIATE` for the two-row write; callers pinned by test). NOT inside `cohort_provenance_correction.py` (3,414 lines) and NOT a second writer of the cohort keys — this surface writes `entry_intent` and its attestation ONLY.
- **F5 — THE SINGLE-WRITER SEAM. Ruler: CHARC, RULED HERE:** `update_entry_intent`, both CLI `click.Choice` lists, the web entry form and the review form REFUSE `unintended_execution` (tight-by-design mirrors, each with a typed message naming `assign-intent`); the model validator ACCEPTS it (widened); the drift test covers the three-valued CHECK. Pinned by a test that greps the five surfaces' Choice/allowlist literals AND by execution (each surface refuses with the message).
- **F6 — REBUILD SCOPE. Ruler: CHARC, RULED HERE:** ONE edit to the `trades` DDL. NO other `trades` change rides this rebuild (no column added, no CHECK strengthened, no D58 fence) — a rebuild is the riskiest migration shape and it carries exactly the change it exists for. D58 stays its own arc.

Any further fork the census finds is ADDED to the packet with both branches, not decided in the plan.

## §4 THE ACCEPTANCE TEST (RD holds the plan to it)

1. **Trade 20 ADMITS on `contemporaneous_record`** citing `why_now` + `notes`: tier detected (no AMN intent; earliest instrument 2026-08-08T20:01:08 > entry 2026-08-07); zero corrections on the cited fields; outcome (exit fill 2026-08-11) post-dates the record; the attestation row carries the text snapshot; `trades.entry_intent = 'unintended_execution'`; BOTH rows land or NEITHER (a forced failure between the two statements leaves both absent).
2. **Per-clause discriminators, each a one-mutation synthetic in the real row shape:** a trade whose ticker HAS a latch intent row → tier `structural` REQUIRED, tier-2 REFUSED (the null-implementation of "provably post-dates" admits it); an instrument recorded ONE SECOND BEFORE the entry → REFUSE (boundary twin: one second AFTER → ADMIT); a planted `reconciliation_corrections` row on `notes` for that trade (raw insert, the 18-B.1 technique) → REFUSE naming the field; `--cite pre_trade_locked_at` → REFUSE (not citable); a cited field whose live text differs from the snapshot at replay → the drift reader names it.
3. **The value lands in NO cohort read, COMPUTED:** the four `cohort_intent.py` readers + the H1 count before/after 20's assignment: every N unchanged; 20 appears in each reader's named-exclusion output with reason `unintended_execution` (the §3 pass's "the one that actually distinguishes"). A refactor to an `else`-branch coercing unknown → `standard` FAILS this test.
4. **Migration:** the one-edit bytewise proof; 58 columns in order; every row tuple identical; child row counts unchanged; `foreign_key_check` empty; `ux_trades_one_open_per_ticker` still UNIQUE-partial (a second open trade on a ticker REFUSED post-migration — the D30 discriminator for the partial index); `trg_trades_attempt_id_immutable` still fires (an UPDATE of `attempt_id` ABORTS post-migration); run twice = no-op; the D51 diff = two pairs, zero deletions; the model rejects a fourth value; `test_expected_schema_version_is_head` → 40.
5. **The single-writer seam:** each of the five refusing surfaces refuses `unintended_execution` by execution with the typed message; the AST caller walk shows `assign-intent`'s service is the only path that writes the value.
6. **Display:** every surface renders the token with its OWN class/label, never the `standard` or `unclassified` styling (TestClient on the trend + card; the CLI label).
7. **§VII.F (if F1 = a):** the header hash == the hash derived from the committed clause text; the ratification is quoted in the ledger with the operator's words.

## §5 THE WITNESS (post-merge; OPERATOR-EXECUTED, orchestrator-scripted; one step per result; BEFORE 10-03 or AFTER 10-20)

1. Stop `swing web`; plain sqlite3 `BEGIN EXCLUSIVE; ROLLBACK` succeeds.
2. `swing db-migrate`: the `22b` gate image path ECHOED (one backup); `python scripts/schema_manifest.py --db <live>` clean at v40.
3. `swing trade assign-intent 20 --value unintended_execution --cite why_now,notes --reason "<r>" --dry-run` → ADMIT, tier `contemporaneous_record`, the three predicates printed with their values, nothing written.
4. The same without `--dry-run`; `journal`/`hypothesis list` show trade 20 NAMED as excluded with reason; the attestation row read back.
5. RD re-reads the H-cohort counts; the witness QUOTES the readers' numbers (no expected count pinned).

## §6 RETURN

The plan cell returns the census packet FIRST (the orchestrator posts it to CHARC and RD with per-item rulers); F1's ratified text is a gate before the loop opens; the ORCHESTRATOR posts the return report to `charc` and `rd` after its QA; executing waits for 22-A2's merge + witness (the serial rule). CHARC closes 22-B on the §5 witness; D32's production proof, if not already read at 22-A2's step 2, is read at step 2 here.
