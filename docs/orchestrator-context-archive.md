# Orchestrator Context — Archive

> **Purpose:** Historical record of older lessons captured + superseded recent-decisions previously inline in `docs/orchestrator-context.md`. Active orchestrator handoff is `docs/orchestrator-context.md`.
>
> **Bootstrap discipline:** Fresh orchestrator does NOT need to read this file. Grep on demand for specific historical context (commit hashes, prior lesson framings, settled-and-archived decisions).
>
> **Retention:** Lessons-captured cap at ~30 most recent entries in active; older lessons migrate here at end of each phase ship (one-phase cooldown). See `docs/orchestrator-context.md` §"Maintenance: retention discipline."
>
> **Archive split trigger:** if this file exceeds ~80k tokens, revisit hierarchical decomposition (e.g., split by lesson domain: HTMX / yfinance / Windows / SQLite / dispatch-discipline / brief-drafting). See `docs/orchestrator-context.md` §"Maintenance: retention discipline."

---

## Lessons captured (older entries; chronological oldest first)

> Most-recent ~30 entries retained in active `docs/orchestrator-context.md`. Older entries migrated here as they age past the cap. For lessons that durably prevent code failure (yfinance regressions; HTMX failure surfaces; Windows ACL gotchas; SQL/Python idiom collisions), check CLAUDE.md gotchas — most have been promoted there.

- **Adversarial review catches architectural improvements TDD alone misses.** Examples: Tranche B-ops Session 2 caught `open_risk_position_count` collapse; Session 3 caught the `TradeStopFormVM.force` dead field; Tranche C pipeline-linkage caught two additional mixed-anchor surfaces (`_step_export`, `candidates_by_ticker`) beyond the brief's enumeration. **Why this works:** code review surfaces what the writer didn't think of; the writer's TDD discipline only covers what they thought to test.
- **Pre-registration discipline forces honest "defer" outcomes.** Session 2c's anti-rationalization clause held through 4 review rounds. Decision unchanged across review; only the justification framing changed. **Why this works:** the discipline is structural, not aspirational.
- **Sample-size-driven failure is a study-design failure, not a discipline failure.** Session 2c had 11 signals against ≥30 needed; the study technically completed but methodologically failed. The right framing is "process succeeded, question inconclusive."
- **Filter-rule studies need activation-rate sanity check.** Lesson from Session 2c (variant filter was a no-op because no signal had earnings within X=10 trading days). Captured as forward-looking amendment in `research/studies/earnings-proximity-exclusion.md` §Amendments.
- **Same bug class often has multiple surfaces.** Bug 7 (mixed-anchor) showed up in today_decisions, chart-scope resolver, _step_export, and candidates_by_ticker. The brief named the first two; review found the other two. **Always enumerate read-paths that touch the affected data, not just the obvious surface.**
- **Operator-drives framing protects against AI methodology homogenization.** Detailed in QuantEcon companion. The recursive concern about AI-aided development is narrowed but not eliminated by this discipline.
- **Stale-server failure mode after code changes.** Documented in CLAUDE.md gotchas. Jinja templates auto-reload; Python dataclasses do not. Always restart `swing web` after class/field changes.
- **HTMX OOB-swap partial drift.** Documented in CLAUDE.md gotchas. Use `{% include %}` to share partials between full-page and OOB-swap render paths; never hand-duplicate markup.
- **Production-gating mismatch in instrumentation can lie silently.** When instrumenting production logic for diagnostic measurement, the instrumentation must mimic production's gating order, not the criteria's emission order. Tranche C candidate-sparsity diagnostic R1 Critical caught this: original D2 walked emitted criterion order, but production `bucket_for` applies `risk_feasibility` as hard pre-filter. Net effect: capital hypothesis appeared 3.5× weaker than reality before fix. **General lesson:** instrumentation can pass unit tests and still produce systematically biased aggregate numbers if it doesn't mimic production's control flow. Production-vs-instrumentation parity needs explicit verification when measurement matters for decisions.
- **Diagnostic findings can be informational without being prescriptive.** Operator may reasonably decline to act on a diagnostic finding even when the finding is statistically meaningful. Capital-sensitivity finding (Tranche C diagnostic, 2026-04-25) is the canonical example: structural blocking change is large in proportional terms (18.6% → 1.3%) but the deterministic operational change (5 → 10 SPX A+ signals/year) doesn't cross workflow-relevance threshold for the operator's actual usage. Diagnostic does its job by surfacing the data; operator decides whether the data warrants action. Don't push action when "informational" is a defensible response.
- **Some bugs are sample-size failures, not discipline failures.** Session 2c's defer outcome with 11 signals against ≥30 needed was a study-design failure (variant filter was a structural no-op on the chosen universe), not a process failure (pre-registration discipline held through 4 review rounds, decision unchanged). Distinguish these in triage: process-level success can coexist with question-level inconclusive.
- **Brief drafting drift on tracking state.** Don't claim files are uncommitted/untracked without verifying current `git status`. The post-2c housekeeping brief mistakenly claimed `docs/Bugs.txt` was uncommitted when it had been committed by Session 2b's mid-session catch-up commit. Implementer caught and corrected. Pattern: brief drafting that touches tracking state should explicitly call for `git status` verification at dispatch rather than asserting state from orchestrator memory.
- **Manifest-integrity generalization.** Manifests must reflect actual code state at run time, not the most-recent commit when the artifact was committed. Parity-check D3 originally committed a manifest pointing at D2's SHA when the run included an uncommitted wrapper class (`_CountingPriceFetcher` for cache-stat instrumentation); R1 caught it. Generalization of the candidate-sparsity diagnostic R1 Critical lesson: instrumentation/manifest can lie silently, and adversarial review is the surface that catches it. Pattern: any artifact that asserts code-state provenance must verify against the actual run-time state, not the latest committed state. If you've been editing during a run and have uncommitted changes, your manifest's `git_sha` claim is wrong.
- **n=1 parity with non-A+ sample bounds the parity claim.** Tier 1 result on a sample with zero A+ candidates verifies parity at the watch/skip classification level but not at A+. When designing parity studies, anticipate sample composition: if the production run typically produces zero or near-zero A+, the parity verification cannot exercise A+ classification logic. Operator-facing interpretation must preserve this bound. Generalizable beyond the parity check: any classification-equivalence study should anticipate sample composition vs the bucket categories the equivalence claim covers.
- **Brief drafting: canonical-template references win over prose count assertions.** When a brief says "mirror canonical template X" and ALSO independently asserts a count of cases, but the canonical template has a different count, the implementer must do judgment work that should have been done at draft time. Build_watchlist mixed-anchor fix Brief §0 (mirror canonical, 3 tests) vs §4.1 (add a second test, 2 implied) had this inconsistency; implementer correctly chose canonical-template fidelity. Pattern: when a brief points at a canonical template, prose counts must match the template OR explicitly call out the deviation. Prefer "mirror the template (which has N cases)" over independent count assertions.
- **Grouping-key fields need canonicalization-at-persistence-boundary, not just display safety.** When a brief introduces a free-text field that becomes a grouping/aggregation key downstream, the brief should specify canonicalization-at-persistence-boundary requirements (NFC normalization, control-char handling, whitespace normalization, empty-string handling) — not just display-level safety. Trade hypothesis_label work R1/R2 caught this: brief specified "free-text safety" (display-level), but the field is the grouping key for evidence aggregation. Adversarial review pushed canonicalization scope beyond brief minimum because grouping-key instability would corrupt outcome-by-hypothesis statistics. Pattern for future briefs introducing free-text fields: ask "is this field a grouping key downstream?" If yes, specify canonicalization at the persistence boundary (e.g., inside the entry service, not at display time).
- **WAL-mode SQLite requires `Connection.backup()` API, not `shutil` byte copy.** Production DB runs in WAL mode (`PRAGMA journal_mode=WAL`), which means committed data may live in the `.db-wal` sidecar until checkpoint. A naive `shutil.copyfileobj` byte copy can miss the WAL contents or capture a torn read mid-checkpoint, producing a corrupt backup. SQLite's `sqlite3.Connection.backup()` API is the WAL-safe equivalent (same atomic-replace pattern at the destination, but the inner copy uses SQLite's online backup contract). Weekly DB backup work R1→R2→R3 progression caught this: brief prescribed `shutil`, implementer correctly chose `Connection.backup()` per the existing pattern in `swing/cli.py:42-55`. Pattern for future briefs touching DB persistence on a WAL-mode database: specify `Connection.backup()` explicitly, not generic file-copy.
- **Parallel implementers in the same working tree cross-contaminate commits.** When multiple Claude Code implementers run concurrently against the same git working tree, files staged by one implementer can be picked up by another's `git add`, producing mixed-scope commits. Weekly DB backup work commits `375344f` and `43b4d35` accidentally bundled trade-analyze implementer's `swing/journal/analyze.py` + `tests/journal/test_analyze.py` and a `trade_analyze_cmd` block. Not implementer-caused (they did the right thing within their scope); orchestrator-side architectural issue. **Pattern for future parallel dispatches: use the `using-git-worktrees` skill / git worktrees so each implementer has an isolated working tree.** Worktrees are cheap (shared `.git/`); cross-contamination is invisible until a return report surfaces it.
- **Transient parallel-execution test errors resolve when all commits land.** During 2026-04-25 evening's parallel batch, `tests/data/test_backup.py` showed import errors when 3c ran (mid-batch) and was excluded by hyp1 (also mid-batch). After all four agents committed all their work, the actual test suite was 948 passing, 0 failing — the import error was a transient state during partial completion. **Don't act on intermediate-state errors during parallel execution; wait for all dispatches to land then verify final state.** Each agent reports their own snapshot accurately; the snapshots' truth depends on when they were taken relative to other agents' commits.
- **Compounding-confound test fixtures can pass despite a vacuous primary discriminator.** Session 2 R4 (2026-04-26) caught a precedence-surface test that arithmetic-on-paper distinguished pre-fix from post-fix on the precedence key, but two independent confounds masked the bug: (a) `monkeypatch` targeted `dashboard._flag_tags` instead of `watchlist._flag_tags` (the bound reference imported transitively into watchlist module), and (b) ticker names were alphabetically aligned with precedence ordering, so the alphabetical tiebreaker re-ordered the result back into pre-fix-equivalent shape. Generalization beyond the existing `feedback_regression_test_arithmetic.md` discipline: when a test asserts on a primary key, **empirically verify the discriminator by temporarily disabling the keyed-on element and re-running the test** — if it still passes, the test is vacuous despite arithmetic that "should" distinguish. Verified-empirically supplements asserted-arithmetic. Memory file `feedback_regression_test_arithmetic.md` extended with this failure mode in the same housekeeping commit.
- **Production-gating-aware-vs-literal-spec mismatch is a recurring class of finding (3 occurrences as of 2026-04-25).** The pattern: brief specifies a logical rule literally (e.g., "candidates with bucket=watch and only one specific failed criterion"); production's actual gating order makes the literal rule impossible to satisfy on real data because production filters earlier. Three documented cases: (1) candidate-sparsity diagnostic R1 Critical (instrumentation walked emitted criterion order; production gates risk_feasibility first); (2) parity-check R1 (manifest provenance based on commit-time vs run-time state); (3) hypothesis-backend hyp1 R1 (capital-blocked matcher needed `bucket in ('watch', 'skip')` because production's risk_feasibility hard-pre-filter routes capital-blocked candidates to skip not watch). **Generalization:** when a brief specifies a candidate/data filter rule, ALWAYS verify the rule against production's actual gating order (read `swing/evaluation/scoring.bucket_for` and the upstream pipeline). Brief intent (what concept the rule is meant to capture) should be documented separately from the literal rule (which may need to be wider in implementation). Adversarial review reliably catches these; orchestrator should pre-empt by spec'ing intent + verification step in the brief.
- **Sort-neutrality is structurally guaranteed when the new tag never enters the existing sort tuple.** Lesson from chart-pattern flag-v1 brainstorm R1 M2 (2026-04-26). First-cut design tried to modify `_sort_watchlist` to exclude the new flag-pattern tag from `len(tags)`; the cleaner fix was a sibling `pattern_tags` VM field — moves the boundary one layer up and removes the temptation to touch sort code at all. **Generalization for future "decorative tag" features:** keep the sort-input tuple frozen at the existing scoring vocabulary; add new tags as parallel render-only data. If you find yourself touching sort logic to introduce a "neutral" tag, you've already lost — re-architect.
- **ToCToU on form-driven workflows is easy to overlook when "centralizing" looks elegant.** Lesson from chart-pattern flag-v1 brainstorm R3 M1/M2 (2026-04-26). The instinct to centralize cache resolution into `record_entry` was the right "DRY" instinct but ignored the temporal gap between form render and form submit (operator could see one classification, submit, get a different one persisted). Fix: resolve at entry-surface, persist a snapshot + audit anchor; accept hidden-field tampering as residual V1 risk for personal-use scope. **Watch-item template for future form-handling briefs:** "Does the form display a value computed at render time? Does the POST handler re-compute it? If yes, ToCToU."
- **Audit anchors must be PERSISTED to be audit anchors.** Lesson from chart-pattern flag-v1 brainstorm R4 M1 (2026-04-26). R3 added a `pipeline_run_id` field to `EntryRequest` as an "audit anchor" but didn't persist it to the trade row — provenance theater that would have evaporated at record_entry return. **Pattern:** when introducing an audit/provenance field on a transient request type, the next checklist item is "is this column also added to the persistent target row?" If not, it's not really an audit anchor.
- **Schema-layer guarantees beat repo-layer guarantees on every dimension except backward-compat migration cost.** Lesson from chart-pattern flag-v1 brainstorm R2 M2 (2026-04-26). Cache table got a row-level CHECK because it was a fresh CREATE TABLE (cheap); trade table couldn't get one without a heavyweight CREATE-COPY-DROP-RENAME migration, so the constraint went repo-layer-only with explicit residual-risk acknowledgment. **The asymmetry is a real cost** — non-repo writers (e.g., raw SQL via sqlite3 CLI) can violate the trade-row invariant. **Pattern:** when migration cost forces deferring schema-layer enforcement, document the residual gap explicitly and bundle the schema fix with the next column-change migration to amortize the cost.
- **Symmetric refusal across entry surfaces.** Lesson from chart-pattern flag-v1 brainstorm R1 C1 (2026-04-26). The form's "Not classified" stub design for out-of-scope tickers conflicted with the CLI's broad-flag-acceptance design — operator could bypass locked-constraint #5 (cached-only consumption) via CLI. Fix: CLI parity gate refuses `--chart-pattern-operator` without cached row, mirroring form's stub gate. **Pattern:** when one entry surface shows a stub for "feature X is unavailable here," the other surface's analogous control should refuse with an explicit error, not silently allow.
- **Adversarial review verdict ≠ perfection; rounds aren't redundant.** Lesson from chart-pattern flag-v1 brainstorm (2026-04-26). 5-round arc found 22+ distinct findings (2 critical, 11 major, 3 minor accepted-with-rationale, 5+ resolved-by-fix at minor severity). The final R5 minor was a precision nit in a comment that no operator would ever read but a future implementer might. Each round refined a specific failure mode; the rounds aren't redundant work, they're a systematic widening of the spec's defended perimeter. The cycle is worth its cost on high-stakes design work.
- **Synthetic test fixtures must verify parameter→measurement 1:1 mapping empirically before threshold-pair tests are committed.** Lesson from chart-pattern flag-v1 Phase 1 execution (2026-04-26). The original `make_flag_bars` synthetic builder had three latent confounds (pole's first-bar half-band, pre-run-band low, close-relative tightness scaling) that made threshold-pair tests pass-for-the-wrong-reason. Two fix-up commits (`065d368`, `96abfaf`) corrected the mapping. **Generalization:** for any synthetic test fixture for a measurement-gating algorithm, pull representative measurements from the fixture under known parameters and assert the expected values BEFORE writing threshold-pair tests against those parameters. Otherwise threshold-pair tests are vacuous despite arithmetic that "should" distinguish.
- **Detection-outcome tests at search-path-dependent boundaries are NOT gate-correctness tests; pair them with cfg-injection sensitivity tests.** Lesson from chart-pattern flag-v1 Phase 1 (Codex R1 M1, 2026-04-26). The original tightness 0.601/0.599 test happened to pass for the wrong reason — search shifted to a window failing OTHER gates, not because tightness_ratio crossed 0.6. **Fix pattern:** for every gate's threshold-pair test, add a parallel cfg-injection sensitivity test that tightens the gate's cfg threshold and verifies the same fixture flips. The cfg-injection test isolates "is THIS gate enforcing the boundary?" from "does the synthetic happen to detect/reject at these parameters?" Naming convention: `*_above/below_threshold_*` for detection-outcome regression; `*_is_threshold_sensitive` for true gate-correctness via cfg-injection.
- **Reference-enumeration test patterns address determinism + correctness simultaneously for ranking/search algorithms.** Lesson from chart-pattern flag-v1 Phase 1 (Codex R1 M2, 2026-04-26). Tasks 1.11 (search) and 1.12 (best-attempted ranking) originally only asserted determinism (two calls produce same result). After R1 M2, they enumerate all (M, N) candidates via classifier helpers and assert the search picks the EXACT highest-confidence (or max-min-soft-clearance) candidate. Lockstep risk vs the helpers themselves is bounded if per-gate threshold-pair tests catch helper bugs (defense-in-depth). **Pattern:** for ranking/search algorithms, write reference-enumeration tests that compute the expected ranking via the algorithm's helpers and assert the algorithm's verdict matches.
- **Batched-implementation-then-per-element-validation is acceptable for tightly-coupled measurement code IF the plan explicitly documents the chosen pattern.** Lesson from chart-pattern flag-v1 Phase 1 (Codex R1 M4 ACCEPTED, 2026-04-26). Strict per-task TDD red-green-commit is rigid for architectural and stateful code. For tightly-coupled measurement code where the search loop calls every gate, per-gate scaffolding-then-deletion produces throw-away code. Plan deliberately structured Tasks 1.4-1.13 as test-only validation against Task 1.3's batch implementation; plan task text explicitly says "Run to see GREEN (gate already implemented in 1.3)" — adversarial review accepted the deviation because the pattern was documented in advance. **Generalization:** when an algorithm's components are too coupled to red-green-commit in isolation, batch the implementation in one task and use subsequent tasks for per-element test-only validation; document the deviation explicitly in the plan task text so the deviation isn't mistaken for TDD discipline failure.
- **`copowers:executing-plans` → `superpowers:subagent-driven-development` self-collides when multiple subagents pick up the same task.** Lesson from chart-pattern flag-v1 Phase 2 execution (2026-04-26). Phase 2 produced 16 commits instead of the planned 9 because parallel subagents picked up Tasks 2.4 (PipelinePatternClassification dataclass; commits `2d00e9f` + `b8d0435` 82 seconds apart with identical titles + cleanup `5424229`), 2.5 (pattern_classifications repo; commits `22c9f65` + `e3bafba` 42 seconds apart), and 2.7 (insert_trade extension). Implementer self-diagnosed as "an unidentified parallel agent / session" but commit metadata (same author, tight timestamps, identical commit titles) confirmed internal subagent parallelism. ~30% of the chain was cleanup of redundant work. **Mitigation in dispatch briefs (operator decision 2026-04-26):** explicit task partitioning — each task assigned to exactly one subagent; agents may execute multiple tasks; task sets must be DISJOINT across agents; agents must verify task deliverable doesn't already exist before starting (abort + report if it does). Worktree isolation is the heavier fallback if task-partitioning alone is insufficient.
- **Plan-spec test text with literal FK references can render ValueError tests vacuous when PRAGMA foreign_keys=ON.** Lesson from chart-pattern flag-v1 Phase 2 (2026-04-26). Phase 2 plan lines 1620-1670 used literal `pipeline_run_id=1`/`=42` in invariant-test bodies; with PRAGMA foreign_keys=ON (project default), schema-layer FK IntegrityError fires BEFORE the repo-layer ValueError can. Mitigated via `_seed_pipeline_run` helper at execution time. **Watch-item template for future plans:** when literal FK references appear in test bodies, the plan must specify "seed FK target" pattern explicitly (or use sentinel values that don't collide with the FK constraint). Codex review will catch this if the test asserts on `ValueError` and the actual exception is `IntegrityError`, but earlier prevention is cheaper.
- **Cross-column biconditional invariants need exhaustive truth-table enumeration, not incremental invariant listing.** Lesson from chart-pattern flag-v1 Phase 2 (Codex R1 Major 1, 2026-04-26). Phase 2's `_validate_chart_pattern_invariant` was mis-implemented twice: spec-divergence in `c606df1` caught at code review; missing 4th invariant (orphan confidence: `algo=NULL, conf=set, anchor=NULL`) caught by Codex R1 Major 1 fixed in `115c96b`. Listing 3 invariants instead of explicitly enumerating ALL combinations of (algo, conf, anchor) over the {None, "flag", "none"} × {None, set} × {None, set} cube minus the spec's permitted shapes is fragile. **Better template for biconditional invariants:** parameterized table of all combinations → permitted/raises that's exhaustive over the cube; tests parameterize over the same table.
- **Brief artifacts must be committed immediately; untracked briefs are vulnerable to working-tree state issues.** Lesson from chart-pattern flag-v1 Phase 2 dispatch (2026-04-26, refined post-Phase-3). Orchestrator drafted the Phase 2 dispatch brief and left it untracked, expecting the dispatch chain to commit it (as happened with Phase 1's `9f0a778`). Initial diagnosis was that subagent activity deleted the untracked file; **post-Phase-3 verification confirmed the file was actually on disk the whole time** — `ls` returned "No such file" due to transient Windows ACL state on the working directory (the same ACL pattern that blocks scratch-directory cleanup). Actual loss was illusion; recovery was instantaneous when the directory state cleared. **Pattern stands:** untracked files in the working tree are vulnerable to transient inaccessibility (ACL state, subagent activity, IDE indexing); orchestrator MUST commit briefs in the orchestrator-thread immediately after writing them, not rely on the dispatch chain to commit. Robustness is the principle even when the specific failure mode turns out to be illusion.
- **Discriminating-test discipline applies to log-line format assertions, not just test arithmetic.** Lesson from chart-pattern flag-v1 Phase 3 (Codex R1 Major 2, 2026-04-26). Implementer's first attempt at the end-of-step summary log test asserted `"errors" in m`, which would have passed regardless of whether the formula was `total - errors - missing` or `total - errors` or any other variant. Codex caught the substring-match weakness; fix tightened to exact-string assertion `"flag_classifier: 0/1 ok, 1 errors"`. **Generalization:** when the assertion's purpose is to verify EXACT format (log-line schema, JSON-key presence, error-message wording, etc.), substring matching is insufficient. Use exact-equality or structural assertions. Memory file `feedback_regression_test_arithmetic.md` extended with this generalization.
- **Spec/plan formula transcription gap: when the plan provides a code formula, the spec should also specify the formula's semantic intent.** Lesson from chart-pattern flag-v1 Phase 3 (Codex R1 Major 1, 2026-04-26). Spec §3.3 said `flag_classifier: {ok}/{total} ok, {errors} errors` without specifying `ok`/`total` semantics. Plan provided `ok = total - errors - sum(missing)` formula. Implementer faithfully copied the formula. Codex caught the conflation: `total = len(targets)` includes fetcher-failed tickers that never reached the classifier, so the denominator is wrong for "classifier health." **Pattern:** when a plan provides a code formula, the spec should also state the formula's semantic intent in one line so plan-code can be sanity-checked against the spec — not just transcribed from plan to implementation.
- **Subsequent-phase tests can surface earlier-phase contract bugs.** Lesson from chart-pattern flag-v1 Phase 3 (Codex R3 Major 1, 2026-04-26). Phase 3's strengthened happy-path test (asserting full PipelinePatternClassification field-mapping) surfaced that Phase 2's `_row_to_classification` repo function returns ISO date strings instead of `date` objects despite the dataclass being annotated `date | None`. Phase 3 was scope-locked OUT of `swing/data/` (Phase 2 carve-out territory), so the fix was deferred to Phase 4 as Task 4.0a (carve-out extension). **Pattern:** when phase carve-outs lock subsequent phases out of earlier-phase code, the subsequent phase's tests should be the strongest possible regression surface against the earlier-phase contracts; adversarial review at the boundary will catch contract drift; remediation lands in the consuming phase as an explicit prerequisite carve-out extension.
- **Single-subagent dispatch reduces but does not eliminate self-collision.** Lesson from chart-pattern flag-v1 Phase 3 (2026-04-26). Phase 3 was dispatched to a single subagent for sequential Tasks 3.1 → 3.4. Despite no agent-vs-agent collision possible, the subagent produced a rogue duplicate Task 3.2 commit (b080da9) that was immediately reverted (132142c). Most plausible mechanism: subagent context drift causing re-attempt of a completed task without recognizing the prior completion. **Brief discipline + observable verification is the next layer:** subagent must include `git log --grep="Task X.Y" --oneline` output in commit body before each task commit; makes duplicate-task detection observable to Codex review. Worktree isolation is the heavier fallback if the observable-verification layer also fails.
- **Brief task-ID rule scope: task implementations only.** Lesson from chart-pattern flag-v1 Phase 3 (Codex R2 Major 1, 2026-04-26). Phase 2 set the precedent that adversarial review-fix commits don't carry task IDs (commit `115c96b` is named `fix(data): chart_pattern invariant rejects orphan confidence...` without a Task N.M ID). Phase 3 followed the precedent. Codex R2 Major 1 raised the consistency concern; ACCEPTED with rationale (review-fix commits aren't task implementations and don't risk the partitioning failure modes the rule was designed to prevent). **Formalized in standing conventions (Recent decisions 2026-04-26):** task implementations get task IDs; review-fix commits use round + finding ID format; format-only cleanup commits no task ID needed.
- **Single-subagent dispatch + observable verification successfully prevents rogue duplicates** (Phase 4 vindication, 2026-04-26). Phase 4 used the brief discipline + observable verification (subagent must include `git log --grep="Task X.Y"` output in commit body) and produced ZERO rogue task duplicates across 13 commits — Phase 3's `b080da9`-style failure did NOT recur. Single-sequential-subagent dispatch + observable evidence is a working alternative to worktree isolation for the subagent-collision failure mode at this scale. Worktree isolation reserved as fallback for future phases if a new failure mode emerges.
- **Observable-verification grep needs subject-only refinement to eliminate forward-reference false positives.** Lesson from chart-pattern flag-v1 Phase 4 (2026-04-26). The `git log --grep="Task X.Y"` rule generates false positives when commit bodies cross-reference future task IDs in narrative prose (e.g., commit body mentions "Task 4.7 follow-up"). Refinement: `git log --pretty='%s' --grep='^[a-z]+\([a-z]+\): Task X.Y'` — subject-only, anchored to conventional-commit `<type>(<area>): Task X.Y` structure. Eliminates false positives without constraining commit-body prose. Captured in standing conventions; Phase 5+ briefs adopt the refinement.
- **Sort-coupling test vacuousness extension to discriminating-test discipline.** Lesson from chart-pattern flag-v1 Phase 4 (Codex R2 Major 1, 2026-04-26). Even after the R1-split into separate sort-neutrality and wiring tests, the sort-neutrality test was STILL vacuous because the deterministic ticker-ASC tiebreaker made the alphabetically-first ticker (AAA) also the classified one — both correct sort and the hypothetical pattern-tag-first-bug placed AAA first. The fix flipped the ticker assignment so the classified ticker became alphabetically-second; sort-neutrality now requires alphabetical to win OVER any hypothetical pattern-tag bug. **Generalization beyond the existing `feedback_regression_test_arithmetic` discipline:** when designing a discriminating test for an architectural-separation claim, mentally simulate the SPECIFIC bug you're guarding against — is there an output value where the bug AND correct behavior coincidentally agree? If so, the test setup must be INVERTED so the bug's output diverges. Stronger than "verify the discriminator empirically" (which catches re-masking via secondary keys but not symmetry-via-deterministic-tiebreaker). Memory file `feedback_regression_test_arithmetic.md` extended with this generalization.
- **Codex's adversarial review consistently catches subtle test-discipline failures across phases.** Pattern observation (2026-04-26 post-Phase-4 retrospective). Phase 1 caught threshold tests passing for the wrong reason (cfg-injection sensitivity needed); Phase 3 caught log-line substring weakness (exact-equality needed); Phase 4 caught compounding-confound conflation AND ticker-symmetry vacuousness in a 2-round arc. Without the Codex cycle, the tests would have shipped passing-but-not-actually-discriminating. The 5-round adversarial-review investment is yielding repeated ROI on test-quality dimension; not just spec-fidelity.
- **Brief scope-deviation pattern: downstream tests of an authorized carve-out file are naturally in-scope.** Lesson from chart-pattern flag-v1 Phase 4 (R1 Major 2 ACCEPTED, 2026-04-26). Phase 4 brief authorized Task 4.0a Phase 2 carve-out extension touching `swing/data/repos/pattern_classifications.py` (the date deserialization fix). Downstream test file `tests/pipeline/test_step_charts_classification.py` had `.isoformat()` workarounds that became broken once dates became `date` objects — Task 4.8 fixed them. Codex flagged the scope deviation; ACCEPTED as in-scope-by-spirit. **Pattern for future briefs:** when authorizing a carve-out extension touching upstream behavior, the brief should explicitly say "downstream tests of the carve-out file are in-scope by extension" OR explicitly enumerate them. Otherwise legitimate-but-unauthorized work surfaces as scope deviations during review.
- **Base-layout 5-VM rule applies only when `base.html.j2` dereferences the field.** Lesson from chart-pattern flag-v1 Phase 4 (2026-04-26). Phase 4 brief blanket-required all 5 base-layout VMs (`DashboardVM`, `PipelineVM`, `JournalVM`, `WatchlistVM`, `PageErrorVM`) to gain `pattern_tags`. Implementer correctly scoped to 2 (`WatchlistVM`, `DashboardVM`) per spec §3.5 because flag-tag rendering lives in `partials/watchlist_row.html.j2` (consumer-scoped); `base.html.j2` doesn't reference `pattern_tags`. CLAUDE.md gotcha applies only when `base.html.j2` actually dereferences the new field. **Future briefs:** verify whether the gotcha applies before requiring its mitigation; don't blanket-require the 5-VM rule for fields that aren't base-layout-consumed.
- **Internal code-review BEFORE Codex saves a round.** Lesson from chart-pattern flag-v1 Phase 5 (2026-04-26). Phase 5 plan explicitly anticipated the I1 fix (try/except ValueError around `record_entry`) at lines 3801–3802; implementer missed it during initial implementation; INTERNAL code-review caught it before adversarial Codex round (commit `8507c1a`). Without the internal review pass, this would have surfaced as Codex R1 finding. **Pattern:** running internal code-review pass BEFORE adversarial Codex pre-empts plan-anticipated misses, saves Codex round budget, and tightens the cumulative arc. Implementer sequence: TDD → internal code-review → Codex adversarial review → fix loop. The internal pass costs little; the round saved is meaningful when Codex rounds add 5-15 minutes each.
- **Codex's contextual advantage at finding cross-feature interactions vs internal review.** Lesson from chart-pattern flag-v1 Phase 5 (Codex R1 Major 2, 2026-04-26). The clearest example yet of Codex's whole-diff review catching interactions internal code-review naturally misses. Phase 5 added `chart_pattern_*` form fields; the soft-warn confirm flow (an EXISTING route path Phase 5 didn't directly touch) silently dropped the new hidden inputs on the second-submit round-trip — violating spec §3.6. Internal review naturally focuses on the brief's listed surface area (entry form, POST handler, CLI flag); Codex's whole-diff review caught the soft-warn × new-feature interaction. **Pattern:** when a new feature adds form fields OR persistence fields, ALL existing code paths that re-render the form OR re-submit values are interaction surfaces. Brief should preempt by enumerating them but rarely will catch all. Adversarial review's contextual whole-diff scan is the reliable safety net.
- **Schema-message coupling for IntegrityError mapping is a V1-vs-future-hardening tradeoff.** Lesson from chart-pattern flag-v1 Phase 5 (Codex R2 Minor 1, 2026-04-26). Phase 5's IntegrityError handler in `entry_post` substring-matches SQLite error text to distinguish chart_pattern-related failures from generic ones (treats "FOREIGN KEY constraint failed" as chart-pattern-specific when `cp_anchor_value is not None`). Easier-now (no schema introspection or pre-insert checks); more-fragile-later (future schema additions could silently turn 400 into 500 or misclassify integrity failures). Acceptable for personal-use single-operator scope; documented in `swing/web/routes/trades.py` per follow-up commit `4ef9044`. **Pattern:** when a handler narrows by error-message substring, the coupling is brittle to schema changes; document the coupling explicitly so future schema-change reviewers see the load-bearing assumption. Forward-looking remediation if scope broadens: pre-insert FK existence check OR explicit CHECK-name discriminator.
- **Single-subagent + observable verification VINDICATED 2 consecutive phases.** Lesson refined from chart-pattern flag-v1 Phases 4 + 5 (2026-04-26). Phase 4 had ZERO rogue duplicates after adopting the discipline; Phase 5 also had ZERO. Subject-only grep refinement (operator decision 2026-04-26 post-Phase-4) eliminated forward-reference false positives. Worktree isolation NOT escalated; reserved as fallback for novel failure modes only. **Generalization:** single-subagent dispatch + observable verification + 3-tier commit-message convention is the working baseline for `subagent-driven-development` invocations at this project's scale. Each phase's discipline carries forward to the next; operator decisions about discipline tighten the brief template incrementally.
- **3-tier commit-message convention works cleanly in practice.** Lesson from chart-pattern flag-v1 Phase 5 (2026-04-26). Phase 5 commit log demonstrates the convention disambiguates implementation-vs-review work without ambiguity: task implementations (`feat(area): Task X.Y — ...`); review-fix commits including INTERNAL code-review (`fix(web): code-review I1 — ...`) AND Codex (`fix(web): Codex R1 Major 2 — ...`); format-only cleanup (no task ID). **Refinement:** the convention extends naturally to internal code-review fixes (not just Codex); use the prefix `code-review` (not `internal-review` or other variants) per Phase 5 commit `8507c1a` precedent. Phase 6 added a 4th case: internal-Codex rounds (subagent-dispatched within-task) need `(internal)` qualifier to distinguish from orchestrator-Codex rounds (wrapper-dispatched post-task). See Binding conventions above for the formalized convention.
- **Plan-supplied test code can have plan-internal contradictions that surface only at implementation time.** Lesson from chart-pattern flag-v1 Phase 6 (internal review caught pre-Codex, 2026-04-26). Phase 6 plan asserted `"flag (0.78)" in price_ax.get_title()`; mpf actually renders title as `fig.suptitle`, not on the price axes. Implementer's first-pass workaround (`price_ax.set_title(title)`) made tests pass but produced duplicate title in PNG (visible in manual visual verification). Internal review caught it BEFORE Codex round; fix in commit `803607e`. **Pattern:** when a plan-supplied test makes assumptions about library behavior, verify the assumption empirically before adapting production code to satisfy the test. Especially relevant when test asserts on rendered output structure (matplotlib axes vs figure-level rendering).
- **Manual visual verification is not optional for rendering work.** Lesson from chart-pattern flag-v1 Phase 6 (2026-04-26). Phase 6 had 27 tests passing with the duplicate-title regression in place; only the manual `Read` of the rendered PNG caught it. Tests verified structural correctness (LineCollection count, title string equality on `fig.suptitle`); they did NOT verify visual correctness (do bands look reasonable? is title legible? is `set_title` producing a duplicate visible title?). **Pattern:** when production code emits structured artifacts (PNG, PDF, HTML, SVG), structural tests + manual visual verification are both required for shippable rendering work. Phase 6's "render IS the artifact" framing was load-bearing.
- **Non-discriminating "discriminating" tests via monkeypatch-capture failures.** Lesson from chart-pattern flag-v1 Phase 6 (internal review caught pre-Codex, 2026-04-26). Phase 6 plan-supplied LineCollection-count comparison silently degraded to vacuous (`3 > 0` always-true) because the baseline-capture monkeypatch couldn't fire on the None path (`mpf.plot(...)` without `returnfig=True` doesn't expose the figure to capture). Internal review caught it; fix in commit `d606840` rebuilt the baseline by direct mpf call instead of monkeypatch. **Pattern:** when a test's discrimination depends on monkeypatch capture, verify the capture actually populated; if the production codepath doesn't take the captured route, the test is vacuous. Adds to the existing compounding-confound failure-mode catalog (now: secondary-key re-masking; bound-reference confusion; substring-vs-exact; symmetry-via-deterministic-tiebreaker; monkeypatch-capture failure).
- **Internal-Codex-round-BEFORE-orchestrator-Codex-round extends the internal-code-review-BEFORE-Codex pattern.** Lesson from chart-pattern flag-v1 Phase 6 (2026-04-26). Phase 6 implementer subagent dispatched its OWN Codex round (commit `c215c79`) BEFORE the orchestrator's wrapper-driven Codex round ran. Caught autoscale ordering bug (drawing hlines before get_ylim) that orchestrator's R1 would have caught later. Round budget saved: estimated 1–2 orchestrator rounds. **Pattern evolution:** internal review pre-empts orchestrator review at multiple discipline levels (manual code-review per Phase 5 lesson; internal-Codex round per Phase 6 lesson). Each layer adds modest cost; tradeoff vs Codex round budget. Implementer sequence: TDD → internal code-review → internal-Codex round → orchestrator-Codex round → fix loop. The `(internal)` qualifier on internal-Codex commits keeps the audit trail readable.
- **Subagent role-partitioning within a task is collision-safe; task-partitioning is the binding constraint.** Lesson from chart-pattern flag-v1 Phase 6 (2026-04-26). Phase 6 dispatched 7 subagents within a single task (Task 6.1) at different roles (implementer / spec reviewer / fix-implementer at multiple stages / code-quality reviewer / cleanup-batch implementer / Codex-fix implementers). ZERO rogue duplicate task commits. **Pattern:** the disjoint-task-partitioning discipline operates at the TASK level, not the subagent-count level. Multi-stage review-fix pipelines within a task are fine; only task-level overlap creates rogue duplicates. 3-phase ZERO-rogue track record (Phases 4 + 5 + 6) vindicates the role-partitioning interpretation.
- **Documentation of contract pins resolves coupling concerns into explicit contracts.** Lesson from chart-pattern flag-v1 Phase 6 (Codex R3 Major 1, 2026-04-26). Phase 6 R3 M1 raised "positional-int axis vs date-axis contract not documented" as a coupling concern (test assertions like `(80, 100)` lock to mpf internals). Resolution: documented the V1 design choice explicitly in `_bar_idx`, `PatternOverlay`, and discriminating test docstrings. **Pattern:** when test assertions encode a library-internal abstraction, document that as the V1 contract explicitly. Otherwise future Codex rounds will keep flagging the abstraction as "hidden coupling." The documentation turns implicit coupling into explicit contract pin — Codex stops re-raising; future maintainers see the load-bearing assumption.
- **R3→R4 induced-bug pattern: every "force a specific behavior" fix requires verifying the new behavior's failure mode is also handled.** Lesson from chart-pattern flag-v1 Phase 7 implementer-side (2026-04-27). Codex R3 Minor 1 fix forced UTF-8 encoding on the JSON read (`read_text(encoding="utf-8")`); this introduced R4 Major 1 (UnicodeDecodeError on invalid UTF-8 bytes now bypasses the path-bearing ValueError handler). R4 M1 was caught by the next Codex round; fix wrapped the read in try/except UnicodeDecodeError → ValueError with path. **Generalization:** tightening one error path can MOVE the failure point; the upstream catch (or wrapper) must cover the new failure mode introduced by the tightening. Add to the lessons catalog. Pattern especially relevant for: encoding pins, type pins, exception-narrowing — each "force this specific behavior" change creates a new failure-mode surface that needs explicit handling.
- **Brief skeleton bugs surface as Codex R1 findings.** Lesson from chart-pattern flag-v1 Phase 7 implementer-side (Codex R1 Majors 2 + 4, 2026-04-27). Phase 7 brief skeleton's `result.pattern in ("none", None)` permissive assertion AND silent-default `notes = meta.get("notes", "")` were both caught by Codex R1 as drifts from spec §4.2 verbatim. The brief was correct in INTENT but loose in skeleton text; spec authority wins. **Pattern:** brief skeletons (especially code skeletons embedded in briefs as task specifications) are starting points, not infallible specs; adversarial review correctly catches brief-skeleton drift from spec verbatim. **Implication for orchestrator:** when including code skeletons in briefs, mark them as "starting point — verify against spec" rather than implying verbatim-correct. Or — more practically — accept that adversarial review will catch the drift; the cycle works.
- **Subject-only grep regex needs ERE + POSIX digit class to actually function.** Lesson from chart-pattern flag-v1 Phase 7 implementer-side (Open Question 1, 2026-04-27). Convention's `git log --grep='^[a-z]+\([a-z]+\): Task X.Y'` returns empty under git's default BRE — `+` is literal in BRE (not quantifier); `\d` is Perl-style (POSIX uses `[0-9]`). The "expected empty" output matched for the wrong reason; the regex was technically non-functional. Convention amended (2026-04-27) to specify `-E` (ERE) + `[0-9]`. **Past briefs that used the BRE-incompatible regex were technically non-functional; ZERO-rogue track record was real because (a) implementers read commit subjects directly anyway, and (b) other partitioning rules carried the load.** Lesson: when a convention prescribes a regex, empirically verify it works as specified; otherwise the convention's force is purely social, not technical.
- **Internal-review-BEFORE-Codex pre-empts brief-skeleton drift.** Lesson from chart-pattern flag-v1 Phase 7 implementer-side (2026-04-27). Phase 7 implementer ran 5 internal manual code-review fix dispatches BEFORE invoking adversarial Codex (`b473753`, `c2f4460`, `48ef30a`, `1389aa0`, `5db8f9d` — all `code-review T7.X` prefix). These caught README-drift findings (verbatim claim, _v2 filename footgun, README §6+§7 alignment, README §2 bar-count, MIN_BARS value, malformed-CSV behavior) that would have surfaced as Codex Major findings without the internal pass. **Internal-review-BEFORE-Codex pattern (Phase 5 lesson) extends to brief-skeleton drift, not just plan-anticipated misses** — internal review can catch documentation/skeleton drift just as effectively as code-correctness drift. Estimated Codex round budget saved: 2-3 rounds.
- **5-round Codex loop shows compound ROI even on small (<500-line) test-only diffs.** Lesson from chart-pattern flag-v1 Phase 7 implementer-side (2026-04-27). Phase 7 was test-infrastructure-only (helper module + parametrized integration test + README); diff was small. Despite this, 5 Codex rounds caught DISTINCT failure-mode classes per round: R1 (4 Majors: parser wrap, JSON schema, 60-bar contract, assertion shape — structural), R2 (2 Majors: in-memory canonicalization documentation drift, notes-required governance gap — second-order interactions), R3 (1 Major: JSON root type — defense-in-depth), R4 (1 Major: UTF-8 pin → UnicodeDecodeError bypass — induced-bug from R3 fix), R5 (clean). **Generalization:** Codex round budget is roughly proportional to BUG SURFACE complexity, not diff line-count. Test infrastructure for governance contracts (label enums, schema validation, error paths) has dense bug surface despite small line-count. The 5-round cycle is worth its cost even on small diffs when the diff contains contract-enforcement code.
- **Manual visual verification IS load-bearing — string-equality tests are not enough for rendered output.** Lesson reinforced from chart-pattern flag-v1 mathtext fix regression (commit `2fd0ecc`, surfaced 2026-04-27 manual verification round). Orchestrator applied a tiny one-line fix (`$` → `\$` escape in chart title format string), updated test assertions to match the new title STRING, and committed without re-rendering a chart PNG to verify the visual. Tests passed (string-equality on title); rendered title still showed mathtext interpretation (italics on "stop", `$` consumed). The Phase 6 lesson "Manual visual verification is not optional for rendering work" applied to that fix and was violated. **Pattern:** any change to text-rendering format strings (matplotlib, mplfinance, jinja-template-rendering-to-PDF, etc.) MUST include manual visual verification of the rendered output, NOT just string-equality assertions. Even one-line "small" fixes deserve the visual re-render check when the output is structured artifact rather than plain text. Orchestrator-side discipline: when applying ANY rendering-related fix in the orchestrator-thread (vs dispatching to implementer), block the commit until visual verification runs OR explicitly defer to implementer dispatch where visual verification is in-scope.
- **Subagent role-partitioning matures into a multi-stage review pipeline.** Lesson refined from chart-pattern flag-v1 Phase 7 implementer-side (2026-04-27). Phase 7 dispatched 8 subagents at distinct roles: 2 implementer (Tasks 7.1 + 7.2) + 2 spec-review + 2 code-quality-review + 1 final-review (consolidated all four review angles per Phase 6 role-partitioning lesson) + 4 fix dispatches across R1–R4. ZERO rogue duplicate task commits. **The role-partitioning approach has converged into a multi-stage pipeline: TDD implement → spec review → code-quality review → final review → orchestrator Codex → fix loop.** Each stage's output feeds the next; no two subagents claim the same task. 4-phase ZERO-rogue track record (Phases 4 + 5 + 6 + 7) confirms the pipeline is robust. Generalization: subagent count scales with REVIEW DEPTH per task, not task count; collision risk stays bounded because role partitioning is orthogonal to task partitioning.
- **Adversarial review can catch factual errors in accepted-with-rationale dispositions, not just new findings.** Lesson from Tier-1 mathtext fix dispatch (Codex R2 on R1 acceptance, 2026-04-27). The implementer's R1 acceptance rationale for the ticker-mathtext-hardening concern cited `swing/pipeline/finviz_schema.py:22-41` as part of the defensive perimeter; R2 noted the validator only checks structural columns/header/row-count, NOT ticker content — so it does NOT filter mathtext metacharacters. The disposition (ACCEPTED out-of-scope per brief §4.5 pre-designation) was unchanged but the rationale was tightened by removing the over-broad citation. **Generalization:** when accepting a finding, cite specific evidence; expect the next round to fact-check those citations. "Accepted-with-rationale" decisions can themselves contain factual errors that a subsequent round catches and corrects without changing the underlying disposition. Pattern complement to "Compounding-confound test fixtures" (Phase 4 lesson) and "Substring-vs-exact" (Phase 3 lesson) — both about discriminating-test discipline; this one extends the discipline to acceptance-rationale citations.
- **Brief-drafting refinement: distinguish discriminating assertions from coupled text updates.** Lesson from Tier-1 mathtext fix dispatch (2026-04-27). Brief §4.1 specified updating BOTH `tests/rendering/test_chart_overlay.py:270` (asserted `expected`) AND `:287` (passed as `title=` to a baseline `mpf.plot` for LineCollection-count comparison; never asserted on). Implementer's TDD red-phase showed only line 270 flipped to RED — line 287's update was a coupled-text update (must stay in sync, but doesn't independently discriminate the fix). **Pattern for future briefs specifying multiple test-line updates:** pre-classify each line as "discriminating assertion" (will flip RED → GREEN with the fix) vs "coupled text update" (must stay in sync but doesn't independently discriminate). Avoids implementer confusion and tightens the brief's TDD red-phase expectation. Adds to brief-drafting taxonomy alongside "canonical-template references win over prose count assertions" (build_watchlist mixed-anchor lesson).
- **Brief pre-designation of "out-of-scope watch items" enables clean adversarial dispositions.** Lesson from Tier-1 mathtext fix dispatch (2026-04-27). Brief §4.5 explicitly listed ticker-mathtext metacharacters (`^`, `_`, `\`) as "out-of-scope but worth noting if found" — pre-empting the predictable adversarial concern. When R1 surfaced exactly that concern, disposition was mechanical: cite the brief, accept, capture in follow-ups. Saves a round of back-and-forth on whether the finding is in-scope. **Generalization:** when a brief anticipates an adversarial concern that's intentionally deferred OR genuinely out of scope, explicitly pre-designate it in the brief's adversarial-review §. Codex still surfaces it (it can't NOT find it), but the brief's pre-designation makes the disposition trivial. Combines with "downstream tests of authorized carve-out file are naturally in-scope" (Phase 4 lesson) — the same brief-pre-designation principle, applied to in-scope-by-extension cases.
- **Manual visual verification 4-point check format is reusable for rendering-fix briefs.** Lesson from Tier-1 mathtext fix dispatch (2026-04-27). Brief §4.4 specified 4 explicit confirmation criteria (spacing / italics / glyphs / suffix) for the rendered PNG; implementer reported PASS/FAIL per criterion with one-sentence-each evidence. Format produced clean, audit-trail-friendly evidence the operator could independently re-verify by opening the PNGs themselves. **Reusable pattern:** any future rendering-fix brief should specify N explicit visual-confirmation criteria in §4-equivalent and require the implementer report PASS/FAIL per criterion in the return report. Phase 6 lesson "Manual visual verification is not optional for rendering work" gets a concrete brief-template implementation. Operator-side independent re-verification is the final acceptance gate; implementer's job is to produce clean evidence + PNGs accessible at known paths.
- **Adversarial review can find real defects in brief-excluded territory.** Lesson from chart-access UX dispatch (Tier-2 #2/#3, R2 Major, 2026-04-27). The cross-surface chart-scope drift race exists project-wide; brief properly excluded modifications to `chart_scope.py` and the watchlist expand pattern; Codex correctly surfaced the race as a Major. Disposition: ACCEPTED out-of-scope, captured as cross-surface follow-up (later closed by chart-scope policy v2 dispatch). **Generalization:** when a brief carves out "do not modify X," adversarial review may find defects in X. The carve-out is correct (scope discipline), the finding is real, and the disposition is "ACCEPTED, capture as cross-surface dispatch." This validates brief discipline + requires orchestrator backlog discipline to actually action the captured items. Pattern complement to "Brief pre-designation of out-of-scope watch items" (Tier-1 mathtext lesson) — both are about handling adversarial findings against brief-excluded scope.
- **Live verification on data-state-dependent features can be blocked by data state.** Lesson from chart-access UX dispatch (#2 in-scope-path verification, 2026-04-27). The `/charts/{ticker}.png` 303-redirect happy path couldn't be visually verified at dispatch time because today's pipeline produced zero `chart_status='ok'` chart_targets (yfinance-side fetcher_failed). Implementer correctly chose comprehensive TestClient coverage + flagged the data-state limitation in return report. **Pattern:** when designing a dispatch's verification procedure, anticipate data-state preconditions and explicitly call out what happens when they don't hold. For data-dependent paths, TestClient with synthetic data can compensate; for production-data-bound visual verification, defer to natural-workflow exercise.
- **Stale dev-server is a recurrent failure mode in live verification.** Lesson from chart-access UX dispatch (live verification, 2026-04-27). Operator's pre-existing `swing web` on port 8080 was running pre-dispatch code; first verification curl returned StaticFiles' default 404 instead of the new HTML 404. Implementer correctly diagnosed by spinning a fresh server on port 8081 and running side-by-side. **Pattern:** verification scripts that hit `localhost:<port>` need to either explicitly restart the server first OR explicitly verify they're hitting fresh code (e.g., via a known new-endpoint marker that didn't exist pre-dispatch). For implementer dispatches: add to brief escape-hatches "if route doesn't fire as expected, check whether dev server is running pre-dispatch code by hitting a new-route marker."
- **Compounding-confound class recurs in writing-plans phase.** Lesson reinforced from chart-scope policy v2 writing-plans (R3 Major 1 + R4 Major 1, 2026-04-27). R3 caught stale-binding tests routed through the legacy heuristic resolver path NOT the FK-backed path (would have passed without exercising the new code). R4 caught helper-direct tests passing while render_chart never actually called the new helper (wiring never verified). Both are the same Phase 4 + Bug 7 + Phase 6-monkeypatch-capture-failure class: tests that pass for the wrong reason. **Generalization:** every plan task with a discriminating test should include a "would this test fail if the implementation never actually called the new code?" sanity-check question. This is now the canonical question for plan-task drafting; absence is a writing-plans-phase smell.
- **Real-codebase verification before plan-drafting is essential.** Lesson from chart-scope policy v2 writing-plans (R1 Major 1, R2 Major 1, R3 Major 1+2, 2026-04-27). Multiple Codex findings caught implementer mis-references to actual fixtures (`seeded_db` returns tuple vs `db_conn` yields connection), Trade ctor signatures, repo function signatures, and mplfinance import scope (`mpf.plot` patched on the wrong namespace). Each was a fresh-eye discovery against the actual codebase. **Pattern:** writing-plans implementers must grep actual fixtures + signatures + import paths before drafting test code in plan tasks; codebase-vs-spec-text drift is a primary failure mode in plan-drafting. Add to writing-plans skill posture as a pre-condition step.
- **5-round Codex cycle is normal for writing-plans on substantive scope; reduces executing-plans cycles.** Lesson from chart-scope policy v2 (writing-plans 5 rounds + executing-plans 2 rounds, 2026-04-27/28). Plan-quality investment compounds: each plan-phase Codex round catches issues that would otherwise surface as executing-phase bugs. **Generalization:** invest in plan rigor at writing-plans time; expect 4-5 rounds for substantive multi-task plans. Executing-plans phase Codex rounds should be 1-3 if writing-plans was thorough; if executing-plans needs 5+ rounds, that's a writing-plans-phase failure surfaced post-dispatch.
- **Snapshot-semantic claims at spec/plan time should explicitly address transaction isolation.** Lesson from chart-scope policy v2 executing-plans (R1 Major 1, 2026-04-28). Spec discussed open-position tier snapshot semantics in TEMPORAL terms ("`_step_charts` queries `list_open_trades` mid-pipeline; result persisted as immutable snapshot") but did NOT address SQLite isolation: the 3 reads (candidates, watchlist, trades) could tear under a concurrent writer. Codex caught the gap; explicit BEGIN/ROLLBACK wrap closes it. **Generalization:** when a spec or plan claims "snapshot persisted immutably," the writing-plans phase MUST verify the WRITE is wrapped in a single transaction (not just the rows being immutable post-write). The temporal-snapshot framing is necessary but insufficient — transaction-scope must be explicit. Add to writing-plans phase as a checklist item for any spec section claiming snapshot semantics.
- **Operator-facing message strings emitted from MULTIPLE resolver paths must be valid for ALL paths.** Lesson from chart-scope policy v2 executing-plans (R1 Major 2, 2026-04-28). The single `CHART_REASON_MESSAGES["out-of-scope"]` was emitted by both V2 FK-backed path AND legacy heuristic path with different scope semantics — the V2 message described 3 tiers (`aplus + open_position + tag_aware_top_n`) while the legacy path used pre-V2 scope. Operator-facing misinformation. Fix: split into `out-of-scope` (V2/FK) + `out-of-scope-legacy` (heuristic) reason codes with appropriately-worded messages. **Generalization:** when a single message string crosses code paths (FK-backed vs heuristic; web vs CLI; legacy vs current), audit each path's semantics; the message must be valid for ALL paths OR split into per-path variants. Pattern combines with chart-pattern flag-v1 Phase 5 lesson on Codex's contextual advantage at finding cross-feature interactions.
- **Python config-dataclass defaults are SHADOWED at runtime by explicit overrides in tracked toml files.** Lesson from chart-scope policy v2 post-rollout (operator-caught, 2026-04-28). Task 8 raised `chart_top_n_watch` Python default 5 → 10 in `swing/config.py`. All tests passed; the executing-plans Codex review verdict was clean. **Operator's production runtime continued to use N=5** because `swing.config.toml:93` had an explicit `chart_top_n_watch = 5` override (committed back in Phase 2 era, `d0084cc`) that shadowed the new default at config-load time. **Generalization:** Python dataclass defaults are starting-points; tracked toml/yaml/json overrides win at runtime. When changing a default, MUST grep tracked config files for the field name before declaring the change sufficient. Standard pre-flight check for default-value changes: `grep -rn "<field_name>" .` (or scoped to known config-file paths) — if any tracked config file mentions the field, that file's override needs updating in the same commit OR its presence must be explicitly accepted as "operator may opt to override the new default." Pattern is the same class as Phase 5 R1 Major 2 (operator-facing strings emitted from multiple paths) but on the input side: defaults emitted from multiple sources (Python + toml + per-environment overrides) must be audited end-to-end. Add to writing-plans phase as a checklist item for any spec section claiming a default change.
- **Multi-path data ingestion needs full-path audit when a feature plumbs a new field.** Lesson from sector/industry capture executing-plans (Codex R2 Major 1, 2026-04-29). The plan focused on `_step_evaluate` (the pipeline runner's primary candidate-ingestion path) and missed `eval_cmd` (the standalone CLI ingestion path that also writes to the candidates table). Sector/industry plumbed correctly through the pipeline path but were silently dropped via standalone `swing eval`. Trade-entry surfaces would observe empty values whenever the latest evaluation_run came from standalone eval rather than pipeline eval — `latest_evaluation_run_id()` falls back to standalone evals as second-priority, so the failure mode is real, not hypothetical. Codex R2 caught it; fix mirrored the `dataclasses.replace` plumbing pattern from `_step_evaluate` into `eval_cmd`. **Generalization:** when a feature plumbs through ONE ingestion path, audit ALL ingestion paths for the same data BEFORE declaring the feature complete. This is the input-side analog of "operator-facing message strings emitted from MULTIPLE resolver paths" (Phase 5 R1 Major 2) and "Python config-dataclass defaults are SHADOWED at runtime by tracked toml" (post-`aeb2084`). All three are the same failure class — multi-path entry points to a single semantic surface need end-to-end audit, not just primary-path coverage. Add to writing-plans phase as a checklist item for any feature introducing new persisted fields: enumerate ALL writers to the affected table (pipeline runner, CLI commands, web routes, future-deferred surfaces) and verify each writer plumbs the new field OR is explicitly accepted as out-of-scope with documented degradation behavior.
- **Spec/plan silence on form-driven success-path response shape is a recurrent failure class.** Lesson from hyp-recs trade-prep expansion executing-plans (Codex R1 Major 1, 2026-04-29). Spec specified failure-path response (form re-render with error) + intra-form mechanics (origin field POST round-trip) + cross-cutting freshness footer, but did NOT specify what entry_post should RETURN on the SUCCESS path when origin=hyp-recs. The plan inherited the silence; executing-plans implementer faithfully implemented what the plan said and Codex R1 caught the gap. Failure mode in production: form `<tr>` (replacing the hyp-rec row) gets replaced with an open-positions row on success, leaving an open-positions row inside the hyp-recs `<tbody>` (visually broken) AND the hyp-recs section is not refreshed (just-traded ticker stays in recommendations panel). **Generalization:** when a spec introduces a new form-driven surface, the writing-plans phase MUST explicitly enumerate ALL response paths — error 4xx; validation re-render with field errors; success 200; OOB-swap interactions with cross-feature containers. Silence on any one path is a load-bearing assumption that will surface as either a production bug OR a Codex finding at executing-plans time. Add to the writing-plans phase as a checklist item for any spec section introducing form-handling routes: response-path matrix (status code × content type × origin variant) explicitly enumerated; OOB-swap interactions with affected cross-feature containers explicitly listed; behavior under each path discriminated by tests in the plan. Pattern is the same class as multi-path-ingestion above and Phase 5 R1 Major 2 cross-path consistency — unenumerated paths are silent failure surfaces.
- **Worktree protocol environmental note: Windows entry-point binary lock.** Lesson from hyp-recs trade-prep expansion executing-plans Task 5.7 (2026-04-29). Plan's pinned-baseline capture protocol uses temporary git-worktree at HEAD `<SHA>` + `pip install -e ".[dev,web]"` to bootstrap the baseline tree's import graph. On the operator's Windows environment, `pip install -e` failed because a long-running `swing.exe` (PID 29772) held the entry-point binary file (`%APPDATA%\Python\Python314\Scripts\swing.exe`) open via Windows file-lock semantics. Implementer recovered via `PYTHONPATH=/tmp/swing-baseline` instead — functionally equivalent for `sys.path` resolution; verified the import correctly resolved to baseline tree (no `_build_active_recommendations` present in the baseline pre-extraction). Captured the regression baseline cleanly. **Generalization:** worktree-protocol bootstrap on Windows must anticipate entry-point binary locks. Two recovery paths: (a) stop the running `swing.exe` process before `pip install -e` (cleanest; requires operator action OR scriptable detection), or (b) use `PYTHONPATH` override to skip the editable install entirely. Add to plan §"If you get stuck" template for future Task 5.7-class work: "On Windows, if `pip install -e` fails with a permission/file-lock error on `swing.exe`, either stop the running web server OR use `PYTHONPATH=<worktree-path>` to bypass the install." Pre-empts the manual recovery path the implementer hit.
- **JS-execution test harness gap matters more than I weighted it.** Lesson reinforced from hyp-recs success-path fix executing-plans (operator-witnessed production verification, 2026-04-29). The dispatch shipped 1309 fast tests passing including 5 integration tests + 1 structural-guard for the new entry_post OOB-swap behavior. Operator manual verification surfaced THREE production bugs the tests didn't catch: (a) open-positions table never updated by entry_post primary swap (pre-existing); (b) watchlist tbody nukes on hyp-recs origin entry submit (mechanism unknown from static read); (c) exclude_tickers leaks the just-traded ticker on hard-navigate (full dashboard render path doesn't pass exclude_tickers). **TestClient assertions on response body structure verify response SHAPE; they do NOT verify HTMX runtime DOM state after swap-application, OOB-swap interaction with primary-swap, or cross-section update behavior in a real browser.** The Bug 1 follow-up captured 2026-04-25 ("JS-execution test harness gap") was correctly identified as a real gap; I de-weighted it ("defer until 5+ event-handling-related bugs accumulate") because no prior dispatch had hit a production bug TestClient missed. This dispatch's failure is the third bug class TestClient missed in HTMX-driven UX (Bug 1 itself + Bug 2 sizing-hint hx-trigger; this dispatch). **Generalization:** when shipping HTMX-driven workflows, the writing-plans phase must specify EITHER (a) operator-witnessed manual verification as a binding done-criterion BEFORE dispatch can be considered shippable, OR (b) browser-driven test harness (Playwright/Selenium) for the new code paths. Plan tasks asserting "TestClient response body contains `hx-swap-oob='true'`" are necessary but insufficient — they prove the response structure but not the rendered effect. Add to writing-plans phase as a checklist item for any new HTMX-driven workflow: "Done criteria includes operator-witnessed manual verification OR browser-driven test harness." If neither is feasible, the dispatch should be flagged as "structurally complete; production verification required before shippable."
- **ACCEPTED-with-rationale Codex findings on production-facing surfaces are conditional acceptance, not closure.** Lesson from hyp-recs success-path fix executing-plans triage (2026-04-29). Codex R1 Major 1 ("`exclude_tickers` invariant breadth") was ACCEPTED-with-rationale in implementer's return report and orchestrator triage — rationale: "follow-up tracked; full dashboard render path doesn't pass exclude_tickers but transient at trade-time only." Operator's production verification confirmed it as a LIVE production bug: hard-navigating away and back leaves the just-traded ticker in the hyp-recs panel. **The "ACCEPTED-with-rationale" framing implied closure; in reality the finding was conditionally accepted contingent on operator-paced re-evaluation post-production-verification.** Generalization: orchestrator triage should distinguish two ACCEPTED-with-rationale subclasses — (a) ACCEPTED-CLOSED (genuinely out-of-scope; e.g., test/dev-only surfaces; pre-existing pre-dispatch bugs) and (b) ACCEPTED-CONDITIONAL (production-facing impact; deferred to post-production-verification re-evaluation). Subclass (b) findings should be flagged in dispatch return report AND re-checked at production verification. Add to triage discipline: when a Codex finding affects a production-facing surface, "ACCEPTED-with-rationale" requires explicit decision on whether the surface is operator-verifiable post-dispatch; if yes, the finding stays open until production verification confirms either fix or genuine non-impact; if no, escalate to immediate fix in this dispatch's scope. Pattern complement to "Adversarial review can find real defects in brief-excluded territory" (Tier-2 #2/#3 lesson 2026-04-27) — both lessons are about Codex findings that correctly identify real concerns the dispatch chose not to address; this lesson tightens the orchestrator-side discipline for tracking those findings through to production verification.
- **Helper-internal anchoring of side-effecting boundaries.** Lesson from OHLCV archive consolidation executing-plans (Codex R1 Critical 1, 2026-04-30). The shared helper `read_or_fetch_archive(ticker, *, end_date, ...)` originally anchored its yfinance fetch boundary at the caller-supplied `end_date`. When called with `end_date = today` during market hours, yfinance's `interval=1d` includes the in-progress bar (per CLAUDE.md gotcha) — that intraday partial would have landed in the persisted archive, corrupting it for ALL future readers across ALL consumers. Fix: helper internally anchors fetch boundary at `_last_completed_session_today()`; caller's `end_date` only affects the return slice. **Generalization:** when a helper accepts a caller-supplied "as-of" / "end-date" parameter, that parameter MUST NOT control upstream side-effects (cache writes, archive boundaries, persisted state) — it should ONLY control read-slicing of the helper's return value. Side-effecting boundaries should be helper-internal and anchored at the safest stable value (e.g., last completed session for time-series cache writes; transactional snapshot point for state writes). Pattern complement to "Snapshot-semantic claims at spec/plan time should explicitly address transaction isolation" (chart-scope policy v2 R1 M1, 2026-04-28) — both lessons are about implicit caller-controllable side-effects that escape into shared state. Add to writing-plans phase as a checklist item for any helper accepting a caller-supplied as-of/date parameter: "Does this parameter control any side-effect (cache write, archive boundary, persisted state)? If yes, replace with helper-internal anchoring." This is also a cross-feature failure mode TestClient cannot catch — it surfaces only when the helper is called with `end_date = today` during market hours, which most unit tests don't exercise.
- **External-API empty-result must be treated as transient, not authoritative-empty.** Lesson from OHLCV archive consolidation executing-plans (Codex R2 Major 2, 2026-04-30). The full-refresh path of the OHLCV archive helper would replace existing archive content with empty content if yfinance returned an empty DataFrame. Real production failure mode: yfinance occasionally returns empty for transient reasons (rate limit, network blip, weekend, unknown ticker handling drift) — without the fallback, an established ticker's archive would be blanked, requiring re-fetch on every subsequent call AND losing all historical context until the next successful full-history pull. Fix: full-refresh falls back to existing archive on empty fetch; meta stays stale (so next call retries the refresh). **Generalization:** any write-through cache (or persistent archive) backed by an external API MUST distinguish "API returned empty" from "data is authoritatively empty." For yfinance and similar flaky external APIs, "empty" is almost always transient. Append-or-fall-back pattern: never blank existing cache content from an empty API response; let stale metadata trigger retry on next call. The ONLY case where empty-as-authoritative is correct is when the API has explicit "no data found" semantics distinct from "transient failure" — and even then, prefer leaving stale data + flagging the staleness over blanking. Pattern complement to "Multi-path data ingestion needs full-path audit" (sector capture R2 M1, 2026-04-29) and the existing CLAUDE.md yfinance gotchas. Add to CLAUDE.md gotchas as a write-through-cache discipline note alongside the existing yfinance-API quirks.
- **Plan-text contracts must NOT over-state vs framework's effective behavior.** Lesson from Phase 4 cleanup-remainder executing-plans (Codex R1 Major 1, 2026-04-30). The plan-text framed `today_decisions` site as "pipeline-bound" contract, but the underlying `list_for_session` actually falls back to date-only filter when `eval_id=None`. Pre-migration code preserved this fallback semantically; the plan's strict pipeline-bound contract would have silently NARROWED behavior post-migration. Codex caught the over-strong assertion; resolution preserved the date-only fallback inline with documenting comment. **Generalization:** when a plan's per-site contract classification asserts a contract type (e.g., "pipeline-bound" vs "with-fallback"), the writing-plans phase MUST verify the helper's contract is at least as permissive as the original code's effective contract — including layered fallbacks the original code may rely on. Plan-author's mental model is insufficient; verification must be test-derived OR call-trace-derived. The check is: "if I substitute the helper for the inline query, does the new code preserve behavior on EVERY input the original code accepts?" If no, the plan-text over-states the contract. Add to writing-plans phase as a checklist item for any helper-substitution refactor: "Verify the helper's effective contract against the original code's effective contract via test-or-trace, not against the plan-author's stated contract." Pattern complement to "Multi-path data ingestion needs full-path audit" (2026-04-29) and "Bug-class durability vs scope-of-closure" (2026-04-29) — all three lessons share the underlying discipline that effective-behavior must be empirically verified, not asserted.
- **"Additive" dataclass field changes are NOT zero-cost — hand-constructed test sites break.** Lesson from Phase 4 cleanup-remainder executing-plans Task 1 (2026-04-30). `PipelineRunBinding` was extended with an `action_session_date` field (additive change; existing positional-arg construction patterns continued to work in production code via the helpers). Five test sites that hand-constructed the dataclass broke because they did NOT pass the new kwarg — pytest collection failures surfaced at green-phase verification. Production consumers (which use the constructor via the helpers) were unaffected; only test fixtures hand-constructing the dataclass needed updates. **Generalization:** "additive" dataclass changes are zero-cost only in production code that constructs via factory helpers; they break ANY test fixture that hand-constructs the dataclass via positional or kwargs-explicit syntax. Per-task scope estimates that frame field additions as "additive; no consumer changes" are incomplete — every test fixture that mentions the dataclass-being-extended is a refactor surface. Add to writing-plans phase as a checklist item for any dataclass extension: `grep -rn "DataclassName(" tests/` to enumerate hand-constructed sites BEFORE estimating refactor scope. Mitigation options: (a) provide a default value for the new field (zero-cost for hand-constructed sites that omit it); (b) factor a default-providing factory helper for tests; (c) accept the refactor cost and update each test site explicitly. Phase 4 chose (c) because the new field is semantically required by the new contract.
- **Plan task consolidation is the correct response to test-only tasks that violate per-task TDD.** Lesson from Phase 4.5 hypothesis-label web-form gap writing-plans (Codex R1 Major 4, 2026-04-30). Original 5-task plan partitioned implementation (Task 4: route + soft-warn round-trip with primary tests) and regression-pin tests (Task 5: snapshot-trust + rationale-fail re-render preservation). Codex caught that Task 5 had no RED phase — Task 4's implementation already made Task 5's tests pass at write-time. Resolution: fold Task 5's two tests into Task 4 as additional RED-then-GREEN cycles within the same task; eliminate Task 5. **Distinct from the Phase 1 batched-implementation-then-per-element-validation pattern (chart-pattern flag-v1 R1 M4 ACCEPTED-with-rationale, 2026-04-26):** that pattern applies to tightly-coupled measurement code where per-component red-green-commit produces throwaway scaffolding, AND the plan task text explicitly says "Run to see GREEN (already implemented in [prior task])" so the deviation is documented in advance. For routine CRUD threading like Phase 4.5 (route Form param + EntryRequest pass-through + soft-warn round-trip), per-task TDD is the binding discipline; consolidation is the right resolution, not acceptance. Add to writing-plans phase as a checklist item: "Does any task ship tests-only against another task's already-shipped implementation? If yes, either consolidate into the implementing task OR explicitly document as the Phase 1 batched-implementation pattern with rationale visible in the plan task text." Pattern complement to the Phase 1 lesson — same surface, opposite default: routine work consolidates, tightly-coupled measurement work documents-and-accepts.
- **Fragment-faithful resubmit testing closes a class of soft-warn round-trip vacuousness.** Lesson from Phase 4.5 hypothesis-label web-form gap writing-plans (Codex R1 Major 1, 2026-04-30). Original test design hand-crafted the second POST in soft-warn round-trip tests (`POST /trades/entry` with `force=true` + an explicitly-listed dict of form values the test author wrote). Codex caught that hand-crafted resubmits are vacuous against a class of bugs: if the server's `soft_warn_confirm.html.j2` partial misnames the hidden input (e.g., emits `<input name="hypothesi_label">` due to a typo, or omits the field entirely from the auto-iterated `form_values.items()` loop), the hand-crafted POST still carries the correct field name and the persisted trade row gets the right value — the test passes despite the production bug. **Fix: a `_parse_hidden_inputs(html)` regex helper extracts hidden-input name/value pairs from the actual server response; the second POST replays the parsed dict verbatim.** With the bug present (server response missing/misnamed field), the parsed dict is missing/misnamed; the second POST persists NULL or an unrelated value; the test fails. **Generalization:** any test simulating a multi-step UI workflow where a server-emitted intermediate fragment is the contract surface (soft-warn confirm, multi-step wizard, OOB swap with hand-off form fields, HTMX `hx-include` chains) MUST replay the server's actual response to drive the next step — never hand-craft the next request body from the test author's mental model of what the server emits. The mental-model approach masks emission-side bugs that the contract surface is specifically there to catch. Pattern is the soft-warn-test analog of the Phase 4 sort-coupling-vacuousness lesson and Phase 6 monkeypatch-capture-failure lesson — all are about test setups that elide the actual contract under test. Catalog of vacuousness failure modes now includes: secondary-key re-masking (Phase 4 R1 M1), bound-reference confusion (Phase 4 R2 M1), substring-vs-exact (Phase 3 R1 M2), symmetry-via-deterministic-tiebreaker (Phase 4 R2 M1), monkeypatch-capture failure (Phase 6 internal review), hand-crafted-multi-step-resubmit (Phase 4.5 R1 M1).
- **Subject-only ERE grep convention has cross-phase collision noise; effective contract is unchanged but the regex is no longer a sole-collision detector.** Lesson from Phase 4.5 hypothesis-label web-form gap executing-plans (implementer return report Open Question 1, 2026-04-30). The `git log -E --pretty='%s' --grep='^[a-z]+\([a-z]+\): Task X.Y'` convention assumes task IDs are repo-history-unique. They're not: prior phases under the same `feat(area):` scope reused `Task 3.x` / `Task 4.x` numbering. Phase 4.5 Task 3.1 and Phase 4 Task 3.1 both match the regex. Each Phase 4.5 commit landed cleanly as ONE NEW LINE on top of the prior phase's matches; the implementer manually verified by reading dates/SHAs. The convention's force is preserved — the grep still surfaces duplicate task commits within THIS phase reliably (single-subagent + sequential dispatch + immediate-after-commit verification means a duplicate would appear within seconds of the prior matching commit), and Codex review can read the grep output and distinguish stale (pre-phase) from fresh (this-phase) matches by date or SHA. But the regex is no longer a SOLE-collision detector across the repo's full history. **Mitigation options for future phases:** (a) phase-prefix the convention — `feat(area): Phase N Task X.Y — ...` — adds noise to subjects but eliminates cross-phase collisions; (b) date-anchor the grep — `git log --since=<phase-start-date> -E ...` — preserves the existing convention but scopes to this-phase commits; (c) accept the cross-phase noise — the convention's force is "make duplicate-detection observable"; cross-phase collisions don't undermine that because the implementer reads dates/SHAs to distinguish fresh from stale. **Recommendation:** option (c) for now — the implementer manually distinguishes; ZERO-rogue track record extends to Phase 4.5 (14 phases now). If cross-phase collisions become a recurring observability burden (e.g., the implementer reports it BLOCKING duplicate detection rather than just being noise), upgrade to option (b) since it preserves the existing convention text; option (a) is heavier and only worth adopting if (b) also proves insufficient. **Add to writing-plans phase as advisory note:** when authoring a plan, note that prior phases may have used the same Task numbering; the verification grep will show prior-phase matches as well. The implementer is responsible for distinguishing fresh from stale.
- **Synthetic-fixture coverage gap can mask real-world data shape bugs even with full test-suite passing.** Lesson from TOS-import TRD-as-withdrawal fix (operator-surfaced 2026-04-30 via 4/30 Schwab/TOS export; commit `c9159c7`, 2026-05-01). The bug existed since `extract_cash_movements` was authored: the function blindly accepted any non-`BAL` non-empty TYPE as a cash movement, so trade settlements (TYPE=`TRD`, negative amount = stock purchase) classified as withdrawals and would double-decrement equity if imported. The fast suite was 1380 passing throughout — the existing test fixture (`tests/fixtures/tos/synthetic-tos.csv`) only exercised TYPE codes `DEP` + `WD` and a 7-column header; real Schwab/TOS exports have TYPE codes `BAL` + `TRD` + `CRC` (cash receipt / external transfer-in) + a 9-column header (with `Misc Fees` + `Commissions & Fees` columns). The fixture was structurally insufficient against the production data shape; no test would have caught the bug because no test exercised a TRD row. **Co-located bug also surfaced:** REF parsing (`.strip('"').replace("=", "")`) left a stray leading `"` on Excel-style `="..."` force-as-text values because `=` was at the boundary instead of `"`; reordering to `.strip("=").strip('"')` handles cleanly. The synthetic fixture used unquoted refs (`DEP-001`) so this bug also wasn't exercised. **Generalization:** when fixing a bug surfaced via real-world data, write the discriminating test using the operator's ACTUAL artifact format (real header schema, real TYPE codes, real REF format with quoting), not a simplified synthetic. The discriminating test is "would the existing fixture have caught this bug?" — if no, the fixture is a coverage gap, not just a missing test case. Pattern complement to "Multi-path data ingestion needs full-path audit" (sector capture R2 M1, 2026-04-29) and "External-API empty-result must be treated as transient" (OHLCV archive R2 M2, 2026-04-30) — all three lessons share the underlying discipline that effective-behavior must be empirically verified against real data shapes, not synthetic stand-ins.
- **External research artifacts encode platform-foreign assumptions; map-then-modify, don't direct-adopt.** Lesson from journal v1.2 incorporation analysis (2026-05-01). Operator commissioned external research producing a 1953-line swing-trading-journal spec (`reference/Future Work/Trading Journal/swing_trading_journal_ai_ingestion_v1.2.md`) with 3-revision evolution chain. Direct adoption would have introduced concepts that conflict with our actual platform shape: Setup_Playbook as DB-versioned rows (our setups are encoded in `swing/evaluation/scoring.py` + `criteria.py` — code-versioned via git, not DB-versioned); pre-trade quality score 0-10 self-rated (pipeline already computes A+/watch/skip + criteria pass/fail; self-rating duplicates and conflicts); Screen_Definitions versioning (our `finviz_schema.py` is git-versioned); pyramiding R-views (operator at $7,500 capital with 5 concurrent doesn't pyramid); 7-value discretionary trade_origin enum (our pipeline-driven flow has 4 actual paths). v1.2 assumes a **discretionary trader** independently planning each trade with thesis/why_now/invalidation; OUR platform is a **framework-research-loop** where the pipeline scores candidates A+/watch/skip and operator selects from scored set with hypothesis pre-registration. Both are valid trading-system architectures but the journal-discipline-measurement layer differs substantially. **Generalization:** when consuming external research artifacts (commissioned design specs, AI-generated blueprints, methodology references from `reference/methodology/`), read them as the SOURCE of ideas — then explicitly map each concept against the platform's actual shape and DROP / MODIFY / ADOPT each piece. Don't accept blueprint scope as scope-binding; the blueprint's authors haven't seen our platform's existing infrastructure. **Output of the analysis** belongs in the cross-phase backlog as decomposed sub-bundles with explicit rationale per modification, not as "implement spec verbatim." Pattern complement to V2.1 §VII.F source-of-truth correction protocol — both lessons are about treating external authorities as evidence inputs, not as binding prescriptions.
- **Brief-speculation about consumer code state should be empirically verified at brief-draft time, not inherited from secondary sources.** Lesson from Phase 5 configuration page writing-plans (return report 2026-05-01; commit chain `b637d9a..e8c6396`). Brief asserted "risk_floor is currently a code constant" inheriting the claim from `docs/phase3e-todo.md:770` and `project_capital_risk_floor.md` memory. Empirical audit at §0 Read found `risk_floor` already lives at `cfg.account.risk_equity_floor` in tracked `swing.config.toml:22`, consumed at 3 production sites (`swing/pipeline/runner.py:424`, `:558`, `swing/web/view_models/dashboard.py:496`) — Task 0a (constant promotion) de-scoped to no-op. Two related field-path corrections from the same brief: brief said `web.chart_top_n_watch` (actual: `pipeline.chart_top_n_watch`); brief said top-level `cfg.chase_factor` (actual: `cfg.web.chase_factor`). Brief discipline (verify field paths in §0 Read) caught the false-asserted facts before code work began, but the false-asserted text in the brief itself was orchestrator-side rework. **Generalization:** when an orchestrator brief asserts "the field/constant/cache is currently at X" or "currently does Y," the source of truth is a `grep -rn` audit at brief-draft time, NOT a previous orchestrator note OR a memory file OR a secondary `phase3e-todo` entry. Secondary sources accumulate drift faster than orchestrator inspects them; passing through stale claims creates implementer rework and burns Codex review budget on plan corrections that should have been brief-time corrections. If the brief MUST defer due to context-budget pressure, prefix the assertion with `PER <source>; verify in §0 Read` so the implementer is on notice that the claim is unverified, AND add the verification to the §0 Read explicit list. **Pattern complement to** "Brief drafting drift on tracking state" (post-2c housekeeping, 2026-04-25) — same surface (brief-time assertions about current code/file state); both lessons say verify-empirically beats inherit-from-memory.
- **Adding a NEW base-extending VM inherits ALL existing fields the base layout dereferences — separate audit dimension from the "new field" 5-VM rule.** Lesson from Phase 5 configuration page writing-plans (Task 7 resolution, 2026-05-01). CLAUDE.md gotcha "base.html.j2 is shared — new `vm.foo` field requires adding to EVERY base-layout VM" frames the rule around ADDING a new field that the base layout dereferences (current set: 5 VMs — DashboardVM, PipelineVM, JournalVM, WatchlistVM, PageErrorVM). The complement case — ADDING a NEW base-extending VM (here: ConfigPageVM) — must inherit ALL existing fields the base layout already dereferences (`session_date`, `stale_banner`, `price_source_degraded`, `price_source_degraded_until`, `ohlcv_source_degraded`). Brief framed Task 7 around "5-VM rule applies if base.html.j2 dereferences NEW fields"; implementer correctly identified that the existing-field-inheritance is its own audit dimension. Plan Task 4.0 includes the banner-guard fields with safe defaults on `ConfigPageVM` despite the nav link itself being static. **Generalization:** when introducing a new VM that extends `base.html.j2`, the audit has TWO independent dimensions: (a) does `base.html.j2` dereference new fields (5-VM rule applies — propagate to existing VMs); (b) does the new VM include all existing fields `base.html.j2` already dereferences (with safe defaults — separate audit). Both checks needed independently. **Add to writing-plans phase as checklist item:** "Does this dispatch introduce a new VM that extends `base.html.j2`? If yes, enumerate the existing fields `base.html.j2` dereferences and require the new VM to include them all with safe defaults."
- **Subagent self-collision can extend across multiple intervening tasks; not just tight-window. Worktree isolation is the documented escalation when task-partitioning + observable-verification prove insufficient.** Lesson from Phase 5 configuration page executing-plans first attempt (commit chain `dff70ca..2278e97`, halted; force-reset to `e42f5be` 2026-05-01). Five commits landed; the fifth (`2278e97`, 23:07) advertised `Task 1.2` subject but actually rewrote `swing/config_overrides.py` (overwriting the plan-aligned API from `0b85046` at 22:58 with a non-plan API — `get_field_source` returning `"user"` instead of `"override"`, raising `KeyError` instead of `ValueError`, hardcoded `code_only` heuristic) AND did Task 2.1 route wiring under the wrong subject AND created a non-plan test file `tests/config_overrides/test_config_overrides.py` using a `Path.cwd()` mkdtemp fixture pattern (yielding 66 ACL-locked `.config-overrides-*` directories at repo root). Time gap from `0b85046` to `2278e97`: 9:47 minutes, with intervening Task 2.0 commit at 23:03 — NOT the tight-window collision shape from chart-pattern Phase 2 (42-82 seconds). Same fundamental class — another subagent in the same `subagent-driven-development` chain overwrote landed work — but with extended time window because the colliding subagent was dispatched for a downstream task with intervening other-task commits in between. **Generalization:** the `subagent-driven-development` task-partitioning + observable-verification baseline is a probabilistic mitigation, not a hard guarantee; the 14-phase ZERO-rogue track record was real evidence but not permanent immunity. The documented escalation per chart-pattern Phase 2 lesson — **worktree isolation** (`superpowers:using-git-worktrees`) — is the hard-guarantee fallback. **Operator decision 2026-05-01:** escalate to worktree isolation for the Phase 5 re-dispatch; brief revision committed at the same time. **Add to executing-plans dispatch briefs as standing requirement:** worktree isolation is the default for any plan with >5 task commits OR with intermediate state mutations to shared files (config layer, schema, base-layout VMs); single-tree dispatch is now the exception requiring justification, not the default.
- **Don't infer "external interference" from circumstantial machine-state evidence without operator confirmation.** Lesson from Phase 5 configuration page executing-plans halt triage (orchestrator analysis 2026-05-01). On receiving the implementer's halt-report, orchestrator hypothesized "external parallel writer" based on (a) `.codex-pytest-*` directory naming pattern, (b) 9:47-minute gap between duplicate `Task 1.2` commits, (c) divergent test fixture pattern using `Path.cwd()` mkdtemp. Operator pushed back: no other dispatch was running on the machine; no other process had write access. The actual cause was extended-time-window subagent self-collision within the same dispatch chain (lesson above). **Why the diagnostic error mattered:** the "external parallel writer" hypothesis pointed to recommending an investigation step (Option D — find the parallel writer + quiesce it) that was moot. The correct diagnosis (subagent self-collision) immediately points to the documented worktree-isolation escalation. **Generalization:** the orchestrator has indirect evidence (file artifacts, commit metadata, system-reminder text); the operator has direct evidence (what they actually dispatched, what other processes are running). When a triage hypothesis depends on machine-state assertions about parallel processes / sessions / autonomous agents, the orchestrator MUST surface those assertions for operator confirmation BEFORE recommending action — not present them as conclusions. **Pattern complement to** "Brief drafting drift on tracking state" (post-2c housekeeping, 2026-04-25) AND "Brief-speculation about consumer code state" (Phase 5 writing-plans, 2026-05-01) — same anti-pattern surface (orchestrator inferring state from secondary evidence rather than verifying empirically); applied here to incident triage rather than brief drafting.
- **Off-spec Codex per-task invocation by subagents during executing-plans dispatch is a real failure mode; mitigation is a global PreToolUse hook gated by a marker file.** Lesson from Phase 5 configuration page executing-plans first-attempt copowers source review (2026-05-01). `copowers:executing-plans` SKILL.md Step 5 documents Codex/`adversarial-critic` as ONLY invoked at end-of-dispatch. `superpowers:subagent-driven-development` SKILL.md doesn't mention Codex anywhere. But the first attempt's `.codex-pytest-6681924b-...` directory at 22:56 (BEFORE Task 1.2 commit at 22:58) is evidence Codex was running mid-execution under its sandbox user (`AughtSevernIII\CodexSandboxOffline`) — most likely cause: a subagent independently invoked `Skill(copowers:adversarial-critic)`, `Skill(copowers:review)`, or `mcp__plugin_copowers_codex__codex` directly, off the documented flow. Combined with subagent self-collision, this AMPLIFIES the rogue-commit failure mode: a subagent that gets a Codex `ISSUES_FOUND` verdict per-task can be convinced to "fix" issues by overwriting landed work. **Mitigation (now in place globally):** PreToolUse hook at `~/.claude/hooks/block-copowers-during-subagent.sh`, registered in `~/.claude/settings.json`. Blocks the four copowers/Codex tool invocations when `.copowers-subagent-active` marker file is present at project root. Workflow: orchestrator creates marker before subagent dispatch (`touch .copowers-subagent-active`); removes marker before invoking adversarial-critic at end (`rm .copowers-subagent-active`). Hook is harness-level — cannot be bypassed by subagent reasoning. **Apply pattern to ALL future executing-plans dispatches:** invoke `superpowers:subagent-driven-development` and `copowers:adversarial-critic` separately (NOT via the `copowers:executing-plans` wrapper, which bundles both phases without marker-file management between them); manage the marker file between the two invocations. **Generalization:** the copowers wrapper skills bundle multiple phases for convenience; when a phase-boundary requires a coordination action (here: marker-file management), the orchestrator must invoke the components directly rather than the wrapper. Future copowers wrappers should consider exposing the phase-boundary as a hook point.
- **Orchestrator briefs MUST NOT exclude documented skill requirements without explicit operator authorization + rationale.** Lesson from Phase 5 configuration page executing-plans first-attempt incident triage (2026-05-01). The original Phase 5 executing-plans brief (`da86730`, now revised) explicitly listed "Worktree isolation" as OUT-OF-SCOPE with rationale "single-subagent dispatch baseline (14-phase ZERO-rogue track record). Escalate ONLY if a novel collision mode emerges." But `superpowers:subagent-driven-development` SKILL.md line 268-269 documents: `**Required workflow skills:** **superpowers:using-git-worktrees** - REQUIRED: Set up isolated workspace before starting`. So the brief positively excluded what the skill required. The 14-phase ZERO-rogue track record was real evidence that prior dispatches without worktrees worked in practice, but the skill's documented requirement was a stronger constraint than the empirical track record. **Generalization:** when authoring an orchestrator brief, READ the actual `SKILL.md` of every skill the brief invokes; don't exclude documented requirements without explicit operator authorization + rationale that overrides the skill's own contract. Empirical track record can support relaxing requirements, but doing so is a documented decision requiring operator awareness — not a default. **Add to brief-drafting checklist:** for every `superpowers:*` or `copowers:*` skill the brief invokes, read its SKILL.md and enumerate its `Required workflow skills` / required setup steps; if any are excluded, surface the exclusion + rationale to operator before finalizing. (Migrated from active orchestrator-context.md 2026-05-06 per retention discipline; superseded as oldest active lesson when Finviz-API subprocess-cfg-propagation lesson landed.)
- **Documented-but-unimplemented Sub-X→Sub-Y handoff items from prior return reports require explicit dispatch attention as binding done-criteria, not informational notes.** Lesson from Phase 7 Sub-C operator-gate hotfix `5e9981c` (2026-05-05). Sub-B's return report explicitly flagged: "complete_trade_review wiring at swing/web/routes/trades.py:1169 (currently calls update_trade_review_fields directly inside its own with conn:; switch to the B.6 service for atomic state transition)" as a Sub-C T1 territory item. Sub-C didn't make the switch. Result: review-completed trades stayed in `state='closed'` with reviewed_at populated (state machine internally inconsistent; terminal `reviewed` state unreachable on the web review path). Operator-witnessed gate caught it; hotfix landed at integration-layer. **Generalization:** when a sub-dispatch's return report enumerates handoff items for a successor sub-dispatch (commonly: "Sub-Y T1 unblocks this; Sub-Y owns the migration"), the successor's brief MUST elevate those items to binding done-criteria, NOT keep them as informational notes. Add to executing-plans dispatch briefs §6 "Done criteria": "All handoff items enumerated in the predecessor sub-dispatch's return report are addressed (with explicit verification check per item)." Adversarial review checklist: per-item discriminating test asserting the handoff is implemented (e.g., for Sub-C T1 the test would have asserted `state == 'reviewed'` post-review-POST; that test was missing). (Migrated from active orchestrator-context.md 2026-05-06 per retention discipline; superseded as oldest active lesson when Finviz-API code-review-I1 lesson landed.)

> **Lessons below archived 2026-05-07 in post-Phase-8-ship retention rotation batch (18 entries; Phase 6 + Phase 7 era).** Active orchestrator-context.md count was 48 against ~30 cap; rotation brought active back to 30. Phase 8 era lessons (introduced 2026-05-07) retained in active for ~one-phase cooldown.

- **HX-Redirect target route must be verified to exist; TestClient verifies the header but doesn't follow the redirect.** Lesson from Phase 6 operator-witnessed verification gate (operator-witnessed defect at S5, fixed in worktree as code-review I3 2026-05-04). Phase 6 review POST returned `HX-Redirect: /trades` per plan but no `GET /trades` route was registered (route table for `swing/web/routes/trades.py` has `/trades/entry/form`, `/trades/{id}/exit`, etc., but no bare `/trades` listing). HX-Redirect mechanic worked correctly (header propagated, browser navigated to `/trades`); destination 404'd. TestClient verified status code (204) + header value (`/trades`) but didn't follow the redirect to verify target resolves. Operator-witnessed gate caught it; fixed inline by changing target to `/reviews/pending` (workflow-aligned: see what's next to review). Pattern complement to Phase 5 R1 Major 2 (browser swallows 303 to swap-target) — same browser-only failure-surface family. **Generalization:** future tests for HX-Redirect routes should either (a) assert target route is registered in app's route table (programmatic verification: `assert any(r.path == target for r in app.routes)`), OR (b) follow the redirect with a second TestClient call and assert 200. Add to writing-plans phase as checklist item for any HX-Redirect-emitting handler: "Does the redirect target route resolve?" Operator-witnessed browser verification gate is BINDING for HTMX work; this lesson is the third such failure surface to surface in a row (Phase 5 R1 M1 HX-Request header; Phase 5 R1 M2 HX-Redirect-vs-303-swap; Phase 6 operator-witnessed I3 HX-Redirect-target-unrouted).
- **Default discipline: no main-side commits while an executing-plans dispatch is in flight, even when worktree isolation makes merge mechanics safe.** Lesson from Phase 6 dispatch-in-flight orchestrator behavior 2026-05-04. While Phase 6 executing-plans dispatch was running on worktree branch `phase6-post-trade-review` (base `a7c4bda`), orchestrator added Finviz + Schwab API integration backlog entries to `docs/phase3e-todo.md` + committed to main (`197f99d`). Verified file-disjoint with dispatch's worktree scope (docs-only on main vs `swing/` + `tests/` on worktree); zero merge collision (final `git merge --no-ff` clean). **However, the discipline justification stands:** batching orchestrator edits until dispatch return reduces reviewer cognitive load (one delta to triage, not interleaved deltas) AND pre-empts subtle failure modes if a future edit DOES touch dispatch-scope files (e.g., orchestrator-context.md updates that subagents might read mid-dispatch and act on). **Generalization:** the standing default is **hold orchestrator-side edits until dispatch return**, exception only for verified-non-overlapping pure-docs edits with operator awareness. Backlog additions, lessons captures, and most documentation polish are NOT urgent and can wait. Operator surfaced the discipline observation 2026-05-04 ("low impact of collision" but worth noting); applies prospectively.
- **State-bearing entity validation must distinguish operation enforcement from data invariant.** Lesson from Phase 7 brainstorm (Codex R1 Critical 1, 2026-05-04). Required-fields-by-state read naively as a data invariant ("all rows in state X must have all required fields") contradicts legacy migration: legacy rows in any state lack new pre-trade fields. Spec resolution (§3.5.1): operation-contextual validation — `entry_create` enforces full required set; `transition_managing/partial_exited/closed` triggers suffice; `transition_reviewed` enforces Phase 6 review-completion fields only; legacy rows exempt by NULLABLE schema. **Generalization:** for any state-machine spec, separate "validation that fires at write time on user-input operations" from "invariant claims about persisted data." The two have different semantics; conflation produces specs that contradict legacy migration. Pattern complement to Phase 6 lesson "state-bearing entities require enumeration of ALL state-transition UI surfaces" — both about state-machine completeness, different facets.
- **Status→state predicate rewrite is not uniform substitution.** Lesson from Phase 7 brainstorm (Codex R1 Major 1, 2026-05-04). Replacing `trades.status` with the new state column doesn't translate uniformly across call-sites. Different operation purposes need different rewrites: review-precondition uses `state == 'closed'` only; active-trade-filter uses `state IN ('entered','managing','partial_exited')`; closed-or-reviewed-aggregator uses `state IN ('closed','reviewed')`; write-paths use a fourth predicate. Naive regex-replace produces logic bugs (e.g., letting reviewed trades through review again). Spec §5.2 enumerated 4 operation-specific predicate categories. **Generalization:** when migrating a single boolean/enum column to a richer state space, audit each call-site for SEMANTIC purpose; predicate REWRITE is per-purpose, not per-occurrence. Add to writing-plans-phase checklist for any state-bearing column refactor: enumerate every call-site; classify by semantic purpose; specify per-purpose rewrite.
- **Operational discipline must be operationalized in code, not just stated in spec text.** Lesson from Phase 7 brainstorm (Codex R2 Major 1, 2026-05-04). Spec stated "backup-then-migrate" as a precedent but didn't specify HOW backup is created during migration. SQL migration files cannot create backups (executing against the open connection, can't pause to copy); the natural home is the migration runner Python wrapper. Spec resolution (§12.1): runner-discipline section, SQLite-native methods ONLY (`Connection.backup()` / `VACUUM INTO`), 4 binding integrity checks (PRAGMA integrity_check; expected-table set; row-count parity for migrated tables; size advisory). Pattern complement to existing archived lesson on WAL-mode `Connection.backup()` API — that lesson identifies the right TOOL; this lesson ensures the tool actually RUNS. **Generalization:** spec preconditions of the form "do X before Y" need to specify which code path executes X. Discipline-as-text is theater; discipline-as-code-in-the-runner is binding.
- **Plan-authoring is its own bug surface beyond spec design; cross-section consistency + per-line enumeration are the high-leverage adversarial checks.** Lesson from Phase 7 writing-plans (4 Codex rounds → NO_NEW_CRITICAL_MAJOR; 13 substantive issues across R1+R2+R3, 2 advisory minors at R4, 2026-05-04). Spec landed at NO_NEW_CRITICAL_MAJOR after 3 design-rounds; plan still surfaced 13 substantive issues. Two distinct failure modes drove the round count: (a) **cross-section consistency drift** — multiple R1+R2 majors were "plan §X says A, plan §Y says B" (HX-Redirect target route inconsistent across two route definitions; integrity-check count 2 vs 4; OPERATION_REQUIRED_FIELDS subset vs full set). Spec-only review can't catch these because they're internal-to-plan. (b) **Per-line "verify at task time" looseness** — R1 + R2 + R3 each surfaced new vague entries in the §2.1 status→state predicate-rewrite mapping; the discipline of "no line says verify at task time" required 3 rounds to reach. **Generalization:** writing-plans phase Codex rounds aren't redundant with brainstorm Codex rounds; they catch a different bug class (plan-authoring discipline, not design correctness). Budget 4-7 rounds for substantive plan dispatches; expect cross-section consistency drift + per-line looseness as the dominant failure modes.
- **"Illustrative" placeholders in discriminating-test plan tasks are vacuous tests in disguise.** Lesson from Phase 7 writing-plans (Codex R3 Major 3, 2026-05-04). A plan task labeled its discriminating test as "illustrative" with a `# TODO: pick real field` comment — the test's literal value would not actually flip RED → GREEN with the implementation, just the labels suggested otherwise. Codex caught it; fix replaced placeholder with a real field + real failure mode. **Generalization:** the discriminating-test gate (per `feedback_regression_test_arithmetic.md`) has its own meta-discipline: the CONTENT of the test must specify a real field with a real failure mode. Hand-wave labels ("illustrative", "see task body for actual field", "TODO discriminator") don't satisfy the gate. Plan tasks proposing discriminating tests must specify the EXACT field + EXACT pre-fix expected value + EXACT post-fix expected value at plan-authoring time. Pattern extends archived chart-pattern flag-v1 Phase 4 lesson "Compounding-confound test fixtures can pass despite a vacuous primary discriminator" — same family, plan-time variant.
- **Binding green gate at sub-dispatch boundary is impossible if cross-cutting predicate rewrites are deferred to later sub-dispatches.** Lesson from Phase 7 Sub-A executing-plans (implementer-surfaced 2026-05-04; orchestrator-confirmed structural plan bug; option (a) chained-branch posture adopted). Plan §4.0.1 said "T6 binding green gate" while plan §3 carve-out assigned predicate rewrites for journal/cli/web/services to Sub-B/Sub-C. Mutually contradictory — full suite cannot be green while production code in dropped-API consumer files (Sub-B/Sub-C territory) still references the changed APIs. Implementer's pragmatic shim approach (Exit class stub + insert_exit_with_event stub + _ExitLikeRow NamedTuple) extended green-coverage by 247 tests but couldn't fully restore the suite (217 failed + 20 errors remained). **Operator decision 2026-05-04: chained-branch posture** — Sub-A NOT merged to main; Sub-B + Sub-C dispatch from chained worktree branches based on Sub-A's HEAD (`phase7-sub-b-services` from `phase7-sub-a-schema`; `phase7-sub-c-web` from `phase7-sub-b-services`); integration `git merge --no-ff phase7-sub-c-web` to main only after Sub-C ships full suite green. Production DB stays at the prior schema version throughout (no migration triggered until merge); operator's trade workflows continue working unaffected. **Generalization for future plans with sub-dispatch decomposition:** writing-plans dispatch must check explicitly for binding-green-gate-vs-cross-cutting-rewrite contradiction. Resolution defaults: (a) chained-branch posture if the cross-cutting work is large + operator workflows depend on the touched APIs; (b) extend boundary-restoring sub-dispatch's scope if the cross-cutting work is small. Add as writing-plans §5 adversarial-review watch item: "if any sub-dispatch claims a binding green gate, verify the carve-out doesn't push gate-blocking rewrites to a later sub-dispatch." Pattern complement to Phase 6 lesson "state-bearing entities require enumeration of ALL state-transition UI surfaces" — both about completeness checks at plan-authoring time.
- **SQLite `executescript()` leaves transactions open on partial failure — needs explicit try/except + rollback at the migration runner.** Lesson from Phase 7 Sub-A (Codex R1 Major 3, fixed in commit `78c7005`, 2026-05-04). Pre-existing latent bug: `_apply_migration()` called `conn.executescript(sql) ; conn.commit()` without try/except; on partial failure (e.g., CREATE+INSERT succeeds, then nonexistent-table reference fails), the transaction stays open + the failed-state DB is never rolled back. Phase 7's large transactional migration 0014 surfaced it (smaller migrations had no failure modes that exercised this path). Fix: wrap `executescript()`+`commit()` in try/except; on exception, `conn.rollback()` then re-raise. Discriminating regression test: malformed migration with deliberate fail-mid-sequence; assert probe table doesn't exist post-failure AND `conn.in_transaction == False`. **Generalization:** any future migration runner work (Phases 8+9 schema bumps; Schwab API integration if it adds tables; chart-pattern flag-v2 if pattern catalog expands) inherits this rollback discipline. Pattern complement to existing CLAUDE.md gotcha on `Connection.backup()` API for WAL-mode SQLite — both are SQLite hygiene at the migration-runner layer.
- **Backup-gate condition needs to fire only when there's existing data to back up.** Lesson from Phase 7 Sub-A (code-review I1, fixed in commit `2e41e6b`, 2026-05-04). Initial Sub-A backup-gate fired on `current<14 AND target>=14`, which fires on fresh DBs (current=0) and mid-walk states (current=12 walking to 14) — broke production fresh-install + cascaded RED across 977 test occurrences. Tightened to `current_version == 13 AND target >= 14` with 3 discriminating regression tests (fresh-DB no-backup; mid-walk no-backup; v13→v14 backup-fires). **Generalization:** any future backup-gated migration must fire only when `current_version == (target - 1)` AND target is the gated version; never when bootstrapping or mid-walk. Applies to future schema-bump migrations in Phases 8+9 (each will add a new backup gate at its own version-bump boundary). Pattern complement to "Operational discipline operationalized in code, not stated in spec text" lesson — backup discipline IS code, AND that code needs the right firing condition or it cascades to false-positive failures.
- **Datetime-column impedance mismatch: chronology field vs creation-timestamp field require explicit audit.** Lesson from Phase 7 Sub-B (Codex R1 M1, R4 M1, R5 M1 — 4 of 6 Codex rounds were datetime-canonicalization-driven, 2026-05-04). Tests/code paths assumed `event_ts` (command time, e.g., `datetime.now()` at CLI invocation) == `entry_date`/`exit_date` (trade chronology, operator-passed). They're independent: CLI sets `event_ts=now`; operator passes `entry_date`/`exit_date` as the trade's actual chronology (often back-recorded). Storing `event_ts` as the canonical chronology field breaks back-recorded entries/exits AND any downstream substr-based date matching (tos_import reconciliation; journal aggregation by date). Fixed via `_normalize_trade_event_date_to_iso` helper that takes operator-passed date AND wall-clock time AND combines them as `<entry_date>T<HH:MM:SS>` ISO format. **Generalization:** when a service has BOTH a chronology field (operator-meaningful date) AND a creation-timestamp field (system wall-clock), audit which one feeds each persisted column. Chronology fields go to date-keyed columns (`entry_date`, `exit_date`, `fill_datetime` for ordering); creation-timestamps go to audit-trail columns (`trade_events.ts`). Pattern applies to Phase 8 Daily_Management (snapshot date vs snapshot creation time), Phase 9 Reconciliation_Run (run date vs reconcile-execution time), Schwab API integration (broker fill datetime vs system-receipt datetime). Add as writing-plans §5 adversarial-review watch item: "any service introducing a date-keyed column must specify which input field feeds it (chronology vs creation-timestamp)."
- **Lexicographic ordering on text-stored datetimes is a contract requiring naive-only inputs.** Lesson from Phase 7 Sub-B (Codex R3 M1, fixed in commit `9edee54`, 2026-05-04). SQLite TEXT column `fills.fill_datetime` is sorted lexicographically by `ORDER BY`. A tz-aware ISO string (`"2026-05-03T13:45:30+05:00"`) sorts AFTER a chronologically-later naive string (`"2026-05-03T09:00:00"`) because `+` sorts before all digits in ASCII — but the tz-aware time (13:45 EAT = 08:45 UTC) is actually EARLIER than the naive 09:00. Service-layer validators must enforce naive-only inputs OR canonicalize to UTC-naive at persistence time. Sub-B chose validator-rejection: `_normalize_trade_event_date_to_iso` rejects `tzinfo`-aware datetimes with a clear error. **Generalization:** anywhere SQLite `ORDER BY` runs over text-stored datetimes (audit `trade_events.ts`, `pipeline_runs.started_ts`, `evaluation_runs.run_ts`, `fills.fill_datetime`, `weather_runs.run_ts`, `pipeline_pattern_classifications.classified_at`), the persistence layer needs explicit policy: (a) naive-only inputs (validator rejects tz-aware) OR (b) canonicalize to UTC-naive at write. Add as writing-plans §5 adversarial-review watch item: "any new TEXT datetime column with `ORDER BY` consumers must specify naive-vs-canonicalized policy + the validator that enforces it." Pattern complement to existing CLAUDE.md gotcha "yfinance interval='1d' includes in-progress bar" — both about datetime-as-data hazards.
- **Codex round count is the wrong heuristic for adversarial-review thrash; chain-convergence shape is the right one.** Lesson from Phase 7 Sub-B (6 Codex rounds vs brief's 2-4 expected band, 2026-05-04). Brief said "expect 2-4 rounds"; actual was 6. Each round caught a real follow-on issue triggered by the prior round's fix: R3 from R2's introduction of dual-form date parsing; R4 from R1's chronology-fix-on-only-one-side; R5 from R4's helper-sharing that widened entry-side validation surface. R6 confirmed clean. **This is discriminating-review discipline working as designed, not adversarial thrash.** Implementer's return report documented the chain → orchestrator triage could audit convergence rather than the bare round count. **Generalization:** when a dispatch's adversarial review exceeds the brief's expected round band, look at the FINDING-CHAIN shape: each round catches an issue, prior fix introduces the next round's surface (convergence) vs each round catches unrelated issues that should have been caught earlier (thrash). Convergence is healthy; thrash signals plan/spec gaps. Brief's expected round band should be widened for tasks with cross-cutting validation surfaces (Phase 7 Sub-B touched 5 service entry points sharing datetime canonicalization). Pattern complement to existing chart-pattern flag-v1 lesson "5-round Codex loop shows compound ROI even on small (<500-line) test-only diffs" (archived) — both about Codex round-count expectations being context-dependent, not universal.
- **Schema migration table-rebuilds must enumerate + preserve EXISTING CHECK + FK constraints, not just ADD new ones.** Lesson from Phase 7 Sub-C (Codex R1 Major 1, fixed at route layer in commits `117dc97` + `2b9d6f3`, 2026-05-05). Sub-A migration 0014 rebuilt the `trades` table to drop `status` + add ~28 new columns. The CREATE-COPY-DROP-RENAME rebuild pattern silently lost existing constraints on `chart_pattern_algo` (a Phase 6-era CHECK enum + FK to `pipeline_pattern_classifications`); forged snapshots could persist post-Phase-7. Sub-A's writing-plans review didn't catch this; it manifested 3 layers downstream during Sub-C's adversarial review. Fix landed at route layer (Sub-C territory) with enum + FK existence + cache-content match check — defense-in-depth without re-touching Sub-A's locked migration SQL. **Generalization:** any future schema migration that uses table-rebuild (CREATE-COPY-DROP-RENAME) MUST enumerate every existing CHECK + FK constraint on the rebuilt table + explicitly carry them forward in the new schema OR explicitly justify the drop. Phase 8/9 schema bumps + any Schwab-API-driven schema work inherits this discipline. Add as writing-plans §5 adversarial-review watch item: "for any table-rebuild migration, list every CHECK + FK on the original table + per-constraint disposition (carry-forward / drop-with-justification) in plan." Pattern complement to existing archived lesson "Schema-layer guarantees beat repo-layer guarantees on every dimension except backward-compat migration cost" — that lesson is about adding NEW schema-layer constraints; this lesson is about preserving EXISTING ones during rebuilds.
- **Plan implementation-sketches vs binding requirements need explicit distinction at plan-authoring time.** Lesson from Phase 7 Sub-C (C.3 deviation 2026-05-05). Plan §6 C.3 sketched success-path response as "204 + HX-Redirect to registered route"; existing operator UX is "stay on watchlist/dashboard with OOB updates" via 200 + dashboard chunks. Switching would have broken ~20 existing entry-post tests AND regressed UX. Implementer correctly treated plan sketch as illustrative (kept existing OOB pattern) and surfaced the deviation transparently in return report. **Generalization for plan-authoring:** plan tasks should explicitly distinguish "binding requirement" (must implement this exact pattern) from "implementation sketch" (illustrative; implementer chooses the right pattern given the actual codebase). Plan-authoring lesson family: this complements existing Phase 7 brainstorm lesson on "binding green gate at sub-dispatch boundary infeasible if cross-cutting predicate rewrites deferred" — both about plan-authoring rigor where the writer's intent isn't the implementation contract. Add to writing-plans §5 watch items: "for each non-trivial implementation pattern in the plan, mark BINDING vs ILLUSTRATIVE." Related: Sub-C also surfaced plan §6 C.3's catalyst vocabulary listed stale values vs the operator-confirmed migration 0014 CHECK enum (different from §1.1 — 9 values matched migration but a different section had 9 different values); implementer used migration as ground truth. Future plan-vocabulary tables should be cross-checked against migration CHECK enums during writing-plans review.
- **Broader-grep refresh runs BEFORE the shim-deletion task, not just during.** Lesson from Phase 7 Sub-C (C.14 audit found `research/parity/fetcher.py` consumer missed by C.10/C.11/C.12 enumeration, 2026-05-05). The dispatch brief §3 + plan §3 enumerated shim consumer files; C.14 final-deletion audit caught one missed file. Mechanical fix (added migration to C.13c bundle) but the surprise added a Codex round. **Generalization:** for any sub-dispatch ending in a deletion task (deprecated-API removal; legacy-shim cleanup; obsolete-table drop), run a project-wide grep refresh BEFORE the deletion task starts, not just at audit time. Refresh catches plan-time-missed call sites + makes the deletion-prerequisite migration list complete before the audit pass. Pattern complement to existing archived chart-pattern flag-v1 Phase 6 lesson "Subsequent-phase tests can surface earlier-phase contract bugs" — both about cross-phase dependencies that need explicit boundary management. Add to executing-plans dispatch briefs that include deletion tasks: "prior task MUST run broader-grep refresh + extend prior-task scope to cover any newly-found consumers."
- **Test fixture connection state must mirror production runtime PRAGMA state — `foreign_keys` defaults to OFF on fresh sqlite3 connections but production sets ON.** Lesson from Phase 7 operator-witnessed verification gate 2026-05-05 (catastrophic migration data loss + recovery; hotfix `283d4fa` on Sub-C worktree before integration merge). Sub-A's migration 0014 step 10 (CREATE-COPY-DROP-RENAME on trades for the table-rebuild) `DROP TABLE trades` triggered ON DELETE CASCADE on `fills.trade_id` AND `trade_events.trade_id` because production runs `foreign_keys=ON` (set explicitly by `db.ensure_schema`). Result: just-populated 5 fills + pre-existing 11 trade_events were wiped during the rebuild; trades + aggregate columns survived (rebuilt-via-COPY pattern), creating a phantom-aggregate state where `current_size=39` etc. had no underlying fills. Sub-A T9 + T10 tests passed because their fixture connections used sqlite3's default `foreign_keys=OFF`; CASCADE didn't fire in tests; the production-vs-test PRAGMA divergence wasn't caught. **Generalization:** every migration-touching test must explicitly set production-equivalent PRAGMA state on its fixture connection (`conn.execute("PRAGMA foreign_keys=ON")`) before exercising the migration path. Add to writing-plans §5 watch items: "for any schema migration test, verify fixture PRAGMA state matches production (foreign_keys=ON; journal_mode=WAL where applicable)." Pattern complement to existing CLAUDE.md gotcha on WAL-mode SQLite + `Connection.backup()` — both about test-vs-production SQLite-state divergence as a discriminating-test failure mode.
- **Schema-rebuild migrations need foreign_keys=OFF discipline at the runner level, not in each migration SQL.** Lesson from Phase 7 hotfix `283d4fa` (companion to test-fixture lesson above, 2026-05-05). Per SQLite docs §11.2, table-rebuild migrations (CREATE-COPY-DROP-RENAME) should disable foreign_keys for the duration to prevent CASCADE-on-DROP from wiping referenced tables. PRAGMA inside BEGIN TRANSACTION is a no-op (SQLite ignores PRAGMA foreign_keys inside active transactions); the discipline must be at the runner level OUTSIDE the transaction. Hotfix patched `_apply_migration` in `swing/data/db.py` to save current foreign_keys value, set OFF before `executescript`, restore prior value after (in a `finally:` block). All current + future migrations through the runner inherit the discipline automatically. **Generalization:** any future migration runner work (Phase 8/9 schema bumps; Schwab API integration if it adds tables; chart-pattern flag-v2 if pattern catalog expands) inherits this protection. Pattern complement to existing CLAUDE.md gotcha on Connection.backup() + Phase 7 brainstorm "Operational discipline must be operationalized in code, not stated in spec text" lessons — same SQLite-runner hygiene family.
