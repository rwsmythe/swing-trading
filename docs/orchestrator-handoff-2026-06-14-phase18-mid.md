# Orchestrator Handoff — 2026-06-14 (Phase 18 mid; 18-A/B/C CLOSED; sub-agent dispatch model ADOPTED)

**Audience:** a fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). **Read [`docs/orchestrator-context.md`](orchestrator-context.md) end-to-end first** (the durable role framing) — then this doc (the delta since the 2026-06-13 Phase-17-close handoff). Bootstrap via `scripts/orchestrator_bootstrap.md`.

## 1. Clean boundary (verify: `git log --oneline -8` + `git status -sb` + `git worktree list`)
- main HEAD `52ddd64b` (the operator commits docs/research/comms to main in parallel — expect drift; **stage explicit pathspecs, commit promptly**, charter §5.8). Origin in sync; **single `main` worktree** (all 18-A/B/C worktrees merged ff + removed).
- **~8258 fast tests green** on `main` (re-run on the merged head at the 18-C close `2b381461`; docs-only commits since). **Schema v30** (18-C migration 0030). Ruff clean. **ZERO `Co-Authored-By`** holds.
- Divergence discipline: rebase the branch onto main → `merge --ff-only` → re-run the suite ON the merged head (never carry a branch count forward).

## 2. >>> THE BIG NEW THING: the sub-agent dispatch model is ADOPTED <<<
Dispatch is no longer operator-paste-into-a-new-CC-window. **The orchestrator spawns the implementer as a sub-agent via the Agent tool**, on the operator's "go". Full adopted procedure: **orchestrator-context.md §"Dispatch execution — orchestrator-spawned implementer sub-agents"** + the SPOF **[`docs/implementer-dispatch-recipe.md`](implementer-dispatch-recipe.md)** + the cells **`.claude/agents/implementer-<model>-<effort>.md`** (5: opus-max/xhigh/high, sonnet-high/med) + the design/conditions [`docs/orchestrator-subagent-dispatch-automation-proposal.md`](orchestrator-subagent-dispatch-automation-proposal.md) + [`docs/orchestrator-subagent-dispatch-charc-architecture-pass.md`](orchestrator-subagent-dispatch-charc-architecture-pass.md). In one line:
- **Flow:** operator commissions + "go" → orchestrator SELECTS the cell per the rubric (writing-plans ~`opus-xhigh`, executing ~`opus-high`; by TASK not phase) + **announces it (vetoable) before spawn** (C-e) → `Agent(subagent_type="implementer-…", run_in_background=true)` (NO `model` override — the cell carries model+effort; NO `isolation` — the sub-agent makes its own `.worktrees/<name>`, C-b) → the sub-agent works, hand-runs Codex to convergence, returns to the orchestrator → **orchestrator QA-against-disk, verifying convergence from the REAL `.copowers-findings.md`** (C-c) → posts the QA'd return → the gate → operator authorizes → orchestrator rebase + `merge --ff-only` + suite-on-merged-head.
- **You (orchestrator) own failure handling (C-d):** Agent-returns-`null` → re-dispatch once then escalate; WSL-Codex unreachable → take over; stall → check the task output, recover/escalate.
- **Proven end-to-end this generation:** 18-B writing-plans (the pilot) + 18-B executing + 18-C full round (writing-plans `opus-xhigh` 17-round-converged + executing `opus-high`). The cells live-pickup at a turn-boundary rescan (a fresh session has them loaded). Effort is NOT runtime-observable — verification is config + the smoke spawn, not an effort readout.
- **Re-ground brief anchors at branch start; the implementer reads the recipe via its cell.** The brief MUST be committed to main before dispatch (the sub-agent's worktree branches from main). For an executing dispatch off a writing-plans branch, REBASE that branch onto current main first so it carries the recipe + the latest brief + merged prior arcs (then pass `BASELINE_SHA` = the post-rebase plan-branch HEAD).

## 3. Phase 18 (data-collection integrity) — 18-A/B/C CLOSED; D/E/F + riders queued
Live tracker: [`docs/phase18-todo.md`](phase18-todo.md). **CLOSED:** **18-A** temporal-log NaN writer fix + the shared `swing/data/ohlcv_finiteness.py` predicate (`c45d8752`). **18-B** OHLC write-path audit — closed the Schwab-ladder ingest GAP, killing the #24-#26 two-path-divergence class (`5cce72db`). **18-C** yfinance call audit — `yfinance_calls` table, **schema v29→v30** (migration 0030), operator-witnessed live-migration verified, FK `ON DELETE SET NULL` (§9 director-locked) (`2b381461`). All three-eye-gate-passed.
**QUEUED (operator sequences; do NOT commission until "go"):** **18-D** (RD research data-collection health monitor) + **18-E** (operational tool-health monitor) — both CONSUME `yfinance_calls` + the integrity data, so they're the natural next step now their input exists; **18-F** (GUI health stoplights, base-layout — operator-witnessed browser gate, downstream of D+E); **18-G** (broad dead-brief sweep, D10); **R1** (declare `requests` in pyproject — its OWN tiny dispatch; it does NOT ride 18-C, confirmed); **18-H** (open-bug/small-feature catch-all). **CHARC owes the architecture passes for 18-D/18-E/18-F at commissioning** (they cross the new-standing-process / base-layout tripwires).

## 4. The two directors + comms (unchanged)
CHARC (tool/architecture director) + RD (research/measurement director). You POST status/return_report/query to them via `python scripts/role_mail.py post --from orchestrator --to charc,rd[,operator] --type … --body-file …`; there is NO orchestrator inbox — direction comes FROM the operator in chat (the operator relays director rulings). **Measurement-core arcs (18-A/B/C/D and the monitors): RD measurement-integrity is MERGE-BLOCKING; CHARC verifies architecture/schema on the shipped diff.** Surface plan-level design decisions for director ratification BEFORE executing (the 18-A decision-#3 / 18-C §9 SET NULL pattern). Run `role_mail` FROM the main repo dir (see §6).

## 5. Binding disciplines (unchanged; full detail in orchestrator-context + memory/)
Merge at gate-pass/convergence without asking; AWAIT the return before QA; QA every product against DISK (the diff, data shapes vs live code, subjects + trailers `[]`, convergence from the real transcript); copowers Codex to convergence via the WSL CLI (the recipe §3 carries the verbatim incantation + the staging frictions); the IMPLEMENTER never posts to the mailbox (you post post-QA); commit the brief BEFORE dispatch; always provide the model/effort recommendation (= the cell choice now). **Schema arcs:** the operator-witnessed live-migration + backup gate at merge (independently verify the live DB after: v-number, integrity, the new table/FK, a backup was taken). **Trailer-parse hazard** (a `Word:` first body line parses as a `%(trailers)`): keep the final `-m` paragraph plain prose; recover a polluted commit by amend+force-with-lease (latest) or, for a MID-branch commit, `reset --hard <bad^>` → `cherry-pick <bad>` → `commit --amend` the message → `cherry-pick` the rest (`rebase -i` is unavailable here). **Tripwire:** new module/schema/dependency/standing-process/base-layout → CHARC architecture pass before dispatch.

## 6. Operational gotchas banked THIS generation (save the next one the re-derivation)
- **cwd drift:** `cd .worktrees/<wt> && pytest` leaves the tool's cwd in the worktree, breaking the next `git -C`/relative path / `role_mail`. Run worktree pytest in a SUBSHELL: `( cd .worktrees/<wt> && python -m pytest … )`. If cwd has drifted, `cd /c/Users/rwsmy/swing-trading` to reset.
- **role_mail body-file:** write the scratch body to the MAIN repo root and run `python scripts/role_mail.py … --body-file <name>` FROM the main repo (a drifted cwd resolved both `scripts/role_mail.py` and the body-file to the worktree and the post failed). Delete the scratch file after; `.copowers-findings.md` is gitignored but `.role-mail-outbox-*.md` is NOT — rm it.
- **Live-DB migration verification:** open read-only (`sqlite3.connect("file:<db>?mode=ro", uri=True)`). `PRAGMA foreign_key_list` columns are `(id,seq,table,from,to,on_update,on_delete,match)` — **on_delete is index 6, NOT 5** (index 5 is on_update; I misread it once and nearly reported a false discrepancy). Cross-check the FK against the stored DDL substring too.
- **Agent tool:** no per-call `effort` param (only `model`); to pin effort use the cell's frontmatter or inherit the session. A mid-session-created cell is NOT callable that same session (turn-boundary rescan; a fresh generation has them).

## 7. Trailing items (organic; no active dispatch — carried from Phase 16)
The TROX frozen-not-removed check (next nightly), the dividend/interest marker capture (first `DIVIDEND_OR_INTEREST` txn), the monthly cadence-page witness + the RD QAs. Tracked in `phase18-todo.md` / `phase17-todo.md §"Phase 16 trailing items"`.

## 8. FIRST ACTION
Greet the operator; confirm the clean boundary (main `52ddd64b`, in sync, ~8258 green, schema v30, single worktree, Phase 18 mid with 18-A/B/C closed). Announce the new generation to both directors (`role_mail … --to charc,rd --type status`). You have NO active dispatch and NO in-flight work — 18-A/B/C are merged + closed. Surface §3's queued arcs + the §7 trailing items. **Do NOT commission 18-D/E/F or anything until the operator says go.** When you do dispatch, use the §2 sub-agent model (it's the adopted mechanism now).

*End of handoff. Phase 18 is half-built (inputs trustworthy + observable); the dispatch substrate itself was rebuilt this generation (the implementer sub-agent library) and is proven across three arcs.*
