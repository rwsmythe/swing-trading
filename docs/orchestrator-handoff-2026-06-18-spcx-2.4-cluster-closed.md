# Orchestrator Handoff — 2026-06-18 (SPCX / §2.4 cluster CLOSED; 18-H.5 audit closed Phase-1-only)

**Audience:** a fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). **Read [`docs/orchestrator-context.md`](orchestrator-context.md) end-to-end first** (durable role framing), then this doc (the delta this generation), then `git log --oneline -20` + `git status`. Bootstrap via `scripts/orchestrator_bootstrap.md`.

## 1. Clean boundary (verify: `git log --oneline -10` + `git status -sb` + `git worktree list`)
- main HEAD **`3ab23d86`** (CHARC's equity_delta-limbo close commit). **origin ~76 behind — NOT pushed** (operator cadence; push only when asked). **Single `main` worktree** (all this-gen worktrees ff-merged + swept; no husks).
- **~8657 fast green** on `main` (re-confirmed on the merged head at the equity_delta-limbo close). **Schema v31** (0031 `untracked_broker_position`, prior gen) — **this generation added ZERO migrations** (SPCX = config-list; §2.4 + equity_delta-limbo = no-schema). Ruff clean. **ZERO `Co-Authored-By`** holds (every commit `%(trailers)` = `[]`).
- Untracked/NOT-the-orchestrator's-lane: `.codex/` + `AGENTS.md` (the concurrent harness-scaffold arc) + `exports/research/shadow-expectancy-*` (research run outputs). LEAVE them.
- Merge discipline: rebase the branch onto CURRENT main → `merge --ff-only` → re-run the fast suite ON the merged head + READ the `N passed` line (the binding no-false-green).

## 2. What this generation shipped (all merged + gate-passed + witnessed where applicable)
The **SPCX out-of-framework saga** (RD-commissioned, CHARC-architected) — three arcs, plus an audit close:

- **18-H.5 dead-code-hardening audit — CLOSED Phase-1-only.** Phase 1 read-only audit (opus-high) → catalog merged (`docs/18-H.5-phase1-dead-code-audit-catalog.md`); **A=1 / B=0 / C=8** (the reader/monitor layer's defenses are correctly aimed at genuinely-unconstrained vectors; the treadmill corollary is already honored). The lone A-1 (the `disc.ambiguity_kind is None` disjunct in the `reconcile.py` 409 guard, dead vs the migration-0019 cross-column CHECK). **Phase 2 DECLINED by operator + CHARC** → A-1 left as documented belt-and-suspenders; no remediation.
- **SPCX out-of-framework carve-out (path B)** (`5b24ef68`-era; merged earlier) — a `[reconciliation] out_of_framework_tickers` config-list in `user-config.toml` (NO schema) + a `swing/trades/schwab_reconciliation.py` orphan-pass carve-out (skip the `untracked_broker_position` emit for declared tickers + a #27 exclusion line) + a scoped/audited/idempotent existing-row resolve (`resolve_out_of_framework_orphans`). **Merged + live-witnessed** (declared SPCX → no orphan + the #27 line; undeclared still banners; existing orphans cleared).
- **swing-NLV §2.4 coherence** (`e1017b1e`) — generalized the step-8 both-flat gate to `broker_flat_swing`; `swing_nlv = source_nlv − Σ declared MV` (same snapshot); reuse `equity_delta`; drift-only; RAW run-row columns + swing detail in `actual_value_json` on fire + a `pipeline.log` coherent line (§9.4). Two real MAJORs caught+fixed in executing (non-finite ledger/NLV). **Merged + live-witnessed:** the witness FIRED a real `equity_delta` → the operator did **path (a)** (recorded the SPCX outlay as a swing cash_movement) → coherent. §2.4 is **L2-sound** (it fired on a REAL ledger-vs-swing-NLV drift, not a false one).
- **equity_delta limbo-routing fix** (`8fee6a1d`) — the 18-H.6.1 pivot-skip twin: widened `_pivot_classify_and_dispatch_for_run`'s skip-set to `("untracked_broker_position","equity_delta")` so a fired `equity_delta` stays `unresolved`/cleanable, not `pending_ambiguity_resolution`. CHARC **§0 ruled (A)** (cleanability; NO `MATERIAL_BY_TYPE` change — material=0 correct-by-design; the (B) materiality bump deferred). **Merged + witness-CLEAN** (id 71 cleared → `acknowledged_immaterial`, `ambiguity_kind` NULL; no limbo `equity_delta` remains). **CLOSED** in charc-state + phase18-todo (`3ab23d86`).

## 3. Disciplines banked THIS generation (apply them)
- **Mid-flight brief-change mitigation (NEW — adopt as standing):** snapshot the commissioning brief's last-commit sha at dispatch (`git log -1 --format=%H -- <brief>`) and DIFF it at the return. The swing-NLV writing-plans built against a STALE brief — RD folded a §9 L2 checklist (the run-row-stays-RAW resolution) into the brief DURING the writing-plans run; the orchestrator caught it by eye at QA. Pin the sha so it's caught mechanically. (Held on the next two arcs.)
- **The live-witness earns its keep — it tests the DESIGN PREMISE, not just the code.** The swing-NLV §2.4 witness FIRED where coherence was expected, exposing a flawed brief premise (`NLV − SPCX ≈ ledger` was self-contradicted by the brief's own cited numbers — the SPCX outlay consumed swing-tracked cash the ledger never recorded). Byte-tests passed (synthetic numbers); only the live run caught it (the 18-E pattern). Keep operator live-witnesses BINDING.
- **Path (a) is the director-blessed OOF-cash-side model:** record an out-of-framework holding's cash outlay as a swing `cash_movement` (a benign one-time acknowledge, same as a manual deposit; self-suppressing on resolution; zero measurement impact). RD+CHARC ruled the resulting `cash_movement_mismatch` (id 72) a NON-ISSUE — correct routing, not collateral damage. **Worth documenting** in the cycle-checklist / a path-B note (flagged to CHARC; not yet landed).
- **The sub-agent git-write friction is RESOLVED:** bare-git-from-worktree-cwd (recipe §2) held across every dispatch this gen — no `git -C` for sub-agent commits; no permission denials.
- **`role_mail --body "..."` backtick hazard:** backticks in a double-quoted `--body` trigger shell command-substitution (dropped one word once). Use plain text or `--body-file`.
- **Reviewer-B WSL-CRLF phantom recurs:** `codex exec review` from WSL on a Windows CRLF tree sometimes reports a bogus mass-modified-files churn — cross-check against the Windows-side `git diff --numstat` before trusting it. A `.gitattributes` normalization would kill it at the source (a candidate -H/scaffold item).

## 4. Phase 18 state — queue (operator-sequenced; nothing commissioned)
Live tracker: [`docs/phase18-todo.md`](phase18-todo.md). **No live arc; no in-flight dispatch.**
- **18-G** (dead-brief sweep) · **18-H.7** (nightly research-health ATTENTION → rd-push; 3 design points deferred) · the **RD watch-standard amendment** (RD's own action).
- **18-H.5 Phase 2** = DECLINED (A-1 stays belt-and-suspenders).
- **§2.4 (B) materiality bump** (`MATERIAL_BY_TYPE["equity_delta"] 0→1`) = a SEPARATE deferred operator decision, NOT this arc, NOT commissioned.
- **id 72** (`cash_movement_mismatch`, material=0, off the dashboard) — operator-resolvable whenever via the tier-2 reconcile flow (benign, like a manual-deposit ack). Not a defect (RD+CHARC NON-ISSUE).
- **Research monitor yellows** (`coverage_gaps` + `excluded_reason_breakdown`) — RD-evaluated, non-actionable, operating correctly.

## 5. Directors + comms (unchanged)
CHARC (tool/architecture) + RD (research/measurement). POST status/return_report via `python scripts/role_mail.py post --from orchestrator --to charc[,rd] --type … [--body / --body-file]` from the MAIN repo dir. **No orchestrator inbox** — direction comes FROM the operator (who relays director rulings). The IMPLEMENTER never posts (you post post-QA). RD merge-blocks on L2/measurement arcs; CHARC owns the §3 / tripwire / C-conditions gate + QA-on-disk on carve-out/schema arcs.

## 6. The dispatch model (unchanged; full detail in orchestrator-context §Dispatch execution + the recipe)
Orchestrator spawns the implementer as a **sub-agent via the Agent tool** (`.claude/agents/implementer-<model>-<effort>` cells; SPOF [`docs/implementer-dispatch-recipe.md`](implementer-dispatch-recipe.md)) on the operator's "go": commit the brief to main FIRST (briefs were CHARC-authored this gen) → select the cell by TASK (announce, vetoable) → `Agent(run_in_background=true)` (NO model/isolation override — the cell makes its own `.worktrees/<name>` + carries model+effort). Await; **QA-against-disk** (diff, trailers `[]`, convergence from the REAL tracked transcript [`reasoning effort: high`], C-conditions, data-shapes-vs-live-code). **Run the gating Reviewer B (codex-auto-review)** at QA — detached-checkout the rebased tip in the MAIN repo, `wsl … codex exec review --base main -c model_reasoning_effort=high`, re-attach (cross-check the CRLF phantom). Post the QA'd return to the directors → director gates + operator live-witness → operator authorizes → rebase + `merge --ff-only` + merged-head no-false-green + close-out (status docs + worktree sweep). **Writing-plans** = `implementer-opus-xhigh` + review-fast; **executing** = `implementer-opus-high` (opus-max for measurement-chain/irreversible) + review-strong + the gating Reviewer B, both repo-access.

## 7. The concurrent harness-scaffold arc (be aware, do NOT touch)
A SEPARATE generation/CHARC advances `.codex/`, `AGENTS.md`, `docs/charc-state.md`, `docs/harness-*`, the `docs(scaffold-backlog)`/`docs(harness-backlog)` commits — docs-only, FREQUENT. Always branch worktrees from CURRENT main + rebase before merge. Leave its artifacts alone.

## 8. FIRST ACTION
Greet the operator; confirm the clean boundary (main `3ab23d86`, ~8657 green, v31, single worktree, **no live arc, nothing in flight**; the SPCX/§2.4 cluster is fully closed). Announce the new generation to both directors (`role_mail … --to charc,rd --type status`). NO active dispatch. The operator sequences what's next (the §4 queue, or new work). Do NOT push origin unless asked.

*End of handoff. This generation closed the entire SPCX out-of-framework saga (carve-out path B → swing-NLV §2.4 coherence → equity_delta limbo fix), all merged + witnessed, plus the 18-H.5 audit (Phase-1-only). The live-witness twice earned its keep (the §2.4 design-premise catch; the id-71 clear). The board is clean.*
