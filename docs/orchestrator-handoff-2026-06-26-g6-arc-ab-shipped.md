# Orchestrator Handoff — 2026-06-26 (G6 Arc A + Arc B SHIPPED; a seven-arc generation)

**Audience:** a fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). **Read [`docs/orchestrator-context.md`](orchestrator-context.md) end-to-end first** (durable role framing), then this doc (the delta this generation), then `git log --oneline -20` + `git status`. Bootstrap via `scripts/orchestrator_bootstrap.md` (NOTE: that doc was corrected THIS generation — see arc 7; the orchestrator inbox now EXISTS).

## 1. Clean boundary (verify: `git log --oneline -10` + `git status -sb` + `git worktree list`)
- main HEAD **`633d5c8b`** when written (CHARC's status commit recording G6 Arc B merged + Arc-A Part-B closed). My last ARC-merge was **`1f8b8545`** (G6 Arc B code) / **`d4c4f119`** (its transcript). The concurrent CHARC/RD/scaffold generations advance main FREQUENTLY (docs-only) — main is ~235 ahead of origin.
- **~8871 swing fast green** on `main` (re-confirmed on each merged head). **Schema v31** — this generation added **ZERO migrations** (every arc was no-schema by design). Ruff clean. **ZERO `Co-Authored-By`** holds (every commit `%(trailers)` = `[]`).
- **origin ~235 behind — NOT pushed** (operator cadence; push only when asked). **Single `main` worktree.**
- Merge discipline (unchanged): rebase the branch onto CURRENT main → `merge --ff-only` → re-run the FULL fast suite ON the merged head + READ the `N passed` line (the binding no-false-green; memory `feedback_no_false_green_claim`).

## 2. What this generation shipped (SEVEN arcs, all merged + gated + director-cleared; ZERO schema)
1. **D20** — Schwab `transaction_id` `^[0-9]+$` constraint at the `SchwabTransactionResponse.__post_init__` barrier (Phase-18-close hardening; closes the `oof:`/`void:` collision proofs at one site). Merged swing **`7eabb3d2`**. CHARC-lane. (Used `re.fullmatch`, not `.match` — the `"123\n"` footgun.)
2. **B-9 + B-12** — **CROSS-REPO (harness-template)** genericity-guard instance-aware REDESIGN (the `is_instance_surface()` model so a filled germination is green-by-construction while CORE contamination still fails loud) + peer-director-add code. Merged harness-template **`f0da3aa`**.
3. **B-16** — **CROSS-REPO (harness-template)** role-identity-lost-on-resume: a session-keyed role-recovery store (`comms/roles/<sid>`) so a resumed session recovers its role. Merged harness-template **`db644b4`**.
4. **18-H.7** — nightly research-health RED -> `role_mail` push to RD (added `pipeline` to `VALID_FROM` as a status-only automated emitter; edge-trigger reads the prior `latest.json` before the write; best-effort under `step_guard`). Merged swing **`ceecd837`**.
5. **coverage_gaps CALIBRATION C** — research-health #3 current-vs-historical calibration. The FIRST live research-health RED (842 gaps) was a benign false-RED (a 2026-06-22 missed manual run); CALIBRATION C accepts a whole-session miss (BOTH zero-global-obs AND no-run-ledger-row) + a skip-warning-explained interior hole, while still reding a genuine current failure. Merged swing **`83c21305`**.
6. **G6 Arc A** — orchestrator per-generation comms inbox + session registry: a single-sourced stdlib `scripts/comms_session_registry.py` + a `scripts/comms_session_hook.py` (SessionStart register/prune + a gate-free heartbeat, wired in `.claude/settings.json`) + `role_mail` orchestrator addressing (`--to orchestrator` newest-live / `--to orchestrator:<sid>` registry-independent / `read --role orchestrator --session <sid>`). Merged swing **`94e8c7cb`**.
7. **G6 Arc B** — orchestrator launch role + GUI bus: `start_directors.ps1` gains the `orchestrator` role (role-aware `--effort xhigh` for the orchestrator, directors stay `max`; role-aware `New-SessionName`); `comms_ui.py` gains `orchestrator` in `LAUNCH_ROLES` + a read-only per-generation orchestrator bus (walks `comms/orchestrator/<sid>/inbox` ON DISK, defensive-never-raises); and `orchestrator_bootstrap.md` was corrected to the inbox-now-exists reality. Merged swing **`1f8b8545`**. (Operator §5.10 BROWSER witness PASS; it ALSO closed Arc-A deferred Part B = the live `payload["session_id"]` field name.)

## 3. Disciplines banked THIS generation (apply them)
- **Plan-stage director review (NEW memory `feedback_plan_stage_director_review`) — now a STANDING expectation.** After the orchestrator's plan-QA (PASS) and BEFORE dispatching executing, route the PLAN to the owning director(s) for a plan-stage review: **CHARC** for harness/monitor/architecture/§3-tripwire surfaces, **RD** for watch-standard/measurement semantics, BOTH when it touches both. The operator works with the director to resolve. Trigger = a plan that crosses a director's tripwire OR makes a design/semantic decision in their domain (esp. a strengthening beyond the brief's lean). QA-first still binds; distinct from the post-exec return report. (18-H.7 → CHARC; coverage_gaps → CHARC+RD; G6 A/B → CHARC.)
- **529-recovery (a sub-agent dying on a transient API-529 on its FINAL return).** The implementation is usually COMPLETE + COMMITTED on disk before the failure — do NOT blindly re-dispatch the whole task. Inspect the worktree (commits, status, the §3 conditions on disk), run the before-review suite YOURSELF, then dispatch a NARROWLY-SCOPED recovery agent for ONLY the missing gate (e.g. codex-auto-review + the return). Zero rework. (G6 Arc A; `SendMessage` to continue a dead agent is NOT available to the orchestrator.)
- **codex-auto-review earns its keep — it caught a real defect on 3 arcs that review-strong's diff-focused pass missed** (18-H.4 lock-finalize; 18-H.7 the `pipeline` authority-widening; G6 Arc B the stale `orchestrator_bootstrap.md` + the invalid orchestrator resume-drain). It reads UN-CHANGED surrounding code (repo-access). Run it on production-adjacent arcs. Corollary: when a settled design will be re-litigated (G6 Arc B's disk-enumeration bus), carry a "binding-constraint — do not re-litigate" note INTO the executing review prompt (it worked — the executing review did not re-raise it).
- **Cross-repo mechanics (harness-template arcs B-9/B-12, B-16 — relevant for Arc C):** the PLAN lives in the SWING control repo (`docs/plans/`), NEVER in the agnostic harness-template (a project-term-laden plan would contaminate it); the CODE is committed in `C:\Users\rwsmy\harness-template` (base its own `master`); the convergence transcript is persisted to SWING `docs/reviews/`. The accept gate is stdlib `unittest` (NOT pytest). The worktree `.git` is a file WSL cannot resolve -> feed the diff via stdin; codex-auto-review's `--commit/--base` can't run -> use the `codex exec @ effort=high` substitution over the bundle.
- **`role_mail` from the MAIN repo dir** (carried from the prior gen, memory `feedback_role_mail_run_from_main_repo`): always `cd "c:/Users/rwsmy/swing-trading"` before `role_mail post` (posting from a worktree silently misdelivers to the worktree's gitignored `comms/`). The `--body` backtick/`$` hazard -> use `--body-file` for anything with backticks/`$`/regex.

## 4. Trailing / queue (operator sequences; verify against CHARC's current state)
- **B.1 (CHARC fast-follow, COMMISSIONED-as-next):** the orchestrator resume-drain `--session` story — a `role_mail` newest-live self-read (no `--session` -> newest-live) so a resumed/launched orchestrator can drain its OWN inbox; it REVERSES the Arc-A require-session contract, so it gets its own TDD + review. (Surfaced by codex-auto-review at G6 Arc B; out-of-scope there.)
- **G6 Arc C** — the GUI re-sync swing→harness-template scaffold: port swing's B-11 dark-theme + B-13 expanded-`<details>` fixes (swing→scaffold) + GRAFT the orchestrator-bootstrap/launch/bus mechanism onto the scaffold. CROSS-REPO (harness-template). Operator-sequenced (A→B+C; A+B now done).
- **Orphaned worktree dir** `.worktrees/g6b-orchestrator-launch-bus` — git-DEREGISTERED + gitignored (`.gitignore:213`) but the DIRECTORY couldn't be deleted (`Device or resource busy` — the TaskStop-doesn't-kill-detached family). Harmless (doesn't pollute git status); `rm -rf` it once the holding handle releases.
- Older (from prior gens, status uncertain — verify with CHARC): 18-G dead-brief sweep; RD watch-standard amendments (RD folds the coverage_gaps §3.1 accept-semantics + the 18-H.7 demand-spec); 18-H.5; TROX age-off; the dividend marker.

## 5. The dispatch model + comms (unchanged; full detail in orchestrator-context §Dispatch execution + the recipe)
Orchestrator spawns the implementer as a **sub-agent via the Agent tool** (`.claude/agents/implementer-<model>-<effort>` cells; SPOF [`docs/implementer-dispatch-recipe.md`](implementer-dispatch-recipe.md)) on the operator's "go": commit the brief/plan to main (or a worktree the implementer reuses) FIRST → select the cell by TASK (announce, vetoable) → `Agent(run_in_background=true)` (NO model/isolation override — the cell makes its own `.worktrees/<name>` + carries model+effort). Await; **QA-against-disk** (diff scope, trailers `[]`, the conditions AS SHIPPED [verify on disk, never trust the report], convergence from the REAL transcript). This generation's tiers: writing-plans = `implementer-opus-xhigh`, executing = `implementer-opus-high` (the operator concurs; `-max` only for measurement-core/irreversible). The implementer NEVER posts to a director — YOU post the QA'd return to charc (+rd/operator as routed) AFTER QA. **No orchestrator inbox to read FROM** in V1 for direction (direction comes from the operator in chat) — BUT note Arc A shipped a per-gen inbox, so a future generation that runs as a launched/registered CC session CAN be addressed `--to orchestrator`; B.1 adds self-drain.

## 6. The concurrent harness-scaffold / director generations (be aware, do NOT touch)
SEPARATE generations (CHARC, RD, the scaffold/Arc-C lane) advance `docs/charc-state.md`, `docs/rd-state.md`, `docs/*-context.md`, `docs/charter`, `.codex/`, `AGENTS.md`, `exports/research/*` — docs-only, FREQUENT (main ~235 ahead of origin). Always branch worktrees from CURRENT main + rebase before merge. Leave their artifacts alone (untracked `.codex/`, `AGENTS.md`, `exports/research/*` in your `git status` is theirs).

## 7. FIRST ACTION
Greet the operator; confirm the clean boundary (main at/after `633d5c8b`, ~8871 swing green, v31, single worktree, **no live arc, nothing in flight**; G6 Arc A+B shipped; the orchestrator per-gen inbox + launch + bus are live). Announce the new generation to both directors (`cd <repo>; role_mail … --to charc,rd --type status`). NO active dispatch. The operator sequences what's next (B.1, Arc C, or new work). Do NOT push origin unless asked.

*End of handoff. This generation closed seven arcs (D20, B-9/B-12, B-16, 18-H.7, coverage_gaps CALIBRATION C, G6 Arc A, G6 Arc B), all merged + director-cleared + (where applicable) operator-witnessed, ZERO schema, ZERO Co-Authored-By. It banked the plan-stage-director-review discipline (now standing) + the 529-recovery pattern, and survived a transient 529 mid-arc with no rework. The board is clean.*
