# Orchestrator Handoff — 2026-06-13 (Phase 17 CLOSED — no active phase)

**Audience:** A fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). You drive copowers cycles + QA + merge + housekeeping; you do NOT implement. **Read the durable role framing in [`docs/orchestrator-context.md`](orchestrator-context.md)** for disciplines, the WSL-Codex transport, and merge mechanics. This doc is the Phase-17-close delta.

## 1. Clean boundary (verify: `git log --oneline -8` + `git status -sb` + `git worktree list`)
- Working tree clean post-close; origin in sync; **single `main` worktree** (all four 17-D-quick-win + 17-D.4 worktrees were created at `.worktrees/<n>`, QA'd, merged ff, and removed — none linger).
- **~8132 fast tests green** (3× consecutive clean `-n auto` runs on the close HEAD — the 17-D.4 fix de-flaked the suite); **schema v29 UNCHANGED** (Phase 17 took zero migrations); ruff clean; **ZERO `Co-Authored-By`** (every Phase-17 commit trailer-scanned `[]`).
- The operator commits research/comms work to main in parallel constantly. **Divergence discipline: stage explicit paths only; rebase the branch onto main, then `merge --ff-only`; re-run the suite ON the merged head** (never carry a branch count forward).

## 2. Phase 17 — CLOSED 2026-06-13 (full per-arc history: [`docs/phase17-todo.md`](phase17-todo.md) — the canonical record)
*Consolidation & Parity-Drift Elimination — paying down drift-class (not capability-class) debt.*
- **17-A** single-evaluation orchestration (D6): one shared `swing/evaluation/orchestration.py:orchestrate_evaluation` for `swing eval` + the nightly, killing the comment-enforced parity-drift; `bc05958c`; fixed a latent CLI crash en route. Live gate: 110 rows byte-identical.
- **17-B** `step_guard` extraction (D1): the ~9× best-effort step-wrapper boilerplate → one tested context manager `swing/pipeline/step_guard.py` (runner −85 net lines, zero behavior change); `5c86c254`.
- **17-C** exports retention (D3): **CLOSED as already-solved, NOT built.** The premise was false — `swing/rendering/retention.py:archive_old_exports` already zip-retains dated exports every pipeline run. Caught at writing-plans by the implementer grounding the brief vs live code (the orchestrator's brief had missed it — lesson banked: memory `feedback_brief_premise_check_existing_mechanism`).
- **17-D** bug-container (all actionable items closed): `.1` closed-not-a-defect (the `swing eval` displacement concern — structurally already closed) · `.2` comms dark-mode (`991430c8` + icon-parity `5df335d4`; operator-gated) · `.3` `review_log_cadence` LeaseRevoked re-raise (`aab2c5e4`; coupled-flip of 17-B's characterization) · `.4` the `-n auto` process-global polluter (`0867337d`) — research L2 tests raw-`del`'d `swing.*` from `sys.modules` without restoring → reimport rebuilt exception classes with new identities → web-route `except` missed them → intermittent 500s; **healed at the research-test boundary via a `tests/research/conftest.py` autouse fixture → suite DE-FLAKED** · `.5` weather-chart declutter + 20MA (`735cc353`; operator-gated).
- **Riders** not required as Phase-17 work (operator); reconciled with CHARC's audit: R2 (frozen-clock) landed in orchestrator-context §Binding conventions; **R1 (D7, `requests`-dep) + R3 carried to Phase 18** (R3 → arc 18-C).
- Dead dispatch/commissioning briefs archived to `docs/archive/phase17/`.
- **CHARC's phase-close audit landed CLEAN TO CLOSE** ([`docs/phase17-close-audit-charc.md`](phase17-close-audit-charc.md)): every arc landed or correctly withdrawn; both §2.4 tripwire-gate exercises held; zero false-negatives; the harness probe was the first fully-clean run. The thesis paid off — consolidation flushed THREE latent *production* defects (17-A's CLI IntegrityError, 17-D.3's revoke-swallow, 17-D.4's `-n auto` 500-flake), none from a user report.

## 3. Trailing items (organic — no active dispatch)
These are NOT Phase-17 blockers; they carry forward (tracked in [`docs/phase17-todo.md`](phase17-todo.md) §"Phase 16 trailing items"):
1. **TROX frozen-not-removed check** — after the next nightly (screen-absent + unpinned → FROZEN-not-removed per the absent-skip contract; no injection/suppression entries; streak holds; removal fires only on a screen re-entry).
2. **Dividend/interest marker capture** — ORGANIC: the account's first `DIVIDEND_OR_INTEREST` transaction (until then every such txn tier-2 flags; unskip `test_dividend_marker_set_is_real_payload_sourced`).
3. **Monthly cadence-page witness** + the **RD QAs** (the 0026 §ADDENDUM language, Arc-9's rendered text, the measurement-universe note).
4. **17-C deferred trivia** (NOT an arc): `archive_old_exports` zips but never prunes `exports/archive/*.zip` (~1 small zip/month; currently empty; revisit only if it ever matters).

## 4. Binding disciplines (unchanged; full detail in orchestrator-context.md + the memory/ directory)
Merge at QA-pass/convergence without asking; AWAIT the implementer's return before QA; QA every product against DISK (verify the diff, data shapes vs live code, subjects + trailers `[]`); copowers Codex to convergence via the WSL CLI (`wsl.exe bash -ilc 'export PATH="$HOME/.local/node22/bin:$PATH"; codex …'`; from a worktree PRE-GENERATE the diff — the worktree `.git` is unreachable from WSL); implementers persist Codex prompts AND responses to a gitignored `.copowers-findings.md`; commit the dispatch brief BEFORE the inline prompt; always provide the paste-ready inline prompt; the IMPLEMENTER never posts to the mailbox (orchestrator posts post-QA); operator gates are binding and witness the UNSEEDED default; "pause" = stop immediately. **Tripwire:** new module/schema/dependency/standing process → CHARC architecture pass before dispatch (else self-certify "no tripwire crossed" in the brief). **Before scoping a new-capability arc, grep ALL of `swing/` for a pre-existing mechanism that already provides it** (the 17-C lesson).

## 5. FIRST ACTION
Stand by. Greet the operator; confirm the clean boundary (main HEAD, in sync, ~8132 green, v29, Phase 17 closed, all worktrees removed). You have NO active phase and NO close-out task. Surface the §3 trailing items if a nightly has run. **Phase 18 (data-collection integrity) is already scoped + operator-approved** ([`docs/phase18-todo.md`](phase18-todo.md): 6 arcs + 18-G broad-brief sweep + 18-H catch-all + the carried riders; CHARC owes the architecture passes for 18-B/18-C/18-E/18-F at their commissioning) — but **do NOT open or commission it until the operator says go.** Ask the operator what's next.

> **Shared-tree commit hazard (banked as charter §5.8, `914950a5`):** the operator commits to `main` in this same clone in parallel. A `git mv`/`git add` left staged in the index between your operations can be **swept into the operator's next commit** (it happened at this very close — the 9 brief renames landed via an operator commit). Mitigation: stage with explicit pathspecs AND commit promptly; don't leave staged changes lingering. Verify with `git log` after, not just `git status`.

*End of handoff. Phase 17 closed; the `swing eval`↔nightly parity-drift is gone, the runner's step-wrapper boilerplate is one tested guard, the exports-retention "gap" turned out already-solved, and the `-n auto` suite is reliable again.*
