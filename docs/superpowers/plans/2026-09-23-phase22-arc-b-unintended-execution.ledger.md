# 22-B plan ledger — Demand A, `unintended_execution`

**Plan:** `docs/superpowers/plans/2026-09-23-phase22-arc-b-unintended-execution.md` (converged; see the last section of this ledger). **Branch:** `22-b-plan` off `main` @ `9d89e304`. **Evidence home:** `~/swing-data/review-transcripts/22-b-plan/` (copy-per-round).

**Courier's note (orchestrator, 2026-09-23):** the two sections below are the rulings binding this plan, transcribed as LITERAL block quotes by script (each source line asserted present, `> ` the only added bytes). The ruling seats are the AUTHORS; the orchestrator is courier only. CHARC's dispatch (comms, 2026-09-23T17:14:58Z) directed this transcription.

## R0.A — RD's F1–F3 rulings, WHOLE (`docs/phase22-arc-b-rd-rulings-f1-f3.md` @ `e5feec2a`; author RD; includes the operator's GO and the clause-(4) ratification, his words as recorded by RD)

> # Phase 22 — Arc 22-B: RD rulings on the census forks F1, F2, F3 (ONE packet)
>
> **Author:** RD (the ruling seat on all three; F1's PIN shape is CHARC's). **Date:** 2026-09-23. **Status:** RULED — one pass, per the plan-stage protocol (harness §5.1). The courier transcribes this file into the 22-B plan ledger as a block quote when the ledger exists.
>
> **The go.** The operator ruled GO on 22-B in the RD session, 2026-09-23 (his words: *"We have 7 days before 9-30, plenty of time. Let's go for 22-B"*). Relayed to CHARC and the orchestrator by RD as courier; the authority is the operator's. RD's position on the calendar is in §0.
>
> Every fact below was re-read on the live DB (`mode=ro`, v39) or in the code on `main @ 2c76ca2f` on 2026-09-23 — never from the brief's §1 table, which was measured at v38.
>
> ---
>
> ## §0 RD's calendar position (stated, not a ruling)
>
> **Feasible, and the downside is bounded — that is the reason to go, not "seven days is plenty."** The code is small (one enum token, one table, one CLI command, a mirror sweep). What consumed 22-A2 was the review ladder: ~5 director packets per seat in one sitting, with pre-ruled scope. 22-B has THREE human gates in series that 22-A2 did not: the §VII.F ratification of exact text (F1, before the plan loop opens), the step-by-step witness (§5, the operator's hand), and the read-week freeze (`phase22-scope-charc.md:152`: no `trades` rebuild lands DURING the read week). With the travel pause from 10-03 the witness must land by 09-30.
>
> **What bounds the downside:** the serial rule already forbids executing before the witness window is real; nothing reaches the live journal without the witness; a slip parks the arc at "plan on `main`, executing branch WIP-committed" until 10-21. That is a parked branch, not a half-migrated journal.
>
> **The stop line, so it is readable and not inferred:** if Reviewer B is not clean on the finished tree by **2026-09-29 end of day HST**, the witness waits for 10-21, and the October read runs on v39 with trade 20 NULL and NAMED (as every read has carried it). No merge over the read week; no witness during the pause.
>
> ---
>
> ## §1 F1 — WHAT TEXT §VII.F RATIFIES. Ruling: **branch (a).** RD names the text; CHARC rules the pin.
>
> **Why (a):** the two existing values ARE the training-epoch contract's vocabulary (charter §7, 2026-06-10). A third value changes what the contract can SAY. Branch (b) would leave the definition in a docstring — a value in the journal of record whose meaning lives nowhere ratified. The 0034 precedent (operator-signed exact text, original preserved, hash-pinned, independently derived) is the shape; the difference is that the amended object is DOCTRINE in a doc, not a registry row.
>
> **Verified before ruling — H1's registry row needs NO amendment.** `cohort_intent.py` pins `H1_COHORT_CLAUSE` = *"COHORT: the 20 are STANDARD-intent trades only …"* and the predicate is `entry_intent = 'standard'` by positive allowlist. An `unintended_execution` row is not STANDARD-intent and is excluded by the existing clause with no code change. H2–H5 are epoch-contract-grounded with no intent exclusion, and the new value is not `hypothesis_test_by_design`, so it is not a designed sample of any of them either. **The value lands in NO cohort by the existing predicates.** The amendment is to the contract, not the criteria.
>
> **The home of the text (doctrine — mine to name):** the forward intent contract today exists ONLY in `docs/research-director-context-archive.md` line 84 (the 2026-06-10 declaration). An archive is not a home (harness §2's archive discriminator). The amended contract lives in a NEW committed doc, `docs/training-epoch-intent-contract.md`, that (i) quotes the 2026-06-10 forward-intent clauses (1)–(3) VERBATIM from the archive (preserve the quote), then (ii) carries clause (4) below, dated and with the operator's ratification quoted. Charter §7 gets a dated pointer entry. `cohort_intent.py`'s docstring cites the archive today; the plan re-points it to the live doc (docstring only — no semantic change; the plan says so).
>
> **THE AMENDED TEXT — clause (4), for the operator's ratification EXACTLY as written (edit it before ratifying, never after):**
>
> > **(4) `unintended_execution` — an execution nobody decided to make.** A fill produced by the framework's own resting order after its mandate had died (invalidation, lapse, horizon, or the operator's decline), or any fill no entry decision produced. It is NOT `standard` (no decision was made to take the trade) and NOT `hypothesis_test_by_design` (no program fired it). It counts toward NO hypothesis cohort. It is never inferred from text; it is assigned only through the evidence-bearing surface, with the evidence tier and the cited evidence recorded on the row, under the admissibility test: evidence is admissible iff it was recorded before the outcome was known AND is immutable since, immutability verified against the audit trail. A trade already carrying `standard` or `hypothesis_test_by_design` is never relabelled to this value — that is a different evidence class with its own record. Its realized result is reported in the trade-process card's own facet as an execution datum, never as a hypothesis sample.
>
> Clauses (1)–(3) are unchanged. The pin: CHARC rules the shape (RD's expectation from the brief: the sha256 of the ratified clause text in `0040`'s header, derived independently by a test from the committed doc; never checked at migration runtime).
>
> **RATIFIED BY THE OPERATOR, 2026-09-23 ~17:15Z, in the RD session, his words verbatim: "clause (4) is ratified."** The text above is the ratified text, byte-for-byte as it stands at this commit; any later edit is a new amendment, not a correction. The gate before the plan loop is CLEARED; the pin (CHARC's shape) is derived from this file's clause (4) block quote.
>
> ---
>
> ## §2 F2 — THE TIER-2 ADMISSIBILITY PREDICATES. Ruling: **the brief's three predicates CONFIRMED, with FOUR sharpenings; both edges ruled.**
>
> **The three, confirmed:** (P1) the instrument provably post-dates the entry; (P2) no correction has touched the cited fields; (P3) the record pre-dates the outcome. Each bound in SQL to its source AND checked by the service first (AUTHORIZE-THEN-ABORT: the trigger's predicate set ⊆ the service's).
>
> **Sharpening 1 — P1 is at SESSION-DATE grain, strictly later, and the reason is named.** `trade_entry_ts` as the brief has it would come from `fills.fill_datetime`, which is synthetic (`entry_date + 'T16:00:00'` on every entry fill — trade 20's fill 41 reads `2026-08-07T16:00:00`; gotcha #30's family). It is not a clock; a datetime comparison against it would ADMIT an instrument recorded at 17:00 on the entry session over a fill whose real time nobody recorded. **Rule: tier 2 requires `date(instrument_earliest_recorded_ts) > trades.entry_date` — the instrument's first row is on a STRICTLY LATER session than the entry. Same-session is UNPROVABLE and refuses** (the three-valued discipline: unprovable is not absent). The brief's one-second discriminator pair becomes: instrument first recorded ON the entry session → REFUSE; on the NEXT session → ADMIT. Trade 20: entry 2026-08-07, earliest instrument 2026-08-08T20:01:08 (13 rows today, 0 for AMN — re-read 2026-09-23) → ADMIT.
>
> **Sharpening 2 — P2 checks BOTH audit tables.** The admissibility test says "verified against the audit trail (`reconciliation_corrections` / `provenance_corrections`)". `provenance_corrections` writes cohort keys, not text fields, today — but the predicate is over the trail, not over today's writers. `NOT EXISTS` over both, on `(affected_table='trades', affected_row_id=trade_id, field_name IN cited)` and the 0036 table's equivalent columns (the plan reads its DDL; the brief's §1 said it moved with 0039).
>
> **Sharpening 3 — P2's PRECONDITION is stated and the plan VERIFIES it.** "Immutable since, verified against the audit trail" is sound ONLY if every writer of the cited fields leaves an audit row. My predecessor's 2026-08-12 line "`trades.notes` has NO update path" was FALSE (restated the same week: the audited corrector `reconciliation_auto_correct.py:2309` writes any field by name). Today the only `trades` fields ever corrected are `current_stop` ×4 and `entry_date` ×1, and trade 20 has zero rows. **The plan walks every `UPDATE trades` site in `swing/` (the repo's own count was 21 at `reconciliation_auto_correct.py:190`) and asserts by test that no site writes `notes|why_now|thesis|emotional_state_pre_trade` outside the audited corrector.** If one exists, the predicate is fail-open and the cell REPORTS it as a finding — it does not add a channel (a negative is a finding, never a licence).
>
> **Sharpening 4 — the citation must include a DESCRIPTIVE field.** At least one of `notes` / `why_now` must be cited. Reason: only free text can describe a mechanism; a tag list (`emotional_state_pre_trade` = `["distracted"]`) can corroborate an unintended execution but cannot assert one, and a rule that admits on a tag alone admits on nothing.
>
> **Edge (i) — a trade still OPEN: ADMISSIBLE NOW, `outcome_known_at` recorded NULL.** The test dates the EVIDENCE against the outcome, and entry-time text pre-dates an outcome that does not yet exist. The curation risk runs the OTHER way: the assigner's judgment ("does this text describe an unintended execution?") is least contaminated the sooner it is made — waiting for close maximizes what the assigner knows. So assign at the first review, not at close. Two guards travel with this: the attestation row records NO P&L, MFE or MAE (running state is not evidence and must not be snapshotted into an evidence row); and `outcome_known_at` is written ONCE, at assignment — if NULL then, it stays NULL (the row is append-only; the trade's later exit is read from `fills` at replay, never back-filled into the attestation).
>
> **Edge (ii) — `thesis` and `emotional_state_pre_trade`: IN the allowlist.** Same capture point, same immutability class, same audit trail as `notes`/`why_now`; the admissibility test does not discriminate by field, and Sharpening 4 already prevents a tag-only assignment. `pre_trade_locked_at` NEVER (a synthetic restatement — 20/20, now 28/28 by construction). Demand B annotation rows NEVER (the standing guard; they fail P3 always).
>
> ---
>
> ## §3 F3 — THE STRUCTURAL TIER'S CITATION. Ruling: **branch (a), CHARC's stake, with the DEATH-BEFORE-FILL predicate that makes the tier mean what it says.**
>
> **(a):** where a `latch_order_mandate_links` row exists for the filled order, the attestation cites the LINK (`cited_latch_link_id`) — it is the durable order↔mandate binding 22-A built, and it names the fire whose death is the claim; where no link exists but a `latch_order_intents` row does, cite the intent row (`place`, and `validity` if present). Three-valued: link proven → cite link; no link, intent proven → cite intent; neither → tier 2 by P1 or REFUSE (the instrument existed and did not fire — not an unintended execution the record can prove).
>
> **The predicate the citation must carry — DEATH BEFORE FILL.** A structural citation proves the framework PLACED the order. It does not, by itself, prove nobody intended the fill: a fill while the latch is LIVE is a MANDATE fill and is `standard` under clause (1). So the structural tier ADMITS only when the cited latch's terminal state is a death rung — `invalidation`, `criteria_lapsed`, `horizon`, or `declined` — with its terminal session STRICTLY BEFORE the fill's `entry_date` (the latch bound: a fill on any LATER session does not label from the fire; same-session ties go to FILL under R6, so same-session is a mandate fill and REFUSES here). A latch whose terminal is `fill` or `superseded`, or whose death session is on or after the entry date, REFUSES with the reason naming the rung and both dates.
>
> **Precondition, stated (a ruling names what exists today):** `swing/latches/service.py` projects `declined` and `criteria_lapsed` onto the `horizon_expired` state with the REASON first-class (`:54-74`), and runs a fill pass (`:1134`). Whether the ledger, for a fill landing AFTER a death rung, preserves the death terminal (with the fill visible separately) or overwrites it with `filled` is a fact the plan READS from the resolution code, not from this ruling. If the ledger cannot express death-then-fill, that is a precondition gap: the structural tier's death-before-fill predicate becomes a follow-on with a primitive to build, and 22-B ships tier 2 plus a structural tier that REFUSES until the primitive exists (fail closed, never a weaker tier-1). Today's only instance (trade 20) is tier 2 and is unaffected either way.
>
> **The AMN geometry is the discriminator fixture** (real breach geometry, per the tri-case-gate rule): fire 08-03, `skip` and close 30.80 below invalidation 30.85 on 08-07, fill 08-07 at 36.43. Under this ruling: a synthetic latch for that mandate with invalidation terminal 08-06 and fill 08-07 → ADMIT structural; terminal 08-07 (same session) → REFUSE; a live latch (no death rung) → REFUSE naming it as a mandate fill. Boundary twins pinned, inequality direction pinned.
>
> ---
>
> ## §4 What RD holds the plan to, beyond the brief's §4
>
> - The §4.3 computed-cohort test is run BEFORE and AFTER trade 20's assignment on the four readers AND `swing hypothesis list`; every N unchanged; 20 named in each exclusion line with reason `unintended_execution`.
> - The witness step 5 (§5) quotes the readers' numbers; RD re-reads the H-cohort counts on the live DB and states them beside the reader's, method named. No expected count is pinned in the script.
> - 22-A2's acceptance cases stay green byte-unchanged (A2-09 pin) — the composition of two `trades`-adjacent rebuilds in one week is exactly the class with no rung; the merge-time composition read asks the runtime question (code × live v40) and states the exclusive-lock result before `git merge`.
> - The trade-process card's per-tab N is quoted WITH the tier page's N until D61 lands (the interim over-count note carries over).
>
> — RD, 2026-09-23

## R0.B — CHARC's §2 scope rulings and §3 fork census (`docs/phase22-arc-b-commissioning-brief.md` @ `9d89e304`, §2 through the end of §3; author CHARC; carries the F1 pin, F4, F5, F6 and 0040's conditions)

> ## §2 SCOPE SHAPE — CHARC's rulings (mine; stated so the census does not re-open them), with RD's F1–F3 encoded where they bind
>
> **IN:**
> - **Migration `0040`: the `trades` REBUILD widening ONE enum token** — under the 22-A2 F12 conditions transposed: (1) the new DDL is the STORED v39 `trades` DDL with EXACTLY ONE textual edit (the IN-list gains `'unintended_execution'`), proven bytewise by a test that applies that one edit to the v39 text; (2) ONE transaction, explicit column list on the INSERT…SELECT (58 columns + none added), row counts equal, every row's tuple identical, `PRAGMA foreign_key_check` empty, the five child tables' row counts unchanged; (3) order: CREATE new → copy → DROP old (takes the five dependants) → RENAME → re-create the FOUR indexes and the ONE trigger VERBATIM from their source migrations (`0014`/`0027`-era for the partial UNIQUE; `0038` for the attempt-id pair); (4) the D51 diff reads EXACTLY TWO changed pairs (the header, the table) PLUS the new table's objects as ADDITIONS, and ZERO deletions — the five `trades` dependants hash identical; (5) the reversibility header names the edit and the gate row `BackupGateSpec(39, "22b", …)`; (6) `EXPECTED_SCHEMA_VERSION` 39 → 40. `trades` has NO AUTOINCREMENT (`id INTEGER PRIMARY KEY`, the rowid-reuse gotcha) — no sequence carry; the plan says so rather than copying 0039's step. **(7) THE F1 PIN (CHARC's ruling, the shape RD expected):** `0040`'s header carries the sha256 of clause (4)'s ratified text; a test derives that hash INDEPENDENTLY from the committed doc `docs/training-epoch-intent-contract.md` (the block-quoted clause (4) EXACTLY as it stands at `rd-rulings-f1-f3.md` @ `e5feec2a` — byte-for-byte, the plan COPIES it, never retypes it) and asserts equality; the migration NEVER reads the docs tree at runtime. Any later edit to the clause is a NEW amendment with a new hash and a new ratification.
> - **The `entry_intent_attestations` table (NEW, append-only, its SCHEMA IS THE EVIDENCE RULE — the 0036 pattern), encoding RD's F2/F3:** one row per assignment of `unintended_execution`; columns: `trade_id` (FK, UNIQUE — one head per trade), `assigned_value` (CHECK = `'unintended_execution'` only — this table records ONLY the evidence-bearing value; the other two are entry-time facts, not assignments), `admission_tier` (CHECK IN `('structural','contemporaneous_record')`); **tier-2 columns:** `instrument_earliest_recorded_ts` + `trade_entry_date` with the SQL bind **`date(instrument_earliest_recorded_ts) > trade_entry_date`** — P1 at SESSION-DATE grain, STRICTLY later; same-session is UNPROVABLE and refuses (F2 S1: `fill_datetime` is synthetic `T16:00:00`, not a clock — never compare against it); **structural columns (F3 (a)):** `cited_latch_link_id` (FK `latch_order_mandate_links`) when a link exists, else `cited_latch_intent_id` (FK `latch_order_intents`), NULL both iff tier 2; **the DEATH-BEFORE-FILL predicate:** `cited_latch_terminal_rung` (CHECK IN `('invalidation','criteria_lapsed','horizon','declined')` — the death rungs ONLY; `fill`/`superseded` never admit) + `cited_latch_terminal_session` with the bind **`cited_latch_terminal_session < trade_entry_date`** (strictly before; same-session is a MANDATE fill under R6 and refuses); `cited_fields_json` (a CLOSED list drawn from the allowlist `notes|why_now|thesis|emotional_state_pre_trade` — F2 edge (ii) puts all four IN; NEVER `pre_trade_locked_at`; NEVER an annotation row) with **F2 S4's bind: at least one of `notes`/`why_now` is cited** (a tag-only citation admits on nothing); `cited_text_snapshot_json` (the cited fields' text AT ASSIGNMENT, so a later edit is visible against the snapshot); `audit_trail_checked_at` + `corrections_touching_cited_fields` (must be 0 — the D36 check the code performs, AND its SQL twin over **BOTH audit tables** (F2 S2): `NOT EXISTS` on `reconciliation_corrections (affected_table='trades', affected_row_id=NEW.trade_id, field_name IN cited)` AND the equivalent over `provenance_corrections`'s columns — the plan reads 0039's DDL for the exact names); `outcome_known_at` (the trade's first exit fill datetime, or NULL if open — **F2 edge (i): an OPEN trade is ADMISSIBLE NOW, `outcome_known_at` written ONCE at assignment, NEVER back-filled; the row carries NO P&L / MFE / MAE — running state is not evidence**) with the SQL bind `trade_entry_date < date(outcome_known_at) OR outcome_known_at IS NULL` (test (a)); `reason`, `applied_at`, `applied_by`. Append-only TRIPLE (`no_update` + `no_delete` + conflict-scoped `no_replace`, the 22-I convention) + every `WHEN` clause `NOT COALESCE(...,0)`. **Both tiers' predicate sets live in the service FIRST and in the triggers as the SQL twin** (AUTHORIZE-THEN-ABORT: trigger set ⊆ service set, closure-checked by a test that enumerates both).
> - **F2 SHARPENING 3 — THE PRECONDITION WALK (a plan task, its output a FINDING, never a channel):** the plan walks EVERY `UPDATE trades` site in `swing/` (the repo's own count was 21 at `reconciliation_auto_correct.py:190`; the plan counts again, by READ, gotcha #36 — a dynamic-SQL writer does not show a column name) and asserts by test that NO site writes `notes|why_now|thesis|emotional_state_pre_trade` outside the audited corrector. If one exists, P2 is fail-open for that field and the cell REPORTS it (the plan ledger names the site) — it does not add an audit channel in this arc.
> - **F3's PRECONDITION READ (a plan task, fail-closed):** whether the latch ledger PRESERVES a death terminal when a fill lands on a later session (the fill visible separately) or OVERWRITES it with `filled` is read from the resolution code (`swing/latches/service.py:54-74`, `:1134`, the fill pass), not from the ruling. If death-then-fill is NOT expressible, the structural tier ships REFUSING (typed message: "the ledger cannot yet prove death-before-fill; tier 2 or a follow-on primitive") and the gap is a register candidate for CHARC — never a weaker tier-1. Trade 20 is tier 2 and unaffected either way.
> - **The assignment surface, CLI ONLY:** `swing trade assign-intent TRADE_ID --value unintended_execution --cite notes,why_now --reason "<r>" [--dry-run]` — the service (a new `swing/trades/entry_intent_assignment.py`, F4-shaped: preflight outside the transaction; the write is ONE `BEGIN IMMEDIATE` inserting the attestation row THEN updating `trades.entry_intent`, both or neither). **The tier is DETECTED, never chosen (three-valued, F3):** a `latch_order_mandate_links` row for the trade's filled order → `structural`, citing the LINK (REQUIRED); no link but a `latch_order_intents` row → `structural`, citing the intent row; neither, and the instrument's first row is on a strictly later session than `entry_date` → `contemporaneous_record`; neither, and the instrument's first row is on or before the entry session → REFUSE ("the instrument existed and did not fire; this is not an unintended execution the record can prove"). A structural citation then ADMITS only under death-before-fill; a live latch or a same/later-session death REFUSES naming the rung and both dates ("a mandate fill is `standard` under clause (1)"). Refusals are typed and name the recovery. The seam obligation: `update_entry_intent` (the existing review-time writer) and both `click.Choice` lists and the web entry form's validation REFUSE `unintended_execution` with a typed message naming `assign-intent` — the value has exactly ONE writer, pinned by a test. The surface refuses when `entry_intent IS NOT NULL` (no relabel path — OUT below).
> - **The #11 mirror sweep** (the §3 pass's manifest, now 7 files + 2 templates, §1's file:line list), classified per site by READ as widened / tight-by-design / value-agnostic; the SQL-vs-Python drift test over `trades.entry_intent`'s CHECK (does one exist today? the plan measures; if not, it is added — the mandatory mirror member); a CSS class for the token; the display maps. **`cohort_intent.py`'s docstring re-points from the archive to `docs/training-epoch-intent-contract.md` — docstring ONLY, no semantic change, and the plan says so** (RD's F1).
> - **The §VII.F record (F1, RATIFIED):** the NEW doc `docs/training-epoch-intent-contract.md` — (i) clauses (1)–(3) quoted VERBATIM from `research-director-context-archive.md` line 84 (preserve the quote), (ii) clause (4) copied byte-for-byte from `rd-rulings-f1-f3.md` @ `e5feec2a`, dated, with the operator's ratification quoted (*"clause (4) is ratified."*, 2026-09-23); RD's charter §7 gets a dated pointer entry (RD's content — the plan PROPOSES the line, RD lands it; the cell does not edit RD's charter). The doc is a docs-only deliverable of the plan cell, committed BEFORE the migration task so the hash test has its input.
> - **Trade 20's assignment at a witnessed gate** (§5).
> - **The 22-A2 acceptance cases stay GREEN byte-unchanged** (A2-09-style pin on 22-A2's own case set) and the four cohort readers' outputs + `swing hypothesis list` are asserted UNCHANGED before/after 20's assignment except that 20 is now NAMED as `unintended_execution` in the excluded-with-reason line (F10's "never silently uncounted", one more reason string).
>
> **OUT:** 22-C (abandonment intent) and 22-E (Demand B annotations) — RD: one schema family, one arc or strictly sequential, NEVER parallel with 22-B; the era model / 22-A2i; trades 19/24/28 (named-pending); any UI (the entry form does not offer the value; the assignment is CLI); the `initial_stop` correction surface; inference of the value from text (`by_design_keywords` untouched); a relabel path for a trade that already carries `standard`/`by_design` (a NON-EMPTY relabel is a NEW evidence class — the 22-A2 F1.1 ruling applies verbatim, and clause (4) says so in its own text); a death-then-fill PRIMITIVE for the latch ledger if F3's precondition read finds it absent (a follow-on, banked); D58's snapshot fence (its own arc); any `trades` change beyond the one enum edit (F6).
>
> ## §3 FORK CENSUS — ALL SIX RULED; the census adds only what it newly finds
>
> - **F1 — WHAT TEXT §VII.F RATIFIES, and how is it pinned. RULED: RD branch (a), clause (4) text at `rd-rulings-f1-f3.md` §1 @ `e5feec2a`; RATIFIED by the operator 2026-09-23 (*"clause (4) is ratified."*); the PIN is CHARC's, ruled in §2 item (7): test-pinned sha256 in `0040`'s header, derived independently from the committed doc, NEVER checked at migration runtime.** RD verified before ruling that H1's registry row needs NO amendment (`H1_COHORT_CLAUSE` already reads STANDARD-intent only) — the amendment is to the CONTRACT, not the criteria; `0034` is untouched. The home is the new live doc (an archive is not a home — harness §2's archive discriminator).
> - **F2 — THE TIER-2 ADMISSIBILITY PREDICATES. RULED: the three CONFIRMED with FOUR sharpenings; both edges ruled** (`rd-rulings-f1-f3.md` §2): S1 P1 at session-date grain, strictly later, same-session refuses; S2 P2 over BOTH audit tables; S3 P2's precondition VERIFIED by the plan's `UPDATE trades` walk, a negative reported as a finding; S4 a DESCRIPTIVE field (`notes`/`why_now`) must be cited; edge (i) open trade ADMISSIBLE now, `outcome_known_at` NULL written once, no running state on the row; edge (ii) `thesis` + `emotional_state_pre_trade` IN the allowlist. Encoded in §2's table bullet.
> - **F3 — THE STRUCTURAL TIER's citation. RULED: branch (a) (link, else intent) PLUS DEATH-BEFORE-FILL** (`rd-rulings-f1-f3.md` §3): terminal rung ∈ the four death rungs with terminal session STRICTLY before `entry_date`; `fill`/`superseded`/live/same-session REFUSE as mandate fills; the ledger's death-then-fill expressibility is a PRECONDITION the plan READS, fail-closed if absent. **The AMN geometry is the discriminator fixture** (fire 08-03, `skip` and close 30.80 below invalidation 30.85 on 08-07, fill 08-07 at 36.43): synthetic latch with invalidation terminal 08-06 + fill 08-07 → ADMIT structural; terminal 08-07 → REFUSE; live latch → REFUSE as a mandate fill. Encoded in §2.
> - **F4 — MODULE + SERVICE SHAPE. RULED (CHARC):** new module `swing/trades/entry_intent_assignment.py` (a §3 new-module tripwire, approved in advance on the F4 conditions: preflight outside the transaction; ONE `BEGIN IMMEDIATE` for the two-row write; callers pinned by test). NOT inside `cohort_provenance_correction.py` (3,414 lines) and NOT a second writer of the cohort keys — this surface writes `entry_intent` and its attestation ONLY.
> - **F5 — THE SINGLE-WRITER SEAM. RULED (CHARC):** `update_entry_intent`, both CLI `click.Choice` lists, the web entry form and the review form REFUSE `unintended_execution` (tight-by-design mirrors, each with a typed message naming `assign-intent`); the model validator ACCEPTS it (widened); the drift test covers the three-valued CHECK. Pinned by a test that greps the five surfaces' Choice/allowlist literals AND by execution (each surface refuses with the message).
> - **F6 — REBUILD SCOPE. RULED (CHARC):** ONE edit to the `trades` DDL. NO other `trades` change rides this rebuild (no column added, no CHECK strengthened, no D58 fence) — a rebuild is the riskiest migration shape and it carries exactly the change it exists for. D58 stays its own arc.
>
> Any further fork the census finds is ADDED to the packet with both branches and a named ruler, not decided in the plan. **A census that finds none says so in one line; the loop opens on that line (the F1 pre-loop gate is already cleared).**

## R0.C — Census (cell)

**Cell:** the 22-B writing-plans implementer, 2026-09-23. **Surfaces:** code at worktree `22-b-plan` (base `9d89e304`); the live DB `%USERPROFILE%/swing-data/swing.db` opened with plain `sqlite3` `mode=ro` URI (never `swing`'s `connect`); three weekly backup images (`backups/swing-202631/32/33.db`) COPIED to the session scratchpad first and opened `mode=ro` there. No Codex. Every count below carries its method; a grep count is a LOWER bound, a read is named as a read.

### C.1 Kept premises re-verified (claim / mechanic that makes it true / surface measured)

| # | claim (brief/ruling) | result | mechanic + surface |
|---|---|---|---|
| K1 | `trades` 58 cols / 14 CHECKs / 3,419 DDL chars | HOLDS | `PRAGMA table_info`; `\bCHECK\b` over `sqlite_master.sql`; live v39 |
| K2 | 5 `trades` dependants | HOLDS — **source correction:** partial UNIQUE from `0014:230` (not "0014/0027-era"; 0027 never touches it); `idx_trades_candidate_id` + `idx_trades_pattern_evaluation_id` from `0021:39,41` (the brief names no source); attempt pair `0038:113,117` | `sqlite_master WHERE tbl_name='trades'` (live) + grep of each name over `swing/data/migrations/*.sql` |
| K3 | 5 tables FK→`trades` | HOLDS | `sql LIKE '%REFERENCES trades%'`, live |
| K4 | intent set NULL 1 / by_design 20 / standard 7; 28 trades, 0 open | HOLDS (all 28 `state='reviewed'`) | `GROUP BY`, live |
| K5 | trade 20 bytes (two-space `notes`, `why_now`, `thesis`, `emotional_state_pre_trade='["distracted"]'`, `hypothesis_label` NULL) | HOLDS | `repr()` of the row, live |
| K6 | `MIN(latch_order_intents.recorded_ts)` = `2026-08-08T20:01:08`, 13 rows, AMN 0 | HOLDS as a FIRST-ROW fact — **but see N1: the instrument was DEPLOYED before 08-07** | live `MIN`/`COUNT`; backups w31/w32/w33 |
| K7 | 0 corrections on trade 20; trades-field corrections only `current_stop`×4, `entry_date`×1; `provenance_corrections` 2 rows (23, 25) | HOLDS | `GROUP BY affected_table, field_name`, live |
| K8 | 0027's CHECK is COLUMN-level inside `trades` DDL → widening is a rebuild | HOLDS; stored DDL begins `CREATE TABLE "trades" (` (the 0014 RENAME product), so the one-edit proof compares STORED v39 text + one edit against STORED v40 text | live `sqlite_master.sql` read |
| K9 | `trades` has no AUTOINCREMENT → no sequence carry | HOLDS (`id INTEGER PRIMARY KEY`) | stored DDL |
| K10 | gate shape `BackupGateSpec(38,"22a2",…)` exists; `EXPECTED_SCHEMA_VERSION=39` | HOLDS (`db.py:102`; gate table `db.py:645-698`); manifest "header" = the `# schema_version N` line of `tests/data/schema_manifest_head.tsv` | read |
| K11 | D51 hash re-types "verbatim" safely | HOLDS: `scripts/schema_manifest.py` drops line-start `--` lines and collapses whitespace before sha256, so re-created dependants hash identical iff their TOKENS match | read of `schema_manifest.py:1-80` |
| K12 | no SQL-vs-Python drift test exists for `trades.entry_intent` | CONFIRMED ABSENT (`test_migration_0027_entry_intent.py:48` hard-codes the constant; nothing compares the stored CHECK to `ENTRY_INTENTS`) → the plan adds it | read of the 0027 test + `tests/trades/test_intent.py` |
| K13 | `H1_COHORT_CLAUSE` + `entry_intent = 'standard'` predicate | HOLDS (`cohort_intent.py:73-87,171-176`) — **but see N2** | read |
| K14 | clauses (1)–(3) at `research-director-context-archive.md` line 84 | HOLDS; exact span = from `(1) A+ fires` through `graded as practice. ` (before `**Epoch-integrity`) | Python slice, `encoding='utf-8'` |
| K15 | clause (4) byte-for-byte at `e5feec2a` | HOLDS: ONE line starting `> **(4)`; body (leading `> ` removed, no newline) = 1,024 UTF-8 bytes, NON-ASCII (em-dash), sha256 `5a78e547f64df25e0f891bd271c8d5e51bbd884866ca6d04c6c85b7803ef3886`; the RD file is byte-identical `e5feec2a`→`9d89e304` | `git show e5feec2a:…` read as BYTES; `git diff --stat` empty |
| K16 | F2 S1: every entry fill is synthetic `T16:00:00` | HOLDS 28/28 (and every exit/stop/trim fill too: 16/12/3) | `substr(fill_datetime,12)`, live |
| K17 | `pre_trade_locked_at` synthetic "28/28 by construction" (RD edge (ii)) | **27/28** — trade 19 is the exception (its `entry_date` was corrected 07-23→07-31 via the coupled surface; `pre_trade_locked_at` kept 07-23). Doctrine unaffected (the field is never citable) | `pre_trade_locked_at = entry_date||'T16:00:00'`, live |
| K18 | F2 S3 precondition: no writer of the four fields outside the audited corrector | **HOLDS** (walk below, C.2) | read |
| K19 | F3 precondition: death-then-fill expressible | **HOLDS — expressible** (C.3) | read of `swing/latches/service.py` + `swing/trades/latched_origin.py` |
| K20 | CLI surface name | `swing trade entry` (not "enter"); `assign-intent` is unused in `trade_group` | `grep '@trade_group.command'` |
| K21 | A2-09 pin | `tests/trades/test_22a2_case_closure.py:79-140` pins 22-A's SIX cases by `inspect.getsource` sha256; 22-B's pin is the same shape over 22-A2's own `CASES_22A2` implementers. Hazard: A2-08's roster regex `a2_(\d{2,3}[a-z]?)` walks ALL of `tests/`, so a 22-B test NAME carrying `a2_<digits>` registers as a phantom | read |

### C.2 F2 S3 — the `UPDATE trades` walk (by READ)

Method: `grep -rin` over `swing/**/*.py` for `update trades`, `trades\s*SET`, bare/trailing `UPDATE` lines, f-string `UPDATE {` builders, `REPLACE INTO trades`, `DELETE FROM trades`, `INSERT INTO trades`; each hit READ; migrations grepped separately. Result — production UPDATE sites and the columns each SETs:

| site | columns written |
|---|---|
| `swing/data/repos/fills.py:211` | `current_size`, `current_avg_cost`, `last_fill_at` |
| `swing/data/repos/trades.py:685` | `current_stop` |
| `swing/data/repos/trades.py:965` (dynamic, fixed list) | the 10 review fields + `failure_mode` |
| `swing/data/repos/trades.py:1008` | `entry_intent` |
| `swing/data/repos/trades.py:1054` | `entry_date` |
| `swing/data/repos/trades.py:1108` | `hypothesis_label`, `candidate_id`, `trade_origin` |
| `swing/diagnostics/backfill_trades_sector_industry.py:303,315` | `sector`, `industry` |
| `swing/trades/entry.py:2257` | `risk_policy_id_at_lock` |
| `swing/trades/state.py:149` | `state` |
| `swing/trades/reconciliation_auto_correct.py:2309` (dynamic) | ANY real column, audited (`reconciliation_corrections` row) — the audited corrector |
| `0014_phase7…sql:89,97,102,110` (one-shot, v13→14) | `state`, `pre_trade_locked_at`, `trade_origin`, size aggregates |

No `REPLACE INTO trades`, no `DELETE FROM trades`; the only INSERT is `insert_trade_with_event` (capture, `trades.py:374-562`). **No site writes `notes|why_now|thesis|emotional_state_pre_trade` outside the audited corrector — P2's precondition holds; no finding to report.** I could not reproduce the comment's "21" at `reconciliation_auto_correct.py:190` (its method is unstated); by the method above there are 10 production statements + the corrector's dynamic builder + 4 one-shot migration statements.

**Two P2 encoding corrections found on the walk (not forks — they make S2's "over the trail" true):**
- **(P2-a)** `provenance_corrections` has NO `affected_table`/`field_name` columns. Its equivalents are `trade_id` + `corrected_fields_json`, whose entries are TABLE-QUALIFIED (`"trades.hypothesis_label"`, …), and its 0036/0039 CHECK pins that array to EXACTLY the three cohort keys — so a provenance row naming a citable text field is SCHEMA-UNREACHABLE today (the leg is required by S2 and is vacuous-by-schema until a future widening). Consequence for §4.2's "planted `provenance_corrections` row on `notes` (raw insert)": a raw insert cannot plant it (CHECK). Encoding: the test plants it on a scratch DB under `PRAGMA ignore_check_constraints=ON`, from the 22-A2 fully-cited fixture (`tests/trades/_cohort_provenance_fixtures.py`) with the array rewritten to include `trades.notes`; the citation-graph trigger's citations are satisfied by the fixture. Challenge invited.
- **(P2-b)** `_handle_multi_field_correction` (`reconciliation_auto_correct.py:2870-2960`) writes ONE audit row per multi-field correction with `field_name` = `'price'` if present else the FIRST key; the other fields appear only as keys inside `pre_correction_value_json`/`applied_value_json`. So `field_name IN cited` alone is FAIL-OPEN for a cited field riding as a non-first key. Encoding: P2 (service AND trigger) matches `field_name IN cited` OR any key of the JSON envelopes in `cited ∪ {'trades.'||f}`. Live incidence: 0 `trades` rows touching a text field (live `GROUP BY`).

### C.3 F3 precondition — death-then-fill IS expressible (file:line)

- `swing/latches/service.py:720-848` `_resolve_terminal`: the NON-fill terminal is the EARLIEST of invalidation / declined / criteria_lapsed / horizon (`order_key` = session, then rank); the fill is returned ONLY when `fill.order_key <= nonfill.order_key` (`:845-847`). A fill STRICTLY AFTER a death returns the DEATH terminal (`clear_reason` = the rung, `clear_session` = its date, `clear_trade_id` None). The windowed rung never matches a post-death fill (`effective_end` bound, `:826-832`); the exact `candidate_id` rung consumes it without recording it (`:834-837`). So the ledger PRESERVES the death; the fill is visible separately (the trade's own entry fill), never on the latch.
- 22-A already asks exactly the F3 question: `swing/trades/latched_origin.py:1181` `find_accepted_latch_order(conn, broker_order_id=…)` (link by the entry fill's envelope order id) → `:2400` `mandate_alive_at(…, fill_session, exclude_trade_ids)` delegating to `derive_latches` with the probe bounded BEFORE the fill session (a same-session breach is outside `bar_bound` → admits, `latched_origin.py:19-29`), returning `clear_reason`/`clear_session`/`probe_evidence`.
- **Encoding:** the structural-LINK tier delegates to those two functions (no second comparison — gotcha #31) and records `probe_evidence` JSON on the attestation (the `provenance_corrections.cited_latch_probe_json` precedent). `criteria_lapsed` reaches a terminal only when the lapse rule is ARMED (`criteria_lapse_armed` default False, `service.py:1242`), so today it is a reachable CHECK member with no live producer.

### C.4 F5 — the refusing-surface count is EIGHT, not five (by READ of every `ENTRY_INTENTS` / `entry_intent` Choice/allowlist site)

1. `swing/cli.py:602-603` `trade entry --entry-intent` `click.Choice`; 2. `swing/cli.py:1558-1559` `trade review --entry-intent`; 3. **`swing/cli.py:1723-1778` `trade backfill-intent`** — a free-text `click.prompt` validated only by `update_entry_intent` (brief omits); 4. `swing/web/routes/trades.py:837-838` entry form; 5. `swing/web/routes/trades.py:3545-3558` review form; 6. **`swing/trades/entry.py:569-576` `EntryRequest.__post_init__`** — the gate of the ONLY insert path (`entry.py:2180-2242`), and it validates against `ENTRY_INTENTS`, so widening the constant OPENS the insert path unless it refuses (brief omits); 7. `swing/data/repos/trades.py:987-992` `update_entry_intent`; 8. **the audited corrector** `reconciliation_auto_correct.py:2309` — `_RESERVED_JOURNAL_FIELDS` (`:200`) does not reserve `("trades","entry_intent")`, so `swing journal correct` can write the value with only a `reconciliation_corrections` row (brief omits). Encoding (same F5 rule, more sites): each refuses with the typed message naming `assign-intent`; (8) via a value-conditional reservation through the existing `_reservation_applies`. Plus: `swing/trades/intent.py:17` `ENTRY_INTENT_DISPLAY` feeds BOTH web `<select>`s (`view_models/trades.py:862,1592`) and its no-drift test asserts equality with `ENTRY_INTENTS` — so the display map is split: LABELS cover all three values, CHOICES exclude the new one (a named `models.py` constant for the assertable-at-entry set).

### C.5 NEW FORKS (each: both branches executable, ONE ruler)

**N1 — P1's "instrument": first ROW or DEPLOYMENT? Ruler: RD.** Measured: the weekly image `backups/swing-202632.db` (mtime 2026-08-03 17:30 HST) is at schema **v33 with `latch_order_intents` PRESENT and 0 rows**; `swing-202631.db` (07-27) is v31. So 21-B's prepared-order instrument was LIVE at least 4 sessions before trade 20's 08-07 entry and first USED 08-08 (K6). Two corroborating facts: 21-A's `latch_view_events` recorded the AMN latch (candidate 11926, detection 2026-08-03) displayed `armed` on 08-01/08-02; and trade 20's entry fill 41 carries a Schwab source envelope dating the order `2026-08-01` (operator-corrected to 08-07), so the ORDER PLACEMENT may itself predate or postdate deployment (deployment is bounded only to (07-27, 08-03]; no in-DB record timestamps a migration's application). RD's own AMN geometry (§3: invalidation close ON 08-07, the fill session) would make the fill a same-session MANDATE fill under R6 had a link existed.
- **(a) First-row grain (as encoded):** `instrument_earliest_recorded_ts = MIN(latch_order_intents.recorded_ts)`; trade 20 ADMITS tier 2 (08-08 > 08-07). The witness is as briefed.
- **(b) Deployment grain:** the instrument's start is a pinned session constant (≤ 2026-08-03 by the w32 image — an upper bound; the header cites the image); tier 2 requires `entry_date` (and, if RD rules it, the order-placement session) STRICTLY BEFORE it. Trade 20 REFUSES ("the instrument existed and did not fire"); the arc ships the surface with trade 20 still NULL and NAMED; witness step 3 becomes a refusal witness and step 4 does not run.

**N2 — clause (4) "counts toward NO hypothesis cohort" vs the code. Ruler: RD.** `cohort_intent.py:171-176`: for every epoch-contract cohort (H2–H5) `cohort_entry_intent` is `None` and `trade_counts_toward_cohort` returns True for ANY `entry_intent`; the SQL half (`metrics/cohort.py:78-83`) applies no intent filter when passed `None`. So an `unintended_execution` trade whose `hypothesis_label` matches H2–H5 COUNTS. RD's F1 check ("lands in NO cohort by the existing predicates") holds for H1 by its criterion and for H2–H5 only by LABEL ABSENCE — true for trade 20 (label NULL), not in general.
- **(a) Exclude in code:** `unintended_execution` is excluded from EVERY cohort — `trade_counts_toward_cohort` returns False for it and the SQL path adds `entry_intent IS NOT 'unintended_execution'` whenever no criterion predicate applies — grounded in clause (4) as a named authority; the four readers + `hypothesis list` get the case. Cohort code CHANGES (the brief said none).
- **(b) No cohort code change:** the assignment service REFUSES a trade whose `hypothesis_label` matches any registered epoch-contract cohort (typed: the label would place it in H<n>, which applies no intent predicate); a test pins the refusal. Residual (declared): a LATER label write (e.g. `correct-cohort-provenance`) can still place an attested trade in an H2–H5 cohort.

**N3 — the "named exclusion" for trade 20 has no mechanism. Ruler: RD.** The readers' named-exclusion output is `tier2_excluded` (`frozen_value_evidence.py:1481` `tier2_cohort_exclusions` → `tier2_cohort_lines`), which names only IN-COHORT (label-matched) trades excluded by a 22-A2 tier-2 replay verdict; intent-filtered trades are dropped silently at `recommendations/hypothesis.py:547-552` (and the tier / journal / card equivalents); trade 20 (NULL label) is in NO cohort's candidate set, so it cannot appear in any per-cohort line. §2's last bullet, §4.3, RD §4 bullet 1 and §5 step 4 ("20 NAMED as `unintended_execution` in the excluded-with-reason line") rest on a surface that does not exist.
- **(a) Per-cohort only:** a label-matched `unintended_execution` trade is named under its cohort's row (needs N2(a)); trade 20 appears in NO line; §4.3 becomes "every N unchanged and 20 in no cohort's candidate set, before and after", with the naming discriminator on synthetic H1-/H2-labelled trades.
- **(b) Program-level line:** each of the four readers and `swing hypothesis list` emits ONE line naming every `unintended_execution` trade regardless of label ("not counted toward any cohort: trade N (unintended_execution)"); trade 20 appears; new output on four surfaces.

**N4 — the single-writer seam in REVERSE: may a generic writer change a trade ALREADY carrying `unintended_execution`? Ruler: CHARC (F5's owner).** Clause (4) forbids relabelling TO the value; nothing covers FROM. Concrete composition defect: under F5 the review form's choices exclude the value, so for an attested trade `review_form.html.j2:129-133` renders no `selected` option, the browser submits `""`, and `routes/trades.py:3553-3621` calls `update_entry_intent(…, None)` — **the first web review CLEARS the attested value**, which is exactly when RD's edge (i) says to assign. `trade review --entry-intent`, `backfill-intent --force` and the corrector reach it too.
- **(a) Terminal for generic writers:** all eight C.4 surfaces refuse to change a row whose CURRENT `entry_intent` is `unintended_execution` (typed: "attested; a reversal is a new evidence class"); the review form renders the attested value read-only and omits the field from the POST.
- **(b) Overwritable:** generic writers may change it; the attestation row persists; the drift reader names "attested, column now <v>".

**N5 — the intent-only structural branch cannot be keyed to the fill as ruled. Ruler: RD.** F3 (a) says "no link, but a `latch_order_intents` row → cite the intent (`place`, and `validity` if present)". But `place`/`decline` rows carry NO broker order id (0033 CHECK: "`place` and `decline` … have observed nothing"), so a place row ties to a fill only by a ticker/date heuristic — the heuristic 22-A's case 4 declined (`test_a_place_intent_without_a_validity_row_falls_through_case_4`); and `mandate_alive_at` needs a LINK (its frozen-value guards). Rows that DO name a broker order: `validity` (accepted), `cancel`, `attest acted_manually`.
- **(a) Key by broker order id:** an unlinked intent matches iff its `actual_broker_order_id` equals the entry fill's envelope order id (`broker_order_id_from_envelope`); the latch is that row's `candidate_id`'s derivation (`build_latch_derivation`), death-before-fill bounded before the fill session with the subject trade excluded — the `mandate_alive_at` discipline minus the link-only frozen-value guards; cite the intent row (and its `validated_place_intent_id`).
- **(b) Structural = link only:** an unlinked intent naming the order REFUSES (typed: "a latch intent without an accepted-order link cannot tie this fill to a mandate; tier 2 cannot apply because the instrument recorded it"); `cited_latch_intent_id` stays in the schema only if RD keeps (a).

Both N5 branches leave trade 20 unaffected (0 AMN links, 0 AMN intents).

### C.6 Encodings the plan will carry (not forks; each challengeable)

- **E1** F1 hash input = the UTF-8 bytes of the clause-(4) line with its leading `> ` removed, no trailing newline; derived from `docs/training-epoch-intent-contract.md` with explicit `encoding`/bytes (the cp1252 DECODE gotcha: the clause is non-ASCII).
- **E2** `outcome_known_at` = the EARLIEST non-entry fill (`action IN ('trim','exit','stop')`). Trade 20's outcome fill is fill 44, `action='stop'`, `2026-08-11T16:00:00` — an `exit`-only encoding would record NULL and treat a closed trade as open.
- **E3** P2 per C.2 (P2-a, P2-b).
- **E4** the structural-link tier per C.3.
- **E5** the F5 surfaces per C.4.
- **E6** the drift reader is a pure function in `entry_intent_assignment.py` exercised by test (no new CLI command beyond `assign-intent`); it compares the cited-text snapshot AND the frozen `trade_entry_date` against live.
- **E7** 0040 re-creates the five dependants from `0014:230`, `0021:39,41`, `0038:113,117`.
- **E8** the 22-A2 composition pin covers `CASES_22A2`'s implementers; no 22-B test name carries an `a2_<digits>` token.

## Round table

| round | tier | model/effort | footer | verdict | findings (C/M/m) | task-bearing | depth (orchestrator-read) | tokens used |
|---|---|---|---|---|---|---|---|---|
| 1 | fast | gpt-5.6-luna / high | present | NEW_CRITICAL_MAJOR_FOUND | 0/16/2 | 18 (all accepted, applied) | 386,359 at return (build 2.1.280, claude-opus-5-5; orchestrator-read `cell_depth.py --live 1`) -- rounds 2-3 re-dispatched to a fresh cell | 293,572 |
| 2 | fast | gpt-5.6-luna / high | present | NEW_CRITICAL_MAJOR_FOUND | 0/8/6 | 11 (all 14 accepted, applied; R2-05 reframed, R2-14 in part) | 370,011 at return (build 2.1.280, claude-opus-5-5; orchestrator-read `cell_depth.py --live 1`) -- round 3 re-dispatched to a fresh cell | 405,845 |
| 3 | fast | gpt-5.6-luna / high | present | NEW_CRITICAL_MAJOR_FOUND | 0/9/1 | 10 (all accepted, applied); R3-09 surfaced fork R0.K, loop STOPPED | 334,797 at return (build 2.1.280, claude-opus-5-5; orchestrator-read `cell_depth.py --live 1`) | 451,928 |

## R0.D — CHARC rules N4: branch (a), value TERMINAL for generic writers (mail 20260923T173456Z; author CHARC; text of record on main in the brief section 3 @ 605fa4db; literal block quote of the mail body, transcribed by the courier)

> 22-B CENSUS ROUND 0 -- CHARC RULES N4 ONLY. N1/N2/N3/N5 are RD's; I hold on them and will speak only to dissent from a LANDED ruling, upward. Verified at the sites before ruling: review_form.html.j2:129-133 (the select is always rendered and always submitted; the empty option is "unclassified"); routes/trades.py:3618-3621 (presence-gated, so "" reaches update_entry_intent as None); repos/trades.py:970-992 (the writer validates membership only, never the CURRENT value); cli.py:1690-1712 (review --entry-intent) and the backfill-intent --force/--trade-id re-prompt path; reconciliation_auto_correct.py:200-208 (_RESERVED_JOURNAL_FIELDS -- the coupled-column mechanism already routes hypothesis_label/candidate_id/trade_origin) and :2300-2309 (the dynamic UPDATE builder). The defect is real; it fires at exactly the moment F2 edge (i) says to assign.
>
> N4 RULING: BRANCH (a). unintended_execution is TERMINAL for every generic writer. The full text is on main in the brief, section 3 "N4" @ 605fa4db (amended by replacement; F5's surface set corrected to the census's EIGHT; section 2 item (4) gains the new trigger as a D51 addition). The shape, so the courier can transcribe it and the cell can build it:
>
> THREE LAYERS, each named to the writer it catches; the SQL layer is the TWIN of the service layer (AUTHORIZE-THEN-ABORT, trigger set within the service set):
> (1) update_entry_intent reads the CURRENT value inside the caller's transaction and raises a typed AttestedIntentError when it is unintended_execution and the new value differs. One check covers the web review route, trade review --entry-intent, and backfill-intent (all three call it). --force / --trade-id SKIP an attested row with the message; they never prompt over it.
> (2) The corrector: ("trades","entry_intent") joins _RESERVED_JOURNAL_FIELDS -- the existing mechanism, its message routing to assign-intent. This is the ONLY layer that reaches the dynamic UPDATE trades SET {field} builder (gotcha 36: a column-name grep cannot see it).
> (3) The SQL twin in 0040: trg_trades_entry_intent_attested_terminal, BEFORE UPDATE OF entry_intent ON trades WHEN COALESCE(OLD.entry_intent,'') = 'unintended_execution' AND NEW.entry_intent IS NOT OLD.entry_intent -> RAISE(ABORT). A NEW trades dependant, created after the RENAME with the five verbatim ones. F6 is UNTOUCHED: the table DDL still carries exactly ONE edit; a trigger is a separate sqlite_master object; the D51 diff shows it as an ADDITION (section 2 item (4) amended). Same-value UPDATE passes (idempotent); NULL -> value passes (the assignment path).
>
> THE REVIEW FORM renders the attested value READ-ONLY with no select and OMITS the field from the POST, so the presence gate preserves it. A handcrafted POST with entry_intent="" for an attested trade gets the typed refusal as a 4xx fragment, value preserved.
>
> THE MESSAGE tells the truth of this arc: "entry_intent is attested (entry_intent_attestations row N); no reversal surface exists -- a reversal is a NEW evidence class with its own record." It does NOT name assign-intent as the recovery: assign-intent refuses a non-NULL row under F5. A mistaken attestation is a register item (mine, banked when the first one exists), not a channel in 22-B.
>
> BRANCH (b) REJECTED: an overwritable attested column makes the attestation row and the journal disagree BY DESIGN, and "attested, column now X" is a report about a defect the schema could have refused.
>
> DISCRIMINATORS for section 4.2 (each in the real row shape): TestClient review POST for an attested trade WITHOUT the field -> preserved; WITH "" -> 4xx typed, preserved; CLI trade review --entry-intent standard on an attested trade -> ClickException with the message; backfill-intent --force -> row skipped, message printed, value preserved; the corrector on ("trades","entry_intent") -> ReservedJournalFieldError; a RAW UPDATE trades SET entry_intent='standard' on plain sqlite3 -> the trigger ABORTS (layer 3 tested ALONE, so the service layer cannot mask it); the same-value UPDATE -> passes.
>
> RULING-FACT CORRECTIONS the cell reported: ACCEPTED as the code has them (the quoted "trades" DDL, the dependant sources, 27/28 synthetic, outcome_known_at from the earliest trim/exit/STOP fill, provenance_corrections' actual columns and the ignore_check_constraints planting, the envelope-keys read for multi-field corrections, F5 = eight). The brief's section 1/2 text they correct is SUPERSEDED by the ledger's R0.C where they differ; the plan builds from the code, and the ledger is the record. F2-S3's precondition HOLDS by the cell's walk and F3's death-then-fill IS expressible (service.py:720-848) -- both are the plan's to pin by test, not to re-argue.
>
> FRESH-CELL RE-DISPATCH: concur. 339,945 at census return leaves no room for the plan and three rounds; a fresh opus-high off the committed ledger plus the five landed rulings is the right shape, and the round-gate depth read stays a GATE for the new cell too.
>
> Courier: block-quote this ruling into the ledger and post the landed commit; the ruling is in flight until then.

## R0.E — RD rules N1, N2, N3, N5 in one packet (`docs/phase22-arc-b-rd-rulings-census-n1-n5.md` @ `7a6f7539` on main, WHOLE; author RD; literal block quote, transcribed by the courier)

> # Phase 22 — Arc 22-B: RD rulings on the round-0 census forks N1, N2, N3, N5 (ONE packet)
>
> **Author:** RD (the named ruler on N1/N2/N3/N5; N4 is CHARC's and is not ruled here). **Date:** 2026-09-23. **Packet answered:** the orchestrator's `20260923T173229Z` query; the census is ledger section R0.C on branch `22-b-plan @ b134d776`. **Status:** RULED, one pass. The courier block-quotes this file into the ledger; each ruling is in flight until that commit is posted back.
>
> Facts re-read by me on 2026-09-23 before ruling (method beside each): 21-B's merge `d5d03bb9` is dated `2026-08-03 01:04:14 -1000` (`git show -s`); the weekly image `backups/swing-202632.db` (mtime 2026-08-03 17:30 HST) is v33 with `latch_order_intents` present at 0 rows, and `swing-202631.db` (07-27) is v31 (opened `mode=ro&immutable=1`); no migration log with timestamps exists (`schema_version` has one column, `version`); trade 20's entry fill 41 carries a Schwab envelope `entry_date 2026-08-01` (the order-ENTERED date, D31) operator-corrected to 08-07; `latch_view_events` for AMN: one row, candidate 11926, detection 2026-08-03, first viewed `2026-08-01T05:09:56`, last `2026-08-02T23:43:45`, state `armed`, **`actionable_ever_viewed = 0`** — the same signature as every FTRE and VSTS row (all 0) and unlike OII and CADL (1); `swing/latches/classification.py:71-72` defines `actionable_ever_viewed = 1` as "an actionable mandate was presented".
>
> ---
>
> ## N1 — What is "the instrument" for P1? Ruling: **NEITHER branch as offered. The semantic is "the structural record could NOT have existed", proven from the instrument's OWN record, in an ordered three-valued probe. First-row grain is REJECTED as a proxy. Trade 20 ADMITS on the primary leg AND on the fallback leg.**
>
> **Why not (a), first row:** it errs toward admission — an instrument deployed and unused for days reads as absent. That is a ruling stated on a proxy, and my own rule is that such a ruling voids itself on the first innocent change (a quiet deployment week would admit anything). **Why not (b) as written:** deployment is the right fallback but the wrong PRIMARY — the question P1 asks is not "did the table exist" but "could the framework have recorded this placement", and for any mandate the 21-A panel displayed, the framework's own telemetry answers that directly.
>
> **The rule, ordered; the FIRST leg that can speak decides:**
>
> 1. **Telemetry leg (primary).** Take the mandate's `latch_view_events` rows for the fire the fill descends from (the AMN row is candidate 11926). If ANY row with `view_session_date` on or before the PLACEMENT session has `actionable_ever_viewed = 1`, the panel presented an actionable prepared order and no intent row was recorded → **REFUSE: "the instrument offered the order and did not fire; this is not an unintended execution the record can prove."** If rows exist and ALL are `actionable_ever_viewed = 0` through the placement session → the framework never offered a recordable placement → the structural record could not exist → **tier 2 admissible.**
> 2. **Deployment leg (fallback, only when the mandate has NO telemetry rows).** Tier 2 requires the PLACEMENT session strictly before the 21-B instrument's deployment session, a pinned constant **`2026-08-03`** whose derivation the plan states in the constant's docstring: lower bound the merge `d5d03bb9` (2026-08-03 01:04 HST), upper bound the w32 image (2026-08-03 17:30 HST, v33, table present, 0 rows) — both bounds on one calendar day, so the session is determined, not estimated. Placement on or after that session with no telemetry and no intent row → **REFUSE** (the instrument existed; nothing says it was not offered).
> 3. **Neither leg can speak** (no telemetry rows AND no placement date derivable) → **UNPROVABLE → REFUSE**, naming the missing fact. Unprovable is not absent.
>
> **The placement session** is the fill envelope's Schwab order-entered date (`schwab_source_value_json.entry_date` — the D31 value, which is the order's ENTERED date and is here exactly the fact wanted), falling back to `trades.entry_date` when no envelope exists. The fallback is conservative: the fill is never earlier than the placement, so substituting it can only make the placement look LATER, i.e. harder to admit.
>
> **Trade 20 under the rule:** leg 1 speaks — AMN telemetry exists, one row, `actionable_ever_viewed = 0`, viewed 07-31/08-01 HST around the 08-01 placement → **ADMITS tier 2.** Leg 2 would also admit (placement 08-01 < deployment 08-03). The witness is as briefed. The attestation row records WHICH leg admitted and its evidence (the telemetry row id, or the constant and the placement date) in the tier-reason JSON, so a replay can re-derive it.
>
> **Discriminators the plan carries (one mutation each, real row shape):** the AMN telemetry row with `actionable_ever_viewed` flipped to 1 → REFUSE naming the offer; the same row deleted (no telemetry) with placement 08-01 → ADMIT on leg 2; no telemetry with placement 08-03 → REFUSE (boundary twin: 08-02 → ADMIT); no telemetry and no envelope with `entry_date` 08-03 → REFUSE; the OII shape (actionable 1, a validity row exists) → not tier 2 at all — routes to the structural tier.
>
> **Precondition stated:** leg 1 depends on the 21-A telemetry's coverage — a mandate whose panel view was never recorded (telemetry unhealthy) falls to leg 2 by construction; that is the correct direction (a missing observation must not admit). The `latch_view_events` table must be append-only for this leg to be evidence; the plan reads its 0032 triggers and states what it found.
>
> ---
>
> ## N2 — Clause (4) "counts toward NO hypothesis cohort" vs the code. Ruling: **(a) — exclude in CODE, both halves, grounded in clause (4) as a named authority. The brief's "no cohort code changes" is WITHDRAWN, and the premise behind it was mine.**
>
> I wrote in F1 that the value "lands in NO cohort by the existing predicates". True for H1 by its criterion; for H2–H5 it was true only by trade 20's label absence, and I generalized from one row. The cell caught it; own it. A ratified doctrine sentence that the code does not enforce is a docstring with a signature on it — exactly branch (b) of F1, which I refused.
>
> **(b) is refused because it is fail-open by declaration:** a refusal at assignment time plus a declared residual that a later label write re-admits the trade is a guard the next arc can walk around without noticing. Clause (4) says NO cohort; the readers must say it.
>
> **Encoding, so D29's design is honoured rather than bypassed:** the exclusion is NOT a blanket filter justified by one authority. For epoch-contract cohorts the predicate gains `entry_intent IS NOT 'unintended_execution'` (NULL still counts, as today — NULL is unclassified, not excluded), grounded in the contract's clause (4) exactly as H1's `entry_intent = 'standard'` is grounded in its criterion text: a `CONTRACT_EXCLUSION_CLAUSE` constant pinned by test against the committed contract doc, so a future doctrine edit that drops the sentence FAILS the test rather than leaving a filter citing an authority that no longer says it. `trade_counts_toward_cohort` returns False for the value on every cohort; the SQL half (`metrics/cohort.py`) applies the same predicate when no criterion predicate applies; the four readers and `swing hypothesis list` get the case. `cohort_intent_authority` for H2–H5 still answers `epoch_contract` — the authority did not change, its text did.
>
> ---
>
> ## N3 — Trade 20 cannot appear in any existing named-exclusion line. Ruling: **(a) per-cohort naming, with N2(a). My §4 bullet 1, the brief's §4.3 and §5 step 4 are RESTATED; (b) is refused.**
>
> The line I wrote ("20 NAMED in each reader's exclusion line") was a channel I described without reading — my own 09-08 rule, violated in the packet that cited it. The 22-A2 exclusion lines name LABEL-MATCHED trades dropped by a tier-2 replay; trade 20 has no label and is in no candidate set. Nothing to name it in.
>
> **Restated acceptance (replaces §4.3 and my §4 bullet 1):** before and after trade 20's assignment, every reader's N is unchanged AND trade 20 is in NO cohort's candidate set (asserted, not inferred). The naming discriminator runs on SYNTHETIC label-matched trades — one H1-labelled, one H2-labelled, each with `entry_intent = 'unintended_execution'` — which must appear under their cohort's row as excluded with reason `unintended_execution` (N2(a)'s output) and must not count. **Witness step 4 restated:** `journal` (or the trade view) shows trade 20 rendering its OWN label `unintended_execution` (F5's display map — never `Unclassified`, never `standard`); `swing hypothesis list` shows every N unchanged from the pre-assignment quote; the attestation row is read back.
>
> **Why not (b):** four surfaces gain a new output line for a population that today has zero label-matched members, and "never silently uncounted" is already served: the trade renders its own intent label wherever a trade renders, and clause (4) routes its result to the trade-process card's facet. A program-level line is a surface for a future case; it is not bought on the case that does not need it.
>
> ---
>
> ## N5 — The intent-only structural branch. Ruling: **(b) — structural = the LINK only. An unlinked order-naming intent REFUSES, typed, naming the recovery. `cited_latch_intent_id` is DROPPED from the schema; `cited_latch_link_id` is the structural citation.**
>
> My F3 clause "no link but a `latch_order_intents` row → cite the intent row" was written against a table I had not read: 0033's CHECK forbids a broker order id on `place`/`decline` rows, so a place row ties to a fill only by a ticker/date heuristic — identity asserted from a date, which my own standing rule forbids in both directions, and the heuristic 22-A's case 4 already declined. Withdrawn.
>
> **Why not (a):** it keys by order-id equality (proven identity — admissible in principle) but then re-derives death-before-fill "minus the link-only frozen-value guards" — a second comparison path beside `mandate_alive_at`, the gotcha-#31 shape, and one that drops exactly the guards that make the link tier sound. A weaker structural tier is worse than none: it would carry the structural LABEL on evidence the link tier would refuse.
>
> **The refusal's recovery is 22-A's own admitting path:** a validity/cancel/attest row naming the fill's order without a link is a LEGACY shape (links are minted at acceptance since 0037); if such a row ever needs structural admission, the primitive is a link backfilled under 22-A's rules from that row — a follow-on with its own record, not a heuristic in 22-B. Fail closed; the message names it. Trade 20 is unaffected (0 AMN links, 0 AMN intents).
>
> ---
>
> ## Ruling-fact corrections I accept, and one encoding I adopt
>
> - `pre_trade_locked_at` is synthetic on **27/28**, not "28/28 by construction" — trade 19's coupled entry-date correction moved `entry_date` and left the lock stamp. My sentence was a count I had not run. Doctrine unaffected; the field is never citable.
> - **E2 adopted:** `outcome_known_at` = the EARLIEST non-entry fill (`trim`/`exit`/`stop`). Trade 20's outcome is fill 44 (`stop`, 2026-08-11); an exit-only encoding would have recorded a closed trade as open — the direction that flatters, refused.
> - **P2-a / P2-b adopted** (the `provenance_corrections` leg is schema-vacuous today and planted under `ignore_check_constraints` from the 22-A2 fixture; multi-field corrections match on envelope keys too). Live incidence 0 on both; the predicate is over the trail, not over today's writers.
> - **C.4's EIGHT refusing surfaces** (not five) are CHARC's F5 scope; noted here only because my F2 precondition walk relied on the same read — the walk found no unaudited writer of the four citable fields, so P2's precondition HOLDS (the cell's C.2, method stated). No finding.
> - The K17/K5 byte facts and the K15 hash (`5a78e547…3886`, 1,024 UTF-8 bytes, non-ASCII) are the cell's; CHARC's pin test derives its own.
>
> **Consequence for CHARC's lane, flagged not ruled:** N2(a) changes `swing/metrics/cohort_intent.py` and `swing/metrics/cohort.py` — cohort code the brief's §2 said would not change. That is scope his §3 pass and §2 bullet must absorb; the tripwire set is unchanged (no new module, no new schema beyond 0040 and the attestation table).
>
> — RD, 2026-09-23

## R0.F — New fork(s) (fresh cell)

**Cell:** the 22-B writing-plans implementer (fresh), 2026-09-23. **Surfaces:** code at `22-b-plan @ 902ac83d`; brief text of record `bd4314ea` (read via `git show`, not merged; the orchestrator merges); live DB opened with plain `sqlite3` `mode=ro` URI (v39). Three forks are OPEN. Per the orchestrator's instruction (HOLD round 1) the plan is written with each fork as a MARKED drop-in sub-task; no Codex round runs until they are ruled.

### R0.F-1 — `latch_view_events` is not append-only; N1 leg 1 rests on it. Ruler: **RD.** (Measured by CHARC; relayed by the orchestrator; recorded here as instructed.)

Facts re-read by this cell: the table's only triggers are `trg_lve_identity_coherent_insert` / `_update` (identity block only; live `sqlite_master`); the writer `swing/data/repos/latch_view_events.py:record_view` is SELECT-then-UPDATE-or-INSERT, with `actionable_ever_viewed` kept monotonic by `MAX(actionable_ever_viewed, ?)` in the writer's UPDATE (`:157-168`), not by the schema; nothing blocks a DELETE. **The orchestrator asked what the writer's INSERT carries: it is a plain `INSERT INTO latch_view_events (...) VALUES (...)` (`:179-190`) with NO `ON CONFLICT` clause; the lost-insert race is caught as `sqlite3.IntegrityError` (`:191`) and re-routed to the UPDATE path.**

- **(a) Belts in 0040:** `trg_lve_actionable_ever_viewed_monotonic` (`BEFORE UPDATE OF actionable_ever_viewed ... WHEN NOT COALESCE(NEW.actionable_ever_viewed >= OLD.actionable_ever_viewed, 0)` -> ABORT) + `trg_lve_no_delete` (`BEFORE DELETE` -> ABORT); the D51 diff gains two ADDITIONS; F6 untouched. **The cell's finding against (a) as drafted:** a DELETE trigger alone is FAIL-OPEN to `INSERT OR REPLACE` (the CLAUDE.md REPLACE facet: `recursive_triggers` is OFF, so REPLACE's implicit delete fires no DELETE trigger), so a `REPLACE` on the `(candidate_id, view_session_date, surface)` UNIQUE key rewrites a 1 to a 0 past both belts. **(a') = (a) plus `trg_lve_no_replace`**, conflict-scoped: `BEFORE INSERT ... WHEN EXISTS (SELECT 1 FROM latch_view_events WHERE candidate_id = NEW.candidate_id AND view_session_date = NEW.view_session_date AND surface = NEW.surface)` -> ABORT. It is COMPATIBLE with the writer: the writer uses no `ON CONFLICT` (the third-facet hazard does not arise), and `RAISE(ABORT)` surfaces as `sqlite3.IntegrityError`, which the lost-race handler already catches and re-routes to UPDATE, so the handler's behaviour is unchanged (a plan test pins it). Discriminators for (a)/(a'): a raw `UPDATE ... SET actionable_ever_viewed = 0` on a 1-row -> ABORT; the writer's own 0->1 merge -> passes; a same-value UPDATE -> passes; a raw `DELETE` -> ABORT; (a') a raw `INSERT OR REPLACE` of an existing key -> ABORT; the writer's first-insert race (two connections) -> still merges, `view_count` = 2.
- **(b) Declared accepted limitation:** leg 1 stands; the AL reads *"telemetry monotonicity is a WRITER property (`record_view`'s `MAX`), not a schema property; a raw UPDATE/DELETE/REPLACE on `latch_view_events` can change what leg 1 reads. Mitigation: the attestation row SNAPSHOTS the leg-1 rows it read (ids and `actionable_ever_viewed` at read), so a later mutation is visible against the snapshot at replay."*

### R0.F-2 — N1 leg 1 as ruled does not speak for trade 20; which telemetry rows are EVIDENCE? Ruler: **RD.**

Facts (live `mode=ro`, this cell; each contradicts an incidental fact in R0.E N1): (i) the AMN row (`view_event_id` 5, candidate 11926) has **`view_session_date = 2026-08-03`**, while the placement session is the envelope's **`2026-08-01`** (fill 41 `schwab_source_value_json.entry_date`; a SATURDAY). Under the ruled text ("rows with `view_session_date` on or before the PLACEMENT session") the AMN row is OUTSIDE the window: zero rows qualify. (ii) `viewed_ts` is LOCAL naive time (`swing/web/routes/latches.py:56-58,355`, `datetime.now()`), so the AMN views ran **2026-08-01T05:09 to 08-02T23:43 HST**, not "07-31/08-01 HST". (iii) **The AMN row was written BEFORE migration 0033** (last view 08-02T23:43 HST; 21-B's merge `d5d03bb9` has COMMITTER date 2026-08-03 01:04 HST, while its AUTHOR date is 07-31 00:06, which is why `%ad` and `%cd` disagree), so its three `actionable_*` columns are **0033's LEGACY BACKFILL `0`** (`0033_latch_order_intents.sql:154-171`), which that migration's own header defines as *"no actionable presentation is RECORDED for this row"*: an absence of observation, not an observed non-offer. Rows 1-5 (FTRE and VSTS twice each, AMN once) are pre-0033; rows 6 onward are post. **Consequence: "trade 20 ADMITS on the primary leg" does not hold under the ruled text; trade 20 ADMITS on the deployment leg (placement 08-01 < 08-03) under both branches below.** The witness `admitted_leg` read-back depends on the ruling.

- **(a) Leg 1 speaks only through rows RECORDED under the actionability instrument, windowed on the view clock:** a row with `actionable_ever_viewed = 1` -> REFUSE at any date (an observed offer); a row with `actionable_ever_viewed = 0` counts as a "not offered" observation only when `date(first_viewed_ts) > '2026-08-03'` (strictly after the deployment session, the same pinned constant as leg 2, since 0033 IS the 21-B instrument); window = the trade's ticker's rows with `date(first_viewed_ts) <= placement_date`. No speaking row -> leg 2. Trade 20: the AMN row does not speak -> leg 2 -> ADMIT, `admitted_leg = 'deployment'`.
- **(b) The ruled text, literally:** window = the ticker's rows with `view_session_date <= placement_date`; backfilled zeros count as observations; no windowed row -> leg 2. Trade 20: no windowed row -> leg 2 -> ADMIT, `admitted_leg = 'deployment'`. Residual: a post-0033 weekend placement whose views carry the NEXT session date is excluded from its own window and falls to leg 2 (fail-closed after 08-03: it REFUSES there).
- (c), for completeness, the reading RD's worked case assumed (a view wall-clock window, backfilled zeros count) -> trade 20 admits on `telemetry`. The cell does not offer it as a branch because it admits on a value 0033 itself labels unrecorded, but it is the ruler's to take.

Both (a) and (b) need the FIRE scoping stated: the plan encodes "the trade's TICKER's rows in the window" (every fire of that ticker; any 1 refuses), because in the tier-2 case no link exists to name the fire (N5: identity is never asserted from a date). Challenge invited.

### R0.F-3 — three 22-A2 acceptance cases are HEAD-coupled; "byte-unchanged" (brief §4.8, RD §4 bullet 3) is unsatisfiable for them under ANY v40 migration. Ruler: **CHARC.**

Found by reading every `CASES_22A2` implementer's source for version/head tokens (a script over the registry's 106 ids, AST function bodies read). Three go red under 0040 by construction, whatever 0040 contains:
- **A2-18** `test_a2_18_the_manifest_diff_is_three_changed_objects_and_nothing_else` migrates v38 to HEAD and asserts `missing == unexpected == {}` and exactly 0039's three changed objects; 0040 adds the new table and trigger(s) and changes `trades`.
- **A2-19** `test_a2_19_the_committed_fixture_is_the_v39_head` asserts `"# schema_version 39\n"` in the committed fixture; D51 requires the fixture regenerated to v40 in the migration commit.
- **A2-22** `test_a2_22_the_post_base_roster_is_one_22a2_row_after_the_base_23` asserts the live gate table equals base plus EXACTLY the one 22a2 row; condition (5) adds `BackupGateSpec(39, "22b", ...)`.
(Of the other 103: thirteen more reach HEAD (A2-10..15, 20, 25, 29, 31, 60, 101, 102, via `_migrate_to_head` / `head_create_statement` / `ensure_schema`), and by the lines read each asserts only 0039-owned objects, so they are not coupled to the head VERSION; that they stay green is proven by EXECUTION on the 22-B tree (plan Task 12), not by this read. The composition pin keeps all 103 byte-unchanged.)

- **(a) Re-scope the three to their OWN migration, once, so no later arc meets them again:** A2-18 migrates v38 to **39** explicitly (not HEAD); A2-19 keeps its id, is renamed `test_a2_19_the_committed_fixture_matches_head` (the naming rule: a HEAD-tracking test carries no number) and drops the `39` literal; A2-22 asserts the 22a2 row is the FIRST post-base row (a prefix, not the whole tail). The 22-B composition pin records the three as NAMED exemptions with their new sha256; the other 103 stay byte-unchanged.
- **(b) Minimal literal bump:** edit the three to v40 facts (A2-18's expected diff grows by 0040's objects; A2-19 reads `40`; A2-22's post-base tuple gains the 22b row); the same named exemptions in the pin. Every later migration arc re-edits them.

### R0.F-4 — corrections and encodings from the fresh cell (not forks; each challengeable)

- **Brief `bd4314ea` is internally stale in one bullet:** §2 "The assignment surface" still reads "no link but a `latch_order_intents` row -> `structural`, citing the intent row" and the first-row P1 rule. R0.E N1/N5 and §2's own table bullet supersede it; the plan encodes the rulings. Also §4.5 "each of the five refusing surfaces": the surface set is EIGHT (N4 amendment, R0.D); the plan encodes eight.
- **E9** Tier detection needs the entry fill's broker order id (`broker_order_id_from_envelope`, `latched_origin.py:662`). A trade with NO envelope order id can be neither tied to nor untied from a link; encoded FAIL-CLOSED: if any link or order-naming intent exists for the trade's ticker with `detection_date <= entry_date`, REFUSE ("the fill carries no broker order id; a recorded order for this ticker exists"); otherwise proceed to the tier-2 legs with `placement_session = entry_date` (the conservative fallback R0.E N1 names).
- **E10** The seam lands BEFORE the widening: a new `ENTRY_INTENTS_ASSERTABLE` (the two entry-time values) is what the eight generic surfaces validate against, introduced first as a behaviour-preserving refactor; `ENTRY_INTENTS` widens only in the migration task, so no commit ever has the value accepted by a generic writer.
- **E11** The error types live in `swing/data/models.py` beside `ENTRY_INTENTS` (the schema-enum home; the repo writer cannot import `swing/trades/` upward): `EntryIntentSeamError(ValueError)` (a generic writer asked to write the value; the message names `swing trade assign-intent`) and `AttestedIntentError(ValueError)` (the N4 message, naming the attestation row id). Both subclass `ValueError`, so every existing `except ValueError` boundary (the CLI `ClickException` wrap, the web 4xx) keeps working.
- **E12** Naming output (N3 (a)): a NEW sibling field `intent_excluded: tuple[tuple[int, str], ...]` on the four readers' result types and ONE renderer line `not counted: trade N (unintended_execution)`; NOT folded into `tier2_excluded`, which is 22-A2's replay-verdict channel counted by `tier2_count_marker` as excluded/unverifiable (merging would mislabel an intent exclusion as a tier-2 verdict).
- **E13** The SQL half (N2 (a)): `list_trades_for_cohort` gains `exclude_entry_intents: Collection[str] = ()` (default EMPTY, so the process card's observational callers at `metrics/process.py:590,598` and `count_per_cohort` stay unfiltered, per D29 and clause (4)'s "reported in the trade-process card's own facet"); the governed readers pass `cohort_excluded_entry_intents(name)`. The predicate is `entry_intent IS NOT ?` per excluded value (NULL-safe: NULL counts).
- **E14** The corrector reservation (N4 layer 2) is UNCONDITIONAL on `("trades","entry_intent")`, as ruled; live corrector writes to `entry_intent`: 0 (R0.C K7's `GROUP BY affected_table, field_name`), so no replay breaks.
- **E15** The contract doc `docs/training-epoch-intent-contract.md` is committed by THIS cell with the plan (brief §2: "a docs-only deliverable of the plan cell"). Pin input reproduced from the written file: 1,024 UTF-8 bytes, sha256 `5a78e547f64df25e0f891bd271c8d5e51bbd884866ca6d04c6c85b7803ef3886` (read as BYTES, one trailing `\r` stripped for autocrlf checkouts), equal to R0.C K15.

## R0.G — RD rules the N1 telemetry precondition: CHARC's two belts ADOPTED into 0040 (author RD; courier transcription, literal block quotes)

### R0.G.1 — the replaced precondition paragraph (`docs/phase22-arc-b-rd-rulings-census-n1-n5.md` @ `c33bd6e7`, N1; supersedes by replacement the paragraph quoted in R0.E)

> **Precondition stated:** leg 1 depends on the 21-A telemetry's coverage — a mandate whose panel view was never recorded (telemetry unhealthy) falls to leg 2 by construction; that is the correct direction (a missing observation must not admit). **The evidence property leg 1 needs is NOT append-only — the table is an upsert tally by design (`swing/data/repos/latch_view_events.py:110-125`) and cannot be. It is narrower: `actionable_ever_viewed` is MONOTONIC (a 0 read today proves every earlier read was 0) and the row cannot VANISH.** Measured 2026-09-23 (CHARC's finding, re-read by me): the live table carries only the two identity-coherence triggers; the monotonic merge (`MAX`, `:167`) and the lost-insert recovery (`:190-205`, the loser takes the UPDATE path so an actionable render is never dropped) are WRITER properties, and the standard says immutability is verified against the schema or the trail, never inferred from writer-absence. **RULING (replaces the sentence that stood here): 0040 adds CHARC's two belts to `latch_view_events` — `trg_lve_actionable_ever_viewed_monotonic` (BEFORE UPDATE OF that column, `WHEN NOT COALESCE(NEW.actionable_ever_viewed >= OLD.actionable_ever_viewed, 0)` → ABORT; the SQL twin of the writer's MAX, so no production path aborts) and `trg_lve_no_delete` (BEFORE DELETE → ABORT). No BEFORE INSERT belt (insert is not the exposure; the plan reads the writer's SELECT-then-INSERT and says so). The declared-limitation alternative is DECLINED: a table that becomes evidence earns the barriers, and these cost a dozen lines in a migration already open. With the belts, leg 1's premise is structural. The identity triggers stay; F6 untouched (not the `trades` DDL); the D51 diff gains two additions. Discriminator: a raw `UPDATE latch_view_events SET actionable_ever_viewed = 0` on AMN's row 5 over plain sqlite3 ABORTS; a raw DELETE of it ABORTS; the writer's 0→1 UPDATE passes.**

### R0.G.2 — RD's ruling mail (20260923T174156Z, body whole)

> PRIMARY: RD ruled. Orchestrator: round 1 may open once this lands in the ledger.
>
> N1 PRECONDITION: CHARC is right, and it was my sentence. "Append-only" is false of a tally table by construction; what leg 1 needs is MONOTONIC + NO-VANISH on actionable_ever_viewed, and today both are writer properties (MAX at :167; the lost-insert loser takes the UPDATE path at :190-205 -- verified by me, so no production path leaves a 0 where an actionable render happened). Writer-absence is the inference the standard forbids.
>
> RULING, ONE LINE: ADOPT CHARC's two belts into 0040 -- trg_lve_actionable_ever_viewed_monotonic (BEFORE UPDATE OF the column, WHEN NOT COALESCE(NEW >= OLD, 0) -> ABORT) and trg_lve_no_delete (BEFORE DELETE -> ABORT). NO BEFORE INSERT belt. The declared-limitation alternative is DECLINED (a table that becomes evidence earns the barriers). Identity triggers stay; F6 untouched; D51 gains two additions. Discriminators: raw UPDATE ...=0 on AMN row 5 ABORTS; raw DELETE ABORTS; the writer's 0->1 passes.
>
> Recorded by REPLACEMENT of the precondition sentence in docs/phase22-arc-b-rd-rulings-census-n1-n5.md N1 (this commit); courier block-quotes the replaced paragraph into the ledger beside R0.E.

*Courier note: CHARC absorbed this into the brief's 0040 bullet by replacement at `03faa9a2` (main) with four discriminators; no dissent (mail 20260923T174319Z).*

## R0.H — RD rules R0.F-1 (a') and R0.F-2 (a) (author RD; courier transcription, literal block quotes)

### R0.H.1 — the replaced paragraphs (`docs/phase22-arc-b-rd-rulings-census-n1-n5.md` @ `8644affa`, N1; the lines added by `c33bd6e7..8644affa`, each quoted whole; they supersede by replacement the corresponding text in R0.E/R0.G.1)

> 1. **Telemetry leg (primary) — RE-STATED BY REPLACEMENT 2026-09-23 (R0.F-2, ruled (a)).** A `latch_view_events` row SPEAKS for leg 1 only if it was RECORDED UNDER THE ACTIONABILITY INSTRUMENT: `date(first_viewed_ts) >= 2026-08-03` (the same deployment constant as leg 2 — 0033 added the actionability columns and BACKFILLED every earlier row to 0, which its own header defines as "no actionable presentation is RECORDED", a deployment fact restated, not an observation; live rows 1–5 are that backfill). The window is by VIEW TIMESTAMP, not `view_session_date` (the action session the latch was displayed under, which can post-date the view); timestamps are LOCAL NAIVE (HST, `routes/latches.py:56-58`) and are compared at date grain with the placement session. Among the speaking rows for the mandate: any row with `date(first_viewed_ts)` on or before the placement session and `actionable_ever_viewed = 1` → **REFUSE** ("the instrument offered the order and did not fire"; `ever` carries no ordering claim, so an offer that may have preceded placement refuses — the safe direction); speaking rows exist with a view on or before placement and ALL are 0 → **tier 2 admissible**; rows whose first view is AFTER placement say nothing about the pre-placement state → **silent**. No speaking row → leg 2.
>
> **Trade 20 under the rule (CORRECTED 2026-09-23 — my worked example was wrong twice: the AMN row's views ran 2026-08-01 05:09 to 08-02 23:43 HST, not "07-31/08-01", and its 0 is the 0033 backfill, not an observed non-offer):** AMN's only row (id 5) has `first_viewed_ts` 2026-08-01 < 2026-08-03 → it is a backfilled row → **leg 1 is SILENT**. Leg 2: placement 2026-08-01 (fill 41 envelope) < deployment 2026-08-03 → **ADMITS tier 2 on LEG 2**, `admitted_leg = deployment`. The witness step 3 prints leg 2 and the constant's derivation; the operator's read-back names it. The outcome is unchanged; the EVIDENCE the row cites is not, and that is the point of recording the leg. The attestation row records WHICH leg admitted and its evidence (the telemetry row id, or the constant and the placement date) in the tier-reason JSON, so a replay can re-derive it.
>
> **Precondition stated:** leg 1 depends on the 21-A telemetry's coverage — a mandate whose panel view was never recorded (telemetry unhealthy) falls to leg 2 by construction; that is the correct direction (a missing observation must not admit). **The evidence property leg 1 needs is NOT append-only — the table is an upsert tally by design (`swing/data/repos/latch_view_events.py:110-125`) and cannot be. It is narrower: `actionable_ever_viewed` is MONOTONIC (a 0 read today proves every earlier read was 0) and the row cannot VANISH.** Measured 2026-09-23 (CHARC's finding, re-read by me): the live table carries only the two identity-coherence triggers; the monotonic merge (`MAX`, `:167`) and the lost-insert recovery (`:190-205`, the loser takes the UPDATE path so an actionable render is never dropped) are WRITER properties, and the standard says immutability is verified against the schema or the trail, never inferred from writer-absence. **RULING (replaces the sentence that stood here): 0040 adds CHARC's two belts to `latch_view_events` — `trg_lve_actionable_ever_viewed_monotonic` (BEFORE UPDATE OF that column, `WHEN NOT COALESCE(NEW.actionable_ever_viewed >= OLD.actionable_ever_viewed, 0)` → ABORT; the SQL twin of the writer's MAX, so no production path aborts) and `trg_lve_no_delete` (BEFORE DELETE → ABORT). **A conflict-scoped BEFORE INSERT belt IS required (R0.F-1, ruled (a′), replacing "no BEFORE INSERT belt"):** `trg_lve_no_replace` — BEFORE INSERT WHEN EXISTS a row on the table's UNIQUE key → RAISE(ABORT). My "insert is not the exposure" missed the CLAUDE.md REPLACE gotcha I had in front of me: `INSERT OR REPLACE`'s implicit delete fires NO DELETE trigger with `recursive_triggers` OFF, so the two belts alone leave a REPLACE that resets `actionable_ever_viewed` 1→0 reachable — the 22-I convention is a TRIPLE and I adopted two of it. The writer (`repos/latch_view_events.py:179-191`) is a plain INSERT with no `ON CONFLICT`, so the third facet of that gotcha (a barrier that cannot see `DO NOTHING`) does not bite; its lost-race `except sqlite3.IntegrityError` already catches the trigger's ABORT (SQLITE_CONSTRAINT) and routes to the UPDATE path — the writer is UNCHANGED, and the plan proves that BY EXECUTION (plain INSERT on an existing key → the handler's UPDATE path runs; `INSERT OR REPLACE` on AMN's key with `actionable_ever_viewed 0` → ABORTS). The residual-AL branch (a) is DECLINED for the same reason as before: the table is evidence. The declared-limitation alternative is DECLINED: a table that becomes evidence earns the barriers, and these cost a dozen lines in a migration already open. With the belts, leg 1's premise is structural. The identity triggers stay; F6 untouched (not the `trades` DDL); the D51 diff gains two additions. Discriminator: a raw `UPDATE latch_view_events SET actionable_ever_viewed = 0` on AMN's row 5 over plain sqlite3 ABORTS; a raw DELETE of it ABORTS; the writer's 0→1 UPDATE passes.**

### R0.H.2 — RD's ruling mail (20260923T175654Z, body whole)

> PRIMARY: orchestrator (courier). CC charc (successor seat; R0.F-3 is yours, untouched here).
>
> R0.F-1 -- RULED (a'): the two belts PLUS trg_lve_no_replace (BEFORE INSERT WHEN EXISTS a row on the UNIQUE key -> RAISE(ABORT)). My "no BEFORE INSERT belt" missed the CLAUDE.md REPLACE gotcha; the 22-I convention is a TRIPLE and I adopted two of it. The writer is a plain INSERT (no ON CONFLICT), so the DO-NOTHING facet does not bite, and its IntegrityError handler already routes the ABORT to the UPDATE path -- writer UNCHANGED, proven BY EXECUTION (plain INSERT on an existing key -> UPDATE path runs; INSERT OR REPLACE on AMN's key with 0 -> ABORTS). Residual-AL branch DECLINED.
>
> R0.F-2 -- RULED (a): a latch_view_events row speaks for leg 1 ONLY if recorded under the actionability instrument: date(first_viewed_ts) >= 2026-08-03 (the same constant as leg 2; 0033 backfilled every earlier row to 0 = "no actionable presentation RECORDED", a deployment fact, not an observation; live rows 1-5 are that backfill). Window by VIEW TIMESTAMP at date grain (local naive HST), never view_session_date. Speaking row with ever=1 on/before placement -> REFUSE (ever carries no ordering claim; refuse is the safe direction); speaking rows all 0 with a view on/before placement -> admissible; first view after placement -> silent. TRADE 20: row 5 first_viewed 08-01 < 08-03 -> backfilled -> leg 1 SILENT -> leg 2 ADMITS (placement 08-01 < deployment 08-03), admitted_leg = deployment; witness step 3 prints leg 2. Outcome unchanged; the cited evidence changes, which is why the leg is recorded. MY WORKED EXAMPLE WAS WRONG TWICE (the "07-31/08-01" timestamps; "admits on leg 1") -- corrected by replacement in the doc.
>
> Both landed by REPLACEMENT in docs/phase22-arc-b-rd-rulings-census-n1-n5.md N1 (this commit); courier block-quotes the three replaced paragraphs into the ledger beside R0.G. R0.F-4: the brief's assignment-surface bullet is CHARC's to check at 03faa9a2; "eight" is right.
>
> NOTE FOR CHARC, not a ruling: 0033's header says latch_view_events "held ZERO rows on the live DB at this writing" (07-30) -- at MIGRATION time (08-03) it held rows 1-5, so the backfill DID move rows on production. True when written, false when run; the R0.F-2 ruling rests on it being a backfill, which the timestamps prove.

## R0.I — CHARC rules R0.F-3 (a) and answers R0.F-4 (mail 20260923T175822Z, body whole; author CHARC; text of record the brief @ `0f317868` on main; courier transcription, literal block quote)

> 22-B R0.F -- CHARC RULES R0.F-3 ONLY (F-1/F-2 are RD's, landed at 8644affa; no dissent; both absorbed into the brief by replacement -- see the sha in the subject). R0.F-4 answered at the end.
>
> R0.F-3 -- RULED: BRANCH (a). Re-scope the three ONCE to their OWN migration; they never move again.
>
> WHY: A2-18 (migrate to HEAD, assert exactly 0039's diff), A2-19 (the literal "# schema_version 39" in the committed fixture + fixture == HEAD; read at tests/data/test_migration_0039_provenance_corrections_tier2.py:338-350), A2-22 (the gate table holds exactly one post-base row) are HEAD claims wearing 0039's name -- the stale-_is_NN class the version-mirror rule already names (CHARC-ruled 2026-09-07; rule text at tests/data/test_migration_0035_fills_trades_price_divergence.py:62: a version NUMBER belongs in a test ONLY when the assertion is about that migration's own text or post-migrate state, `_migrate(tmp_path, 28)` -> `== 28`). Branch (b) (bump the literals to 40) is exactly the drift that rule was written against; it re-opens at 0041 and every migration after.
>
> THE RE-SCOPE, per case:
> - A2-18: image at 38, run_migrations(c, target_version=39) (the helper exists -- tests/data/test_22a_task2_migration_0037.py:107 uses it), diff == 0039's four changed pairs. Stable forever.
> - A2-22: the gate roster / "exactly one post-base row" asserted over a target_version=39 run from a 38 image -- ONE gate fires. A HEAD run under 22-B fires TWO (the 39 gate then the 40 gate) and must not be what the test counts.
> - A2-19: its LITERAL is RETIRED. Its "the committed fixture matches HEAD" clause is ALREADY the _head tests' claim (tests/data/test_schema_manifest_head.py + test_expected_schema_version_is_head) and is NOT duplicated. The test becomes: migrate to 39; the migrated connection's schema_version == 39; the three 0039 objects (provenance_corrections, trg_provenance_corrections_append_only_update, trg_provenance_corrections_citation_graph) hash to the values A2-18 pins under the D51b normalization.
> - Case ids RETAINED in CASES_22A2 (tests/trades/case_registry_22a2.py; the closure test walks TOKENS, not bodies). The 22-A2 composition pin (brief 4.8) names the three as "re-scoped 22-B, reason: HEAD-claim in a version-named case"; the other 103 stay byte-unchanged and the pin asserts that.
>
> FORWARD RULE FOR THIS ARC (so 22-C does not meet this fork): every 22-B migration case pins target_version=40, never HEAD. State it in the plan's test roster header.
>
> This is ONE task in the plan (re-scope three tests, extend the pin), executed BEFORE 0040 is written so the pin is red-then-green on the arc's own change. Naming and versioning stay separate commits per the 09-07 rule.
>
> R0.F-4 -- ANSWERED: the cell was RIGHT; the "assignment surface" bullet at bd4314ea still carried first-row P1 and intent-row citation (my miss -- I amended the column bullets and not the prose that repeated them). Fixed by replacement now, together with 4.1, 4.2's P1 line, 4.5 five->eight, 5 step 3, and the F-1 (a') no_replace belt + F-2 (a) window folded into the 0040 bullet. Text of record: the brief at main >= the sha in the subject.
>
> ROUND 1 may open on this landing at CHARC's side; RD's two landed at 8644affa. This seat rolls on that boundary; the plan's CHARC section-3 pass is the successor's.

## Round 1 — Reviewer A (`fast`), 2026-09-23

**Invocation:** `codex exec -p fast -c model_reasoning_effort=high -s read-only --skip-git-repo-check -` from the worktree (read-only repo access), stdin = prompt + the plan (uncommitted working copy, post R0.G-R0.I encoding) + the brief @ `0f317868` + ledger R0.C-R0.I + the contract doc (180,727 bytes). The `fast` profile's own effort is `medium` (`~/.codex/fast.config.toml`), so `-c model_reasoning_effort=high` is passed to satisfy assertion 2.
**Assertions:** (1) banner model `gpt-5.6-luna` = the fast profile's model; (2) `reasoning effort: high`; (3) `^ERROR` = 0; (4) `^tokens used` = 1 (293,572); (5) `^NEW_CRITICAL_MAJOR_FOUND` = 2 (codex 0.155.1 emits the final message twice; one distinct token), `^NO_NEW_CRITICAL_MAJOR` = 0. The process was confirmed exited (`pgrep -af "codex exec"`, filtered, = 0; exit file `exit=0`) before the transcript was read. No content-filter event. Scratch: no `.codex-*` / `.copowers-*` existed in the worktree (`ls -a`); round files were staged outside it, and the one transcript hit on `.codex` (line 1133) is prose from a repo doc Codex read, not a prior-round file.
**Evidence (copy-per-round, `C:/Users/rwsmy/swing-data/review-transcripts/22-b-plan/`):** `.codex-review-r1.txt` 790,639 B · `.codex-prompt-r1.md` 5,479 B · `.codex-bundle-r1.md` 180,727 B · `.copowers-findings.md` (this adjudication + the findings section).
**Result:** 18 findings — 0 critical / 16 major / 2 minor; **18 task-bearing** (every finding changes a task or a test; none contested as instrument). All ACCEPTED and applied to the plan working copy (uncommitted; the single converged-plan commit rule).

| id | sev | class | disposition (applied) |
|---|---|---|---|
| R1-01 | major | TASK | ACCEPT. Verified: a `click.Choice` would reject the value at parse time, so the typed message is unreachable. Surfaces 1-2 use a new `EntryIntentParam(click.ParamType)` that fails with `SEAM_MESSAGE` for the value. |
| R1-02 | major | TASK (tagged RULING-CONFLICT; it is a plan-internal ordering conflict, not a ruling defect) | ACCEPT. `update_entry_intent` order fixed: read current → same-value no-op → (Task 7) attested-differs refusal → seam refusal → assertable check → write. |
| R1-03 | major | TASK | ACCEPT. b22_12 narrowed to surfaces 1-7; new b22_13 pins the corrector's UNCONDITIONAL reservation over all three values + None (E14 as ruled). |
| R1-04 | major | TASK | ACCEPT, verified: `get_authoritative_entry_fill` selects 12 columns (`repos/fills.py:301-308`), leaving `schwab_source_value_json` None. The service uses the helper for identity and a separate envelope read; new b22_107. The binding trigger pins the AUTHORITATIVE entry fill order. |
| R1-05 | major | TASK | ACCEPT, verified: `envelope_is_canonical` (`latched_origin.py:735`) is 22-A's one-authority rule for exactly this divergence. The service refuses a non-canonical envelope first; new b22_108. |
| R1-06 | major | TASK | ACCEPT. Closure limited to ADMISSION triggers; the append-only triple, the N4 trigger and the lve belts go in `SCHEMA_ONLY`, each with its own tests. |
| R1-07 | major | TASK | ACCEPT. The cited-fields predicate tests `type <> 'text'`, so JSON null/numbers cannot slip past `NOT IN`; distinctness and the COALESCE wrap are stated. |
| R1-08 | major | TASK | ACCEPT. Every date CHECK is COALESCE-closed (`date('2026-00-01')` is NULL and a NULL CHECK passes) and applies to all compared dates, including `outcome_known_at`'s datetime shape. |
| R1-09 | major | TASK | ACCEPT. Exact serialization specified: `json.dumps(sorted(cite))`; snapshot `sort_keys=True, ensure_ascii=False`. |
| R1-10 | major | TASK | ACCEPT. New b22_109 (a positive `thesis` + `emotional_state_pre_trade` citation beside a descriptive field). |
| R1-11 | major | TASK | ACCEPT. b22_63 and b22_97 parametrized over `trim`. |
| R1-12 | major | TASK | ACCEPT, with a code fact: `mandate_alive_at` FORCES the lapse rule OFF (`latched_origin.py:2444-2446`), so `criteria_lapsed` is unreachable as a structural clear reason. b22_91 is parametrized over `invalidation`/`horizon`/`declined`, and the unreachability is pinned. |
| R1-13 | major | TASK | ACCEPT. New b22_115: a production write-closure over the execute-site inventory. |
| R1-14 | major | TASK (contested interpretation → task-bearing, fail-safe) | ACCEPT the gap and REINTERPRET: brief 4.7 / RD §4 bullet 4 "quoted WITH" is encoded as a witness/read QUOTING obligation (Task 13 step 5), not a UI change, because brief §2 OUT excludes UI. **Flagged for the orchestrator/RD:** if it was meant as a card change, that is a scope addition for them to rule. |
| R1-15 | major | TASK | ACCEPT. The closure maps trigger NAMES (no message-code parsing). |
| R1-16 | major | TASK | ACCEPT. The b22_150 walk covers every `execute*` call; unresolved SQL must sit on a reviewed `DYNAMIC_SQL_SITES` inventory (a closure check, not a hand list). |
| R1-17 | minor | TASK | ACCEPT, verified: `trade_group` has no `--db` (`cli.py:577-580`). The DB is opened via `ctx` config + `connect`, as `trade review` does; `ASSIGNMENT_APPLIED_BY = "operator"`. |
| R1-18 | minor | TASK | ACCEPT. b22_03 strips `\r` before hashing; a CRLF twin is tested. |

**Cell-found in this round's wake (uncounted, CF-1):** `attestation_id` gains `CHECK (attestation_id > 0)`. Per `0037:518-532`, an omitted INTEGER PK presents as -1 inside a BEFORE INSERT trigger, and the conflict-scoped `trg_eia_no_replace` must be able to ignore -1. Applied.

## Self-check after round 1 (third cell; UNCOUNTED, no Codex) — SS-1..SS-10

**Cell:** the third 22-B writing-plans cell, 2026-09-23. **Input:** the post-round-1 working copy, verified byte-identical to `.plan-working-copy-after-r1.md` (sha256 `ff6cfc59…c9b`, `cmp` equal). **Method:** each R1 fix's class stated once, then the whole plan re-read for it. Code facts were read at the anchors named. Live facts came from plain `sqlite3` `mode=ro`, and every rebuild experiment ran on a COPY made with the backup API. **Output:** the plan working copy, still uncommitted, backed up as `.plan-working-copy-after-ss.md` (sha256 `41a9680c19ee7950e67d63519475a4e77d99d8b12b095f04056eac413c6006c3`, 329 lines, 72,098 B).

| id | class (source) | finding | fix applied to the plan |
|---|---|---|---|
| SS-1 | original defect, sitting in R1-12's area | Task 5 treated any `_probe_refusal` as `structural_unprovable`. But `mandate_alive_at` returns a PROVEN death as `_probe_refusal("mandate_not_alive", clear_reason=, clear_session=)` (`latched_origin.py` ~`:2540-2556`), and `admitted=True` means ALIVE at the fill (`_fill_wins` ~`:2271`). So the plan's ADMIT branch was unreachable. | Task 5 step 5: verdict mapped by its real shape (six cases). `LatchProbeInvariantError` propagates. |
| SS-2 | original defect | The paired CHECK makes `cited_latch_probe_json` NOT NULL for structural rows, but `probe_evidence` is None on the death path except when `clear_reason` is `fill`. The admitting row was unconstructible. | The service builds the probe JSON from the verdict fields (Task 5); Task 4's column note updated. |
| SS-3 | R1-05 class residual (one authority over an envelope key) | `trg_eia_tier2` binds `placement_session` to the envelope `entry_date`. That key is outside `ENVELOPE_KEYS_READ_BY_BOTH_DOMAINS`, so `envelope_is_canonical` does not cover it: Python's `json.loads` keeps the LAST duplicate key, `json_extract` keeps the FIRST. | The service reads `entry_date` through the SAME `json_extract` on the authoritative entry fill; a non-`YYYY-MM-DD` value is `unprovable`. New b22_78. Live: 14/14 entry envelopes carry a 10-char text `entry_date` (`GROUP BY action, length, typeof`). |
| SS-4 | R1-08 residual | R1-08 asked for malformed-date tests; none was added. | New b22_77, parametrized over the four compared date columns. |
| SS-5 | R1-09 residual | b22_181 wrote `["notes","why_now"]` with no space, while the specified `json.dumps` emits `["notes", "why_now"]`. `leg_evidence_json` / `cited_latch_probe_json` serialization was unspecified. | One serialization for all JSON columns; b22_181 asserts the exact stored bytes. |
| SS-6 | R1-13 residual (ordering) | b22_115 sat in Task 6 but depends on Task 10's execute-site inventory. | Moved to Task 10. The allowed set names `insert_trade_with_event` (sole caller `entry.py:2242`, by grep of `insert_trade_with_event(` over `swing/`). |
| SS-7 | R1-06 / R1-15 residual | Task 4 called `trg_eia_structural` "service_only", while Task 11 maps it as an admission trigger. Separately, Task 11 drove EVERY predicate through a `preflight` refusal code, and derived-value predicates (binding, outcome, placement) have none. | Wording aligned. Task 11 now has two predicate kinds: REFUSAL (drives `preflight`) and DERIVED-VALUE (a service-built row per admitting path passes the real triggers, plus the raw one-mutation bites). |
| SS-8 | R1-14 residual | The acceptance map pointed 4.7's card-N clause at b22_140-143. | That clause now points at Task 13 witness step 5. |
| SS-9 | R1-17 residual | Task 13 step 3 ran "against the copy", but no override exists: `trade_group` has no `--db`. | A scratch `--config` (main group, `cli.py:218-225`) with `db_path` → the copy and `backups_dir` → the scratchpad. |
| SS-10 | R1-02 residual | The new `update_entry_intent` order omitted the existing pre-v27 PRAGMA branch (`repos/trades.py:993-1006`). Task 7 also said "no-op write" where Task 2 says "return without writing". | Pre-v27 branch stays first; Task 7 wording aligned. |

Residual classes swept with NO hit: R1-01 (no `click.Choice` left on a generic surface), R1-03, R1-04, R1-07, R1-10, R1-11, R1-15 (no message-code parsing left), R1-16, R1-18, CF-1. Test ids: each `b22_NN` defines one test (grep `-o 'b22_[0-9]+'`, counted per id and read). The AL numbering gap (AL-4 absent) is instrument-level and left alone.

## R0.J — NEW fork (third cell, found by the self-check BEFORE round 2; the loop is HELD). Ruler: **CHARC** (0040's rebuild shape, brief §2 item (3))

**The premise that failed, BY EXECUTION.** Brief §2 item (3) orders the rebuild as CREATE new → copy → DROP old → RENAME → re-create the five dependants, and plan Task 3 steps 1-4 encode exactly that. It fails on the live v39 schema. Setup: a copy of the live DB (backup API over `mode=ro`), plain `sqlite3`, `foreign_keys=OFF`, one transaction. The failure: `ALTER TABLE trades_new RENAME TO trades` raises `OperationalError: error in trigger trg_provenance_corrections_citation_graph: no such table: main.trades`. SQLite 3.50.4's non-legacy RENAME re-parses every trigger. 0039's citation-graph trigger on `provenance_corrections` references `trades` (6 textual hits), and the DROP has just removed that table.

**The exposure is exactly ONE object.** Method: live `sqlite_master WHERE sql LIKE '%trades%' AND tbl_name <> 'trades' AND type IN ('trigger','view')`. A textual LIKE is a SUPERSET of references, and it returned 1 row, so the count is exact. The object is `trg_provenance_corrections_citation_graph`, 103,406 chars. The precedents never met this: 0014 rebuilt `trades` at v13, before any foreign trigger referenced it. A fresh-DB `ensure_schema` meets the same failure, because 0039 precedes 0040.

- **(a) Bracket the RENAME with `PRAGMA legacy_alter_table=ON;` … `=OFF;`**, and harden the runner: `_apply_migration`'s `finally` (`db.py` ~`:497`) also restores `legacy_alter_table=OFF`. The pragma is NOT transactional, so a script failing between the two lines would leave it ON on the connection. Measured on the copy: the RENAME succeeds; every non-`trades` `sqlite_master` object is byte-identical to v39; `trades` equals the one-edit text; `foreign_key_check` = []; the pragma reads 0 after. The D51 diff is unchanged from the ruled shape. Cost: 2 SQL lines, one line in `db.py` (already in the carve-out), and b22_45.
- **(b) `DROP TRIGGER trg_provenance_corrections_citation_graph;` before the rebuild, and re-create it VERBATIM from `0039` after the dependants.** Measured on the copy: the same four results as (a). Costs: 103,406 chars of 0039 duplicated into 0040. `head_create_statement('trg_provenance_corrections_citation_graph')` (`tests/data/_migration_text.py:47`, the LAST migration that creates the name) then resolves to 0040 for every 22-A / 22-A2 test that reads it: the composition class, on the very object the §4.8 pin guards. It also adds a sixth re-created object to condition (3)'s list.
- **Under either branch** (plan Task 3, added): b22_44. A closure over the foreign objects referencing `trades`: the set is equal at v39 and v40, and each member is byte-identical. Then the citation-graph trigger still FIRES against the rebuilt `trades`: a 22-A2 fully-cited fixture row inserts, and its one-mutation twin aborts.

**Cell's lean, stated as a lean and not a ruling:** (a). It keeps 0040's text to the change it exists for (F6's spirit), touches no 22-A2 object, and leaves `head_create_statement` resolution unchanged. The hazard it adds (the non-transactional pragma) is closed by the runner line and pinned by b22_45. **No review round runs until CHARC rules** (plan-stage rule 1). Both branches are carried as a marked drop-in at plan Task 3 step 3.

## R0.J-RULING — CHARC rules R0.J: branch (a), the RENAME bracketed by PRAGMA legacy_alter_table; the runner restores the PRIOR value (author CHARC, successor seat; courier transcription, literal block quotes)

### R0.J-RULING.1 — the replaced brief text (`docs/phase22-arc-b-commissioning-brief.md`, lines added by `0f317868..43227001`, each quoted whole; the text of record)

> - **Migration `0040`: the `trades` REBUILD widening ONE enum token** — under the 22-A2 F12 conditions transposed: (1) the new DDL is the STORED v39 `trades` DDL with EXACTLY ONE textual edit (the IN-list gains `'unintended_execution'`), proven bytewise by a test that applies that one edit to the v39 text; (2) ONE transaction, explicit column list on the INSERT…SELECT (58 columns + none added), row counts equal, every row's tuple identical, `PRAGMA foreign_key_check` empty, the five child tables' row counts unchanged; (3) order: CREATE new → copy → DROP old (takes the five dependants) → **RENAME inside a two-statement bracket `PRAGMA legacy_alter_table=ON;` / `ALTER TABLE trades_new RENAME TO trades;` / `PRAGMA legacy_alter_table=OFF;` — nothing else inside the window** → re-create the FOUR indexes and the ONE trigger VERBATIM from their source migrations (`0014`/`0027`-era for the partial UNIQUE; `0038` for the attempt-id pair). **R0.J (CHARC-ruled 2026-09-23, branch (a); the bare RENAME FAILS BY EXECUTION on v39):** SQLite 3.50.4's modern RENAME re-parses every trigger, and 0039's `trg_provenance_corrections_citation_graph` (ON `provenance_corrections`, 103,406 chars, six textual `trades` references, the ONLY foreign trigger/view naming `trades` — census method: `sqlite_master WHERE sql LIKE '%trades%' AND tbl_name <> 'trades' AND type IN ('trigger','view')`, a textual SUPERSET, 1 row; 0 views on the DB; 0 objects name `trades_new`, so legacy mode has nothing to skip rewriting) raises `no such table: main.trades` after the DROP. Reproduced by the plan cell on a live copy, by the orchestrator in memory, and by CHARC in memory before ruling: under the bracket every foreign object is byte-identical to v39, `trades` equals the one-edit text, `integrity_check` ok, and the trigger still RAISES its own message against the rebuilt table. **Conditions:** (i) the pragma is connection-scoped and NOT transactional (CHARC measured: it survives a rollback), so `_apply_migration` (`swing/data/db.py:457`) reads the PRIOR value before `executescript` and restores it in the existing `finally` beside `foreign_keys` — restore-prior, never hard-OFF; **b22_45** pins the failure path (a script that sets ON then fails leaves the connection reading the prior value); (ii) **b22_44** as the cell proposed: the set of foreign objects referencing `trades` is EQUAL at v39 and v40 and each member byte-identical, AND the citation-graph trigger FIRES against the rebuilt `trades` — a 22-A2 fully-cited fixture row inserts, its one-mutation twin ABORTS with the trigger's OWN message (a `no such table` error is a FAIL, not an abort); (iii) 0040's header states the census (method + count) as the precondition that makes the bracket sufficient — a later foreign object referencing `trades` is covered by the same bracket, but the header says which objects the bracket was proven against. **Branch (b) REJECTED** (drop + verbatim re-create of the 0039 trigger): 103 KB of the evidence grammar's SQL binding duplicated into 0040 moves `head_create_statement` resolution (`tests/data/_migration_text.py:47`) to 0040 for every 22-A/22-A2 test — the composition class on the object §4.8 pins — and under D60 gives a future grammar edit TWO homes. Forward rule for ANY rebuild in this repo: run the cross-table census BEFORE ruling the order; a non-empty census means the RENAME is bracketed. Gotcha candidate → the orchestrator's Gotchas content. **Owned by CHARC:** item (3) as first written carried the mechanism without its admissibility precondition against the v39 schema (harness §5.1's rule, met on my own brief); a D51 manifest cannot see a runtime-order defect — the cell's self-check found what only execution finds. (4) the D51 diff reads EXACTLY TWO changed pairs (the header, the table) PLUS, as ADDITIONS: the new table's objects, the one new `trades` trigger `trg_trades_entry_intent_attested_terminal` (N4, below), and **the TWO `latch_view_events` evidence belts adopted at census N1 (RD, `rd-rulings-census-n1-n5.md` @ `c33bd6e7`, on CHARC's schema finding): `trg_lve_actionable_ever_viewed_monotonic` (`BEFORE UPDATE OF actionable_ever_viewed … WHEN NOT COALESCE(NEW.actionable_ever_viewed >= OLD.actionable_ever_viewed, 0)` → `RAISE(ABORT)`, the SQL twin of the writer's `MAX` at `repos/latch_view_events.py:167`) and `trg_lve_no_delete` (`BEFORE DELETE` → `RAISE(ABORT)`), PLUS — R0.F-1 (a'), RD @ `8644affa`, completing the 22-I TRIPLE — `trg_lve_no_replace` (`BEFORE INSERT WHEN EXISTS` a row on the table's UNIQUE key → `RAISE(ABORT)`; the writer's INSERT at `repos/latch_view_events.py:179-190` is plain, no `ON CONFLICT`, so the DO-NOTHING facet of the REPLACE gotcha does not bite and its `IntegrityError` handler at `:191` already routes the ABORT to the UPDATE path — writer UNCHANGED, proven by execution); the identity triggers stay** — and ZERO deletions; the five pre-existing `trades` dependants hash identical. The belts' discriminators (§4.4): a raw `UPDATE … SET actionable_ever_viewed = 0` on AMN's row 5 ABORTS; a raw `DELETE` ABORTS; `INSERT OR REPLACE` on AMN's key with 0 ABORTS; the writer's plain INSERT on an existing key takes the UPDATE path; the writer's 0 → 1 passes. **Leg 1's WINDOW — R0.F-2 (a), RD, same commit: a `latch_view_events` row speaks ONLY if recorded under the actionability instrument, `date(first_viewed_ts) >= 2026-08-03` (the same constant as leg 2; 0033 backfilled every earlier row to 0 = "no actionable presentation RECORDED", a deployment fact; live rows 1–5 ARE that backfill); window by VIEW TIMESTAMP at date grain (local naive HST), never `view_session_date`. Trade 20: row 5 first viewed 08-01 → backfilled → leg 1 SILENT → ADMITS on LEG 2, `admitted_leg = deployment`.** Why the table earns barriers now: leg 1 makes it EVIDENCE, and "monotonic + no-vanish" was a WRITER property only — the inference the admissibility standard forbids. (5) the reversibility header names the edit and the gate row `BackupGateSpec(39, "22b", …)`; (6) `EXPECTED_SCHEMA_VERSION` 39 → 40. `trades` has NO AUTOINCREMENT (`id INTEGER PRIMARY KEY`, the rowid-reuse gotcha) — no sequence carry; the plan says so rather than copying 0039's step. **(7) THE F1 PIN (CHARC's ruling, the shape RD expected):** `0040`'s header carries the sha256 of clause (4)'s ratified text; a test derives that hash INDEPENDENTLY from the committed doc `docs/training-epoch-intent-contract.md` (the block-quoted clause (4) EXACTLY as it stands at `rd-rulings-f1-f3.md` @ `e5feec2a` — byte-for-byte, the plan COPIES it, never retypes it) and asserts equality; the migration NEVER reads the docs tree at runtime. Any later edit to the clause is a NEW amendment with a new hash and a new ratification.

### R0.J-RULING.2 — CHARC's ruling mail (20260923T183610Z, body whole)

> R0.J RULED: branch (a). PRIMARY CHARC; RD CC, no ruling asked. Text of record: brief section 2 item (3) REPLACED at main @ 43227001 (docs/phase22-arc-b-commissioning-brief.md). Transcribe into the ledger as a block quote from that commit; merge main into 22-b-plan so the cell reads it in-tree. Round 2 may open on the transcription.
>
> REPRODUCED AT THIS SEAT before ruling (in memory off the live v39 sqlite_master, mode=ro, SQLite 3.50.4): the bare RENAME raises "error in trigger trg_provenance_corrections_citation_graph: no such table: main.trades"; under the bracket every foreign object is byte-identical to v39, trades equals the one-edit text, integrity_check ok, PRAGMA legacy_alter_table reads 0 after, and INSERT INTO provenance_corrections DEFAULT VALUES raises the trigger's OWN citation-graph message against the rebuilt table (IntegrityError, not "no such table"). Hazard measured: the pragma set ON then a rollback leaves the connection reading 1 -- the runner belt is necessary, not decorative.
>
> CONDITIONS (all in the brief text):
> (i) The bracket is TWO statements wide: PRAGMA legacy_alter_table=ON; the RENAME; PRAGMA legacy_alter_table=OFF; nothing else inside the window (a future ALTER inserted inside would silently inherit legacy semantics). One comment line names the reason (0039's cross-table trigger; modern RENAME re-parses every trigger).
> (ii) Runner: _apply_migration (swing/data/db.py:457) reads the PRIOR legacy_alter_table value before executescript and restores it in the existing finally beside foreign_keys -- restore-prior, never hard-OFF, symmetric with the foreign_keys idiom already there. b22_45 pins the failure path: a script that sets ON then fails leaves the connection reading the prior value. Note for the plan: unlike foreign_keys, this pragma is NOT a no-op inside a transaction (the RENAME succeeded with it set after BEGIN), so the in-script bracket is valid.
> (iii) b22_44 as the cell proposed, second half sharpened: the foreign-object set referencing trades is EQUAL at v39 and v40 and each member byte-identical; the citation-graph trigger FIRES against the rebuilt trades -- a 22-A2 fully-cited fixture inserts, its one-mutation twin ABORTS with the trigger's own message. A "no such table" error is a FAIL. Run it on both shapes the fresh-DB ensure_schema and the live-copy migration produce.
> (iv) 0040's header states the census as the precondition the bracket was proven against: method sqlite_master WHERE sql LIKE '%trades%' AND tbl_name <> 'trades' AND type IN ('trigger','view'), a textual superset, 1 row; 0 views; 0 objects name trades_new (so legacy mode skips rewriting nothing).
>
> BRANCH (b) REJECTED: 103,406 chars of the evidence grammar's SQL binding copied into 0040 moves head_create_statement (tests/data/_migration_text.py:47) to 0040 for every 22-A/22-A2 test -- the composition class on the very object section 4.8 pins -- and under D60 gives a future grammar edit two homes. F6 unchanged: one DDL edit; D58 stays out.
>
> FORWARD RULE (any rebuild in this repo): run the cross-table census BEFORE ruling the order; a non-empty census means the RENAME is bracketed. Gotcha candidate for the orchestrator's Gotchas content at the compression; no CLAUDE.md edit from this seat.
>
> OWNED: item (3) as I first wrote it carried the mechanism without its admissibility precondition against the v39 schema -- harness 5.1's rule, met on my own brief. The D51 manifest cannot see a runtime-order defect; the cell's self-check found what only execution finds, before a counted round was spent on it. Credit to the third cell.
>
> SS-1..SS-10: read, no ruling asked, none needed from this seat. SS-3 (entry_date read outside envelope_is_canonical's keys; json.loads keeps LAST, json_extract FIRST) is the right fix shape -- the service reads it the trigger's way.

## R0.J-RULING encoded (third cell, before round 2; no Codex)

CHARC's four conditions went into the plan working copy, and branch (b) with its drop-in marker was deleted:
- (i) The Task 3 step 3 bracket is exactly two statements wide around the RENAME, with one comment line naming the reason.
- (ii) `_apply_migration` reads the prior `legacy_alter_table` value and restores that prior value in its `finally` (never a hard OFF). b22_45 runs with prior 0 and prior 1.
- (iii) b22_44 is two parts (set equality plus the trigger firing with its OWN message; a `no such table` error counts as a FAIL), run on both shapes. Task 13 step 3 runs the same helper on the live copy.
- (iv) Header item (vii) states the census.
Backup: `.plan-working-copy-after-r0j.md` (sha256 `2db615e8664c94439c2e89366584e3d0db52f8f782346914e24b32631d4c0275`, 339 lines, 73,431 B). That working copy is the round-2 input.

## Round 2 — Reviewer A (`fast`), 2026-09-23

**Invocation:** `codex exec -p fast -c model_reasoning_effort=high -s read-only --skip-git-repo-check -`, run from the worktree (read-only repo access). It ran through an LF runner script (`file` verified) under `wsl.exe bash -lc`, with the output and exit files pre-created Windows-side.
- stdin: the prompt, the plan (post-R0.J working copy), the brief @ `43227001`, ledger excerpt R0.C, R0.D-R0.I, R0.J and R0.J-RULING, and the contract doc.
- Excluded from the bundle: the round table, the Round 1 section, and the self-check section. The prompt forbids reading this ledger file, `.codex*`/`.copowers*` files, and `review-transcripts`.
- Staging was OUTSIDE the worktree, in the session scratchpad; the worktree holds no `.codex-*`/`.copowers-*` files.

**Assertions:**
1. Banner model `gpt-5.6-luna` (the fast profile's).
2. `reasoning effort: high`.
3. `^ERROR` = 0.
4. `^tokens used` = 1 (405,845).
5. `^NEW_CRITICAL_MAJOR_FOUND` = 2 (codex 0.155.1 prints its final message twice; one distinct token); `^NO_NEW_CRITICAL_MAJOR` = 0.

**Process checks:**
- Process confirmed exited before the transcript was read: `pgrep -af 'codex exec'`, with pgrep/grep filtered out, returned 0; the exit file reads `exit=0`.
- Scratch check: transcript hits on `ledger.md` / `review-transcripts` are lines of the echoed input or of repo docs Codex read (CLAUDE.md-family prose). No exec read of the ledger or of a prior-round file.
- No content-filter event.

**Evidence (copy-per-round, `C:/Users/rwsmy/swing-data/review-transcripts/22-b-plan/`):** `.codex-review-r2.txt` 1,164,560 B · `.codex-prompt-r2.md` 5,997 B · `.codex-bundle-r2.md` 210,094 B · `.copowers-findings.md` 22,130 B (round-2 response appended). Post-fix plan backup: `.plan-working-copy-after-r2.md`, 79,950 B, sha256 `beada97145fe28f83cb2d089b28ace192b9609507db4755d1d4ff85ab00b2dca`.

**Result:** 14 findings, 0 critical / 8 major / 6 minor. 11 are task-bearing and 3 are instrument (R2-11/12/13, per the reviewer's own class; none contested). All 14 are ACCEPTED (R2-05 reframed, R2-14 partly declined) and applied to the plan working copy, which stays uncommitted.

| id | sev | class | disposition (applied) |
|---|---|---|---|
| R2-01 | major | TASK | ACCEPT. `ensure_schema` goes straight to HEAD, so b22_44's fresh shape is now empty → `target_version=39` → snapshot → `target_version=40`, plus a separate `ensure_schema` DB compared to the snapshot. |
| R2-02 | major | TASK | ACCEPT, verified: both review surfaces commit `complete_trade_review` in their own `with conn:` BEFORE `update_entry_intent` (`cli.py` ~1669-1712, `routes/trades.py` ~3592-3621). New pure read `assert_entry_intent_change_allowed`, called by both surfaces BEFORE the review commit and by `update_entry_intent` step (c). b22_121 and b22_123 assert the review fields and `state` are unchanged. |
| R2-03 | major | TASK | ACCEPT. The `no_entry_fill` refusal comes before any envelope read; new b22_119. |
| R2-04 | major | TASK | ACCEPT. Every date CHECK copies the `0033:100-126` precedent (year 1-9999; the ISO-seconds GLOB with hh/mm/ss bounds for `outcome_known_at`), with the Python mirror via `strptime`. b22_77 gains the invalid-time and year-0000 cases. |
| R2-05 | major | TASK (reframed) | PARTLY FALSE BY EXECUTION: `json_valid(x) AND json_type(x)='array'` in a CHECK short-circuits and yields `IntegrityError` (measured). The TRIGGER half is real, also measured: BEFORE triggers run before CHECKs, and `json_each(NEW.x)` raises `OperationalError: malformed JSON` even behind a `json_valid` conjunct. Fix: `json_each(CASE WHEN json_valid(NEW.x) THEN NEW.x ELSE '[]' END)`, with the predicate leading on `json_valid` (measured: the trigger's own ABORT). New b22_48. |
| R2-06 | major | TASK | ACCEPT. Telemetry evidence is bound to EXACTLY the PRE set, both directions plus values. Deployment evidence and the probe JSON's link/rung/session/decline/fire keys are bound by VALUE (`json_extract`, never bytes: the service serializes with spaces). New b22_79; Task 11's derived-value list updated. |
| R2-07 | major | TASK | ACCEPT. New b22_46: the header census text, plus the same facts by execution at v39. |
| R2-08 | minor | TASK | ACCEPT. New b22_47: a static parse showing the bracket is exactly three statements. |
| R2-09 | minor | TASK | ACCEPT. New b22_117 (entry-date drift) and b22_118 (leg-1 actionability drift, with the belt dropped in the test DB). |
| R2-10 | minor | TASK | ACCEPT, verified: `dashboard.py` ~495-504 renders only `tier2_count_marker`. It is removed from the renderer-site list. |
| R2-11 | minor | INSTRUMENT | ACCEPT. Witness step 3 now captures the `hypothesis list` pre-image first. |
| R2-12 | minor | INSTRUMENT | ACCEPT. b22_83 asserts only the structured `AssignmentResult` (its fields are named in the plan); stdout stays in b22_110. |
| R2-13 | minor | INSTRUMENT | ACCEPT. The brief pin becomes `43227001` in the plan header and the acceptance-map heading. |
| R2-14 | major | TASK | ACCEPT IN PART. `drift_report` gains (v) the authoritative entry fill has moved, and (vi) the envelope order id / `entry_date` has changed, both compared against the already-frozen `entry_fill_id` / `entry_broker_order_id` / `placement_session` (no new column); new b22_116. DECLINED: snapshotting fill quantity, price and origin, because they are not admission inputs. |

## Round 3 — Reviewer A (`fast`), 2026-09-23 (fourth cell; the LAST counted plan round)

**Invocation:** `codex exec -p fast -c model_reasoning_effort=high -s read-only --skip-git-repo-check -`, run from the worktree (read-only repo access). It ran through an LF runner script (`file` verified; copied beside the evidence as `run_r3.sh`) under `wsl.exe bash -lc`. The output and exit files were pre-created Windows-side.
- stdin: the round-2 prompt with only the round number and the finding-id prefix changed (`diff`: two lines), the plan (the post-round-2 working copy, sha256 `beada971…2dca` verified before any edit), the brief @ `43227001` (in-tree; `git diff --stat 43227001` empty), ledger R0.C, R0.D-R0.I, R0.J and R0.J-RULING (ledger lines 114-212, 220-421, 471-508), and the contract doc. Total 216,715 B.
- The prompt forbids reading this ledger file, `.codex*`/`.copowers*` files and `review-transcripts`. It describes both verdict tokens and writes neither line-initially (`grep -c` over the prompt = 0).
- Staging was OUTSIDE the worktree, in the session scratchpad. The worktree holds no `.codex-*` or `.copowers-*` file (`ls -a | grep`, empty).

**Assertions:**
1. Banner model `gpt-5.6-luna` (the fast profile's).
2. `reasoning effort: high`.
3. `^ERROR` = 0.
4. `^tokens used` = 1 (451,928).
5. `^NEW_CRITICAL_MAJOR_FOUND` = 2 (codex 0.155.1 prints its final message twice; one distinct token); `^NO_NEW_CRITICAL_MAJOR` = 0.

**Process checks:**
- The process was confirmed exited before the transcript was read. `pgrep -af 'codex exec'`, with pgrep/grep filtered out, returned 0; the exit file reads `exit=0`; the harness reported the background task complete.
- Scratch check: of the transcript's `exec` command lines, 0 name `ledger`, `review-transcripts`, `.copowers` or `.codex` (grep over the line after each `^exec`). No prior-round file was read.
- No content-filter event (the final message says nothing of one).

**Evidence (copy-per-round, `C:/Users/rwsmy/swing-data/review-transcripts/22-b-plan/`, copied the moment the assertions passed):** `.codex-review-r3.txt` 762,963 B · `.codex-prompt-r3.md` 5,997 B · `.codex-bundle-r3.md` 216,715 B · `run_r3.sh` 845 B · `.copowers-findings.md` (the round-3 response appended). Post-fix plan backup: `.plan-working-copy-after-r3.md`, 348 lines, 89,248 B, sha256 `0b61a2db2f7aa188671fd395329471cd7462e0e084a9ae9cdf82f4a9e8c24460` (byte-identical to the uncommitted working copy, `cmp`).

**Result:** 10 findings, 0 critical / 9 major / 1 minor. The reviewer marked all 10 `[TASK]`. The cell's classification agrees on 9. R3-01 reads as instrument-level wording, but it is contested, so it counts as task-bearing (fail-safe). **10 task-bearing.** All 10 are ACCEPTED and applied to the plan working copy, which stays uncommitted. **R3-09 also surfaced a design question no ruling covers: fork R0.K below. The loop is STOPPED on it.** There is no self-sweep and no converged commit until it is ruled.

| id | sev | class | disposition (applied) |
|---|---|---|---|
| R3-01 | major | TASK (contested: wording; counted task-bearing) | ACCEPT. The plan does not declare `outcome_known_at` twice. But the Task 4 date-CHECK paragraph sat INSIDE the `trade_entry_date` column bullet and read as a declaration. It is now a separate bullet headed "a NOTE on the column CHECKs, not a column", and it states that each column is declared once. |
| R3-02 | major | TASK | ACCEPT. Task 3's b22_25 expects {the N4 trigger, the three belts} only. Task 4's commit extends the named constant with the attestation objects, and the file is listed in Task 4. |
| R3-03 | major | TASK | ACCEPT, verified: `process_grade_trend.html.j2:69-70` prefixes `intent-`, and `_INTENT_CSS_CLASS` (`process_grade_trend.py:78-82`) holds bare tokens. The mapping value is `"unintended"`. The `app.css` edit is DROPPED: no `.intent-*` rule exists for any value (grep of `swing/web/`; `.process-grade-marker` at `app.css:408` is the only marker rule), and the hook is the class plus `data-entry-intent`, as `test_metrics_process_grade_trend_route.py:139-143` pins. b22_140 asserts `intent-unintended` and the absence of `intent-intent-`. |
| R3-04 | major | TASK | ACCEPT, verified: `_build_templates` (`web/app.py:133-145`) registers no globals, and `ReviewVM` (`view_models/trades.py:1344-1351`) carries the choices and the raw value only. The VM gains `entry_intent_attested_label: str = ""`, filled by the builder via `swing.trades.intent.entry_intent_label` (`intent.py:30`), and the template renders the field. b22_122 asserts the text. |
| R3-05 | major | TASK, RULING-CONFLICT (brief §0 item 5) | ACCEPT, verified: `split_into_partials` runs `DELETE FROM fills WHERE fill_id = ?` (`reconciliation_auto_correct.py:3216`), and `0039:84-115` already solved this exact case. The shape is copied: `entry_fill_id … ON DELETE SET NULL`, plus `entry_fill_id_at_assignment NOT NULL`, the pairing CHECK, and `trg_eia_no_update` permitting ONLY the FK-driven nulling (the `0039:700-762` shape). The binding trigger, drift (v) and Task 11 use the frozen number. New b22_191, which goes RED under RESTRICT. b22_72 covers a manual nulling while the fill exists, and b22_116 gains the deleted-fill case. NOT a new fork: the brief's doctrine and the 0039 precedent together fix the answer. |
| R3-06 | major | TASK | ACCEPT, verified: `envelope_is_canonical` returns True for a blank string (`latched_origin.py:794-795`). Measured with plain sqlite3: `json_extract('', '$.a')` RAISES `malformed JSON`, and so does `0 AND json_extract('', …)`, so short-circuit is no guard. Every envelope read (the binding trigger, `trg_eia_tier2`'s placement, the service's SS-3 read) goes through `<ENV>` = `CASE WHEN json_valid(x) THEN x END`. A blank envelope is ABSENT in both domains and takes the E9 path. New b22_193. Live: 0 blank envelopes of 59 fills (26 NULL). |
| R3-07 | major | TASK | ACCEPT. Preflight step 2 gains `blank_reason` (`reason.strip()` empty). Python's strip is stricter than SQLite's `trim`, so the service refuses everything the CHECK refuses. New b22_194 (dry-run and write). |
| R3-08 | major | TASK | ACCEPT. b22_160's map carries each admission trigger's D51b-normalized SQL sha256, plus a seventh entry for the table's own CHECK binds (the table DDL hash and the bind ids). An added predicate changes a hash and FAILS the test, naming the object. This is the repo's existing pin-by-hash idiom (A2-09, b22_170), not a predicate parser. |
| R3-09 | major | TASK | ACCEPT the finding as stated: drift (iv) now compares every snapshotted leg-1 field (existence, `actionable_ever_viewed`, `first_viewed_ts`, `view_session_date`) and re-derives the PRE set. New b22_195. **The cell's read goes further, and that part is fork R0.K:** a PRE-assignment raw UPDATE of `first_viewed_ts` can MANUFACTURE a telemetry admission, and neither the snapshot nor the drift reader can see it. |
| R3-10 | minor | TASK | ACCEPT. The convention now reads "every VALIDATION trigger's `WHEN` is `NOT COALESCE(…)`", with a named `TOTAL_WHEN` list for barrier triggers: the N4 trigger in its ruled text (R0.D), and the two `*_no_replace`. b22_37 gains the static half. The N4 trigger's ruled text is NOT rewritten. |

**Cell-found during round 3 (uncounted; found while the round ran, not by Codex):**
- **CF-R3-1 (task-bearing; authorize-then-abort):** the table bind `trade_entry_date < substr(outcome_known_at,1,10)` (the brief's test (a)) had NO service twin. Preflight step 4 computed the outcome and never refused it, so a same-session outcome was ADMITTED by the service and ABORTED by the CHECK. Live incidence is 1 of 28 trades (trade 11, entry and first stop both 2026-05-22), measured with plain `sqlite3` `mode=ro`, `MIN(fill_datetime)` over `trim`/`exit`/`stop` per trade. That trade is already refused by `already_set`. Fix: the `outcome_not_after_record` refusal; new b22_49 (the raw CHECK boundary twin) and b22_196 (the service twin). Task 11's CHECK entry maps it.
- **CF-R3-2 (id hygiene):** `…_null_does_not_pass_b22_52..` named an open series whose next members (53, 54) are OTHER tests. It is now ONE parametrized test, b22_52. `test_update_delete_replace_abort_b22_72..74` named three ids with one name. It is now three tests: b22_72, b22_73, b22_74.

## R0.K — NEW fork (fourth cell, from round 3's R3-09; the loop is STOPPED). Ruler: **RD** (the leg-1 evidence semantics; he ruled the belt set at R0.G/R0.H). CHARC absorbs any 0040 change into brief §2 item (4), as at R0.G.

**What no ruling covers.** R0.H (R0.F-2 (a)) windows leg 1 by `first_viewed_ts` at date grain, over the trade's `ticker`'s rows. R0.G/R0.H belt ONLY `actionable_ever_viewed` (monotonic), deletion and REPLACE. They name the property leg 1 needs as "`actionable_ever_viewed` is MONOTONIC and the row cannot VANISH". The two WINDOW inputs are unprotected against a raw UPDATE, and nothing leaves a trail:
- `first_viewed_ts`: no trigger names it. The identity trigger (`0032:123-139`) covers `candidate_id, evaluation_run_id, ticker, detection_date, pipeline_run_id` only.
- `ticker`: its coherence with `candidate_id` is enforced, but a coherent re-point to another A+ fire of a different ticker passes.

**Why it is not already covered.**
- The standard (brief §0 item 1; RD's admissibility test) requires "(b) immutable since, verified against the audit trail, never inferred from writer-absence."
- RD applied exactly that to `actionable_ever_viewed` and DECLINED the declared-limitation branch twice ("a table that becomes evidence earns the barriers").
- But brief §2 item (4) ENUMERATES 0040's D51 additions, so the plan can neither add a fourth belt nor declare an AL without contradicting one ruling or the other.

**The exposure, derived from the ruled predicate (Task 4 `trg_eia_tier2`; Task 5 leg 1).** A speaking row needs `date(first_viewed_ts) >= 2026-08-03`. The PRE window needs `date(first_viewed_ts) <= placement`. So leg 1 can speak only when placement >= 08-03, and in that region leg 2 always REFUSES. Leg 1's all-0 PRE set is therefore the ONLY admitting path there, and a raw UPDATE BEFORE assignment can manufacture it:
- (i) Move an `actionable_ever_viewed = 1` row's `first_viewed_ts` past placement. The refusing row goes silent, and any remaining all-0 PRE row admits.
- (ii) Move a 0-row's `first_viewed_ts` INTO the window where no PRE row existed. The leg-2 REFUSE becomes a telemetry ADMIT.

The attestation snapshots the PRE rows AFTER the tamper, and the drift reader (R3-09) compares against that snapshot. So neither sees a pre-assignment move. Fabrication by a raw INSERT of a new key is a different class: it is outside every evidence table's threat model in this repo, and it is not raised here.

**Facts (cell, 2026-09-23; plain `sqlite3` `mode=ro` on live v39, and reads of the code):**
- The sole production UPDATE of the table is `swing/data/repos/latch_view_events.py:157-169`. It SETs `last_viewed_ts`, `latch_state_at_last_view`, `actionable_at_last_view`, `view_count` and `actionable_ever_viewed`, and never `first_viewed_ts` or `ticker`.
- The writer enumeration comes from grep of `latch_view_events` with UPDATE/INSERT/DELETE/REPLACE over `swing/**/*.py`: 2 sites, the UPDATE above and the INSERT at `:180`. A grep is a LOWER bound; Task 10's execute-site inventory is the closure.
- Live: 33 rows; 28 have `date(first_viewed_ts) >= '2026-08-03'` (they speak); 0 have `first_viewed_ts > last_viewed_ts` (a shape the writer cannot produce).

**Branches (both executable; the plan carries a marked drop-in at Task 3 step 6):**
- **(a) A fourth belt in 0040:** `trg_lve_view_window_immutable`, `BEFORE UPDATE OF first_viewed_ts, ticker ON latch_view_events` → `RAISE(ABORT)`.
  - The writer's UPDATE lists neither column, so the trigger never fires on a production path. (`UPDATE OF` fires only for SET-listed columns; the existing b22_39/b22_43 prove the writer path stays green.)
  - Discriminators: a raw `UPDATE … SET first_viewed_ts` on a speaking row ABORTS; a coherent raw re-point of `ticker` (with `candidate_id` etc.) ABORTS; the writer's `record_view` merge passes.
  - Cost: brief §2 item (4)'s addition list gains one member (CHARC absorbs, as at R0.G); b22_25's constant gains it; F6 is untouched (not `trades`).
  - `view_session_date` is left out on purpose: the leg predicate never reads it (R0.H: "never `view_session_date`"). The drift reader still reports it.
- **(b) Declared AL-6, service-prevented** (recipe §3's weaker citation; it must say which kind it is):
  - "The leg-1 window inputs `first_viewed_ts` / `ticker` are WRITER-immutable, not schema-immutable. The sole writer never SETs them (`repos/latch_view_events.py:157-169`; 2 writer sites by grep, a lower bound). Live incidence of a writer-impossible shape: 0 of 33. A post-assignment move is DETECTED by drift (iv). A pre-assignment move is NOT detectable."
  - This is the writer-absence inference the standard names. RD declined it for `actionable_ever_viewed`.

**Cell's lean, stated as a lean and not a ruling:** (a). The standard is RD's own, and he has applied it twice to this table. The belt costs five SQL lines and two tests. Trade 20 is unaffected under both branches: its only AMN row (id 5) is first viewed 2026-08-01 < 08-03, so it never speaks, and it admits on leg 2.

**No review round and no converged commit until RD rules** (plan-stage rule 1). The plan working copy carries every round-3 fix; the branch the ruling selects replaces the drop-in marker. A successor cell then runs the uncounted self-sweep (rounds 1-3 + SS + round-3 residuals) and makes the single converged commit. No fourth counted round is asked for: the fork is a ruling question, not a review finding the plan can close.

## R0.K-RULING — RD rules R0.K: branch (a), the FOURTH `latch_view_events` belt `trg_lve_view_window_immutable`; R1-14 CONFIRMED (author RD; CHARC absorbed into the brief; courier transcription by the successor orchestrator generation, literal block quotes)

### R0.K-RULING.1 — the added sentence block (`docs/phase22-arc-b-rd-rulings-census-n1-n5.md` @ `226eb6da`, N1; inserted by `43227001..226eb6da` into the precondition paragraph, nothing removed; quoted whole)

> **FOURTH BELT (R0.K, ruled (a) 2026-09-23): `trg_lve_view_window_immutable` — BEFORE UPDATE OF `first_viewed_ts`, `ticker` → RAISE(ABORT).** Leg 1's INPUTS are `first_viewed_ts` (the window) and the identity block (`ticker`/`candidate_id`, already guarded against `candidates` by `trg_lve_identity_coherent_update`); the triple guarded the evidence column and the row's existence but not the window, and under R0.F-2 (a) leg 1 can speak only where leg 2 refuses, so a raw write to `first_viewed_ts` before assignment could MANUFACTURE an admit that the snapshot and the drift reader (both later) cannot see. The sole production writer never sets either column (`repos/latch_view_events.py:157-169`, orchestrator-verified), so no production path aborts. The AL branch (b) is DECLINED a third time on the same ground: writer-absence is not evidence. **The rule that closes this class rather than this instance: every column a leg READS is a column the schema must GUARD — the plan lists leg 1's read set beside the belt set and asserts by test that the two are equal.** Trade 20 unaffected (row 5 first viewed 08-01 → leg 1 silent → leg 2). Discriminator: raw `UPDATE latch_view_events SET first_viewed_ts='2026-08-03T00:00:00' WHERE view_event_id=5` ABORTS; the writer's last-view UPDATE passes.

### R0.K-RULING.2 — RD's ruling mail (20260923T192521Z, body whole)

> PRIMARY: orchestrator (successor, courier). CC charc (one more D51 addition in brief section 2 item (4) is yours to absorb).
>
> R0.K -- RULED (a): a FOURTH belt trg_lve_view_window_immutable, BEFORE UPDATE OF first_viewed_ts, ticker -> RAISE(ABORT). The triple guarded the evidence column and the row's existence, not the WINDOW leg 1 reads; under R0.F-2 (a) leg 1 speaks only where leg 2 refuses, so an unguarded first_viewed_ts is a manufacturable admit the snapshot and drift reader run too late to see. No production path aborts (the writer never sets either column, :157-169). AL branch (b) DECLINED, third time, same ground: writer-absence is not evidence.
>
> THE RULE, so 22-C does not meet this fork: every column a leg READS is a column the schema GUARDS -- the plan lists leg 1's read set (first_viewed_ts + the identity block) beside the belt set and pins their equality by test. Trade 20 unaffected. Discriminator: raw UPDATE of row 5's first_viewed_ts ABORTS; the writer's last-view UPDATE passes.
>
> R1-14 -- CONFIRMED (banked twice, closing it now): the card-N-beside-tier-N line is a READ obligation on the witness and on my reads, NOT a card UI change. The encoding stands.
>
> R3-05 (RESTRICT vs the 0039 SET NULL shape on entry_fill_id): read; consistent with cohort bookkeeping never blocking a money-bearing operation; nothing from me. CF-R3-1 (same-session outcome refused typed, trade 11 the live instance): the right direction.
>
> Landed by ADDITION in docs/phase22-arc-b-rd-rulings-census-n1-n5.md N1 (this commit); courier block-quotes the added sentence block beside R0.H.

### R0.K-RULING.3 — the replaced brief line (`docs/phase22-arc-b-commissioning-brief.md` @ `8c66a39a`, §2 item (4); the one line changed by `226eb6da..8c66a39a`, quoted whole; CHARC's text of record for the build, the authority is RD's ruling at `226eb6da`)

> - **Migration `0040`: the `trades` REBUILD widening ONE enum token** — under the 22-A2 F12 conditions transposed: (1) the new DDL is the STORED v39 `trades` DDL with EXACTLY ONE textual edit (the IN-list gains `'unintended_execution'`), proven bytewise by a test that applies that one edit to the v39 text; (2) ONE transaction, explicit column list on the INSERT…SELECT (58 columns + none added), row counts equal, every row's tuple identical, `PRAGMA foreign_key_check` empty, the five child tables' row counts unchanged; (3) order: CREATE new → copy → DROP old (takes the five dependants) → **RENAME inside a two-statement bracket `PRAGMA legacy_alter_table=ON;` / `ALTER TABLE trades_new RENAME TO trades;` / `PRAGMA legacy_alter_table=OFF;` — nothing else inside the window** → re-create the FOUR indexes and the ONE trigger VERBATIM from their source migrations (`0014`/`0027`-era for the partial UNIQUE; `0038` for the attempt-id pair). **R0.J (CHARC-ruled 2026-09-23, branch (a); the bare RENAME FAILS BY EXECUTION on v39):** SQLite 3.50.4's modern RENAME re-parses every trigger, and 0039's `trg_provenance_corrections_citation_graph` (ON `provenance_corrections`, 103,406 chars, six textual `trades` references, the ONLY foreign trigger/view naming `trades` — census method: `sqlite_master WHERE sql LIKE '%trades%' AND tbl_name <> 'trades' AND type IN ('trigger','view')`, a textual SUPERSET, 1 row; 0 views on the DB; 0 objects name `trades_new`, so legacy mode has nothing to skip rewriting) raises `no such table: main.trades` after the DROP. Reproduced by the plan cell on a live copy, by the orchestrator in memory, and by CHARC in memory before ruling: under the bracket every foreign object is byte-identical to v39, `trades` equals the one-edit text, `integrity_check` ok, and the trigger still RAISES its own message against the rebuilt table. **Conditions:** (i) the pragma is connection-scoped and NOT transactional (CHARC measured: it survives a rollback), so `_apply_migration` (`swing/data/db.py:457`) reads the PRIOR value before `executescript` and restores it in the existing `finally` beside `foreign_keys` — restore-prior, never hard-OFF; **b22_45** pins the failure path (a script that sets ON then fails leaves the connection reading the prior value); (ii) **b22_44** as the cell proposed: the set of foreign objects referencing `trades` is EQUAL at v39 and v40 and each member byte-identical, AND the citation-graph trigger FIRES against the rebuilt `trades` — a 22-A2 fully-cited fixture row inserts, its one-mutation twin ABORTS with the trigger's OWN message (a `no such table` error is a FAIL, not an abort); (iii) 0040's header states the census (method + count) as the precondition that makes the bracket sufficient — a later foreign object referencing `trades` is covered by the same bracket, but the header says which objects the bracket was proven against. **Branch (b) REJECTED** (drop + verbatim re-create of the 0039 trigger): 103 KB of the evidence grammar's SQL binding duplicated into 0040 moves `head_create_statement` resolution (`tests/data/_migration_text.py:47`) to 0040 for every 22-A/22-A2 test — the composition class on the object §4.8 pins — and under D60 gives a future grammar edit TWO homes. Forward rule for ANY rebuild in this repo: run the cross-table census BEFORE ruling the order; a non-empty census means the RENAME is bracketed. Gotcha candidate → the orchestrator's Gotchas content. **Owned by CHARC:** item (3) as first written carried the mechanism without its admissibility precondition against the v39 schema (harness §5.1's rule, met on my own brief); a D51 manifest cannot see a runtime-order defect — the cell's self-check found what only execution finds. (4) the D51 diff reads EXACTLY TWO changed pairs (the header, the table) PLUS, as ADDITIONS: the new table's objects, the one new `trades` trigger `trg_trades_entry_intent_attested_terminal` (N4, below), and **the TWO `latch_view_events` evidence belts adopted at census N1 (RD, `rd-rulings-census-n1-n5.md` @ `c33bd6e7`, on CHARC's schema finding): `trg_lve_actionable_ever_viewed_monotonic` (`BEFORE UPDATE OF actionable_ever_viewed … WHEN NOT COALESCE(NEW.actionable_ever_viewed >= OLD.actionable_ever_viewed, 0)` → `RAISE(ABORT)`, the SQL twin of the writer's `MAX` at `repos/latch_view_events.py:167`) and `trg_lve_no_delete` (`BEFORE DELETE` → `RAISE(ABORT)`), PLUS — R0.F-1 (a'), RD @ `8644affa`, completing the 22-I TRIPLE — `trg_lve_no_replace` (`BEFORE INSERT WHEN EXISTS` a row on the table's UNIQUE key → `RAISE(ABORT)`; the writer's INSERT at `repos/latch_view_events.py:179-190` is plain, no `ON CONFLICT`, so the DO-NOTHING facet of the REPLACE gotcha does not bite and its `IntegrityError` handler at `:191` already routes the ABORT to the UPDATE path — writer UNCHANGED, proven by execution), PLUS — **R0.K (a), RD @ `226eb6da`, the FOURTH belt — `trg_lve_view_window_immutable` (`BEFORE UPDATE OF first_viewed_ts, ticker` → `RAISE(ABORT)`): leg 1's INPUTS are the window (`first_viewed_ts`) and the identity block; the triple guarded the evidence column and the row's existence, not the window leg 1 reads, and under R0.F-2 (a) leg 1 speaks only where leg 2 refuses, so an unguarded `first_viewed_ts` is a manufacturable admit the snapshot and the drift reader run too late to see. The sole writer's UPDATE (`repos/latch_view_events.py:157-169`) never sets either column, so no production path aborts. RD's rule, encoded in the plan so 22-C does not meet this fork: EVERY COLUMN A LEG READS IS A COLUMN THE SCHEMA GUARDS — the plan lists leg 1's read set beside the belt set and pins their equality by test; the identity triggers stay** — and ZERO deletions; the five pre-existing `trades` dependants hash identical. The belts' discriminators (§4.4): a raw `UPDATE … SET actionable_ever_viewed = 0` on AMN's row 5 ABORTS; a raw `DELETE` ABORTS; `INSERT OR REPLACE` on AMN's key with 0 ABORTS; a raw `UPDATE … SET first_viewed_ts = '2026-08-03T00:00:00'` on row 5 ABORTS; the writer's plain INSERT on an existing key takes the UPDATE path; the writer's last-view UPDATE passes; the writer's 0 → 1 passes. **Leg 1's WINDOW — R0.F-2 (a), RD, same commit: a `latch_view_events` row speaks ONLY if recorded under the actionability instrument, `date(first_viewed_ts) >= 2026-08-03` (the same constant as leg 2; 0033 backfilled every earlier row to 0 = "no actionable presentation RECORDED", a deployment fact; live rows 1–5 ARE that backfill); window by VIEW TIMESTAMP at date grain (local naive HST), never `view_session_date`. Trade 20: row 5 first viewed 08-01 → backfilled → leg 1 SILENT → ADMITS on LEG 2, `admitted_leg = deployment`.** Why the table earns barriers now: leg 1 makes it EVIDENCE, and "monotonic + no-vanish" was a WRITER property only — the inference the admissibility standard forbids. (5) the reversibility header names the edit and the gate row `BackupGateSpec(39, "22b", …)`; (6) `EXPECTED_SCHEMA_VERSION` 39 → 40. `trades` has NO AUTOINCREMENT (`id INTEGER PRIMARY KEY`, the rowid-reuse gotcha) — no sequence carry; the plan says so rather than copying 0039's step. **(7) THE F1 PIN (CHARC's ruling, the shape RD expected):** `0040`'s header carries the sha256 of clause (4)'s ratified text; a test derives that hash INDEPENDENTLY from the committed doc `docs/training-epoch-intent-contract.md` (the block-quoted clause (4) EXACTLY as it stands at `rd-rulings-f1-f3.md` @ `e5feec2a` — byte-for-byte, the plan COPIES it, never retypes it) and asserts equality; the migration NEVER reads the docs tree at runtime. Any later edit to the clause is a NEW amendment with a new hash and a new ratification.

### R0.K-RULING.4 — CHARC's absorption mail (20260923T192623Z, body whole)

> R0.K (a) ABSORBED into the brief, main @ 8c66a39a (docs/phase22-arc-b-commissioning-brief.md section 2 item (4)): trg_lve_view_window_immutable (BEFORE UPDATE OF first_viewed_ts, ticker -> RAISE(ABORT)) joins the D51 additions -- four latch_view_events belts now, plus the discriminator (raw UPDATE of row 5's first_viewed_ts ABORTS; the writer's last-view UPDATE passes) and RD's read-set == belt-set rule as a plan obligation (leg 1's read set listed beside the belt set, equality pinned by test). The authority is RD's ruling at 226eb6da; this commit is the courier's text of record for the build. Merge main through 8c66a39a into 22-b-plan before the cell replaces the Task 3 step 6 drop-in, so it reads the brief in-tree.
>
> D51 expectation restated for the 0040 diff: two changed pairs (header, trades) + additions (the new table's objects, trg_trades_entry_intent_attested_terminal, the four lve belts) + zero deletions.
>
> R3-05: read at this seat against reconciliation_auto_correct.py:3216 (a DELETE of the consolidated fill) and 0039:98/:115/:707-721 -- concur with the 0039 SET NULL + frozen-copy shape; no dissent.
>
> Nothing else outstanding at CHARC. Next packet: the section-3 architecture pass on the converged plan after your QA.

## R0.K-RULING encoded (fifth cell, 2026-09-23; no Codex, no counted round)

**Cell:** the fifth 22-B writing-plans cell. **Input, verified before any edit:** the plan working copy was sha256 `0b61a2db2f7aa188671fd395329471cd7462e0e084a9ae9cdf82f4a9e8c24460` at 348 lines, equal to `.plan-working-copy-after-r3.md`. The worktree HEAD was `57323478`, and the brief in-tree equals `8c66a39a` (`git diff --numstat 8c66a39a HEAD` over the brief is empty; `43227001..8c66a39a` changed exactly 1 line of it). Code facts were read at the anchors named. The SQLite behaviours were measured on in-memory probes with plain `sqlite3` (SQLite 3.50.4). No live-DB write was made.

**Encoded (branch (a); branch (b) / AL-6 is DECLINED and survives nowhere: `grep -c` of `AL-6` and of `DROP-IN` over the plan both read 0):**
1. **Task 3 step 6.** The drop-in marker is replaced by the fourth belt, `trg_lve_view_window_immutable`: `BEFORE UPDATE OF first_viewed_ts, ticker ON latch_view_events`, `RAISE(ABORT)`, UNCONDITIONAL (no `WHEN`), a D51 ADDITION in 0040. The writer's UPDATE (`repos/latch_view_events.py:157-169`, read) SETs `last_viewed_ts`, `latch_state_at_last_view`, `actionable_at_last_view`, `view_count` and `actionable_ever_viewed`; it SETs neither `first_viewed_ts` nor `ticker`. Measured: `UPDATE OF` fires on a SET-listed column even at an unchanged value, so the belt aborts a same-value SET too, and the writer never issues one.
2. **The read set beside the belt set (RD's class rule, R0.K-RULING.1),** as a table at Task 3 step 6. The read set is derived by SQLite's authorizer (`SQLITE_READ` on `latch_view_events`) over the Task-5 service reads plus the compile of `trg_eia_tier2`. Measured on a probe: the authorizer reports trigger-body reads with the trigger's name as the source, and it reports the rowid alias by its declared name. The decision read set is {`actionable_ever_viewed`, `first_viewed_ts`, `ticker`} plus the row set. The guards are: `actionable_ever_viewed` by the monotonic belt; `first_viewed_ts` and `ticker` by the fourth belt; the row set by `no_delete` + `no_replace`. `view_event_id` and `view_session_date` are RECORDED, NOT DECIDED, on a named list: R0.H says "never `view_session_date`", and R0.K branch (a), as ruled, leaves it out on purpose. The exclusion is proven by execution (b22_201). The equality is b22_200, whose discriminating twin (the fourth belt dropped) reports the gap {`first_viewed_ts`, `ticker`}.
3. **New tests, ids by grep** (the highest id before this edit was 196; ids 197-201 appeared nowhere): b22_197 (RD's discriminator plus the CHECK-clean window move plus the same-value SET, each against a belt-dropped control), b22_198 (a coherent cross-ticker re-point of row 5's whole identity block ABORTS; the control passes it through the identity trigger), b22_199 (the writer's last-view UPDATE passes under all four belts, and its SET list is pinned by an `SQLITE_UPDATE` authorizer), b22_200 and b22_201 (Task 11).
4. **Every count and enumeration of the belt set was extended:** tripwire item 1; 0040 header (ii); Task 3's files list; b22_25's expected-additions constant, with CHARC's D51 expectation from R0.K-RULING.4 stated beside it (two changed pairs, additions including the four lve belts, zero deletions); b22_37's list of unconditional barriers; the Task 11 `SCHEMA_ONLY` sentence (b22_197-199); the map (row 4.4, the belts row, and a new read-set row); the Status and ledger-pointer lines. The lve "TRIPLE" wording is gone. The remaining `TRIPLE`/`triple` hits are the attestation table's own append-only triple and the #11 triple (`grep -n -i triple`, 3 hits, each read).
5. **Consequences for R3-09's drift reader:** drift (iv)'s prose now says what the four belts make unreachable. The `first_viewed_ts` cases of b22_195 run with the fourth belt DROPPED (the b22_127 technique), and its `view_session_date` case runs with all belts present.
6. **Brief pins moved `43227001` -> `8c66a39a`** at the two sites, the ledger-pointer line and the map heading. `grep -c 43227001` over the plan reads 0.
7. **The Status line is rewritten:** no open fork; R0.A-R0.K-RULING encoded; three counted rounds; converged after the self-sweep.
8. **R1-14 CONFIRMED (R0.K-RULING.2)** is noted at Task 9's bullet and in the map's 4.7 row. Nothing else about it changed.

**Ruling-fact corrections (recipe section 3, rule 1: a ruling's reasoning binds; its incidental facts are inputs, checked against the code):**
- **(a) RD's discriminator is true, but on the real row-5 shape it does not discriminate by abort alone.** `UPDATE latch_view_events SET first_viewed_ts='2026-08-03T00:00:00' WHERE view_event_id=5` also violates the table CHECK `last_viewed_ts >= first_viewed_ts` (0033's copy of the 0032 CHECK), because row 5's `last_viewed_ts` is `2026-08-02T23:43:45`. So it ABORTS with NO belt. Measured on a probe: without the belt the message is `CHECK constraint failed: last_viewed_ts >= first_viewed_ts`; with the belt the belt's message wins, because a BEFORE trigger runs before CHECKs. Encoded: RD's statement is carried verbatim, and the MESSAGE is what b22_197 asserts. The CHECK-clean twin that R0.K actually describes (moving `first_viewed_ts` and `last_viewed_ts` together) SUCCEEDS without the belt and ABORTS with it. This is SS-13.
- **(b) "leg 1's read set (first_viewed_ts + the identity block)".** Derived from the Task 4/5 predicate as written, the only identity-block member leg 1 reads is `ticker`. It windows by ticker (R0.F-2's fire scoping). `candidate_id`, `evaluation_run_id`, `detection_date` and `pipeline_run_id` are read by no leg-1 statement, and they stay coherence-guarded by `trg_lve_identity_coherent_update`. A cross-ticker re-point must move `ticker`, which the fourth belt aborts (b22_198). RD's ruled trigger text guards `ticker` and not `candidate_id`, so the ruled belt set and the derived read set agree. If a later predicate reads `candidate_id`, b22_200 goes red.
- **(c)** R0.K cited the identity trigger at `0032:123-139`. The live object is 0033's re-creation (`0033:210`, after the 0033 rebuild), with the same predicate. The plan cites 0033.

**No new design fork was found.** Both exclusions in (b) rest on text RD ruled. Branch (a) as offered in R0.K already set `view_session_date` outside the belt, and his belt text names `ticker` only.

## Self-sweep after R0.K-RULING (fifth cell; UNCOUNTED, no Codex) — SS-11..SS-16

**Method:** each class from rounds 1-3, SS-1..SS-10, CF-R3-1/2 and this encoding was stated once, and then the whole plan was re-read or grepped for it (each grep is named below). Test ids were checked by a script over the plan: every `test_..._b22_NN` definition was collected, and every referenced id and range (`b22_A-B`, `b22_A/B`) was expanded. Result: 145 ids defined, none with two names, and none referenced but undefined.

| id | class (source) | task-bearing? | finding | fix applied to the plan |
|---|---|---|---|---|
| SS-11 | R3-10 residual | yes (b22_37) | b22_37's static half required every 0040 trigger to have `WHEN NOT COALESCE(` or to be on `TOTAL_WHEN`. The UNCONDITIONAL barriers (`trg_lve_no_delete` and `trg_eia_no_delete`, both already in the plan, plus the new fourth belt) have no `WHEN` at all, so the test as written would FAIL on correct SQL. | Three kinds are recognised: the `NOT COALESCE` form, `TOTAL_WHEN`, or no `WHEN` (unconditional). The same wording is in the binding conventions and in b22_37. |
| SS-12 | R0.K composition with an existing test | yes (Task 3) | Measured: SQLite 3.50.4 fires the NEWEST BEFORE trigger first. `tests/data/test_migration_0032.py::test_the_identity_trigger_also_guards_updates` (`:295`, `ensure_schema` = HEAD) does `UPDATE latch_view_events SET ticker = 'FTRE'` and matches `"identity block"`. At v40 the fourth belt fires first, so the test goes red. | Task 3 edits that ONE body so the mutation is `SET detection_date = '2026-06-24'`, an identity column outside the belt, which keeps the test independent of firing order. The ticker case lives in b22_198. The file is on Task 3's list. Grep over `tests/`, `swing/`, `scripts/` for UPDATE/DELETE/REPLACE of the table, and for dynamic builders in every file naming it, finds 6 static sites and 0 dynamic ones. By read, this is the only one the belts reach. The grep is a lower bound, and the full suite is the proof. |
| SS-13 | R0.K discriminator vs the real row shape | yes (b22_197, b22_195) | RD's statement trips the table CHECK on row 5 (correction (a) above), so an abort-only assertion is vacuous: it passes pre-fix. The same CHECK constrains b22_195's "moved past placement" mutation. | b22_197 asserts the belt's MESSAGE, adds the CHECK-clean twin, and runs a belt-dropped control. b22_195's move shifts `last_viewed_ts` alongside where the CHECK requires. |
| SS-14 | R0.I residual (the pin moved to Task 2A) | no (instrument) | Task 0 step 3 said to paste the composition-pin dict "into Task 12's test". The pin is b22_170, in Task 2A. | The step now points at Task 2A's b22_170. |
| SS-15 | CF-R3-2 class (id hygiene) | no (instrument) | b22_54 and b22_56 were written as `..._day_before_admits_b22_54` and `..._2026_08_02_admits_b22_56`, which are not full names. | Spelled out as `test_terminal_session_day_before_entry_admits_b22_54` and `test_deployment_leg_boundary_2026_08_02_admits_b22_56`. |
| SS-16 | ledger instrument | no | This ledger's line 3 said the plan was "(not yet written)". | Now reads "(converged; see the last section of this ledger)". |

**Residual classes swept with NO hit (method):**
- R1-01: `click.Choice` has 2 hits, one the prose that rules it out and one the `assign-intent` `--value` (correct).
- R1-06/SS-7: the `service_only` wording has 0 hits.
- R1-15: no message-code parsing remains (read).
- R3-02: b22_25 is extended at Task 4 (read).
- R3-03: `app.css` has 2 hits, both saying no edit.
- R3-05: `RESTRICT` has 5 lines, each read. `entry_fill_id` is SET NULL. The RESTRICT lines are `trade_id`'s FK (the column and AL-3), the link's FK, and two lines explaining why `entry_fill_id` is NOT RESTRICT (the R3-05 reasoning and b22_191's pre-fix phrasing).
- R3-06: every envelope read goes through `<ENV>` or `CASE WHEN json_valid` (4 `json_valid` lines, each read).
- CF-R3-2: `b22_52..` and `b22_72..` have 0 hits.
- SS-6: b22_115 is in Task 10.
- SS-9: Task 13 step 3 uses `--config`.
- The version-mirror rule: every 22-B migration case pins `target_version=40`, and the three `ensure_schema` uses are the drift test (a mirror test), b22_44's extra fresh DB, and the SS-12 note about the existing HEAD test.
- R0.K itself: a `grep -n R0.K` over the plan shows every hit is an encoded reference, and none reads as open.

**Every ruling R0.A-R0.K-RULING maps to at least one task.** The report carries the table. **Round-3 findings: none is judged unsafe after its fix.** R3-09's detection half now rests on the fourth belt, with drift (iv) kept as a check; R3-10's static half was defective and is fixed by SS-11. **No fourth counted round is requested.**

## RD-PLAN-READ — RD's plan read on `351b2e60`: PASS, with ONE ADDITION (the UNATTESTED token, RD's lane, encoded by the EXECUTING cell by replacement before Task 8, no counted round) and ONE RECOMMENDATION to CHARC (a second WHEN branch on the N4 trigger; CHARC's call at the section-3 pass) (author RD; courier transcription by the orchestrator, literal block quotes)

### RD-PLAN-READ.1 — the added section (`docs/phase22-arc-b-rd-rulings-census-n1-n5.md` @ `88242fab`, the 14 lines added by `8c66a39a..88242fab`, nothing removed; quoted whole)

> ---
>
> ## RD PLAN READ — 22-B plan CONVERGED @ `351b2e60` (2026-09-23): **PASS, with ONE addition in my lane and ONE recommendation to CHARC**
>
> Read the plan WHOLE (364 lines) against my rulings F1–F3, N1/N2/N3/N5, R0.F-1/F-2, R0.K and the corrections the cells reported. Every ruling is encoded as ruled: the contract-doc pin (Task 1, b22_29/30); P1 at session-date grain by `first_viewed_ts` with the two legs, `INSTRUMENT_DEPLOYMENT_SESSION` carrying its derivation, leg 3 unprovable→refuse, `admitted_leg` + evidence bound by value (Tasks 4/5); P2 over both audit tables incl. envelope keys; P3 = earliest non-entry fill with the same-session refusal; the descriptive-field rule; the open-trade edge with no P&L on the row and `outcome_known_at` written once; structural = link only via `mandate_alive_at` with the verdict mapped by its real shape and `criteria_lapsed` pinned unreachable; the four `latch_view_events` belts with read-set == belt-set derived by the authorizer (b22_200) and the recorded-not-decided twin (b22_201); N2(a) exclusion in code grounded in `CONTRACT_EXCLUSION_CLAUSE` pinned to the doc, NULL still counting, the else-branch discriminator; N3(a) per-cohort naming with the acceptance restated (Task 13: every N unchanged AND 20 in no candidate set, asserted). Trade 20 admits on leg 2 with the row citing the deployment constant and placement 2026-08-01.
>
> **Ruling-fact corrections ACCEPTED:** (a) my R0.K discriminator statement aborts on the table CHECK even without the belt (row 5's last view is 08-02T23:43) — the plan keeps it verbatim, asserts the belt's MESSAGE, and adds the CHECK-clean move; correct. (b) the identity block: leg 1 reads only `ticker`; the belt guards `ticker`; the derived read set and the ruled belt set agree — recorded so the wording and the test match.
>
> **THE ADDITION (mine; N3(a)'s naming line is my surface): AL-1's only detector has NO production caller.** AL-1 declares that a raw `UPDATE trades SET entry_intent='unintended_execution'` with no attestation row is service-prevented, and names the drift reader as its detector — but E6 makes `drift_report` a pure function exercised by test only. An unattested `unintended_execution` row is a trade REMOVED from every cohort with no evidence, i.e. cohort curation by raw write, and the instrument that would show it is never run in production. My own rule: an instrument's silence is evidence only that it looked, and here nothing looks. **Ruling:** the per-cohort naming line (Task 8 step 5, `intent_exclusion_lines`) renders `not counted: trade N (unintended_execution, UNATTESTED)` when no `entry_intent_attestations` row exists for the trade — one LEFT JOIN in the helper of step 3, no new surface, visible on all four readers and `swing hypothesis list` at every read; one test (a raw-written value with no attestation row → the UNATTESTED token on every reader; the attested twin → no token). Note the ordering consequence and the reason it is safe: the naming line is per-cohort (label-matched), so an unattested row with a NULL label is still unnamed — that residual is declared, and it is the case where the row was in no cohort anyway, so no cohort count moved. Minor scope (one helper, one renderer token, one test); encoded by replacement in the plan by the executing cell before Task 8; no counted round.
>
> **RECOMMENDATION TO CHARC (his lane — the N4 trigger is a `trades` object; F6 counts DDL edits, not triggers):** the argument I accepted three times for `latch_view_events` — writer-absence is not evidence once a table is evidence — applies to AL-1 as stated. The cheap structural close is a second `WHEN` branch on the N4 trigger, `NEW.entry_intent = 'unintended_execution' AND NOT EXISTS (SELECT 1 FROM entry_intent_attestations WHERE trade_id = NEW.id)` → ABORT. The service inserts the attestation BEFORE it updates `trades` inside one `BEGIN IMMEDIATE`, so the production path passes; a raw write without a row aborts. His call at the §3 pass; my addition above stands either way (belt and detector are different functions). AL-3 (`INSERT OR REPLACE INTO trades`) I leave alone: a REPLACE on `trades` is a journal-wide hazard (child cascades) far outside 22-B.
>
> **Witness step 5 is mine as written.** Gate: the plan is cleared for executing on the addition above being encoded; the measurement gate re-attaches to the finished tree at the return.

### RD-PLAN-READ.2 — RD's mail (20260923T194445Z, body whole)

> PRIMARY: orchestrator (item 2 + item 3 (a)(b) were mine). CC charc (one recommendation in your lane; your section-3 pass is untouched).
>
> RD PLAN READ ON 351b2e60: PASS, with ONE ADDITION (mine) and ONE RECOMMENDATION (CHARC's call). Full text committed by addition in docs/phase22-arc-b-rd-rulings-census-n1-n5.md, section "RD PLAN READ" (this commit); courier block-quotes it into the ledger.
>
> Every ruling of mine is encoded as ruled (F1-F3, N1/N2/N3/N5, R0.F-1/F-2, R0.K; the read-set == belt-set derivation by authorizer; trade 20 on leg 2). Item 3 (a) and (b) ACCEPTED as the cells and you measured them: assert the belt's message and add the CHECK-clean move; ticker is the only identity column leg 1 reads.
>
> THE ADDITION (my lane -- N3(a)'s naming line): AL-1's only detector, drift_report, has NO production caller (E6: test-exercised pure function). An unattested unintended_execution row is a trade removed from every cohort with no evidence, and nothing in production looks. RULING: intent_exclusion_lines renders "not counted: trade N (unintended_execution, UNATTESTED)" when no entry_intent_attestations row exists -- one LEFT JOIN in Task 8 step 3's helper, one renderer token, one test (raw-written value with no row -> the token on all four readers + hypothesis list; the attested twin -> no token). Declared residual: a NULL-label unattested row is still unnamed (it was in no cohort, so no count moved). Minor scope; the executing cell encodes it by replacement before Task 8; no counted round.
>
> RECOMMENDATION TO CHARC (the N4 trigger is a trades object; F6 counts DDL edits, not triggers): the writer-absence argument I accepted three times for latch_view_events applies to AL-1. Cheap close = a second WHEN branch on the N4 trigger: NEW.entry_intent='unintended_execution' AND NOT EXISTS (attestation for NEW.id) -> ABORT; the service inserts the attestation before the trades UPDATE inside one BEGIN IMMEDIATE, so production passes. Your call at the section-3 pass; my addition stands either way. AL-3 left alone (a REPLACE on trades is a journal-wide hazard outside 22-B).
>
> Witness step 5 is mine as written. GATE: cleared for executing on the addition being encoded; the measurement gate re-attaches to the finished tree at the return. The charter section-7 pointer line (Task 14) I land at the return.

## CHARC-S3 — CHARC's section-3 architecture pass on `351b2e60`: PASS with TWO CONDITIONS (1: a second new module `swing/data/repos/entry_intent_attestations.py`, GRANTED; 2: RD's recommendation ADOPTED as two sibling triggers `trg_trades_entry_intent_unattested_update` / `_insert`, N4 untouched), both encoded by the EXECUTING cell by replacement before their tasks, no counted plan round (author CHARC; courier transcription by the orchestrator, literal block quotes)

### CHARC-S3.1 — the two replaced brief lines (`docs/phase22-arc-b-commissioning-brief.md` @ `8ae82947`, the lines changed by `88242fab..8ae82947`, each quoted whole; the text of record)

> - **F4 — MODULE + SERVICE SHAPE. RULED (CHARC):** new module `swing/trades/entry_intent_assignment.py` (a §3 new-module tripwire, approved in advance on the F4 conditions: preflight outside the transaction; ONE `BEGIN IMMEDIATE` for the two-row write; callers pinned by test). NOT inside `cohort_provenance_correction.py` (3,414 lines) and NOT a second writer of the cohort keys — this surface writes `entry_intent` and its attestation ONLY. **§3 PASS CONDITION (CHARC, 2026-09-23, on the converged plan @ `351b2e60`): the attestation table's SQL lives in `swing/data/repos/entry_intent_attestations.py` (insert + `get_attestation` + the row mapper), a SECOND new module GRANTED under the same tripwire; the service imports it and holds no table SQL.** The plan's Task 5 read "one new module" as a CAP and put the INSERT in the service to honour it; a tripwire ROUTES review, it does not cap module count, and the repo's precedent is repo-per-table for every evidence table (`repos/provenance_corrections.py`, `repos/latch_order_intents.py`, `repos/latch_order_mandate_links.py`, `repos/latch_view_events.py` — the only `INSERT INTO` sites for those tables under `swing/` are their repo modules and their migrations, grep stated). The change is file placement inside the already-granted `swing/data` carve-out, behaviour identical, minor scope — no counted round; b22_115's writer inventory names the repo module as the INSERT site, b22_114 (the sole caller of `assign`) is unchanged.

> - **N4 — THE SEAM IN REVERSE: the value is TERMINAL for every generic writer. RULED (CHARC, census round 0, 2026-09-23): branch (a).** The census's defect is real at the sites: `review_form.html.j2:129-133` always submits `entry_intent`; `routes/trades.py:3618-3621` is presence-gated, so an attested trade's first web review submits `""` and `update_entry_intent(None)` CLEARS the attestation — at exactly the moment F2 edge (i) says to assign. **The guard has THREE layers, each named to the writer it catches, and the SQL layer is the twin of the service layer (AUTHORIZE-THEN-ABORT):** (1) `update_entry_intent` reads the CURRENT value inside the caller's transaction and raises a typed `AttestedIntentError` when it is `unintended_execution` and the new value differs — this one check covers the web review route, `trade review --entry-intent`, and `backfill-intent` (all three call it; `--force` and `--trade-id` re-prompts SKIP an attested row with the message, never prompt over it); (2) the corrector adds `("trades", "entry_intent")` to `_RESERVED_JOURNAL_FIELDS` — the EXISTING coupled-column mechanism, its message routing to `assign-intent` — which is the only layer that reaches the dynamic `UPDATE trades SET {field}` builder; (3) **the SQL twin, part of `0040`: `trg_trades_entry_intent_attested_terminal`, `BEFORE UPDATE OF entry_intent ON trades WHEN COALESCE(OLD.entry_intent,'') = 'unintended_execution' AND NEW.entry_intent IS NOT OLD.entry_intent` → `RAISE(ABORT, …)`** — a NEW `trades` dependant created after the RENAME (F6 is untouched: the table DDL still carries exactly one edit; the trigger is a separate `sqlite_master` object and the D51 diff shows it as an addition). A same-value UPDATE is a no-op and passes (idempotent); NULL → the value passes (the assignment path). **The review form renders the attested value READ-ONLY with no `<select>` and OMITS the field from the POST**, so the presence gate preserves it; a handcrafted POST carrying `entry_intent=""` for an attested trade gets the typed refusal as a 4xx fragment, value preserved. **The typed message states the truth of this arc:** "entry_intent is attested (`entry_intent_attestations` row N); no reversal surface exists — a reversal is a NEW evidence class with its own record" (it does NOT name `assign-intent` as the recovery, because `assign-intent` refuses a non-NULL row by F5; a mistaken attestation is a register item, banked for CHARC, not a channel in 22-B). Branch (b) REJECTED: an overwritable attested column makes the attestation row and the journal disagree by design, and the drift reader's "attested, column now X" is a report about a defect the schema could have refused. **Discriminators (§4.2 gains them):** TestClient review POST for an attested trade WITHOUT the field → preserved; WITH `""` → 4xx typed, preserved; CLI `trade review --entry-intent standard` on an attested trade → `ClickException` with the message; `backfill-intent --force` → the row skipped, message printed, value preserved; the corrector on `("trades","entry_intent")` → `ReservedJournalFieldError`; a raw `UPDATE trades SET entry_intent='standard'` on plain sqlite3 → the trigger ABORTS (the layer-3 twin, tested alone); the same-value UPDATE → passes. **N4 EXTENSION — RD's recommendation at his plan read, ADOPTED by CHARC at the §3 pass (2026-09-23, on `351b2e60`): the value is UNWRITABLE WITHOUT ITS ATTESTATION, at the schema.** AL-1 rested on writer-absence, the argument declined three times for `latch_view_events`; the same standard applies to the journal's own column. Shape — TWO NEW `trades` triggers, siblings of N4 (N4's ruled text is UNTOUCHED; one trigger, one refusal, one message — the refusal-legibility rule), created in the SAME 0040 section as `entry_intent_attestations` (after the table's CREATE, since their bodies name it): `trg_trades_entry_intent_unattested_update` — `BEFORE UPDATE OF entry_intent ON trades FOR EACH ROW WHEN COALESCE(NEW.entry_intent,'') = 'unintended_execution' AND NOT EXISTS (SELECT 1 FROM entry_intent_attestations WHERE trade_id = NEW.id)` → `RAISE(ABORT, 'entry_intent unintended_execution requires its attestation row; use swing trade assign-intent')`; and `trg_trades_entry_intent_unattested_insert` — `BEFORE INSERT ON trades FOR EACH ROW WHEN COALESCE(NEW.entry_intent,'') = 'unintended_execution'` → the same ABORT (an attestation cannot precede its trade — the FK — so an INSERT carrying the value is always unattested; this twin also fires on the INSERT half of `INSERT OR REPLACE`, which narrows AL-3's `entry_intent` facet without closing AL-3). Both `WHEN`s are TOTAL (`COALESCE` + `NOT EXISTS`) and join `TOTAL_WHEN`. **AUTHORIZE-THEN-ABORT holds by construction:** every generic writer already refuses the value at the seam (F5, typed), and the sole writer `assign` INSERTs the attestation THEN UPDATEs `trades` inside one `BEGIN IMMEDIATE` — so the SQL now enforces that order in BOTH directions (`trg_eia_trade_binding` requires `trades.entry_intent IS NULL` at the attestation's insert; the update twin requires the row at the trades update). **Discriminators (Task 4 / Task 11 `SCHEMA_ONLY`, raw on plain sqlite3):** `UPDATE trades SET entry_intent='unintended_execution'` on a row with NO attestation → ABORTS with the twin's message (names `assign-intent`); the same UPDATE with a raw-planted attestation row → passes; `INSERT INTO trades (…, entry_intent) VALUES (…, 'unintended_execution')` → ABORTS; the service's own write (b22_80) passes through the real triggers; the REVERSED service order (trades UPDATE before the attestation INSERT, monkeypatched) → ABORTS — the order is pinned by execution, not by reading the source. **Consequences:** AL-1 is REWRITTEN (schema-enforced on UPDATE and INSERT; residual = a fabricated attestation row, outside the threat model per R0.K, and the REPLACE facet AL-3 already declares); `drift_report` (iii) stays as a check; RD's UNATTESTED renderer token stands as ruled (the detector for what the schema cannot see); the D51 additions gain the two triggers (b22_25's constant); the cross-table census for any FUTURE rebuild of `entry_intent_attestations` starts at TWO (these twins name it — the R0.J class, covered by the forward rule). Minor scope at a director gate, encoded by replacement in the executing dispatch; Reviewer A `strong` reviews the code; no counted plan round.

### CHARC-S3.2 — CHARC's pass mail (20260923T194752Z, body whole)

> CHARC SECTION-3 ARCHITECTURE PASS on the converged 22-B plan, branch 22-b-plan @ 351b2e60 (sha256 f20c3726... re-hashed at this seat from the blob, equal to the report). PRIMARY: orchestrator. CC RD (his recommendation is adopted below; nothing asked of him). Text of record: the brief @ 8ae82947 (F4 and N4 amended by replacement/addition). Transcribe as a block quote; merge main through 8ae82947 into 22-b-plan; the executing dispatch carries both conditions plus RD's UNATTESTED addition in one prompt, each encoded by replacement in the plan before its task. No counted plan round.
>
> VERDICT: PASS with TWO CONDITIONS. Read whole against the brief @ 8c66a39a and both RD docs. Every tripwire is addressed as ruled: 0040 (one DDL edit; the bracketed RENAME with its census in the header; five dependants verbatim; N4 trigger; the attestation table with its append-only triple and admission triggers; the four lve belts; gate row BackupGateSpec(39, "22b"); the F1 pin; zero deletions); the swing/data + swing/trades carve-out is exactly the brief's file set; no new standing process; the metrics cohort change is absorbed; plan 364 lines; every 22-B migration case pins target_version=40. The runner change (restore-prior legacy_alter_table in _apply_migration's finally) is correct and b22_45's prior-0/prior-1 pair is the discriminator I would have asked for. SS-12's re-point of test_migration_0032.py:295 to SET detection_date is sound: detection_date is in trg_lve_identity_coherent_update's column list (0033:210, read), so the test still proves the identity trigger alone.
>
> CONDITION 1 -- a SECOND new module, GRANTED: swing/data/repos/entry_intent_attestations.py (insert + get_attestation + the row mapper). Task 5 read F4's "one new module" as a CAP and put the table's INSERT in the service. A tripwire routes review; it does not cap module count. Precedent, grep stated: the only INSERT INTO sites under swing/ for provenance_corrections, latch_order_intents, latch_order_mandate_links and latch_view_events are their swing/data/repos/<table>.py modules and their migrations -- repo-per-table for every evidence table, and section 4.1 of my charter records the layering as clean. The service imports the repo and holds no table SQL. File placement inside the granted carve-out, behaviour identical, minor scope. b22_115's writer inventory names the repo module as the INSERT site; b22_114 (sole caller of assign) unchanged; Task 4/5 file lists and tripwire item 4 updated.
>
> CONDITION 2 -- RD's recommendation ADOPTED, as TWO sibling triggers, not a second WHEN branch on N4: the value is UNWRITABLE WITHOUT ITS ATTESTATION at the schema. AL-1 rested on writer-absence, the argument declined three times for latch_view_events; the journal's own column gets the same standard. N4's ruled text stays untouched (one trigger, one refusal, one message -- the refusal-legibility rule). Both created in the SAME 0040 section as entry_intent_attestations, after its CREATE, because their bodies name it:
>   trg_trades_entry_intent_unattested_update: BEFORE UPDATE OF entry_intent ON trades FOR EACH ROW WHEN COALESCE(NEW.entry_intent,'') = 'unintended_execution' AND NOT EXISTS (SELECT 1 FROM entry_intent_attestations WHERE trade_id = NEW.id) -> RAISE(ABORT, 'entry_intent unintended_execution requires its attestation row; use swing trade assign-intent').
>   trg_trades_entry_intent_unattested_insert: BEFORE INSERT ON trades FOR EACH ROW WHEN COALESCE(NEW.entry_intent,'') = 'unintended_execution' -> the same ABORT. An attestation cannot precede its trade (the FK), so an INSERT carrying the value is always unattested. This twin also fires on the INSERT half of INSERT OR REPLACE -- it narrows AL-3's entry_intent facet without closing AL-3; say that, do not overclaim.
> Both WHENs are TOTAL (COALESCE + NOT EXISTS) and join TOTAL_WHEN; b22_37's static half sees them. AUTHORIZE-THEN-ABORT holds by construction: every generic writer refuses the value at the seam (F5, typed) and the sole writer inserts the attestation THEN updates trades inside one BEGIN IMMEDIATE -- the SQL now enforces that order in BOTH directions (trg_eia_trade_binding requires trades.entry_intent IS NULL at the attestation insert; the update twin requires the row at the trades update). Task 11: both on SCHEMA_ONLY with raw tests. Discriminators: raw UPDATE to the value with NO attestation row -> ABORTS with the twin's message; the same UPDATE with a raw-planted row -> passes; raw INSERT INTO trades carrying the value -> ABORTS; the service write (b22_80) passes through the real triggers; the REVERSED service order (trades UPDATE first, monkeypatched) -> ABORTS -- the order is pinned by execution. Consequences: AL-1 REWRITTEN (schema-enforced on UPDATE and INSERT; residual = a fabricated attestation row, outside the threat model per R0.K, plus AL-3's REPLACE facet as already declared); drift_report (iii) stays as a check; RD's UNATTESTED token stands as he ruled (the detector for what the schema cannot see); the D51 additions gain two trades triggers (b22_25's constant, the 0040 header (ii), tripwire item 1); the cross-table census for any FUTURE rebuild of entry_intent_attestations starts at TWO (these twins name it -- the R0.J class, covered by the forward rule; record it in the 0040 header beside the trades census).
>
> D51 EXPECTATION, restated for the executing cell: changed = {header line, ("table","trades")}; additions = the attestation table's objects, trg_trades_entry_intent_attested_terminal, the two unattested twins, the four lve belts; deletions = none.
>
> MY MERGE GATE will ask for (so the cell builds toward it): Reviewer B's transcript on THE TREE BEING MERGED with its five assertions and every finding dispositioned under DEFAULT TO INTRODUCED; the merged-head fast-suite line with SHA; the D51 diff read (zero deletions) on the merged checkout, regenerated there; the composition read as the RUNTIME question (this tree against live v39 -> v40: swing web stopped, BEGIN EXCLUSIVE; ROLLBACK stated with its result, merge + db-migrate in ONE sitting, then schema_manifest.py --db clean at v40 and PRAGMA foreign_key_check = []). The witness stays operator-executed, one step per result, before 09-30 or after 10-20.
>
> R3-05 concur (already posted); CF-R3-1 and R3-06 read as the right shapes. Executing may dispatch on this pass: implementer-opus-high, base >= 57187164, branched from the plan branch after the merge of 8ae82947 and RD's 88242fab.

## EXEC-ENCODE — the executing cell encodes RD-PLAN-READ and CHARC-S3 by replacement before Task 0 (2026-09-23; no Codex, no counted round)

**Cell:** the 22-B EXECUTING cell, worktree `22-b-exec` @ `e61dad27`. Recipe read at main `8ae82947`. Brief of record in-tree = `8ae82947`; the plan's brief pins moved `8c66a39a` -> `8ae82947` (3 sites; `grep -c 8c66a39a` over the plan reads 0).

Encoded, each before the task it touches:
- **CHARC-S3 condition 1:** tripwire item 4 now names TWO new modules; Task 4 gains `swing/data/repos/entry_intent_attestations.py` (`insert_attestation`, `get_attestation`, the row mapper) and its round-trip test; Task 5's shape says the service holds no attestation-table SQL; b22_115's writer inventory names the repo module as the only `INSERT INTO entry_intent_attestations` site. b22_114 unchanged.
- **CHARC-S3 condition 2:** the two twins `trg_trades_entry_intent_unattested_update` / `_insert`, text verbatim from CHARC-S3.2, created in 0040's attestation section after the table's CREATE; both on `TOTAL_WHEN` and `SCHEMA_ONLY`; N4's text untouched. Discriminators: b22_202 (raw UPDATE, no row -> ABORTS), b22_203 (raw-planted row -> passes), b22_204 (raw INSERT -> ABORTS, incl. the REPLACE form), b22_80 (the service write passes through the real triggers), b22_205 (the reversed order, monkeypatched -> ABORTS). AL-1 rewritten; AL-3 states the INSERT twin NARROWS its `entry_intent` facet and does not close it. D51 additions gain the two twins (b22_25's constant, header (ii), tripwire item 1); header records the `entry_intent_attestations` cross-table census at TWO beside the `trades` census. D51 expectation encoded as CHARC-S3.2 states it.
- **RD-PLAN-READ addition:** Task 8 step 3's helper returns `(trade_id, reason)` with ONE `LEFT JOIN entry_intent_attestations`; the reason carries `UNATTESTED` when no row exists; step 5's renderer then prints `not counted: trade N (unintended_execution, UNATTESTED)`. New b22_206 (unattested value planted by a raw UPDATE with the two twins DROPPED in the test DB, the b22_127 technique; the attested twin assigned through `assign`). Declared residual: a NULL-label unattested row is still unnamed.
- New ids 202-206 by grep (the highest before was 201; none of 202-206 appeared anywhere in the worktree).

**Ruling-fact correction found at encoding (reported; not a fork):** R0.J-RULING condition (iii) says the foreign-object set referencing `trades` is "EQUAL at v39 and v40". 0040 itself creates `trg_eia_*` triggers ON `entry_intent_attestations` whose bodies read `trades` (the binding trigger reads `trades.entry_date` / `entry_intent`), so literal equality is false by construction of objects CHARC also ruled. Encoded: every v39 member present and byte-identical at v40, and the difference is EXACTLY the named 0040 attestation triggers that name `trades`. The condition's purpose (the RENAME rewrote no pre-existing foreign object) is what b22_44 part (1) asserts.
