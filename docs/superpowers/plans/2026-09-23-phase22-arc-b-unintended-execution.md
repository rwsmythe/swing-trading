# 22-B — Demand A: `unintended_execution` (the third `entry_intent` value + its evidence-bearing assignment surface) — IMPLEMENTATION PLAN

**Status:** CONVERGED. No fork is open. Every ruling R0.A-R0.K-RULING is encoded, and the plan carries only the ruled branches (R0.J-RULING: the RENAME bracket, branch (a); R0.K-RULING: the fourth `latch_view_events` belt, branch (a)). Reviewer A (`fast`, plan loop) ran its three counted rounds; the uncounted self-sweep followed (ledger SS-11+). The plan is executable as written.
**Ledger (arguments, rulings, census, review history):** `docs/superpowers/plans/2026-09-23-phase22-arc-b-unintended-execution.ledger.md` — R0.A (RD F1-F3), R0.B (CHARC scope + F4-F6), R0.C (census K1-K21, C.1-C.6, E1-E8), R0.D (CHARC N4), R0.E (RD N1/N2/N3/N5), R0.F (fresh-cell forks + E9-E15), R0.G/R0.H (RD: the first three `latch_view_events` belts; the leg-1 window), R0.I (CHARC: the three 22-A2 cases re-scoped to 39), R0.J/R0.J-RULING (the RENAME bracket), R0.K/R0.K-RULING (RD: the fourth belt `trg_lve_view_window_immutable`; the read-set == belt-set rule; R1-14 confirmed). **The brief of record is `docs/phase22-arc-b-commissioning-brief.md` @ `0f63d6a6`.**
**Base for executing:** `main` at or above `57187164` (22-A2 merged), with this plan merged. **Executing cell:** `implementer-opus-high` (schema migration on the journal of record). **Reviewer A:** `strong`, to convergence. **Reviewer B:** the orchestrator's, on the finished tree. **STOP LINE:** B clean by 2026-09-29 EOD HST or the witness waits for 10-21.

## Tripwires crossed (§3) — and only these

1. **Migration `0040`** (v39 → v40): a `trades` REBUILD carrying exactly ONE DDL edit (the `entry_intent` IN-list gains `'unintended_execution'`); the five existing `trades` dependants re-created VERBATIM; ONE new `trades` trigger `trg_trades_entry_intent_attested_terminal` (N4 layer 3); ONE new append-only table `entry_intent_attestations` with its triggers; TWO more new `trades` triggers created in the attestation section after that table's CREATE, `trg_trades_entry_intent_unattested_update` and `trg_trades_entry_intent_unattested_insert` (CHARC-S3 condition 2, RD's recommendation adopted: the value is unwritable without its attestation row); the FOUR `latch_view_events` evidence belts (`trg_lve_actionable_ever_viewed_monotonic`, `trg_lve_no_delete`, `trg_lve_no_replace` — R0.G/R0.H; `trg_lve_view_window_immutable` — R0.K-RULING); all D51 ADDITIONS. Gate row `BackupGateSpec(39, "22b", …)`. ZERO D51 deletions.
2. **`swing/data` + `swing/trades` carve-out:** `swing/data/models.py` (enum widening, split constants, two error types, the attestation dataclass), `swing/data/db.py` (version + gate row + `_apply_migration` restoring the prior `legacy_alter_table`, R0.J), `swing/data/repos/trades.py` (`update_entry_intent` seam + N4 layer 1), `swing/trades/entry.py` (`EntryRequest` seam), `swing/trades/reconciliation_auto_correct.py` (N4 layer 2), `swing/trades/intent.py` (display split).
3. **`swing/metrics` cohort code** (N2 (a)): `swing/metrics/cohort_intent.py`, `swing/metrics/cohort.py`, and the four governed readers (`swing/journal/stats.py`, `swing/metrics/tier.py`, `swing/recommendations/hypothesis.py`, `swing/web/view_models/metrics/hypothesis_progress_card.py`) — no new tripwire, CHARC's §3 pass absorbs it.
4. **TWO new modules (CHARC-S3 condition 1):** `swing/trades/entry_intent_assignment.py` (F4, the service) and `swing/data/repos/entry_intent_attestations.py` (the attestation table's SQL: `insert_attestation` + `get_attestation` + the row mapper, the repo-per-table precedent of every evidence table). The service imports the repo and holds NO table SQL.
Plus `swing/cli.py` (the `assign-intent` command; the seam at three existing commands), `swing/web/` (review form read-only, entry form seam, one intent CSS-class token in the trend map with no `app.css` rule, labels). **Nothing else.** OUT: see brief §2 OUT (22-C, 22-E, any UI for assignment, relabel paths, a death-then-fill primitive, D58, any second `trades` edit).

## Binding conventions for the executing cell

- TDD per task: failing test → SEE it fail the RIGHT way → minimal code → green → commit (`feat(…): Task N — …`). Compute every discriminator under BOTH the pre-fix and post-fix path.
- **One transaction per migration (gotcha #9)**; the migration runner owns BEGIN/COMMIT; `foreign_keys=OFF` at the runner, so `PRAGMA foreign_key_check` after RENAME is asserted by TEST.
- **Every VALIDATION trigger's `WHEN` is `NOT COALESCE(<predicate>, 0)`** (a NULL `WHEN` fails open). A BARRIER trigger whose `WHEN` is not of that form sits on a named `TOTAL_WHEN` list and is shown non-NULL by execution over every NULL operand combination. The list is the N4 trigger (its ruled text, R0.D), `trg_lve_no_replace` and `trg_eia_no_replace` (`EXISTS` is never NULL), and the two unattested twins `trg_trades_entry_intent_unattested_update` / `_insert` (CHARC-S3.2: `COALESCE(NEW.entry_intent,'') = …` and `NOT EXISTS` are never NULL). An UNCONDITIONAL barrier has NO `WHEN` clause at all (`trg_lve_no_delete`, `trg_eia_no_delete`, `trg_lve_view_window_immutable`): it fires on every matching row, so no NULL hazard exists. b22_37 enforces all three kinds statically (R3-10, SS-11). **Every table CHECK is NULL-closed** (a CHECK that evaluates NULL PASSES — each CHECK either is total over NOT NULL columns or is covered by the paired-tier CHECK; a per-CHECK NULL-mutation test proves it).
- **One rounding authority:** no numeric comparison crosses Python↔SQL in this arc; every compared value is TEXT (dates as `YYYY-MM-DD`, text snapshots as JSON strings) compared BYTEWISE.
- **Every migration commit regenerates the D51 manifest** (`python scripts/schema_manifest.py --write`, from THIS checkout) and the diff is READ: zero pure DELETIONS -- a changed object reads as a `-`/`+` PAIR, and 0040's diff is EXACTLY TWO changed pairs (the `# schema_version` header, `table trades`) plus the additions, per the brief's D51 ruling (4), which governs (corrected by replacement at the G7 stop, 2026-09-24, on CHARC's note; the prior text "zero `-` lines" contradicted the ruling).
- **Grep each enum member SEPARATELY** (`'standard'`, `'hypothesis_test_by_design'`, `'unintended_execution'`, `ENTRY_INTENTS`); a token grep is a LOWER bound — the manifest is established by READ, and the drift test is the only mirror that defends the set.
- **Version-number naming rule:** HEAD-tracking tests are `…_is_head`; the v39→v40 commit edits bodies only, never renames (a rename sweep never rides the version commit).
- **No test name carries an `a2_<digits>` token** (A2-08's roster regex walks all of `tests/`). New 22-B test ids are `b22_NN`.
- **ASCII** in every CLI/stdout string; the contract doc's non-ASCII bytes are read as BYTES (`encoding='utf-8'` or `rb`, one trailing `\r` stripped — autocrlf checkouts).
- Test fixtures derive from REAL row shapes (the live trade-20 / AMN / fill-41 bytes are quoted in ledger R0.C K5 and R0.F-2); a raw `conn.execute` INSERT plants pre-existing "bad" rows (the 18-B.1 technique) and `PRAGMA ignore_check_constraints=ON` plants schema-unreachable `provenance_corrections` rows (R0.C P2-a).
- Full fast suite (`python -m pytest -m "not slow" -q`) GREEN before Reviewer A and again at the end; `ruff check swing/` clean.

## Constants introduced (single-sourced; every other site imports them)

| name | home | value |
|---|---|---|
| `UNINTENDED_EXECUTION` | `swing/data/models.py` | `"unintended_execution"` |
| `ENTRY_INTENTS` | `swing/data/models.py` | widened to the three values (Task 3) — the SCHEMA enum |
| `ENTRY_INTENTS_ASSERTABLE` | `swing/data/models.py` | `frozenset({"standard","hypothesis_test_by_design"})` — what every GENERIC writer accepts (E10) |
| `EntryIntentSeamError(ValueError)` / `AttestedIntentError(ValueError)` | `swing/data/models.py` | the two typed refusals (E11) |
| `SEAM_MESSAGE` | `swing/data/models.py` | `"entry_intent 'unintended_execution' is evidence-bearing: it is written only by 'swing trade assign-intent', which records its evidence"` (ASCII) |
| `attested_message(attestation_id)` | `swing/data/models.py` | `"entry_intent is attested (entry_intent_attestations row {N}); no reversal surface exists -- a reversal is a NEW evidence class with its own record"` (N4's words, ASCII) |
| `CITABLE_FIELDS` / `DESCRIPTIVE_FIELDS` | `swing/trades/entry_intent_assignment.py` | `("notes","why_now","thesis","emotional_state_pre_trade")` / `("notes","why_now")` |
| `DEATH_RUNGS` | same | `("invalidation","criteria_lapsed","horizon","declined")` — the `clear_reason` tokens of `swing/latches/service.py:68-75` `_STATE_BY_CLEAR_REASON` (read by the plan cell; the non-death members are `fill` and `superseded`) |
| `INSTRUMENT_DEPLOYMENT_SESSION` | same | `"2026-08-03"`; docstring states the derivation (merge `d5d03bb9` COMMITTER date 2026-08-03 01:04 HST lower bound; w32 image 2026-08-03 17:30 HST at v33 with `latch_order_intents` present, 0 rows, upper bound — one calendar day, determined, not estimated) |
| `CONTRACT_EXCLUSION_CLAUSE` | `swing/metrics/cohort_intent.py` | `"It counts toward NO hypothesis cohort."` (verbatim sentence of clause (4)) |
| `CONTRACT_EXCLUDED_ENTRY_INTENTS` | same | `(UNINTENDED_EXECUTION,)` |
| `CLAUSE4_SHA256` | `0040` header text + a test constant | `5a78e547f64df25e0f891bd271c8d5e51bbd884866ca6d04c6c85b7803ef3886` |

---

## Task 0 — Preflight (no commit)

1. `git log -1` shows the base ≥ `57187164`; this plan and `docs/training-epoch-intent-contract.md` are present.
2. Re-ground every anchor this plan cites (line numbers drift): the eight seam surfaces (Task 2 table), `update_entry_intent` (`swing/data/repos/trades.py` ~`:970`), `_RESERVED_JOURNAL_FIELDS` (`reconciliation_auto_correct.py` ~`:200`), `find_accepted_latch_order` (`latched_origin.py` ~`:1181`), `mandate_alive_at` (~`:2400`), `broker_order_id_from_envelope` (~`:662`), the four readers (Task 8 table).
3. Capture the 22-A2 composition pin BEFORE any edit: a throwaway script computes `sha256(inspect.getsource(fn))` for every `CASES_22A2` implementing function (106 ids; the A2-08 discovery rule) and prints a dict literal; paste it into Task 2A's pin test (b22_170; Task 12 only re-runs it). Record the base SHA it was captured at in the test's docstring.
4. Run the fast suite on the untouched base; record the pass count. Any pre-existing red is reported, not fixed.

## Task 1 — The contract doc's pins (tests only; the doc is already committed by the plan cell)

**Files:** `tests/docs/test_training_epoch_intent_contract.py` (new).
- `test_clause4_line_occurs_exactly_once_b22_01` — read the doc as BYTES; lines split on `b"\n"`, one trailing `b"\r"` stripped; exactly ONE line starts with `b"> **(4)"`.
- `test_clause4_is_byte_identical_to_the_ratified_text_b22_02` — that line equals the single `> **(4)` line of `git show e5feec2a:docs/phase22-arc-b-rd-rulings-f1-f3.md` (via `subprocess.run([...], capture_output=True)` — BYTES, never `text=True`); skip-with-reason only if git is unavailable.
- `test_clause4_sha256_is_the_pinned_value_b22_03` — the SAME line reader as b22_01 (bytes, one trailing `b"\r"` stripped BEFORE hashing and measuring): `sha256(line[2:])` == `CLAUSE4_SHA256` and `len(line[2:]) == 1024`; a CRLF twin of the doc (written by the test) hashes identically.
- `test_clauses_1_to_3_are_verbatim_from_the_archive_b22_04` — the doc's clauses-(1)-(3) block-quote body equals `docs/research-director-context-archive.md` line 84 sliced from `(1) A+ fires` up to (not including) `**Epoch-integrity`, right-stripped (both read `encoding="utf-8"`).
- `test_ratification_is_quoted_b22_05` — the doc contains `"clause (4) is ratified."`.
Commit: `test(docs): Task 1 — pin the training-epoch intent contract (clause 4 bytes + hash, clauses 1-3 verbatim)`.

## Task 2 — The single-writer seam FIRST (behaviour-preserving for today's two values) — F5 × 8, E10, E11

**Why first:** once `ENTRY_INTENTS` widens (Task 3), every surface validating against it would ACCEPT the value. So every GENERIC writer is repointed to `ENTRY_INTENTS_ASSERTABLE` and given the typed refusal NOW; the widening then opens nothing.

| # | surface (census C.4) | change |
|---|---|---|
| 1 | `swing/cli.py` `trade entry --entry-intent` (~`:602`) | a new `EntryIntentParam(click.ParamType)` in `cli.py` (NOT `click.Choice`, which would reject the value at parse time before any typed message): accepts `ENTRY_INTENTS_ASSERTABLE`; `unintended_execution` → `self.fail(SEAM_MESSAGE)`; anything else → `self.fail` with the generic message |
| 2 | `swing/cli.py` `trade review --entry-intent` (~`:1558`) | the same `EntryIntentParam` |
| 3 | `swing/cli.py` `trade backfill-intent` free-text prompt (~`:1723-1778`) | a typed `unintended_execution` answer → `EntryIntentSeamError` caught → message echoed, row NOT written, loop continues |
| 4 | `swing/web/routes/trades.py` entry form early validation (~`:837`) | validate against `ENTRY_INTENTS_ASSERTABLE`; the value → the existing 4xx re-render with `SEAM_MESSAGE` |
| 5 | `swing/web/routes/trades.py` review form (~`:3545-3558`) | same, 4xx fragment |
| 6 | `swing/trades/entry.py` `EntryRequest.__post_init__` (~`:569`) | the value → `EntryIntentSeamError(SEAM_MESSAGE)`; other non-members keep today's message |
| 7 | `swing/data/repos/trades.py` `update_entry_intent` | ORDER (the full order; Task 7 inserts step (c)). The existing pre-v27 `PRAGMA table_info` branch (`repos/trades.py` ~`:993-1006`) stays FIRST and unchanged (SS-10). Then: (a) read the row's CURRENT `entry_intent` (missing row → `ValueError` as today); (b) `new == current` → return without writing (idempotent, including the value on an attested row); (d) `new == UNINTENDED_EXECUTION` → `EntryIntentSeamError`; (e) `new` not None and ∉ `ENTRY_INTENTS_ASSERTABLE` → today's `ValueError`; (f) write |
| 8 | `swing/trades/reconciliation_auto_correct.py` `_RESERVED_JOURNAL_FIELDS` | add `("trades","entry_intent")` → owning-surface text `"swing trade assign-intent (unintended_execution) / swing trade review --entry-intent (other values)"` (E14: unconditional, as ruled) |

Also: `swing/trades/intent.py` — `ENTRY_INTENT_DISPLAY` stays the two-choice tuple (it feeds BOTH `<select>`s) and gains a sibling `ENTRY_INTENT_LABELS` covering the value set (label for the new value lands in Task 3 with the widening); the no-drift test now asserts `{v for v,_ in ENTRY_INTENT_DISPLAY} == ENTRY_INTENTS_ASSERTABLE`.

**Tests** (`tests/trades/test_22b_seam.py`, new; plus edits to the existing no-drift test):
- `test_each_generic_surface_refuses_with_the_seam_message_b22_10` — parametrized over the 8 surfaces, BY EXECUTION: CLI via `CliRunner` (1-3), TestClient POST (4-5, asserting 4xx + the message in the fragment), `EntryRequest(...)` (6), `update_entry_intent(conn, …)` (7), the corrector's field-level entry with `("trades","entry_intent")` → `ReservedJournalFieldError` (8). Pre-fix the message is the generic "must be one of"/not reserved → RED.
- `test_seam_surfaces_import_the_assertable_constant_b22_11` — static: each of files 1-7 references `ENTRY_INTENTS_ASSERTABLE` (or `EntryIntentSeamError`) and none contains a two-element literal of the intent tokens (closure by READ: the test enumerates the files; a new `ENTRY_INTENTS` membership check anywhere in `swing/` outside `models.py`, `Trade.__post_init__`, the drift test, and the assignment module FAILS — the static closure walk over `ast` `Name`/`Attribute` references to `ENTRY_INTENTS`).
- `test_today_values_still_accepted_on_surfaces_1_to_7_b22_12` — `standard` and `hypothesis_test_by_design` still pass on surfaces 1-7 (behaviour-preserving). `test_the_corrector_reserves_entry_intent_for_every_value_b22_13` — surface 8 refuses ALL three values (`None` included) with `ReservedJournalFieldError` naming the owning surfaces (E14: unconditional, as ruled).
Commit: `feat(trades): Task 2 — single-writer seam: eight generic surfaces refuse unintended_execution (typed, names assign-intent)`.

## Task 2A — Re-scope A2-18 / A2-19 / A2-22 ONCE to `target_version=39`, and the 22-A2 composition pin (R0.I, ruled (a)) — BEFORE 0040 exists

**Why here:** the three are HEAD claims wearing 0039's name; re-scoped to their own migration they never move again, and the pin is red-then-green on the arc's own change. Case ids are RETAINED in `CASES_22A2` (A2-08 walks tokens, not bodies).
1. **The pin first (RED):** `tests/trades/test_22b_composition_pin.py::test_the_22a2_case_functions_are_byte_unchanged_except_three_re_scoped_b22_170` — the Task-0 dict of 106 shas; closure first (the implementing set, found by the A2-08 rule, equals the keys); the 103 byte-unchanged; A2-18, A2-19, A2-22 listed in `RE_SCOPED_22B = {id: (new_sha, "re-scoped 22-B, reason: HEAD-claim in a version-named case; ledger R0.I")}` with the post-re-scope shas. RED until step 2 lands.
2. **Re-scope the bodies (names unchanged in this commit):**
   - **A2-18:** image at 38 → `run_migrations(c, target_version=39)` (helper as at `tests/data/test_22a_task2_migration_0037.py:107`); the diff equals the changed set the body asserts today (`("table","provenance_corrections")` + the two triggers; R0.I says "four changed pairs" — re-ground by EXECUTING `compare()` at 39 and encode what it returns, reporting the count); PLUS a module constant `V39_OBJECT_HASHES` = the D51b-normalized sha256 of those three objects, captured from the committed v39 `schema_manifest_head.tsv` at the base (before Task 3 regenerates it), asserted equal to the migrated-to-39 objects' hashes.
   - **A2-19:** the literal `"# schema_version 39\n"` is RETIRED and the fixture==HEAD clause DROPPED (already the `_head` tests' claim, not duplicated); the body becomes: migrate a 38 image to `target_version=39`; `schema_version == 39`; the three 0039 objects hash to `V39_OBJECT_HASHES`.
   - **A2-22:** asserted over a `target_version=39` run from a 38 image: EXACTLY ONE gate image written (the 22a2 one); plus `backup_gate_for_pre_version(38)` is the 22a2 row. It no longer asserts `POST_BASE_ROSTER`'s length (a HEAD run under 22-B fires two gates and must not be what it counts).
3. **Naming (a SEPARATE commit, the 09-07 rule):** A2-19's name asserts a claim it no longer makes → rename to `test_a2_19_the_0039_migration_yields_v39_and_its_three_objects` (token `a2_19` retained); the pin's key updated in the same commit.
Commits: `test(22a2): Task 2A — re-scope A2-18/A2-19/A2-22 to target_version=39; composition pin over 106 cases` then `test(22a2): Task 2A — rename A2-19 to the claim it now makes`.

## Task 3 — Migration `0040` part 1: the `trades` rebuild, the N4 trigger, the gate, the widening (#11 in ONE commit)

**Files:** `swing/data/migrations/0040_entry_intent_unintended_execution.sql` (new), `swing/data/db.py`, `swing/data/models.py`, `swing/trades/intent.py`, `tests/data/schema_manifest_head.tsv` (regenerated), `tests/data/test_migration_0040_entry_intent.py` (new), `tests/data/test_entry_intent_drift.py` (new), `tests/data/test_backup_gate_table.py` (`POST_BASE_ROSTER` constant only), `tests/data/test_migration_0032.py` (ONE existing body, step 6's composition note, SS-12), the version-mirror test bodies (39→40).

**0040 header (comment block, before `BEGIN`):** REVERSIBILITY HEADER naming: (i) the ONE edit (the `entry_intent` IN-list: `('standard','hypothesis_test_by_design')` → `('standard','hypothesis_test_by_design','unintended_execution')`); (ii) every object CREATED (the N4 trigger, the attestation table + its triggers, the two unattested `trades` twins `trg_trades_entry_intent_unattested_update` / `_insert` (CHARC-S3 condition 2; Task 4 appends them and this header item), the four `latch_view_events` belts) with its `DROP` statement; (iii) the five dependants re-created verbatim with their SOURCE (`0014:230` partial UNIQUE `ux_trades_one_open_per_ticker`; `0021:39,41` the two indexes; `0038:113,117` `ux_trades_attempt_id` + `trg_trades_attempt_id_immutable`); (iv) the gate `BackupGateSpec(39, "22b", …)`; (v) **the F1 pin:** `-- clause (4) sha256: 5a78e547f64df25e0f891bd271c8d5e51bbd884866ca6d04c6c85b7803ef3886 (docs/training-epoch-intent-contract.md; derived by test, never read at runtime)`; (vi) "`trades` has NO AUTOINCREMENT (`id INTEGER PRIMARY KEY`) — no sequence carry" (NOT 0039's sequence step); (vii) **the R0.J census, the precondition the RENAME bracket was proven against (condition (iv)):** method `SELECT … FROM sqlite_master WHERE sql LIKE '%trades%' AND tbl_name <> 'trades' AND type IN ('trigger','view')` (a textual superset) → 1 row, `trg_provenance_corrections_citation_graph`; 0 views; 0 objects name `trades_new`, so legacy mode has no rewrite to skip. A later foreign object referencing `trades` is covered by the same bracket; the header records which objects it was proven against. **Beside it (CHARC-S3.2), the census for any FUTURE rebuild of `entry_intent_attestations`:** the same method with `'%entry_intent_attestations%'` and `tbl_name <> 'entry_intent_attestations'` → TWO rows at v40, the two unattested twins (they name the table in their bodies); and the v40 `trades` census for a future `trades` rebuild, measured at v40 and stated with its members (the v39 row plus 0040's own attestation triggers whose bodies name `trades`).

**0040 body, in order (one transaction, the runner's):**
1. `CREATE TABLE trades_new (...)` = the STORED v39 `trades` DDL text with the ONE edit and the table name — the stored DDL begins `CREATE TABLE "trades" (` (quoted; the 0014 RENAME product, R0.C K8).
2. `INSERT INTO trades_new (<58 columns, explicit, in PRAGMA order>) SELECT <same 58> FROM trades;`
3. `DROP TABLE trades;` (drops the five dependants) → the RENAME inside a bracket EXACTLY two statements wide (R0.J-RULING, branch (a), condition (i)); NOTHING else goes inside the window:
   ```
   -- R0.J: modern RENAME re-parses every trigger; 0039's cross-table trg_provenance_corrections_citation_graph names trades.
   PRAGMA legacy_alter_table=ON;
   ALTER TABLE trades_new RENAME TO trades;
   PRAGMA legacy_alter_table=OFF;
   ```
   Without the bracket the RENAME FAILS by execution: `error in trigger trg_provenance_corrections_citation_graph: no such table: main.trades`. Unlike `foreign_keys`, this pragma is NOT a no-op inside a transaction, so the in-script bracket is valid. **Runner (condition (ii)):** `_apply_migration` (`swing/data/db.py` ~`:457`) reads `prior_legacy = PRAGMA legacy_alter_table` BEFORE `executescript` and RESTORES that prior value in its existing `finally`, beside `foreign_keys`. It restores the prior value, never a hard OFF, because the pragma is connection-scoped and survives a rollback.
4. Re-create the five dependants VERBATIM from their sources (token-identical — D51 hashes under whitespace collapse + line-comment drop, R0.C K11).
5. `CREATE TRIGGER trg_trades_entry_intent_attested_terminal BEFORE UPDATE OF entry_intent ON trades FOR EACH ROW WHEN COALESCE(OLD.entry_intent,'') = 'unintended_execution' AND NEW.entry_intent IS NOT OLD.entry_intent BEGIN SELECT RAISE(ABORT, '<attested message, generic form without row id>'); END;` (both WHEN operands are non-NULL by construction — no NULL-`WHEN` hazard; a test pins it).
6. The FOUR `latch_view_events` evidence belts. The first three are R0.G/R0.H, ruled (a'): `trg_lve_actionable_ever_viewed_monotonic` (`BEFORE UPDATE OF actionable_ever_viewed ON latch_view_events FOR EACH ROW WHEN NOT COALESCE(NEW.actionable_ever_viewed >= OLD.actionable_ever_viewed, 0)` → ABORT — the SQL twin of the writer's `MAX` at `repos/latch_view_events.py:167`), `trg_lve_no_delete` (`BEFORE DELETE` → ABORT), `trg_lve_no_replace` (`BEFORE INSERT … WHEN EXISTS (SELECT 1 FROM latch_view_events WHERE candidate_id = NEW.candidate_id AND view_session_date = NEW.view_session_date AND surface = NEW.surface)` → ABORT; the 0037 `trg_loml_no_replace` shape, keyed on the 0033 UNIQUE). The identity triggers stay. The writer is UNCHANGED: its INSERT (`:179-190`) is plain (no `ON CONFLICT`), and its lost-race `except sqlite3.IntegrityError` (`:191`) catches the ABORT (SQLITE_CONSTRAINT) and takes the UPDATE path — proven by execution below.
   **The fourth belt (R0.K-RULING, RD branch (a); brief §2 item (4) @ `0f63d6a6`):** `CREATE TRIGGER trg_lve_view_window_immutable BEFORE UPDATE OF first_viewed_ts, ticker ON latch_view_events FOR EACH ROW BEGIN SELECT RAISE(ABORT, 'latch_view_events first_viewed_ts and ticker are immutable: leg-1 evidence window (22-B R0.K)'); END;`. It is an UNCONDITIONAL barrier with no `WHEN`. `UPDATE OF` fires whenever a listed column is in the SET list, even at an unchanged value (measured, SQLite 3.50.4). The sole production UPDATE (`repos/latch_view_events.py:157-169`, read) SETs exactly `last_viewed_ts`, `latch_state_at_last_view`, `actionable_at_last_view`, `view_count` and `actionable_ever_viewed`. It lists neither column, so no production path aborts; b22_199 pins that SET list by execution. The identity triggers stay (`trg_lve_identity_coherent_insert`/`_update`; the live text is 0033's re-creation, `0033:182`, `:210`).
   **Leg 1's read set beside the belt set (RD's class rule, R0.K-RULING.1: every column a leg READS is a column the schema GUARDS).** The read set is DERIVED, not listed from memory. b22_200 derives it from the predicate as compiled: SQLite's authorizer (`conn.set_authorizer`, `SQLITE_READ`, table `latch_view_events`) records it while the Task-5 leg-1 service reads run and while the tier-2 INSERT compiles `trg_eia_tier2`. Measured: the authorizer reports trigger-body reads with the trigger's name as the source, and it reports the rowid alias by its declared name. Derived from the Task 4/5 predicate as written, the reads are:

   | column (authorizer-derived) | why leg 1 reads it | guard (trigger, migration) | guard kind |
   |---|---|---|---|
   | `actionable_ever_viewed` | the verdict: any PRE row at 1 REFUSES; an all-0 PRE set ADMITS | `trg_lve_actionable_ever_viewed_monotonic` (0040) | monotonic: the admit-manufacturing direction (1 → 0) ABORTS; 0 → 1 (the writer's `MAX`) can only turn an admit into a refusal |
   | `first_viewed_ts` | the window: SPEAK `>= 2026-08-03`, PRE `<= placement_session` | `trg_lve_view_window_immutable` (0040) | immutable |
   | `ticker` | the scope: the trade's ticker's rows (R0.F-2 fire scoping) | `trg_lve_view_window_immutable` (0040); coherence with `candidate_id` is also enforced by `trg_lve_identity_coherent_update` (0033) | immutable |
   | the row set | which rows exist | `trg_lve_no_delete` + `trg_lve_no_replace` (0040) | no vanish; REPLACE-proof |

   **RECORDED, NOT DECIDED.** The authorizer set also holds `view_event_id` (the evidence id in `leg_evidence_json`) and `view_session_date` (recorded, and bound to live by `trg_eia_tier2` at insert). No leg-1 branch reads either one. R0.H says "never `view_session_date`", and R0.K branch (a), as ruled, leaves it out on purpose. They sit on a named `LEG1_RECORDED_NOT_DECIDED` list with that reason. b22_201 proves by EXECUTION that neither can move a verdict, and drift (iv) names a post-assignment change to either. **Outside the read set:** `candidate_id`, `evaluation_run_id`, `detection_date`, `pipeline_run_id`, `surface`. No leg-1 statement reads them, because the leg windows by `ticker`. RD's mail names "the identity block"; derived from the predicate, the only member the leg reads is `ticker`. They stay coherence-guarded by `trg_lve_identity_coherent_update`, and a cross-ticker re-point must move `ticker`, which the fourth belt ABORTS (b22_198). If a later predicate reads any column not in the table above, the authorizer set gains it and b22_200 goes RED until a belt guards it. That is the class closure RD asked for, so 22-C does not meet this fork.
   **Composition with an existing test (SS-12, measured):** SQLite fires the NEWEST `BEFORE` trigger first. `tests/data/test_migration_0032.py::test_the_identity_trigger_also_guards_updates` (`:295`, on `ensure_schema`, i.e. HEAD) runs `UPDATE latch_view_events SET ticker = 'FTRE'` and matches `"identity block"`. At v40 the fourth belt fires first, and the match goes RED. Fix, in this commit and body only: the mutation becomes `SET detection_date = '2026-06-24'`. That is an identity column outside the belt, so the test keeps proving the identity trigger ALONE at HEAD, independent of firing order. The `ticker` case moves to b22_198. By grep over `tests/`, `swing/` and `scripts/` for UPDATE/DELETE/REPLACE of `latch_view_events` (with no dynamic builders in any file naming the table), this is the only existing test the four belts reach; the grep is a LOWER bound, and the full suite is the proof.
7. (Task 4 appends the attestation table here.)
8. `UPDATE schema_version SET version = 40;` per the runner's convention (re-ground against 0039's tail).

**Python (same commit):** `ENTRY_INTENTS` gains the value; `Trade.__post_init__` accepts it (widened — the READ path must hydrate an attested row); `ENTRY_INTENT_LABELS[UNINTENDED_EXECUTION] = "Unintended execution"`; `entry_intent_label` reads `ENTRY_INTENT_LABELS`; `EXPECTED_SCHEMA_VERSION = 40`; `PHASE22_ARC_B_PRE_MIGRATION_EXPECTED_TABLES` (the v39 table set, measured by `sqlite_master` on a fresh v39 DB, not typed) + `_phase22_arc_b_backup_gate` + the `BackupGateSpec(39, "22b", PHASE22_ARC_B_PRE_MIGRATION_EXPECTED_TABLES, "_phase22_arc_b_backup_gate", "pre-22-B")` row (copy the 22a2 row's shape; gate clause `pre_version == 39 AND target >= 40`).

**The three re-scoped 22-A2 cases are NOT touched here** (Task 2A re-scoped them to `target_version=39` before this task). This commit adds `(39, "22b", "_phase22_arc_b_backup_gate", "PHASE22_ARC_B_PRE_MIGRATION_EXPECTED_TABLES", "pre-22-B")` to the `POST_BASE_ROSTER` constant (not a 22-A2 case body) so the non-case table tests stay green.

**Version-mirror sweep:** grep `EXPECTED_SCHEMA_VERSION\s*==\s*39|== 39\b` over `tests/` (44 lines / 32 files at `902ac83d` — a LOWER bound); READ each hit; edit only version assertions, bodies only.

**Tests** (`tests/data/test_migration_0040_entry_intent.py`):
**Every migration case below pins `target_version=40` (the R0.I forward rule); none migrates to HEAD.**
- `test_one_edit_of_the_v39_stored_ddl_equals_the_v40_stored_ddl_b22_20` — build v39 (`run_migrations(c, target_version=39)`), read stored `trades` SQL, apply the ONE textual edit (the anchor occurs EXACTLY once — asserted), migrate to 40, stored SQL equals the edited text BYTEWISE.
- `test_58_columns_in_order_and_every_row_tuple_identical_b22_21` — a v39 DB seeded with ≥3 real-shape trades (incl. trade-20's bytes: the two-space `notes`) + fills + a `daily_management_records`/`trade_events`/`reconciliation_discrepancies`/`provenance_corrections` child each; after migrate: `PRAGMA table_info` names/types/order identical; `SELECT *` tuples identical; the five child tables' row counts unchanged; `PRAGMA foreign_key_check` == `[]`.
- `test_the_partial_unique_still_refuses_a_second_open_trade_b22_22` (D30 discriminator) and `test_attempt_id_is_still_write_once_b22_23`.
- `test_the_five_dependants_are_verbatim_from_their_sources_b22_24` — stored SQL of each == `create_statement_in(<source migration>, name)` (the 0039 test helper shape).
- `test_every_foreign_object_referencing_trades_is_unchanged_and_still_fires_b22_44` (R0.J-RULING condition (iii)), parametrized over BOTH shapes (R2-01: `ensure_schema` goes straight to HEAD, so it cannot supply a v39 'before'). FRESH shape: an empty DB → `run_migrations(c, target_version=39)` → snapshot → `run_migrations(c, target_version=40)`, and additionally a separate `ensure_schema(fresh)` DB whose foreign-object set equals the v39 snapshot's. SEEDED shape: a v39 image carrying the real-shape rows of b22_21 → `target_version=40` (Task 13 step 3 runs the same helper on the live copy). Two parts:
  - (1) Every v39 member of the set of `sqlite_master` triggers/views on OTHER tables whose SQL contains `trades` is present at v40 with byte-identical stored SQL, and the v40 set minus the v39 set is EXACTLY the named 0040-created attestation triggers whose bodies name `trades` (measured at Task 4 and pasted as a literal). **Executing-cell correction (R0.J-RULING condition (iii)'s "EQUAL at v39 and v40"):** 0040 itself creates `trg_eia_*` triggers ON `entry_intent_attestations` that read `trades`, so literal equality is false by construction of objects CHARC also ruled; the condition's purpose, that the RENAME rewrote no pre-existing foreign object, is what this part asserts. In Task 3's commit (before the attestation section exists) the difference is empty.
  - (2) The citation-graph trigger FIRES against the REBUILT `trades`: a 22-A2 fully-cited fixture row (`tests/trades/_cohort_provenance_fixtures.py`) INSERTS, and its one-mutation twin ABORTS with the trigger's OWN message. A `no such table` error is a FAIL, never an abort.
  - (3) (RULING G1b) `trg_eia_trade_binding` names a THIRD table, `fill_envelope_identity`: by the same method over that table, every v39 member is byte-identical at v40 and the v40 set minus the v39 set is EXACTLY `{trg_eia_trade_binding}`; 0040's header census note names it (a future rebuild of `fill_envelope_identity` meets R0.J).
- `test_header_states_the_r0j_census_b22_46` (R2-07): 0040's header carries the census method text (the `sqlite_master … LIKE '%trades%' AND tbl_name <> 'trades' AND type IN ('trigger','view')` query), `1 row`, the trigger's name, `0 views` and `0 objects name trades_new`. Those facts also hold by EXECUTION on a `target_version=39` DB, so a stale header FAILS.
- `test_the_legacy_bracket_is_exactly_three_statements_b22_47` (R2-08): static parse of 0040 with `--` comment lines dropped. `legacy_alter_table` occurs exactly twice, and the statements between and including them are exactly `PRAGMA legacy_alter_table=ON`, `ALTER TABLE trades_new RENAME TO trades`, `PRAGMA legacy_alter_table=OFF`.
- `test_runner_restores_the_prior_legacy_alter_table_value_b22_45` (condition (ii)). A throwaway migration script sets `legacy_alter_table=ON` and then fails; through the real `_apply_migration`, the connection afterwards reads its PRIOR value. Run it with prior 0 and prior 1: a hard-OFF implementation FAILS the prior-1 case, and a no-restore implementation FAILS the prior-0 case. On success, 0040 leaves the pragma reading the prior value too.
- `test_the_manifest_diff_is_one_changed_table_plus_additions_zero_deletions_b22_25` — `schema_manifest.compare(v39, v40)` (both images built with `run_migrations(c, target_version=…)`, never HEAD): `changed == {("table","trades")}` (the header line is not an object); `missing == ∅`; `unexpected ==` exactly {the N4 trigger, the four `latch_view_events` belts} in THIS commit (R3-02: the attestation objects do not exist until Task 4). Task 4's commit extends the expected set with the attestation table and its indexes and triggers AND the two unattested `trades` twins, as one edit to one named constant. That is the D51 expectation as CHARC restated it last (CHARC-S3.2, superseding R0.K-RULING.4 by addition): changed = {the `# schema_version` header line, `("table","trades")`}; additions = the attestation table's objects, `trg_trades_entry_intent_attested_terminal`, the two unattested twins, the four lve belts; deletions = none.
- `test_head_fixture_is_regenerated_b22_26` — rides the existing `test_head_manifest_matches_fixture` (no new assertion; listed so the roster names it).
- `test_run_twice_is_a_noop_b22_27`; `test_the_22b_gate_fires_once_from_39_and_the_cli_echoes_one_image_b22_28` (the A2-21 shape: ONE `swing-pre-22b-migration-*` image in `backups_dir`, counted by STEM so a later gate never moves it, none in the DB's parent — the D32 production shape).
- `test_header_carries_the_clause4_hash_and_it_matches_the_doc_b22_29` — parse `-- clause (4) sha256: <hex>` from 0040's header; equals `CLAUSE4_SHA256`; equals the hash derived INDEPENDENTLY from the doc (Task 1's reader, called fresh).
- `test_migration_never_reads_the_docs_tree_b22_30` — 0040's text contains no `docs/` path outside `--` comments (the runtime never opens the doc; the runner executes SQL only).
- `tests/data/test_entry_intent_drift.py::test_entry_intent_check_equals_python_enum_b22_31` — **the drift test (the mandatory mirror, K12: absent today; a mirror test, so HEAD-tracking and version-free in its name):** parse the IN-list out of the STORED `trades` DDL of `ensure_schema(tmp)`; `set(...) == ENTRY_INTENTS`; and `ENTRY_INTENTS_ASSERTABLE < ENTRY_INTENTS` with the difference exactly `{UNINTENDED_EXECUTION}`.
- `test_the_model_rejects_a_fourth_value_b22_32`; `test_each_member_greps_separately_b22_33` — for each of the three tokens, the set of `swing/` files containing it (READ-classified list committed in the test as widened / tight-by-design / value-agnostic with one-line reasons) equals the grep result — a new site FAILS until classified.
- N4 layer 3 ALONE on plain `sqlite3` (no service): `test_raw_update_of_an_attested_row_aborts_b22_34` (`'unintended_execution'` → `'standard'` and → NULL: both ABORT); `test_same_value_update_passes_b22_35`; `test_null_to_value_passes_b22_36`; `test_the_trigger_when_clause_is_never_null_b22_37` (OLD NULL / NEW NULL combos evaluated; PLUS the static half, R3-10 and SS-11: every `CREATE TRIGGER` in 0040 has a `WHEN NOT COALESCE(`, OR is on `TOTAL_WHEN`, OR has NO `WHEN` clause (an unconditional barrier: `trg_lve_no_delete`, `trg_eia_no_delete`, `trg_lve_view_window_immutable`), and each `TOTAL_WHEN` member's `WHEN` evaluates non-NULL over its NULL operand combinations).
- The four belts (R0.H's and R0.K-RULING's discriminators, AMN's row 5 shape planted RAW, belts tested ALONE on plain `sqlite3`): `test_lve_raw_downgrade_of_row5_aborts_b22_38` (a 1 set to 0); `test_lve_writer_zero_to_one_passes_b22_39` (`record_view` actionable=1 over a 0 row); `test_lve_same_value_update_passes_b22_40`; `test_lve_raw_delete_aborts_b22_41`; `test_lve_insert_or_replace_on_row5_key_aborts_b22_42` (`INSERT OR REPLACE` with `actionable_ever_viewed 0`); `test_lve_plain_insert_on_existing_key_takes_the_update_path_b22_43` (the writer's lost-race branch driven BY EXECUTION: pre-plant the key, call `record_view` with `get_view` monkeypatched to return None once → the INSERT hits the belt → the handler re-reads and merges → `view_count` += 1, `actionable_ever_viewed` = max). The fourth belt (R0.K-RULING). Each case also runs on a control DB with `trg_lve_view_window_immutable` DROPPED, so the belt is shown to be the refusing object (pre-fix RED); every assertion matches the BELT's message, never a bare `IntegrityError`: `test_lve_raw_first_viewed_ts_update_of_row5_aborts_b22_197`, parametrized. (A) RD's discriminator verbatim, `UPDATE latch_view_events SET first_viewed_ts='2026-08-03T00:00:00' WHERE view_event_id=5` → ABORTS with the belt's message. **On the real row-5 shape that statement ALSO violates the table CHECK `last_viewed_ts >= first_viewed_ts`** (`0033`, the 0032 CHECK preserved; row 5's `last_viewed_ts` is `2026-08-02T23:43:45`). So on the control DB it aborts with the CHECK's message, and only the MESSAGE discriminates. A `BEFORE` trigger runs before CHECKs (measured, ledger R2-05), so the belt's message wins when the belt is present (SS-13). (B) The CHECK-clean manufacturing move that R0.K describes, `SET first_viewed_ts='2026-08-03T00:00:00', last_viewed_ts='2026-08-03T00:00:00'`, puts row 5 into the SPEAK window as a 0 row. It SUCCEEDS on the control DB and ABORTS with the belt's message. (C) A same-value `SET first_viewed_ts = first_viewed_ts` ABORTS, because `UPDATE OF` fires on the SET list, and it SUCCEEDS on the control DB; `test_lve_coherent_ticker_repoint_aborts_b22_198` (a second A+ fire of ANOTHER ticker is planted raw with its `evaluation_runs` row; row 5's whole identity block is re-pointed to it coherently (`candidate_id`, `evaluation_run_id`, `ticker`, `detection_date`, `pipeline_run_id`) → ABORTS with the belt's message; on the control DB the identity trigger PASSES it, which proves that coherence alone does not protect the scope); `test_lve_writer_last_view_update_passes_under_all_four_belts_b22_199` (`record_view` on row 5's key with a later `viewed_ts` and actionable 0 → the UPDATE path runs: `last_viewed_ts` advances, `view_count` += 1, and `first_viewed_ts`/`ticker` are byte-unchanged. An authorizer collecting `SQLITE_UPDATE` columns during the call sees exactly {`last_viewed_ts`, `latch_state_at_last_view`, `actionable_at_last_view`, `view_count`, `actionable_ever_viewed`}, disjoint from {`first_viewed_ts`, `ticker`}. The writer's SET list is pinned by execution, not by a read of the source).
Commit (ONE): `feat(data): Task 3 — migration 0040 part 1: trades rebuild (one edit), N4 terminal trigger, 22b gate, v40, enum widened with the drift test`.

## Task 4 — Migration `0040` part 2: `entry_intent_attestations` (its SCHEMA IS THE EVIDENCE RULE)

**Files:** `0040` (append before the version line: the table, its triggers, THEN the two unattested `trades` twins; header item (ii) and the header's `entry_intent_attestations` census extended), `swing/data/models.py` (`EntryIntentAttestation` frozen dataclass + `__post_init__` mirroring EVERY CHECK), **`swing/data/repos/entry_intent_attestations.py` (new, CHARC-S3 condition 1): `insert_attestation(conn, att) -> int` (a plain `INSERT`, no `ON CONFLICT`, no transaction management — the caller's transaction), `get_attestation(conn, trade_id) -> EntryIntentAttestation | None`, and the `_row_to_attestation` mapper; the ONLY `INSERT INTO entry_intent_attestations` site under `swing/` besides 0040**, manifest regenerated, `tests/data/test_migration_0040_attestations.py` (new), `tests/data/test_repo_entry_intent_attestations.py` (new: insert/get round-trip through the real triggers, `get_attestation` None for an unattested trade), b22_25's expected-additions constant (extended, R3-02), b22_37 (its static half now also sees the two twins on `TOTAL_WHEN`).

**Table (columns; CHECKs NULL-closed):**
- `attestation_id INTEGER PRIMARY KEY AUTOINCREMENT CHECK (attestation_id > 0)` (an OMITTED integer PK presents as -1 inside a BEFORE INSERT trigger; the CHECK makes -1 unstorable so `trg_eia_no_replace` can ignore `NEW.attestation_id = -1` — the 0037 `fill_envelope_identity` convention, `0037:518-532`)
- `trade_id INTEGER NOT NULL UNIQUE REFERENCES trades(id) ON DELETE RESTRICT`
- `assigned_value TEXT NOT NULL CHECK (assigned_value = 'unintended_execution')`
- `admission_tier TEXT NOT NULL CHECK (admission_tier IN ('structural','contemporaneous_record'))`
- `trade_entry_date TEXT NOT NULL CHECK (COALESCE(length(trade_entry_date) = 10 AND date(trade_entry_date) IS NOT NULL AND date(trade_entry_date) = trade_entry_date AND CAST(substr(trade_entry_date,1,4) AS INTEGER) BETWEEN 1 AND 9999, 0))`.
- **Date CHECKs (a NOTE on the column CHECKs, not a column; each column below is declared ONCE, R3-01).** Every date CHECK in this table copies the `0033:100-126` precedent VERBATIM in shape and is COALESCE-closed (`date('2026-00-01')` is NULL, and a NULL CHECK PASSES). The same date idiom is the CHECK of `placement_session` and `cited_latch_terminal_session` (`x IS NULL OR COALESCE(…, 0)`). The CHECK of `outcome_known_at` (declared in the evidence bullet) is `x IS NULL OR COALESCE(…, 0)` over 0033's ISO-seconds predicate: length 19, the digit GLOB, `datetime(x) IS NOT NULL`, the date-prefix round-trip, year 1-9999, and hh ≤ 23 / mm ≤ 59 / ss ≤ 59 (R2-04). The Python `__post_init__` mirror parses with `date.fromisoformat` / `datetime.strptime(x, '%Y-%m-%dT%H:%M:%S')`, which rejects the same set, year 0000 included.
- `entry_fill_id INTEGER REFERENCES fills(fill_id) ON DELETE SET NULL` + `entry_fill_id_at_assignment INTEGER NOT NULL` + `CHECK (entry_fill_id IS NULL OR entry_fill_id = entry_fill_id_at_assignment)`. This is the `0039:84-115` shape, copied (R3-05). RESTRICT would let evidence bookkeeping BLOCK a money-bearing operation: `split_into_partials` DELETEs the consolidated fill, entry fills included (`reconciliation_auto_correct.py` ~`:3216`), and brief §0 item 5 forbids the block. The pointer may go NULL. The frozen NUMBER is what the binding trigger and the drift reader compare against. Deletion is detected from `entry_fill_id IS NULL` and never from the number matching, because `fills.fill_id` is a bare rowid that a split of the max fill REUSES (0039's R11 note).
- `entry_broker_order_id TEXT` (NULL iff the entry fill's envelope carries none — E9)
- **tier 2:** `placement_session TEXT`, `placement_session_source TEXT CHECK (placement_session_source IN ('schwab_envelope','entry_date_fallback'))`, `admitted_leg TEXT CHECK (admitted_leg IN ('telemetry','deployment'))`, `leg_evidence_json TEXT` (leg 1: `{"telemetry_rows":[{"view_event_id":N,"actionable_ever_viewed":0,"first_viewed_ts":"…","view_session_date":"…"}…]}`; leg 2: `{"deployment_session":"2026-08-03","placement_session":"…"}` — the replay input, N1)
- **structural (N5 (b): LINK ONLY; no `cited_latch_intent_id` column):** `cited_latch_link_id INTEGER REFERENCES latch_order_mandate_links(link_id) ON DELETE RESTRICT`, `cited_latch_terminal_rung TEXT CHECK (cited_latch_terminal_rung IN ('invalidation','criteria_lapsed','horizon','declined'))`, `cited_latch_terminal_session TEXT`, `cited_latch_probe_json TEXT` (the service-built verdict object of Task 5 step 5; the `provenance_corrections.cited_latch_probe_json` precedent)
- **evidence:** `cited_fields_json TEXT NOT NULL`, `cited_text_snapshot_json TEXT NOT NULL`, `audit_trail_checked_at TEXT NOT NULL`, `corrections_touching_cited_fields INTEGER NOT NULL CHECK (corrections_touching_cited_fields = 0)`, `outcome_known_at TEXT` (NULL iff the trade had no non-entry fill at assignment), `reason TEXT NOT NULL CHECK (length(trim(reason)) > 0)`, `applied_at TEXT NOT NULL`, `applied_by TEXT NOT NULL`
- **paired-tier CHECK:** `(admission_tier = 'structural' AND cited_latch_link_id IS NOT NULL AND cited_latch_terminal_rung IS NOT NULL AND cited_latch_terminal_session IS NOT NULL AND cited_latch_probe_json IS NOT NULL AND placement_session IS NULL AND placement_session_source IS NULL AND admitted_leg IS NULL AND leg_evidence_json IS NULL) OR (admission_tier = 'contemporaneous_record' AND cited_latch_link_id IS NULL AND cited_latch_terminal_rung IS NULL AND cited_latch_terminal_session IS NULL AND cited_latch_probe_json IS NULL AND placement_session IS NOT NULL AND placement_session_source IS NOT NULL AND admitted_leg IS NOT NULL AND leg_evidence_json IS NOT NULL)` — total (every operand non-NULL by construction).
- **binds (each NULL-closed by the paired CHECK or `COALESCE(…,0)`):** `COALESCE(cited_latch_terminal_session < trade_entry_date, 1)` (death STRICTLY before the fill session — NULL only on tier 2, where the paired CHECK forces NULL); `COALESCE(admitted_leg <> 'deployment' OR placement_session < '2026-08-03', 1)`; `outcome_known_at IS NULL OR trade_entry_date < substr(outcome_known_at,1,10)`; `json_valid(cited_fields_json) AND json_type(cited_fields_json) = 'array' AND json_array_length(cited_fields_json) >= 1`; `json_valid(cited_text_snapshot_json) AND json_type(cited_text_snapshot_json) = 'object'`; `json_valid` on the three optional JSON columns when non-NULL. (Measured: `json_valid(x) AND json_type(x) = ...` short-circuits in a CHECK, so malformed JSON is a CHECK failure, `IntegrityError`.)

**Triggers (BEFORE INSERT unless named; every `WHEN NOT COALESCE(…,0)`; each is the SQL twin of a Task-5 service check — Task 11 closure-checks it):**
- `trg_eia_trade_binding` — the trade exists; `NEW.trade_entry_date = trades.entry_date`; `trades.entry_intent IS NULL` at insert (no relabel; the service inserts BEFORE it updates `trades`); `NEW.entry_fill_id` (equal to `NEW.entry_fill_id_at_assignment`, NOT NULL at insert) is the trade's AUTHORITATIVE entry fill (the first `action = 'entry'` fill by `fill_datetime ASC, fill_id ASC` — the `get_authoritative_entry_fill` order, `repos/fills.py:297-308`); `NEW.entry_broker_order_id IS (SELECT fei.broker_order_id FROM fill_envelope_identity fei JOIN fills f ON f.fill_id = fei.fill_id AND fei.envelope_raw = f.schwab_source_value_json WHERE f.fill_id = NEW.entry_fill_id_at_assignment AND fei.envelope_state = 'canonical')`. **RULING G1b (CHARC, brief @ `93cfe8bd`): ZERO SQL reads of a fill envelope anywhere in 22-B, NO exemption list.** The twin binds the AUTHORITY's STORED reading of the fill's CURRENT document (22-A PERSIST-CANONICAL; `fei.envelope_raw = f.schwab_source_value_json` is the sanctioned TEXT equality), never a parse of the envelope; the 22-A whole-tree sweep `test_no_sql_anywhere_in_swing_reads_a_fill_envelope` GREEN with zero offenders is the acceptance, and nothing is added to it. `IS` is total; a raw insert naming an order id that differs from the stored canonical reading, or naming one over a REFUSED reading, ABORTS (b22_210, b22_211). **The NULL-id close (RULING G1d item 3, CHARC, brief @ `640a8f28`; "NULL on both sides" admitted a raw NULL id over a REFUSED reading, the fabrication class):** a further conjunct, `NEW.entry_broker_order_id IS NOT NULL OR EXISTS (fill with f.schwab_source_value_json IS NULL) OR EXISTS (a 'canonical' fei reading of the fill's CURRENT document, the same join)`, inside the same `WHEN NOT COALESCE(…, 0)` (every term is `IS NOT NULL` or `EXISTS`, so none can go NULL). The envelope term is ensure's own predicate for what gets a reading, and it is a presence test on the column, which the 22-A sweep permits (it forbids `json_*` only). So a NULL id over a refused reading ABORTS (b22_213), a NULL id over a non-NULL envelope with NO reading of its current document ABORTS (b22_216: the reading row raw-DELETED with the fei no-delete barrier dropped in the test DB, and the document rewritten raw after its reading), a NULL id over a NULL envelope PASSES (b22_214), and a NULL id over a canonical no-key reading PASSES (b22_215). Each ABORT case PASSES on a CONTROL trigger, the stored body with the close cut out, proven to be the RULING G1b body by its manifest hash. A blank envelope gets a canonical no-key reading (CHARC-measured), so b22_193 stands unchanged. The R3-06 `CASE WHEN json_valid(<envelope>)` guard LEAVES 0040 entirely (its hazard class exits the schema). The trigger therefore names a THIRD table, `fill_envelope_identity`; 0040's header records it in the census any future rebuild of that table meets (R0.J method, measured on a `target_version=40` image: 2 rows, `trg_provenance_corrections_citation_graph` and `trg_eia_trade_binding`; b22_44 part (3)).
- `trg_eia_cited_fields` — `NOT EXISTS (SELECT 1 FROM json_each(NEW.cited_fields_json) WHERE type <> 'text' OR value NOT IN ('notes','why_now','thesis','emotional_state_pre_trade'))` (the `type` test catches JSON `null`/numbers, which `NOT IN` alone would miss); no duplicates (`COUNT(DISTINCT value) = COUNT(*)`); at least one of `notes`/`why_now` (F2 S4); the snapshot's key set == the cited set and each snapshot value `IS` the live `trades` column (TEXT vs TEXT, bytewise). The whole predicate inside `WHEN NOT COALESCE(…, 0)`. **Malformed JSON (R2-05, measured):** BEFORE triggers run before CHECKs, and a `json_each(NEW.x)` subquery raises `OperationalError: malformed JSON` EVEN behind a `json_valid(NEW.x) AND …` conjunct. So every trigger's JSON source is `json_each(CASE WHEN json_valid(NEW.x) THEN NEW.x ELSE '[]' END)` (`'{}'` for objects), and its predicate leads with `json_valid(NEW.x)`. A malformed blob is then the trigger's own ABORT, never an `OperationalError`. This applies to every trigger that reads `cited_fields_json`, `cited_text_snapshot_json`, `leg_evidence_json` or `cited_latch_probe_json`.
- `trg_eia_audit_trail` (P2 twin, F2 S2 + P2-b) — `NOT EXISTS` in `reconciliation_corrections` with `affected_table = 'trades' AND affected_row_id = NEW.trade_id` AND (`field_name` ∈ cited OR any key of `json_each(pre_correction_value_json)` / `json_each(applied_value_json)` ∈ cited ∪ `'trades.'||cited`); AND `NOT EXISTS` in `provenance_corrections` with `trade_id = NEW.trade_id` AND any element of `json_each(corrected_fields_json)` ∈ `'trades.'||cited` (P2-a: schema-vacuous today, required by S2).
- `trg_eia_outcome` (P3 twin) — `NEW.outcome_known_at IS (SELECT MIN(fill_datetime) FROM fills WHERE trade_id = NEW.trade_id AND action IN ('trim','exit','stop'))` (E2: the EARLIEST non-entry fill; NULL iff none).
- `trg_eia_tier2` (P1 twin; fires only when `NEW.admission_tier = 'contemporaneous_record'`) — `NOT EXISTS` a link with `broker_order_id = NEW.entry_broker_order_id`; `NOT EXISTS` a `latch_order_intents` row with `actual_broker_order_id = NEW.entry_broker_order_id`; when `entry_broker_order_id IS NULL`, `NOT EXISTS` a link or order-naming intent for the trade's ticker with `detection_date <= NEW.trade_entry_date` (E9); the source pairing: `placement_session_source = 'entry_date_fallback'` ⇒ `placement_session = trade_entry_date`, while under `'schwab_envelope'` SQL binds NOTHING read from the envelope (RULING G1b: the `placement_session = json_extract(…'$.entry_date')` bind is DROPPED; the value is a SERVICE_ONLY derivation replay-verified in Python by drift (vi), Task 11) — SQL keeps the stored value's date CHECK, this pairing, the deployment bound and the leg predicates over `NEW.placement_session` AS STORED; **the leg predicate (R0.H, ruled (a))** — let SPEAK = the trade's ticker's `latch_view_events` rows with `date(first_viewed_ts) >= '2026-08-03'` (recorded under the actionability instrument; earlier rows are 0033's backfill and never speak), and PRE = SPEAK rows with `date(first_viewed_ts) <= NEW.placement_session` (view clock, date grain, local naive; NEVER `view_session_date`). Then: `NOT EXISTS` a PRE row with `actionable_ever_viewed = 1`; `admitted_leg = 'telemetry'` ⇒ `EXISTS` a PRE row, and the evidence is EXACTLY the PRE set (R2-06). Every PRE row's `view_event_id` appears in `$.telemetry_rows` with `actionable_ever_viewed` 0 and its `first_viewed_ts` / `view_session_date` equal to live. Every evidence id is a PRE row, the entry count equals the PRE count, and `$` has exactly the one key `telemetry_rows`; `admitted_leg = 'deployment'` ⇒ `NOT EXISTS` a PRE row AND `placement_session < '2026-08-03'` AND the evidence is bound by VALUE, never by bytes (the service serializes with spaces; `json_object` is compact): `json_extract(leg_evidence_json,'$.deployment_session') = '2026-08-03'`, `json_extract(…,'$.placement_session') = NEW.placement_session`, and exactly 2 keys (R2-06).
- `trg_eia_structural` (fires when `'structural'`) — the cited link exists and `broker_order_id = NEW.entry_broker_order_id` (NOT NULL); the death-before-fill DERIVATION is service-only (`mandate_alive_at`, Python); the trigger binds the recorded rung/session shape (paired + bind CHECKs) AND binds the probe JSON to the row by VALUE (R2-06): `$.link_id = NEW.cited_latch_link_id`, `$.clear_reason = NEW.cited_latch_terminal_rung`, `$.clear_session = NEW.cited_latch_terminal_session`, `$.decline_reason = 'mandate_not_alive'`, and `$.fire_candidate_id` = the link's `candidate_id`; the remaining keys (`horizon_session`, `bars_through`, `freeze_tier`) are service-only. The trigger is in Task 11's admission map; the DERIVATION is Task 11's `SERVICE_ONLY` entry (SS-7).
- Append-only TRIPLE: `trg_eia_no_update` (`BEFORE UPDATE` → ABORT, EXCEPT the one FK-driven nulling, copied from the `0039:700-762` shape: `NEW.entry_fill_id IS NULL AND OLD.entry_fill_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM fills WHERE fill_id = OLD.entry_fill_id)` with every other column `IS` its OLD value; the whole permit sits inside `WHEN NOT COALESCE(…, 0)`, R3-05), `trg_eia_no_delete` (`BEFORE DELETE` → ABORT), `trg_eia_no_replace` (conflict-scoped `BEFORE INSERT … WHEN EXISTS (SELECT 1 FROM entry_intent_attestations WHERE trade_id = NEW.trade_id OR attestation_id = NEW.attestation_id)` → ABORT; the service's insert has no `ON CONFLICT`, so the third-facet hazard does not arise — a test pins it).
- **The two unattested `trades` twins (CHARC-S3 condition 2, RD's recommendation ADOPTED; N4's ruled text UNTOUCHED — one trigger, one refusal, one message).** Created in THIS section AFTER `CREATE TABLE entry_intent_attestations`, because their bodies name it. Text verbatim from CHARC-S3.2:
  - `trg_trades_entry_intent_unattested_update`: `BEFORE UPDATE OF entry_intent ON trades FOR EACH ROW WHEN COALESCE(NEW.entry_intent,'') = 'unintended_execution' AND NOT EXISTS (SELECT 1 FROM entry_intent_attestations WHERE trade_id = NEW.id)` → `RAISE(ABORT, 'entry_intent unintended_execution requires its attestation row; use swing trade assign-intent')`.
  - `trg_trades_entry_intent_unattested_insert`: `BEFORE INSERT ON trades FOR EACH ROW WHEN COALESCE(NEW.entry_intent,'') = 'unintended_execution'` → the same ABORT. An attestation cannot precede its trade (the FK), so an INSERT carrying the value is always unattested. It also fires on the INSERT half of `INSERT OR REPLACE INTO trades`: it NARROWS AL-3's `entry_intent` facet and does NOT close AL-3.
  - Both `WHEN`s are TOTAL and sit on `TOTAL_WHEN` (b22_37). Both are `SCHEMA_ONLY` in Task 11 (no service operation mirrors a barrier). AUTHORIZE-THEN-ABORT holds by construction: every generic writer refuses the value at the seam (F5), and the sole writer inserts the attestation THEN updates `trades` in one `BEGIN IMMEDIATE`. The SQL enforces that order in BOTH directions: `trg_eia_trade_binding` requires `trades.entry_intent IS NULL` at the attestation's insert, and the update twin requires the row at the `trades` update.

**Tests** (`tests/data/test_migration_0040_attestations.py`; each a ONE-mutation of a valid real-shape row planted RAW on plain `sqlite3`, trigger tested ALONE):
- `test_a_valid_tier2_row_inserts_b22_50` / `test_a_valid_structural_row_inserts_b22_51` (baselines).
- `test_each_check_null_mutation_does_not_pass_b22_52` — ONE test, parametrized with one case per CHECK: `admission_tier` NULL, a tier-2 row with `placement_session` NULL, a structural row with `cited_latch_terminal_session` NULL, and so on for every CHECK. Each is REFUSED.
- `test_terminal_session_equal_to_entry_refuses_b22_53` / `test_terminal_session_day_before_entry_admits_b22_54` (inequality direction pinned).
- `test_malformed_dates_refuse_b22_77` (SS-4, R1-08's missing test) — parametrized over `trade_entry_date`, `placement_session`, `cited_latch_terminal_session` (`2026-00-01`, `2026-8-1`, `2026-02-30`) and `outcome_known_at` (`2026-08-11 16:00:00`, `2026-00-11T16:00:00`): each REFUSED.
- `test_deployment_leg_boundary_2026_08_03_refuses_b22_55` / `test_deployment_leg_boundary_2026_08_02_admits_b22_56`.
- `test_cited_field_outside_allowlist_refuses_b22_57` (`pre_trade_locked_at`); `test_tag_only_citation_refuses_b22_58` (`["emotional_state_pre_trade"]`); `test_snapshot_not_equal_to_live_text_refuses_b22_59` (one byte: the two-space `notes` with one space).
- `test_a_reconciliation_correction_on_a_cited_field_refuses_b22_60` (planted `field_name = 'notes'`) and `test_a_multi_field_correction_carrying_notes_as_a_non_first_key_refuses_b22_61` (P2-b) and `test_a_provenance_correction_naming_trades_notes_refuses_b22_62` (planted under `ignore_check_constraints` from `tests/trades/_cohort_provenance_fixtures.py`, P2-a) — an implementation checking one table FAILS 60 or 62.
- `test_outcome_not_the_earliest_non_entry_fill_refuses_b22_63` — parametrized: (`stop` then `exit`, recording the `exit`) and (`trim` then `stop`, recording the `stop`) both REFUSE; recording the earliest (incl. a `trim`) ADMITS (E2).
- `test_tier2_with_a_link_for_the_order_refuses_b22_64`; `test_tier2_with_an_intent_naming_the_order_refuses_b22_65`; `test_no_order_id_and_a_ticker_link_refuses_b22_66` (E9).
- Leg twins (R0.H): `test_pre_row_with_actionable_1_refuses_b22_67`; `test_telemetry_leg_without_a_pre_row_refuses_b22_68`; `test_deployment_leg_with_a_pre_row_refuses_b22_69`; `test_a_backfilled_row_before_0803_never_speaks_b22_70` (first viewed 08-02, `actionable_ever_viewed` 1, placement 08-02 → the deployment leg is still accepted); `test_first_view_on_0803_speaks_b22_76` (the `>=` boundary twin of b22_70).
- `test_valid_json_but_wrong_evidence_refuses_b22_79` (R2-06), parametrized one-mutation: a telemetry evidence set with an EXTRA id, with a MISSING PRE id, and with a PRE row's `first_viewed_ts` altered; deployment evidence naming a different placement, and one with an extra key; a probe JSON with a different `link_id` / `clear_reason` / `clear_session`, and `decline_reason` not `mandate_not_alive`. Each REFUSED. `test_malformed_json_is_a_constraint_abort_b22_48` (R2-05): the text `x` in each of the four JSON columns → `IntegrityError` (the trigger's or the CHECK's message), never `OperationalError`.
- RULING G1b's trigger twin ALONE, raw on plain `sqlite3`, the stored reading established by `ensure_entry_fill_identities` (the production writer's path): `test_an_order_id_other_than_the_stored_canonical_reading_aborts_b22_210` (the b22_50 row with `entry_broker_order_id` one byte different → ABORTS with the binding message; the b22_50 control passes) and `test_an_order_id_over_a_refused_reading_aborts_b22_211` (the entry fill's envelope is a document the authority REFUSES — a padded order id — so its reading is `refused`; the row naming the order id → ABORTS). Pre-fix arithmetic: the retired `json_extract` bind read the padded value `" 1007427919619 "`, so a row naming it PASSED; under the stored-reading bind it aborts.
- RULING G1d item 3's NULL-id close, the trigger ALONE, raw on plain `sqlite3`, each with a CONTROL (the stored body with the close cut out, asserted to carry the RULING G1b manifest hash `e7125868…`, installed in the test DB): `test_a_null_id_over_a_refused_reading_aborts_b22_213` (padded order id → reading `refused`; NULL id ABORTS with the binding message; the control ADMITS it), `test_a_null_id_over_a_fill_with_no_envelope_passes_b22_214` (envelope NULL, no reading; PASSES on both), `test_a_null_id_over_a_canonical_no_key_reading_passes_b22_215` (envelope with no `schwab_order_id` → reading `canonical`, id NULL; PASSES on both), `test_a_null_id_over_an_unread_envelope_aborts_b22_216` (parametrized: the reading row raw-DELETED with `trg_fei_no_delete` dropped in the test DB, the ruling's case; and the document rewritten raw after its reading; NULL id ABORTS, the control ADMITS). The real verdict runs in a SAVEPOINT rolled back before the control runs (the table is append-only). Pre-fix arithmetic: under the G1b body `NULL IS NULL` held in all four worlds, so the two ABORT cases admitted (red observed at encoding: 3 failed, 2 passed).
- `test_relabel_refused_when_trades_entry_intent_is_set_b22_71`; `test_update_aborts_b22_72` (every column, `entry_fill_id` included while its fill EXISTS: a manual nulling ABORTS); `test_delete_aborts_b22_73`; `test_insert_or_replace_aborts_b22_74`.
- `test_deleting_the_attested_entry_fill_is_not_blocked_b22_191` (R3-05). With `foreign_keys=ON`, the real `split_into_partials` runs on an attested trade's entry fill and SUCCEEDS: `entry_fill_id` goes NULL, `entry_fill_id_at_assignment` and every other column are byte-unchanged. (The drift line for a deleted fill is b22_116's case, in Task 5, where `drift_report` exists.) Under the pre-fix RESTRICT it raises `IntegrityError`, so the test goes RED.
- `test_outcome_on_the_entry_session_refuses_b22_49` (CF-R3-1): the bind `trade_entry_date < substr(outcome_known_at,1,10)` with outcome `2026-08-07T16:00:00` on a `2026-08-07` entry → REFUSED; `2026-08-08T16:00:00` → ADMITS (the inequality direction pinned).
- `test_model_post_init_mirrors_every_check_b22_75` — for each CHECK above a Python mutation raises `ValueError` (the #11 triple: CHECK + constant + `__post_init__`).
- The unattested twins, raw on plain `sqlite3`, each asserting the TWIN's message (never a bare `IntegrityError`) and each run on a control DB with the twin under test DROPPED, where the same statement SUCCEEDS (pre-fix RED): `test_raw_update_to_the_value_without_an_attestation_aborts_b22_202` (`UPDATE trades SET entry_intent='unintended_execution'` on a NULL-intent trade with no attestation row → ABORTS naming `assign-intent`); `test_the_same_update_with_a_raw_planted_attestation_passes_b22_203` (a valid attestation row planted raw first — the b22_50 row — then the same UPDATE → passes, and the N4 trigger then guards it); `test_raw_insert_of_a_trade_carrying_the_value_aborts_b22_204` (`INSERT INTO trades (…, entry_intent) VALUES (…, 'unintended_execution')` → ABORTS; the `INSERT OR REPLACE` form on an existing trade id ABORTS too — AL-3's `entry_intent` facet, narrowed). The service-order halves (b22_80 passes through the real triggers; the reversed order aborts, b22_205) are Task 5's.
Commit: `feat(data): Task 4 — migration 0040 part 2: entry_intent_attestations (append-only triple, evidence triggers)`.

## Task 5 — The service `swing/trades/entry_intent_assignment.py` (F4)

**Shape (F4 conditions):** `preflight(conn, cfg, *, trade_id, cite, reason, now) -> AssignmentVerdict` reads only, over the STORED envelope readings `assign` establishes (standalone on an un-read envelope it raises the step-5 invariant); `assign(conn, cfg, *, trade_id, cite, reason, applied_by, dry_run) -> AssignmentResult` REJECTS a caller-held transaction (`conn.in_transaction` → `RuntimeError`), opens `BEGIN IMMEDIATE`, calls `ensure_entry_fill_identities(conn)` BEFORE ANY READ (RULING G1b; rung 6's idiom, `latched_origin.py:1827`; a drift raise → the typed refusal `identity_drift`, fail-closed, nothing written), re-runs `preflight` INSIDE it (`dry_run` takes the same path and ROLLBACKs, the readings `ensure` appended included, so a dry run writes nothing), then INSERTs the attestation row THEN `UPDATE trades SET entry_intent = 'unintended_execution' WHERE id = ? AND entry_intent IS NULL` (rowcount must be 1), COMMIT; any exception → ROLLBACK → re-raise. Both rows or neither. **The service holds NO attestation-table SQL (CHARC-S3 condition 1):** it builds an `EntryIntentAttestation` and calls `swing.data.repos.entry_intent_attestations.insert_attestation` inside its transaction, and reads through `get_attestation`; its own SQL is reads of `trades` / `fills` / the latch and audit tables, and the one `UPDATE trades`. **Files:** `swing/trades/entry_intent_assignment.py` (new), `tests/trades/test_22b_assignment_service.py` (new); it imports the Task-4 repo module.

**`preflight`, in order; the FIRST failing step returns a typed refusal `(code, message)` — each message names its recovery, ASCII:**
1. Trade exists; not voided (`voided_trade_ids`); `entry_intent IS NULL` → else `already_set` ("no relabel path; a non-empty relabel is a new evidence class"). `get_authoritative_entry_fill` returns a fill → else `no_entry_fill` REFUSE (R2-03; it returns `Fill | None`, `repos/fills.py:297-314`), BEFORE any envelope read.
2. `cite` ⊆ `CITABLE_FIELDS`, non-empty, no duplicates → else `not_citable` (names the field); ∩ `DESCRIPTIVE_FIELDS` non-empty → else `no_descriptive_field` (F2 S4). Each cited field's live text non-NULL and non-blank → else `empty_cited_field`. `reason.strip()` non-empty → else `blank_reason` (R3-07: click's `required=True` accepts `""`, and without this step a dry run would report ADMIT for a row the table CHECK `length(trim(reason)) > 0` refuses. Python's `strip()` is the STRICTER of the two, so the service refuses everything the CHECK refuses).
3. **P2** over BOTH audit tables, the Task-4 predicate in Python (field_name OR envelope keys; `provenance_corrections.corrected_fields_json`) → else `cited_field_corrected` (names field + correction id). Records `audit_trail_checked_at = now` and the count (must be 0).
4. **P3:** `outcome_known_at` = earliest non-entry fill `fill_datetime` or `None` (open trade — F2 edge (i): ADMISSIBLE; written once; the row carries NO P&L/MFE/MAE). If it is not None and its date is on or before `entry_date` → `outcome_not_after_record` REFUSE ("the outcome (first non-entry fill, <date>) is on the entry session; the record cannot be shown to pre-date it"). This step is CF-R3-1. It is the service twin of the table bind `trade_entry_date < substr(outcome_known_at,1,10)`, the brief's test (a); without it the service admits a row the CHECK aborts. Live incidence: 1 of 28 trades (trade 11, entry and stop both 2026-05-22), measured with plain `sqlite3` `mode=ro`. That trade is already refused at step 1 (`already_set`).
5. **Tier detection (DETECTED, never chosen):** the entry fill = `get_authoritative_entry_fill(conn, trade_id)` (identity ONLY — that helper hydrates 12 columns and leaves `schwab_source_value_json` None, `repos/fills.py:301-308`); the envelope COLUMN is read by a SEPARATE `SELECT schwab_source_value_json FROM fills WHERE fill_id = ?` (its value, never a `json_*` of it). A NULL or blank (whitespace-only) envelope is ABSENT: no reading is expected, `order_id` is None, and the E9 path with the `entry_date_fallback` applies. Otherwise the order id is the STORED reading (RULING G1b, 22-A PERSIST-CANONICAL): `stored_identity(conn, fill_id, raw)` (`swing/data/repos/fill_envelope_identity.py`; `fill_id` AND `envelope_raw` = the fill's CURRENT column), established by `assign`'s `ensure_entry_fill_identities` call (it RE-VERIFIES, and covers exactly the entry fills 22-B reads). The stored state REPLACES `envelope_is_canonical` as the refuse-first (same authority, persisted): `refused` → `envelope_refused` REFUSE (never admits); `canonical` → `order_id` = the stored `broker_order_id`. An envelope present with NO reading after `ensure` → `EnvelopeReadingMissingError`, an invariant breach that RAISES and rolls back, never a refusal (b22_212).
   - `order_id` and `find_accepted_latch_order(conn, broker_order_id=order_id)` returns ≥1 link → **structural**: `mandate_alive_at(conn, cfg, order=links[0], fill_session=date.fromisoformat(entry_date), exclude_trade_ids=frozenset({trade_id}))` (NO second comparison — gotcha #31). **Map the verdict by its REAL shape (SS-1).** A PROVEN death is itself a probe refusal: `recognised_but_underivable=True`, `decline_reason == "mandate_not_alive"`, carrying `clear_reason`/`clear_session` (`latched_origin.py` ~`:2540-2556`). `admitted=True` means the mandate was ALIVE at the fill (`_fill_wins`, ~`:2271`: a same-session death loses to the fill). So:
     - `mandate_not_alive` with `clear_reason` ∈ `DEATH_RUNGS` and `clear_session < entry_date` → **ADMIT structural**.
     - `mandate_not_alive` with `clear_reason` `fill` (ANOTHER trade consumed the mandate; the subject is excluded) or `superseded` → `not_a_death_rung` REFUSE (names the rung and both dates).
     - `admitted=True`, `clear_reason` None (live) → `mandate_fill` ("a mandate fill is `standard` under clause (1)").
     - `admitted=True` with a death on or after the entry session → `death_not_before_fill` (names the rung and both dates).
     - any OTHER `decline_reason` (`frozen_value_drift`, `aliveness_unverifiable`, `fire_not_derivable`, …) → `structural_unprovable` (names it).
     - `LatchProbeInvariantError` propagates (an invariant breach, never a refusal).

     **`cited_latch_probe_json` is BUILT by the service (SS-2)** from the verdict: `{"decline_reason","clear_reason","clear_session","horizon_session","bars_through","freeze_tier","link_id","fire_candidate_id"}`. `probe_evidence` is None on the death path, except when `clear_reason` is `fill`, so it cannot fill the paired CHECK's NOT NULL. >1 link → `ambiguous_links` REFUSE.
   - `order_id` and NO link but a `latch_order_intents` row with `actual_broker_order_id = order_id` → `unlinked_intent` REFUSE (N5 (b): "a latch intent without an accepted-order link cannot tie this fill to a mandate; tier 2 cannot apply because the instrument recorded it; the recovery is a link backfilled under 22-A's rules").
   - no `order_id` → E9 fail-closed check; then tier 2 with `placement_session = entry_date`, source `entry_date_fallback`.
   - otherwise → **tier 2 (`contemporaneous_record`)**: `placement_session` = the envelope's `entry_date` (source `schwab_envelope`), else `entry_date`. **ONE engine (RULING G1b; SS-3's first-key/last-key note is MOOT, because only Python reads the envelope):** the service reads `entry_date` in PYTHON (`json.loads`, only AFTER the stored reading is known `canonical`) and PERSISTS `placement_session` + `placement_session_source`; SQL binds the stored value (Task 4). JSON `null` or an absent key → `entry_date_fallback`. **RULING G1d item 1, (a) (CHARC, brief @ `640a8f28`):** the stored reading refuses duplicates and padding for `ENVELOPE_KEYS_READ_BY_BOTH_DOMAINS` (`schwab_order_id`, `schwab_instrument_symbol`) ONLY, and a duplicated `entry_date` is stored `canonical` (measured at encoding, re-measured by CHARC). So the Python read counts the ROOT object's `entry_date` keys itself (the canonicaliser's `object_pairs_hook` idiom) and REFUSES `entry_date_ambiguous` on more than one, before `json.loads`' last-key rule can choose. That is its OWN code: `envelope_refused` means only "the stored reading's state is refused" (the refusal-legibility rule), and the message names the fact (the record's entered date cannot be read; the envelope carries `entry_date` N times). Widening 22-A's canonicaliser to `entry_date` is REJECTED (the key set is named for the TWO-ENGINE read; `entry_date` is read by one engine). A present value that is not `YYYY-MM-DD` (a padded one included) → `unprovable` REFUSE (live: 14/14 entry envelopes carry a 10-char text `entry_date`). **Leg 1 (R0.H, ruled (a)):** SPEAK/PRE as defined in Task 4, over `latch_view_events WHERE ticker = trade.ticker`: any PRE row with `actionable_ever_viewed = 1` → `instrument_offered` REFUSE ("the instrument offered the order and did not fire; this is not an unintended execution the record can prove"); PRE non-empty and all 0 → ADMIT `telemetry` (evidence = the PRE rows: id, `actionable_ever_viewed`, `first_viewed_ts`, `view_session_date`); SPEAK rows whose first view is AFTER placement → SILENT. **Leg 2** (PRE empty): `placement_session < INSTRUMENT_DEPLOYMENT_SESSION` → ADMIT `deployment` (evidence = the constant + the placement session); otherwise `instrument_existed` REFUSE. **Leg 3:** placement underivable → `unprovable` REFUSE naming the missing fact.
6. Serialization (exact): `cited_fields_json = json.dumps(sorted(cite))` (sorted, default separators → `["notes", "why_now"]`); `cited_text_snapshot_json = json.dumps({f: live_text}, ensure_ascii=False, sort_keys=True)`; `leg_evidence_json` and `cited_latch_probe_json` use the same `json.dumps(obj, ensure_ascii=False, sort_keys=True)` (default separators) (SS-5). The CLI's `--cite` order never reaches storage.

**`drift_report(conn, trade_id) -> list[str]` (E6, pure read):** names (i) a cited field whose live text differs from the snapshot; (ii) a `trade_entry_date` differing from live `entry_date`; (iii) `trades.entry_intent = 'unintended_execution'` with NO attestation row (the value written outside the seam — raw SQL is not schema-refused; this reader is its detector); (iv) leg-1 evidence drift (R3-09). This covers a snapshotted telemetry row that no longer exists, and one whose live `actionable_ever_viewed`, `first_viewed_ts` or `view_session_date` differs from the snapshot. The four belts make a vanished row, a 1 → 0 downgrade and a moved `first_viewed_ts`/`ticker` unreachable (R0.K-RULING). A raw UPDATE of `view_session_date` or `view_event_id` stays reachable, and neither can move a verdict (b22_201). It also covers the PRE set RE-DERIVED from live rows no longer equalling the snapshotted ids. Under the belts only a raw INSERT of a new key can change that set, and fabrication is outside the threat model (ledger R0.K). The reader states each as a check, not a hope, and it still names a change a dropped belt would let through. Two more (R2-14): (v) `entry_fill_id IS NULL` (the fill was deleted, e.g. by a split, R3-05), or the trade's CURRENT authoritative entry fill (by `get_authoritative_entry_fill`'s order) is not `entry_fill_id_at_assignment`; (vi) (RULING G1b: Python over STORED values; `drift_report` is a pure read and never calls `ensure`) that fill's CURRENT stored reading — `stored_identity` against its CURRENT envelope — names an order id other than `entry_broker_order_id` (an absent envelope reads None), or is `refused`, or does not exist (a document never read is named UNVERIFIED, not assumed); or, when the source was `schwab_envelope`, the `entry_date` re-read in Python from that canonical document differs from `placement_session` (the replay that makes `placement_session`'s SERVICE_ONLY derivation a checked claim, Task 11). The admission inputs are already frozen as columns, so this needs no new column. Fill quantity, price and origin are NOT admission inputs and are not snapshotted (declined with that reason).

**Tests** (`tests/trades/test_22b_assignment_service.py`; real-shape fixtures: trade 20's row bytes, fill 41's envelope `{"entry_date": "2026-08-01", …, "schwab_order_id": "1007427919619", …}`, fill 44 `stop` `2026-08-11T16:00:00`, the AMN telemetry row 5):
- `test_trade20_shape_admits_contemporaneous_record_b22_80` — tier `contemporaneous_record`, `admitted_leg = 'deployment'` (row 5 first viewed 08-01 < 08-03 → leg 1 SILENT; placement 08-01 < 08-03), `placement_session = '2026-08-01'`, `outcome_known_at = '2026-08-11T16:00:00'`, snapshot byte-for-byte (two-space `notes`), `trades.entry_intent` set.
- b22_80 is also the unattested-twin discriminator "the service's own write passes through the real triggers" (CHARC-S3.2): it runs on a `target_version=40` DB carrying both twins and the N4 trigger. `test_the_reversed_service_order_aborts_b22_205` — the same trade-20 fixture with the service's two writes REVERSED by monkeypatch (the `trades` UPDATE issued before `insert_attestation`) → the update twin ABORTS with its message, and a fresh connection sees neither row. The order is pinned by execution, not by reading the source.
- `test_both_rows_or_neither_b22_81` — monkeypatch the `trades` UPDATE to raise after the INSERT: fresh connection sees NO attestation row and `entry_intent` NULL.
- `test_caller_held_transaction_is_rejected_b22_82`; `test_dry_run_writes_nothing_and_returns_the_predicates_b22_83` (R2-12: asserts the STRUCTURED `AssignmentResult` only, which has `admitted`, `refusal_code`, `message`, `tier`, `admitted_leg`, `leg_evidence`, `placement_session`, `corrections_by_table` (`{'reconciliation_corrections': 0, 'provenance_corrections': 0}`), `outcome_known_at`, `attestation_id` (None on dry run); the service never writes to stdout; stdout is b22_110's) (fresh-connection read after; RULING G1b: `fill_envelope_identity` is unchanged too, since the dry run ROLLBACKs `ensure`'s appends).
- N1 discriminators (R0.E, one mutation each, real row shape): `test_a_speaking_row_flipped_to_actionable_refuses_naming_the_offer_b22_84` (the brief's "telemetry row in the leg-1 window": a row first viewed 08-05 with placement 08-06, `actionable_ever_viewed` 0 → 1); `test_no_telemetry_placement_0801_admits_on_deployment_b22_85`; `test_no_telemetry_placement_0803_refuses_b22_86`; `test_no_telemetry_placement_0802_admits_boundary_twin_b22_87`; `test_no_envelope_entry_date_0803_refuses_b22_88`; `test_oii_shape_routes_structural_not_tier2_b22_89` (actionable 1 + a validity row naming the order + a link → structural path, never tier 2).
- `test_a_speaking_zero_row_before_placement_admits_on_telemetry_b22_90` (a row first viewed 08-05, ever 0, placement 08-06 → `telemetry`); `test_a_speaking_row_first_viewed_after_placement_is_silent_b22_105` (first viewed 08-07, placement 08-06 → leg 2 → REFUSE since 08-06 ≥ 08-03); `test_row5_flipped_to_1_is_still_silent_b22_106` (the AMN backfill row with `actionable_ever_viewed` 1 does not speak → leg 2 ADMITS — the ruled consequence; contrast b22_84, where the flipped row SPEAKS).
- F3 structural, the AMN geometry (fire 08-03, `skip` + close 30.80 below invalidation 30.85 on 08-07, fill 08-07 @ 36.43; a synthetic link for that mandate): `test_invalidation_terminal_0806_fill_0807_admits_structural_b22_91`; `test_terminal_0807_same_session_refuses_b22_92`; `test_live_latch_refuses_as_mandate_fill_b22_93`; `test_terminal_fill_or_superseded_refuses_b22_94` (parametrized). These drive the REAL `mandate_alive_at` over a seeded derivation (reuse the 22-A task-9 fixture builders; do not stub the probe). **Death-rung coverage:** b22_91 is parametrized over `invalidation`, `horizon`, `declined` (each seeded to die on 08-06 → ADMIT, and its 08-07 same-session twin → REFUSE); `criteria_lapsed` is asserted UNREACHABLE through the probe (`mandate_alive_at` forces the lapse rule OFF, `latched_origin.py:2444-2446`; `clear_reason` is never `criteria_lapsed` there) — it stays a CHECK member as ruled, and the test pins the fact.
- N5: `test_unlinked_intent_naming_the_order_refuses_typed_b22_95`.
- F2: `test_open_trade_admits_with_outcome_null_b22_96` and `test_a_later_trim_or_exit_does_not_backfill_b22_97` (parametrized over a later `trim` and a later `exit`; row bytes identical after an exit fill lands); `test_pre_trade_locked_at_not_citable_b22_98`; `test_emotional_state_alone_refuses_b22_99`; `test_planted_reconciliation_correction_on_notes_refuses_b22_100`; `test_planted_provenance_correction_on_notes_refuses_b22_101`; `test_entry_intent_already_set_refuses_b22_102`; `test_drift_reader_names_a_changed_cited_field_b22_103`; `test_drift_reader_names_a_value_without_attestation_b22_104`; `test_drift_reader_names_a_changed_entry_date_b22_117` (R2-09); `test_drift_reader_names_changed_leg1_actionability_b22_118` (R2-09; the belt DROPPED in the test DB, the b22_127 technique, since the belts make it unreachable otherwise); `test_drift_reader_names_a_moved_authoritative_fill_and_a_changed_envelope_b22_116` (R2-14: a later-inserted earlier-dated entry fill; an envelope rewritten raw and then READ by `ensure`, named as a changed order id; the same rewrite UNREAD, named unverified (RULING G1b); R3-05: the entry fill DELETED, `entry_fill_id` NULL); `test_no_entry_fill_refuses_typed_b22_119` (R2-03). `test_drift_reader_names_moved_leg1_view_fields_b22_195` (R3-09, parametrized one-mutation on a telemetry-admitted row, each a raw UPDATE): `view_session_date` changed, with all four belts present (no belt covers it); `first_viewed_ts` moved past placement (with `last_viewed_ts` moved alongside it where the CHECK `last_viewed_ts >= first_viewed_ts` requires, SS-13), and a SECOND speaking row's `first_viewed_ts` moved INTO the PRE window, both with `trg_lve_view_window_immutable` DROPPED in the test DB (the b22_127 technique, since the fourth belt makes them unreachable otherwise, R0.K-RULING). Each is named. `test_blank_envelope_reads_as_absent_in_both_domains_b22_193` (R3-06, its SERVICE half, kept by RULING G1b): a whitespace-only envelope on the entry fill gives `order_id` None with no reading consulted, then the E9 path, then placement `entry_date_fallback`, and the service-built row INSERTS through the real triggers. Its malformed-JSON-guard discriminator RETIRED with the guard; the 22-A sweep is its SQL discriminator. `test_blank_reason_refuses_typed_b22_194` (R3-07): `reason="  "` gives `blank_reason` on BOTH `dry_run=True` and the write path, and nothing is written. `test_same_session_outcome_refuses_typed_b22_196` (CF-R3-1): an exit fill on the entry session gives `outcome_not_after_record`, and the next-session twin ADMITS. `test_thesis_and_emotional_state_are_citable_beside_a_descriptive_field_b22_109` (`--cite notes,thesis,emotional_state_pre_trade` ADMITS; the trigger baseline b22_50 has the same variant). `test_envelope_order_id_survives_the_helper_b22_107` (trade 20's shape yields `order_id == "1007427919619"` through the real helper + separate read). `test_padded_or_duplicate_key_envelope_refuses_b22_108` (RULING G1b: the code is `envelope_refused`, from the stored reading's `refused` state; it was `non_canonical_envelope` from the retired Python-side check). `test_duplicate_entry_date_key_refuses_b22_78` (REWRITTEN by RULING G1b as a refusal case): an envelope carrying `entry_date` twice (`"2026-08-01"` first, `"2026-08-05"` last), with no link or intent → `entry_date_ambiguous` (RULING G1d (a); the message names the fact and is ASCII), nothing written. The stored reading is `canonical` for this document (the measured correction in step 5), so the refusal is the Python read's own duplicate count; the test pins both facts. Pre-fix arithmetic: a bare `json.loads` read takes `2026-08-05`, and on this fixture leg 2 then refuses `instrument_existed` by coincidence of the date; the discriminating twin places both keys before 08-03 (`2026-08-01` / `2026-08-02`), where a bare `json.loads` read ADMITS on `2026-08-02` and the duplicate count refuses.
- RULING G1b (new): `test_a_refused_reading_on_the_entry_fill_refuses_b22_208` (an undecodable envelope; `ensure` stores `refused`; `envelope_refused`, nothing written, `fill_envelope_identity` unchanged); `test_a_drifted_reading_refuses_identity_drift_b22_209` (a stored reading for the entry fill's CURRENT document that disagrees with the canonicaliser, planted raw — the forged/old-grammar row `record_identity` exists to catch; `identity_drift`, nothing written; MEASURED at encoding: rewriting the envelope raw AFTER a reading does NOT drift, since the new document is a new `(fill_id, envelope_raw)` key and `ensure` appends a fresh reading); `test_an_envelope_with_no_reading_after_ensure_raises_the_invariant_b22_212` (`ensure` monkeypatched to a no-op → `EnvelopeReadingMissingError` raised, never a refusal, nothing written).
Commit: `feat(trades): Task 5 — entry_intent_assignment service (tier detection, N1 legs, P2 both tables, P3, single transaction)`.

## Task 6 — CLI `swing trade assign-intent`

`@trade_group.command("assign-intent")` with `@click.pass_context`: `TRADE_ID`, `--value` (`click.Choice([UNINTENDED_EXECUTION])` — the ONLY writer that names it), `--cite` (comma list), `--reason` (required, non-blank), `--dry-run`. The DB is opened exactly as `trade review` opens it (the group's `ctx` config + `swing.data.db.connect`; `trade_group` has NO `--db` option, `cli.py:577-580`). `applied_by` = a module constant `ASSIGNMENT_APPLIED_BY = "operator"` (the `PROVENANCE_CORRECTION_APPLIED_BY` pattern, `cohort_provenance_correction.py:3047`). Output (ASCII): tier, `admitted_leg` + evidence, the three predicates WITH their values (P1: the leg and its evidence; P2: `corrections 0/0 over reconciliation_corrections, provenance_corrections`; P3: outcome date or `open`), and on write the attestation id. Refusal → `ClickException(message)` (exit 1). Service `ValueError`s wrapped at the boundary.
**Tests** (`tests/cli/test_assign_intent_cli.py`): `test_dry_run_prints_predicates_and_writes_nothing_b22_110`; `test_write_prints_attestation_id_b22_111`; `test_refusal_exits_nonzero_with_the_typed_message_b22_112`; `test_stdout_is_ascii_b22_113` (subprocess, the cp1252 encode gotcha); `test_assign_intent_is_the_only_caller_of_assign_b22_114` — the AST caller walk over `swing/`: the only call site of `entry_intent_assignment.assign` is this command (F4: callers pinned). (The write-closure b22_115 lands in Task 10, where its execute-site inventory is built — SS-6.)
Commit: `feat(cli): Task 6 — swing trade assign-intent (dry-run prints the three predicates with values)`.

## Task 7 — N4: the value is TERMINAL for every generic writer (layers 1-2 + the review form)

1. **Layer 1** `update_entry_intent` step (c) = `assert_entry_intent_change_allowed(conn, trade_id=…, entry_intent=…)` (a pure read, reused by the review surfaces BEFORE their review commit, step 2): read `entry_intent` for the row (inside the caller's `with conn:`); if it is `UNINTENDED_EXECUTION` and the new value differs → `AttestedIntentError(attested_message(<attestation_id read from the table, or '?' pre-v40>))`. Same value → returns without writing (Task 2 step (b)).
2. **Terminality is checked BEFORE the review commits (R2-02).** Both review surfaces commit `complete_trade_review` in its own `with conn:` and only THEN call `update_entry_intent` in a second one (`cli.py` ~`:1669-1712`, `routes/trades.py` ~`:3592-3621`), so a refusal there would land after the review is persisted. Each surface therefore calls `assert_entry_intent_change_allowed(conn, trade_id=…, entry_intent=…)` BEFORE `complete_trade_review`. That is a pure read, exported from `repos/trades.py`, and it is the SAME function `update_entry_intent`'s step (c) calls. `AttestedIntentError` → `ClickException` (CLI) / the 4xx fragment (web), with NOTHING written. `trade review --entry-intent`: the error → `ClickException` with the message. `backfill-intent`: the default query already skips non-NULL; under `--force` / `--trade-id`, an attested row is SKIPPED with the message echoed, never prompted.
3. **Layer 2** is Task 2's reservation (already landed) — this task adds its N4 test.
4. **Review form:** `review_form.html.j2` renders, for an attested trade, a read-only `<span>` with `{{ vm.entry_intent_attested_label }}` and NO `<select name="entry_intent">` (the field is absent from the POST, so the presence gate at `routes/trades.py` ~`:3553` preserves it). `ReviewVM` (`view_models/trades.py` ~`:1344-1351`) gains `entry_intent_attested: bool = False` and `entry_intent_attested_label: str = ""`. The builder (~`:1592`) sets the label from `swing.trades.intent.entry_intent_label(...)`. The template never calls the function, because no such Jinja global is registered (`web/app.py` ~`:133-145`, R3-04). Both are review-form VM fields only, not base-layout VM fields, and both have safe defaults. A handcrafted POST carrying `entry_intent=""` for an attested trade → `AttestedIntentError` → 4xx fragment with the message; value preserved.
**Tests** (`tests/trades/test_22b_terminal.py`, `tests/web/test_routes/test_trade_review_attested.py`): `test_review_post_without_the_field_preserves_b22_120`; `test_review_post_with_empty_refuses_4xx_preserved_b22_121` (R2-02: review fields and `state` byte-unchanged after the refusal, read on a fresh connection); `test_review_form_renders_read_only_no_select_b22_122` (TestClient GET; the fragment carries the text `Unintended execution` and no `name="entry_intent"`, R3-04); `test_cli_review_entry_intent_standard_refuses_b22_123` (R2-02: same unchanged-review assertion); `test_backfill_force_skips_attested_row_b22_124`; `test_corrector_on_entry_intent_raises_reserved_b22_125`; `test_update_entry_intent_same_value_passes_b22_126`; `test_layer1_alone_refuses_with_trigger_absent_b22_127` (a pre-trigger DB shape: DROP the trigger in the test DB; the service layer alone still refuses — so neither layer masks the other; layer 3 alone is b22_34).
Commit: `feat(trades): Task 7 — N4 terminality: update_entry_intent refuses a change from an attested value; review form read-only`.

## Task 8 — Cohort code: exclusion in CODE (N2 (a)) and per-cohort NAMING (N3 (a))

1. `cohort_intent.py`: `CONTRACT_EXCLUSION_CLAUSE`, `CONTRACT_EXCLUDED_ENTRY_INTENTS`; `cohort_excluded_entry_intents(name, *, registered_names=None) -> tuple[str, ...]` returns `CONTRACT_EXCLUDED_ENTRY_INTENTS` for EVERY name (clause (4) is program-wide: "NO hypothesis cohort"; an orphan label is not a hypothesis cohort, and excluding there is the fail-closed direction — declared); `trade_counts_toward_cohort` returns `False` when `entry_intent in CONTRACT_EXCLUDED_ENTRY_INTENTS`, BEFORE the criterion check (so H1 and H2-H5 agree). `cohort_intent_authority` UNCHANGED (H2-H5 still `epoch_contract` — the authority did not change, its text did). Module docstring: authority 2 re-points from the archive to `docs/training-epoch-intent-contract.md` and states clause (4)'s exclusion as a NAMED third grounding (docstring + the constant; no other semantic change).
2. `cohort.py` `list_trades_for_cohort` / `list_closed_trades_for_cohort`: new kwarg `exclude_entry_intents: Collection[str] = ()` → one `entry_intent IS NOT ?` clause per value (NULL-safe: NULL counts). Default EMPTY (E13): `metrics/process.py` and `count_per_cohort` stay unfiltered (observational; clause (4) routes the result to the process card's facet).
3. New helper `list_intent_excluded_for_cohort(conn, *, hypothesis_label, state_filter) -> list[tuple[int, str]]` in `cohort.py`: label-matched (the same `label_matches_hypothesis_sql`), voided-excluded, `entry_intent IN CONTRACT_EXCLUDED_ENTRY_INTENTS` trades, each as `(trade_id, reason)`. **RD-PLAN-READ addition (the UNATTESTED token):** the query carries ONE `LEFT JOIN entry_intent_attestations a ON a.trade_id = t.id`, and `reason` is `intent_exclusion_reason(entry_intent, attested=a.attestation_id IS NOT NULL)` from `cohort_intent.py`: `"unintended_execution"` when the row exists, `"unintended_execution, " + UNATTESTED_TOKEN` (`UNATTESTED_TOKEN = "UNATTESTED"`) when it does not. That is AL-1's production detector for what the schema cannot see (a fabricated or pre-trigger raw write); the twins make the unattested state unreachable through SQL on a v40 DB, and this token still names it on every read if it ever exists. **Declared residual (RD):** a NULL-label unattested row is still unnamed, because the naming line is per-cohort (label-matched); that row was in no cohort, so no count moved.
4. The FOUR governed readers each pass `exclude_entry_intents=cohort_excluded_entry_intents(name, …)` (SQL readers) or rely on `trade_counts_toward_cohort` (in-memory readers), AND populate a new sibling field `intent_excluded: tuple[tuple[int, str], ...] = ()` (E12) from the step-3 helper's `(trade_id, reason)` pairs for the cohort's label-matched excluded trades (closed + open where the reader counts open) — every reader names through the helper, so the UNATTESTED token reaches all four and `hypothesis list`:

| reader | file (anchor) | result type |
|---|---|---|
| journal hypothesis progress | `swing/journal/stats.py` (~`:376-470`) | `HypothesisProgress` (~`:336`) |
| tier comparison | `swing/metrics/tier.py` (~`:700-752`) | `CohortStatistics` (~`:244`) |
| tripwire / `swing hypothesis list` | `swing/recommendations/hypothesis.py` `compute_tripwire_status` (~`:535-600`) | `TripwireStatus` (~`:202`) |
| hypothesis-progress card | `swing/web/view_models/metrics/hypothesis_progress_card.py` (~`:345`, `:441`) | its cohort VM (~`:145`) |

5. ONE renderer `intent_exclusion_lines(intent_excluded) -> tuple[str, ...]` (in `cohort_intent.py`; ASCII: `f"not counted: trade {tid} ({reason})"`, so an unattested row renders `not counted: trade N (unintended_execution, UNATTESTED)`), rendered BESIDE `tier2_cohort_lines(...)` at every site that renders those: `cli.py` (~`:4972`, `:5009`), `journal/stats.py` (~`:519`), `tier.py` (~`:252`), `hypothesis_progress_card.py` (~`:151`). NOT the dashboard: `web/view_models/dashboard.py` (~`:495-504`) renders only `tier2_count_marker` (a count marker, no cohort lines), so it is not a renderer site; its marker is unchanged (R2-10). `tier2_count_marker` is NOT fed intent exclusions.
6. Closure: `test_every_cohort_membership_reader_is_governed_or_reasoned_b22_130` — a STATIC walk over `swing/` for every call of `_label_matches_hypothesis`, `label_matches_hypothesis_sql`, `list_trades_for_cohort`, `list_closed_trades_for_cohort`, `count_per_cohort`; each call site's module is on the GOVERNED list (the four) or the REASONED-EXCLUSION list (`metrics/process.py` — observational facet; `metrics/cohort.py:count_per_cohort` — D29 observational tabs; `web/view_models/metrics/trade_process_card.py` — the one caller of `count_per_cohort`, the same D29 tabs (found by the walk at execution, cell 4); `diagnostics/metrics_wiring_audit.py` — string mentions only; `metrics/label_match.py` — the matcher itself). A new site FAILS until classified.
**Tests** (`tests/metrics/test_22b_cohort_exclusion.py`). Fixture note (a consequence of the CHARC-S3 twins): a v40 DB refuses a raw `unintended_execution` write, so every synthetic `unintended_execution` trade here is ATTESTED through the real `assign(...)` on a deployment-leg shape (no envelope, `entry_date` before 2026-08-03, no link/intent/telemetry for its ticker), unless the test's subject is the unattested state (b22_206).
- `test_contract_exclusion_clause_is_in_the_committed_doc_b22_131` — `CONTRACT_EXCLUSION_CLAUSE` is a substring of the doc's clause-(4) line (bytes read, Task-1 reader) — a doctrine edit dropping the sentence FAILS.
- `test_trade_counts_toward_cohort_false_for_every_cohort_b22_132` (H1, each registered H2-H5, an orphan name, with and without `registered_names`).
- `test_sql_and_memory_halves_agree_b22_133` — for a seeded DB with one trade per (intent ∈ {NULL, standard, by_design, unintended}) × (label ∈ {H1, H2}), the SQL reader set == the in-memory reader set per cohort.
- `test_h1_and_h2_labelled_unintended_trades_are_excluded_and_named_b22_134` — on all four readers + the `hypothesis list` CLI output: each synthetic trade is NOT counted and its line `not counted: trade N (unintended_execution)` appears under ITS cohort only.
- `test_null_intent_h2_trade_still_counts_b22_135` (N2's NULL twin).
- `test_an_else_branch_coercing_unknown_to_standard_fails_b22_136` — monkeypatch `trade_counts_toward_cohort` into the coercing variant; b22_134 goes red (the discriminator is proven by execution).
- `test_process_card_and_count_per_cohort_are_unchanged_b22_137` (observational surfaces still list the unintended trade under its label).
- `test_an_unattested_value_is_named_unattested_on_every_reader_b22_206` (RD-PLAN-READ). Two H1-labelled `unintended_execution` trades: one ATTESTED (assigned through the real `assign(...)` on a deployment-leg shape, so its attestation row exists), one UNATTESTED (the value written by a raw `UPDATE` after the two unattested twins are DROPPED in the test DB — the b22_127 technique; on a v40 DB the twins make the raw write unreachable otherwise, and the drop is the planting method, stated). On all four readers AND the `hypothesis list` CLI output: the unattested trade's line is `not counted: trade N (unintended_execution, UNATTESTED)`; the attested twin's line is `not counted: trade M (unintended_execution)` with no token. Pre-fix (no LEFT JOIN) the unattested line carries no token → RED.
Commit: `feat(metrics): Task 8 — clause (4) exclusion in cohort code with per-cohort naming (four readers + hypothesis list)`.

## Task 9 — Display

- `swing/web/view_models/metrics/process_grade_trend.py` `_INTENT_CSS_CLASS` (~`:78`) gains `"unintended_execution": "unintended"`. The value is the BARE token, because the template already prefixes it: `process_grade_trend.html.j2:70` renders `class="process-grade-marker intent-{{ marker.entry_intent_css_class }}"` and `:69` renders `data-entry-intent="{{ … }}"`. So the marker carries `intent-unintended` + `data-entry-intent="unintended"`; mapping to `"intent-unintended"` would render `intent-intent-unintended` (R3-03). NO `app.css` edit: no `.intent-*` rule exists for ANY value today (grep of `swing/web/`; `.process-grade-marker` at `app.css:408` is the only marker rule). The per-intent hook is the class plus the data attribute, as the existing route test pins for the other three (`tests/web/test_routes/test_metrics_process_grade_trend_route.py:139-143`). The legend `process_grade_trend.html.j2` (~`:58`, ASCII) gains `/ unintended` **and moves to ITS OWN LINE, never shortened (RULING G3a (ii), CHARC 2026-09-24): with the fourth value the text at x=6 ends x~385 against the first series swatch at x=360 (cell-measured by font metrics); the collision is a property of the row, not the text, so the legend takes its own line or the swatch row moves, whichever is the smaller template change.**
- `swing/web/view_models/metrics/trade_process_card.py` (~`:52-54`) filter options gain the value (the facet clause (4) routes the result to), labelled via `entry_intent_label`. **RULING G3a (iii): EVERY facet option is labelled by `entry_intent_label` -- one label source; the pre-existing hard-coded "Standard" (`trade_process_card.py:58`) becomes the label home's "Standard entry" (and likewise for any other hard-coded value label), a text change b22_141 pins.** **RULING G3c (CHARC 2026-09-24): the NULL sentinel's text gets ONE home too -- `NULL_ENTRY_INTENT_LABEL = "Unclassified"` in `swing/trades/intent.py`, imported at `cli.py:1386` (`trade analyze`) and the card's Unclassified facet; `entry_intent_label`'s None contract unchanged; "All" stays local; a static pin in b22_141's pattern forbids an `'Unclassified'` string literal under `swing/` outside `intent.py` (comments excluded), mutation RED by re-hard-coding one site.**
- `review_form.html.j2` prose (~`:136`) — READ; edit only if it enumerates the values.
- CLI `trade analyze` (`cli.py` ~`:1356`) already maps via `entry_intent_label` — verify it renders `Unintended execution`, never `Unclassified`.
- Per RD §4 bullet 4 / brief 4.7 ("the trade-process card's per-tab N is QUOTED WITH the tier page's N until D61 lands"): read as a QUOTING obligation on the witness and RD's reads, NOT a UI change (brief §2 OUT excludes UI; the card has no such note today, `trade_process_card.html.j2:23`). Encoded in Task 13's witness step 5 (both Ns quoted, each with its method); no card change. This is R1-14, CONFIRMED by RD (R0.K-RULING.2: "a READ obligation on the witness and on my reads, NOT a card UI change. The encoding stands.").
**Tests:** `test_trend_renders_own_class_not_standard_or_unclassified_b22_140` (TestClient; asserts `data-entry-intent="unintended"` and the class token `intent-unintended`, and the ABSENCE of `intent-intent-`); `test_process_card_filter_offers_the_value_b22_141`; `test_cli_analyze_label_b22_142` (the witness step-4 surface). **b22_143 (`test_journal_trade_view_renders_own_label`) is RETIRED (RULING G3a (i), CHARC 2026-09-24): no stored-trade page renders `entry_intent` for ANY value (`journal_trade_detail.html.j2` and `trades/detail.html.j2`, three reads); branch A declined as a duplicate of b22_122; a page that renders it is OUT under brief §2 and banked as a post-merge display-gap follow-on for RD/operator.**
Commit: `feat(web): Task 9 — unintended_execution renders with its own class and label`.

## Task 10 — The two precondition pins (F2 S3, F3)

- `tests/trades/test_22b_preconditions.py::test_no_unaudited_writer_of_the_citable_fields_b22_150` — an AST walk over EVERY `.execute` / `.executemany` / `.executescript` call in `swing/**/*.py`: a call whose SQL argument resolves statically (literal, f-string with literal SQL skeleton, or module constant) is classified from its text; a call whose SQL does NOT resolve is required to be on a reviewed inventory `DYNAMIC_SQL_SITES = {(module, function): reason}` (at least the corrector's field-SET builder and `repos/trades.py`'s review-fields builder, each with the column set it can write). Assertion: no resolved statement SETs `notes|why_now|thesis|emotional_state_pre_trade`, and no inventoried dynamic site can, except the audited corrector and the migration runner's schema-rebuild IDENTITY copies (today 0014 and 0040), each asserted column-list == select-list in order (RULING G3b, RD 2026-09-24, replacing "except the audited corrector"). A new unresolved site FAILS until inventoried (the closure check, not a hand list). A found writer is a REPORTED finding, never a new channel.
- `test_no_other_production_writer_of_the_value_b22_115` (R1-13; moved here from Task 6 because it needs this inventory, SS-6) — over the same inventory: every SQL site that writes `trades.entry_intent` or INSERTs into `entry_intent_attestations` is one of {`entry_intent_assignment.py` (the service: the `trades.entry_intent` UPDATE only), `swing/data/repos/entry_intent_attestations.py:insert_attestation` (the ONLY `INSERT INTO entry_intent_attestations` site, CHARC-S3 condition 1; its sole caller is the service, b22_114's walk), `update_entry_intent` (seam-guarded), `insert_trade_with_event` (sole caller `entry.py` ~`:2242`, `EntryRequest`-guarded), the corrector's dynamic builder (reserved)}; a new site FAILS.
- `test_death_then_fill_is_expressible_b22_151` — R0.C C.3 pinned by execution: a derivation where an `invalidation` lands on session D and a same-ticker in-zone fill on D+1 → `_resolve_terminal` returns the DEATH terminal (`clear_reason='invalidation'`, `clear_session=D`, `clear_trade_id` None); the same-session twin returns `fill` (R6 tie → fill).
Commit: `test(trades): Task 10 — pin the F2-S3 writer walk, the value's write-closure, and the F3 death-then-fill precondition`.

## Task 11 — AUTHORIZE-THEN-ABORT closure (trigger predicate set ⊆ service predicate set)

`tests/trades/test_22b_authorize_then_abort.py::test_every_admission_trigger_predicate_has_a_reached_service_check_b22_160` — a hand-written map `{trigger_name: [predicate_id, ...]}` over the ADMISSION triggers (`trg_eia_trade_binding`, `trg_eia_cited_fields`, `trg_eia_audit_trail`, `trg_eia_outcome`, `trg_eia_tier2`, `trg_eia_structural`), CLOSURE-CHECKED by trigger NAME against the stored v40 schema (`run_migrations(c, target_version=40)`; no message-code parsing): every trigger on `entry_intent_attestations` is in the map or in `SCHEMA_ONLY` (`trg_eia_no_update`, `trg_eia_no_delete`, `trg_eia_no_replace` — barriers with no service operation to mirror, tested alone by b22_72-74, and `trg_eia_no_update`'s one FK-nulling permit by b22_191, R3-05; the N4 trigger, the two unattested `trades` twins (CHARC-S3.2; b22_202-205) and the four lve belts likewise, b22_34-43 and b22_197-199). The closure's name check covers `trades` too: every 0040-created trigger on `trades` is on `SCHEMA_ONLY`. Predicate ids come in TWO kinds (SS-7). A **REFUSAL** predicate maps to a `preflight` refusal code, and a parametrized case drives its ONE-mutation fixture through `preflight` FIRST, asserting that code. A **DERIVED-VALUE** predicate covers a field the service COMPUTES rather than admits on: `entry_fill_id` / `entry_fill_id_at_assignment`, `entry_broker_order_id` (bound by the trigger to the STORED reading, RULING G1b), `outcome_known_at`, the snapshot values, the leg evidence, and the probe JSON's bound keys (R2-06). Its proof has two halves: a service-built row for EACH admitting path (b22_80 deployment, b22_90 telemetry, b22_91 structural) inserts through the REAL triggers, and the raw one-mutation test in Task 4 shows the trigger bites. Together, no row the service admits is aborted by SQL. `trg_eia_trade_binding`'s entry carries the predicate id `null_id_absent_or_canonical_reading` (RULING G1d item 3), a REFUSAL predicate: its service twin is the step-5 refuse-first, `envelope_refused` on a refused reading (b22_208), with an envelope that has NO reading after `ensure` raising `EnvelopeReadingMissingError` (b22_212, an invariant, never a refusal); its SQL one-mutation tests are b22_213 and b22_216, and its passing twins are b22_214, b22_215 and b22_193. The close MOVED that trigger's hash (manifest `e7125868…` at RULING G1b → `636c8a6c…`); the map's literal is taken from the `target_version=40` image when Task 11 is written, as below. `SERVICE_ONLY = {death-before-fill derivation, placement_session derivation}`, each with its reason — for the second, CHARC's words (RULING G1b): "persist-canonical: SQL never reads the envelope; provenance is replay-verified in Python by drift (vi)"; `SQL_ONLY = ∅` among admission predicates.

**The map is closed over PREDICATES, not only trigger names (R3-08).** A name-keyed map stays green when a predicate is ADDED to an existing trigger. So each admission-map entry carries the D51b-normalized sha256 of that trigger's stored SQL. The table's own CHECK binds are a SEVENTH entry, keyed `("table", "entry_intent_attestations")`, with the table DDL's hash and its bind ids: death strictly before fill, the deployment bound, the outcome-after-record bind (CF-R3-1: service refusal `outcome_not_after_record`), the JSON-shape CHECKs, the date CHECKs, `reason` non-blank (R3-07: `blank_reason`) and `entry_fill_id`'s pairing. A stored hash that differs from the map's FAILS and names the object. So a new SQL refusal cannot land without the map being re-edited, together with its predicate id, its refusal code or derived-value proof, and its one-mutation test. The hashes are taken from the `target_version=40` image when this test is written, and each is pasted as a literal (RULING G1b moved the bodies of `trg_eia_trade_binding` and `trg_eia_tier2`, and RULING G1d's NULL-id close moved `trg_eia_trade_binding`'s again, all before Task 11 exists, so the hashes are simply taken after them).

**Leg 1's read set == its belt set (R0.K-RULING.1, RD's class rule; the table is at Task 3 step 6).** File `tests/trades/test_22b_leg1_read_set.py`. It lands here because it needs Task 4's `trg_eia_tier2` and Task 5's service.
- `test_leg1_read_set_equals_the_belt_set_b22_200`. **READ SET, derived by the authorizer:** use a FRESH connection on a `target_version=40` DB (so no cached statement escapes the authorizer). `conn.set_authorizer` collects every `SQLITE_READ` whose table is `latch_view_events`, from any source, while two things run: `assign(..., dry_run=False)` on the b22_90 telemetry-admitting fixture (this covers the service's leg-1 reads and the compile of every attestation `BEFORE INSERT` trigger, `trg_eia_tier2` included) and `preflight` on the b22_84 refusing fixture. Subtract `LEG1_RECORDED_NOT_DECIDED = {"view_event_id", "view_session_date"}` (each with its reason as a string). **BELT SET, derived from the stored schema:** the union of the `BEFORE UPDATE OF <columns>` lists parsed from the stored `sqlite_master.sql` of the named belts `LVE_EVIDENCE_BELTS` = (`trg_lve_actionable_ever_viewed_monotonic`, `trg_lve_no_delete`, `trg_lve_no_replace`, `trg_lve_view_window_immutable`); these are the same four names as b22_25's expected additions, and each must exist. Assert READ SET == BELT SET == {`actionable_ever_viewed`, `first_viewed_ts`, `ticker`}. Also assert that `trg_lve_no_delete` and `trg_lve_no_replace` exist; they guard the row set, which is not a column. **Discriminating twin (the arithmetic, both paths):** on a copy with `trg_lve_view_window_immutable` DROPPED (the R0.H three-belt state), the same derivation gives a belt set of {`actionable_ever_viewed`} against a read set of {`actionable_ever_viewed`, `first_viewed_ts`, `ticker`}. The check reports the gap {`first_viewed_ts`, `ticker`}, and the test asserts exactly that gap. A future leg-1 statement reading any further column (for example `candidate_id` or `surface`) grows the read set and FAILS the equality.
- `test_recorded_not_decided_columns_cannot_move_a_verdict_b22_201`. On the b22_90 fixture (telemetry ADMIT) and the b22_84 fixture (`instrument_offered` REFUSE), with all four belts present: a raw UPDATE before `preflight`, first of `view_session_date` (to a free date, clear of the UNIQUE key) and then of `view_event_id` (to a fresh id). Each leaves (`admitted`, `refusal_code`, `tier`, `admitted_leg`) identical to the unmutated run. This is the proof that makes the `LEG1_RECORDED_NOT_DECIDED` exclusion a checked claim rather than a label.
Commit: `test(trades): Task 11 — authorize-then-abort closure over the attestation triggers; leg-1 read set == belt set`.

**RULING G4 rider (CHARC 2026-09-24), encoded before Task 12:** the service refuses a copied source date of the wrong shape with ONE typed code `source_date_malformed` (naming the column and the value) at `preflight` step 1, BEFORE any string comparison and before `EntryIntentAttestation.__post_init__` -- `trades.entry_date` must be `YYYY-MM-DD` and `fills.fill_datetime` ISO seconds (`YYYY-MM-DDTHH:MM:SS`); both are bare `TEXT NOT NULL` at v40, and the service compares dates bytewise, which is sound only over canonical shapes. Discriminators: entry_date `2026-8-07` -> `source_date_malformed` (NOT `outcome_not_after_record` / `instrument_existed`); fill_datetime `2026-08-11 16:00:00` -> `source_date_malformed` (NOT an untyped raise); `test_a_copied_value_of_the_wrong_shape_never_reaches_sql_b22_160` asserts the code; dry run and write path both. A source-column CHECK is OUT (F6; the D37/D38 sweep).

## Task 12 — Composition by EXECUTION on 22-B's tree

The pin itself landed in Task 2A. Here: run the full fast suite on the finished tree; `b22_170`, A2-08 and A2-09 green; every 22-A2 and 22-A case GREEN by EXECUTION, including the thirteen HEAD-reaching 22-A2 cases the census read as 0039-scoped (ledger R0.F-3 note) — a red among them is a composition finding, reported, never re-pinned. No commit unless a fix is needed.

## Task 13 — Trade-20 acceptance on a live-shape DB copy + the witness script content

1. `tests/trades/test_22b_acceptance_trade20.py::test_trade20_before_and_after_every_reader_n_unchanged_b22_180` — a fixture DB carrying the live shapes (trade 20 + its fills 41/44 + AMN telemetry row 5 + ≥1 counted trade per cohort, real bytes): capture the four readers' N + `hypothesis list` output; assign trade 20 through `assign(...)`; every N identical; trade 20 in NO cohort's candidate set before AND after (asserted by calling each reader's membership step, not inferred from N); `hypothesis list` output line-for-line identical (N3 (a): 20 was in no cohort and is in none).
2. `test_trade20_attestation_readback_b22_181` — the row, EXACT stored bytes (Task 5 step 6 serialization): tier; `admitted_leg = 'deployment'`; `leg_evidence_json == '{"deployment_session": "2026-08-03", "placement_session": "2026-08-01"}'`; `cited_fields_json == '["notes", "why_now"]'`; snapshot bytes; `outcome_known_at = '2026-08-11T16:00:00'`.
3. **Live dry-run (read-only, no write, executing cell):** copy the live DB to the scratchpad with plain `sqlite3` (the backup API over a `mode=ro` connection). Write a scratch config whose `[paths] db_path` names the copy and whose `backups_dir` is the scratchpad: `trade_group` has no `--db`, and the main group's `--config` (`cli.py:218-225`) is the only override (SS-9). Then `PYTHONPATH=. python -m swing.cli --config <scratch> db-migrate`, and `… trade assign-intent 20 … --dry-run` against the same config. Never open the live file through `swing`'s `connect`. Report the printed predicates. On the migrated COPY, also run b22_44's two checks with its helper, imported, not retyped (R0.J-RULING (iii), the live-copy shape): the foreign-object set is equal to the pre-migration copy's and byte-identical, and the citation-graph trigger aborts a one-mutation fixture row with its own message. Report both results.
4. **Witness script (post-merge, operator-executed, ONE step per result; the orchestrator scripts it from this list):** (1) stop `swing web`; plain-sqlite3 `BEGIN EXCLUSIVE; ROLLBACK` result stated; (2) `swing db-migrate` → the `22b` image path echoed, ONE image in `backups_dir`; `python scripts/schema_manifest.py --db <live>` clean at v40; `PRAGMA foreign_key_check` = []; (3) FIRST capture the pre-image: `swing hypothesis list` saved to a file (R2-11); then `swing trade assign-intent 20 --value unintended_execution --cite why_now,notes --reason "<r>" --dry-run` → ADMIT, tier `contemporaneous_record`, the predicates with values — leg 1 SILENT (row 5 is 0033 backfill), leg 2 printed with the constant's derivation (`2026-08-03`) and placement `2026-08-01`; the witness QUOTES the printed leg, it does not pin one; (4) the same without `--dry-run`; `swing trade analyze 20` prints `Intent: Unintended execution` (RULING G3a (i): the journal/trade view renders no intent for any value), and the operator LOOKS at the trend page in a browser (RULING G3a (ii): the legend on its own line, clear of the swatch row; presentation is visible only at the witness rung); `swing hypothesis list` line-for-line equal to a pre-image captured at step 3; the attestation row read on `mode=ro`; (5) RD re-reads the H-cohort counts on the live DB beside the readers' (no expected count pinned), and the trade-process card's per-tab N is quoted WITH the tier page's N for the same cohort, each with its method (RD §4 bullet 4, until D61).
Commit: `test(trades): Task 13 — trade-20 acceptance on live shapes (readers unchanged, attestation read-back)`.

## Task 14 — Close-out (no new code)

Full fast suite green (report the count read off the final head); `ruff check swing/` clean; D51 diff re-read (zero deletions); the charter-§7 pointer line PROPOSED for RD in the return report (RD lands it; the cell never edits RD's charter): *"2026-09-23 — the forward intent contract now lives in `docs/training-epoch-intent-contract.md` (clause (4) `unintended_execution`, ratified; pinned by sha256 in migration 0040's header)."*; Reviewer A (`strong`) to convergence per recipe §3.

---

## Test roster → acceptance map (brief §4 @ `0f63d6a6`)

**FORWARD RULE (R0.I):** every 22-B migration case pins `run_migrations(c, target_version=40)` — NEVER HEAD. A claim about HEAD belongs only to the `_head` tests (`test_schema_manifest_head.py`, `test_expected_schema_version_is_head`) and to mirror tests whose name carries no version (the drift test). Naming and versioning land in separate commits.

| brief §4 item | tests |
|---|---|
| 4.1 trade 20 admits tier 2 citing `why_now`+`notes`; both-or-neither; snapshot bytes; outcome post-dates the record | b22_80, b22_81, b22_181, b22_49, b22_196; witness steps 3-4 |
| 4.2 structural required when a link exists; tier-2 refused there | b22_64, b22_89, b22_91 |
| 4.2 P1 (N1 legs, R0.H window) + boundary twins; tier detection on a blank envelope | b22_55/56, b22_67-70, b22_76, b22_78, b22_79, b22_84-88, b22_90, b22_105/106, b22_193 |
| 4.2 F3 AMN geometry: 08-06 admit / 08-07 refuse / live refuse / fill+superseded refuse | b22_91-94, b22_53/54 |
| 4.2 P2 both tables (+ multi-field keys) | b22_60-62, b22_100/101 |
| 4.2 `pre_trade_locked_at` / tag-only / open trade / no back-fill / already-set / blank reason / drift | b22_57/58, b22_96-99, b22_102-104, b22_116-119, b22_71, b22_194, b22_195 |
| 4.2 N4 discriminators (review POST ×2, CLI review, backfill --force, corrector, raw UPDATE alone, same-value) | b22_120-127, b22_34-37 |
| 4.2 N5 unlinked intent refuses | b22_65, b22_95 |
| 4.3 NO cohort read, computed; H1/H2 synthetic named; NULL twin; else-branch fails; the UNATTESTED token (RD-PLAN-READ) | b22_130-137, b22_180, b22_206 |
| 4.4 migration: one-edit proof, 58 cols, tuples, children, fk_check, partial UNIQUE, attempt_id, twice, D51 (two changed pairs + additions incl. the two unattested twins and the four lve belts, zero deletions, CHARC-S3.2), fourth value, `_is_head`, F1 hash; the attestation never blocks a fill deletion (brief §0 item 5) | b22_20-33, b22_29, b22_44-47 (R0.J), b22_48, b22_49, b22_50-77, b22_191 |
| 4.5 single-writer seam — EIGHT surfaces by execution; only `assign-intent` writes | b22_10-13, b22_114, b22_115 |
| 4.6 F2 S3 precondition walk | b22_150 |
| 4.7 display: own class/label | b22_140-142 (b22_143 RETIRED, RULING G3a (i)); witness step 4 (`trade analyze 20` + the operator's browser look at the trend page) |
| 4.7 card N quoted WITH tier N (until D61) | Task 13 witness step 5 (a quoting obligation, not UI — Task 9; R1-14, CONFIRMED by RD at R0.K-RULING.2) |
| 4.8 22-A2 cases green; 103 byte-unchanged, three re-scoped (R0.I) | b22_170 (Task 2A), A2-08, A2-09, Task 12 |
| 4.9 death-then-fill precondition, file:line | b22_151; ledger R0.C C.3 |
| F1 contract doc | b22_01-05, b22_29-30 |
| R0.G/R0.H + R0.K-RULING: the four telemetry belts (brief §2 item (4) discriminators) | b22_38-43, b22_197-199 |
| R0.K-RULING.1: leg 1's read set == its belt set (the class rule); the recorded-not-decided exclusion checked | b22_200, b22_201 |
| CHARC-S3 condition 2: the value unwritable without its attestation (raw UPDATE aborts / planted row passes / raw INSERT aborts / service write passes / reversed order aborts) | b22_202, b22_203, b22_204, b22_80, b22_205 |
| RULING G1b: ZERO SQL reads of a fill envelope (no exemption); the stored reading is the refuse-first; drift contained; the trigger twin alone; the third table in the census | 22-A sweep GREEN (0 offenders), b22_78, b22_108, b22_116, b22_193, b22_208-212, b22_44 (3) |
| RULING G1d: item 1 (a), a doubled root `entry_date` refuses under its own code `entry_date_ambiguous`; item 3, the NULL-id close on `trg_eia_trade_binding` (each ABORT case shown to pass on the G1b control) | b22_78, b22_213-216, b22_193 (unchanged), 22-A sweep GREEN (0 offenders) |
| CHARC-S3 condition 1: the attestation SQL in its own repo module | b22_115 (the INSERT site), Task 4 repo round-trip |

## Accepted limitations (declared; the reasons live in the ledger)

- **AL-1 (REWRITTEN, CHARC-S3.2)** The value is SCHEMA-enforced on UPDATE and INSERT: `trg_trades_entry_intent_unattested_update` / `_insert` abort any write of `unintended_execution` to `trades` without its `entry_intent_attestations` row, beside the eight service-enforced seam surfaces. Residual: a FABRICATED attestation row (a raw INSERT satisfying every admission trigger), outside the threat model per R0.K; plus AL-3's REPLACE facet as declared. Detectors that stay: `drift_report` (iii) as a check, and RD's UNATTESTED token on the per-cohort naming line (Task 8, b22_206) for what the schema cannot see.
- **AL-2** Death-before-fill is derived in Python (`mandate_alive_at`), not in SQL; the trigger binds the recorded shape (Task 11 `SERVICE_ONLY`).
- **AL-3** `INSERT OR REPLACE INTO trades` would bypass the UPDATE-scoped N4 trigger; no production writer issues it (R0.C C.2: no `REPLACE INTO trades`), and the attestation FK is `ON DELETE RESTRICT`. Service-prevented, incidence 0. The INSERT twin `trg_trades_entry_intent_unattested_insert` fires on REPLACE's INSERT half, so a REPLACE carrying `unintended_execution` aborts: it NARROWS AL-3's `entry_intent` facet and does NOT close AL-3 (a REPLACE carrying another value, or rewriting other columns of an attested row, is still reachable).
- **AL-5** Orphan labels: the clause-(4) exclusion applies to every cohort name including unregistered ones (fail-closed direction; `cohort_intent_authority` still answers `none` for them).
