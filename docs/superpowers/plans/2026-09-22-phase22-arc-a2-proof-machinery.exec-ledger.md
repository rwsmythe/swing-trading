# Phase 22 arc 22-A2 (PROOF MACHINERY) -- EXECUTING ledger

Plan: `2026-09-22-phase22-arc-a2-proof-machinery.md` (merged at `23313918`). Plan ledger: `...proof-machinery.ledger.md`. Branch `22-a2-exec`, base `05702929`.

## Gate table (depth read by the orchestrator from `scripts/cell_depth.py --live`; never a cell's self-estimate)

| gate | cell | after | last commit | depth | disposition |
|---|---|---|---|---|---|
| G1 | exec cell 1 (implementer-opus-high) | Task 3 (+ R4.0 edits, Tasks 1-2) | `010e8f9f` | 557,321 (OVER) | NOT resumed; fresh exec cell 2 dispatched off `010e8f9f` from Task 4. Cell 1's uncommitted Task-4 drafts (session scratchpad) are NOT carried; cell 2 writes Task 4 from the plan. |
| G-T4 | exec cell 2 (implementer-opus-high) | Task 4 | `cd02374b` | 208,150 | resumed. Orchestrator re-measured the P-correction: `9f315cc6:docs/rd-state.md` line 57 = 1015 bytes / **1005 chars** (plan section 4 + ledger R1.0 say 1006 -- a premise count error; sha256 + offsets unaffected; A2-37 asserts 1005). |
| G-T5 | exec cell 2 | Task 5 | `503e27b2` | 339,960 | NOT resumed (Task 5 cost ~130K; Task 6 would cross the cap mid-task). Fresh exec cell 3 from Task 6. |
| G-T6 | exec cell 3 (implementer-opus-high) | Task 6 | `aa2d44eb` | 267,300 | resumed for the F2.I-NEG follow-on ONLY (a Task-5 code change, red-first, RD's three discriminators), then gate G-NEG. Red-first evidenced for Task 6 (10/10 red for the expected reasons, then green). Encodings 1-5 carried to Task 12's record. |
| G-NEG | exec cell 3 | (nothing written) | `392b106f` | 314,529 | STOPPED on a fork, correctly: F2.I-NEG cannot be encoded without editing 0039's citation trigger. Probe on the truthful trade-25 payload: control ADMITTED; 3-segment (no match_only) REFUSED; `record_position` in `interval` REFUSED; at top level REFUSED -- `interval_segment_order` fixes `json_array_length = 4` with `segments[2] = 'match_only'` (orchestrator re-read the 0039 text), `interval_closed` admits only `endpoints`+`segments`, `blob_closed` fixes 28 keys. Routed to CHARC (shape). |
| G-U1 | exec cell 4 (implementer-opus-high) | (nothing written) | `758e1103` | 169,731 | STOPPED on a fork: F2.I-NEG's general zero-length rule + G-NEG's always-present `[1]='writer_absence_only'` together REFUSE record_at == fire_hi, which criterion 4 / F6(ii) / A2-57 admit (writer_absence_only = 0 s there). `pipeline_runs.finished_ts` is second-grain (`swing/pipeline/lease.py:38-39`, `timespec="seconds"`, orchestrator re-read; 152/152 live rows 19 chars per the cell), so equality is order-indeterminate. Live: 0 of 138 pre-barrier runs have fire_hi >= barrier. Options (a) strict `record_at > fire_hi` [cell's rec, ruler RD], (b) writer_absence_only optional in SQL [CHARC], (c) emit 0 s [not rec]. Routed to RD. |
| G-U1 (done) | exec cell 4 | unit 1: F2.I-NEG + G-NEG + G-U1, ONE commit | `d3bef313` | 270,532 | DONE, red-first (35 red, then green). tests/trades+data+cli `-n 8`: 5062 passed / 9 skipped / 0 failed; ruff clean. Fixture diff vs `05702929`: exactly four changed pairs, zero deletions (orchestrator re-counted 4/4). Trade 25 unchanged (A2-42/43). NOT resumed: the orchestrator seat rolls; Task 7 onward goes to a fresh cell. Cell 4's encodings 1-3 carried to Task 12's record. |
| G-T7 | exec cell 5 (implementer-opus-high) | Task 7 (A2-67..A2-76) | `91b3f194` | 280,030 | DONE, red-first (14 collected: 13 red for the expected reasons -- 12 `TypeError` on the new kwargs, A2-75's caller set empty; the one pass is the no-evidence counterfactual -- then 14 green). tests/trades+data+cli `-n 8`: 5076 passed / 9 skipped / 0 failed; ruff clean. Orchestrator re-ran the new service file + the CLI command file + the case closure: 140 passed. The CLI service-signature pin moved SIX -> EIGHT params (`frozen_value_evidence`, `evidence_repo`; docstring says why neither carries a cohort value) -- distinct from Task 8's CLI params pin 5 -> 6. NOT resumed (Task 8 would cross the cap); fresh cell 6 from Task 8. Cell 5's notes 1-9 carried to Task 12's record; note 4 (the preview under a caller-held transaction runs the git preflight inside it) and note 7 (`frozen_value_evidence` has no manifest of its own, so an edit to `_to_utc_naive` does not move `FROZEN_VALUE_EVIDENCE_VERSION`) routed to CHARC as non-blocking shape questions. |
| G-T8 | exec cell 6 (implementer-opus-high) | Task 8 (A2-77..A2-80) | `2f4b47f0` | 190,143 | DONE, red-first (`-n 0`: 7 red for the expected reason -- the option absent: A2-77 manifest 5 vs 6, five `No such option` exit-2s, A2-80 help -- then 132 green). A `click.Path(exists=True)` mutation turned exactly A2-70's CLI [missing] leg red, then reverted. tests/trades+data+cli `-n 8`: 5082 passed / 9 skipped / 0 failed; ruff clean. Orchestrator re-ran the CLI command file + the case closure: 132 passed. RESUMED for the G-T7 follow-on (CHARC's Q1 guard + Q2 digest pin, one red-first unit), then gate G-T7F. Cell 6's notes 1-7 carried to Task 12's record. |
| G-T7F | exec cell 6 | the G-T7 follow-on (CHARC Q1 + Q2), ONE commit | `ee157a28` | 288,604 | DONE, red-first (`-n 0`: 5 red -- Q1 `DID NOT RAISE` with a counting git wrapper, three Q2 `AttributeError`s on the absent digest functions, the hash-seed subprocess import error; the one pass is Q1's no-evidence twin -- then green). tests/trades+data+cli `-n 8`: 5088 passed / 9 skipped / 0 failed; ruff clean; A2-60 and A2-75 green unchanged. Orchestrator re-ran the new file: 6 passed. **Tip reworded by the orchestrator** (the cell committed `889e5e72`, whose body began `Q1:` and was parsed as a trailer; nothing cited it; message-only amend, tree `dfbdd5e7` byte-identical, trailers now empty -- `889e5e72` is unreachable and must never be cited). **Q2 deviation routed to CHARC:** the cell encoded the ruling's principle ("every function the blob is a function of") as the STATIC REFERENCE CLOSURE of the six named roots = 79 members (48 functions, 27 constants, 4 classes), not the literal six; plus note 2 (`read_artifact_facts` / `_parse_reflog_instant` produce verdict-bearing facts but are not in the closure) and note 3 (0039's trigger binds `$.evidence_version` to the literal `'2026-09-23.1'` -- orchestrator re-read `0039:1956` -- so once 0039 is live every version bump is a migration). NOT resumed; fresh cell 7 from Task 9. |
| G-T9 | exec cell 7 (implementer-opus-high) | Task 9 (A2-81..A2-90) | `9f175d2e` | 280,735 | DONE, red-first (`-n 0`: 18 of 18 red -- 16 `AttributeError` on the absent replay functions, A2-90 on the absent `ProvenanceCorrectionReport.replay`, the budget test after a test-side `dataclasses.replace` fix; A2-75 red on the new callers named first -- then green). One implementation regression fixed in-unit (a `deadline` kwarg into `_run_git` broke A2-69's wrapper; moved to a local wrapper, `_run_git` unchanged). tests/trades+data+cli `-n 8`: 5106 passed / 9 skipped / 0 failed; ruff clean. Digest pin unchanged (79 members; no member edited -- `read_artifact_facts`, `_run_git`, `VERDICT_BEARING_KEYS` verified outside the closure before editing). Orchestrator re-ran replay + service + follow-on + closure: 146 passed. The version-moved check was WITHHELD per the G-T7F/AMEND redirects (none written). Notes 1 (the drift reader calls `replay_verdict` directly, not via `tier2_cohort_exclusions` as R4.2 ruling 2 lists) and 6 (the memo is keyed `(id, resolved_sha)` but looked up by id; the SHA half recorded, not consulted) routed to CHARC. NOT resumed; fresh cell 8 for the G-T7F + G-T7F-AMEND encoding unit. |
| G-T7FE | exec cell 8 (implementer-opus-high) | G-T7F + G-T7F-AMEND encoding, ONE commit | `abcbd632` | 291,629 | DONE, red-first (`-n 0`, four files: 49 red for the expected reasons, 65 pass; then green), with ONE test written post-implementation (`..._a_moved_version_is_on_every_stale_reason_line`, pinning the cell's reading 3; checked by mutation instead). RD's discriminators mutation-checked both ways (version-only-stale and keys-only impls each go red), reverted byte-identical. tests/trades+data+cli `-n 8`: 5127 passed / 9 skipped / 0 failed; ruff clean. Fixture `05702929..HEAD`: numstat `4 4`, four changed pairs, zero deletions (orchestrator re-read); `schema_manifest.py --check` clean. Orchestrator re-ran follow-on + replay + conjunction + 0039 + service: 129 passed. Closure 95 members (54 fn / 34 const / 7 cls). First derivation version `2026-09-23.2`, digest `e098e9cd...1c1b` (`.1` not reused: it is the grammar literal AND the retired source-text pair's version). **Fact corrected against the ruling, by construction:** a MISSING `derivation_version` is refused by the new type clause (NULL `json_type` under COALESCE), NOT by `blob_closed` (`json_remove(...) = '{}'` refuses extras, passes absences) -- encoded as the code behaves. Routed: the grammar constant is now a closure member (CHARC); the moved version appended to EVERY stale reason, not only `_mismatch` (RD, doctrine #6); an ADMITTED row's observation never reaches the four readers through `tier2_cohort_exclusions`' non-ADMIT return (CHARC, blocks Task 10's shape). NOT resumed; fresh cell 9 for the G-T9 follow-on. |
| G-T9F | exec cell 9 (implementer-opus-high) | G-T9 item 2 (memo dropped; ref resolved once per invocation; A2-89 rewritten) + G-T7FE-B's four pins, ONE commit | `112202cb` | 234,565 | DONE, red-first (`-n 0`: 3 red for the expected reasons -- A2-89 and the drift reader read `(3,3)` vs `(1,1)`, the per-row-resolve failure; `resolve_remote_ref` absent -- then green; one test-side counting fix, the test's own `remote_tip()` counted). The six pins on existing behaviour passed first run (NOT faked red); each was proved by mutation M1-M5 (`_mismatch`-only suffix, always-append, append-on-unverifiable, resolve-under-caller-tx, module-level cache), each reverted `cmp` byte-identical. tests/trades+data+cli `-n 8`: 5135 passed / 9 skipped / 0 failed; ruff clean; A2-60/A2-75 unchanged. **Derivation digest MOVED as ruled:** `read_artifact_facts` gained a `resolution=` param (a non-member entry point would have duplicated verdict-bearing git logic outside the digest); pair `("2026-09-23.3", "f99090619b50...5193")` APPENDED, `.2` untouched; closure 95 -> 100; trade 25's stored-blob literal moved `.2` -> `.3` (numstat 1/1). Orchestrator re-ran replay + follow-on + closure + service + conjunction: 191 passed. Note 1 (`resolve_remote_ref` is NOT a digest member -- replay-only, writes no blob, but it chooses the ref the merge-base checks against) routed to CHARC. NOT resumed: the orchestrator seat rolls here. **NEXT: Task 10** with G-T7FE-A+C item C (`Tier2CohortRead`) folded in, to a fresh cell. |
| G-T10 | exec cell 10 (implementer-opus-high) | (nothing written) | `b6903a0a` | 167,176 | STOPPED on a fork, correctly (the brief's named stop): "the four P35 readers" do not map onto four once-per-invocation call sites. Orchestrator re-read on disk: `compute_hypothesis_progress_breakdown` calls `compute_tripwire_status` inside its per-hypothesis loop (`swing/journal/stats.py:416`), so both calling `tier2_cohort_exclusions` means 1+H replays per journal review; the breakdown is reached from WEB GETs (`swing/web/view_models/dashboard.py:239`, via `build_recommendation_progress` at `:559`/`:1204`; trade-entry via `hypothesis_prefill.py:59`) while the plan classes it a CLI reader (no budget; `GIT_TIMEOUT_SECONDS = 10.0` vs `WEB_REPLAY_BUDGET_SECONDS = 2.0`), and the dashboard displays N (`dashboard.py:468`) with no exclusion line; `swing hypothesis list` calls `compute_tripwire_status` once per hypothesis (`cli.py:4953`) and `swing hypothesis status` (`cli.py:4982`) is an unrostered surface; `compute_tier_comparison` is web-only (tier VM, deviation outcome `tier.py:778`, metrics index). Options: (a) thread an optional `cohort_read` + `budget_seconds` through the four [cell's rec, orchestrator concurs]; (b) hoist to ~10 surfaces; (c) literal four, unthreaded [not rec]. Digest check done read-only: 100 members, `.3` digest reproduced; `tier2_cohort_exclusions`, `ReplayVerdict`, `VERDICT_*` NOT members, `REASON_WEB_BUDGET_EXHAUSTED` IS -- no `.4` expected. Routed: item 1 (caller set, threading, per-surface budget, `hypothesis list`/`status`) PRIMARY CHARC; item 2 (must a web surface that DISPLAYS N render the named exclusion) PRIMARY RD. Cell 10 held for resume on the rulings. |
| G-T10 (done) | exec cell 10 (resumed on G-T10-1 + G-T10-2) | Task 10 (A2-91..A2-97c) + G-T7FE-A+C item C + G-T10-1/-2, ONE commit | `fc5df863` | 408,359 (OVER) | DONE, red-first (`-n 0`: 110 collected, 73 red for the expected reasons -- missing `tier2_excluded` fields, missing lines/markers, empty invocation counts, `TypeError` on `budget_seconds`, absent `Tier2CohortRead`, A2-75's empty caller set; the 11 pre-passing are zero-exclusion ABSENT cases, proved by mutation M3/M7) then green; mutations M1-M8 each red then restored byte-identical. tests/trades+data+cli+web+journal+metrics+recommendations `-n 8`: 8506 passed / 9 skipped / 0 failed; ruff clean; `schema_manifest.py --check` clean. **Orchestrator QA on disk:** trailers empty, tree clean; task set re-run (cohort readers + replay + correction service + closure/registry files): 248 passed; `tier2_cohort_exclusions(` has exactly FOUR call sites (`journal/stats.py:378`, `metrics/tier.py:677`, `recommendations/hypothesis.py:537`, `hypothesis_progress_card.py:474`); the derivation history/version and `tests/fixtures` have ZERO diff (digest `.3` unchanged, no `.4`). Post-code tests: the `/hyp-recs/refresh` rows (found by M6; checked by M6/M3/M4) and four A2-08 renames/splits with bodies unchanged. **Facts differing from the rulings, routed:** (F1) the metrics index is THREE invocations, not two -- the deviation-outcome card also reaches `compute_tier_comparison` (orchestrator re-read `metrics/index.py:254/263/287`); the declared <= 4 s bound does not hold as written -> CHARC. (F3) the metrics-index cards show no cohort N, so no marker (cell's reading) -> RD to confirm. (F4) `hypothesis status` prints the marker AND the named lines: CHARC's (4) says "the named line", RD's G-T10-2 says full names are not duplicated off the four named surfaces -> RD PRIMARY on render content, CHARC CC. (N1) cohort counts built WITHOUT the tier-2 read: `swing/metrics/process.py:590,598` and `trade_process_card.py:161` (`count_per_cohort`) -- untouched, a fifth caller if wired -> CHARC disposition. NOT resumed (OVER). Both directors rolled while this unit ran (CHARC `ca2adbed`, RD `a4ac72ee`); the routed items go to their successors. Cell 10's notes carried to Task 12's record. |

## G1 record (orchestrator QA of cell 1's gate report, against disk)

- Commits `84f63a63` (R4.0 edits), `37bd7929` (Task 1), `a045835b` (Task 2), `010e8f9f` (Task 3); trailers empty on all four.
- Fixture diff `05702929..010e8f9f` on `tests/data/schema_manifest_head.tsv`: exactly four changed line pairs (header 38->39; table `provenance_corrections`; triggers `..._append_only_update`, `..._citation_graph`), zero other deletions -- F12 condition (4) as ruled.
- Cell 1 reports: tests/data + tests/trades + tests/cli green after one fix (the 0036 HEAD nullable-column roster); the 22-A mutation matrix (`test_22a_task11_citation_evidence.py`) green against the HEAD 0039 trigger (the latch_ladder byte-unchanged-semantics evidence); ruff clean. Full fast suite NOT yet run (G3).
- Cell 1's plan-vs-implementation notes 1-5 (frozen_value_evidence.py created at Task 3 holding only the version constant, for #11 same-commit; TIER2-PREDICATE markers on the four rung-9 predicates too; A2-28 gained `ticker_AMN_in_text`; the P34 classification (i)/(ii) to be recorded at Task 12; the 0039 SQL composed by an UNCOMMITTED scratch generator -- the generated SQL is the reviewed artifact) are carried to cell 2 verbatim via its dispatch.


## G-T5 record (orchestrator QA)

- **TDD DEVIATION, recorded:** Task 5's tests (A2-42..A2-60) were written AFTER the implementation (no red-first run). Cell 2's substitute: ten one-line mutations of the module, each failing exactly its targeted discriminator (A2-50 SQLite half-up; A2-45 `<=`; A2-46 local-offset date; A2-48 committer date; A2-52 substring ticker; A2-55 no year rule; A2-56 year rule on ISO; A2-57 no fire bracket; A2-44 no ancestry; A2-49 no pivot check). Accepted as evidence of discrimination, NOT as TDD; cell 3 is instructed red-first. Reviewer B is told to weigh it.
- **Encodings for Task 12's record (cell 2's notes 1-4):** A2-60 walks name-reference reachability, not an AST call walk (`_BLOB_MIRRORS` counts); a failed blob mirror refuses as `tier2_unverifiable` naming the predicate id (an added refusal class; `blob_unbuildable` for an absent epoch row / unparseable endpoint); UTC rendering = naive-UTC `isoformat()` + `Z`; A2-42 compares builder vs literal on `VERDICT_BEARING_KEYS` + constants, segments/prose covered by A2-43 on live values.
- **OPEN, routed to RD (F2.I is his):** `match_only` = [record_at, barrier_armed_at) goes NEGATIVE when the record is authored after the barrier was armed (a long-latched pre-barrier mandate). The plan is silent; the code computes it literally. Trade 25 is unaffected (1,977,720 s). Options per cell 2: clamp to zero, a distinct kind, or refuse.
- **For Task 7:** the correction service's dependency manifest names `_to_utc_naive` (`cohort_provenance_correction.py:258/287`); `frozen_value_evidence` imports it at call time -- the manifest may need a line.

## RULING F2.I-NEG -- RD, LANDED 2026-09-23 (closes the G-T5 OPEN item)

**Author: RD. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, span verified present) from `comms/orchestrator/read/20260923T014035Z-rd-rd-ruling-f2-i-record-after-barrier-no-m.md` (posted 2026-09-23T01:40:35Z), the block quote. RD's closing NOTE (Task-5 deviation accepted as a substitute for THAT task, recorded; B weighs it) is not a ruling. **Encoding owner:** the executing cell, as a Task-5 follow-on commit with RD's three discriminators, red-first.

> **F2.I-NEG -- RULING: NONE of (a)/(b)/(c). Segments are defined by the ORDERED endpoints, and a segment of zero or negative length is NOT EMITTED. When `record_at >= barrier_armed_at`: `writer_absence_only` = [fire_hi, barrier_armed_at); NO `match_only` segment exists; `covered` = [barrier_armed_at, read_at]; `record_at` stays in the endpoints with a recorded `record_position = 'inside_coverage'` (else `'before_barrier'`). The verdict is unchanged (criteria 1-4 as ruled; gaps never decide). Negative seconds never appear anywhere; every emitted segment has `seconds > 0`.**
> Reason. The `match_only` span exists to name the period in which the ONLY thing standing between the frozen values and a silent change was a net-change check -- the operator's record with no barrier behind it. When the record is authored after the barrier armed, that period never existed: from `barrier_armed_at` the row is immutable by trigger, and the record merely confirms values already under coverage. So the evidence is not weaker but STRONGER (the uncovered window is shorter and purely writer-absence), which is why (c) REFUSE is wrong -- it would refuse the better evidence. (a) clamp emits a zero-length `match_only`, which ASSERTS that a match-only period existed for 0 s: a misstatement in the row's own voice, and the exact shape the "existence is not completeness" family warns about. (b) a new kind `match_after_barrier` names a span whose coverage is the barrier's, not the match's -- merging two evidence kinds under one label. None of the three states what actually happened; the ordered-endpoint rule does, and it is the rule the interval was built on (I-1: "segments" are what the endpoints induce, not a fixed roster of four).
> Consequences for the encoding, stated so they are not re-derived: (i) the SQL assertion "the four segment kinds are present in order" becomes "the emitted kinds appear in the canonical order `fire, writer_absence_only, match_only, covered` with `match_only` OPTIONAL, and `match_only` present IFF the blob's `record_at.utc < barrier_armed_at.utc`" -- if SQL cannot compare the `utc` fields (R8-03: it binds raw only) then the presence rule is a SERVICE check with a named `-- TIER2-PREDICATE` twin asserting only the ORDER, and the IFF is pinned in Python (A2-60's closure covers it); (ii) `uncovered_window_prose` names the case: "writer_absence_only <d> days (<fire_hi> to <barrier_armed_at>); covered from <barrier_armed_at>; record authored inside coverage at <record_at>"; (iii) `VERDICT_BEARING_KEYS` gains `record_position`; (iv) the writer_absence_only segment's END is `min(record_at, barrier_armed_at)` in both cases -- one formula, no branch.
> Discriminators (both on the trade-25 world with the barrier time moved, nothing else): record = barrier_armed_at + 1 s -> segments exactly `[fire, writer_absence_only, covered]`, `record_position = 'inside_coverage'`, prose contains "inside coverage", every `seconds > 0`; record = barrier_armed_at - 1 s -> `match_only` present with `seconds == 1`. Boundary twin: record == barrier_armed_at -> NO `match_only` (empty is not emitted), `inside_coverage`. A literal impl emits negative seconds on the first case and fails; a clamp impl emits `match_only 0` and fails; a refuse impl refuses the first case and fails.
> Preconditions: all exist today (F2.I I-1 endpoints; F6(ii) `record_at >= fire_hi`; the epoch row). Trade 25 is unaffected (22.89 d, `before_barrier`).

## RULING G-NEG -- CHARC, LANDED 2026-09-23 (closes the G-NEG fork)

**Author: CHARC. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, span verified present) from `comms/orchestrator/read/20260923T015516Z-charc-22-a2-g-neg-ruled-amend-0039-in-branch-a.md` (posted 2026-09-23T01:55:16Z), the block quote. **Encoding owner:** fresh exec cell 4, ONE commit `fix(data,trades): Tasks 3+5 follow-on -- F2.I-NEG` (RD's F2.I-NEG + this ruling), red-first, then Task 7 onward.

> **G-NEG -- RULING. (1) AMEND 0039's TEXT IN THE BRANCH, Option A. 0039 is unmerged and unapplied, so it is a draft and the edit is the migration's own text, not a new migration; F12's condition (1) is untouched (it proves the TABLE DDL's three edits; the trigger is not in that proof) and condition (4) still reads exactly four changed pairs (the citation-graph hash moves within the same pair; the fixture regenerates in the commit). `interval_segment_order` becomes: length 3 or 4; `[0] = 'fire'`, `[1] = 'writer_absence_only'`; length 4 -> `[2] = 'match_only'` AND `[3] IN ('covered','uncovered_barrier_absent')`; length 3 -> `[2] IN ('covered','uncovered_barrier_absent')`. The presence IFF (match_only iff `record_at.utc < barrier_armed_at.utc`) stays a SERVICE check with its own `-- TIER2-PREDICATE` twin asserting order only (RD's consequence (i); R8-03: SQL never reads a utc value). Option C is REFUSED for the reason the packet gives: it ships RD's rejected (c) by accident. (2) `record_position` LIVES INSIDE `interval`, branch (a): it is a property of the interval's own endpoints (where `record_at` sits relative to `barrier_armed_at`), computed from them; the top-level 28-key roster stays closed and A2-42 stays put; `interval_closed` widens by exactly one typed key, `record_position IN ('before_barrier','inside_coverage')`; `VERDICT_BEARING_KEYS` gains `interval.record_position` (RD's (iii)). AND ONE BELT THAT SQL CAN ASSERT WITHOUT A UTC READ OR A DURATION: `record_position = 'before_barrier'` IFF `json_array_length(segments) = 4`, and `= 'inside_coverage'` IFF length 3 -- the two representations of one fact must agree, in the trigger, as its own `-- TIER2-PREDICATE record_position_consistent` with a named service twin. Discriminator: RD's +1 s case with `record_position` forged to `'before_barrier'` -> ABORT; the -1 s case forged to `'inside_coverage'` -> ABORT (each: the belt removed -> ACCEPTED). (3) The commit form is yours and I concur: ONE commit carrying the 0039 text edit, both Python mirrors, `build_interval`, the key sets, the prose, the regenerated fixture and RD's three discriminators red-first; A2-60's closure and the 0039 tests re-run in it; the 0039 reversibility header needs no change (the three-edit list is unchanged).**

## RULING G-U1 -- RD, LANDED 2026-09-23 (closes the G-U1 fork)

**Author: RD. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, span verified present) from `comms/orchestrator/read/20260923T020008Z-rd-rd-ruling-g-u1-a-criterion-4-strict-equa.md` (posted 2026-09-23T02:00:08Z), the block quote. **Encoding:** folded into unit 1's single commit (criterion 4 strict; A2-57 at_hi flips to REFUSE; new twin fire_hi + 1 s ADMITS with writer_absence_only.seconds == 1; every fire-bracket comparison reads CLOSED).

> **G-U1 -- RULING: (a). Criterion 4 is STRICT: `record_at > fire_hi`. Equality REFUSES `window_indeterminate`. The fire bracket is CLOSED on both ends, `[fire_lo, fire_hi]`. F2.I-NEG's "zero-length is not emitted" stays literal and general; G-NEG's SQL order is unchanged. A2-57's `at_hi` case flips from ADMIT to REFUSE `window_indeterminate`; its other three assertions stand. New boundary twin: record = fire_hi + 1 s ADMITS with `writer_absence_only.seconds == 1`.**
> Reason. My F6(ii) equality-admits clause rested on an unstated premise -- that `finished_ts` is an exact instant, so a record AT it post-dates the persist. The measured fact (lease.py:38-39, `timespec="seconds"`; 152/152 live rows at 19 chars) falsifies the premise: at the stored grain, equality is ORDER-INDETERMINATE -- the record may sit at .2 s and the true finish at .9 s. F6(ii)'s own reason was "a record that cannot be proven to post-date the candidate's persistence refuses `window_indeterminate`"; at equality under truncation it cannot be proven, so the reason already decides the case and the clause follows it. This is a refusal of genuinely uncertain evidence, not of better evidence -- the asymmetry that ruled F2.I-NEG the other way. (b) would move CHARC's shape to absorb a case my own premise created; (c) contradicts the zero-length reason.
> Owned: this fork is the composition of two of my rulings (F6(ii) equality-admits and F2.I-NEG no-empty-segments), each sound alone, and I did not name their cross-product when I issued the second one hours after the first. That is the A4-R9-3 rule in my own §6, met again. The general form for the encoder: every fire-bracket comparison in this arc reads the bracket as CLOSED, and a record equal to EITHER bound is indeterminate -- `record_at < fire_lo` -> `window_negative`; `fire_lo <= record_at <= fire_hi` -> `window_indeterminate`; `record_at > fire_hi` -> computed. One rule, no exceptions, so the next equality case does not need a ruling.
> Preconditions: exist today (the second-grain `finished_ts` is a measured property of the live ledger; if a future arc stores sub-second finish instants the strictness is still correct, merely conservative by at most one second). Trade 25 unaffected (205,346 s).

## RULING G-T7 Q1 + Q2 -- CHARC, LANDED 2026-09-23 (closes the two G-T7 shape questions)

**Author: CHARC. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, span verified present) from `comms/orchestrator/read/20260923T024713Z-charc-22-a2-g-t7-ruled-q1-b-refuse-in-tx-previ.md` (posted 2026-09-23T02:47:13Z), the two block quotes. CHARC's closing NOTE (both land before Reviewer B opens; neither touches Task 8's files) is not a ruling. **Encoding owner:** the executing cell, red-first, after G-T8 and BEFORE Reviewer B: Q1's guard + its two discriminators and Q2's `(version, digest)` pin in one follow-on unit; Q2's `evidence_version_moved` replay reason lands with `replay_verdict` in Task 9.

> **G-T7 Q1 -- RULING: (b). The preview REFUSES a supplied `--frozen-value-evidence` when `conn.in_transaction` is true, with a typed message ("tier-2 evidence needs a preflight outside any transaction; call the preview on a connection that holds none"); the no-evidence preview is unchanged. Reason: S12.1 #9 is "no network call inside a transaction", and a git subprocess under a caller-held SAVEPOINT is exactly that; "the only production caller holds no transaction" is a claim about TODAY's callers -- the unenforceable-future-promise shape (gotcha #31), and (a) would ship it as an accepted limitation whose truth decays the day a second caller appears. One guard, one discriminator: a test that opens a transaction, calls the preview with evidence, and asserts the typed refusal with NO git subprocess started (monkeypatched git asserts zero calls); its twin, the same call with no evidence, still previews. The apply's existing caller-held rejection stays as is.**

> **G-T7 Q2 -- RULING: (b), the evidence-side dependency pin, in this arc, minimal form. `FROZEN_VALUE_EVIDENCE_VERSION` is BOUND to a digest of the source of every function the seventh-column blob is a function of -- `_to_utc_naive` (imported from the correction module), `build_interval`, `render_price`, `find_token`, `evaluate_conjunction` and the blob builder -- through the pattern `cohort_provenance_correction.py:300-376` already runs for `_derive` (`DERIVATION_RULE_HISTORY`: an append-only `(version, digest)` history, the current pair asserted by test). An edit to any member without a version bump then FAILS THE SUITE. This is 22A-R14-01's exact lesson (a behaviour change without a bump was invisible for six commits on 22-A), met on this arc's own version constant; it is the second instance of a pattern this codebase has already proven, not a design. Why (a) is insufficient: the replay-mismatch path is a DETECTOR and it is correct (fail-closed, excluded, named), but under a silent code change it names the wrong cause -- every tier-2 row flips to `<key>_mismatch`, which reads as "the evidence changed" when the CODE changed. With the pin, a moved version is a deliberate act, and replay reports it as its own reason: when a stored blob's `evidence_version` differs from the current constant, the verdict is `tier2_evidence_stale` with reason `evidence_version_moved (stored X, current Y)` -- still excluded (RD's F10: never admitted on the stored grade; doctrine #6 unchanged), but legible, and the reason names the recovery (re-run the correction's evidence under the current version, an operator act at a witnessed gate, never automatic). (c) is refused: a register row for a defect whose fix is one test and one reason string is deferral without a reason.**

## RULING G-T7F -- CHARC, LANDED 2026-09-23 (closes the G-T7F packet: the Q2 member set, the fact producers, the bump cost)

**Author: CHARC. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, the four-line block verified contiguous and present) from `comms/orchestrator/read/20260923T031212Z-charc-g-t7f-ruled-ast-closure-facts-root-gramm.md` (posted 2026-09-23T03:12:12Z). CHARC's Q1-text line, also verbatim: "Q1 text: the " Nothing was written." suffix STANDS -- the error class's own convention governs every refusal it emits; my sentence was the content, not the form. No change." **Encoding owner:** the executing cell, ONE red-first follow-on unit AFTER G-T9 and BEFORE Reviewer B. It carries the AST-based digest, `read_artifact_facts` as the seventh root, the new `FROZEN_VALUE_EVIDENCE_DERIVATION_VERSION` and its blob key `derivation_version`, the 0039 in-branch edit, the regenerated manifest (four pairs, zero deletions), and replay's `derivation_version_moved`. The last RENAMES and SUPERSEDES the G-T7 Q2 `evidence_version_moved` reason, which was withheld from Task 9 by orchestrator message to cell 7.

> **G-T7F -- RULING, three items, one design correction that resolves all three.**
> **ITEM 1: (a), the reference CLOSURE (79 members by the cell's walk, `read_artifact_facts` added per item 2), NOT the literal six -- the six leave the functions they call unpinned, the same hole one level down. BUT THE DIGEST HASHES BEHAVIOUR, NOT TEXT: each member is digested from its `ast.dump` with the docstring node removed (functions and classes) or its AST value (constants); comments never reach the AST. A docstring or comment edit inside any of the 79 therefore moves nothing -- D59's lesson (comments are not behaviour) applied to the one instrument that would otherwise force a migration over a comment. The walk's LookupError on an unplaceable name stays (fail loud on a member the walk cannot see).**
> **ITEM 2: (a). `read_artifact_facts` is the seventh root; its closure brings `_parse_reflog_instant`. Same argument as Q2 itself: a change to how `%aI` is parsed moves a verdict-bearing key and must move a version.**
> **ITEM 3: NONE of (i)/(ii)/(iii) as posed -- the packet conflates TWO quantities under one constant, and splitting them is the ruling. (A) `FROZEN_VALUE_EVIDENCE_VERSION` stays what the SQL trigger pins by literal (`= '2026-09-23.1'`, 0039:1956): it is the blob's GRAMMAR version -- the key roster, the types, the bindings -- and it moves ONLY when the roster or a binding changes, which is a trigger edit and therefore a migration BY NATURE, so the cost the packet fears is not a cost the pin adds. This keeps the structural SQL-side check I ruled at F12/G-NEG exactly as it is. (B) NEW: `FROZEN_VALUE_EVIDENCE_DERIVATION_VERSION`, the constant the 80-member AST digest is bound to (the `(version, digest)` append-only history + current-pair test from Q2), carried in the blob as a NEW key `derivation_version`; SQL checks `json_type = 'text'` ONLY (a derivation change is invisible to SQL by construction -- R8-03's family -- so SQL has nothing truthful to assert about its value). A derivation edit then bumps (B) with NO migration; the suite enforces the bump; replay compares the stored `derivation_version` to the current constant and reports `derivation_version_moved (stored X, current Y)` as the stale reason (this RENAMES the G-T7 Q2 reason `evidence_version_moved`; a moved GRAMMAR version cannot reach replay, because the trigger refuses the write). Encoding cost, bounded: one blob key -> the top-level roster 28 -> 29 (30 with `record_position` inside `interval` unchanged), `blob_closed`'s `json_remove` list +1, one `type = 'text'` predicate with its `-- TIER2-PREDICATE` marker and service twin, `VERDICT_BEARING_KEYS` unchanged (the derivation version is compared by its OWN rule, not as a mismatch key), the 0039 text edited IN-BRANCH under the G-NEG precedent (unapplied), the manifest re-regenerated (still four pairs, zero deletions), A2-42's literal blob gains the key. Discriminators: a raw insert with `derivation_version` absent -> ABORT (blob_closed); non-text -> ABORT; a stored row whose `derivation_version` != current -> replay `tier2_evidence_stale` / `derivation_version_moved`, excluded and named; a docstring edit to a closure member -> the digest test stays GREEN (the AST-hash discriminator, pinned by editing a fixture copy's docstring and re-digesting); a one-token body edit -> RED until the constant moves.**

## RULING G-T7F-AMEND -- CHARC, LANDED 2026-09-23 (SUPERSEDES G-T7F item 3's replay-verdict clause and G-T7 Q2's "names the recovery" sentence; RD's dissent accepted in full)

**Author: CHARC. Courier: the orchestrator. RD CC** (the dissent is his; CHARC conceded it in full). Transcribed BYTE-FOR-BYTE (script-extracted, span verified present) from `comms/orchestrator/read/20260923T031601Z-charc-supersedes-g-t7f-replay-clause-verdict-r.md` (posted 2026-09-23T03:16:01Z), the block quote. Everything else in G-T7F STANDS. **Encoding owner:** the SAME post-G-T9 follow-on unit as G-T7F, ONE commit: the verdict follows the recomputation, `derivation_version_moved` is an OBSERVATION beside it, RD's three discriminators, and the grammar-bump obligation written into 0039's reversibility header. D60 goes on the register (CHARC's).

> **G-T7F-AMEND (supersedes the replay-verdict clause of G-T7F item 3 and G-T7 Q2's "names the recovery" sentence). A moved DERIVATION version is CONTEXT DRIFT, never divergence (RD, doctrine #6). Replay ALWAYS re-derives the blob under the CURRENT code and compares `VERDICT_BEARING_KEYS`; the verdict follows the RECOMPUTATION, never the version: every key equal -> `ADMIT`, with `derivation_version_moved (stored X, current Y)` RETURNED AS AN OBSERVATION beside the verdict (rendered by the drift reader and the four readers' exclusion/observation line, never persisted); any key different -> `tier2_evidence_stale` naming BOTH the mismatched key AND the moved version as the cause. The pin keeps its whole value -- a code change can never masquerade as an evidence change, because the moved version is always on the reason line -- and the absorbing state is gone without a chain, a migration or an operator act. The admission is on a fresh recomputation under current code, not on the stored grade. Discriminators (RD's, adopted): derivation bumped + keys equal -> ADMIT with the observation rendered (a version-only-stale impl FAILS); derivation bumped + one key differs -> stale naming BOTH (a keys-only impl that omits the version FAILS); derivation unchanged -> the observation is absent. ENCODE IT IN THE SAME post-G-T9 follow-on unit as G-T7F -- one commit, not two. THE GRAMMAR-BUMP OBLIGATION (RD's second facet, CHARC's shape): a migration that bumps `FROZEN_VALUE_EVIDENCE_VERSION` (the grammar) must DECLARE how PRE-EXISTING rows replay -- a per-version recompute adapter, or a declared exclusion with its reason -- as part of that migration, because the composition read at the gate is CODE x THE LIVE ROWS, not only CODE x THE LIVE SCHEMA. Written into 0039's reversibility header by the executing cell in the same unit, and banked on the register as D60 by CHARC (this sitting).**

## RULING G-T9 -- CHARC, LANDED 2026-09-23 (closes the G-T9 packet; corrects R4.2 ruling 2 and R4.2's memo key by replacement)

**Author: CHARC. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, span verified present) from `comms/orchestrator/read/20260923T033114Z-charc-22-a2-g-t9-ruled-drift-reader-calls-repl.md` (posted 2026-09-23T03:31:14Z), the block quote. **Encoding owner:** the executing cell, as a small red-first Task-9 follow-on unit AFTER G-T7FE and BEFORE Task 10. Item 1 is already encoded as ruled (A2-75 at `9f175d2e`); item 2 drops the memo, resolves the ref ONCE per invocation and passes it into every row's `replay_verdict`, and rewrites A2-89 with the three discriminators. It lands after G-T7FE because cell 8 is editing `replay_verdict` now.

> **G-T9 -- RULING. ITEM 1: (a). The drift reader is a RENDERER of every tier-2 row, ADMIT rows included, so it calls `replay_verdict` directly; `tier2_cohort_exclusions` stays an EXCLUDER returning only non-ADMIT rows for the four cohort readers. R4.2 ruling 2 is corrected by replacement: the callers of `replay_verdict` are exactly {`tier2_cohort_exclusions`, `read_provenance_corrections`}, pinned both ways by A2-75; the callers of `tier2_cohort_exclusions` are exactly the four P35 readers (pinned at Task 10). The property that mattered -- ONE verdict function, no reader reads `admission_tier` as a verdict -- is preserved, because both entry points reach the same `replay_verdict`. (b) is refused: it changes a return contract four readers depend on to serve a fifth that needs something different. ITEM 2: (c), DROP the per-row memo -- a memo that can fire only on a duplicate select from a primary-keyed table is a claim about a cache that does not exist, and my R4.2 key `(provenance_correction_id, resolved_origin_main_sha)` described exactly that. What the memo was REACHING FOR is kept in its true shape: the remote ref is RESOLVED ONCE PER INVOCATION of `tier2_cohort_exclusions` / `read_provenance_corrections` (one `git rev-parse` + one reflog read), and that resolved SHA is passed into every row's `replay_verdict` -- a per-invocation resolution, not a cache; per-row git calls (`show`, `merge-base`, `rev-list`) are unavoidable because each row cites its own commit. A2-89 is rewritten: (i) one `replay_verdict` per tier-2 row per invocation; (ii) exactly ONE ref resolution per invocation regardless of row count (a per-row-resolve impl fails: N calls); (iii) a SECOND invocation re-resolves and re-runs git (a module-level cache fails: zero calls). F10-shape's "no cache across calls" is unchanged.**

## RULING G-T7FE-B -- RD, LANDED 2026-09-23 (closes G-T7FE item B; items A and C are CHARC's, open)

**Author: RD. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, the four-line block verified contiguous and present) from `comms/orchestrator/read/20260923T035840Z-rd-rd-ruling-g-t7fe-item-b-a-every-stale-re.md` (posted 2026-09-23T03:58:40Z). RD's closing NOTE (the missing-key correction is the NULL-`WHEN` gotcha applied correctly; the #11 comparator defends the roster; no action) is not a ruling. The post-implementation test at `abcbd632` is ACCEPTED for this pin on its mutation check, recorded as such. **Encoding owner:** the executing cell. The case already encoded at `abcbd632` stands; the four missing discriminators (criterion-3 + bumped, `author_instant_changed` + bumped, any stale + unchanged -> no token, `tier2_unverifiable` + bumped -> field without reason) are added red-first in the next unit that touches replay.

> **G-T7FE-B -- RULING: (a). When the derivation version has moved, EVERY `tier2_evidence_stale` reason carries `derivation_version_moved (stored X, current Y)` -- criterion refusals, `author_instant_changed`, and `<key>_mismatch` alike. `tier2_unverifiable` carries the observation FIELD but not in its reason. When the version has not moved, no reason mentions it.**
> Reason. The AMEND's purpose sentence is the rule: "a code change can never masquerade as an evidence change, because the moved version is always on the reason line." A criterion refusal under moved code is the case where that masquerade is MOST likely -- the current code refused evidence the old code admitted, and the likelier cause is the code, not the record. Restricting the suffix to `<key>_mismatch` (option b) would let exactly that case read as evidence decay on the one line the operator reads; the observation field alone is a second surface, and a cause that lives only on a second surface is the silent case one level down. `tier2_unverifiable` is different in kind: no verdict was reached, so the version is not a candidate CAUSE of anything -- it is context, and context goes in the field, not the reason.
> Discriminators (the cell's mutation check already covers the first; the others complete the set): criterion-3 refusal (five-cent pivot) + derivation bumped -> reason contains BOTH `pivot` and `derivation_version_moved` (a `_mismatch`-only suffix impl FAILS); `author_instant_changed` + bumped -> both; `<key>_mismatch` + bumped -> both (AMEND's); any stale + version UNCHANGED -> reason contains no `derivation_version` token (an always-append impl FAILS); `tier2_unverifiable` + bumped -> observation field set, reason WITHOUT the version. The test written after the implementation is accepted for THIS pin on the strength of its mutation check, recorded as such.
> Preconditions: exist (the observation field, the split constant, the AMEND's recomputation verdict, all at abcbd632).

## RULING G-T7FE-A+C -- CHARC, LANDED 2026-09-23 (closes G-T7FE items A and C; with G-T7FE-B the packet is closed)

**Author: CHARC. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, span verified present) from `comms/orchestrator/read/20260923T035845Z-charc-22-a2-g-t7fe-ruled-a-constant-stays-in-c.md` (posted 2026-09-23T03:58:45Z), the block quote. CHARC ACCEPTS the blob_closed fact correction for the record. **Encoding owner:** item A is already as ruled at `abcbd632` (no change). Item C is encoded IN TASK 10: `tier2_cohort_exclusions` returns the named frozen dataclass `Tier2CohortRead(exclusions, observations)`, each of the four readers counts from `exclusions` only and renders both, and the two discriminators are added to A2-91..A2-97 per reader.

> **G-T7FE -- RULING. ITEM A: (a), the grammar constant STAYS in the digest closure. Under the split it is a value the builder EMITS, so the derivation digest is a true function of what the builder writes only if it includes it; a grammar bump is already a migration plus the D60 declaration, and the extra derivation bump it forces is free and honest (the blob's bytes changed). ITEM C: (b), the contract is EXTENDED, never changed: `tier2_cohort_exclusions` returns a NAMED result -- a frozen dataclass `Tier2CohortRead(exclusions: dict[int, ReplayVerdict], observations: dict[int, ReplayVerdict])` (a named type, never a bare positional tuple, which rots at the second field) -- where `exclusions` is exactly the non-ADMIT set G-T9 kept and `observations` is every ADMITTED row whose verdict carries a non-empty observation (today: `derivation_version_moved`). Each of the four readers COUNTS from `exclusions` only and RENDERS both dicts on its named line; the count logic never reads `observations`. (a) is refused (it reopens the excluder contract G-T9 kept for the four readers); (c) is refused because an admitted-under-moved-code row would read as unremarkable on exactly the surfaces the operator reads. Discriminators, added to A2-91..A2-97 for each reader: an ADMITTED row with a moved derivation version -> N UNCHANGED and the observation line rendered (an impl that excludes on observation fails on N; an impl that ignores observations fails on the render); the zero-data state renders neither line. The drift reader is unchanged (it calls `replay_verdict` per row and renders everything).**

## RULING G-T9F -- CHARC, LANDED 2026-09-23 (closes G-T9F note 1; no encoding owed)

**Author: CHARC. Courier: the orchestrator.** Transcribed BYTE-FOR-BYTE (script-extracted, span verified present) from `comms/orchestrator/read/20260923T042145Z-charc-22-a2-g-t9f-ruled-resolve-remote-ref-sta.md` (posted 2026-09-23T04:21:45Z), the block quote. **Encoding:** none. `resolve_remote_ref` stays outside the digest as the code already stands at `112202cb`; its behaviour is pinned by A2-84, the rewritten A2-89 and the G-T7FE-B unverifiable pin.

> **G-T9F -- RULING: (a). resolve_remote_ref stays OUTSIDE the derivation digest. The digest pins what a STORED BLOB is a function of, so a stored row can be compared honestly against a recomputation; resolve_remote_ref writes no blob byte -- it composes two stages that ARE members (_ref_stage, _reflog_stage) and maps their failures, on the replay path only, and every replay verdict is recomputed fresh at read and never persisted, so there is nothing a stored row can drift against. Pinning it would force a version bump for replay plumbing changes, which is the wrong instrument: replay-only behaviour is pinned by TESTS, not by the digest -- A2-84 (timeout / missing ref -> tier2_unverifiable, the failure mapping), A2-89 as rewritten (one resolution per invocation, a second invocation re-resolves), and the unverifiable-carries-the-observation-field pin from G-T7FE-B. Its ONE verdict-bearing decision -- WHICH ref the merge-base checks run against -- is REMOTE_REF, a constant that is already a closure member, so the choice of ref is pinned; only the plumbing that reads it is not. The cell's choice to edit the member rather than add a non-member entry point was right for exactly the reason it gave (a non-member copy of show/merge-base/rev-list would unpin verdict-bearing facts), and the version moving .2 -> .3 with .2 appended is the mechanism working as intended.**

## RULING G-T10-2 -- RD, LANDED 2026-09-23 (closes G-T10 item 2; item 1 is CHARC's, open)

**Author: RD. Courier: the orchestrator. CHARC CC** (RD states item 1(a) is consistent with this ruling). Transcribed BYTE-FOR-BYTE (script-extracted, the five-line block verified contiguous and present) from `comms/orchestrator/read/20260923T043131Z-rd-rd-ruling-g-t10-item-2-a-rendered-cohort.md` (posted 2026-09-23T04:31:31Z). **Encoding owner:** the executing cell, IN TASK 10: every surface that renders a cohort N from an excluding read carries the compact marker (count + canonical surface; `unverifiable` never merged with `excluded`), with RD's three discriminators per rendering surface; whether `swing hypothesis status` is in this arc's scope is CHARC's item 1.

> **G-T10-2 -- RULING: the naming obligation attaches to the COUNT, not to the surface. ANY surface that RENDERS a cohort N derived from an excluding read renders, beside that N, at minimum a compact exclusion marker with the count and the canonical surface to open -- e.g. `2/20 (1 excluded: see hypothesis list)` -- empty in the zero-exclusion state. The FULL names and reasons live on the four named surfaces (hypothesis list, journal review, the card, the tier page) and are not duplicated. A surface that CONSUMES the read but renders NO count (the trade-entry prefill, if it only uses N for logic) renders nothing. The test at each site is "does this surface show the operator a number?" -- yes -> marker; no -> nothing.**
> Reason. F10's sentence is "excluded and NAMED in the read's output, never silently uncounted." The dashboard is where the operator looks; it shows `N` today at dashboard.py:468. Under item 1(a) that N is CORRECT (the read excluded the row), but a surface that can say `2/20` and structurally cannot say `one is excluded` is the asymmetric surface -- the zero-data-reads-clean family in its purest form: the operator reads 2/20 as the whole truth while the reader knows 3 with one excluded, and the only signal is on a page he did not open. The read named it; the surface dropped it. Naming that lives only on a page the operator must think to visit is the silent case one level down (the same reasoning as G-T7FE-B, one surface up). The marker is the minimum that makes the shown number honest; the full line stays where it belongs. The budget-exhausted case is the same: `(1 unverifiable)` is the honest dashboard state under a web timeout, never a bare N.
> Scope reading, stated so CHARC can object in his item: this is NOT a new UI surface. It is R0.12 note 8's own reasoning ("F10's never-silently-uncounted reaching existing readers") reaching an existing COUNT RENDER on an existing page; the VM already carries `tier2_excluded`; the render is one conditional span, no HTMX, TestClient-pinnable. `swing hypothesis status`, if it prints N, is in the same class by this test -- whether it is in THIS arc's scope is CHARC's item 1 call; the semantic does not change.
> Discriminators per rendering surface: one excluded row -> the N renders with the marker carrying the count `1` and the canonical surface's name; zero excluded -> the marker is ABSENT (an always-render impl FAILS); one `tier2_unverifiable` -> the marker says `unverifiable`, not `excluded` (a merged-categories impl FAILS -- evidence kinds are never merged). Web pins via `with TestClient(app) as client:`.
> Preconditions: exist (Tier2CohortRead at Task 10; the VM field; item 1(a)'s single call per breakdown, which is what makes the marker cost one read, not 1+H).

## RULING G-T10-1 -- CHARC, LANDED 2026-09-23 (closes G-T10 item 1; with G-T10-2 the fork is closed)

**Author: CHARC. Courier: the orchestrator. RD CC.** Transcribed BYTE-FOR-BYTE (script-extracted, the one-line block verified present; the literal `≤` escapes are CHARC's text as posted, not the transcriber's) from `comms/orchestrator/read/20260923T043138Z-charc-22-a2-g-t10-item-1-ruled-a-threaded-read.md` (posted 2026-09-23T04:31:38Z). **Encoding owner:** exec cell 10, resumed, IN TASK 10 together with G-T10-2: the threaded `cohort_read` + `budget_seconds`, the per-route invocation-count pin, `hypothesis list` line once per row, `hypothesis status` rendered (and its N carrying G-T10-2's marker).

> **G-T10 ITEM 1 -- RULING: (a), with four refinements. (1) CALLERS: `tier2_cohort_exclusions` is called by EXACTLY the four P35 readers (A2-75 exact, G-T9 kept). `compute_tripwire_status` gains `cohort_read: Tier2CohortRead | None = None` and calls only when None; `compute_hypothesis_progress_breakdown` -- itself one of the four -- calls ONCE per invocation and threads the read into every per-hypothesis `compute_tripwire_status`, so one journal review is ONE replay pass, never 1+H. (b) is refused (ten callers contradicts the pinned four); (c) is refused (1+H replays and unbudgeted web GETs -- the hold A2-97b exists to prevent). (2) BUDGET THREADING: `budget_seconds: float | None = None` is threaded through the breakdown, `build_recommendation_progress`, `compute_tier_comparison` and the card VM; the WEB callers -- the dashboard VM, the trade-entry prefill, the tier VM, the card route -- pass `WEB_REPLAY_BUDGET_SECONDS` (imported from `swing/trades/frozen_value_evidence`, so `swing/metrics` and `swing/journal` never import a web constant); CLI callers pass None. The budget is PER INVOCATION of `tier2_cohort_exclusions`; a page that composes N readers spends at most N budgets -- DECLARED with the numbers: the dashboard GET = 1 invocation (≤ 2 s + one call timeout), the metrics index = 2 (card + tier), so ≤ 4 s worst case at one tier-2 row; pinned by a test counting invocations per route in the zero-data and one-row states. (3) `swing hypothesis list`: H invocations (one ref resolution each) are accepted -- hoisting would mint a fifth caller for a CLI path with no budget to protect; the exclusion/observation line renders ONCE PER ROW, because an exclusion is a fact about THAT cohort's N (trade 25 appears under H1 only). (4) `swing hypothesis status` is IN SCOPE for the RENDER: it calls the reader directly, so the named line costs one print and one test; a shown N that silently excludes a row on a surface that already holds the exclusion is the silent case, and a CLI command is a surface, not a fifth caller. No digest movement is expected under (a) and none is accepted without a member change.**

## RULING G-T10-F3+F4 -- RD, LANDED 2026-09-23 (closes G-T10 items F3 and F4; F1 and N1 are CHARC's, open)

**Author: RD (successor seat `swing-rd-20260922-1915`). Courier: the orchestrator. CHARC CC.** The two ruling paragraphs are transcribed BYTE-FOR-BYTE (script-extracted, each verified present) from `comms/orchestrator/read/20260923T051921Z-rd-rd-rules-g-t10-f3-confirmed-f4-status-fu.md` (posted 2026-09-23T05:19:21Z); RD posted them as plain paragraphs, so the `> ` prefix is the transcriber's and is the ONLY added byte. RD's framing lines above and below them are not a ruling. **Encoding owner:** a small red-first follow-on unit AFTER Task 11 (F4 only: drop the `tier2_count_marker` call and marker append in `hypothesis status`, keep the named lines, the status test asserts lines present and marker ABSENT). F3 needs no code (the index cards carry no marker as shipped at `fc5df863`).

> ITEM F3 -- RULING: CONFIRMED, with one sharpening of my own G-T10-2 text. The obligation attaches to a COHORT MEMBERSHIP COUNT -- the N the operator reads as "how many rows are in this cohort" -- not to every statistic derived from an excluding read. The metrics-index cards render (index.py:244 expectancy R (all); :259 a count of REGISTERED COHORTS, i.e. hypotheses, not members; :269 A+ expectancy R; :302 a delta %). None is a membership count, so none carries a marker. Each card is a LINK (index.html.j2:11) whose one job is to open the surface that does carry it: tier comparison renders its N with the marker (tier.py:872), deviation outcome renders it (deviation_outcome.html.j2:47), the hyp-recs rows render it (hypothesis_recommendations_row.html.j2:32). Sharpening: G-T10-2's test sentence read "does this surface show the operator a number?" -- read it as "a cohort COUNT." An expectancy on a linking card is one click from its marker; putting the marker on every derived statistic would spread it to every metric on every page, which is the duplication the same ruling forbids. Discriminator unchanged at each marker site (one excluded -> marker with count 1; zero -> absent; one unverifiable -> says unverifiable). Precondition: exists.
>
> ITEM F4 -- RULING: `hypothesis status` renders the FULL NAMED LINES and NO MARKER. It is a per-cohort DETAIL surface (one hypothesis, cli.py:4975), so it joins the named surfaces as the fifth (hypothesis list, journal review, the card, the tier page, hypothesis status) -- CHARC's G-T10-1 (4) stands. What ships today (cli.py:5004-5008) prints a marker reading "see hypothesis list" one line ABOVE the very lines it points to: a pointer to another surface on the surface that already holds the content is a wrong pointer, and the full lines exceed the marker's "at minimum." G-T10-2's "not duplicated" clause governs COUNT-ONLY surfaces (the dashboard rows, deviation outcome, tier comparison N) and is untouched. Encoding: drop the `tier2_count_marker` call at cli.py:5004 and the marker append at :5005-5006; keep the `tier2_cohort_lines` loop at :5007-5008; adjust the status test to assert the named lines present and the marker string ABSENT (an impl that keeps both FAILS; zero exclusions -> neither, unchanged). No reader, digest, or budget change; one CLI print path.

## RULING G-T10-F1+N1 -- CHARC, LANDED 2026-09-23 (closes G-T10 item F1; N1 BANKED with one question serialized to RD)

**Author: CHARC (successor seat `swing-charc-20260922-1913`). Courier: the orchestrator. RD CC** (RD holds the N1 question, which CHARC posted to RD's inbox directly, `20260923T051959Z`). The two paragraphs are transcribed BYTE-FOR-BYTE (script-extracted, each verified present) from `comms/orchestrator/read/20260923T051959Z-charc-charc-rules-g-t10-f1-three-confirmed-bou.md` (posted 2026-09-23T05:19:59Z); posted as plain paragraphs, so the `> ` prefix is the transcriber's and is the ONLY added byte. **F1 supersedes by replacement G-T10-1 (2)'s "metrics index = 2 ... <= 4 s" sentence.** **Encoding:** F1 none (the cell already pinned 3 at `fc5df863`); N1 none on 22-A2 (post-merge follow-on if RD rules YES; one-line comments at the three sites if NO -- both outside this arc's tasks).

> ITEM F1 -- RULING: THREE, CONFIRMED; the declared bound is AMENDED BY REPLACEMENT. The G-T10-1 (2) sentence "the metrics index = 2 (card + tier), so <= 4 s worst case at one tier-2 row" is superseded by: "the metrics index = 3 invocations (the progress card via hypothesis_progress_card.py:474; the tier card via tier_comparison.py:100 -> tier.py:677; the deviation card via deviation_outcome.py:98 -> compute_deviation_outcome -> compute_tier_comparison -> tier.py:677), so <= 6 s plus three call timeouts worst case at one tier-2 row; pinned by the per-route test counting 3 in the zero-data and one-row states." My count of 2 was a paraphrase of the card list; the code held three, and the cell was right to pin what the code does. NO shared-read threading on this arc: sharing one read across the index's three cards needs compute_tier_comparison to accept a cohort_read (the compute_tripwire_status shape) and a threading path through two VMs, for a page whose worst case moves from 4 s to 6 s at ONE tier-2 row, and the digest is unchanged at .3 -- code on an executing arc past its last cheap moment for a bound nobody has measured as exceeded. BANKED with a named trigger: if the metrics index GET is MEASURED over its budget in the operator's use (a number, not a forecast), the fix shape is cohort_read on compute_tier_comparison threaded from the index extractor, one invocation for the page. Within-one-invocation sharing is not the F10-shape cross-call cache; the breakdown already does it.
>
> ITEM N1 -- DISPOSITION: BANKED, not commissioned on 22-A2, not declined; ONE question serialized to RD. The three sites (process.py:590 and :598 list_closed_trades_for_cohort, the panel cohort and the intent-filtered slice; trade_process_card.py:161 count_per_cohort) compute a per-hypothesis N and an expectancy over closed trades -- the same class of read as compute_tier_comparison, which the census DID roster. They are untouched on 22-A2 and STAY untouched: the pinned four (A2-75) is this arc's census, and widening it mid-executing reopens A2-75, the digest pin and the caller-pin tests on an arc at Task 11 with a 09-30 witness. What decides the follow-on is not mine: whether the trade-process card's n_closed (per tab and the All aggregate) is a cohort N in F10's sense, such that trade 25 counted on the process card while excluded on the tier page is two surfaces disagreeing on the A+ N -- the exact consistency compute_deviation_outcome's own docstring claims between tier and deviation. RD rules that ONE question (PRIMARY: RD; posted in this message so the orchestrator does nothing on it). If RD rules YES: a post-merge follow-on unit, not a 22-A2 task -- list_closed_trades_for_cohort gains cohort_read: Tier2CohortRead | None = None and count_per_cohort the same; the card reads ONCE (web budget) and threads it across every tab and the All call, so the caller pin widens to FIVE by a ruled amendment, and G-T10-2's marker lands beside every tab N; the CLI path passes None. If RD rules NO: DECLINED with RD's ground recorded, and the three sites carry a one-line comment saying so, so the next reader does not re-open it from a grep. Either way the register row is mine and is written at the operator's next sitting (I am bootstrapped and holding file edits for the operator's direction); this message is the record until then.
