# Data-Integrity Arc -- Executing-Plans Dispatch Brief

**Audience:** Fresh Claude Code instance dispatched as the data-integrity-arc executing-plans implementer. No prior conversation context.

**Mission:** Execute the LOCKed, Codex-converged implementation plan task-by-task (TDD) into a SHIPPED, merge-ready arc -- the regular-session + completed-day data-integrity arc.

**Plan (AUTHORITATIVE -- execute verbatim):** `docs/superpowers/plans/2026-06-05-data-integrity-regular-session-completed-day-plan.md` (1957 lines, 20 TDD tasks: Slice A=A1-A5 / C=C1-C8 / B=B1-B3 / D=D1-D4; Codex-converged R1[1crit+4maj]->R6 `NO_NEW_CRITICAL_MAJOR`). Each task is a red->green->commit unit with exact file:line, real code, and binding implementer-notes. **Re-grep every cited file:line at STEP 0** (discipline #2) -- the plan was grounded on HEAD `38e3bc37`; you branch from `37c06cad` (the plan-merge HEAD; swing/ code is identical -- only the plan doc + a research CSV changed since).

**Spec (reference only -- the plan supersedes it for implementation):** `docs/superpowers/specs/2026-06-05-data-integrity-regular-session-completed-day-design.md`.

**Brief lineage:** writing-plans dispatch `docs/data-integrity-arc-writing-plans-dispatch-brief.md`; brainstorm dispatch `docs/data-integrity-arc-brainstorming-dispatch-brief.md`.

**Context:** main HEAD at this dispatch = **`37c06cad`** (branch from it). ~7130 fast tests green; **schema v24 (this arc adds NO schema -- plan §"Schema verdict": NONE)**. The operator's PARALLEL Minervini research is PAUSED until Phase 15 completes, but the operator MAY still land research commits on main (disjoint `research/`/`reference/` + a `.gitignore` mod) -- do NOT touch/stash/commit it; if main moves under you the orchestrator rebases at merge.

**Cumulative discipline (BINDING):** the **session-anchor read/write mismatch** family (Issue #5 / Slice D is the instance -- read the WRITER's anchor before locking a read predicate; `>=` forward, strict `>` backward); the **yfinance partial-bar strip** + **F6 write-through-archive empty-result** rule (Slice C's `write_window` keeps F6/M3 ON TOP of the atomic barrier -- an empty incoming must NOT blank valid history); the **Schwab camelCase signature-pin** (A1 re-validates `needExtendedHoursData` on schwabdev 3.0.5 via `inspect.signature`); the **append-only / lock-at-observation L3** invariant (the guard ADDS rejection, never re-fetch/regeneration); **Windows cp1252 -- ASCII only** in any `print`/`click.echo` (the C8 CLI command); `feedback_verify_regression_test_arithmetic` (the plan already computes each value under BOTH paths per task -- preserve that when a test needs adjustment). **TDD** (write failing test -> SEE it fail -> minimal impl -> SEE it pass -> commit, per task). **ZERO `Co-Authored-By`; no `--no-verify`; final `-m` paragraph plain prose; verify `git log -1 --format='%(trailers)'` is `[]`.**

**Skill posture:** `copowers:executing-plans` (wraps `superpowers:subagent-driven-development`). After ALL 20 tasks complete, run the **SINGLE Codex chain to convergence** (`NO_NEW_CRITICAL_MAJOR`; ~5-round cap suspended -- `feedback_codex_round_limit_suspended`). **Codex transport (MCP DEAD):** `wsl.exe bash -ilc 'export PATH="$HOME/.local/node22/bin:$PATH"; codex exec ...'` (PATH prefix REQUIRED; `codex --version` -> codex-cli 0.135.0 is the liveness probe; feed the prompt via STDIN `cat prompt.txt | codex exec -s read-only --skip-git-repo-check -`; pre-generate the diff on Windows -- the worktree `.git` is unreachable from WSL; tell Codex NOT to run git). **Persist BOTH the prompts AND the responses** (each round's verdict/findings incl. the final `NO_NEW_CRITICAL_MAJOR` line) to `.copowers-findings.md` so the orchestrator can independently confirm convergence at QA (`feedback_implementer_persist_codex_responses`).

---

## §1 Slice execution order (plan §Architecture -- dependency order A -> C -> B -> D)
- **Slice A (A1-A5)** -- ext-hours pull fix. Independently shippable; directly kills the ~16% `OhlcvBar invariant violated` rate. `needExtendedHoursData=False` + `needPreviousClose=False` on `price_history` (marketdata.py:428) + the A1 signature-pin + the typed `SchwabBarConsistencyError` (subclass `SchwabApiError`, custom `__init__`) + the mapper float-normalization (round o/h/l/c to `_OHLC_ROUND_DP=4` BEFORE `OhlcvBar`; the model stays strict -- NO global epsilon).
- **Slice C (C1-C8)** -- THE KEYSTONE (depends on A). The completed-day strip lives in **`_write_archive_atomic`** (the TRUE single chokepoint all 4 Shape-A write paths funnel through -- this is the R1 CRITICAL fix; a `write_window`-only strip is UNSOUND because `_backward_compat_rename` runs on every `resolve_ohlcv_window` read). `write_window` keeps F6/M3 merge ON TOP. The **date-only** lock-guards at `_bar_for_date` + `build_ohlc_today_json` (C5 is the guard-boundary proof: the date-only guard CANNOT validate ext-hours -- that's why A exists). C7/C8 remediation: the auto-overwrite covers only the Schwab-SUCCESS observe run, so **C8 `swing schwab purge-marketdata-archive` is a REQUIRED gate step, not an optional belt** (OQ-1b).
- **Slice B (B1-B3)** -- quotes regular-session. Implement ALL of B with SYNTHETIC-payload unit tests. **B1's live cassette is an OPERATOR gate (B1 Step 3): you do NOT record it against the live env** -- the slow `test_quote_fields_live.py` only asserts the cassette EXISTS; the operator records it during the gate. B2 is L1-correct either way (drops to yfinance when regular fields absent -- safe to ship before the cassette confirms field availability).
- **Slice D (D1-D4)** -- uniform topbar (parallelizable). `PageKind` + `topbar_session_date` helper; route every base-layout VM's `session_date`; the D3 explicit-MANIFEST (30 VMs) + AST-completeness + the non-vacuous representative registry + the `_FrozenNow`/`_freeze_web_clock` behavioral proof; D4 the AST provenance DENYLIST lint. **D3 `_REPRESENTATIVES` MUST be wired to real VM builders** (the non-vacuous guard FAILS until >=6 covering both PageKinds + >=1 metrics + >=1 non-metrics are wired -- it cannot ship vacuous).

## §2 Pre-merge gate YOU deliver (in the worktree)
1. **Full fast suite green** (`python -m pytest -m "not slow" -q`). Isolate the 3 known xdist co-residency flakes (`test_ohlcv_reader_re_export_identity`, `test_read_cohort_csv_against_committed_v2trf`, `test_prices_refresh_uses_pipeline_eval_anchor`) -- confirm each passes `-n0` if it co-resident-fails. Report the count + any isolation (`feedback_no_false_green_claim` -- do NOT carry a stale count forward).
2. **`ruff check swing/`** clean.
3. NO dependency change in this arc (contrast the schwabdev arc) -- a standard `pip install -e ".[dev,web]"` in the worktree is fine; **no isolated venv needed**.
4. Codex convergence (`.copowers-findings.md`).

## §3 Operator gates (POST-merge; you do NOT run these -- enumerate them as PENDING in the return)
1. fast suite green on the MERGED HEAD (orchestrator re-runs/isolates). 2. **operator browser gate** -- topbar consistency across pages, LIGHT + DARK (the D3 cross-VM test backs it). 3. **operator-witnessed LIVE Schwab re-fetch** (post-merge, like the schwabdev GATE-B): confirm the ~16% `OhlcvBar invariant violated` rate collapses toward ~0% AND witness the UNSEEDED normal pipeline run (`feedback_seeded_gate_masks_default_state`). 4. the **REQUIRED one-time `swing schwab purge-marketdata-archive`** + **Slice B's live-quote cassette** recording (operator confirms `regularMarket*` appear). **You do NOT touch the operator's live DB** -- run all tests against `tmp_path` / `ensure_schema(...)` DBs.

## §4 OUT OF SCOPE (plan + spec §15)
Issue #3's FIX (the `_count_open_at_run` predicate -- banked, its own brief, OQ-5); Issue #2/#4 (the polish batch); a date-system rewrite (L4 -- harden, don't rebuild); Schwab Phase B/C; historical re-locking of already-locked ext-hours observations (OQ-7, L6-accept); any schema change (v24 holds).

## §5 Dispatch metadata
- **Subagent:** `general-purpose`, foreground, harness-default model. **Worktree:** branch `data-integrity-arc-executing-plans` from main HEAD **`37c06cad`**. `python -m swing.cli`; prefix shell with the worktree dir; re-check `git branch --show-current` before EACH commit (degraded-harness discipline -- if tool calls start cancelling mid-batch, drop to single sequential calls + re-Read before each Edit + verify each commit). **You do NOT merge** (orchestrator action at convergence + gate-pass). **Leave the worktree INTACT** at return (do NOT tear down -- it holds `.copowers-findings.md` for the orchestrator's independent convergence check; the orchestrator tears it down post-merge).
- **SINGLE Codex chain** to convergence after all 20 tasks.

## §6 Return report (mirror prior executing returns)
Final HEAD + commit list (grouped by slice/task); the **fast-suite result on the worktree** (exact count + any `-n0` isolation of the known flakes); `ruff` clean; the Codex convergent verdict (cite `.copowers-findings.md` -- rounds + the verbatim `NO_NEW_CRITICAL_MAJOR`); confirm the slice order shipped (A->C->B->D) + D3 `_REPRESENTATIVES` wired (>=6, both kinds); the schema verdict (NONE -- v24); the 4 OPERATOR gates enumerated as PENDING (incl. what each confirms -- esp. the live re-fetch error-rate collapse + the required purge + the B1 cassette); per-axis test-arithmetic preserved; ZERO `Co-Authored-By` (`%(trailers)`==`[]`); worktree INTACT; merge-readiness.

---

*End of brief. Data-integrity executing-plans dispatch -- drive the converged 1957-line / 20-task plan to a shipped arc (A ext-hours pull -> C write-barrier[`_write_archive_atomic` chokepoint]+date-only lock-guard+remediation[required purge] -> B quotes[synthetic tests; live cassette is an operator gate] -> D uniform topbar[wire `_REPRESENTATIVES`]). NO schema (v24). The binding post-merge gate is the operator-witnessed live Schwab re-fetch confirming the ~16% `OhlcvBar` error rate collapses. Deliver: a green, ruff-clean, Codex-converged worktree the orchestrator merges.*
