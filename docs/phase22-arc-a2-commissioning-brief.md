# Phase 22 — Arc 22-A2: PROOF MACHINERY (the tier-2 admission class + trade 25's correction) — commissioning brief

**From:** CHARC. **To:** the orchestrator. **Operator-commissioned 2026-09-22** ("Concur with your recommendation. We will execute these in order without needing additional permissions unless I say otherwise."). **Stage:** WRITING-PLANS under the PLAN-STAGE PROTOCOL (`docs/harness-architecture.md` §5.1 "DESIGN IS SETTLED BEFORE THE PLAN LOOP OPENS"): a premise + fork census FIRST (no Codex), routed as ONE packet; the directors rule in ONE pass each; no review round while a ruling is outstanding; the plan is a task ladder + test roster capped at ~1,000 lines; at most three counted rounds at `fast`, then a self-sweep, then execution. **Cell (writing-plans):** `implementer-opus-xhigh` — the recipe's named exception applies with its reason: a schema-and-contract design where a wrong answer is confident and hard to detect; ten of the thirteen recent CRITICALs on 22-A lived in exactly this machinery (plan S12 preamble). The alias resolves to Opus 5.5 (charter §2.11); effort is the frontmatter's. **Executing cell:** decided at the plan's return (default `implementer-opus-high` by the recipe's schema-migration exception). **Reviewer B REQUIRED** at the orchestrator's gate on the executing tree (production code under `swing/trades` + `swing/data`; charter §2.9). **§3 tripwires crossed:** new schema (migration `0039`), a `swing/trades`/`swing/data` carve-out, possibly a new module — CHARC's architecture pass rides the census packet and the plan, not a separate gate. **Worktree:** `.worktrees/22-a2-plan` off `main` at or above the commit that lands this brief. **Rules read from main by absolute path**, SHA named by the cell.

**Sequencing:** the plan stage runs NOW, in parallel with the two sonnet riders (disjoint files). **Executing dispatches only after D51 merges** (`docs/phase22-rider-d51-schema-manifest-dispatch-brief.md`): 22-A2's migration is the first to regenerate the schema-manifest fixture, and its diff is read at the merge gate. **Calendar, stated honestly:** RD's October read is the first trading week of October (~2026-10-05). The plan stage alone may take the better part of a week. If 22-A2 misses the read, the read carries trades 19/24/25 as NAMED pending-label rows exactly as RD designed (`docs/rd-state.md` §"NAMED-PENDING LABEL ROWS"), and 22-A2 lands before the November read. Nobody compresses a gate to make the date.

## §0 READ FIRST (pointers — the cell LIFTS, it does not re-derive)

1. **The carve manifest, VERBATIM-LIFTABLE:** `docs/superpowers/plans/2026-08-24-phase22-arc-a-entry-path-order-mandate-binding.md` **§S12** (lines ~4057–4162 at `main`): S12.1 the ten settled doctrine points (pre-barrier refuses by default; the tier-2 class; the four-part binary conjunction; SINGLE ROUNDING AUTHORITY; attestation-vs-verdict; fail-closed replay; the practical time anchor; continuity from FIRE SESSION to read; no network call inside `BEGIN IMMEDIATE`; trade 25's correction with its evidence), S12.2 the inherited findings table (R6-02, R6-03 three-valued half, R6-04, R7-01, R7-02, R7-03 three-valued half, R7-06, R8-01, R8-02, R8-03, R8-06 — each with its evidence), S12.2b the round-13/14 instrument items, S12.3 the inherited schema. **RD's binding condition governs: the deferred rulings travel AS RULED, never re-opened.** The struck S4.3 seventh-column block (plan ~2388) names the required-key schema of `cited_frozen_value_evidence_json`; S4.3a is the single-rounding-authority ruling measured by execution.
2. **RD's live rulings that bind the acceptance test:** `docs/phase22-scope-charc.md` §TIER 1 (trade 25 labels FROM THE FIRE, H1, candidate 12284; the 08-17 `watch`-row labeling REFUSED outright; the split ruling; the horizon marker) and `docs/rd-state.md` line ~59 (the four named pending rows, each with RD's disposition).
3. **The shipped 22-A surface the plan builds ON (read it; it is the authority for what exists):** migration `0037` (the single-state epoch `candidates_immutability_epoch(epoch_id CHECK = 1, max_candidate_id_at_barrier, applied_at)` with THREE triggers created after the seeding row; `latch_order_mandate_links` with its append-only triple; the SIX `provenance_corrections` citation columns + the replaced citation-graph trigger; `fill_envelope_identity`); `0038` (`trades.attempt_id`); `swing/data/models.py:3021–3036` (`LATCH_FREEZE_TIERS` two-valued; `PROVENANCE_ADMISSION_TIERS` two-valued with the comment naming `latch_ladder_tier2` as 22-A2's); `swing/trades/latched_origin.py` (the ONE epoch reader `freeze_tier_for_candidate` and its barrier-integrity check, S4.5C); `swing/trades/cohort_provenance_correction.py:2625` + `swing/cli.py:2658` (the Demand-C surface: tier DETECTED from the record; `--cited-candidate` must be `aplus`; "V1 records provenance ONCE per trade. There is no re-correction path"; the five-option CLI manifest pin).
4. **The gotchas that bound the design, by name (CLAUDE.md §SQLite):** REPLACE bypasses DELETE triggers (the append-only TRIPLE); a `WHEN` that can NULL fails OPEN (`WHEN NOT COALESCE(p, 0)`); Python and SQLite ROUND DIFFERENTLY at the half (one rounding authority); #11 mirror family — the 22-A `freeze_tier` widening measured ELEVEN then TWELVE sites, the SQL-vs-Python drift test is the mandatory member, **grep each enum member SEPARATELY**; a rolled-back rowid is reusable; a read on the writer's own connection is the writer quoting itself; migration backup gate `pre_version == N AND target >= N+1`. Harness §5.1: AUTHORIZE-THEN-ABORT (the trigger's predicate set ⊆ the service's admission predicates, closure-checked); ENTRY-vs-LABEL (a guard refuses the LABEL when the row can be written honestly with the keys NULL); a RULED MECHANISM CARRIES ITS ADMISSIBILITY PRECONDITIONS (VISIBILITY + IDENTITY — the R9-03 lesson, CHARC's own).

## §1 LIVE STATE, MEASURED 2026-09-22 (read-only, `mode=ro`, schema v38) — the plan's opening premise-set, to be RE-VERIFIED by the cell with the command stated

| trade | ticker | entry | `entry_intent` | `hypothesis_label` / `candidate_id` / `trade_origin` today | RD's disposition (rd-state ~59) | what 22-A2 owes |
|---|---|---|---|---|---|---|
| 19 | FTRE | 07-31 | standard | Broad-watch / 11852 / `pipeline_watch_manual` | "correctable NOW via the Demand-C surface" | **FORK F1 (below): the surface fills EMPTY keys only and records ONCE — 19 is NOT empty** |
| 24 | RHI | 08-14 | standard | Broad-watch / 12518 / `pipeline_watch_manual` — **MISFILED** (place intent 4 pre-dates the fill by two days) | "needs 22-A2 tier-2/attestation" | the tier-2 escape + **FORK F1** (a NON-EMPTY row) |
| 25 | OII | 08-17 | standard | NULL / NULL / `manual_off_pipeline` | labels from the fire (12284) on the rd-state@`9f315cc6` evidence | **the arc's live acceptance case** — link 1 EXISTS (intent 2, candidate 12284, run 136, order 1007523377009, `pre_barrier_reconstructed`), so pre-barrier admission REFUSES today and the tier-2 conjunction is the only admissible path |
| 28 | PBF | 09-01 | hypothesis_test_by_design | NULL / NULL / `manual_off_pipeline` | "adjudicated at labeling under the hyp-rec latch doctrine" | **FORK F3 (D41)** |
| 20 | AMN | 08-07 | NULL | NULL / NULL / `manual_off_pipeline` | 22-B, not this arc | OUT |

Also measured: `provenance_corrections` holds ONE row (id 1, trade 23 CADL, `last_word`, candidate 12341); the epoch row is `(1, 13591, 2026-09-02T10:03:33Z)`; four link rows, ALL `pre_barrier_reconstructed` (OII 12284, DFTX 12997, IMNM 13578 ×2) — **no `live_at_acceptance` link exists yet**; `fill_envelope_identity` has 6 rows; the barrier has NEVER been retired.

## §2 SCOPE SHAPE — CHARC's rulings (mine to make; stated so the census does not re-open them)

**IN (22-A2 core):**
- **The tier-2 admission class** end to end: the seventh column `cited_frozen_value_evidence_json` (the struck S4.3 key schema, as ruled) · the third admission tier `latch_ladder_tier2` · the four-part conjunction as a PREFLIGHT (S12.1 #3, #9) with attestation fields (`evaluated_at`, method, resolved remote SHA — R6-04/R7-06) · fail-closed replay (S12.1 #6) · the `--frozen-value-evidence` CLI option + the manifest-pin bump · the SINGLE ROUNDING AUTHORITY at criterion 3 (S4.3a, inherited fixed).
- **Trade 25's correction** at a witnessed gate, its evidence re-derived at use (S12.1 #10). This is the acceptance case; RD holds the plan to it.
- **The continuity interval `[fire_session, read]`** as a computed, recorded fact on the correction row (R6-02, R7-01, R8-01/02/03 — the interval has NO implementable definition as ruled; defining it is the plan's first design task and a census item, see F2).
- The #11 sweep for BOTH enum widenings, closure-checked by READ with each member grepped separately, the drift test extended, and the D51 fixture regenerated in the migration commit.

**OUT (each to its own item; named so nothing falls through the carve):**
- **The S12.2b instrument items** (`22A-R13-02/03/04/05`, `22A-R14-01/03/04/05/06`): test-instrument hardening of the 22-A closure walks and the canonicaliser comparator, plus the `fill_envelope_identity` re-attestation design. None bears on the October read; together they would put the plan over the cap on arrival. **They become a follow-on rider, `22-A2i`, commissioned after 22-A2 merges**, with S12.2b lifted verbatim as its brief. The one schema-bearing member (R13-02, the `UNIQUE(fill_id, envelope_raw, canonicalizer_version)` re-attestation) crosses a tripwire and gets its own §3 pass then.
- **22-B** (trade 20; `unintended_execution`; the `trades` rebuild) — after this arc.
- **`trades.initial_stop` has no correction surface** (RHI's 34.77-vs-34.18 R-denominator defect, rd-state §(d)) — a CAPABILITY question the operator/RD originate; NOT smuggled in here.
- **Any UI.** The correction surface stays the CLI (Demand-C's V1 posture).

## §3 FORK CENSUS — what the cell routes as ONE packet (both branches specified; ruler named)

- **F1 — RE-CORRECTION OF A NON-EMPTY ROW (trades 19 and 24). Ruler: RD (evidence semantics) → CHARC (shape), serialized.** The Demand-C surface fills EMPTY keys and records ONCE; 19 and 24 both carry keys today, so "correctable via Demand C" as written cannot execute (the cell verifies this by running `--dry-run` on 19 and quoting the refusal). Branch (a): 22-A2 adds a SUPERSESSION path — a new `provenance_corrections` row citing the row it supersedes (`superseded_by_provenance_correction_id` on the prior row, the `reconciliation_corrections` chain shape; append-only preserved; the head is `superseded_by IS NULL`), and the `trades` keys move to the new derivation. Branch (b): 19/24 stay named-pending until a later arc; 22-A2 ships tier-2 for EMPTY rows only (25). **CHARC's shape position if (a):** the chain shape, never an UPDATE-in-place; the trigger's predicate set stays ⊆ the service's. **RD rules whether a MISFILED label is a tier-2 evidence question at all** (24's place intent pre-dates the fill — is that structural proof, i.e. `latch_ladder`, or tier-2?).
- **F2 — THE ERA MODEL (retirement / re-arm eras, the third and fourth epoch triggers, `gap_era_reconstructed`; S12.3). Ruler: RD (doctrine #8 is his) with CHARC's shape.** The barrier has NEVER been retired and the epoch is single-state by 0037's design; the reversibility header makes retirement a migration-level operation. Branch (a): build the era model now as ruled — a SEPARATE append-only `candidates_immutability_epoch_events` table (the seeded row stays under its triple; the ONE reader consults both), the three-valued `LATCH_FREEZE_TIERS`, `gap_era_reconstructed`. Branch (b): DEFER the era model to `22-A2i` — the continuity interval is still COMPUTED and RECORDED in 22-A2 (a gap fails closed to tier-2 as ruled), but with zero live eras the third freeze tier has no reachable case and would ship as a gate built only from synthetic fixtures. **CHARC recommends (b)** and states the cost: doctrine #8's "any gap demotes" is then enforced by the interval computation alone, not by an era table; RD says whether that satisfies the ruling as ruled or re-opens it.
- **F3 — PBF (28) and D41: hyp-rec citation from a WATCH row. Ruler: RD.** The surface requires an `aplus` cited candidate; a hyp-rec entry has a `watch` row and no recommendation row (D41, register). Branch (a): 22-A2 widens admission to a watch row whose criteria profile matches the registered H2 statement, citation of the recommendation optional-with-reason (D41's stated generalization). Branch (b): PBF is adjudicated by doctrine at labeling (RD's stated disposition) and D41 waits for its own arc. **CHARC has no shape stake; the cell specifies both and RD picks.**
- **F4 — WHERE THE PREFLIGHT LIVES. Ruler: CHARC.** Branch (a): a new module `swing/trades/frozen_value_evidence.py` (the conjunction, the git ancestry check via `subprocess` under a named timeout, attestation construction) — a §3 new-module tripwire, ruled IN ADVANCE: approved if the module has no DB write and is called only by the correction service's preflight. Branch (b): inside `cohort_provenance_correction.py` (already ~2,700 lines). **CHARC rules (a).**
- **F5 — THE TIME ANCHOR'S `origin/main` READ. Ruler: CHARC.** Criterion 1 needs `git merge-base --is-ancestor <sha> origin/main` against a FETCHED remote ref. Branch (a): the preflight runs `git fetch origin main` itself (a network call — OUTSIDE the transaction per doctrine #9, under a timeout; failure REFUSES with a typed message). Branch (b): the preflight reads the local `origin/main` ref as-is and records its SHA + age; staleness is RECORDED never verdict-bearing (S12.1 #3's "depth and anchor-strength are recorded"). **CHARC rules (b) with the age recorded**, because a correction that cannot run offline is a correction that cannot run at the operator's gate, and the replay (doctrine #6) re-evaluates against whatever the remote says later.

Any further fork the census finds is ADDED to the packet with both branches specified, not decided in the plan. **No round runs while any item above is unruled.**

## §4 THE ACCEPTANCE TEST (RD holds the plan to it; the plan's test roster reproduces each from the record)

1. **Trade 25 ADMITS on tier-2** from `docs/rd-state.md`@`9f315cc6` (author-date `2026-08-10T02:41:33-10:00`, line 57, pivot 53.98 / stop 41.42 vs candidate 12284 at the mandate grain, fire run 136, uncovered window 2.38 days) — keys land as H1 / 12284 / the ruled origin, the correction row carries tier `latch_ladder_tier2`, the citation JSON, the interval, and the attestation.
2. **Each conjunction clause has a per-clause discriminator that FAILS its null implementation** (harness §5.1 "AN ACCEPTANCE GATE NEEDS A PER-CLAUSE DISCRIMINATOR"): a synthetic in the REAL row shape with ONE mutated value — a SHA not an ancestor of `origin/main`; a recorded date EQUAL to the fill session (boundary twin: strictly-precedes fails, so equality REFUSES); a value off by the real incident's geometry (five cents at 2dp — S4.3a's measured Python/SQLite half-cent divergence is the mutation, and it must REFUSE for the right reason, never as a rounding artefact); an interval left uncomputed.
3. **Replay is fail-closed:** re-evaluating trade 25's stored attestation with `origin/main` REWRITTEN (a test-local remote whose history drops the cited commit) REFUSES; with descendants GROWN it ADMITS (doctrine #6's context-drift-is-not-divergence).
4. **AUTHORIZE-THEN-ABORT closure:** every predicate the citation trigger enforces on the seventh column maps to a named service-side check, asserted in both directions.
5. **The 22-A six-case gate stays GREEN byte-unchanged** (AMN/OII/VSTS/RHI/breach/boundary) — 22-A2 adds an escape; it moves nothing 22-A ruled.
6. **The #11 drift test** covers three-valued `PROVENANCE_ADMISSION_TIERS` (and `LATCH_FREEZE_TIERS` iff F2(a)).

## §5 THE WITNESS (post-merge, one step per operator result)

Stop `swing web`; `BEGIN EXCLUSIVE; ROLLBACK` on plain sqlite3 (the lock probe, never a process list); `swing db-migrate` (ONE backup, path echoed — **the D32 PRODUCTION PROOF lands here if 22-A2's migration precedes 22-B's**); `python scripts/schema_manifest.py --db <live>` clean at v39; trade 25 `--dry-run` reads ADMIT with the four clauses and the interval printed; then the correction applied; then the H1 count re-read by RD (2/20 → 3/20 by his count, reader to confirm). 19/24 per F1's ruling.

## §6 RETURN

The plan cell returns the census packet FIRST (posted by the orchestrator to CHARC and RD with the per-item rulers named); the directors rule in one pass each; the plan follows. **The ORCHESTRATOR posts the return report to `charc` and `rd` after its QA.** Executing dispatch waits for D51's merge and for every F-item to be LANDED in the plan's ledger as a block quote.
