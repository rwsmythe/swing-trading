# Cross-Phase Operational Backlog

> **Filename note (2026-05-01):** this file is named `phase3e-todo.md` for historical reasons (it was created at the end of the Phase 3d walkthrough as the Phase 3e backlog). It has since accumulated cross-phase items (Phase 4 / 4.5 / 6-9 + standalone bundles + Tier-3 deferrals + research-branch followups). The filename is preserved to keep ~46 cross-references in shipped briefs valid; the canonical title is "Cross-Phase Operational Backlog." Not a commitment, just a trackable list.

> **Archive companion (2026-05-05):** SHIPPED + closed entries previously inline have moved to `docs/phase3e-todo-archive.md`. Fresh-orchestrator bootstrap reads only this active file; grep the archive on demand for historical context (commit hashes, prior dispatches). Retention discipline + archive-split trigger are documented in `docs/orchestrator-context.md` §"Maintenance: retention discipline."

> **Archive companion (2026-05-18 Phase 12.5 #3 T-3.2 split):** Additional SHIPPED entries dated 2026-05-12 and earlier moved to [`docs/phase3e-todo-archive.md`](docs/phase3e-todo-archive.md) (boundary 2026-05-12 inclusive; SHIPPED-only predicate). 23 sections moved this pass; archive doc gained an "Appended Phase 12.5 #3 archive-split" appendix at its end. Grep both files for full history.

> **Archive companion (2026-06-02 Phase 14-close scrub):** at Phase 14 CLOSE the live file was scrubbed to a forward-looking shape -- a curated "Standing Open Backlog" (below) + the #19 PHASE 14 CLOSED record + the #5 Phase-15/close-out punch-list. ALL other dated SHIPPED entries (Phase 14 per-cycle records #1-#4 / #6-#18 + every pre-Phase-14 section) migrated to [`docs/phase3e-todo-archive.md`](docs/phase3e-todo-archive.md) under its "Appended 2026-06-02 Phase 14-close scrub" section. Open items formerly scattered across those archived sections are consolidated (terse, with archive pointers) in the Standing Open Backlog. Grep the archive for full historical detail.

---

## Standing Open Backlog (curated 2026-06-02 Phase 14-close scrub)

> The genuinely-open forward work, consolidated from the pre-scrub scattered sections. Each item is terse; the cited archive section (grep `docs/phase3e-todo-archive.md`) holds the full original entry. Nothing here is a commitment -- it is a trackable list. **Verified at scrub time:** the close-out straggler sweep CLOSED several long-standing items that had shipped without being marked -- `warnings_json` silent-skip visibility (SB2 FB-N6, `runner.py:1541`), sector/industry tamper hardening (Phase 9, `trades.py` cached_sector + `sector_tamper`), `_bulz_*`->`_rr_*` rename (A-4), 3e.9 market-weather chart, cleanup-script `-DeregisterFirst`, pytest-xdist `-n auto`, the E501 ruff residual (now `ruff check swing/` = "All checks passed"), and the trade-exit "Phase 7 will auto-derive" promise (now real display-only auto-derivation). Those are NOT listed below.

### A. Phase 15 (IN PROGRESS -- first arc SHIPPED 2026-06-03) -- the headline forward arc

Full enumeration in the `#19` PHASE 14 CLOSED record + the `#5` punch-list below. Summary:

- **schwabdev v2.5.1 -> 3.0.5 upgrade + Fernet token encryption** -- **SHIPPED 2026-06-03 `7ee04c3`** (full copowers cycle; isolated-venv-green 7053 passed; Codex-converged; the FIRST-EVER L2-LOCK baseline re-anchor `bf7e071`->`9d05a8f`, operator-signed GATE A). **GATE-B live-OAuth cutover DONE 2026-06-03** (operator-validated on v3; 7053 fast tests green post-cutover; ARC FULLY CLOSED). Full record in `#20`. (was `#9`)
- **B-7 operator failure-mode classification** -- **SHIPPED + CLOSED 2026-06-03 `d6092c7d`** (full copowers cycle; 7086 fast tests green; Codex-converged; new nullable `trades.failure_mode` 7-token CHECK -> the FIRST v24 migration; capture-only; orthogonal to grade+mistake-tags; web form + CLI `--failure-mode` + chronology read-back; operator browser gate DONE 2026-06-03, live DB migrated v23->v24). Full record in `#21`. (was Phase 14 FINAL TOUCH)
- **Process-grade-trend chart redesign** -- **SHIPPED + CLOSED 2026-06-04 `e9b0292e`** (full copowers cycle; 7105 fast tests green; Codex-converged; small-multiples = 3 scale-separated inline-SVG panels [GRADES [0,4] / RATE [0,1] / COST [0,max] 0-anchored, per-trade only], distinct theme-aware `--series-*` colors + legend, the cost-axis [0,1] all-zero fallback; presentation-only, NO schema). Operator browser gate PASSED (light/dark + the under-floor default) -- incl. an inline gate-fix for dark-mode axis-label visibility (`.process-grade-trend-chart text { fill: var(--fg-muted) }` -- SVG `<text>` is painted by `fill`, not `.muted`'s `color`). Full record in `#22`. (was the F-3 re-diagnosis follow-on)
- **Reviews-page topbar missing the date** -- **SHIPPED + CLOSED 2026-06-04 `e9b0292e`** (bundled into the PGT-redesign cycle as Slice A; `build_reviews_pending_vm` now stamps `session_date=last_completed_session(datetime.now()).isoformat()` + a render test; the operator browser gate confirmed the `/reviews/pending` topbar date is now populated). Full record in `#22`.
- **Phase 14 cross-sub-bundle integration review** (commissioning brief Sec 9.1 Q6) -- operator browser-witnessed coherence check across charts + review/journal + metrics overview + Schwab. **One of the TWO SMALL near-term items (with the finviz fix), done BEFORE the big data-integrity arc (operator-decided 2026-06-04).** Operator-driven browser walkthrough; orchestrator provides a coherence checklist; any incoherence surfaced is triaged into follow-up fixes. No dispatch (operator drives). **DONE + CLOSED 2026-06-05** (operator browser walkthrough across sections A-K; checklist `docs/phase14-integration-review-checklist.md`). **Verdict: the sub-bundles cohere well** -- the #23 invisible-widen DOUBLY confirmed live (pattern-outcomes tile n=3 not 55 + the review queue correctly empty); #22 PGT legible incl. dark mode; #21 B-7 failure-mode present; thumbnails/charts/dark-mode/cross-links coherent; Schwab status healthy. **4 findings triaged:** **#5** (med) topbar-date inconsistent across base-layout VMs at one moment [some use `action_session_for_run`, some `last_completed_session`] -> FOLDED into the data-integrity arc; **#3** (med) capital-friction position count=0 for Run#89 despite SKYT open since 5/28 (contradicts the row's non-zero capital) -> scope (snapshot-read vs Schwab); **#2** (low-med) non-uniform empty/under-floor messaging + **#4** (low) `/schwab/status` not nav-linked -> one consolidated polish fix.
- **Pattern-observation pool widening (aplus -> aplus+watch)** -- **SHIPPED + CLOSED 2026-06-04 `8ee98fc1`** (full copowers cycle; 7128 fast tests green; Codex-converged; the provable-aplus `pattern_evaluations` consumer isolation keeps the widen INVISIBLE to operator surfaces; two dormant relief levers; NO schema [v24]; operator OQ-1 accept-uncapped + the OQ-4 first-live-run gate PASSED [Run 89: 55 watch detections on a zero-aplus night, 0/55 admitted to the aplus-only tile]; full record in `#23`). A B-family substrate-augmentation item. Widen the nightly pattern detect/observe pool from `bucket=='aplus'` to `aplus+watch` so the Phase-14 temporal observation log accumulates the ~83x-larger watch population as forward-walk learning data, at ZERO capital risk + no Finviz-screen change (the clean expression of the 2026-05-27 arc-closure "decouple data-generation from capital"). **Commission as a standalone copowers cycle AFTER the PGT-redesign arc (+ ideally the Phase-14 integration review) closes** -- do NOT wedge into the in-flight arc (domain = Phase-14 temporal, not Phase-15 integration). **Locked scope (operator triage 2026-06-03):** pool=`aplus+watch` (NOT skip); provenance=`finviz_screen_state` JSON, NO schema change; observe-load=accept-and-measure (#27 audit on any cap; NO silent cap); single Codex chain. The substance is the **observe-load bound + bucket provenance + test rework**, NOT the one-line predicate (`runner.py:1531`). Brief + triage outcome: `docs/pattern-observation-pool-widening-commissioning-brief.md` (Sec 10; brief committed `6d243f0b`).
- **NEW operational items (surfaced at the pool-widening live gate 2026-06-04):**
  - **Finviz signature-provenance fix** -- hash only `column_set`, drop the volatile `first_row`, so the "operator may have edited the saved screen" warning fires ONLY on a real screen-definition change, not on normal top-result drift. **SHIPPED + CLOSED 2026-06-04 `52fcadb1`** (single executing cycle; Codex R1-converged; suite green -- the 1 post-merge fail was the known `test_ohlcv_reader_re_export_identity` xdist co-residency flake, confirmed passing `-n0`; NO schema/lock). **Transitional:** the FIRST post-fix pipeline run emits ONE "signature changed" warning as the stored hash basis shifts, then stabilizes (benign).
  - **Regular-session + completed-day data-integrity arc** -- never consider extended-hours data on ANY Schwab call (`price_history needExtendedHoursData=False` + the quotes regular-session fields) + discount current-day/partial data when the pipeline runs during/after hours (anchor on `last_completed_session`; a guard so the append-only temporal log can NEVER lock a partial/extended bar). **THE BIG active arc** (both small items -- finviz fix + the Phase-14 integration review -- are now DONE 2026-06-05). **FOLDS IN integration-review Issue #5** (topbar-date anchor inconsistent across base-layout VMs -- some `action_session_for_run`, some `last_completed_session`; needs a uniform forward-vs-backward topbar policy -- same session-anchor discipline). Issue #3 (capital-friction count from `account_equity_snapshots`) is adjacent (scope at brainstorm: in-scope vs separate). **Grounded:** `marketdata.py:428` omits `needExtendedHoursData` (-> Schwab default true -> ext-hours candles -> the ~16% `OhlcvBar`-invariant errors -> yfinance fallback); the `last_completed_session`/`action_session_for_run`/`data_asof_date`/`lease_data_asof` + `_slice_to_asof` machinery already exist (the arc HARDENS + uniformly enforces). **Urgency:** the pool-widening now locks forward-walk data nightly. **BRAINSTORM SHIPPED + merged 2026-06-05 `d3c21e31`** (spec `docs/superpowers/specs/2026-06-05-data-integrity-regular-session-completed-day-design.md`, 465 lines; Codex-converged R1[2crit+5maj]->R2[3maj]->R3[1maj]->R4; the AUDIT spine [5 pull / 4 lock / ~20 anchor surfaces]; the **C2 division-of-responsibility** [ext-hours cleanliness is ONLY enforceable at the PULL stage -- the date-only lock-guard cannot validate ext-hours provenance]; **Issue #5 = THREE anchor families** [forward / backward / a `date.today()` naive-calendar bug] -> a required-`PageKind` `topbar_session_date` classifier + an authoritative-registry test; **Issue #3 AUDITED -> routed OUT** [root cause pinned: `_count_open_at_run` `last_fill_at >= started_ts` excludes a position filled before the run -- a METRICS predicate bug, NOT the Schwab snapshot the integration review hypothesized]; typed `SchwabBarConsistencyError` + mapper float-normalization [no global epsilon]; NO schema [v24]; 4 slices A[ext-hours pull]->C[write-barrier+lock-guard+remediation]->B[quotes,after OQ-3]->D[topbar]). Operator OQs for writing-plans: OQ-3 (quote regular-session field availability -- could send the Schwab quote path to yfinance), OQ-5 (Issue #3 -> a separate small `_count_open_at_run` metrics-fix brief: open now or bank), OQ-7 (accept already-locked ext-hours observations as an L6-limitation). **WRITING-PLANS SHIPPED + merged 2026-06-05 `37c06cad`** (plan `docs/superpowers/plans/2026-06-05-data-integrity-regular-session-completed-day-plan.md`, 1957 lines, 20 TDD tasks: Slice A=A1-A5 / C=C1-C8 / B=B1-B3 / D=D1-D4; Codex-converged R1[1crit+4maj]->R6 `NO_NEW_CRITICAL_MAJOR` -- the R1 CRITICAL moved the completed-day strip from `write_window` to `_write_archive_atomic`, the TRUE single chokepoint all 4 Shape-A write paths funnel through [`write_window`/`read_or_fetch_archive`/`_backward_compat_rename`-on-every-read]; the 5+2 OQs LOCKed: OQ-1b no `force_refresh` + a REQUIRED one-time `swing schwab purge-marketdata-archive` gate [Task C8], OQ-2 chokepoint=`_write_archive_atomic`, OQ-3 Slice B gated on an operator-run live-quote cassette [Task B1], OQ-4 `SchwabBarConsistencyError` subclasses `SchwabApiError` [classification is `_classify_http_failure`, no `_classify_schwab_error`], OQ-5 Issue #3 OUT+banked, OQ-6 mapper rounds o/h/l/c to 4dp [no global epsilon], OQ-7 L6-accept; NO schema [v24]; dependency order A->C->B->D [D parallelizable]). QA'd against disk at merge (every load-bearing file:line re-verified; convergence confirmed from the persisted RESPONSES in `.copowers-findings.md`; docs-only ff-merge, suite unaffected). Binding gate = fast suite on merged HEAD + operator browser [topbar light/dark] + operator-witnessed LIVE Schwab re-fetch [the ~16% `OhlcvBar invariant violated` rate collapses] + the required purge + Slice B's live-quote cassette precondition. **EXECUTING-PLANS SHIPPED + merged 2026-06-05 `b237412b`** (28 commits, slice order A->C->B->D; Codex-converged in 3 rounds R1[3maj/1 by-design + 2min]->R2[1maj ext-hours `mark` leak -> `regularMarketMark` only + 1min pre-existing]->R3 `NO_NEW_CRITICAL_MAJOR`; QA'd against disk -- trailers `[]` x28, schema NONE [v24, zero migration files added], load-bearing fixes verified [`_strip_incomplete_sessions` @ohlcv_archive.py:99 called in `_write_archive_atomic`:131 fail-closed; `needExtendedHoursData=False` @marketdata.py:403; mapper `regularMarketMark` only; `PageKind`/`topbar_session_date` @dates.py]; **fast suite 7175 passed / 3 skipped / 0 failed on the merged HEAD** [orchestrator re-ran -- the 3 known xdist flakes did NOT fire; 3 skips pre-existing], ruff clean). **NOT pushed yet** [push held for operator decision]. **4 OPERATOR GATES PENDING (post-merge):** (1) re-run/isolate suite on main [done: 7175 green]; (2) operator browser -- topbar consistency light+dark; (3) operator-witnessed LIVE Schwab re-fetch confirming the ~16% `OhlcvBar invariant violated` rate collapses + the UNSEEDED normal run, then the **REQUIRED one-time `swing schwab purge-marketdata-archive`** [binding, not optional -- Codex R1 MAJOR#3]; (4) Slice B live-quote cassette [operator records `quote_regular_fields.yaml`, confirms `regularMarket*` Last/TradeTime/Bid/Ask]. One banked non-blocking item: R2 MINOR (legacy-shape merge edge -- pre-existing, unreachable by production writers). **OPERATOR GATES 2026-06-05:** Gate 1 (suite) PASS [orchestrator re-ran 7175]; **Gate 2 (topbar light/dark) PASS** (uniform forward/backward policy confirmed across base-layout VMs); **Gate 3 (live re-fetch) -- HEADLINE NOT MET / hypothesis FALSIFIED:** the OhlcvBar-invariant error rate did NOT collapse (flat ~2-4/run incl. Run 90=4 post-fix; ALL 49 historical prod `pricehistory` ERROR rows ARE the invariant) -- the violations are bad **regular-session** Schwab bars, NOT ext-hours artifacts (ext-hours confirmed OFF in Run 90 via `needExtendedHoursData=False`). Arc STILL delivered value: those bad bars are now TYPED (`SchwabBarConsistencyError`) -> clean yfinance fallback with good data; Slices C/D stand. Gate 4 (quote cassette) NOT run. **Gate 3 ALSO root-caused the DOMINANT fallback driver** (~13-22 tickers/run = `unexpected error from T-C.1 wrapper` -> `OperationalError: database is locked`, SQLite write-lock contention, the pre-existing banked T-C.1 item amplified by #23) -> SPUN OUT to its own arc (see the SQLite-lock-contention bullet below). **Data-integrity arc: KEEP merged (mechanisms sound + net-positive); NOT closed -- Gate 4 + the honest close note pending operator.**
- **SQLite lock-contention (WAL) fix** -- ROOT-CAUSED 2026-06-05 at the data-integrity Gate-3 live re-fetch: `OperationalError: database is locked` ([`swing/data/db.py:1159`] `connect()` has no `PRAGMA journal_mode=WAL` -> rollback-journal exclusive whole-DB lock; concurrent OHLCV-fetch audit-row INSERTs [`record_call_start`] contend past the default 5 s busy timeout -> the loser's INSERT raises pre-commit -> ladder `except Exception` catch-all -> silent yfinance fallback with NO audit row). **This IS the pre-existing banked "Schwab market-data ladder T-C.1 wrapper erroring per-ticker -> yfinance fallback" item** (was in CLAUDE.md line-3's 2 banked investigations), now CONFIRMED + amplified by #23's pool widening. **Fix direction (brainstorm to validate):** WAL + a higher `busy_timeout` centralized in `connect()` + fold in the catch-all exception-class+message logging (redaction-safe, NOT `exc_info`) + a concurrency regression test; wide blast radius (backup/migration/test connections); **NO schema** (runtime PRAGMA; v24 holds). **COMMISSIONED as a full copowers cycle 2026-06-05** (brainstorm dispatch brief `docs/sqlite-lock-contention-arc-brainstorming-dispatch-brief.md`). **BRAINSTORM SHIPPED + merged 2026-06-06 `5e135103`** (spec `docs/superpowers/specs/2026-06-05-sqlite-lock-contention-wal-design.md`, 545 lines; Codex 3-round converged R1[5maj/3min]->R2[4maj/3min]->R3 `NO_NEW_CRITICAL_MAJOR`; QA'd against disk -- trailers []x4, schema NONE [v24], spec-only diff). **HEADLINE REFINEMENT R1 (orchestrator-verified against the live DB):** WAL is ALREADY ON (`swing.db` header bytes[18,19]=2,2; `ensure_schema` db.py:1132 sets it, persistent in header) -- the brief's "enable WAL" premise was WRONG; the operative lever is **`busy_timeout`** (`connect()` db.py:1159 sets none -> only Python's implicit 5 s; 8+8=16 concurrent writers [config.py:381/386] each doing `BEGIN IMMEDIATE` audit txns starve a writer past 5 s under #23's amplified load -> `database is locked` pre-commit -> silent yfinance fallback, no audit row). Design: a centralized `open_connection` opener (`DEFAULT_BUSY_TIMEOUT_MS=30000`, module-const + keyword override, NO cfg in db.py; WAL demoted to defense-in-depth) routing ALL swing.db opens (backup source keeps fail-closed `mode=rw`) + fold-in catch-all observability logging (both ladder catch-alls, redaction-safe, no `exc_info`) + G2' lock-wait telemetry (log AFTER lock release) + a 5-layer test strategy. **OQs for operator triage:** OQ-0 accept-R1 (orchestrator-verified; rec accept) / OQ-A busy_timeout value (rec 30000 ms uniform) / OQ-B route-ALL-opens (rec yes) / OQ-C data-driven write-pressure gate (rec ship busy_timeout+telemetry, first live run decides) / OQ-D fold-observability (yes) / OQ-E the bad-bar issue NOT banked [operator 2026-06-06] -> OUT of the lock arc, tracked as its own active fix-target (below). **OQs LOCKED 2026-06-06:** OQ-0 accept-R1 (orchestrator-verified); OQ-A busy_timeout=30000 ms module-const `DEFAULT_BUSY_TIMEOUT_MS` + keyword override + a `cfg.web.db_busy_timeout_ms` runtime knob (NO cfg in db.py); OQ-B route ALL swing.db opens through the centralized opener (backup SOURCE keeps fail-closed `mode=rw`; tokens.db excluded; explicit raw-open inventory required); OQ-C **INCLUDE write-pressure reduction in V1** (operator override of the data-driven-defer -> DESIGN-BEARING writing-plans: select serialized/shared audit connection vs batched start/finish, GROUNDED in audit_service, preserving the single-tx discipline + the in-flight-row visibility contract); OQ-D fold the catch-all observability logging. **WRITING-PLANS SHIPPED + merged 2026-06-06 `fa4aee8d`** (plan `docs/superpowers/plans/2026-06-05-sqlite-lock-contention-wal-plan.md`, 1641 lines, 13 tasks; Codex 3-round converged R1[3maj]->R2[1maj]->R3 `NO_NEW_CRITICAL_MAJOR`; QA'd against disk -- trailers []x3, schema NONE [v24], plan-only diff; **OQ-C precondition independently verified** [`conn` used ONLY for `audit_service.*` in marketdata/ladder -> the serialized shared audit-writer connection is safe]). **OQ-C mechanism selected: a process-wide serialized shared audit-writer connection** (one `check_same_thread=False` conn for the 16 pipeline hooks + a module `audit_service._AUDIT_WRITE_LOCK` wrapping ALL FOUR transaction-owning audit fns; preserves single-tx + in-flight-row + thread-safety; lock is per-process [pipeline subprocess]; batching REJECTED [breaks in-flight]). **EXECUTING-PLANS SHIPPED + merged 2026-06-06 `ffb5fdc6`** (13 commits; Codex 2-round converged R1[2maj REBUTTED + 2min]->R2 `NO_NEW_CRITICAL_MAJOR`; QA'd against disk -- trailers []x13, schema NONE [v24]; **the MAJOR#1 use-after-close rebuttal INDEPENDENTLY VERIFIED** [`get_many` not called in runner.py + structurally web-only -- `PriceCache.get_many` raises without `app.state.price_fetch_executor`; the pipeline market-data fetch is synchronous per-ticker `get`/`get_or_fetch`; `audit_conn` closed in the run-level finally AFTER the synchronous block -> no live worker at close]; **fast suite 7214 passed / 3 skipped / 0 failed on the merged HEAD** [orchestrator re-ran; known xdist flakes did not fire], ruff clean). **CODE-COMPLETE; the BINDING gate is PENDING: operator first-instrumented-live-run** (UNSEEDED `swing pipeline run --manual`: confirm the ~13-22 tickers/run `database is locked` fallback collapses [now class+message-logged at the ladder catch-all] + read the G2' lock-wait telemetry; a residual quote-path starve at the 6 s deadline feeds the banked non-audit-writer telemetry extension). **OPERATOR FIRST-LIVE-RUN GATE 2026-06-06 (Run 92): FAILED headline / SUCCEEDED diagnostic.** The ~13-22 ticker `database is locked` fallback did NOT collapse, BUT the G2' telemetry worked + pinned the TRUE root cause: `audit record_call_start: BEGIN IMMEDIATE FAILED (database is locked) after ~33s (busy_timeout=30000ms)` -- a writer holds the SQLite write lock 30+s during the market-data fetch. **CONFIRMED root cause (orchestrator-verified at runner.py):** the pipeline fetches OHLCV (audit-writing `get_or_fetch`) INSIDE held `lease.fenced_write()` write transactions -- e.g. `_step_pattern_detect` Pass-2 opens `fenced_write` @runner.py:1898 + calls `ohlcv_cache.get_or_fetch` @1994 (exemplar fetch) INSIDE it; the candidate failures point to the same pattern in `_step_charts` / `_step_pattern_observe`. The audit writes (separate shared `audit_conn`) deadlock on the held lock -> 30s busy_timeout -> fail -> yfinance fallback. **busy_timeout + the serialized audit writer CANNOT fix a 30s-held lock by a different connection -- the arc's premise (transient audit-vs-audit contention / "16 concurrent writers") was INCOMPLETE.** **Arc disposition: KEEP merged** (the G2' telemetry made this diagnosable; busy_timeout + serialized writer + the catch-all observability are correct keepers; the OQ-C data-driven gate did its job). **DOWNSIDE:** the 30s busy_timeout makes the deadlock path SLOWER (30s waits vs old ~5s) until the root cause is fixed -- stopgap = lower `cfg.web.db_busy_timeout_ms` (the knob the arc added). **NEW ROOT-CAUSE FIX ARC NEEDED: pre-fetch OHLCV OUTSIDE the `fenced_write` (no network I/O inside a held write transaction) across detect Pass-2 / charts / observe.** **STOPGAP applied `dd3d932e`** (`swing.config.toml [web] db_busy_timeout_ms = 5000` -> audit conn fails fast ~5s not 30s while the fix is built; REVERT to 30000 when it ships). **Fetch-vs-write-ordering fix arc COMMISSIONED 2026-06-06 -- see the bullet below.** **This lock-contention arc: CLOSED 2026-06-06** (the fetch-vs-write fix's live gate PASSED Run 93 -- the deadlock collapsed; the busy_timeout + serialized-audit-writer + G2' lock-wait telemetry + the catch-all class+message observability are all KEPT as the permanent improvements that made the true root cause diagnosable).
- **Fetch-vs-write-ordering fix (OHLCV fetch inside held `fenced_write`)** -- the REAL fix for the Run-92 deadlock (the lock-contention arc's confirmed root cause). Pre-fetch OHLCV bars BEFORE opening `lease.fenced_write()` (keep the consistency-critical SQLite reads in-tx; move the network fetch out) across detect Pass-2 (runner.py:1898/1994) + `_step_charts` + `_step_pattern_observe` -- the COMPLETE `fenced_write`-wrapping-a-fetch locus enumeration is the brainstorm's core deliverable. Reverts the busy_timeout stopgap to 30000. NO schema (v24). **COMMISSIONED 2026-06-06 as a full copowers cycle** (operator-chosen; brainstorm dispatch brief `docs/fetch-vs-write-ordering-arc-brainstorming-dispatch-brief.md`). **BRAINSTORM SHIPPED + merged 2026-06-06 `7d8f109e`** (spec `docs/superpowers/specs/2026-06-06-fetch-vs-write-ordering-design.md`, 430 lines; Codex 3-round converged R1[2maj/2min]->R2[2 new maj]->R3 `NO_NEW_CRITICAL_MAJOR`; QA'd against disk -- trailers []x4, schema NONE [v24], spec-only diff). **COMPLETE 18-block `fenced_write` audit -> EXACTLY 2 active Schwab-audit deadlock loci:** #8 detect Pass-2 (runner.py:1898 fence / 1994 exemplar `get_or_fetch`) + #9 observe (runner.py:2628 fence / 2662 `_bar_for_date`); **`_step_charts` is NOT a locus** (orchestrator-verified: all `get_or_fetch` run OUTSIDE the fences -- the fetch-outside/short-write-fence pattern). **Reorder design:** pre-fetch bars before the fence; detect Pass-2 uses a SNAPSHOT-authoritative exemplar membership (rows read once OUTSIDE -> authoritative for BOTH prefetched bars AND scoring -> no silent score change; an in-fence cheap `SELECT id` re-read on the SAME fence conn emits a #27 divergence audit; Codex-grounded that web/CLI write `pattern_exemplars` outside the lease -> <=1-run staleness, AUDITED not silent). OQs settled: A per-locus prefetch / B revert stopgap in executing / C->snapshot membership / E observable-divergence guard. **OQ-D RESOLVED (operator 2026-06-06): BANK** the daily-management yfinance-under-fence locus [#16 -- NOT a deadlock; tracked as a separate follow-up below]. **WRITING-PLANS SHIPPED + merged 2026-06-06 `0f77731a`** (plan `docs/superpowers/plans/2026-06-06-fetch-vs-write-ordering-plan.md`, 938 lines, 5 TDD tasks; Codex round-1 converged `NO_NEW_CRITICAL_MAJOR` [1 MINOR applied: cfg=None/no-`_conn` short pure-read fence]; QA'd against disk -- trailers []x2, schema NONE, plan-only diff; the binding in-process deadlock-repro test verified discriminating [`_DeadlockProbeCache` 2nd-conn `BEGIN IMMEDIATE` vs a real held fence; the spy RECORDS the flag, never raises -- the pre-fix `try/except: continue` would swallow a raise]; OQ-B revert=Task 4, OQ-D #16 untouched). **EXECUTING-PLANS SHIPPED + merged 2026-06-06 `4f0b4010`** (5 commits; Codex round-1 converged `NO_NEW_CRITICAL_MAJOR`; QA'd against disk -- trailers []x5, schema NONE [v24]; the 2-loci reorder [ONLY the fetch moved out; writes stay in-fence -- runner.py `_step_pattern_detect` + `_step_pattern_observe` only] + the OQ-E #27 divergence guard + the stopgap FULLY reverted [the `[web]` section DELETED -> `db.py` 30000 default]; rebase-then-ff [main had moved to f8eead45]; **fast suite 7223 passed / 3 skipped / 0 failed on the merged HEAD** [orchestrator re-ran; known xdist flakes did not fire]). **NOTE -- a stopgap regression caught + fixed by this arc:** the orchestrator's stopgap commit `dd3d932e` had added a TRAILING `[web]` section to `swing.config.toml` which broke `test_finviz_integration_tracked_toml_token_STRIPPED_at_load` (confirmed RED on the pre-merge main; the orchestrator did NOT re-run the suite after that config-only commit -- the miss); the arc's true-revert (delete the whole `[web]` section) fixes it (GREEN post-merge). **OPERATOR RE-RUN LIVE GATE PASSED 2026-06-06 (Run 93): the deadlock COLLAPSED** -- ZERO `database is locked` / `BEGIN IMMEDIATE FAILED` / `unexpected error from T-C.1 wrapper` lines at the restored 30000ms busy_timeout; the ~13-22 cache-miss liquid tickers now fetch from Schwab; only PLTR+TWLO fell back (`SchwabBarConsistencyError` = the separate bad-bar issue) + CTRA (delisted). **ARC CLOSED.** The T-C.1 banked investigation is RESOLVED (it WAS this fenced-write deadlock). **NEXT: the OhlcvBar bad-bar fix arc** (now active), then the banked daily-mgmt #16.
- **daily-management yfinance-under-fence (#16) -- BANKED follow-up (operator 2026-06-06).** `compute_daily_approximate_snapshot` calls the fetch-capable `read_or_fetch_archive` (daily_management.py:510) INSIDE the per-trade `lease.fenced_write()` (runner.py:3709), so a warm-miss/transient-empty/gap-fill can do yfinance NETWORK I/O while holding the SQLite write lock. NOT the Run-92 Schwab-audit deadlock (no separate `record_call_start` connection) -- a lower-severity latent lock-hold (could block a concurrent web writer during a yfinance fetch). Codex-identified robust fix = a read-only-vs-fetch archive API split (spec-locked-signature refactor of `ohlcv_archive.py` + `daily_management.py` + consumers). **Its own arc when picked up; lower priority than the bad-bar fix.**
- **OhlcvBar bad-bar fix (Schwab regular-session data quality)** -- **NOT banked (operator 2026-06-06).** ~2-4 Schwab regular-session daily bars/run violate the OHLC invariant (`high < max(open,close)` / `low > min(open,close)`); the data-integrity arc made these a clean TYPED (`SchwabBarConsistencyError`) -> yfinance fallback [the END data is correct], BUT the Schwab data-quality defect + the WHOLE-WINDOW discard (the bad bar drops the entire Schwab window -> yfinance for that ticker, losing Schwab's other good bars) is a real issue worth fixing. **Active fix-target -- its own arc (distinct root cause from the lock-contention arc).** Fix-approach is a design question (drop-just-the-bad-bar + keep the rest of the Schwab window / repair-clamp the bad bar / investigate the Schwab root cause / accept-fallback). **COMMISSIONED 2026-06-07 as a full copowers cycle** (operator-chosen after the deadlock fix closed; brainstorm dispatch brief `docs/bad-bar-fix-arc-brainstorming-dispatch-brief.md`; the brainstorm CHARACTERIZES the bad bars first [live `schwab_api_calls` error_messages -- quirk vs irreducible, request- vs response-side] then surfaces the fix-approach as the central OQ-A; keep the `OhlcvBar` invariant STRICT, handle at the mapper). **NEXT: brainstorm.**

- **Applied-research forward set -- MOVED OUT of operational Phase 15 (operator-decided 2026-06-04; tracked in the research branch `research/phase-0-tasks.md`):** B-1..B-8 (substrate-size, Finviz-widening, cohort-stability [gotcha #37 already eliminated-by-construction in the v22 log], D2 `canonical_survival_rate` remediation, real-time prospective tracking, multi-pattern composites, other-gates market-regime) -- the continuation of the closed 2026-05-27 ruleset arc, temporal-log-accumulation-gated -- AND the Minervini correct-entry exemplar recall/sensitivity validation (gated on the operator's transcribed exemplar list; operator plans to begin it AFTER Phase 15 completes). These are APPLIED RESEARCH (NOT operational); any deployable finding spawns a future operational arc, but the investigations themselves are research. Full entries: `research/phase-0-tasks.md` §"Applied research candidates".

### B. Older standalone cross-phase items (predate Phase 14; still open)

- **Schwab API Phase B + Phase C** -- Phase A (read-only auth + reconciliation + market-data ladder) SHIPPED across Phase 11/12 + SB5.5. Phase B = data-source flip `cfg.data_source.primary` yfinance->schwab + parity study; Phase C = trade automation (dry-run -> opt-in live + bilateral verification). Both large; brainstorm-needed. (archive: 2026-05-04 "Schwab API integration"; re-scope vs what shipped)
- **Sec 8.4 Corporate_Actions MVP** -- new `corporate_actions` table + `0024_*.sql` + `swing corporate-action {record,list,resolve}` CLI + manual reconcile; defensive log-only (no auto price-adjust V1). "Phase 11 candidate," never dispatched; full brainstorm->writing->executing cycle (schema work). (archive: 2026-05-13 "Sec 8.4")
- **Schwab "since-inception" Account Statement ingestion** -- seed `cash_movements` + `account_equity_snapshots` historical series + audit `fills` since inception; parser largely reuses `tos_import.py` (Profits-and-Losses + Equities sections are NEW extractors). (archive: 2026-05-12 Phase 9 D/E candidate)
- **account_equity_snapshots cash-basis vs net-liq formalization** -- `equity_dollars` is ambiguous (cash-basis vs Net Liq); add `kind` discriminator OR distinct columns OR auto-derive cash-basis from `cash_movements` (recommended; sequences after inception-CSV ingestion). (archive: 2026-05-12 Phase 9/V2)
- **Minervini + body-of-knowledge reference review** -- 6-axis reconciliation (entry/exit/stop/sizing/journal/discipline) of current implementation vs PRIME (Minervini + Qullamaggie) / CONFIRMING / ALTERNATIVE sources; axes 2+3 largely covered by the 3e.8 investigation; modular per-axis research dispatch. Operator-paced. (archive: 2026-05-12 "Minervini + body-of-knowledge")
- **chart-scope policy v3 (4th tier `hypothesis_rec`)** -- migration extends `pipeline_chart_targets.source` CHECK + resolver gains 4th tier so ALL hyp-recs get charts. Operator-gated deferral (nearly-triggered, kept deferring). (archive: 2026-04-28 "chart-scope policy v3")
- **earnings-proximity vs risk-level correlation study** -- research-branch; gated on n>=10 closed trades (today insufficient). (archive: 2026-05-07)
- **Chart pattern detection v2** -- greenfield 8-phase roadmap (rule-based + template-matching primary; ML re-ranker deferred); Phase-11+-scope multi-phase; brainstorm-needed. NOTE: Phase 13 already shipped V1 detectors -- re-scope vs what landed. (archive: 2026-05-09 "Chart pattern detection v2")

### C. Gated-on-feature (backlog; auto-trigger when the gate fires)

- **Fill.quantity fractional-share forward-compat** -- ~7 modules int-truncate via `_ExitShape`; widen when fractional-share trading lands (likely Schwab Phase B). (archive: 2026-05-05)
- **trade.entry_date datetime promotion** -- date-only TEXT today; promote to ISO datetime when sub-day precision is needed (Schwab broker fill timestamps OR intraday research). Cross-cutting consumer audit. (archive: 2026-05-04)

### D. Low-priority cleanups (defer-indefinitely; pick up opportunistically when touching the surface)

- **TOS reconciliation depth bundle** (3 gaps: close-fill price-mismatch, stop-order vs Account Order History, position-qty vs Equities) -- largely SUBSUMED by the shipped Schwab API reconciliation; retain only as offline-CSV fallback. (archive: 2026-04-30 "TOS reconciliation depth")
- **Tranche B-ops Sec 8 residuals** -- exit-form field preservation; ExitRationale-vs-ExitReason enum split; total-book risk-cap config; book-equity Open-risk %; chart-reason `insufficient-data` split; session-gating propagation to read-only surfaces; img onerror fallback; `TradeEntryFormVM.force` dead-field cleanup; `(str,Enum)`->`StrEnum` migration (3 enums). (archive: 2026-04-24 "Tranche B-ops deferred")
- **Tranche C residuals** -- `build_watchlist` mixed-anchor fix (`watchlist.py` latest-eval vs `evaluation_run_id`); stale `pipeline_chart_targets` rows on lease-revoke; per-ticker `fenced_write` granularity; Hypothesis-5 prod-vs-replay parity (~50x residual gap); Hypothesis-6 Finviz universe reconstruction; Newcombe interval refinement; `recompute_binding_prod_gated.py` parameterization. (archive: 2026-04-25 "Tranche C deferred")
- **2026-04-25/26 follow-up clusters** -- parallel-work (stale `/watchlist` banner; ORDER-BY tiebreaker; multi-run parity; PriceFetcher cache-stat introspection); Bug 1/2 (watchlist-row HTMX trigger refactor; JS-execution test harness [Playwright/Selenium] gap; sizing-hint `hx-trigger` parens-grouping parse bug -- confirmed JS error every form render); hypothesis-engine/analyze/backup; QoL + watchlist-sort (~13 micro-cleanups: colspan-aware error fragments, eval-anchor resolver centralize, `watchlist_ranking.py` extraction, `_TAG_PRECEDENCE` decouple, etc.). (archive: 2026-04-25 + 2026-04-26 follow-up sections)
- **chart-pattern flag-v1 V2+ ideas** -- V2+ patterns (pennant, cup-with-handle, flat-base, tight-channel, Qullamaggie taxonomy; LMW 2000 methodology ref); calibration study; multi-timeframe; intraday; tuning-history versioning; classifier-error dashboard banner; schema-layer + hidden-field tamper hardening. Largely subsumed by Phase 13 detectors + the Chart-pattern-detection-v2 item above; flag-v1-specific bits remain. (archive: 2026-04-26 "flag-v1 brainstorm follow-ups")
- **flag-v1 operator-paced gates 7.3/7.4** (fixture labeling >=15 + FP-biased tuning) + **Tier-3 design Qs #5/#6** (lightning-icon trigger re-eval; multiple-concurrent-advisories vs single stop field). (archive: 2026-04-27 sections)
- **3e.2 realized-from-partial-exits in journal stats total**; **Phase 4 cleanup-remainder** (promote `_STALENESS_THRESHOLD_DAYS=7` to a public constant in `ohlcv_archive.py`); **Phase 8 V2 advisory items** (audit-chain symmetry for legacy `/trades/{id}/stop`; `data-trade-event-id` attr; read-side snapshot consistency for `build_daily_management_timeline_vm`); **Phase 10 V2 candidates** (orphan-emit discrepancy attribution surface; `render_class_d` sum-class + bootstrap CI; per-cohort exclude-paused-intervals filter). (archive: respective dated sections)

### E. Process / hygiene debt + open operator decisions

- **27 pending V2.1 Sec VII.F amendments** (Phase 9 + Phase 10) -- cumulative spec-amendment debt awaiting source-of-truth correction-protocol routing. (archive: 2026-05-13 "Phase 10 closer -- Phase 11 hand-off")
- **Formalize orchestrator-vs-implementer execution-mode policy** -- promote the auto-memory `feedback_orchestrator_vs_implementer_execution` to an orchestrator-context Conventions entry + brief-template `Sec 0` execution-mode-decision step + cost-estimation heuristic. Orchestrator-inline doctrine work (NOT a dispatch). (archive: 2026-05-10)
- **`docs/orchestrator-context.md` ruff-baseline staleness** -- track-record line historically read "ruff baseline 78/26"; live is now 0 ("All checks passed"). Trivial doc refresh at next housekeeping. (archive: 2026-05-10 "Ruff residual cleanup")
- **`reference/Books/` git-tracking posture** -- operator decision: commit (+~110 MB) / keep-untracked / commit-MD-only. (archive: 2026-05-12 "Minervini" tail)
- **3e.8 sell-side advisory deferred-with-gate items** -- Sec 4.H sector-RS check; Sec 4.I volume-confirmed exit; Sec 4.J combined-violation (all second-source gates); Sec 4.A full + Sec 4.C/4.C.bis time-stop (banked, n>=10-gated). (archive: 2026-05-10 "3e.8 disposition")
- **3e.8 V2 watch items** -- extract shared advisory composer (hand-duplicated across 6 sites; drift-risk); `build_open_positions_expanded` cache-I/O-during-read-snapshot refactor. (archive: 2026-05-11 Bundle 1/2/3 V2 sections)

---

## 2026-06-04 #23 PHASE 15 -- pattern-observation pool widening (aplus -> aplus+watch) SHIPPED + CLOSED; the operator OQ-1 accept-uncapped + OQ-4 first-live-run gate PASSED (Run 89)

**SHIPPED 2026-06-04 `8ee98fc1`** (operator-commissioned 2026-06-04). The FOURTH commissioned Phase-15 arc + the FIRST B-family substrate-augmentation item: widen the nightly `_step_pattern_detect`/`_step_pattern_observe` pool from `bucket=='aplus'` to `aplus+watch` so the Phase-14 v22 temporal observation log accumulates the ~83x watch population as forward-walk learning data -- at ZERO capital risk, ZERO Finviz-screen change, **ZERO schema change (v24 holds)**. Full copowers cycle -- brainstorm `a45d3bc4` -> writing-plans `a78443ce` -> executing-plans `8ee98fc1` (13 commits across Parts A-D; ZERO Co-Authored-By); each phase Codex-converged via the WSL CLI fallback. **7128 fast tests green** on the merged HEAD (+23; the merged head == the suite-tested branch SHA -- a pure ff). Briefs: `docs/pattern-observation-pool-widening-{commissioning,brainstorming,writing-plans,executing-plans}-dispatch-brief.md`; spec `docs/superpowers/specs/2026-06-04-pattern-observation-pool-widening-design.md`; plan `docs/superpowers/plans/2026-06-04-pattern-observation-pool-widening-plan.md`.

**What shipped (the substance is NOT the one-line predicate):** the predicate widen (`aplus_tickers`->`detect_pool_tickers`) + the standardized #27 audit vocabulary; **the consumer isolation** -- the detect step ALSO writes the sibling `pattern_evaluations` (which feeds 3 operator-facing surfaces: the pattern-outcomes tile denominator, the review-form B.4 cohort, the active_learning queue -- a gap the commissioning brief's Q5 premise MISSED, caught by the brainstorm Codex chain). The fix: a PROVABLE-aplus SQL ladder (`swing/evaluation/pe_origin.py:PROVABLE_APLUS_PE_PREDICATE`) isolates those 3 to aplus-origin (the **durable `detection_date` historical gate** -- NOT `MIN(pipeline_run_id)`, which leaks via PDE `ON DELETE SET NULL`, proven UNSOUND at writing-plans -> the spec Sec 6.3 was synced; filter-before-LIMIT for the cohort; NO `OR c.id IS NULL`); the by-ticker/by-id trade backlinks are KEPT (a watch-ticker trade still anchors to its detection -- OQ-7). Provenance rides in the EXISTING `finviz_screen_state` JSON (D4 -- already emitted by construction; NO `source_bucket` column). Two DORMANT relief levers (Lever 1 detect-pool cap [deterministic `rs_rank`, after the empty-guard, cap>=1]; Lever 2 status-aware observe pre-fetch SHED [a no-fetch skip, NOT an `expired` transition -- preserves the `ohlc_today_json` NOT-NULL LOCK-at-observation]) ship OFF (cfg defaults None; accept-and-measure, NO silent cap). The truthful `OhlcvCache` fetch-vs-hit telemetry drained at observe entry (the Codex R1 fix for the shared-cache contamination across detect/charts/observe).

**OQ resolutions (operator-paired 2026-06-04):** OQ-1 V1 UNCAPPED + dormant levers; OQ-2 single FDL snapshot; OQ-3 `actual_pool` + per-bucket breakdown; OQ-4 the 4-part gate; OQ-5 document future-consumer aplus-default; OQ-6 the rename; OQ-7 (Codex-surfaced) isolate the 3 silent consumers + KEEP the backlinks.

**The OQ-4 4-part gate PASSED 2026-06-04:** (1) orchestrator QA vs disk (locks held -- zero migrations/schwab/db.py diff; the ladder faithful + sound; the 3 re-baselined existing tests legitimate-not-weakened; Codex R1[1maj telemetry-contamination]->R2 converged); (2) the seeded measurement (detect ~509ms/watch-ticker; ~2 min projected at 252 watch = the upper bound); (3) the seeded step-smoke (the 3 isolated counts unmoved by an 8-watch flood); (4) **the operator-witnessed first live `swing pipeline run` (Run 89)**. Run 89 had ZERO aplus candidates (skip 87 / watch 11 / excluded 2) -> pre-widen it would have detected NOTHING; post-widen it wrote **55 watch detections** (11 tickers x 5 detectors) -- the exact value-add. **Invisible-widen confirmed on live data: 0 of 55 watch PEs admitted by the aplus ladder.** No cap/shed (uncapped); the only warning is the benign "no observable detections" (the 55 brand-new detections become OBSERVABLE next session -- forward-walk observations accumulate from the next nightly run). Real per-night watch pool here was 11 (NOT the snapshot's 249) -> the widen's marginal CPU was ~6s, far below the upper bound; accept-uncapped comfortably validated.

**Surfaced DURING the gate (BANKED, separate pre-existing issues -- NOT the widen):** (a) the Finviz "screen signature changed since prior run" warning despite NO operator edit since the API setup -- investigate what `signature_hash` (`get_latest_signature_hash`) is computed over (screen QUERY [should be stable] vs CSV RESULT [changes every run -> miscalibrated warning]); (b) `fetch_window_via_ladder: unexpected error from the T-C.1 Schwab market-data wrapper` on EVERY ticker (graceful yfinance fallback -> Run 89 completed; non-fatal) -- the widen only amplified visibility (more tickers fetched). Schwab OAuth was 2 days prior (7-day TTL) so likely NOT token expiry -- a wrapper-level error. Both tracked for investigation.

**Phase 15 remaining:** the Phase-14 cross-sub-bundle integration review + B-1..B-8 strategic items + the 2 banked investigations above.

---

## 2026-06-04 #22 PHASE 15 -- process-grade-trend chart redesign (small-multiples) + the reviews-page nav-date fix SHIPPED + CLOSED; operator browser gate PASSED (incl. a dark-mode gate-fix)

**SHIPPED 2026-06-04 `e9b0292e`** (operator-commissioned 2026-06-03). The THIRD commissioned Phase-15 arc -- a presentation-only web/UI bundle: (1) redesign `/metrics/process-grade-trend` from 7 incommensurate series on ONE grade-labeled axis (plunge-lines) to **small-multiples -- 3 scale-separated inline-SVG panels** (GRADES [0,4] / RATE [0,1] / COST [0,max] 0-anchored, `mistake_cost_R_per_trade` only; `_total` table-only), distinct theme-aware `--series-*` colors + a legend via two-class CSS specificity, the cost-axis [0,1] all-zero fallback, under-floor captions; (2) the bundled one-line `/reviews/pending` nav-date fix (`build_reviews_pending_vm` stamps `session_date=last_completed_session(...)`). Full copowers cycle -- brainstorm `bdde2aa5` -> writing-plans `9d71c771` -> executing-plans `e9b0292e` (5 TDD tasks A1+B1-B4 + 1 inline gate-fix; ZERO Co-Authored-By); each phase Codex-converged. **7105 fast tests green** (+19 this arc). Briefs: `docs/process-grade-trend-redesign-{brainstorming,writing-plans,executing-plans}-dispatch-brief.md`; spec `docs/superpowers/specs/2026-06-03-process-grade-trend-redesign-design.md`; plan `docs/superpowers/plans/2026-06-03-process-grade-trend-redesign-plan.md`.

**LOCKS held:** inline-SVG-only (NO matplotlib/JS-lib; the no-matplotlib route test stays green); presentation-only (metric computations read-only); **NO schema change (v24 holds)**; the A-6 theme-aware stroke + the >=5-sample suppression floor + the decoupled badges + the table all preserved; the **test-preservation contract** held (the GRADES panel kept its exact route-test hooks -- 9 route + 11 VM + 5 segment + 1 CSS tests stayed green untouched; zero assertions weakened).

**Operator browser gate (L6, BINDING) PASSED 2026-06-04:** the orchestrator stood up the branch server (`:8081`) from the worktree (presentation-only -> no live-DB mutation); the operator witnessed legibility (light + dark) + the empty/under-floor default + the fixed nav-date. **One dark-mode defect caught + fixed inline** (a Phase-14-style inline gate-fix): the chart's axis/legend/caption labels are SVG `<text class="muted">`, but `.muted` sets only the CSS `color` property -- an SVG `<text>` is painted by `fill` (default BLACK), invisible on the dark background. Fix = `.process-grade-trend-chart text { fill: var(--fg-muted) }` (theme-aware: #666666 light / #adb5bd dark) + a discriminating CSS test. Operator re-witnessed -> PASS. **GOTCHA for future SVG-text work:** `.muted` (and any `color`-only class) does NOT color SVG `<text>` -- SVG text needs a theme-aware `fill`, or it defaults to black + vanishes in dark mode.

**Phase 15 remaining (tracked):** the Phase-14 cross-sub-bundle integration review + the pattern-observation pool widening (BANKED + scoped, `#21`/Sec 10 of its brief) + B-1..B-8 strategic items (`#5` "Phase 15+").

---

## 2026-06-03 #21 PHASE 15 -- B-7 operator failure-mode classification SHIPPED + CLOSED (the FIRST v24 migration); operator browser gate DONE 2026-06-03 (live DB migrated v23->v24, backup snapshotted)

**SHIPPED 2026-06-03 `d6092c7d`** (operator-commissioned 2026-06-03). The second commissioned Phase-15 arc: a capture-only `failure_mode` field on the CR.1 post-trade review surface, for outcome attribution. Full copowers cycle -- brainstorm `0e8018f2` -> writing-plans `855d187a` -> executing-plans `d6092c7d` (Slice A = ONE atomic #11 commit + Slice B 7 tasks + 1 Codex fix; ZERO Co-Authored-By); each phase Codex-converged via the WSL CLI fallback. **7086 fast tests green** on the merged HEAD (+~33 from B-7's own tests). Briefs: `docs/b7-operator-failure-mode-{brainstorming,writing-plans,executing-plans}-dispatch-brief.md`; spec `docs/superpowers/specs/2026-06-03-b7-operator-failure-mode-design.md`; plan `docs/superpowers/plans/2026-06-03-b7-operator-failure-mode-plan.md`.

**What shipped:** a new nullable CHECK-constrained `trades.failure_mode` column (7 tokens: `thesis_invalidated`, `normal_volatility_stop`, `market_regime_shift`, `adverse_event_shock`, `execution_error`, `failed_to_advance`, `other`; NULL = no attribution) -> **migration 0024, the FIRST v24** since the schwabdev arc (a single ADD COLUMN, no rebuild; strict `_b7_backup_gate` `current==23 AND target>=24`; gotcha-#11 atomic -- CHECK + `FAILURE_MODES` frozenset [in `models.py`, the import-cycle fix] + `Trade.__post_init__` validator + the THREE-ERA read [v24 / v21-v23 (real backlinks preserved) / pre-v21] + the PRAGMA-aware `update_trade_review_fields` write path, ALL in one commit). The review-form `<select>` + the POST validate/thread (`... or None`) + the PRAGMA-aware chronology read-back + the CLI `--failure-mode` option (OQ-8; web/CLI parity, the R1 fix). **Kept ORTHOGONAL** to process-grade + mistake-tags (a guard test proves the computational separation; "good loss" vs "bad loss" now distinguishable). Capture-only V1; the failure-mode analysis/distribution tile is the natural next arc (OQ-5).

**OQs (all 8 operator-resolved 2026-06-03):** OQ-1 new v24 column · OQ-2/7 always-shown/optional/NULL/no-sentinel · OQ-3 single-select · OQ-4 the 7-token vocab · OQ-5 capture-only · OQ-6 forward-only · OQ-8 CLI parity IN.

**DONE -- the operator browser gate (the close-out witness; L6; 2026-06-03):** the operator ran `swing web` -> the live DB migrated v23->v24 (orchestrator-verified post-gate via a read-only probe: `schema_version=24`, `failure_mode` column present, backup `swing-pre-b7-migration-20260604T070044Z.db` written) -> the submit witness PASSED (attributed + unseeded-blank). **ARC FULLY CLOSED.** The §1 live-DB discipline held during dev (the live DB was confirmed UNTOUCHED at v23 at merge time; it migrated only at the operator's conscious gate).

---

## 2026-06-03 #20 PHASE 15 FIRST ARC SHIPPED + CLOSED -- schwabdev v2.5.1->3.0.5 + Fernet (full copowers cycle; the FIRST-EVER L2-LOCK baseline re-anchor); the operator GATE-B live-OAuth cutover DONE 2026-06-03

**SHIPPED 2026-06-03 `7ee04c3`** (operator-commissioned 2026-06-02). The first commissioned Phase-15 arc: migrate off **schwabdev 2.5.1** to **3.0.5** + Fernet token-at-rest. Full copowers cycle -- brainstorm `f7e15b9` -> writing-plans `333367f` -> executing-plans `7ee04c3` (20 TDD commits, ZERO Co-Authored-By); each phase Codex-converged via the WSL CLI fallback (`NO_NEW_CRITICAL_MAJOR`). Executed ENTIRELY in an isolated venv (the operator's live 2.5.1 + token DB untouched); **7053 fast tests green** on the merged HEAD in the venv (schwabdev 3.0.5). Briefs: `docs/schwabdev-v3-upgrade-{brainstorming,writing-plans,executing-plans}-dispatch-brief.md`; spec `docs/superpowers/specs/2026-06-02-schwabdev-v3-upgrade-design.md`; plan `docs/superpowers/plans/2026-06-02-schwabdev-v3-upgrade-plan.md`.

**What shipped:** token storage JSON-file -> v3 schwabdev-internal SQLite (`tokens_file=`->`tokens_db=`); the W-A writer owns a pinned 8-col DDL guarded by the T1b DDL-drift test (makes the `>=3.0.5,<4.0.0` floored pin safe); the COMPREHENSIVE non-setup preflight (`_assert_v3_tokens_db_loadable_or_raise`) + the non-interactive construction guard with deterministic `del`+`gc.collect()` lock release (the v3 construct-at-init `input()` hazard); the `revoke_and_delete` logout rewrite (read+decrypt the refresh token from SQLite; rename-first/best-effort-revoke); `account_linked`->`linked_accounts`; **Fernet-at-rest ON by default** (key in `user-config.toml`, masked, enc:-prefix-driven, key-loss = re-setup) -- retires the plaintext-OAuth gotcha; **DELETED the P14.N7 daemon checker + the F-1 topbar badge** (v3 refreshes per-request; `/schwab/status` page preserved, re-sourced off the v3 presence-only reader); the six Schwab gotchas re-validated on 3.0.5 (G1-G6); `aiohttp<3.12` pinned (vcrpy stub break). **NO swing-DB schema change (v23 held).**

**The FIRST-EVER L2-LOCK baseline re-anchor:** `L2_LOCK_BASELINE_SHA` `bf7e071`->`9d05a8f` (`tests/integration/test_l2_lock_source_grep.py`). Reason: the migration churns docstrings/comments embedding `schwabdev.Client(` (NOT new call sites). `docs/schwab-v3-endpoint-diff.md` (9 endpoints, pre==post) proves ZERO new Schwab REST endpoints; operator-signed at GATE A. The lock's SPIRIT (zero new endpoints) preserved; the escalation rule stands.

**DONE -- the operator GATE-B live-OAuth cutover (2026-06-03):** the operator ran the global `pip install -e ".[dev,web]"` (shared env now schwabdev 3.0.5) + `swing schwab logout` -> `setup` (real OAuth; v3 SQLite DB; Fernet key auto-generated) -> `status` -> `fetch` -> `swing web` (UNSEEDED no-badge default witnessed) -- ALL PASSED; web server stopped (ports verified free). Post-cutover **shared-env fast suite 7053 passed / 0 failed** (v3 code on v3 lib, the operator's real env). **ARC FULLY CLOSED.** (Rollback path, unused: revert the migration + reinstall 2.5.1 + re-setup; low blast radius, no swing.db change.)

**PHASE 15 remaining (tracked):** B-7 operator failure-mode classification; the process-grade-trend chart redesign; the Phase-14 cross-sub-bundle integration review; B-1..B-8 strategic items (`#5` "Phase 15+").

---

## 2026-06-02 #19 PHASE 14 CLOSED -- all feature sub-bundles (SB1-SB5 + SB5.5) + the close-out polish batch + the follow-on bundle SHIPPED end-to-end; schema v23; ~7038 fast tests green; the 3 remaining close-out-tail items MOVED TO PHASE 15 (operator-decided 2026-06-02); CLAUDE.md line-3 compact-summary restructure + orchestrator-context in-flight refresh done in this pass

**PHASE 14 CLOSED 2026-06-02 #19** (operator-decided). Phase 14 -- a UX + wiring + methodological-infrastructure phase (NOT a ruleset-deployment phase, per the 2026-05-27 applied research arc closure) -- lands at **schema v23**, **~7038 fast tests green on main** (`ef4fbf5`), **~700+ commits ZERO Co-Authored-By**, **L2 LOCK preserved (zero re-anchor -- the schwabdev v3 upgrade is the first)**.

**SHIPPED end-to-end (per-cycle records in `#1`-`#18`):** SB1 data-wiring `e323339` · SB2 temporal-log (v22) `27f8007` · SB3 chart-surface (v23) `edd098d` · SB4 review+journal `31da4a5` · SB5 metrics-overview `6206fb6` · SB5.5 Schwab (A-3 + P14.N7) `16b3366` · close-out polish batch (P14.N1 both tables + A-1 + A-2 + A-4 + A-6 + A-7 + group-(a) C-1/C-2/C-3/C-5/C-19) `f2cd376` · follow-on bundle (F-1 web-checker + F-2 trend + F-3 polylines + F-4 spines) `77388b3`. Operator-witnessed gates throughout; two inline gate-fixes (`718c403` hx-target row-collapse; the F-1 `web_cmd` apply_overrides fix that made the web Schwab badge LIVE in normal operation).

**3 remaining close-out-tail items MOVED TO PHASE 15 (operator-decided 2026-06-02):**
1. **Process-grade-trend chart redesign** (the F-3 re-diagnosis follow-on, `#18`) -- non-grade metrics (`mistake_cost_R` x2, `disqualifying_violation_rate`) overlaid on the A-F grade axis produce confusing plunge-lines; separate panel/scale, restyle/dash, or table-only. Phase-13-era surface; UX polish.
2. **B-7 operator failure-mode classification** (was the Phase 14 FINAL TOUCH) -- extend the CR.1 review surface for operator-annotated failure reasons; may add a nullable review column (v24) or reuse existing.
3. **Phase 14 cross-sub-bundle integration review** (Sec 9.1 Q6) -- operator browser-witnessed coherence check across charts + review/journal + metrics overview + the Schwab surface; runs early in Phase 15 as a retrospective integration check.

**"Phase 14 CLOSED" housekeeping (THIS pass):** CLAUDE.md line-3 compact-summary restructure (the giant Phase-14 ledger -> a ~5-10-sentence pointer; full per-cycle history preserved in `#1`-`#18` + `docs/CLAUDE.md-archive.md`) + the "Current baseline" + restructure-note refreshed; orchestrator-context.md in-flight section refreshed to "Phase 14 CLOSED" (was stale to 2026-05-23 #3, pre-Phase-14) + lessons-cap trimmed. Repo cleanup also done this session (64 dead local + 18 stale remote branches pruned; scratch removed).

**PHASE 15 (uncommissioned; tracked):** the schwabdev v2.5.1->3.0.5 upgrade + Fernet (`#9`; first L2-LOCK baseline re-anchor; deletes the daemon checker + P14.N7; F-1's diagnosis feeds it) · B-7 · the process-grade-trend chart redesign · the Phase-14 cross-sub-bundle integration review · B-1..B-8 strategic items (substrate-size, Finviz-widening, cohort-stability, survival-rate, real-time tracking, multi-pattern composites, other-gates market-regime) per `#5` "Phase 15+". Phase 15 commissioning is an operator decision.

---

## 2026-05-30 #5 Phase 14 close-out + NEW Sub-bundle 5.5 (Schwab-focused) punch-list -- banked-items consolidation (operator-decided 2026-05-30)

**Purpose:** consolidate every banked/orphaned Phase-14-scope item into ONE close-out tracker so nothing is lost. Compiled via a full sweep of the commissioning brief (Sec 1 deferred-list + Sec 8 forward-look) + the 4 SB executing-plans return reports + the SB1-SB4 phase3e-todo entries. Operator bucketing decisions dated 2026-05-30.

**Updated Phase 14 forward sequence:** SB5 (metrics overview; P14.N5; brainstorm in progress) -> **NEW SB5.5 (Schwab-focused)** -> Phase 14 close-out polish batch + B-7 final touch -> Phase 14 close-out review (Sec 9.1 Q6: all sub-bundles merged + operator browser-witnessed cross-sub-bundle integration).

**NEW Sub-bundle 5.5 -- Schwab-focused (operator-decided 2026-05-30):**
- **A-3 Schwab daily-bar web wiring** (SB3 follow-up #4): the web-side SMA/daily-bar path (`OhlcvCache`/`PriceCache` constructed plain in `swing/web/app.py`) is yfinance-only by wiring; the Schwab market-data ladder (`swing/integrations/schwab/marketdata_ladder.py`, gated `env==production AND marketdata_ladder_enabled`) is pipeline-side + NOT installed on the web caches. Wire it onto the web SMA path. **Touches the L2-locked Schwab surface** -- A-3 installs the EXISTING ladder (intended ZERO new `schwabdev.Client.*` call SITES beyond the ladder's own); confirm the L2 framing at SB5.5 brainstorm.
- **P14.N7 schwabdev background `checker`-thread resilience**: schwabdev's background token-refresh `checker` thread dies with uncaught `ConnectionError`/`NameResolutionError` under sleep/wake/DNS-failure cycles -> silent token-refresh degradation until `swing web` restart. Wrap/replace the checker loop with exception-isolation + retry-with-backoff; add an operator-visible degraded-health surface (`swing schwab status` checker-liveness); discriminating test simulates DNS failure during refresh.
- Coheres around the Schwab integration surface; both infrastructure/resilience; L2 LOCK discipline central. Its own copowers cycle.

**Phase 14 close-out polish batch (small, Phase-14-scope; sequence after SB5/SB5.5):**
- **P14.N1 (dashboard portion -- the orphaned bit)** -- open-positions + hyp-rec TABLE thumbnails. Substrate + the render-direct helper `render_trade_window_thumbnail_svg` (`swing/web/trade_charts.py:87`) exist; SB4 proved the journal-listing VM->row-template pattern; wire onto `open_positions_row.html.j2` + `hypothesis_recommendations_row.html.j2` + their VMs. Banked at SB4 OQ-3 (operator chose "journal listing only"). (P14.N1 substrate [SB3] + journal-listing thumbnails [SB4] already SHIPPED; this is the remaining dashboard-table portion.)
- **A-1 market_weather 200MA fetch-window** (SB3 #2) -- widen the benchmark fetch window to >=200 trading bars (~300 calendar days) at both sites (pipeline `_bars_or_none(window_days=200)` + the refresh handler's `get_or_fetch(ticker=benchmark)`) + a regression asserting >=200 bars reach `render_market_weather_svg`.
- **A-2 theme2_annotated vcp 5-contraction label crowding** (SB3 #3) -- cosmetic; reposition the right-edge labels off the price y-axis ticks.
- **A-4 `_bulz_*` -> general rename** (SB4) -- cosmetic; rename `_bulz_target_price`/`_draw_bulz_zones` (`swing/web/charts.py:609,701`) -> general (`_rr_target_price`/`_draw_risk_reward_zones`) + comments/WARN text; the zones are a GENERAL open-long-position feature, NOT BULZ-specific.
- **A-6 process-grade-trend drill-down chart dark-mode invisibility** (NEW; found at the SB5 S3 gate 2026-05-31; pre-existing, NOT SB5) -- the `/metrics/process-grade-trend` chart's rolling-line `<polyline>` + per-trade `<circle>` markers use SVG-default/unstyled colors (`process_grade_trend.html.j2:39-60`: no `stroke=` on the polyline; NO CSS rule anywhere for `.process-grade-rolling-line`/`.process-grade-marker`), so they vanish against the dark background (visible in light mode only). Fix: give the chart elements theme-aware stroke/fill with dark-mode tokens (mirror the SB5 overview sparkline's `var(--accent)` which has a dark value `#6ab0ff`). Cosmetic; Phase-13-era surface.
- **A-7 schwab web health badge not rendering in normal operation** (NEW; operator-reported 2026-06-01, post-SB5.5 ship) -- the topbar checker-health badge is INVISIBLE in normal browser use because `build_schwab_checker_badge` returns `None` (hides the badge) whenever no liveness sidecar exists at `~/swing-data/schwab-checker-liveness.<env>.json`, and no real checker is writing one (the operator's production Schwab client degrades -- likely expired refresh tokens -- so the daemon checker never starts). NET: the badge vanishes exactly when Schwab integration is DOWN, defeating its purpose (the operator must fall back to `swing schwab status` for any visibility). **Fix-investigation (resolve at the polish-batch brainstorm):** (i) DESIGN -- should the web badge render a VISIBLE "no checker / UNKNOWN" state (the `Schwab?` warn entry already in `_BADGE_MAP`, currently UNREACHABLE from the web path because build returns None first) instead of hiding, so the dashboard always surfaces Schwab health? AND/OR (ii) WIRING -- confirm whether the checker SHOULD be running under the operator's actual production tokens (if tokens are valid but the checker still does not start/write the sidecar, that is a real A-3/P14.N7 wiring bug, not just the by-design hide). **Caveat:** the SB5.5 S6 gate only witnessed the RENDER path via orchestrator-SEEDED sidecars (then cleaned up); the live no-checker-no-badge behavior was NOT flagged at the gate -- this is the gap that surfaced once the synthetic sidecars were removed. Refs: `swing/web/view_models/schwab_checker_badge.py` (the `data is None -> return None` hide); `evaluate_liveness_state` (`UNKNOWN` only when `data is None`); the SB5.5 spec/plan web-badge contract.
- **Group (a) open minor advisories (operator-added to the close-out batch 2026-05-30; C-1/C-2/C-3/C-5/C-6/C-19):** (1) `DailyManagementTileVM` PROVISIONAL flag defaults to `True` -> flip to `False` or make the field required (SB1); (2) daily-management tooltip "covers today" -> "covers this row's session date" (SB1); (3) backfill CLI artifact-write `OSError` -> wrap to `ClickException` instead of a raw traceback (SB1); (4) strengthen the `BEGIN IMMEDIATE` ordering regression test to assert lock-vs-SELECT/UPDATE order (SB1); (5) narrow the backfill apply-path write-lock so it is NOT held during the restore-artifact filesystem write (SB1); (6) harden the cross-SB `test_ohlcv_reader_re_export_identity` xdist co-residency flake (e.g. `@pytest.mark.xdist_group`; passes in isolation). All small; sources in the SB1 executing-plans return report §2 + the cross-SB flake note.

**B-7 operator failure-mode classification UI -- Phase 14 FINAL TOUCH (operator-decided 2026-05-30):** extend the CR.1 review surface to capture operator-annotated failure reasons (e.g. "stopped out on volatility", "thesis invalidated", "execution issue") for outcome-attribution analysis. Likely a small dedicated cycle (new review field(s) + UI) -- assess schema impact at its brainstorm (may add a nullable review column -> v24 STRICT `pre_version`, or reuse existing). Sequenced as the LAST Phase 14 feature before close-out review.

**A-5 styled full-page 404 (SB4 FIX-5) -- CLOSED (operator 2026-05-30, "no intention to revisit").** The JSON 404 stands. Removed from the punch-list.

**Phase 15+ (tracked; NOT Phase 14; depend on temporal-log accumulation / strategic decisions):** B-1 substrate-size augmentation; B-2 Finviz filter widening; B-3 cohort-stability LOCK (gotcha #37 -- already eliminated-by-construction in the v22 temporal log); B-4 D2 survival-rate remediation; B-5 real-time prospective tracking; B-6 multi-pattern composites; B-8 other-gates market-regime investigation.

**Minor V2 items (C-1..C-19):** **Group (a) -- the ~6 genuinely-open minor advisories -- MOVED INTO the close-out polish batch above (operator 2026-05-30).** Remaining OUT of close-out: ~3 accepted-as-LOCKed-by-design (SB2 schema-version test NAMES for grep-history; observe read-then-write benign under single-lease; repo fixture `ohlc_today_json` shape nit) + ~10 already-fixed forward-binding lessons captured for reference (mpf x-axis padding `margins(x=0)` / volume-label auto-scale; `last_completed_session` returns `date`; post-close read-connection; fill-marker nearest-forward x-placement; index-coercion-rejects-numeric; synthetic-bars-use-planted-detections; derived-duration state-predicate). Full detail in the per-SB executing-plans return reports §"V2 candidates" / §"forward-binding lessons". Groups (b)+(c) require NO close-out action.

**Forward action (UPDATED 2026-06-02 -- close-out tail status; per-cycle ship records in `#6`-`#18`):**

- [x] SB5 (metrics overview; P14.N5): brainstorm `3c18b81` -> writing-plans `9635d17` -> executing-plans `6206fb6` (SHIPPED `#6`/`#7`/`#8`)
- [x] SB5.5 (Schwab A-3 + P14.N7): brainstorm `4205b63` -> writing-plans `1bd1558` -> executing-plans `16b3366` (SHIPPED `#10`/`#11`/`#12`)
- [x] **Close-out polish batch** (P14.N1 BOTH tables [open-positions journal-route reuse + hyp-rec `render_watchlist_thumbnail_svg`] + A-1 + A-2 + A-4 + A-6 + A-7 + group-(a) C-1/C-2/C-3/C-5/C-19): brainstorm `83043ba` -> writing-plans `30ce9d4` -> executing-plans `f2cd376` (SHIPPED `#13`/`#14`/`#15`; orchestrator-inline gate-fix `718c403` = open-positions thumbnail `hx-target` row-collapse)
- [x] **Close-out FOLLOW-ON bundle** (F-1 P14.N7 web-checker [the A-7 wiring fix -- `web_cmd` now applies `apply_overrides`; the healthy Schwab badge is now LIVE on main] + F-2 market-weather trend live-compute [two-tier `structural_checks`/`structural_stage` helper] + F-3 segmented polylines + F-4 thumbnail spines): brainstorm `6836aa5` -> writing-plans `48fcbfe` -> executing-plans `77388b3` (SHIPPED `#16`/`#17`/`#18`)
- [-> PHASE 15, `#19`] **process-grade-trend chart redesign** (the F-3 re-diagnosis follow-on) -- MOVED TO PHASE 15 (operator-decided 2026-06-02).
- [-> PHASE 15, `#19`] **B-7 operator failure-mode classification** (was the Phase 14 FINAL TOUCH) -- MOVED TO PHASE 15 (operator-decided 2026-06-02); assess schema (v24 vs reuse existing) at its Phase-15 brainstorm.
- [-> PHASE 15, `#19`] **Phase 14 cross-sub-bundle integration review** (Sec 9.1 Q6: operator browser-witnessed coherence across charts + review/journal + metrics overview + the Schwab surface) -- MOVED TO PHASE 15 (operator-decided 2026-06-02); runs early in Phase 15 as a retrospective check.

**DEFERRED / banked (NOT in the remaining tail):**
- **C-6** (backfill apply-path write-lock narrowing) -- reopens a deliberate `BEGIN IMMEDIATE` TOCTOU + crash-safety ordering (`backfill_trades_sector_industry.py:115-128`); deferred `#14`/`#17`/`#18` to its own follow-up if ever pursued.
- **A-5** (styled full-page 404) -- CLOSED (operator, no revisit).
- **schwabdev v2.5.1 -> 3.0.5 upgrade + Fernet** -- PHASE 15 (`#9`); deletes the daemon checker + P14.N7; F-1's diagnosis feeds it; first L2-LOCK baseline re-anchor.

**PHASE 14 CLOSED 2026-06-02 (`#19`).** The 3 former close-out-tail items (process-grade-trend chart redesign + B-7 + the Sec 9.1 Q6 cross-sub-bundle integration review) were MOVED TO PHASE 15 (operator-decided 2026-06-02); Phase 14 lands at v23 on the shipped state. ALL Phase 14 feature sub-bundles + the close-out polish batch + the follow-on bundle SHIPPED end-to-end.

---
