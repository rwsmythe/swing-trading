# Phase 22 — Arcs 22-I (the REPLACE-exposure sweep, residual) and 22-F (the handler-escape + transaction-hygiene sweep) — commissioning brief

**STATUS: COMMISSIONED — DISPATCHED 2026-09-24 (CHARC, on the operator's concurrence).** The operator concurred with CHARC's re-sequencing recommendation and directed "Perform the resequencing and kick off the work" (2026-09-24 ~17:40Z, CHARC session). **The re-sequencing of record is in [`phase22-scope-charc.md`](phase22-scope-charc.md) §"RE-SEQUENCED 2026-09-24"**: the Tier-3 sweeps move AHEAD of the live-case-gated 22-C/22-D (the book is flat since the 09-17 go-flat; no case exists and none is forecastable), in the order **22-I → 22-F → (22-H after RD's October read)**; 22-C/22-D keep their live-case gate; 22-G stays banked (D35/D47/D71, browser-only, needs a witness rung the 10-03..10-20 pause forbids).

**From:** CHARC. **To:** the orchestrator. **Stage:** WRITING-PLANS for 22-I now, under the PLAN-STAGE PROTOCOL (harness §5.1): premise re-verification + fork census FIRST (no Codex), ONE packet, directors rule in ONE pass each, no round while a ruling is outstanding, plan ≤ ~1,000 lines, ≤ 3 `fast` rounds then a self-sweep. **22-F's plan cell dispatches when 22-I's plan is ACCEPTED** (serial plan loops: both are read-heavy census work and the orchestrator seat QAs one at a time; the D67+D69 rider cell in flight is unaffected). **Plan cells:** `implementer-opus-high` for both (census + one migration shape / census + two ruled fix shapes; every design fork this brief could foresee is ruled in §3). **Executing cells:** `implementer-opus-high`; Reviewer A to convergence per the recipe; Reviewer B at the ORCHESTRATOR's gate under DEFAULT TO INTRODUCED; CHARC merge gate per the 22-B packet shape (B's transcript on the tree being merged with its five assertions; the merged-head suite with SHA; for 22-I the D51 diff and the manifest composition proven by execution on a live-DB copy under the trial code).

**Rules read from main @ `b8f80e91`:** `docs/implementer-dispatch-recipe.md`, `docs/harness-architecture.md` §5.1 (THE B GATE, DEFAULT TO INTRODUCED, the accepted-limitations declaration WITH reasons and the invitation to challenge, the round-gate depth reading, DESIGN IS SETTLED BEFORE THE PLAN LOOP OPENS), the CLAUDE.md §Gotchas SQLite block (REPLACE fires no DELETE trigger; a `BEFORE INSERT` trigger cannot see the conflict algorithm; a NULL `WHEN` fails OPEN; Python/SQLite round differently). A worktree's own recipe is stale by design; the dispatch names main's copies by absolute path.

---

## §0 READ FIRST (pointers — the cell LIFTS, it does not re-derive)

1. **22-I's authority:** [`phase22-scope-charc.md`](phase22-scope-charc.md) §TIER 3 "22-I: THE REPLACE-EXPOSURE SWEEP" — the convention (an append-only guard ships as a TRIPLE `no_update` + `no_delete` + conflict-scoped `no_replace`, plus the NULL-`WHEN` coverage check), the measured-probe standard (control + test, live-DB copy, default `recursive_triggers`), the TWO PRICED COSTS (a `BEFORE INSERT` trigger cannot distinguish `ON CONFLICT DO NOTHING` from `OR REPLACE`, so the WRITER is adjusted with a `WHERE NOT EXISTS` pre-filter and the barrier is NEVER narrowed; an omitted `INTEGER PRIMARY KEY` presents as `-1` inside `BEFORE INSERT`, so the PK clause reads `(NEW.<pk> != -1 AND <pk> = NEW.<pk>)` and NEW tables carry `CHECK (<pk> > 0)`), and the three-direction discriminating set per site (ordinary append SUCCEEDS · conflicting REPLACE ABORTS · explicit conflicting id ABORTS). RD's framing binds: a REPLACE-mutable audit table makes the audit-trail check a writer-absence claim one level up.
2. **22-F's authority:** charter §4 rows **D34** (three instances, three subsystems: `SchwabSchemaParityError`'s MRO is `_RedactedMessageError → RuntimeError`, NOT `SchwabApiError`, `swing/integrations/schwab/client.py:336/:401` on main today; `ReservedJournalFieldError(Exception)` with ZERO `except` sites in `swing/` on main today, grep stated in §1; D35's `:139` `IntegrityError` past a `ValueError` handler) and **D39** (the transaction-hygiene cluster: `BEGIN IMMEDIATE` outside `try`, narrow cleanup catches suppressing `sqlite3.Error` on rollback, the double-failure leak; the 22-A fix shape is the template — catch the rollback failure explicitly, log that the connection must be discarded, raise the cleanup error CHAINED from the original, a both-verbs-fail proxy test per site; plus R8 item 1's sibling from 22-B: the entry command's c2 pre-boundary close re-raise masking a refusal). The fix shapes are ALREADY RULED and land as citations, never re-designed: the **22-A3 post-durability sink** (`_echo_either_sink` / `log_contained`, degraded-success over a durable row, never a 500), **RULING R8's chained refusal** (the typed REFUSED text survives a cleanup failure, the cleanup error NAMED and CHAINED), **R1-3-SURFACES** (CONTAIN on secondary panels, DEGRADE at 200 on governed pages — a 500 is never a refusal, D34), and **the ENTRY-vs-LABEL test** (harness §5.1) for which refusals may block a money-bearing entry.
3. **The 22-B precedents the plan lifts:** `docs/phase22-arc-b-commissioning-brief.md` (the ruling-line blocks (1)–(8): the durability boundary re-evaluated at each handler, never cached; two durability facts in `trade review`; AL-6/AL-7 as declared limitations with reasons); its exec ledger on main `docs/superpowers/plans/2026-09-23-phase22-arc-b-unintended-execution.exec-ledger.md` (the B GATE packet shape, the witness table, the W0–W4 runtime composition read as corrected: ONE gate image in `backups_dir`). Migration precedents for 22-I: `0037` (the first no_replace barriers + the `-1` idiom), `0039`/`0040` (the D51 manifest as the instrument; `PRAGMA foreign_key_check` after any rebuild — 22-I adds triggers ONLY and rebuilds NOTHING).
4. **The register rows that ride:** D46 (the log-containment interface — hand the logger a RENDERED STRING; 22-F's containment sites use the string form, they do not extend the object-hardening machinery), D68 (the exit route's any-exception rebuild — IN 22-F's census as an instance; fix under R1-3-SURFACES' contain shape), D69 (backfill-intent — ALREADY dispatched as a rider; 22-F does not touch it; if its cell is still open at 22-F's execution, 22-F's census lists it as "owned by the rider"), D70 (AL-6/AL-7 are declarations, not 22-F targets), D58 (the pre-migration snapshot fence — NOT in 22-I; declared).

## §1 LIVE STATE — MEASURED 2026-09-24 ~17:30Z at v40 by CHARC (`mode=ro`; re-measure at the plan cell's census, state any delta)

**Schema (22-I):** 9 tables carry triggers, 43 triggers. The TRIPLE is COMPLETE (`no_update` or an immutable-column twin, `no_delete`, `no_replace`) on SIX tables: `candidates`, `entry_intent_attestations`, `fill_envelope_identity`, `latch_order_intents`, `latch_order_mandate_links`, `latch_view_events`. **`candidates_immutability_epoch`** carries `no_insert` + `no_update` + `no_delete` (a single-row table; `no_insert` subsumes `no_replace` — verify by probe, do not assume). **`provenance_corrections`** carries `no_delete` + `no_replace` + a CONDITIONAL `append_only_update` (the fill-pointer clearing exception) + the citation-graph trigger. **`trades`** carries `attempt_id_immutable` + the three `entry_intent` triggers (not an append-only table; NOT in class). Every `no_replace` trigger's `WHEN` is an `EXISTS (SELECT …)` — `EXISTS` never evaluates NULL, so the NULL-`WHEN` hazard does not reach that family; **the `WHEN`-clause triggers that DO carry the hazard shape are `trg_provenance_corrections_append_only_update` and `trg_provenance_corrections_citation_graph` (both `WHEN NOT ( … )` without the `COALESCE` wrap; the 2026-08-25 gotcha was found on the citation trigger and remedied per-field — the sweep PROVES every field's `IS NOT NULL` coverage by probe, one absent key at a time) and `trg_latch_link_mint_on_acceptance` (`WHEN NEW.intent_kind = 'validity' AND … IS NOT NULL` — a MINTING trigger that fails open to "no mint"; classify, do not assume benign).** `PRAGMA recursive_triggers` = 0 on live (the default the REPLACE bypass depends on).
**Declared-append-only or immutability contracts WITHOUT any SQL guard (docstring/LOCK only — the class 22-I closes):** the repo files that declare one, by grep (`append-only|immutab|UPDATE-in-place ONLY|NO INSERT OR REPLACE` over `swing/data/repos/`): `account_equity_snapshots`, `chart_renders`, `daily_management`, `pattern_detection_events`, `pattern_evaluations`, `pattern_exemplars`, `pattern_forward_observations`, `pipeline_step_timings`, `reconciliation` (runs + discrepancies), `reconciliation_corrections`, `schwab_api_calls`, `trades`, `watchlist_close_track`, `yfinance_calls` — **fourteen files, of which the six with triggers above are already guarded; the census names the exact table list and, per table, its contract as WRITTEN (append-only vs UPDATE-in-place-only vs caller-tx-only).** `schwab_api_calls` and `yfinance_calls` are UPDATE-in-place BY DESIGN (`record_call_finish` closes the row), so a blanket `no_update` is WRONG there — a column-scoped immutability twin at most. **Conflict-resolution writers in production code (the priced cost's targets):** `latch_order_intents.py:147` `ON CONFLICT(idempotency_key) DO NOTHING` (already paired with a `WHERE NOT EXISTS` pre-filter per its docstring — verify), `pipeline_step_timings.py:45` `ON CONFLICT(run_id, ordinal) DO NOTHING`, `recommendations.py:18` / `watchlist.py:18` / `weather.py:16` `ON CONFLICT … DO UPDATE` (upsert-by-design tables, NOT append-only — out of class unless the census finds a contract saying otherwise). ZERO `INSERT OR REPLACE` / `REPLACE INTO` / `OR IGNORE` in production code (grep, 45 hits, all prose).
**Handlers (22-F):** 78 custom exception classes in `swing/` — 27 on `RuntimeError`, 26 on `Exception`, 13 on `ValueError`, 3 on `_RedactedMessageError(RuntimeError)` (`SchwabConfigMissingError`, `SchwabPipelineActiveError`, `SchwabSchemaParityError`), 3 on `SchwabApiError`, the rest nested. Operator-path handler tuples: 38 `except (…)` sites across `swing/web/routes/` + `swing/cli.py` (the modal tuple is `(TypeError, ValueError)` ×19); 45 broad `except Exception`/`BaseException` sites there. `swing/web/app.py:149` registers an app-level `@app.exception_handler(Exception)` — so on the web an escape presents as the generic 500 PAGE, never a traceback; on the CLI it presents as a traceback. `ReservedJournalFieldError`: 7 raise sites, 0 `except` sites (unchanged since D34). **D39:** `BEGIN IMMEDIATE` appears in 39 files (157 textual occurrences, prose included); 17 rollback-in-`except` sites. The D39 row's named pre-existing sites (`entry.py`, `entry_date_correction.py`, `reconciliation.py`, `reconciliation_auto_correct.py`, `schwab_reconciliation.py`, `cli.py`, `routes/latches.py`) all still carry the string on main — the census measures which still carry the SHAPE.
**Live DB:** v40, 28 trades / 0 open, `foreign_key_check` []; `swing web` on merged code (pid 40608). Last pipeline run 183 complete; the first v40 run is tonight 17:30 HST (the orchestrator's warning-class comparison and RD's T1 check ride it).

## §2 SCOPE SHAPE — CHARC's rulings (stated so the census does not re-open them)

**22-I (schema; §3 tripwire CROSSED by construction — this brief IS the architecture pass; migration `0041`, triggers only, NO rebuild):**
1. **Enumerate, then probe, then guard.** The census lists every table with a stated append-only / immutability / UPDATE-in-place contract (the fourteen files above are the seed, not the answer), the guard it carries on v40, and the writer(s) that reach it. Every EXISTING guard is PROBED by the measured standard on a backup-API copy of the live DB (control + test): ordinary append succeeds · `INSERT OR REPLACE` on a live UNIQUE/PK conflict ABORTS · an explicit conflicting id ABORTS · a `DELETE` ABORTS · an `UPDATE` of a guarded column ABORTS · **and for every `WHEN`-clause trigger, one probe per predicate input with that input NULL/absent, asserting the trigger still FIRES** (the NULL-`WHEN` coverage check). A guard that passes every probe is CONFIRMED and listed; a guard that fails a probe is a FINDING fixed in `0041`.
2. **Which unguarded tables GET the triple is RD's ruling, not the plan's** (fork F-I1, §3): tables that are MEASUREMENT EVIDENCE or AUDIT TRAIL (RD's lane — the audit-trail check is a writer-absence claim one level up) get the triple in `0041`; tables that are caches or derived state (e.g. `chart_renders`, `pipeline_step_timings`) get a DECLARED residual row in the plan's accepted limitations WITH the reason, never a guard for its own sake. The census hands RD the table list with each contract quoted; RD returns the partition in one packet.
3. **A production `ON CONFLICT` writer on a table that receives the triple is ADJUSTED, never the barrier narrowed** (the priced cost): `WHERE NOT EXISTS` pre-filter, `ON CONFLICT` retained as the belt, the writer's observable contract pinned by tests that do not move with the repair. `pipeline_step_timings.py:45` is the one known candidate; its disposition follows RD's partition.
4. **`0041` is ADDITIVE (CREATE TRIGGER only), carries the D60 declaration ("pre-existing rows replay unchanged: a trigger does not touch stored rows"), the `-1` PK idiom, and `WHEN NOT COALESCE(<predicate>, 0)` on every `WHEN` it writes.** The D51 manifest fixture moves by exactly the added objects (ADDED = the new triggers, CHANGED = the header only, DELETED = 0 — the gate reads that shape). `EXPECTED_SCHEMA_VERSION` → 41; a `BackupGateSpec(40, "22i", …)` row in `_PRE_MIGRATION_BACKUP_GATES` (one row, the table is the single source; the CLI takes no copy when the gate covers — the 09-24 W3 lesson).
5. **The provenance_corrections `WHEN NOT (…)` pair is in the census as the FIRST probe target** — the 2026-08-25 finding was fixed per-field; the per-field remedy is sufficient ONLY if EVERY field carries it, and that is exactly what the probe proves or refutes. If either trigger fails an absent-key probe, its fix in `0041` is `DROP TRIGGER` + `CREATE TRIGGER` with the `COALESCE` wrap (a trigger edit is a new object under D51; D60's declaration applies).
6. **OUT of 22-I:** D58 (the snapshot fence — declared, its own item); any rebuild; any writer beyond the `ON CONFLICT` adjustment; `trades` (not append-only); the epoch table's semantics (22-A2's, closed).

**22-F (code only; NO schema; NO new module — a shared helper, if the plan wants one, lives in an EXISTING module named in the plan; a new module is a §3 tripwire and returns to CHARC before dispatch):**
7. **Two classes, one census, one arc.** Class 1 (D34): for every operator path — every route in `swing/web/routes/` and every command in `swing/cli.py` / `cli_schwab.py` / `cli_config.py` — the census lists the service calls it makes, the exception TYPES those services raise (by READ of the raise sites and the MRO, not by grep of the except clause — the SchwabSchemaParityError lesson), the handler tuple at the path, and the DIFF: types that escape. Class 2 (D39): for every site holding a transaction on an operator path, the three facets — `BEGIN IMMEDIATE` outside `try`; a cleanup catch narrower than `sqlite3.Error` (or `BaseException` where the 22-A3 shape applies); a double-failure path that reports only the first error or leaks the transaction — plus the close-after-commit facet (22-B R7 item 3 / R8 / B-02: a close raise after a durable write reports the wrong verdict). One census table, both classes, every row with file:line and the reachability evidence.
8. **The fix shapes are CITATIONS, not designs.** An escaping type on a web path → the typed refusal the surface was written to give (4xx with the message, or the R1-3-SURFACES contain/degrade shape when the escape is on a post-commit or secondary panel); on a CLI path → `click.ClickException` with the message (the CLI boundary rule). A D39 site → the 22-A fix shape (chained cleanup error, discard-the-connection log, both-verbs-fail proxy test). A close-after-durable site → the two-durability-facts handler (22-B B-02's encoding in `trade review` is the reference implementation). **The ENTRY-vs-LABEL test decides which refusals may block a money-bearing entry; the plan cites it per site rather than re-arguing it.**
9. **Introduced-vs-banked applies INSIDE the sweep too:** the sweep OWNS the class, so every census row is IN SCOPE — but a row whose fix would change BEHAVIOUR THE OPERATOR RELIES ON (a refusal that today lets an entry proceed, or vice versa) is a FORK to RD, not a fix. Cosmetic 500-to-typed conversions are not forks.
10. **D68 (the exit route's any-exception rebuild) is fixed in 22-F** under the R1-3-SURFACES contain shape (its ruling is already written, brief block (2)). D69 is the rider's. D46's interface fix (string, not object) is NOT 22-F's arc — 22-F's NEW containment sites simply use the string form.
11. **22-F needs NO witness rung**: no schema, no migration, TestClient and the CLI runner reach every typed response, and the merge is code-only (no `EXPECTED_SCHEMA_VERSION` move, so a running `swing web` and the nightly are unaffected until restarted). **It may therefore MERGE DURING THE 10-03..10-20 PAUSE.** The operator's browser look at two or three converted surfaces is a post-merge courtesy check after 10-20, not a gate. RD CC on this ruling; dissent goes up.

## §3 FORK CENSUS — the forks this brief foresees, RULED or ROUTED

- **F-I1 — WHICH unguarded tables are evidence (RD rules; ONE packet from the census).** Branches the census must present per table: (a) EVIDENCE/AUDIT → the triple in `0041`; (b) DERIVED/CACHE → declared residual with reason; (c) UPDATE-in-place-by-design → `no_delete` + `no_replace` + a column-scoped immutability twin for the identity columns only (the `schwab_api_calls` / `yfinance_calls` shape), or (b). The census proposes; RD disposes.
- **F-I2 — the provenance_corrections `WHEN NOT` pair fails an absent-key probe.** RULED (CHARC): fix in `0041` by DROP + CREATE with `WHEN NOT COALESCE(…, 0)`, the trigger body byte-identical otherwise, discriminators = the failing probe flips + every prior passing probe stays green + the trade-23 and trade-25 blobs and every fixture replay ADMIT.
- **F-I3 — a guarded table's production writer turns out to use `ON CONFLICT` without the `WHERE NOT EXISTS` pre-filter.** RULED (CHARC): adjust the writer (the priced cost), pin its contract; never narrow the barrier.
- **F-I4 — the epoch's `no_insert` does not subsume `no_replace` under probe.** RULED (CHARC): add `no_replace` in `0041`; a single-row table's `REPLACE` is still DELETE+INSERT and `no_insert` fires on the INSERT half — the probe decides whether the DELETE half needs its own guard (it has one).
- **F-F1 — a census row whose typed refusal would CHANGE whether a money-bearing entry proceeds.** ROUTED to RD as it arises (the ENTRY-vs-LABEL test is the instrument; the census states the current behaviour and both branches).
- **F-F2 — a class-1 escape whose honest fix is a NEW exception type or a base-class change (e.g. re-basing `SchwabSchemaParityError` under `SchwabApiError`).** RULED (CHARC): a base-class change is IN SCOPE when every existing handler of the new base is read and listed (the widening sweep discipline: grep each member separately, read the handlers, the drift test if one exists); a new type is a fork to CHARC with the MRO proposed.
- **F-F3 — the D39 census exceeds what one arc can carry (the row said ~20; the string count is 157).** RULED (CHARC): the arc carries every site on an OPERATOR PATH (routes + CLI); pipeline-internal and research-harness sites are listed and DECLARED with the reason (no operator reads their exit code live; the runner's step_guard contains them), banked to a later sweep by name. Existence ≠ completeness: the declared list is verified against the census by READ, not by the grep that seeded it.

## §4 THE ACCEPTANCE TEST

**22-I:** (1) the census table on the plan, every append-only/immutability contract in `swing/data/repos/` and `migrations/` named with its guard state and its writers; (2) every existing guard PROBED on a live-DB copy with the three-direction set + delete + update + the NULL-`WHEN` per-input probe, results tabulated (the plan ledger carries the probe script's path and output); (3) RD's F-I1 partition landed as a block quote; (4) `0041` adds exactly the guards the partition names, each with the discriminating set RED on v40 and GREEN post-migrate; (5) the D51 fixture diff = header + N added triggers, zero changed objects, zero deleted; `schema_manifest.py --check` clean; (6) every production writer on a newly-guarded table still passes its own tests byte-unchanged, plus the `ON CONFLICT` adjustments' contract pins; (7) the live migration proven by execution on a live-DB copy under the trial code (the gate's manifest composition), `foreign_key_check` []; (8) the full fast suite green on the merged head with SHA.

**22-F:** (1) the census table on the plan (both classes, every operator path, file:line, reachability); (2) per class-1 row: a discriminating test that RED reaches the typed refusal on main's behaviour (a 500 page / a traceback) and GREEN reaches the typed message, the response status pinned; (3) per D39 row: the both-verbs-fail proxy (the 22-A shape) and, where a durable write precedes the cleanup, the close-after-durable discriminator (exit 0 + WARN, or the chained refusal); (4) D68's contain shape: a planted non-race exception after the exit commit → 200 with the degraded-success notice, the exit durable; (5) `ReservedJournalFieldError` and `SchwabSchemaParityError` each reach the operator as a typed message on every path the census lists, by execution; (6) the declared residual list (F-F3) verified by read; (7) the full fast suite green on the merged head with SHA; ruff clean; trailers empty.

## §5 THE WITNESS AND THE CALENDAR

- **22-I's merge is a WITNESS SITTING** (a schema bump makes the live DB unrunnable between merge and migrate): W0 `swing web` stopped; W1 plain `sqlite3` `BEGIN EXCLUSIVE; ROLLBACK` stated; W2 the `--no-ff` merge, `EXPECTED_SCHEMA_VERSION` 41 read from the merged code; W3 `swing db-migrate` — expect ONE gate image `swing-pre-22i-migration-<UTC>Z.db` in `backups_dir`, exit code NOT the verdict; W4 `SELECT version FROM schema_version` = 41 (`mode=ro`), `schema_manifest.py --db` clean, `foreign_key_check` []; W5 the probe script re-run on the LIVE DB read-only where it can be (the `REPLACE` probes run on the COPY only — never a write probe on live); restart `swing web`. **Not across the 17:30 HST pipeline. The sitting lands BEFORE 2026-10-02 EOD HST or AFTER 10-20** (the pause has no witness rung). If Reviewer B is not clean on the finished tree by 10-01 EOD HST, 22-I PARKS to 10-21 and the orchestrator posts the park; 22-F is unaffected by the park.
- **22-F has no witness rung** (§2.11) and may merge at any time, pause included, at the CHARC gate on the code-only packet (B's transcript, the merged-head suite with SHA, the 22-A sweep test, ruff, trailers).
- **22-H** waits for RD's October read (first trading week of October) — not commissioned by this brief.

## §6 RETURN

The 22-I plan cell returns the census packet FIRST (premise re-verification against §1 with every delta stated; the probe results; the F-I1 table list for RD; any NEW fork with both branches and a named ruler — or the one-line "none found"); the orchestrator posts it to `charc` and `rd`; RD rules F-I1 in one pass, CHARC rules anything routed to it in one pass; the loop opens on the landed rulings. The 22-F plan cell dispatches on 22-I's plan acceptance and returns its census the same way (F-F1 items to RD, F-F2 to CHARC). The ORCHESTRATOR posts each return report to `charc` and `rd` after its QA. CHARC's architecture pass for `0041` rides the 22-I plan (the guard set, the writer adjustments, the gate row); RD's gate rides F-I1's encoding and the probe table. CHARC closes 22-I on the §5 sitting and 22-F on its merge gate. Round-gate depth readings per harness §5.1 (the cell returns at each review-round gate and is resumed).
