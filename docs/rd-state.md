# RD — Current State (single source of truth)

> **OVERWRITE this file each session/handoff — do NOT append.** The one always-current state pointer for the RD (Research Director / CIO) role. The dated log in [`docs/research-director-context.md`](research-director-context.md) §7 is APPEND-ONLY history; current state lives HERE only. Bootstrap reads this FIRST. Convention: [`docs/harness-architecture.md`](harness-architecture.md) §6.

---

**Last overwritten:** 2026-07-29 — **GENERATIONAL HANDOFF at context capacity, mid-Phase-21.** Everything durable is committed; nothing load-bearing lives only in a mailbox (swept). Phases 19 and 20 CLOSED; Phase 21 in flight. Schema **v32**. Main is local-only ahead of origin — **the operator pushes; do NOT push.**

---

## 1. PROGRAM STATE — read before quoting any number

**T4 (broad-watch baseline) — the headline result, and it has SOFTENED. Do not repeat the old number.**

- Read #1/#2 verdict: CONFIRMED-NEGATIVE, closed-only **−0.648 → −0.637 (n≈97–100)**. Study: `research/studies/2026-07-03-broad-watch-baseline-t4-decision-read.md` (its 4 cited artifacts are git-tracked under the study's `artifacts/` per watch-standard **v2.1** cite=commit).
- **As of 2026-07-29 the cohort is n=312 and the closed mean is −0.2016.** Diagnosed as BOTH: (a) leveraged-ETF **dilution** — TNA/URTY/UDOW unchanged at ≈−1.0 but fell **27% → 10%** of the cohort; (b) the **non-ETF cohort genuinely improved −0.497 → −0.121**, win rate **10.3% → 22.1%**.
- **Likely mechanism: the unwinding of the survivorship bias read #1 itself identified** — losers realize −1R fast and leave to the closed set while winners linger open, so a young closed-cohort is biased NEGATIVE and rises as winners close.
- **Consequence: direction holds, the frozen positive-branch criterion is NOT met, nothing flips — but the margin thinned by two-thirds and the non-ETF slice is NEAR BREAK-EVEN.** "The watch pool loses money" may be closer to "it roughly breaks even, and the leveraged-ETF slice loses" — which makes the A+-selectivity validation weaker than banked.
- **The "FINAL" label attached 07-05 oversold stability** — it meant *survived the engine fix*, not *the cohort is closed*. **Read #2 must RE-STATE the verdict at the larger N, not restate read #1.**

**H1 (the money question) = 2/20 closed, standard-epoch.** VSTS +$1.80; AMN +$1.17 (D25-corrected). A+ **shadow** cohort: NVCR −1.0R closed, AMN 0.0R closed, VSTS-1 excluded `degenerate_risk`, FTRE open **+1.66R**, VSTS-2 never_triggered. A+ supply ≈ 1/week; **5 fires ever.**

**Posture: STOP-ENGINEERING + MARKET TIME.** Both measurement chains are forensically hardened and witnessed (research chain Phase 19; trading ledger Phase 20 / D25). The weeknight scheduler runs unattended at 17:30 HST.

**Health 2026-07-29:** research-health ALL SEVEN GREEN post-v32; `invalid_ohlc` still exactly baseline 23; drumbeat artifact 0 days old; 730 signals; unattributed 0.

## 2. LIVE TRADING STATE

- **FTRE latch ARMED** — fire 2026-07-20; frozen pivot **18.34**; frozen invalidation **14.88** (NOT the drifted 16.51 — see §6); zone cap **18.89**; horizon **2026-08-31** (30 sessions). Operator has a **GTC buy-limit 18.89** resting; his one standing duty is the invalidation-cancel.
- **VSTS latch ARMED** — fire 2026-07-27; pivot **16.90**; stop 13.40; never_triggered (price ~15.58). Order standing above market.
- **Both are PRE-TELEMETRY latches** (`latch_view_events` created by migration 0032 on 07-28) — see §4.
- **A+ latch posture (adopted 2026-07-23):** an A+ fire LATCHES a GTC entry mandate at the fire's pivot plus a +3% zone cap; it clears ONLY on fill / setup invalidation / horizon; **bucket flicker never clears it**; a re-fire at the SAME pivot re-confirms, at a DIFFERENT pivot SUPERSEDES.

## 3. IN FLIGHT — and my gates

| Item | State | My gate |
|---|---|---|
| **H1 criteria amendment** | Commissioned; brief `docs/h1-criteria-amendment-commissioning-brief.md`; with CHARC for the architecture pass. **CHARC banked D29 (`43ca92eb`): the A+ baseline cohort READERS omit the `entry_intent` filter the amended criterion names — live DB shows 3 labeled where the criterion counts 2.** The amendment fixes the TEXT; the readers still have to be brought to it. | Merge-blocking QA — **read the live registry row MYSELF post-migration**, and confirm the D29 reader gap is either closed or explicitly carried |
| **21-G** (provenance asymmetry) | **CLOSED — MERGED `6b1a7860`**, merged-head 9512/7/0, ruff clean, zero schema (live DB still v32). RD QA cleared first; no gate outstanding at merge. Merge verified by RD on main. | **DISCHARGED.** My corrected D27 text landed at both survey sites (superseded text at zero occurrences); **hit 7's paragraph was left behind and I closed it myself at `c78f3c33`.** |
| **0032 date-CHECK defect** (SQLite CHECK passes on NULL) | RULED `20260729T085410Z`: CONCUR, rides 21-B's rebuild; deliberate + both halves test-pinned; **CONDITION: if any new `latch_view_events` writer lands first, the fix lands first** | Confirm at 21-B merge QA |
| **21-B** (prepared-order form + execution-parity ledger) | Prose review STOPPED at round 30 on a pre-committed falsifiable stop rule (cascade ROSE to 67%; 4 of 6 findings were the fix-pass's own misses — the generator is the plan's SIZE vs site-coupling, ~20-site blast radius / ~4-site miss rate). **RD disposition ruled `20260729T175747Z`:** fix ONLY the view conflation + the derivation-field manifest, the other four must each NAME THE AUTHORITATIVE READING (CHARC's requirement, ADOPTED over my weaker name-them-at-their-sites — a contradiction with both sides standing is resolved by a silent implementer guess), **do NOT run round 31.** Determinations issued: limit price = `floor(zone_cap*100)/100` (cap is a MAXIMUM); sizing basis = LIMIT price not pivot-parity (pivot sizing breaches the 0.5% cap at an ordinary fill); the 1-share divergence from `daily_recommendations` is BY DESIGN, rendered + documented. | **PLAN-STAGE GATE: PASS `20260730T042712Z`** (at `360b753f`, docs-only verified). **Ruling-fidelity sweep DONE — 20 rulings verified faithful; it terminated on the list, not on patience.** Findings: (1) **unit-is-the-LATCH is correct BY CONSTRUCTION but UNSTATED and UNPINNED** → required one assertion that rate denominators key on distinct latch identity (correct-by-construction-without-a-pin is how a ruling silently regresses); (2) CARRY — a future shadow join must tolerate a PERMANENTLY-null twin for pre-2026-06-05 fires (latch corpus from 04, temporal log from ~06-05); (3) false alarm CLEARED (line 141 is the superseded brief text quoted inside the section explaining why it was vacuous). **(4) MY OWN tightened ruling-2 would have violated my PRE_TELEMETRY ruling** — read as the coverage axis it asserts the apparatus was ABSENT when present; the implementer caught it and their THREE-name split (`awareness_view_rows`/`actionable_view_rows`/`observation_recorded`) BEATS my tightening, adopted as authoritative; re-tightened text issued. **BLOCKER RULED `20260730T161212Z` (x4):** (1) **BUILD the validity prompt BEFORE merge** — verified myself: the template has ZERO occurrences of validity/intent_kind, and the result partial only DISPLAYS `validity_outcome` with no form; §F.3's agreement denominator needs `accepted_by_broker`, so the headline measurement is STRUCTURALLY inert without it. Specified work missed, not new scope; ~1/week fires are unrecoverable = a second PRE_TELEMETRY dark window. (2) **FLOOR fix mandated** — `Decimal(str(cap)).quantize('0.01', ROUND_FLOOR)`; the reported `36.05` example is UNREACHABLE from a 2dp pivot (real incidence 7 of 99,901, all whole-dollar >=$141; FTRE/VSTS unaffected) — rarity rejected on my own multiplicity-guard standard; the harm is the framework flagging its OWN order as a mismatch (drumbeat class), not the price. Both tests pinned, use `141.00 -> 145.23`. (3) **A→B→A: the governing answer is his LAST, never the last DISTINCT** — key idempotency on the submission IN CONTEXT (include the prior governing row). My pessimistic default governs UNRESOLVABLE ambiguity; here the fact is knowable, so the rule is RECORD THE TRUTH. (4) **The flattering-direction pattern → an ACCEPTANCE CRITERION:** every failure of a recording instrument is SILENCE and silence reads as 'he did fine', so **the ZERO-DATA state must be distinguishable from the GOOD state and labelled unmeasured** for every rate. **SIX ITEMS RULED `20260730T225945Z`.** Blocker CLOSED (validity prompt reachable; cancel control too). **I OWN an incidence correction: production is `round(pivot*1.03, 4)` FIRST → 43 per 100k, and 35.00 IS affected** — I measured the unrounded product. The ruling survived because it was stated as a SEMANTIC, not a number. **ITEM 6 MERGE-BLOCKING — measured 49.5% of pivots, VSTS LIVE** (cap 17.407: 21-A expects 17.41, 21-B emits 17.40 → the panel calls his correct order a STRAY). 21-A's `round(cap,2)` EXCEEDS the cap, so it is wrong on my own ruling and the defect predates 21-B. **Fix is NOT aligning two constants: (a) SINGLE-SOURCE the mandate price (one Decimal floor fn, both arcs) and (b) SEPARATE PRESENCE FROM PRICE — the presence alarm keys on shape, a limit difference is a per-field DELTA the ledger already records.** **OPERATIONAL: the VSTS mandate is 17.40; if his resting order is 17.41 that is a real 1c delta, NOT a mistake** (the mandate was undefined at that precision when placed). Item 1: a leg-PRESENCE difference is observed, so it FAILS the numerator, never UNKNOWN (symmetric with the ruled `both_absent`=MATCH). Item 2: a displaced childless cycle must not vanish — a retry may NEVER silently stand in for the failed first attempt. Items 3/4/5 not mine; stakes stated. Deviation APPROVED (the plan's letter was unwritable; intent preserved). **Witness widened to FOUR paths: validity, cancel, the A→B→A correction, and a ZERO-DATA render** (ruling 4's flattering-null check — only visible on a screen). **ITEM 5 RULED `20260730T230147Z` (last one owed by me):** accept the subset, but **RECORD THE SHORTFALL** — the route already rebuilds the true live set from the anchor (`routes/latches.py:236-240`) and DISCARDS the difference; a truncated beacon silently inflates the AWAY RATE and is indistinguishable from the dark window my own partial-coverage rule describes. `not_reported` must be distinguishable from `not_rendered` and `rendered_non_actionable`; a persistent shortfall feeds the telemetry-health check. **CHARC's scope correction ADOPTED:** 21-A's `round(cap,2)` basis is wrong and predates 21-B, but **the FALSE ALARM is CREATED by 21-B** — that decides scope. They verified THREE call sites where I reasoned about one. **BANKED — the sweep's blind spot, accepted:** a ruling-fidelity sweep verifies the encoding MATCHES the ruling; **it cannot verify the encoding is WRITABLE.** Thirty rounds + my sweep both missed an unimplementable spec clause because both read the document against itself or an authority and neither EXECUTES it. The operator witness is the only gate that executes — load-bearing twice this week, not ceremonial. **2026-08-03 STATE: the cross-arc item is CLEARED** (`1570c083` verified on disk — `may_assert` gate, stamp-equality at zero occurrences, `regime_close_session` written only from rung-A). **Canonical price SETTLED by operator ruling: the cap, everywhere, one source** — he explicitly retired the 04-25 1% discipline (my explicit-call condition met); `chase_factor` rider CHARC-dispatched, at-or-after merge, not on the branch. Phase-22 item 1 narrowed in the brief by replacement. **Remaining: the FIVE-path witness (validity leads, one step at a time), then my merge-blocking QA** — confirming the 0032 CHECK rider both-halves-pinned, CARRY 1 (distinct-latch denominators), the null-twin carry, and the four unfixed defects still naming winners. |
| **Latch-posture AMENDMENT — `criteria_lapsed`** | ROUTED to CHARC `20260801T145240Z`. My "bucket flicker never clears" left the framework blind to structural DECAY: the only implemented invalidation is a frozen **20-bar Donchian low** (`evaluator.py:58-60`), no base detection at all. Proposed 5th clear reason needs a DIRECTIONAL conjunct — the obvious form would have destroyed FTRE (8 sessions non-A+ while running to 20.70). **NOT for 21-B.** | Awaiting CHARC §3 pass |
| **Schwab auto-fill entry-date DEFECT** | ROUTED `20260801T145327Z`. Mapper writes the order's ENTERED date as `entry_date` instead of EXECUTION date; same order id gives 07-23 in auto-fill vs 07-31 in reconciliation. **Only manifests on orders that REST → the latch posture created it; every future latch fill hits it.** 4 symptoms incl. a FALSE time-stop exit advisory on a 1-day-old trade. No correction surface (D19-class). Discrepancy 95 stays PENDING (operator concurs); `mark_unmatched` would be a FALSE statement. | Merge QA when scoped; trade 19 is an H1-cohort row |
| **21-F** (dashboard latch surfacing) | Proposed; the order cache is a PREREQUISITE (CHARC forbade a broker call on the dashboard hot path) | The telemetry contract must settle BEFORE any dashboard surface ships |
| **21-C** (framework places/cancels orders) | Deferred behind stage-1 evidence + an operator-signed L2 endpoint diff | — |

## 4. RULINGS THE ARCS ARE BUILT ON — a fresh instance must not contradict these

- **Latch derivation:** freeze at fire time (read the FIRE's `candidates` row, never the latest — FTRE's pivot drifted 18.34 → 20.19 while armed); horizon in **NYSE sessions**, bound at the source to `cfg.pipeline.observe_max_pending_window_sessions`; per-FIRE identity with same-action-session duplicates collapsed; latch-specific fill detection; clear-reason recorded not inferred; invalidation on **closes**.
- **Zone escape is a STATE ATTRIBUTE, not a clear condition** (`LATCH_CLEAR_REASONS` = fill / invalidation / horizon / superseded only).
- **The stale-data asymmetry — applies to stale closes, cached order state AND run-level stamps: you may raise a MISMATCH ALARM but you may NEVER assert a MATCH.** Alarm permission is bounded to staleness that is **characterisable** (the price's date is PROVEN, not stamp-inferred) **and self-limiting** (the clock's fault, ending at the next run) — otherwise the alarm becomes the drumbeat class.
- **Three-state latch action:** accept / decline(+reason) / no-action. **No-action must NOT default to "away"** — split it by objective view-telemetry; **an unattested viewed-but-no-action defaults to DISCIPLINE LAPSE.** The instrument must not flatter its subject.
- **PRE_TELEMETRY is a distinct class**, not a flavour of away. Partial coverage: **a view record ESTABLISHES awareness; its absence over a partly-dark window establishes NOTHING.**
- **`pending_live` is excluded from every rate**; **`attested_was_away` gets its own bucket** — the away rate is reported **objective (gates the stage-3 decision)** vs **attested (an explicit upper bound)**.
- **Telemetry records WHICH LATCHES were rendered with actionable detail, plus the surface** — not merely that a surface was viewed.

## 5. RD-OWNED FOLLOW-UPS — with deadlines

- **~~THREE research-side instances of the run-level-stamp shape (gotcha #30)~~ — DISCHARGED 2026-07-28, ahead of the deadline. Trace: [`docs/rd-d27-provenance-trace.md`](rd-d27-provenance-trace.md) (`ff405567`).** Result: **the August read's chain is measured CLEAN on this axis** — proceed without qualification.
  - **Hit 7 (the forward-observation gate): latent, ZERO instances, and the survey's MECHANISM was backwards.** A lagging archive makes the gate STRICTER (under-observation, safe direction), not weaker. The only look-ahead path is a **two-clock-read race** — the detection stamp is `last_completed_session(run_now)` read at LEASE acquisition while both bar cutoffs are read at FETCH time, so a run straddling the 16:00 ET close would stamp a detection older than the data it saw. **0 of 140 completed runs straddled, and it is a PROOF not a sample** (`last_completed_session` is monotone, so equal endpoints ⇒ constant throughout the run). Exposure is not zero forward: longest run on record 73.2 min (median 2.33).
  - **D27 is a NAMING defect, not a value defect.** The two columns agreed **113/113 across the FULL paired history** (not 25), including 17 runs started during US market hours — held by a **code-level cap**, not by healthy nightlies: both bar consumers slice at the clock session (`PriceFetcher.get` at `<= effective`; `OhlcvCache.get_or_fetch` strips `> end`). **The precise claim — I overstated this once and corrected it: bar-derived can never exceed the clock session AS READ AT FETCH TIME; it can exceed the PERSISTED stamp only if the run STRADDLES a session close, which is the SAME event as hit 7's look-ahead path.** So D27's agreement and hit 7's safety rest on ONE condition, measured 0/140 — stronger than treating them as two. Still worth fixing the name (two quantities, one column; my monitors read both). Scoped to the Phase-21 close or Phase 22.
  - **Hits 4 and 5 RE-SCOPED: not deadline-bound.** Both sit in harnesses dormant since before the 2026-05-27 arc closure with **no coupling to the shadow engine** — the August read does not run them. They remain real #30 instances and are a **precondition on any future USE** of those harnesses (a V1↔V2 parity claim must price them first), not a blocker on the read.
- **MONTHLY READ #2 — due the first trading week of August**, with a real agenda: re-state T4 at n=312+ (Wilson LBs, unique-name counts, **the ETF split as a first-class cut**), test the survivorship-unwinding hypothesis, the three survey hits, T9 operationalization, the §3.2 log format. The catch-up read folds in here.
- **A+ spot-check cadence** continues to N=10 priced (currently 3: NVCR / AMN / FTRE).
- **Watch:** PGNY-class floor-boundary marks (the risk/ADR 0.15–0.2 band still amplifies — revisit only on REALIZED evidence); the day-3-vs-day-4 shadow-twin divergence (n=1).

## 6. BEHAVIOURAL — load-bearing, earned

- **Blunt over sycophantic; evidence before assertion** — every number freshly queried, never carried forward. Verify intent before labelling something a mistake, then be direct.
- **THE DOC IS A LEAD, THE CODE IS THE EVIDENCE.** I am reliable on SEMANTICS and unreliable on REMEMBERED CONSTANTS — **any figure a plan will build on must be TRACED at the moment it is stated.** Five instances this tenure: the pruner, the correction-chain grain, write-on-GET, the invented 20-session horizon that became its own citation, and a vacuous acceptance test I wrote.
- **SOURCE EVERY PREMISE FROM THE ROLE OR ARTIFACT THAT OWNS IT** — canonical in `harness-architecture.md` §2. A **fact** comes from code; a **position** from the role holding it, at POST time; **dispatch state** from the ORCHESTRATOR's last state statement; **gate state** from the gate-holder. If unclear, ASK — and NAME the statement being ruled against.
- **PRESERVE THE QUOTE** (§2, fifth row): a downstream editor corrects the surrounding text, never a director's quoted words — **the failure is invisible, because a softened quote still reads like a quote.** The director tightens their own words.
- **SUPERSEDES GOES IN THE SUBJECT** (§3) — a reader must tell from the inbox listing whether they are current. **AND: ONE SUPERSESSION PER MESSAGE** (§3, amended 2026-07-29 — canonical text is CHARC's, do not copy it here). This one bit ME, on 21-G: I sent two corrections under one subject, the subject named only the first, and **only the first was applied** — leaving the artifact's own headline pointing readers at a paragraph that still carried the mechanism I had corrected. **A partially-applied correction can be less coherent than no correction.** Visibility was never the problem; ACTIONABILITY was.
- **A CLEARANCE ATTACHES TO THE TREE I VERIFIED** (§2, 2026-07-29, RD-stated/CHARC-canonicalized): docs-only with my final text in hand may land post-clearance; anything touching code or tests RE-OPENS the gate. The precondition is binding — **docs-only must be GREPPED, not assumed** (docs are load-bearing in this repo).
- **GATE VISIBILITY** — the operator may authorize a merge over an outstanding director gate (his authority), but the merge report must SAY so in one clause. **A gate compressed because it would have passed anyway is advice with a ceremonial step.** Raised against my own gate.
- **NEVER MERGE CATEGORIES THAT DIFFER IN EVIDENCE KIND, even when their outcomes agree** (ruled three times). Outcomes coinciding is what makes merging tempting; reasons differing is why the design breaks. Keep softer measures as separate, explicitly-bounded forms.
- **A RULING IS PROVISIONAL ON ITS PREMISES** — a load-bearing number arriving later RE-OPENS it rather than being inherited by it.
- **Adjudicate residuals by DECOMPOSITION, never by prior plausibility** — D25: the $10 equity_delta instrument caught real corruption that both directors nearly dismissed as deposit drift.
- **Weight REALIZED (closed-only) over marks**; when primary scenarios disagree in sign, the pre-committed realized read binds.
- **QA the DIVERGENCE, never defend the brief — including my own rulings.** Twice this week a CHARC ruling beat my design by pricing a neighbouring step I had not traced; twice I reversed or withdrew my own position on the evidence.
- **A COMMITMENT STATED IN CHAT IS NOT ROUTED.** 2026-08-01: I told the operator three times I would take something to CHARC — the 21-B cross-arc ruling (which the orchestrator was HOLDING A WITNESS on), the posture amendment, the entry-date defect — and sent none of them for hours. **Chat is not even a mailbox**, so this is worse than the mailbox-is-transport-not-a-tracker case I police in others; and from the gate-holder's side it is the GATE-VISIBILITY failure I raised against my own gate on 07-28. **Post it in the same turn I say it, or say I will do it later and record it here.** Caught by the operator, not by me.
- **Run `role_mail` with a `cd <main-repo> &&` prefix in the SAME command** — never trust a prior cd (2 misdelivery instances banked). Long bodies go via `--body-file`, never inline.
- Briefs crossing a CHARC §3 tripwire route through CHARC pre-dispatch. RD is merge-blocking on measurement integrity. **Default posture is STOP-ENGINEERING plus market time; deviations need written justification.**
