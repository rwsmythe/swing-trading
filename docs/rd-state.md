# RD — Current State (single source of truth)

> **OVERWRITE this file each session/handoff — do NOT append.** This is the one always-current state pointer for the RD (Research Director / CIO) role. The dated session log in [`docs/research-director-context.md`](research-director-context.md) §7 is APPEND-ONLY history; current state lives HERE only. Bootstrap reads this file FIRST. Convention: [`docs/harness-architecture.md`](harness-architecture.md) §6.

---

**Last overwritten:** 2026-06-18 (SPCX + §2.4 both merged; research monitor read = benign). **Program state:** the 2026-06-13 data-collection audit chain is CLOSED + automated; watch-standard **v2** adopted 2026-06-16; posture = **STOP-ENGINEERING + market time**. **SPCX out-of-framework carve-out (path B) + the §2.4 swing-NLV coherence fast-follow BOTH MERGED; SPCX live-witness PASS** (§2.4 `e1017b1e`; SPCX `5b24ef68`). **main HEAD = `e1017b1e`**; **schema v31**; **8652 green on the MERGED head** (binding no-false-green confirmed); trailers `[]`. Local-only commits — main is ahead of origin (operator push cadence; do not push). **§2.4 live-witness now CLEAN (operator did path (a) — `cash_movements` id 5 withdraw $372.48; run 60 coherent, swing_coherence_delta −0.18); RD L2 ruling CONFIRMED (`20260618T091737Z`); arc closeable. **ACTIVE: the OOF-buy cash-coherence arc (`swing journal oof-buy`; CHARC-commissioned, brief `27a0347f`) — measurement-core, RD L-checklist MERGE-BLOCKING; pre-loaded + 1 concern front-loaded (`20260619T053533Z`); **EXECUTING returned → RD L-checklist = PASS (`20260621T025638Z`); at the FINAL gate (operator §5.10 live-witness + housekeeping).** (SPCX/§2.4 saga + both follow-ups all closed.)**

## Live workstreams / arcs in flight
- **OOF-buy cash-coherence arc (`swing journal oof-buy`) — CHARC-commissioned (brief `27a0347f`); plan merged `308b4a35`; EXECUTING returned → RD measurement-integrity L-checklist = PASS (`20260621T025638Z`); at the FINAL gate (operator §5.10 live-witness + housekeeping); RD gate DISCHARGED.** Verified independently on disk (base `308b4a35`→HEAD `a21fd42d`): L4 §2.4 chain ZERO diff; C1 binding composition real-derived + tolerance-from-resolved-swing_nlv + price-move-arm-0; idempotency FAIL-LOUD at both the SELECT path AND the IntegrityError belt (my front-loaded concern fully resolved, no silent-drop); L1 cash_movement-only; L3 triple-gated matcher + non-OOF byte-unchanged + branch-disabled belt; non-finite-cost rejected; apply_overrides registry. V1 raw-DB-write residual = concur V2 CHARC, not a blocker. *Front-loaded idempotency concern — CHARC CONFIRMED on disk + ADOPTED (`20260621T012359Z`): the merged plan's ticker+date no-op silently drops a distinct same-day buy = my #27 ledger-loss hole. Course-correction routed into executing — fail-loud `ClickException` on collision + a `--force`/distinct-ref escape + bare-re-run rejected loudly + I1 updated; brief §4 tightened `e4ae1459`; lands BEFORE my gate. (My QA-the-divergence had leaned "acceptable"; CHARC's "fix it now" was the better call — an arc that kills silent ledger drift must not itself ship one, low-probability or not; cheap fix.) Executing-return check upgraded: verify the FULL fail-loud fix present + correct.* The ROOT fix for the OOF cash side (replaces path-(a)'s manual `--withdraw` + the id-72 acknowledge): an explicit command that records the OOF buy's cash outlay (kind=withdraw, OOF-sentinel `ref`) AND adds ONE additive step-7 matcher branch so the recon recognizes it (no `cash_movement_mismatch`). NO schema (reuses the free-form `ref`); ONE authorized `swing/trades` carve-out (additive/guard-only). **RD pre-loaded (`20260619T053533Z`):** L1 (cash_movement only, no trade row) · L2/§5 composition (eval_delta 0 at cost==MV AND after a price move; declared_oof_mv from POSITIONS not cash_movements → no double-count; real-derived test inputs) · idempotency (no double-debit; sentinel can't collide with a real txn_id) · L3-analog (matcher excludes ONLY OOF-sentinel declared rows, non-OOF byte-unchanged, genuine unmatched still fires, registry guard) · L4 (no measurement-chain/schema). **1 concern front-loaded:** idempotency-on-collision must FAIL-LOUD (not a silent no-op that drops a legit distinct same-ticker/same-day reallocation → silent ledger drift, the #27 smell). Binding sign-off at the executing return (the §5 composition test + L1-L4 on the shipped diff). Dispatch rec: writing-plans opus-xhigh / executing opus-max.
- RD-lane forward (once this lands) = the standing watch cadence (weekly glance + `research_health.py`; first monthly read first trading week of July; the broad-watch N≥30 / T4 decision gate at ~26 triggered now).

## Pending RD decisions (operator-sequenced)
- None owed by RD. The §2.4 RD L2 gate is discharged (PASS). Ball is with CHARC (C1-C5 + the scope-extension ratification) + the operator (§5.10 live-witness + merge auth); the orchestrator drives the merge + merged-head suite. RD re-engages only if the live-witness reopens a measurement question.

## Closed recently
- **§2.4 swing-NLV coherence refinement — MERGED (`e1017b1e`; 8652 green; RD L2 PASS) + WITNESS-CLEAN via path (a).** The §5.10 live-witness fired a TRUE drift (run 59, +$372.30) exposing a PATH-B cash-side gap (SPCX bought with swing cash, the outflow un-recorded → ledger overstated swing capital); operator recorded `cash_movements` id 5 (withdraw $372.48) → run 60 coherent (−0.18, no fire); **RD L2 ruling CONFIRMED — true drift, not false; §2.4 L2-sound both directions** (fires on real divergence, quiet only when genuinely resolved). **Path-B cash-side doctrine:** buy OOF with swing cash → swing `cash_movement` WITHDRAW (purchase cost, not MV); sell back → DEPOSIT (keeps it out of trades [L1] + the capital base accurate). MAJOR-1 (non-finite-ledger false-coherence) caught+fixed during executing. Closeable on CHARC practice-bless; 2 follow-ups registered (clear stale id 71; route swing `equity_delta` out of `pending_ambiguity_resolution`).
- **SPCX out-of-framework carve-out (path B)** — MERGED `5b24ef68`, 8638 green on the merged head, **§5.10 live-witness PASS** (declared SPCX → no orphan + the `#27` exclusion line `SPCX: +2.00 sh @ $+392.51`; existing orphans id 69/70 cleared via `swing schwab resolve-out-of-framework`; undeclared-still-banners covered by the scoped-set test). RD L1–L4 sign-off was PASS pre-merge. The ~$392.51 SPCX MV now sits outside the framework by design (= the gap §2.4 restores detection for).
- **equity_delta limbo-routing fix (§2.4 follow-up #2)** — MERGED `8fee6a1d` (8657 green; RD fyi/no-L2-block); a fired `equity_delta` now stays unresolved/cleanable, not tier-2 limbo; id 71 cleared. CHARC §0=(A) (no `MATERIAL_BY_TYPE` flip). Clear to close.
- **id-72 `cash_movement_mismatch` (path-(a) downstream) — RD assessed NON-ISSUE** (`20260618T174409Z`): same pattern as the recurring monthly deposit (cm 4) — a manual cash_movement with no Schwab counterpart, material=0, SELF-SUPPRESSES on resolution (verified cm 4: resolved 06-12 → zero re-emits runs 50-60 in-window). One-time acknowledge; path (a) sound; no follow-up (the clean "satisfies-both" model saves 1 ack on a rare event — not worth a schema arc). **Action: operator acknowledges id 72.**
- The full 2026-06-13 audit chain (18-A writer fix → 18-B consolidation → 18-C yfinance observability → 18-D monitor + FIX-1 baseline → 18-B.1 structural prevention → nightly step + calibrations A/B). Watch-standard **v2** authored + operator-adopted. All RD-lane Phase-18 audit work is done.

## Watch-standard / tripwire status (T1–T7) — as of 2026-06-18
- **Research-health monitor read 2026-06-18 (operator ran a pipeline + flagged YELLOW) = BENIGN, no action** (full record §7 2026-06-18). #1 `temporal_log_finiteness` GREEN (no write-barrier regression) + structural/drumbeat/candidate/transport GREEN. YELLOW = #2 `excluded_reason_breakdown` (`missing_observations` 17.5%, the self-healing maturity band) + #3 `coverage_gaps` (5 = the 5 DINO det186-190 each missing the 06-15 obs; root cause = run-104 `pattern_observe` `no bar for observation_date` DINO skip = correct skip-don't-poison; benign but PERMANENT). **Banked calibration observation:** `coverage_gaps` counts permanent ACCOUNTED holes toward red=10 (now 5/10) → cry-wolf trajectory; V2 = don't count holes explained by a recorded skip-warning. Flagged to operator, not commissioned.
- **All clear (06-17 glance).** Drumbeat alive (run `20260617T070016Z`, artifact age 0d); unattributed **0** across all 7 shown runs (no T2); accrual healthy (51→114 signals). No new live trades this week — epoch intact (no T5/T6/T7). `invalid_ohlc=23` stable (baselined 06-10 cohort; not a #2 regression).
- **The glance's lone `ATTENTION` is the known T3 heuristic re-fire** (it trips on any nonzero trigger rate). **T3 is CLOSED** (golden gate + 2 hand-walks, 06-13; N past 10). Not actionable.
- **Broad-watch priced N ≈ 26 triggered signals** (run `20260617T070016Z`) — numerically approaching the **T4 N≥30** gate (was 10 on 06-13). **BUT zero realized edge:** closed-only **0/7 wins, −0.714R**; the +0.382R `mtm_at_horizon` headline is entirely UNREALIZED marks on 19 open positions; 14/26 triggered are weak-close intraday-touch entries; signal-count overstates evidence (correlated re-detections, ~handful of unique names). Meaningless at this N by design; floors suppress correctly. **At the T4 read, weight closed-only realized — not the blended open-mark number.**
- **First monthly read due: first trading week of July 2026.** T4 (N≥30) is the binding next event; T9 (log N≥100) and T10 (2026-12) pending.

## Behavioral load-bearing (pointers into the charter)
- Blunt over sycophantic; evidence before assertion (every number freshly queried from the live DB / artifacts — never carried forward). Verify intent before labeling a mistake, then be direct.
- QA the DIVERGENCE, never defend the brief (directive #10). Verify the actual code/UI surface before operational guidance (directive #9) — ground claims in `file:line`, not architecture-from-memory.
- Commissioning briefs crossing a CHARC §3 tripwire (new schema / module / dependency / standing process / `swing/{trades,data}` carve-out) route through CHARC pre-dispatch (`harness-architecture.md` §5). RD is merge-blocking on measurement integrity; CHARC challenges measurement locks through the operator, not by override.
- Default posture: STOP-ENGINEERING + market time. Deviations need written justification (the SPCX carve-out is justified as instrument-integrity, not research).
