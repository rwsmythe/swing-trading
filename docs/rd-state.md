# RD — Current State (single source of truth)

> **OVERWRITE this file each session/handoff — do NOT append.** This is the one always-current state pointer for the RD (Research Director / CIO) role. The dated session log in [`docs/research-director-context.md`](research-director-context.md) §7 is APPEND-ONLY history; current state lives HERE only. Bootstrap reads this file FIRST. Convention: [`docs/harness-architecture.md`](harness-architecture.md) §6.

---

**Last overwritten:** 2026-06-24 (Phase-18 closing; `coverage_gaps` calibration in flight). **Program state:** the 2026-06-13 data-collection audit chain is CLOSED + automated (watch-standard **v2** adopted); the SPCX out-of-framework saga is CLOSED + witnessed. Posture = **STOP-ENGINEERING + market time**. **main advanced via concurrent D19/D20/D18/scaffold + RD-doc generations — re-pull HEAD**; **schema v31** (no migration since 0031); **~8775 green**; trailers `[]`; ZERO Co-Authored-By. Local-only commits — main is ahead of origin (operator push cadence; do not push). **ACTIVE: a `coverage_gaps` monitor-calibration commissioned to CHARC (`20260625T063341Z`, brief `c2d7b3ac`) — the 842-gap false-RED fix; RD reviews the calibration SEMANTICS at the executing return. Else standing watch cadence.** **Context-doc compaction DONE 2026-06-21 (Phase-18 close):** `research-director-context.md` 156K → 28K (under the 120K harness threshold); the verbatim §7 log (2026-06-08→21) archived to `docs/research-director-context-archive.md`, replaced inline by a chronological digest + a "new entries append below, keep tight" marker (the CHARC FORM-flag closed). Going forward keep §7 entries TIGHT (binding facts + pointers; the sign-off comms posts hold the detail).

## Live workstreams / arcs in flight
- **drumbeat_liveness #5 recurring FALSE-RED — RE-FLAGGED to CHARC** (`20260625T164541Z`; thread `drumbeat-liveness-false-red`). #5 RED "no engine artifacts" fired 06-24 + 06-25 16:34 (intermittent — GREEN at run 112), but the drumbeat is provably ALIVE (16 artifacts, newest today; main-repo monitor #5 GREEN → overall just YELLOW). Writer (runner.py:1207) + reader (:1361) both use `cfg.paths.exports_dir/research` (no static divergence); the false-RED is LAUNCH-CONTEXT-dependent (web-spawn cwd/config_path→project_root reading an empty exports_root, + shadow-expectancy apparently not writing those runs — CHARC's trace). CORRECTED the merge-note conflation (the orchestrator read it as the Codex-review WSL-path thing; it's the MONITOR CHECK). MATTERS: #5 is the PRIMARY drumbeat-death T1 alarm → false-RED makes it untrustworthy + spams the 18-H.7 push. CHARC engineering; RD reviews fix semantics; not a data emergency. **WIDENED 06-25 (`20260625T165110Z`):** the broken-context run poisons the WHOLE production `latest.json`, not just #5 — CHARC's coverage "842 mature→0" observation was the SAME 16:34 broken run reading an empty DB (live DB still 842 mature; verified). The broken run reads empty cfg paths but WRITES a "successful" envelope to the repo-anchored production `latest.json` → corrupts the 18-F stoplight + 18-H.7 source (recovered 16:49Z; transient, recurs). Fix scope: cfg-path resolution AND/OR a "suspicious-empty-context → write-nothing" guard. **CHARC DIAGNOSED + COMMISSIONED the root+guard fix (`19185b04`, operator-approved); 06-27 (`20260627T071545Z`):** CHARC confirmed the exports half (web `/pipeline/run` relative `--config` + no `cwd=` → cwd-dependent `project_root` → `exports_dir` mis-roots) and CORRECTED me — `db_path` is HOME-anchored, NOT cwd-mis-rooted (I OWNED that over-inference). My grounded db re-exam: empty per_det ⟺ empty DB (coverage SELECT has no WHERE); the push fires only from the leased step. So the db-symptoms require a different-**HOME** launch (re-roots the home-anchored db), NOT the web-spawn → a scheduled/service/elevated launcher. **KEY:** the ROOT fix (absolutize config_path) does NOT cover the HOME-driven db-half → the GUARD must catch the empty-DB read too. RD reviews the guard's real-empty-vs-broken-empty discriminator at the fix (merge-relevant). **Operator holds the fast pin: which launch path on the broken runs.** **PAUSED 06-27 (`20260627T074955Z`):** operator is collecting NON-DEV launch-context data (how `swing web` + the pipeline are started when the false-REDs fire — its cwd + USERPROFILE/HOME, since the /pipeline/run subprocess inherits the web-server env). Writing-plans launch-path coverage + the HOME-env-vector root question HELD until the data lands; root+guard skeleton may proceed; RD reviews guard semantics at the fix. Not urgent (drumbeat healthy; latest.json self-recovers; recurs ~daily — 6× through 06-27 07:42).
- **`coverage_gaps` CALIBRATION C — MERGED + CLOSED `83c21305`** (merged-head 8795 green; both directors cleared; binding live check PASS — the real 842-RED reads accepted-historical; transcript `docs/reviews/coverage-gaps-calibration-executing-codex-findings.md`). Watch-standard §3.1 FOLDED `554619fe`. The first research-health RED's COVERAGE half resolved.
- RD-lane otherwise = the standing watch cadence (weekly glance + `research_health.py`; first monthly read first trading week of July; the broad-watch N≥30 / T4 decision gate at ~26 triggered).

## Pending RD decisions (operator-sequenced)
- None owed by RD.

## Closed recently
- **cash-void command (`swing journal cash-void <id>`, D19) — MERGED + CLOSED `be567712`** (8734 green; all 4 gates incl. operator §5.10 live-witness: $50 seed → restore 1654.78→1604.78→1654.78, 3 guards fail-loud). Born from my cash-correction-gap flag; an append-respecting reversing entry + `void:<id>` sentinel + kind-agnostic matcher; RD L-checklist PASS (`20260621T181459Z`). **The SPCX-double-debit class of error is now operator-correctable without a raw DB write.** D20 (Schwab-txn-id `^[0-9]+$` collision boundary) = CHARC+RD-adjudicated Phase-18-close hardening candidate (closes oof: + void: at once), not a blocker.
- **The SPCX out-of-framework / swing-NLV cash-side saga — FULLY CLOSED + witnessed** (full per-step record in §7, 2026-06-17..21):
  - **SPCX carve-out (path B)** `5b24ef68` — declared-OOF ticker carved out of the orphan pass; live-witness PASS.
  - **§2.4 swing-NLV coherence** `e1017b1e` — restores swing-ledger-vs-NLV drift detection while holding a declared OOF holding; its live-witness fired a TRUE drift exposing a path-B cash-side gap (RD L2 ruling = true positive, not false), resolved via path (a) (record the OOF cash outlay as a swing withdraw).
  - **equity_delta limbo fix (follow-up #2)** `8fee6a1d` — a fired `equity_delta` stays unresolved/cleanable, not tier-2 limbo (RD fyi/no-L2-block).
  - **OOF-buy command (follow-up #1 — the cash-side ROOT fix)** `5855c760` — `swing journal oof-buy --ticker T --cost C` records the OOF outlay AND makes the recon self-reconcile it (replaces path-(a)'s manual `--withdraw` + the id-72 acknowledge). **RD L-checklist = PASS** (`20260621T025638Z`; verified on disk: §2.4 chain zero-diff / C1 real-derived + tolerance-computed + price-move-arm-0 / fail-loud idempotency at BOTH the SELECT path AND the IntegrityError belt — my front-loaded concern, witnessed live / L1 cash-only / L3 triple-gated matcher). **Operator doctrine:** buy OOF with swing cash → `swing journal oof-buy`; OOF SELLs (transfer-IN) are a V2 follow-up. **V1 residual (→ V2 CHARC):** a raw out-of-band DB write of a forged `oof:` withdraw would be self-sourced (closing it needs a schema origin column; counted via `cash_oof_self_sourced_count`). **Operator option (not required):** re-record the existing manual SPCX row (cash_movement id 5, ref `spcx-oof-buy-2026-06-15`, NOT an `oof:` sentinel) via `oof-buy` to stop the id-72 acknowledge.
  - **id-72 `cash_movement_mismatch`** — RD-assessed NON-ISSUE (one-time operator ack; same pattern as the recurring monthly deposit; self-suppresses on resolution).
- **The full 2026-06-13 data-collection audit chain** (18-A/B/C/D/B.1 + nightly half + 2 calibrations) + **watch-standard v2** — CLOSED + automated; the research-health monitor refreshes `latest.json` every pipeline run.

## Watch-standard / tripwire status (T1–T7) — as of 2026-06-24
- **Research-health monitor (06-24) = RED but BENIGN — first live RED, investigated** (full record §7): a single missed manual run (06-22; 06-19 was Juneteenth, correctly excluded) → `coverage_gaps`=842 permanent single-session holes → overall RED. **Substrate INTACT** (#1 finiteness + #4 structural GREEN; drumbeat self-recovered, 06-24 fully observed). The 18-H.7 RED→RD push fired same-day (working as built). The cry-wolf-trajectory below is now REALIZED → calibration commissioned to CHARC (see Live workstreams). Also: `invalid_ohlc` 23→26 (+3 new, #2 yellow, minor).
- **Research-health monitor (06-18) = BENIGN** (full record §7): #1 `temporal_log_finiteness` GREEN (no write-barrier regression); the YELLOW is #2 `missing_observations` (~17.5%, self-healing maturity band) + #3 `coverage_gaps` (5 = the 5 DINO det186-190 each missing the 06-15 obs; root cause = run-104 `pattern_observe` "no bar for observation_date" DINO skip = correct skip-don't-poison; benign but PERMANENT). **Banked calibration observation:** `coverage_gaps` counts permanent ACCOUNTED holes toward red=10 (now 5/10) → cry-wolf trajectory; V2 = don't count holes explained by a recorded skip-warning (flagged, not commissioned).
- **All clear (06-17 glance).** Drumbeat alive; unattributed 0; epoch intact (no live trades; no T5/T6/T7); `invalid_ohlc=23` baselined; T3 CLOSED. **Broad-watch priced N ≈ 26 triggered** — numerically approaching the **T4 N≥30** gate but **ZERO realized edge** (closed-only 0/7 wins, −0.714R; the +0.382R mtm headline is unrealized open marks). **At the T4 read, weight closed-only realized, not the blended number.** First monthly read due first trading week of July 2026; T9 (log N≥100) + T10 (2026-12) pending.

## Behavioral load-bearing (pointers into the charter)
- Blunt over sycophantic; evidence before assertion (every number freshly queried from the live DB / artifacts — never carried forward). Verify intent before labeling a mistake, then be direct.
- QA the DIVERGENCE, never defend the brief (directive #10). Verify the actual code/UI surface before operational guidance (directive #9) — ground claims in `file:line`, not architecture-from-memory. A brief's "≈" between two production figures is the highest-risk line — verify it against LIVE values; tests must use real-derived inputs, never premise-constructed (memory `feedback_adversarial_review_verify_data_shapes`).
- Front-load the merge-blocking L-checklist EARLY (it caught real holes on §2.4's §9.4 + the OOF-buy idempotency); when a CHEAP fix closes the exact failure class an arc exists to kill, push for the fix — don't QA-rationalize the residual as low-probability.
- Commissioning briefs crossing a CHARC §3 tripwire (new schema / module / dependency / standing process / `swing/{trades,data}` carve-out) route through CHARC pre-dispatch (`harness-architecture.md` §5). RD is merge-blocking on measurement integrity; CHARC challenges measurement locks through the operator, not by override.
- Default posture: STOP-ENGINEERING + market time. Deviations need written justification.
