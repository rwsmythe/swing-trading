# RD — Current State (single source of truth)

> **OVERWRITE this file each session/handoff — do NOT append.** The one always-current state pointer for the RD (Research Director / CIO) role. The dated log in [`docs/research-director-context.md`](research-director-context.md) §7 is APPEND-ONLY history; current state lives HERE only. Bootstrap reads this FIRST. Convention: [`docs/harness-architecture.md`](harness-architecture.md) §6.

---

**Last overwritten:** 2026-08-07 — **GENERATIONAL HANDOFF at 83% context, mid-boundary-wave**, then **corrected in place by the incoming generation at ~20:30Z** (the line-38 fact below was false at handoff; §1 health and the §3 wave rows are re-read, not remembered). Phases 19, 20, 21 CLOSED. Schema **v34** live. The Phase-21 boundary paydown wave: items 1, 2, coverage_gaps, 3a, 3b **and 4** MERGED (item 4 at `a88a9e08`); **the trail-MA RENDER FIX rides NEXT as its own micro-item** (CHARC-sequenced ahead of item 5 — it recurs on the next daily record of the only live position), then **item 5 executing** (plan merged, my four rulings issued), then the 3-site `pivot < zone_cap` sweep; items 6–7 queued. *(Head/push state moves constantly — **re-read `git status` rather than trusting this line**; it was stale within an hour of the last two writes.)*

---

## 1. PROGRAM STATE — read before quoting any number

**T4 (broad-watch baseline): the CONFIRMED-NEGATIVE verdict is WITHDRAWN — T4 is UNDETERMINED.** Study: `research/studies/2026-08-01-t4-maturity-withdrawal/` (`dd9cfee9`). The August read proved the prior reads measured **resolution speed, not expectancy**: the FIXED June cohort moved **−1.0000 → +0.2129 purely by aging** (read #1's −0.637 sits on that curve at 58.8% closure; losers resolve in days at `initial_stop`, winners take weeks via `ma_close_below`). The frozen 0026 criterion gates on N≥30 and says nothing about MATURITY, which dominates by ~1.2R. **The replacement estimate is POSITIVE (+0.20 pooled age≥30; non-ETF +0.34, Wilson LB 32.9%) and deliberately NOT banked** — 13-day detection window, 2.4 rows/name, inverts at the ≥35 band. **Separately strong: the leveraged-ETF slice = 38 detections / 6 names / ZERO winners, all `initial_stop`** (identified via `candidates.industry='Exchange Traded Fund'`) — a universe-composition candidate lacking the 6-month gate.

**The instrument that came out of it — the EXPECTANCY CONVERGENCE CURVE:** a fixed cohort's estimated mean R vs cohort AGE; the four censoring scenarios bracket the truth (`closed_only` rises, `mtm` falls; **band width = drawn confidence** — 1.33 wide at read #1, 0.059 at 08-01). Curves are RULESET SIGNATURES (selection→asymptote, exits→shape); compare at EQUAL AGE, never wall-clock; the running-from-epoch curve converges to expectancy MINUS a steady-state censoring bias (plot both, never conflate — the operator's steady-state hypothesis is banked in `research/phase-0-tasks.md` with the resolution-time-distribution first deliverable). **Retention constraint: `_SHADOW_EXPECTANCY_KEEP=90` prunes the artifact series — the June curve's −1.0 start EXPIRES ~November** (flagged in the P22 brief §3).

**H1 = 2/20 closed, standard-epoch** (VSTS +$1.80, AMN +$1.17). **The amendment is LIVE at v34** (migration 0034: mean-R + the 50%-concentration cap; win rate demoted to diagnostic; COHORT clause standard-only; the 0008 Wilson original preserved in `preregistered_decision_criteria`, **five-times verified**, canonical 577 bytes / sha256 `6bdd723c…`). **D29 is FIXED and MERGED** — all four cohort readers thread `entry_intent` via `swing/metrics/cohort_intent.py` (H1 criterion-mandated standard-only; H2–H5 epoch-contract-grounded with NO exclusion — the tuition rule binds EXECUTION-QUALITY reads, not SAMPLE counting); the H1 clause is PINNED against the live registry row.

**Posture: STOP-ENGINEERING + MARKET TIME**, except the commissioned boundary wave (all pre-ruled). **Health 2026-08-07:** coverage_gaps GREEN on live post-fix (the clause-1 widening: moved-past = observed OR skip-explained; the 110 = 55 accepted + 55 graced — accepting 08-04 RESTORED the trailing grace's function on 08-06); `invalid_ohlc` **52 vs baseline 23 — CLIMBING, and the climb is accelerating.** Trajectory read off the per-run manifests (NOT the stoplight): flat 23 through 07-30 → **28** (07-31) → **38** (08-05) → **52** (08-07), i.e. +126% in eight days in steps of +5/+10/+14. 29 signals above baseline = **3.4% of the 853-signal population dropped from pricing on top of the baselined 23** — a sample-loss/bias question for the priced cohort the whole T4/curve program rests on. **THE STOPLIGHT WILL NOT ESCALATE:** CALIBRATION B (`research_health.py:962-977`) maps the EXCESS through the same 25%-of-unique_signals RED threshold, so RED needs excess **>213**; it reads YELLOW today and would still read YELLOW after a further 4× climb. **For this class the TRAJECTORY is the instrument and the colour is not** — re-pull the manifest series each read (`funnel.per_hypothesis.*.excluded`), never the stoplight alone. Meets the predecessor's own "queue if it climbs" condition; queue call is with the operator.

## 2. LIVE TRADING STATE

- **FTRE — trade 19 is CLOSED** *(corrected 2026-08-09; this row said OPEN/`managing` — stale since the handoff)*. True entry **2026-07-31 @ 18.80 × 10 sh** (gap-open fill of the GTC 18.89 limit); **EXIT fill 40 on 2026-08-04 @ 18.40, `action='stop'`, `reason='stop-hit'`, `current_size=0.0`.** **REALIZED ≈ −0.092R** (−0.40 / 4.33 risk-per-share; **−$4.00**). **THE FIRST MATURE LIVE-vs-SHADOW PAIR — and it DISAGREES: live −0.092R vs the shadow twin's +0.4433R, ~0.53R apart, with the MECHANICAL ruleset AHEAD.** Mechanism visible in the row: `current_stop` raised 14.47 → **18.40**, stopped out four sessions later, while the shadow trails on the 10/20/50-MA. **N=1 — BANK NOTHING.** **ATTRIBUTION CORRECTED 2026-08-09 — IT IS A *STRATEGY* DATUM, NOT AN EXECUTION ONE, AND MY FIRST FRAMING WAS WRONG.** I floated the stop-raise as "possibly a deliberate risk decision." **It was not discretionary at all:** the 07-31 record stored `trail_MA_candidate_price` **18.39238** while `current_stop` was still 14.47; by the 08-03 record `current_stop` is **18.40**. **He set the framework's OWN computed candidate, rounded to the cent** — the number persisted BEFORE the act, matched after. `maturity_stage`=`pre_+1.5R` and `open_MFE_R_to_date`=**0.598R** on all three records. So BOTH sides of the pair are mechanical — the framework's ADVISORY vs the shadow's RULESET, differing only in WHEN they begin trailing. **Read as an execution finding it would have been exactly backwards, charging the operator with a 0.53R gap his own instrument produced — one inference from being asserted in a September read.** (`trail_MA_period_days` is **21**, not 20.) **Trade 19 is registry id 5 (Broad-watch), NOT H1** — see §3. **The EXIT-side D31 is CONFIRMED on this row:** fill 40's `schwab_source_value_json` says `exit_date 2026-08-03`, operator-corrected to `2026-08-04` — the defect already fired and he already hand-fixed it; entry+exit are ONE defect with two sites. **THREE KNOWN RECORD DEFECTS, all routed:** `entry_date` says 07-23 (the D31 Schwab auto-fill mapper defect — order-ENTERED date, not execution; wave item 5 fixes it; symptom history; **CORRECTED LIVE 2026-08-11 — correction 38**: `entry_date` 07-23→**07-31**, the three coupled rows moved atomically (`fills[39]` 07-31T16:00:00, `watchlist_archive[143]` 07-31), `pre_trade_locked_at` deliberately UNMOVED per my ruling (b), **disc 95 closed `journal_corrected`** — never `mark_unmatched`, which would have been a false statement); `hypothesis_label` = Broad-watch though his rationale says `aplus-setup` (the H1-cohort relabel question, operator's frozen field); stop 14.47 (the watchlist value) vs the frozen fire invalidation 14.88; risk $43.30 vs the $37.50 cap (pivot-basis sizing, since fixed).
- **AMN latch — FILLED 2026-08-07 (trade 20); see the trade-20 entry in §5.** *(This row previously read "the 6th A+ fire ever, 07-31" — **BOTH WRONG**: the fire's action session is **08-03** (07-31 is its data-as-of date) and it was the 6th **POST-EPOCH** fire, not the 6th ever. Corrected 2026-08-10; the dedup rule + all three counts are in §5.)* Frozen pivot **36.27**, invalidation **30.85** (close-based — **NOT the 30.18 in the 08-07 candidate row; freeze-at-fire governs**), mandate limit **37.35**, 5 sh, horizon 2026-09-14. **The GTC stop-limit WAS placed and RESTED through the setup's decay to `skip`; an intraday spike filled it at 36.43.**
- **VSTS latch ARMED, OFF-SCREEN since 07-30** — renders **UNVERIFIABLE** post-3b with its streak frozen (3 failed sessions named). Frozen 16.90/13.40. **The operator can now DECLINE it** (the corrective path, armed in 3a); his resting buy-stop at 16.90 is inert below the pivot.
- **LQDA — a HYPREC (near_trigger) order, NOT a latch:** his stop-limit implements run-135's rec (buy-stop 89.51, cap 92.19, stop 69.71). **The panel's stray warning is a KNOWN FALSE POSITIVE for this order** — and it was WITNESSED LIVE during the 3b gate (08-07): the panel renders `ORDER_RESTING_LATCH_CLEARED` (its loudest alarm) for resting BUY order `1007505155646`, "matches NO latch's frozen prices … so there is no mandate to log a cancel against." The stray-order erosion behind the §3.5 latch-on-acceptance demand is **on screen today, not hypothetical** — cite it when that item is scoped. If it fills: record promptly, label from the FIRE's run, `standard` intent unless the matcher assigns an H2/H4 cohort — **and HAND-VERIFY the entry date = execution date (it is D31's exact trigger geometry).**
- **The A+ latch posture** (07-23) + the six-rung ladder govern; latch fills label from the FIRE, not the flickered bucket.

## 3. IN FLIGHT — the boundary wave (CHARC's brief `docs/phase21-boundary-paydown-commissioning-brief.md`; every item pre-ruled; THE RULING GOVERNS over any restatement)

| Item | State | My gate |
|---|---|---|
| 1 D29 · 2 sizing-basis · coverage_gaps · 3a declined | **ALL MERGED** (gates passed; live-verified) | done |
| **3b criteria_lapsed (report-only)** | **MERGED at `d45aa6eb` (ff-only, 22 commits); item 3 CLOSED.** RD gate passed `20260807T191144Z`; **operator witness PASSED on two steps** — VSTS = the UNVERIFIABLE off-screen demo (not a clear), **FTRE = 7 failures vs threshold 5 and STILL ARMED, i.e. report-only witnessed at the point where it bites**. A third step was cancelled because the surface did not exist (the calibration read is MINE, not operator-facing) — correct call, the review-ladder lesson applied. Suite on the merged head **10470 passed / 7 skipped / 0 failed** | **DONE — byte-identity verified twice, independently:** orchestrator by patch-id (`63a330b8..706d1ca8` == `e5140606..HEAD` == `abdaed36…`) pre-merge; me by tree diff post-merge (complete delta `706d1ca8`→`d45aa6eb` = 5 docs, all main-side; `swing/`+`tests/`+`scripts/`+config delta **empty**) |
| **4** cancel-decoupling + declined render/route **+ flag B (its WRITE half)** + PRICE_DP×4 + the below-pivot refusal (equality refinement verbatim) | **IN FRONT — the next dispatch, awaiting the operator**; the artifact-scale watch attached (splits like item 3 if findings cluster as interactions); **does NOT carry the `service.py:140` deletion** — that shipped inline at `b81da672`, an ancestor of this plan's base; the plan correctly omits it (orchestrator caught this row stale, 2026-08-08 — same acked-vs-actioned class, flagged before it cost a lap) | **PLAN QA'd PASS, plan `4858abfe` on `item4-cancel-decoupling`; ONE arc (41 findings/13 rounds, none crossing a piece boundary). THREE RD RULINGS ISSUED 2026-08-08** (cancel/flag-B: NO; cancel-EXACT: the CODE is wrong, not the docstring; legacy decline rows: ACCEPT on an EMPTY population). Merge QA still mine |
| **5** D31 entry-date mapper + the correction surface + A-4 discrepancy-type **+ D-2b (the cancel→validity→place attribution chain) + the `mappers.py:285` legless-skip (RD-ruled 2026-08-08)** | **PLAN QA'd PASS (`21c69f58`); my FOUR rulings issued 2026-08-09; executing on the operator's word.** **BEFORE the September read — but NOT for the reason I gave: `trade 19 is an H1-cohort row` was FALSE** (it is `hypothesis_label='Broad-watch baseline (watch); failed: tightness'` → registry **id 5**, not id 1; **H1's 2/20 does NOT move**; my own §1 listing H1's two as VSTS+AMN already refuted it — the corroboration failure between two sections of ONE file). **The real ordering constraint: FIX THE DATE FIRST, THEN DECIDE THE LABEL** — the relabel question (his rationale says `aplus-setup`) is the operator's frozen field, and if it resolves toward A+ the row moves INTO H1; that decision must not be made against an entry date known to be wrong. **GATE: PASS 2026-08-10 (executing QA'd, R21 converged on the shipping tree, 10762 green, +245; all four rulings verified ON THE BRANCH by me). **D-3 RE-RULED — my "empty" was a PROXY, not the ground:** the table gained its first 2 legitimate rows (OII place+validity — the 21-B machinery WORKING) and my stated condition failed while the TRUE one held. ACCEPT restated on the real ground: *no row whose `action_session_date` can reach the ladder as a terminal date* — the roster-derived D-1 test (`test_declined_terminal.py:627`) is the OPERATIVE guard (a hand-listed test would NOT have covered this); **void if a `decline` row exists at merge — one SQL re-count owed at merge.** A ruling stated on a proxy voids itself on the first innocent change. **(c)-BY-PRODUCT, unpredicted: refusing to move `pre_trade_locked_at` FROZE every metric keyed off it** — my §5.3 read-obligation now covers FEWER metrics; re-derive WHICH at the September read, don't assume. **Trailer blemish: 7 commits with `Mutation:` paragraphs parse as trailers — Co-Authored-By ZERO (re-run by me, key-filtered; streak SAFE); my rec to the operator = REWORD pre-merge** (they are mutation-verification PROVENANCE; leaving them = 7 standing false positives in the streak's own audit). **Remaining: operator witness on the live trade-19 correction (deliberately unexecuted), then merge + byte-identity + the D-3 re-count.** RULINGS as issued: (a) `MATERIAL_BY_TYPE`=1 — a category change must NEVER smuggle a severity change; assert the VALUE, not the key (a presence check is what a wrong value satisfies). (b) `pre_trade_locked_at` CANNOT select a branch — it is NOT EVIDENCE: `entry_date + 'T16:00:00'` on 20/20 trades, ZERO exceptions. ONE template; do NOT move it (that would invent a lock time); NAME the staleness in the reason. (c) every §5.3 move runs TOWARD truth — correct them all; R-multiples correctly do NOT move (R is entry_price/initial_stop, untouched by a date); the plan's completeness withdrawal RATIFIED (7→11→13→16 across rounds).** **MY OBLIGATION from (c): the September read must not compare a post-correction capital-cycle/holding-period figure against pre-correction history without saying so.** LQDA is a live instance-in-waiting **whose fill lands through the mappers path** | Merge QA; the trade-19 retro-correction witness. **`mappers.py:285`: `map_orders_to_fill_candidates` (`:241`) returns ONLY the list — the legless `continue` at `:284-299` logs a warning and propagates NO count to `trader.py:374/431`, so nothing reaches `warnings_json` or any recon audit. It feeds Pass 2, so a dropped occurrence is a FALSE NEGATIVE ON A FILL: recon sees no fill, raises no discrepancy, reports clean. THE INSTRUMENT DOES NOT ALARM, IT GOES QUIET** — D25's family, and CLAUDE.md gotcha #27 in the Schwab mapper rather than a pipeline step. **Minimum fix = COUNT-AND-SURFACE, never stop-skipping** (refusing would fail the whole fetch on a legitimately legless parent-conditional row — the resilience is correct; the INVISIBILITY is the defect). **No incidence asserted — not measured; the ruling rests on the ASYMMETRY: a silent drop on the fill path cannot be self-limiting, because the surface that would reveal it is the one being dropped from.** **D-2b correction, MINE: my ruling said "every hop is schema-guaranteed" — FALSE.** `0033` has exactly two UNIQUEs (`(candidate_id,view_session_date,surface)`; `(idempotency_key)`) and **none on `actual_broker_order_id`**, so hop 1 can match ZERO or SEVERAL; only the LAST hop is guaranteed (`:470-471` + the `:796-817` trigger). Item 5 must answer `undetermined` on zero AND on multiple. **Rule accepted (orchestrator's): a ruling's REASONING is binding; its incidental factual claims are still claims — a director's sentence is not a citation.** |
| 6 D32 backups (move-then-retain) · 7 the D9 ambient sweep | queued | — |
| **The micro-item, CONTENT CORRECTED 2026-08-07 20:30Z — the R6 expiry-tie fix ALREADY SHIPPED; what remains is deleting the comment that DENIES it.** The prior text ("recorded-not-fixed in 3b") was **FALSE at handoff**: the fix is live on main at `swing/latches/service.py:1222` (`if not same_pivot and supersede.order_key < final_probe.order_key` inside clause (iii)) with three discriminating tests, as **`6f54221b`** — *post-rebase; the orchestrator's messages cite the pre-rebase `7e615f04`, which is **NOT** reachable from main, so `git merge-base --is-ancestor 7e615f04 main` answers NO. Cite `6f54221b`, or better `git log -S'if not same_pivot and supersede.order_key' -- swing/latches/service.py`.* | **Delete the stale 3a-era deferral block at `service.py:140-145`.** It is worse than stale: it makes affirmative FALSE claims about current code — *"branch (b) — the only place a `superseded` candidate is built — never runs"* and *"Recorded here, NOT fixed here"* — forty lines above the implemented fix. **The comment and this row were CORROBORATING each other: a reader who checked the code to verify the row found the code agreeing with it. Diligence got punished.** | **DONE — `b81da672`, comment-only, on main** (operator ruled it inline rather than folding into item 4). QA PASS, verified independently: one file, and a mechanical filter over the diff shows zero non-comment changed lines. **THE EXECUTION FOUND A FOURTH FALSE CLAIM — inside the half I directed be PRESERVED.** The kept sentence bundled two propositions: *"stamped by the FOLD, not selected here"* (true) *"so its rank governs exactly ONE comparison"* (**false** — `_Terminal.order_key` is `tuple[date,int]` at `:161` and is consumed at FIVE sites, `828/849/951/1190/1219`; never one, not even before R6). It survived only because the direction ALSO delegated the seam. **A bound that is only safe when ignored is not a bound.** |
| **Phase-22 demand** (`docs/phase22-rd-demand-brief.md`): zone-range render + knob REMOVAL (operator ruled NO KNOB; the config-vs-two-sources pin) + the shadow emit cols · capture-basis SECOND LOG (tier-3 = materialization only) · the curve surface (band render, pre-registered populations, the KEEP=90 retention constraint) · **§3.5 LATCH-ON-ACCEPTANCE for hyprecs** (see §4) · carried items | CHARC decomposes at the boundary | — |

**Comms state:** singular inboxes; a fresh orchestrator generation took over 08-07 (clean handoff, `docs/orchestrator-handoff-2026-08-07-boundary-wave.md`). Six recipe items landed at `60488762` (the anchored ^ERROR grep, the footer assertion, the redirect check, gate-vs-ritual, premise-verification, both-modes). **Five of the six were found by implementers against their own work — endorse as its own category at any phase record.**

## 4. RULINGS THE ARCS ARE BUILT ON — a fresh instance must not contradict these

- **THE SIX-RUNG LADDER (R6): `fill > declined > superseded > invalidation > criteria_lapsed > horizon`.** Superseded above invalidation: a re-fire is an affirmative CURRENT fact — "re-based" must not file as "went stale". **R5: decisions are PER-LATCH, never retroactive** (L10 + unit-is-the-latch).
- **`criteria_lapsed` is REPORT-ONLY until armed** (config flag, default off — N is underived and the declines-then-reverses residual is real). **Any arm-flag ruling of mine carries the BOTH-MODES test requirement AT the ruling.** The OQ set: Amendments A+B ratified (lifetime 2a with completeness, bounded at the window's own terminal; ends-at-its-low); **2a tests the HIGH** (the entry-trigger question; constraint 6 stands for invalidation — both err toward mandate-preservation); the **bar-dated** window; the two-term materiality floor (1.0×ADR / 2.0% pivot, NULL→unverifiable, never a substitute); **PAUSE** (2b's completeness makes the price evidence continuous); OQ-18 SPLIT (a within-session pass beats a non-verdict; across sessions the tail rule governs); OQ-16 the rung sits ABOVE telemetry health (rung 5 gates only telemetry-DEPENDENT classifications); critical-stale **YES** (alarms encode DUTY; blame lives in the disposition); Option B (the state reuses `horizon_expired`; the REASON is first-class — my words were disposition+reason, never state).
- **THE ARMING READ (item 3, binding on me):** report-only would-fire counts are a **LOWER BOUND on armed churn** (topology change under-counts, biasing TOWARD arming). A safe-looking bound is NOT sufficient to arm — corroborate on the topology-stable subset; a costly-looking bound IS sufficient not to.
- **`declined`:** operator-authored terminal, uniform, effective session = **CURRENT at POST, server-computed; a stale form anchor → the beacon's reject-with-notice. Backdating impossible by construction** (flag B — its write half lands in item 4 with the render/route).
- **LATCH-ON-ACCEPTANCE (hyprecs):** A+ fires are MANDATES (auto-latch); hyprecs are OFFERS (3–7/session vs 6 A+ fires ever) — **the PLACE INTENT creates the latch**; accepted-offer latches are a **SEPARATE POPULATION in every rate** (the stage-3-gating away rate was defined over mandates).
- **Standing families:** freeze-at-fire; the stale-data asymmetry (alarm-never-assert; characterisable + self-limiting); zero-data renders NOT-YET-MEASURABLE, never clean; recording-vs-alarming are different functions (the affordance to record is never gated on the alarm); never merge categories differing in EVIDENCE KIND; a surface's share count matches the price THAT surface proposes; sizing basis = the LIMIT (the worst fill); "machinery comes out" = claims + renders, NEVER a persisted column unless named with its migration cost; a clearance attaches to the TREE verified (docs-only grepped-not-assumed lands; code/tests re-open); ONE supersession per message; a not-fixed on a director-banked item goes in the RETURN'S FLAGGED LIST, not only a comment.

## 5. RD-OWNED FOLLOW-UPS — with deadlines

- **THE WATCH-STANDARD AMENDMENT (owed, before the September read):** (a) the T4-class MATURITY GATE — a decision read may not execute below a stated closure threshold; operationalize via **bracket width < distance-to-the-decision-boundary AND cohort age past the winner-resolution window**; (b) the item-3 lower-bound discipline for any arming read; (c) the lapsed-live-vs-shadow EXPECTED-divergence line once `criteria_lapsed` arms (OQ-3: the divergence IS the rule's measured value).
- **MONTHLY READ #3 — first trading week of September:** re-state T4 with the curve/bracket instrument at equal age; the ETF universe-composition question; H1 at whatever trade 19's resolution adds (its label + D31 must be fixed FIRST — items 4/5 precede the read).
- **THE A+ FIRE COUNT — DEDUP RULE STATED 2026-08-10 (it is NOT new: R6 already supplies it).** **A fire is a MANDATE = distinct `(ticker, frozen pivot)`; a SAME-pivot re-fire RE-CONFIRMS, a DIFFERENT-pivot re-fire SUPERSEDES** (the R6 ruling live at `service.py:1219` — the count must not get a second, private rule). **Numbers, each with its method** (`candidates`⋈`evaluation_runs`, `bucket='aplus'`, UNCAPPED): **13 raw rows · 10 fire-sessions · NINE MANDATES ever · 7 post-epoch.** 10→9 is SLDB 04-22 + 04-24 both at pivot 8.866 = one mandate re-confirmed. Mandates: SLDB 8.866 · YOU 59.515 · NVCR 18.67 · VSTS 13.56 · AMN 33.48 · FTRE 18.34 · VSTS 16.90 · AMN 36.27 · **OII 53.98**. **"THE 6th A+ FIRE EVER" RECONSTRUCTS EXACTLY — THE ARITHMETIC WAS RIGHT AND THE WORD WAS WRONG:** it was the 6th POST-EPOCH fire (NVCR 06-18, VSTS 06-25, AMN 07-01, FTRE 07-20, VSTS 07-27, AMN 08-03), and `rd-state` called it *"ever"*. **A SCOPE WORD DOING LOAD-BEARING WORK WITH NOTHING MARKING IT — and the SAME clause also had `data_asof` standing in for the action session. Two silent scope errors in one sentence.** Latch-on-acceptance population argument UNCHANGED (3–7 near-triggers PER SESSION vs 9 EVER is starker, not weaker) — but the corrected figure + this rule go into the demand BEFORE Phase-22 scoping.
- **T5 RESOLVED 2026-08-10 — OII A+ (pivot 53.98), the 7th post-epoch fire / 9th mandate ever: TAKEN.** Operator confirmed: **buy stop-limit resting per the A+ fire, waiting for the pivot.** Sequence verified against the run record: the fire run executed **08-07T17:30 (Friday) for action session 08-10** — so the order placed during the item-4 witness came AFTER the fire, per the mandate. Clean, unlike AMN. **Frozen at fire: pivot 53.98 / `initial_stop` 41.42** (the 08-10 fire row — NOT the 40.50/37.89 in older rows; freeze-at-fire governs). **The first mandate born with the full 21-B machinery live** (cancel affordance, decision surface). **At fill it labels `standard` from the FIRE per the latch posture — unambiguous provided the setup HOLDS. If it decays before the pivot is reached, this is the first live case for the AMN doctrine bound** (a latch does not survive its own invalidation; a fill after that point does not label from the fire) **with the machinery in place to act — watch it.**
- **THE ADMISSIBILITY TEST — the evidence rule's FINAL form (2026-08-12, forced by CHARC's Demand-A question).** My rule said *"the framework's OWN contemporaneous record."* **The threat model is TIME, NOT AUTHORSHIP** — what it stops is a judgment formed AFTER the outcome is known, which is indistinguishable from diligence precisely because the person now knows how the trade ended. I wrote "framework's own" only because in every case I had, the framework's record WAS the contemporaneous one. **THE TEST: evidence is admissible for a cohort/intent assignment iff (a) RECORDED BEFORE THE OUTCOME WAS KNOWN and (b) IMMUTABLE SINCE.** Framework-derived records satisfy both by construction; **operator-authored text satisfies both when the framework captured it at entry and cannot amend it** — trade 20's `why_now` (*"Accidental entry while working on latch/unlatch mechanism"*) written 08-07, four days before the 08-11 stop-out, — **CORRECTED 2026-08-12 16:55Z, CHARC's catch: my "WRITE-ONCE, no `UPDATE` path anywhere" was FALSE.** `reconciliation_auto_correct.py:2060` interpolates any PRAGMA-valid column (`UPDATE trades SET {field_name} = ?`) and `_RESERVED_JOURNAL_FIELDS` holds only FOUR entries (`trades.entry_date`, `fills.fill_datetime`, `fills.action`, `fills.trade_id`) — **`notes` is not reserved, so it IS writable through the audited corrector.** **MY FAILURE IS WORSE THAN THE GREP BLIND SPOT: I had ALREADY READ that allowlist this same session and reasoned from it that the COHORT KEYS are writable — then left an earlier claim of my own standing in contradiction. I held the general fact and did not propagate it to the claims it invalidates; two artifacts corroborating each other, except both were mine.** **THE REPAIR (CHARC's, adopted verbatim — and it is the same move I made on their Demand-C rule): TIER-2 ADDITIONALLY REQUIRES THAT NO `reconciliation_corrections` ROW HAS TOUCHED THE CITED FIELD ON THAT TRADE. IMMUTABILITY IS VERIFIED AGAINST THE AUDIT TRAIL, NEVER INFERRED FROM WRITER-ABSENCE** — strictly stronger, because *"no writer exists" is a claim with a shelf life* and rots silently the moment a future arc adds one. **LIVE CHECK: 37 corrections total (`fills.fill_match` 19 · `fills.price` 13 · `trades.current_stop` 2 · `cash_movements.net_amount` 2 · `trades.entry_date` 1); ZERO on any `notes` field ever — so trade 20 is admissible ON EVIDENCE.** **The apparent loophole closes itself: staking a claim before the outcome IS PRE-REGISTRATION — the same property that makes migration 0008 credible.** **TWO-TIER ADMISSION ENDORSED** (structural citation where the instrument existed; contemporaneous-capture attestation where it **PROVABLY** post-dates the event — `latch_order_intents`' earliest `recorded_ts` 2026-08-08T20:01 vs trade 20's 08-07 entry — with the TIER and its reason recorded on the row, per the 0034/parity-ledger house style). **CHARC's correction accepted and material: trade 20 is an entry NOBODY DECIDED TO MAKE, not an off-mandate choice — and `trade_origin='manual_off_pipeline'` makes "manual"/"discretionary" look right FROM THE COLUMN ALONE, which is why `unintended_execution` must name the CLASS, not the mechanism.**
- **REPORT EVERY ABSENCE WITH THE SEARCH THAT PRODUCED IT (CHARC's rule, adopted 2026-08-12 — the absence-form of report-every-count-with-its-method).** THREE roles produced this shape in ONE day — CHARC asserting a tripwire without grepping; the orchestrator grepping ONE file and reporting no such rule existed anywhere, inside a message about existence≠completeness; me reporting "no update path anywhere" from a column-name grep that structurally cannot see a DYNAMIC writer (the column name arrives at runtime through an f-string identifier slot). **The common property: each search was SOUND FOR THE SHAPE IT LOOKED FOR and blind to a shape that exists — so the answer is a METHOD, not more diligence.** *"No DIRECT UPDATE of `trades.notes` in `swing/`"* would have been true, self-limiting, and would have invited the check that found it. **Any "which code writes column X" search must ALSO cover the interpolated-UPDATE shape (`UPDATE <table> SET {`, `SET {field`) and the PRAGMA-derived-allowlist pattern.**
- **THE GUARD THIS CREATED, which neither director had: DEMAND B's ANNOTATION ROWS ARE PERMANENTLY INADMISSIBLE AS TIER-2 EVIDENCE.** They are written AFTER entry by construction — their whole purpose — so they fail test (a) always, while otherwise LOOKING like ideal evidence (framework-captured, immutable, provenanced). **Without the guard, the surface I demanded to record what the record cannot express becomes the channel through which intent is curated after the fact — my own demand defeating my own rule.** Must appear in ALL THREE briefs (A, B, C); it is invisible from inside any one of them. *(Note `pre_trade_locked_at` would have looked like perfect tier-2 corroboration — framework-written, timestamped, entry-time — and is a synthetic restatement. The only reason this ruling does not bless it is that somebody counted it.)*
- **DEMAND C PLAN: RD GATE PASS 2026-08-12 — four rulings issued, two catches were MY errors.** **Q1: `<=` ("must not POST-date")** — the anchor compares SESSION labels but test (a) is about CREATION time; a session-N record is created the evening of N−1, so it strictly precedes any session-N fill even at equal labels. Strict `<` refuses 10 of 11 live rows INCLUDING trades 17/18 — **a rule that refuses its own derivation base is mis-encoded, not strict.** **Q2: the LAST-WORD guard's refusal shape RULED CORRECT — it IS the AMN doctrine bound, ENCODED:** the routed "ticker reappears as `skip` before the fill → uncorrectable" case is literally AMN (fire → decay → `skip` on the entry session → spike fill), and the guard refuses exactly that correction into H1 while admitting CADL (no run-138 row; 12341 stays the last word). **CADL = positive control, AMN = negative control — the distinction I said the bound must draw, derived from citation data.** Asymmetry governs: a wrong refusal costs a legible message; a wrong acceptance contaminates the 2/20 money cohort invisibly. **The formal bound write-up cites this guard as its operational encoding.** **CATCH 1 (mine, the sharp one): my brief's target label was read off SIBLING trades 17/18 — the composition MY OWN RULE forbids, committed in the brief commissioning the rule's encoding.** The faithful derivation from CADL's own candidate is the target: **`'A+ baseline (aplus); failed: TT8_rs_rank'`** (12341 carries `TT8_rs_rank='na'`); membership unaffected; the suffix is TRUE of the fire row and is carried, not stripped. **CATCH 2 (mine): "2/20 → 2/21" was unverified intuition** — measured: `in_flight` 0→1, `current_sample` 2→2 (closed only), target 20 static; becomes 3/20 at CADL's close. The banked verify-premise-arithmetic class, my instance — a test to my arithmetic would have ENFORCED my error. **Plan is NOT converged-clean (R10–11 fixes unreviewed, implementer-flagged) — never cite it as converged; covered by executing-A + B-gate.** **BANKED AS MINE from the clock-domain CRITICAL: `evaluation_runs.run_ts`/`pipeline_runs.*_ts` are naive LOCAL, `hypothesis_status_history` is naive UTC, ~10h apart, compared as text — any RD read WINDOWING ACROSS the two domains carries a ±1-session boundary hazard. The September read normalizes before comparing.**
- **DEMAND C ROUTED 2026-08-12 (operator-concurred) — an AUDITED PROVENANCE-CORRECTION surface for the COHORT KEYS** (`hypothesis_label`, `candidate_id`, `trade_origin`). **Verified: NO `UPDATE` path for any of the three anywhere in `swing/`.** The reconciliation-correction machinery's allowlist is PRAGMA-derived and WOULD admit them, but it requires a DISCREPANCY to hang the correction on and there is none (nothing disagrees between broker and journal) — **minting a synthetic one is exactly what item 5's plan refused** (no `discrepancy_type` truthfully describes an operator mis-reading his own order review). Shape = item 5's `correct-entry-date` generalized; rides with Demands A/B; sequencing CHARC's. **THE BINDING CONSTRAINT IS THE EVIDENCE RULE, NOT THE MECHANISM: a cohort assignment may be corrected ONLY to what the framework's OWN CONTEMPORANEOUS RECORD says — never to a later judgment — and the correction CITES the record it corrects TO, structurally.** **CHARC's ADDITION ADOPTED (it closes a hole I left): structural citation prevents FREE-TYPING but NOT citing a LATER record — so THE SURFACE REFUSES A CITATION WHOSE RECORD DOES NOT PRE-DATE THE FILL.** Contemporaneous becomes a COMPARISON the code performs, at one predicate. **THE ANCHOR IS A GOTCHA-30 TRAP AND I NAME IT: compare against the CITED ROW'S OWN session anchor** (`candidates` → its `evaluation_run_id` → `evaluation_runs.action_session_date`; `daily_recommendations.action_session_date` on its own row) — **NEVER `evaluation_runs.data_asof_date`, a cohort MAX that would stamp a lagging row fresher than it is. The wrong version passes every test seeded from CADL**, whose rows are genuinely contemporaneous. **SEQUENCING: C FIRST (CHARC's answer, better than mine — C needs no vocabulary decision because the target string is READ OFF the H1 rows, and it alone carries an external deadline); I withdraw "rides with A/B".** **H1 is the money question at 2/20; post-hoc cohort assignment on judgment is the cheapest way to flatter it and would look identical to diligence at the time. A surface that lets an operator TYPE a hypothesis label is a surface that lets a future operator CURATE H1** — so the citation must be structural (name the `candidates` + `daily_recommendations` rows, refuse without them), never free text. CADL satisfies it: candidate 12341 + `daily_recommendations` 172 were both written 08-11, BEFORE the 08-12 fill.
- **TRADE 20 (AMN) IS CLOSED — 2026-08-11 @ 33.73, stop-hit (fill 44, 5 sh) vs entry 36.43 / stop 33.72 → REALIZED ≈ −0.996R (−$13.50).** *(This row said OPEN with size 5 until 2026-08-12; the orchestrator handoff says the same. **The open set is now LQDA (2) · ORKA (1) · CADL (18).**)* **It is the DELIBERATE-NULL row — so the project now carries a CLOSED trade WITH A REALIZED RESULT in NO cohort at all.** Correct under my ruling (neither available value is true) and **the sharpest argument for Demand A's priority: the vocabulary gap has stopped costing us a LABEL on an open position and started costing us a RESULT.** It also closes the AMN arc's own evidence — the mandate whose order outlived its invalidation, filled on a spike, stopped out four sessions later for a near-full-R loss. **N=1, no inference drawn** — but the entry my doctrine-bound would have prevented is now a closed −1R, and that belongs in the bound's write-up as its motivating case rather than a hypothetical.
- **TRADE 23 (CADL) — BOUGHT 2026-08-12, AND THE RECORD SAYS IT IS AN A+ MANDATE FILL, NOT A HYP-REC. OPERATOR-FLAGGED FOR BACKFILL; THE LABEL QUESTION IS HIS.** *(Operator's note: "CADL was just bought. It was a hyp-rec yesterday but fell off today with an active order. hyp-rec was NOT captured in stock buy and will need to be backfilled.")* **What the live record says, queried 2026-08-12:** the 08-11 action session has CADL at `bucket='aplus'` (candidate **12341**, pivot **10.8149995803833**, stop 9.161) and its ONLY `daily_recommendations` row is **`recommendation='today_decision'`** (id 172, `rationale='A+ setup, 5.5% ADR, 113% prior trend'`, action_text *"Buy-stop $10.81 × 19 sh × $37.41 risk"*). **The vocabulary is `near_trigger` (171 rows, the hyp-recs) vs `today_decision` (11 rows, the A+ mandates) — CADL is a `today_decision`.** By 08-12 CADL is **absent from the candidate set entirely** (0 rows; the 08-12 run exists, data-as-of 08-11). **So it is a 10th A+ MANDATE under my dedup rule (CADL @ pivot 10.815) and the 8th POST-EPOCH fire** — my §5 mandate list ended at OII 53.98 and must be extended. **And it may be the FIRST CLEAN one:** the buy-stop triggered THROUGH the pivot as the posture intends (entry **10.82** vs mandate buy-stop 10.81 / entry_target 10.815; 18 sh vs the mandate's 19; stop 9.16 vs 9.161) — unlike AMN (decayed to `skip`, invalidation breached) and unlike FTRE (gap-open fill of a GTC limit). **THE RECORD AS IT STANDS: `entry_intent='hypothesis_test_by_design'`, `hypothesis_label=NULL`, `candidate_id=NULL`, `trade_origin='manual_off_pipeline'`.** **Under the epoch contract an A+ fire taken is `standard` (H1); `by_design` is reserved for H2/H4 program fires — so as recorded this is a T7-shaped label on the H1 cohort's most precious sample type, and H1 is 2/20 and starved.** **NOT asserting an error — verify intent before labelling one:** the operator's own word was "hyp-rec", and if he believed it was a `near_trigger` the `by_design` label follows from that belief rather than from a slip. **The question is his and it is posed, not ruled.** **RESOLVED BY THE OPERATOR 2026-08-12: "it was a mistake of mine when reviewing the order for entry. CADL is an A+ fill, NOT a hyp-rec" — the record was right; his read of it was not.** **STEP 1 DONE + VERIFIED BY ME ON THE LIVE DB: `entry_intent='standard'`** (via `swing trade backfill-intent --trade-id 23 --force`; note the prompt default was `skip`, because `suggest_entry_intent(NULL)` returns None — Enter would have written nothing). **STILL OWED, and it needs DEMAND C (below) because NO update path exists:** `hypothesis_label` → **`'A+ baseline (aplus)'`** — the exact string DERIVED from the two existing H1 rows (trades 17 VSTS, 18 AMN), read off the cohort rather than composed — plus `candidate_id` → 12341 and `trade_origin`. **Until the label lands CADL matches NO hypothesis and is invisible to every cohort read: a genuine A+ mandate fill absent from H1. Must precede monthly read #3 (first trading week of September).** **Structurally IDENTICAL to AMN in shape** (mandate placed on-screen → name falls off → resting order fills off-screen) **and DIFFERENT in substance** (AMN decayed below its own invalidation; CADL simply left the screen after triggering) — which is precisely the distinction the AMN doctrine bound must draw, so **CADL is the positive control for that bound.**
- **TRADE 20 `entry_intent` NULL IS DELIBERATE (operator ruling, 2026-08-10) — a DECISION, not an omission.** It stays NULL **until Demand A completes** (none of the two existing values is true; the vocabulary cannot yet describe the entry). **Anyone "helpfully" setting it to `standard` would contaminate the H-cohort denominators — do not touch it.** Demands A+B are **pre-emptively GO gated on item 5 + sweep completing satisfactorily**; a scope-changing finding voids the go. **A go is NOT ratification:** §VII.F ratifies SPECIFIC AMENDED TEXT (the 0034 shape — sha256-pinned, independently derived), none exists yet, and the wording + the third value's NAME return to the operator.
- **WAVE-CLOSE WARNING CLEANUP (operator-originated, 2026-08-10): after the wave completes, a small pass to correctly clear standing warnings — MOSTLY existing tools.** The inventory as I hold it: **VSTS** (UNVERIFIABLE, off-screen since 07-30) → the 3a `declined` terminal + cancel the broker buy-stop + log the cancel via item-4 machinery — all existing; **disc 95** → CLOSED (correction 38, item 5 witnessed); **discs 96/97/98 (trade 19's per-run re-emission siblings, all pre-correction) → CONDITIONAL: the FORWARD TEST decides — I ruled they close `journal_corrected` (EACH with a reason describing ITS OWN row; 97/98's ambiguous-match text ≠ 95's zero-delta text) ONLY IF the next nightly reconciliation run emits nothing new on trade 19; a fifth emission = they are LIVE and the D31 fix did not take. I OWN THE WATCH — read trade 19's discrepancy set after the next recon run, post either way**; **AMN latch** → should clear as `fill` (trade 20) in the derivation — verify at wave close; **LQDA stray-order warning** → CANNOT be correctly cleared by existing tools while the order stays wanted — it is the §3.5 latch-on-acceptance case, so it persists BY DESIGN until Phase 22 (or clears if the order fills/cancels); **trade-20 intent** → waits on Demand A (above), not cleanup; **`invalid_ohlc` 52** → a watch, not clearable. Timing aligns with the operator's own gating of Demands A/B (after item 5 + sweep).
- **BANKED (orchestrator's catch, credited): FREEZE-AT-FIRE EARNED ITS KEEP ON A LIVE CASE.** Verifying AMN's invalidation, the 08-07 candidate row carries `initial_stop = **30.18**` while the LATCH froze **30.85** at the 08-03 fire. **30.18 is the number a later reader reaches for, and it would have made the invalidation look UNBREACHED.** He went to correct me and confirmed me instead.
- **GLANCE ADJUDICATION 2026-08-13 (operator-run, 4 ATTENTION lines):** T3 = the numerator moved 3→4; **the 4th priced A+ signal is AMN detection 08-03 — the decayed-latch fire's own shadow twin**, entered mechanically on the same 08-07 spike, open at `horizon_mtm` ≈ **−0.78R** (backed out of n=3→n=4 means). **A SECOND live-vs-shadow pair is forming: AMN live CLOSED −0.996R vs shadow ≈ −0.78R — AGREEING in direction, unlike FTRE's 0.53R disagreement. N=2 pairs; bank nothing.** No hand-walk owed (golden gate done; 1-in-5 cadence untripped at 4). · AMN NULL-intent line = KNOWN-DELIBERATE, fires until Demand A lands — tolerate, do not special-case the glance. · **Both T7s VERIFIED LEGITIMATE:** LQDA + ORKA labels prefix-match registry id 2 (H2 program fires).
- **TRADE 21 (LQDA) CLOSED 2026-08-12** — stop fill 46 @ **84.70** vs entry 91.81 / initial stop 69.71 (risk 22.10/sh) → **realized ≈ −0.32R (−$14.22)**; `current_stop` was 84.86. **A CLOSED H2-labelled trade: H2 moves 1/10 → 2/10 at the September read.** **OPEN SET NOW: ORKA (1 sh) + CADL (18 sh)** *(three written inventories — mine, the orchestrator's handoff, CHARC's pointer — went stale on the open set within ONE day; re-derive, never quote)*. CADL's NULL label lands via Demand C executing (dispatched on the operator's go 2026-08-13).
- **GLANCE 2026-08-15/17 ADJUDICATED + SPOT-CHECK #4 DONE (T3 cadence satisfied at 5 of 10 priced).** T3: the 5th priced A+ signal is **CADL detection 08-11 — HAND-WALKED against raw bars, EXACT reproduction:** trigger 08-12 (high 10.900 ≥ pivot 10.815), fill 10.815 = max(pivot, open 10.710), stop 10.500 = entry-bar low, stopped 08-13 (low **10.475** pierced by 2.5 CENTS), (10.500−10.815)/0.315 = **−1.0000R** ✓. **THE THIRD LIVE-vs-SHADOW PAIR, and it DISAGREES in the OPPOSITE direction from FTRE: shadow −1.0R, live OPEN and AHEAD** (entry 10.82, stop 9.16→10.07, the 08-13 low 10.475 never came near it; close 11.80 same day). **Mechanism precise: STOP GEOMETRY — the shadow's entry-bar-low stop (0.315 risk) vs the mandate invalidation (1.66 risk). A 2.5-cent wick converted the shadow's outcome.** **The three pairs now LOCALIZE where the rulesets diverge — FTRE = trail TIMING (shadow ahead), CADL = STOP PLACEMENT (live ahead), AMN = entry quality (agree) — which is the decomposition the doctrine read needs. N=3, bank NOTHING.** ORKA T7 re-fired: verified 08-13, unchanged (H2 program fire). **NEW trade 24 RHI 08-14 (3 sh @ 44.20, stop 34.77): record CLEAN** — `standard` intent, Broad-watch label (H5), **`candidate_id` 12518 POPULATED and `trade_origin='pipeline_watch_manual'` — the origin defect that blanked CADL's keys did NOT recur.** Epoch contract HELD.
- **TRADE 25 (OII) RULED 2026-08-23 — LABELS FROM THE FIRE (H1, faithful derivation from candidate 12284); keys stay NULL pending 22-A. THE BOUND'S SECOND CLAUSE, stated as semantics: A LATCH DIES AT ITS OWN INVALIDATION (RUNG 4) OR ABOVE — NEVER OF BUCKET DRIFT.** Verified live: fire 08-10 (`aplus`, frozen pivot 53.98 / invalidation 41.42), then watch 08-12→08-17 with closes RISING 47.97→52.20 (never within $6 of the invalidation) and the pivot FROZEN throughout; **entry 08-17 at EXACTLY 53.98** = the mandate's own buy stop-limit through the frozen pivot; latch ledger holds place intent 1 + validity intent 2 (broker order 1007523377009, `accepted_by_broker`) — **tier-1-quality admissible evidence, the framework's contemporaneous record of the operator ACCEPTING the mandate.** **The controls: AMN = negative (invalidation breached → NOT from the fire) · OII = positive for DRIFT-IS-NOT-DEATH · CADL = positive for the clean trigger. The discriminator is the frozen invalidation — a semantic line the fire itself declared — never the bucket (drift is rung-5/report-only territory; the framework deliberately does not kill latches on criteria drift, and the label must not pretend it does).** **Labeling from the 08-17 watch row REFUSED** (would misfile a mandate fill into H5 exactly as `standard` would have misfiled AMN — H1 at 2/20 silently loses a genuine sample). **The last-word guard's refusal of 12284 STANDS — scope boundary, not defect:** the bucket series is the right authority for UNLATCHED trades; for latched mandates the authority is the LATCH LADDER, and **22-A extends the citation graph** (fire candidate + latch validity row, the two-tier admission shape). **Calendar: trade 25 is OPEN; if it closes before 22-A lands, the September read carries it as a NAMED pending-label row, never silently uncounted.** **22-A ACCEPTANCE TEST — CORRECTED 2026-08-23 (orchestrator's Finding A): my tri-case gate was NON-DISCRIMINATING on the bound's own clause.** All four live cases pass an implementation that OMITS the invalidation comparison — **AMN has ZERO latch rows** (the intent ledger post-dates its 08-07 entry, earliest `recorded_ts` 08-08) so it falls through the no-latch path without reaching the check. **I HELD that fact when I wrote the gate — I proved it ruling Demand A's tier-2 admission — and banked one consequence without propagating the other. Second failure-to-propagate instance** (first: notes-writability). **THE GATE NOW: SIX cases.** (1–4) the live four, with **RHI kept in its REAL shape — place-WITHOUT-validity** (the one live case separating any-latch-row from broker-VALIDATED implementations; the mechanism MUST key on validity — the evidence standard was the operator's `accepted_by_broker` acceptance); (5) **the SYNTHETIC breach case, adopted:** OII's real row shape planted by raw insert (18-B.1 technique — tests DETECTION of a state the ledger doesn't hold), close mutated to **frozen invalidation − 0.05 (the real AMN magnitude)** → must NOT label from the fire AND must land keys-NULL like AMN, never silently broad-watch; (6) **the BOUNDARY case: close EXACTLY EQUAL to the frozen invalidation → NOT a breach → labels FROM the fire.** **INEQUALITY RULED: STRICT LESS-THAN** (constraint-6 posture — invalidation errs toward mandate-preservation); without case 6 a `<=` encoding passes case 5 and silently kills mandates at the line. **DECISION 2 RULED 2026-08-24 — PRE-BARRIER ADMISSION REFUSES BY DEFAULT (adopted as the author proposed), AND TRADE 25 ADMITS ANYWAY on tier-2 evidence nobody knew existed: MY OWN GIT-COMMITTED SNAPSHOT.** `rd-state` @ **`9f315cc6` (2026-08-10T02:41:33-10:00)** records verbatim *"Frozen at fire: pivot 53.98 / `initial_stop` 41.42"* — SEVEN DAYS before the 08-17 fill, tamper-evident by the store's construction; candidate 12284 today matches at the `_PRICE_DP` mandate grain. **THE EXCEPTION CLASS IS THE DEMAND-A TWO-TIER SHAPE REUSED** (the barrier is an instrument; it post-dates every pre-barrier fire): *tier-2 pre-barrier admission = an immutable EXTERNAL contemporaneous record of the frozen values, matching the current row at the mandate grain, with the TIER, the citation, and the UNCOVERED WINDOW recorded ON the correction row.* Operator recollection does not qualify; writer-absence does not qualify. **Trade 25's uncovered window, stated: the fire row was written 08-07T17:30, the snapshot is 08-10T02:41 — ~2.4 days covered by writer-absence only; everything since PROVEN.** **CONSEQUENCE, NOW LOAD-BEARING: rd-state fire-time snapshots are ADMISSIBLE EVIDENCE — the only tier-2 generator for the pre-barrier era. Keep freezing fire values at T5 resolution; NEVER back-edit those lines (git history is what makes them evidence).** **The author's two recorded withdrawals were right** — the briefing rendering is `upsert_recommendation` rewritten in place, ONE derivation rendered twice, not a witness. **Decision 1 (the candidates UPDATE+DELETE barrier) is CHARC's; my input: support — the structural path REQUIRES it going forward or tier-2 becomes the permanent norm; the pre-merge live-pipeline-run condition binding; the DELETE half matters (`candidates.id` is a reusable rowid, the fill_id-reuse class).** **22-A LANDS AFTER THE READ (resolved 08-24): trade 25 carries into the September read as a NAMED pending-label row.** **THE BOUND — CORRECTED AGAIN 2026-08-24 (round 6): THE CARVE-OUT IS WITHDRAWN; THE ORIGINAL FORM WAS RIGHT AND MY "CORRECTION" INTRODUCED THE ERROR.** My "AMN IS the same-session tie" read the 30.80 close off `action_session_date` (forward-looking) — **the close belongs to `data_asof` 2026-08-06, STRICTLY BEFORE the 08-07 fill** (verified: candidate 12189 action_session 08-07 / data_asof 08-06; archive bars agree independently). **THE SESSION-ANCHOR GOTCHA, committed by me, in a ruling about grains — and the orchestrator's "verification" used the same wrong anchor, so it could only agree (the corroboration-failure class again).** AMN is case **5a**. **FINAL FORM (the original, anchor pinned): a latch does not survive its own invalidation — the first COMPLETED session whose close (strict below, `_PRICE_DP`) breaches the frozen value, the close belonging to its `data_asof` session, NEVER `action_session_date` — and a fill on any LATER session does not label from the fire. FILL-WINS GOVERNS ALL TIES, invalidation included.** Grounds that cannot flip on arithmetic: (1) at fill time the mandate was ALIVE and a through-the-pivot fill is the mandate executing; the same-session close is the first OUTCOME, not eligibility; (2) **refusing same-session collapses is SURVIVORSHIP BIAS — T4's losers resolve FAST, the fastest is the fill that collapses on trigger day; ejecting exactly those censors H1's left tail and biases its mean UP. H1 must own its disasters or its answer is worthless**; (3) R6 fidelity, and the bound stays a pure comparison. **CASES: 5a = breach strictly before fill → REFUSE (the real AMN); 5b FLIPS → the synthetic same-session tie ADMITS (pins fill-wins at the invalidation rung); 6 = equality → admit.** **EXT-4 DISSOLVES (R6-06 resolved by dissolution): no comparison judges the fill session's own bar — doctrine that got cheaper when corrected usually just got truer.** **R6-04 RULED: attestation, evaluated-at-admission, method + timestamp on the row (SQL types a label, never remote state).** **R6-07 RULED: the tier-2 threshold is a BINARY CONJUNCTION, not a number** — (i) ancestor of `origin/main`, (ii) recorded date strictly precedes the fill session, (iii) values match the current row at `_PRICE_DP`, (iv) uncovered window recorded; **depth is recorded context, NEVER part of the verdict** (a free dimension does silent work until measured). **R6-08 RULED: replay re-evaluates the SAME conjunction; ANY criterion failing → REFUSE (fail-closed confirmed); context drift (descendant growth) is never divergence.** **BANKED ON MYSELF: second catalog-gotcha committed by its own most frequent citer this arc. Citing the catalog is not immunity; checking the anchor is.** **OPTION C RATIFIED 2026-08-24 (operator-directed split at the proof-machinery seam; evidence: 10 of 13 recent CRITICALs in proof machinery, round 8 the clean experiment — no rulings absorbed, all 3 criticals in the just-authored continuity interval).** 22-A ships the link + entry consultation + barrier + minimal single-state epoch + the six-case gate, **pre-barrier REFUSES OUTRIGHT — no tier-2 escape in this arc.** **THE FOLLOW-ON ARC owns: the tier-2 conjunction/attestations/replay AS RULED (settled doctrine inherited verbatim, never re-derived), the continuity interval, gap-era machinery, AND TRADE 25's CORRECTION.** My conditions: findings travel with evidence (inherited-not-dropped); **HORIZON MARKER — the follow-on lands before the OCTOBER read if trade 25 has closed by then** (a named cost with a date, not a blocker). Trade 25's named-pending-label row governs EVERY read until then. **CRITERION 3 AMENDED AT SOURCE before the follow-on inherits it — SINGLE ROUNDING AUTHORITY:** `round(1.125,2)` = 1.12 Python (half-even) vs 1.13 SQLite (half-away) — verified by execution; the comparison happens entirely in ONE domain, or canonical pre-rounded values compare bytewise; **a Python-rounded value never meets a SQLite-rounded value across an equality.** Bites ANY Python-computes/SQL-validates price comparison (CHARC's gotcha candidate). **THE FOLLOW-ON IS NAMED: 22-A2 (PROOF MACHINERY)** — in the Phase-22 scope doc, carrying: the continuity interval (as ruled in INTENT — fire-session-to-read — mechanism to be stated and routed) · the tier-2 conjunction WITH my single-rounding-authority amendment · attestation/replay semantics · retirement/gap/re-arm eras + the dissolved third/fourth triggers · `gap_era_reconstructed` · **trade 25's correction**. **My two conditions BIND it** (rulings travel as rulings; October horizon marker if trade 25 closes). The ten inherited findings transfer with evidence as the brief's opening premise-set. **CHARC's single-state ruling is the elegant close: the admission reader verifies the barrier triggers EXIST at read time (`sqlite_master`) and refuses structural admission if absent — the drop's consequence made MECHANICAL, not procedural** (Condition 3's "any drop recorded" becomes a consequence nobody can avoid); accepted limitation declared with its reason (drop-and-recreate between two reads is invisible; the era machinery is the structural close). **The rounding gotcha is LANDED in CLAUDE.md** (Python half-even vs SQLite half-away; one rounding authority per comparison). **SS-01 INVERSION banked: the "unreachable invalidation branch" IS the mechanism — fill-wins delivered by the shipped ladder with ZERO authored comparisons; case 28d′ pins `bar_bound` against widening. Doctrine that got FREE got proven.** **FINDING C GATE REQUIREMENT: the frozen invalidation lives on the FIRE candidate's `initial_stop` (OII 41.42 / AMN 30.85), NOT on any latch column and NOT on the trade (trade 25's stop is 37.89 — the FTRE 14.47-vs-14.88 family). The compared number must be PROVABLY the fire's frozen value: immutability WITH EVIDENCE (audit-trail form) or a latch-time SNAPSHOT — a bare "append-only in production" sentence is writer-absence inference and does not pass.** RHI's latch cites candidate 12442 vs the trade's 12518 — a live divergence the mechanism will meet.
- **GLANCE 08-25: THE STOP-GEOMETRY DIVERGENCE IS SYSTEMATIC (banked for the September read + the doctrine read).** Priced A+ now 7 (new: OII det-08-10 + RHI det-08-13 twins, both −1.0 `initial_stop`). **4 of 5 closed non-breakeven shadow A+ rows died at the shadow's ENTRY-BAR-LOW stop while the live counterparts (mandate-width stops) survive or profit** — CADL live TRIMMED 9 sh @ 12.865 on 08-19 (**≈+1.23R REALIZED on the half — the first partial ever; trade 23 `partial_exited`, 9 sh remain**); OII + RHI live managing. **THE READ CAVEAT, binding on me: the shadow A+ arm's closed-only mean (≈−0.51) measures the SHADOW ruleset's stop placement, NOT the live mandate doctrine — report it WITH the caveat + the pair table, or the number indicts the wrong doctrine. The stop axis, not the trail axis, is where the rulesets diverge hardest — the strongest input yet for the deferred doctrine read.** T7 NRIX verified (H2). Spot-check #4 done at N=5; next owed at N=10.
- **THE RHI ANSWER — OPERATOR-CONFIRMED 2026-08-25, RECORDED WHILE TRADE 24 IS STILL OPEN (before-outcome evidence, deliberately committed now): "Placed order filling."** Trade 24 IS a mandate fill of the 08-13 RHI fire. **The evidence set, verified this turn, is the strongest tier-2 case the pre-barrier era holds:** place intent 4 `recorded_ts` **2026-08-12T21:14** — the evening the fire published, TWO DAYS pre-fill — carrying framework buy-stop **44.18** (= frozen pivot), limit 45.50, **quantity 3**; trade 24 = **3 shares @ 44.20** with `initial_stop` 34.77 = the frozen stop. Every element matches. **DISPOSITION: trade 24 is a candidate H1 sample MISFILED in H5 (worse than trade 25's honest NULL — a known-wrong label), pending 22-A2 tier-2/attestation; Demand C correctly CANNOT fix it (last-word guard picks the 08-14 watch row 12518, bucket ≠ aplus → refuses — the guard working as designed on a case outside its authority). The September read names BOTH: trade 25 (NULL-pending) and trade 24 (MISFILED-pending), and H5's live-row reads exclude 24 with the reason stated.** **THE ROOT CAUSE, IN HIS OWN WORDS (the operative sentence for 22-A's UX half): "When I enter position after my order fires with the ticker not in the decision table that day, there is no clear entry for me to choose to indicate what cohort it belongs to."** The entry form derives its cohort affordance from the CURRENT session's decision table — but the latch model's whole point is that fires fill LATER, when the ticker has left the table. **Every instance of the class (AMN, OII 25, RHI 24, CADL originally) is this one sentence.** 22-A's entry-path consultation closes it going forward — **PROVIDED the order→mandate link exists, which requires the VALIDITY capture. Interim operator ritual until 22-A ships: confirm every placed mandate order through the panel's validity flow (OII got one, RHI did not — the asymmetry is the operator simply not completing the flow, now understood, not a panel defect to chase).** *(Superseded question follows for the record:)* - **THE RHI QUESTION (raised to the operator 2026-08-25, HIS frozen field, NOT ruled):** RHI fired A+ for session **08-13** (candidate 12442, frozen pivot 44.18 / stop 34.77) with **latch place-intent 4 recorded, NO validity row**; trade 24 entered **08-14 @ 44.20 — two cents through the frozen pivot, with `initial_stop` exactly 34.77 = the frozen stop**; invalidation never approached; labeled Broad-watch off the entry-session watch row 12518. **Trade-25 doctrine geometry says mandate fill → from-the-fire; the six-case gate (deliberately, RHI is its own control) keys on VALIDATED orders and will leave it Broad-watch. The deciding fact is his: was the 08-14 entry the PLACED order filling, or a fresh discretionary buy?** Former → candidate H1 sample filed in H5, a 22-A2 tier-2/attestation case (place intent framework-captured + fill geometry corroborates). Latter → Broad-watch stands. **Also for 22-A2: WHY did OII's latch get validity and RHI's not — the panel-flow asymmetry is a datum.**
- **1-pre RULED (2026-09-01, the binding audit's first RD item) — IT IS TRADE 25's LIVE OUTCOME, NOT A TWIN.** Verified at the body: it asserts only `('manual_off_pipeline', None, None)` — **a triple identical under EVERY refusal reason, because I ruled they all land there.** An implementation refusing for entirely the wrong reason passes. **RULED: assert `pre_barrier_unproven` specifically, AND that the refusal came from RUNG 9** (clear_reason `None`, no aliveness evidence — copy 5b-pre's own pattern, which exists precisely to close that hole), **AND the docstring must state this is the LIVE production path** (under Option C every fire today is pre-barrier; S9's witness is written around this row — a reader taking it for a twin under-weights it forever). **Why it matters disproportionately: it is the assertion standing between the operator and a WRONG REFUSAL at the witnessed gate — the right triple and the right screen would look identical.** **THE SHAPE: case 2, FORTY LINES BELOW, states this exact defect in its own docstring and pairs itself against it — the knowledge was in the file and not applied one function earlier. The per-clause discriminator rule must be applied per CASE, not per FILE.** Cases 33/36 upgraded WEAKER→**VACUOUS** (a test computing the comparison inside itself and asserting on its own expression, its docstring naming the defect it cannot catch). **REVIEWER B RAN (2026-08-31, the ONLY B pass the arc has had — it had never run in 15 A rounds; audit preserved at `~/swing-data/review-transcripts/`). TWO ITEMS MINE.** **(1) THE ENTRY BLOCK — my principle violated a FOURTH time, and I CORRECTED ITS STATUS: it is NOT shipped.** The handoff called it shipped; verified — `git show main:` returns ZERO occurrences, `bc85c5e1` is NOT an ancestor of main. **Arc-INTRODUCED (task 10), unmerged, never once reachable in production.** Under my boundary introduced = FIX, never cite, so the remedy is unchanged and the urgency is not — but *shipped* would have routed someone to a production hotfix for a defect that has never been in production. **RULED: a malformed `schwab_order_id` REFUSES THE LATCH BINDING, never the ENTRY — trade recorded, keys honest-unset, warning names the malformation (same shape as R3-13 and 37c: the refusal is about the LABEL).** Discriminating case: numeric order id + ticker in today's table → trade written, keys unset, warning; a 400 fails it and a `pipeline_aplus` stamp fails it. **THE CLASS: FOUR instances in ONE arc (NotSessionError, 37c, R11-02's uncontained `record_identity`, this) — every guard added to protect the cohort keys DEFAULTED TO BLOCKING THE ENTRY. That is a default to invert at the seam: every cohort-provenance guard states at its site which of ENTRY or LABEL it refuses, and the answer is always LABEL.** **(2) THE 146/146 FIGURE IS FALSE AS STATED — name coverage, not semantic implementation — AND IT REACHES MY GATE. My clearance is NARROWED: it covered the SPECIFICATIONS I read, never the test BINDINGS, and must not be read as having done so.** Case 4's test builds a broker-ACCEPTED order asserting ADMISSION where S3.4 specifies place-WITHOUT-validity falling through — **the opposite** — and case 4 is the control separating *any latch row* from *broker-VALIDATED*, the evidence standard my trade-25 ruling turned on. All 146 bindings need a SEMANTIC re-audit (does the test assert what the spec states) before the number is restated. **CLASS: a closure meter binding a case id to a test NAME measures NOMENCLATURE — the existence half of existence-vs-completeness, now met at counts, greps, absences, and a coverage meter.**
- **S9 COMPLETE — 22-A FULLY LANDED; LIVE DB AT v37 (2026-09-02; CHARC register/pointer `c9e32eb7`).** Steps 0-6 witnessed: live migration applied (backup PROVEN restorable, v36 pre-image `swing-20260902T000318.db`; ONE OII link row backfilled `pre_barrier_reconstructed`; six barriers; epoch boundary 13591); refuse-by-default witnessed live with the citation-shopping refusal (step 2 — my Demand-C rule at the operator surface); barrier-existence discriminator (step 3: same command/trade/data, reason MOVES to `barrier_not_installed` — the reader CHECKS); post-barrier admission constructed via production writers (step 4, `live_at_acceptance` + six counterfactuals); **step 5 re-specified around TRADE 28/PBF (trade 24's premise was stale — keys populated) and PASSED ON ITS PROPERTY carried by the no-accepted-link DATA assertion, read-only (the resolver writes from rung 6 — asking would have contaminated the measurement); the byte-diff was shallow because D41 BIT LIVE: zero of 230 daily_recommendations rows name PBF, a valid citation is UNCONSTRUCTIBLE for a watch/hyp-rec entry — D41's live evidence.** CHARC's precondition class hit its 4th instance (the original witness held code constant across schemas — could not run); the amended mechanism carried the proven-different-code control (36 vs 37). **STEP 6: browser gate PASS on render+write (HX-Request intact, screen=ledger to the second) — but the ADMITTING ENTRY IS AN OPEN NAMED GATE: four guards refused correctly; the first live post-barrier fire is the witness.** D40-D43 registered (recon one-directional / D41 aplus-only → 22-A2 / export 12-day silent failure / copy-run write surfaces — my quarantine = the template); **D43 near-miss self-reported by the orchestrator: migration 0037 itself writes an arc-mirror pre-image into the swing-data ROOT (D32 class) — benign by ordering only; both files retained.** **MY FORWARD WATCHES: (1) IMNM (fired 09-02) is the natural admitting-gate witness — a NEW latch armed post-v37 whose acceptance mints the link; (2) a DFTX fill will land HONEST-UNSET (place intent row 5 exists but NO link — pre-barrier order, no attestation) — EXPECTED, not a fifth misfile; (3) my sequencing stake for the operator's commissioning call: 22-A2 (D41 generalization + trades 19/24/25/28 labeling) has CALENDAR pressure the engineering follow-ons lack — the October read wants H1's pendings resolved; caller-half-first governs the engineering queue, 22-A2 is a separate arc and should not queue behind it.**
- **22-A MERGED TO MAIN (`9e4a8618`, 2026-09-02; 161 arc commits, zero trailers; merged-head suite 12,077/13/0 run BY the orchestrator on the merged head; live DB v36, 0037 UNAPPLIED, main unpushed).** Gate package `docs/22-a-merge-request.md` @ `6795843e` carried B's transcript+five assertions+verdict, both method-reported sweeps, and the G4-precise 146/146 (B-reimplemented). **S9 STEP 0 PASSED on a v37-migrated COPY: ZERO barrier aborts; `_step_evaluate` APPENDED 55 candidate rows through the barrier (the positive evidence 26 review rounds were structurally blind to); backfill = ONE link row (OII, candidate 12284, `pre_barrier_reconstructed`); epoch boundary 13591. Bound stated correctly by the orchestrator: a FLOOR (no UPDATE/DELETE occurred to test), not proof of absence.** Orchestrator self-reported a Condition-1 ORDERING breach (step 0 sequenced post-merge; operator ruled proceed — protection identical, live DB untouched throughout). **THE ARC CORRECTS NEITHER TRADE 24 NOR 25 — both stay named-pending (confirmed at the gate).** NEXT: S9 steps 1-6 operator-witnessed, then push. **MY FINDING + ACTION (posted both, 09:42Z): run 168 wrote INTO THE LIVE ARTIFACT TREE from copy-state — `shadow-expectancy-20260902T092425Z` (off-series, would read as newest in every glance glob) + `health/latest.json` overwritten. The copy isolated the DB, not the WRITE SURFACES. I QUARANTINED the artifact (renamed `quarantine-copyrun168-…`, bytes intact) and REGENERATED latest.json from the live DB (verified 1188/69 — series-consistent). Any further execution against the retained copy repeats this write-through unless redirected.** Caller-half-first follow-on ordering AGREED from my lane (a contract with no reader is not a contract; `post_commit_warnings` has ZERO readers, `trades.py:1490` discards the result — the failure-conversion direction still open on the live web path). ALSO RAISED (orchestrator, CHARC debt): `export_status=failed` on EVERY nightly for 12+ days behind `state=complete` (WinError 5 ACL, pre-existing, unrelated to 0037) — the run-level-stamp class wearing a status field; NOTHING monitors that artifact family (my drumbeat covers exports/research/ only; the operator's briefing silently stopped).
- **ORKA/PBF/TVTX RESOLUTION (2026-09-02, same evening as the read): MY ORKA INFERENCE WAS WRONG AND THE CORRECTION FOUND TWO REAL GAPS (posted charc+orchestrator).** ORKA's broker stop DID fire — **08-27 @ 96.17** (that bar's low 96.16; the stop worked at its level, BEFORE the 08-31 gap); realized **−0.216R** (fill 52, journal exit recorded 09-02, four sessions late). My "broker still holds it / likely no stop resting" was inferred from reconciliation runs 111-113's silence — **and the silence was structural: the position check is ONE-DIRECTIONAL** (`untracked_broker_position` = broker-has-journal-doesn't; the INVERSE has NO check — search: `position_mismatch|phantom|missing_broker_position|journal_position` over both reconciliation modules → only 20-A voided-trade comments; only backstop = the `equity_delta` threshold, not tripped since position value ≈ proceeds). Routed to the debt register. **PBF (2 sh, filled 09-01 open = disc 103): the record supports the operator's hyp-rec belief on EXACTLY ONE session — 08-20, candidate 12836, watch failing ONLY `proximity_20ma` = the registered H2 statement verbatim; every other session's profile fails the shape and 09-01/02 are `skip`.** The order evidently rested ~8 sessions and filled after profile decay + surface exit — trade 24's affordance gap THIRD instance, and the hyp-rec analogue of AMN's decayed-latch fill (latch-on-acceptance-for-hyprecs, ruled Phase-22 scope, second motivating case). **DISC 103 REQUIRES MANUAL RESOLUTION (my "should self-resolve" chat claim 09-02 was WRONG — the 18-H.6 design keeps untracked_broker_position rows unresolved until operator-acknowledged; I had read that comment the same day: the banked-but-not-propagated class, instance added; `journal discrepancy resolve 103 --resolution journal_corrected` directed).** **DONE — TRADE 28 RECORDED 2026-09-02 (verified on the live DB): PBF 2 sh @ 75.918, stop 67.50, entry_date 09-01, `entry_intent='hypothesis_test_by_design'`, `trade_origin='manual_off_pipeline'`, label+candidate_id NULL (honest-unset as directed), why_now/notes carry the candidate-12836 citation written pre-outcome — the evidence rule satisfied at entry for the first time on an off-surface fill.** Entered via `swing trade entry` CLI (the manual web form has NO navigable path for an off-surface ticker — the affordance gap includes the web layer; the CLI is the working path). Original direction (kept for the class): manual entry NOW (never blocked by the label), intent `by_design` (operator CONFIRMED 08-20 placement), why_now written AT ENTRY naming candidate 12836 + the emission + the order date (admissible under Demand-A: pre-outcome, write-once); labels honest-unset — the Demand-C CLI CANNOT fill them (verified: `--cited-candidate` must be `aplus`, `--cited-recommendation` REQUIRED; PBF = watch, zero rec rows) → PBF is a NAMED PENDING H2 member from today; admit-vs-refuse against the decayed profile adjudicated AT LABELING under the hyp-rec latch doctrine.** TVTX exit DONE + VERIFIED 09-02 (fill 54, 2 sh @ 66.37, realized ≈+0.01R scratch, reason 'other'); disc 103 RESOLVED `journal_corrected` by the operator; open set = 5 (CADL/NRIX/OII/PBF/RHI), one slot free vs cap 6 with DFTX resting + IMNM live — NRIX is the next capacity exit if both fill (record CENSORED for H2, not an outcome). H5 cohort note: TVTX was its only cleanly-labeled live member; closed at ≈+0.01R.
- **MONTHLY READ #3 EXECUTED 2026-09-02 (v2.2's first read — the amendment was written and committed FIRST, `78142970`; OPERATOR-ACKNOWLEDGED 2026-09-02 — v2.2 fully binding).** Study `research/studies/2026-09-02-monthly-read-3/` (`e9e4013c`, artifact committed); charter §7 log appended (`3fafbfba`). **HEADLINE: the out-of-window replication FAILED — July@≥30-sessions −0.6752 [−0.81,−0.54] (n=199/57) vs June +0.1241 [−0.25,+0.50]; non-overlapping CIs; August's unbanked +0.20 was the June regime.** Pooled mature closed-only −0.2869 (CI excludes 0), mtm misses zero by 0.007 → primaries disagree → NOTHING BANKED under v2.2; **T4 UNDETERMINED with the positive direction now the unsupported one.** ETF slice 57/7/−1.01/ZERO winners (Dec T10 re-check). CADL pair = a SIGN FLIP (shadow −1.00 entry-bar-low vs live ≈+1.22R mandate-stop) — the binding caveat's sharpest evidence. invalid_ohlc 28→69, RATE growing (5.8% vs 3.6%) — the read's data-quality watch item. **Read follow-ups: trade-19 correction executable NOW (Demand C surface, FTRE fire citation pre-dates fill); STTK taken-or-passed still outstanding; IMNM decision was live the read's own day.**
- **EXIT RECOMMENDATION + TWO LIVE FINDINGS (2026-09-02, operator asked which of 6 to exit for the queued A+):** **ORKA (trade 22) — and it is not a capacity choice: its journal stop 96.27 was GAPPED THROUGH 08-31 (open 91.00, low 86.00), two completed sessions below the stop, and reconciliation run 113 shows the broker STILL HOLDS IT (only PBF flagged) → no broker stop fired = likely NO broker stop resting. The exit is already overdue by doctrine; selling ≈92 realizes ≈−0.42R.** Doctrine ranking if a second slot is needed (DFTX resting + STTK? + IMNM queued): TVTX next (weakest cohort claim, ≈−0.04R scratch), NRIX after (H2 observation value — a capacity-forced exit must be recorded CENSORED, not outcome), CADL/OII/RHI held (best holding + the two A+ mandate members). **PBF: disc 103 — 2 sh ≈$150.70 at the broker, NOT journaled, unresolved — operator must journal or explain; if real he is at SEVEN positions, not six.**
- **GLANCE 09-02 ADJUDICATED (1 ATTENTION, T3): DENOMINATOR-ONLY movement — no new priced signal, no spot-check due.** Trigger 7/9→7/10→7/11 across the week is entirely `never_triggered` 2→4 in the A+ arm funnel (manifest diff 08-27 vs 09-02; triggered unchanged at 7 = closed 6 + open-at-horizon 1); the glance's first-priced-trade caution is the template line, long satisfied. **The two new arm members are DFTX + STTK by WINDOW-COINCIDENCE — method stated because the artifacts CANNOT ENUMERATE never-triggered signals** (results.csv = triggered-only; per_session.csv = actions-only; manifest = counts-only — an instrument note for the September read: derive the arm roster by manifest-diff + candidates window, or ask for never_triggered identities in a future artifact rev) — the candidates table names EXACTLY two new aplus tickers in the increment window. **THREE NEW A+ MANDATES this week (dedup query: distinct (ticker, round(pivot,4)) aplus post-epoch → now 12): DFTX 49.70 (first fire 08-24, same-pivot re-fire 08-27 = re-confirm under the dedup rule), STTK 8.15 (08-28), IMNM 30.06 (TODAY 09-02, stop 23.09 — lands in tonight's shadow run, expect 7/12 or a trigger).** T5 state from the framework's own record (latch_order_intents, read 09-02): **DFTX TAKEN** — place intent row 5 (08-24, STOP_LIMIT GTC stop 49.70 / limit 51.19 × 3 sh, sizing equity 2642.28 asof 08-21), resting UNFILLED (zero trades since 08-24), no validity row (like CADL/RHI; only OII ever recorded one) — **the AMN de-latch discipline applies to a 6+-session resting order; the 21-A panel is the operator's watch surface for its invalidation**; **STTK: RESOLVED 2026-09-02 — operator: "I missed it. Inadvertent pass" (recorded pre-outcome; T5 closed). NOT a deliberate pass under the forward-intent contract — the fire produced no order because no one saw it, which is the latch-on-acceptance population argument as a live instance: a mandate the framework fired and the operator never met**; **IMNM: fired today, no row yet, the decision is live.** Accrual healthy: 1074→1188 unique signals, unattrib 0, artifact age 0d.
- **CLAUSE-2 ENDORSEMENT CORRECTED (2026-09-02, posted charc+orchestrator; round 10 verdict NEW_CRITICAL_MAJOR_FOUND — implementing the R9-03 contract closed 1 defect and opened 4, two FALSE-SUCCESS on the money path).** I endorsed resolve-by-read as "fail-closed in the right direction" **taking "present" as a primitive — it is not.** Two admissibility preconditions I never stated, both failing at `entry.py:559-590` @ `5296e1da` (verified at the source, incl. the log line "MUST BE DISCARDED rather than reused" followed by the NEXT LINE reusing the connection for the confirming read): **VISIBILITY** (a read on the writer's own connection inside an unresolved transaction is the writer quoting itself — corroboration-failure at the transaction boundary; a failed rollback VOIDS the read) and **IDENTITY** (`SELECT 1 FROM trades WHERE id=?` — a rolled-back rowid is REUSABLE, `sqlite_sequence` rolls back too so AUTOINCREMENT does not pin it — a stamp standing in for per-attempt provenance, #30's family). Without both, "SUCCESS with warning" is the alarm-never-assert INVERSION in the one direction nothing belts (the operator told "Do NOT retry" holding a broker fill with no ledger row = disc 99's family manufactured then instructed to persist). **Split SUPPORTED from my lane (ruling is CHARC's): keep clauses 1+3, revert clause 2 to re-raise (honest — never asserts what it cannot prove), token follow-on inherits the three-part evidence rule: CO-DURABLE (same transaction, else #30) / UNIQUE PER ATTEMPT (rowid fails by construction) / DURABLE-VISIBILITY READ (fresh connection or proven resolution); discriminating tests = rollback-raises-then-read must NOT return SUCCESS + concurrent-insert-takes-the-id must NOT confirm as ours.** Second endorsement-without-preconditions instance this week (P1 was the first); the §6 lesson now covers endorsements explicitly. Merge-gate package drafted at `6219d58b`. **RESOLVED same-day: CHARC ruled the split as recommended — clauses 1+3 stand (R10-01/04 fixed as clause-1 boundary defects), clause 2 REVERTED to re-raise (residual DECLARED with the R10 reproductions, belt-covered direction), my three constraints + two discriminators adopted VERBATIM as the token follow-on's commissioning constraints and clause-2-return gate. CHARC's preferred shape (client-generated `attempt_id` on the entry row, same INSERT, nullable-legacy, UNIQUE partial index WHERE NOT NULL — §3 schema tripwire, own pass) is for the plan to VERIFY not inherit. CANON `bf403b05`: a ruling that prescribes a mechanism STATES the preconditions that make its evidence admissible AND names which exist today — a missing precondition converts the clause into a follow-on with a primitive to build first. Both SQLite facts (reusable rolled-back rowid despite AUTOINCREMENT; writer-quoting-itself read) landed in CLAUDE.md as gotchas. Nothing open on this item.**
- **22-A FIX LEG COMPLETE + B RAN ON THE MERGE TREE (2026-09-01/02; branch `22-a-fix` @ `09321d78`, 28 commits; suite 12,071/0 — orchestrator's run, relayed as such; 0037 still UNAPPLIED, live DB v36 re-verified by him).** A converged r8 then r9 NOT clean: 2 residuals-of-fixes FIXED (R9-05 wrong "STILL OPEN" inference, R9-06 the SS-2 roster-of-strings closure check → AST walk, proven by mutation — **third instrument this arc caught by the class it was built to catch**); R9-01/02/04 STOPPED at the AL-10 raw-writer boundary, zero live incidence (0037 tables absent live). **R9-03 → CHARC → CONTRACT RULED: commit is the point of no return** (post-commit steps best-effort + `post_commit_warnings`; indeterminate commit RESOLVES BY READ — present→SUCCESS+warning, absent→re-raise; both-paths; tests assert RESULT SHAPE + ROW COUNT) — **endorsed from my lane: resolve-by-read is the temporal-clause discipline pointed at a transaction; the exception is not evidence about the fact either way.** My principle extended: cohort bookkeeping never BLOCKS a money-bearing entry, post-commit bookkeeping never MIS-REPORTS one. D39 registered (one sweep, ~20 sites incl. the pre-existing reconciliation siblings). **B post-fix verdict `NEW_CRITICAL_MAJOR_FOUND`, five of six claims clean; B independently REIMPLEMENTED the binding gate: 146 registry ids / 146 bound / zero phantom, each read at its named grain — G4 is now satisfiable with a VERIFIED number, not a relayed one.** The orchestrator reversed his own banking of the two `entry.py` transaction defects on B's independent reproduction (`_entry_transaction` ZERO occurrences on main → ARC-INTRODUCED, fixed) — his second premise-inheritance catch, both self-reported. **B's P1 ROUTED TO ME AND RULED 2026-09-02 (posted orchestrator+charc): the PE-anchor refusal is a DIFFERENT CLASS, not cohort bookkeeping — and it was ALREADY RULED at leg 6 ("guard fires on the ORDINARY path only — APPROVED"); what leg 6 lacked was its REASON. The discriminator, now stated: NEVER's jurisdiction is CLAIM-vs-ABSENCE, not entry-vs-label — honest-unset answers ABSENT provenance; a request ASSERTING an anchor (`pattern_evaluation_id is not None`) that the reservation-fresh derivation REFUTES has NO honest row (NULL silently overrules the request's own claim = curation by machinery; non-NULL records a refuted citation = minting evidence, Demand C's channel), so the guard refuses the ENTRY with the recovery named. Operational test for item 5.2 declarations: "could this row be written honestly with the disputed keys NULL + a named warning?" YES → refuse the LABEL; NO because NULL would silently discard an explicit claim → refuse the ENTRY + name the recovery. Corroborating: R4-06 already ruled the PE backlink a DIFFERENT evidence chain (never-merge-evidence-kinds). Disposition: PRE-EXISTING (`main:trades.py:1269-1276` identical message — orchestrator's quoted evidence), RELOCATED to the single write-time authority, PINNED (37c + declared-exception test) → NOT INTRODUCED, no code change; the disposition entry + next B prompt's accepted-limitations list carry the discriminator. NOT an "exception to the principle" — outside its jurisdiction; the declarations sweep WORKED (it forced the one entry-refusing guard to declare itself and B challenged the declaration).** Proposed disposition (stop A loop; R9-01/02/04 declared limitations with citation; merge gate) AWAITS THE OPERATOR — nothing of mine blocks it. Also canon this batch: 4c-i (a declared divergence reads as settled; re-read BOTH spec sites → decide by EXECUTION → grain change returns to the ruling authority) + seat-relative ladders (a seat-evaluated ladder makes the refusal reason a property of (WORLD, SEAT) — specs pinning a reason NAME THE SEAT; prefer the more specific of two true refusals; three-valued selection BINDING, an unprovable subject beside a live rival still refuses `ambiguous_ticker_orders`).
- **22-A PLAN: RD GATE PASS 2026-08-25** (`f9fa0e61` on main, 3947 lines; planning CLOSED on the operator's ruling at 110 findings / zero oscillation — *the loop stopped because prose cannot be executed, not because it failed*). Verified at the sites: 5a/5b/5b-pre exact with the survivorship docstring; case 5 seeds a non-NULL submitted label (my outcome as ruled would have passed trivially — the plan sharpened it); case 6 equality-admits with the BOTH-OPERANDS variant (stop 41.424/close 41.4199 kills close-only, stop-only and no-rounding at one stroke); tie 28a-28c admit, 28d deleted per SS-01; S1.7 reads `clear_reason` never `state` (the state-collapse trap named, cases 11/11b/11c); case 10 = lapse-forced-off as an INVARIANT; S4.3a both forms closed, 24-row incidence measured; pre-barrier refuses OUTRIGHT, tier-2 verbatim-liftable at S12 AS RULED; case 17 re-based off trade 25 (would have passed for a reason unrelated to its clause). **The S3.7 omission lens (31 clauses × failing case) + S3.8 free-dimension audit are the strongest test-design artifact this project has produced.** Clearance scope stated in the post; executing dispatches on both gates. **My merge gate re-runs at the executing return.** **EXECUTING in flight (opus-xhigh, both gates passed 08-25): leg 2 shipped migration 0037 complete-in-one-task (live DB untouched at v36); leg 3 at 13 commits, closure gate 113→89. CONFIRMED from my lane: `NotSessionError` on the entry path now lands `aliveness_unverifiable` — fail-closed on the COHORT question, fail-OPEN on the operator's ability to ACT (entry proceeds, keys degrade honestly; cohort bookkeeping never blocks a money-bearing entry). The orchestrator's binding-convergence-is-the-FULL-DIFF ruling endorsed. NULL-WHEN-clause trigger gotcha landed (a WHEN evaluating NULL fails OPEN silently — COALESCE guard mandatory). **REPLACE FAMILY 4th INSTANCE (08-25, FIRST in production): `provenance_corrections` — the LIVE table holding the CADL correction — is REPLACE-bypassable (verified with a control on a live-DB copy). MY CLASS'S IMPACT: the git-snapshot anchor is UNAFFECTED; the "verified against the audit trail" instrument was reduced to a writer-absence claim one level up — the fix (no-replace trigger, same-migration, CHARC's scope) restores it to STRUCTURAL. The CADL row is corroborated in git-committed comms/rd-state from 08-13, so silent tampering is DETECTABLE by comparison even pre-fix.** **5th REPLACE instance CONFIRMED (`latch_order_intents`, CHARC-directed probe) — and the orchestrator's premise correction found the shape that matters: the durable exposure is the UNLINKED PLACE row (the PRE-ACCEPTANCE window, when the frozen values the link later copies are still mutable), the validity row being incidentally FK-covered post-0037. Both no-replace triggers land in 0037; the production writer repaired (WHERE NOT EXISTS pre-filter) rather than the barrier narrowed — contract pinned by UNTOUCHED tests. THE EVIDENCE CHAIN IS NOW STRUCTURAL END-TO-END for the first time: place row barriered → acceptance mints link → link+candidates barriered → epoch verified at read; 22-A2's tier-2 era inherits that integrity. THE CONVENTION (CHARC): append-only ships as a TRIPLE (no_update+no_delete+conflict-scoped no_replace) + NULL-WHEN coverage; ON CONFLICT writers adjust the WRITER, never narrow the barrier.** **R3-13 FACT READ (2026-08-26): the live CADL fill CARRIES its envelope (order 1007547048146) and the inconsistent pair has ZERO live incidence (26/26 trusted-origin fills carry envelopes; 25/25 operator_typed lack them, as expected) — the rule is a FORWARD guard, its premise confirmed. CORRECTION-PATH BOUNDARY RATIFIED on the sharper ground: the correction path CANNOT COMMIT the misattribution the rule prevents (it writes only CITED evidence under its own guards, never today's anything) — where the defect is unreachable the guard has no jurisdiction. The persist-canonical design recommendation (CHARC's call) supported as my single-authority ruling generalized: SQL verifies a FACT, never re-derives a JUDGMENT across an engine boundary.** **R3-13 RULED (2026-08-26): a TRUSTED-origin fill with an ABSENT envelope is an INCONSISTENT PAIR no production writer produces (`schwab_auto` writes envelopes by construction) → entry proceeds, keys HONEST-UNSET, a warning NAMES the pair — never today's candidate (attribution from a timing coincidence = #30's family, the silent misattribution the arc exists to prevent). Origin alone NEVER suppresses — envelope-bearing Schwab fills without latches flow on the envelope's own evidence; same-day manual entries link by the operator's affirmative selection. Two discriminating cases: stripped-envelope-with-ticker-in-table must honest-unset+warn; envelope-bearing-no-latch must proceed. R4-06 criterion VERIFIED (no PE tier touches a latch surface; name-excision test pins it).** **LEG-6 RULINGS (2026-08-26): case 37c — the plan's two sites ARE the ruled semantics (recognised-but-REFUSED passes through; the service writes the honest-unset row; the refusal is about the LABEL never the ENTRY); the one-line fix (guard fires on the ORDINARY path only) APPROVED. R4-06 — the honest-unset row KEEPS `pattern_evaluation_id` IFF derived by its own independent ladder, DROPS anything derived from the refused latch claim: the all-three-or-none rule covers the cohort TRIPLE (one provenance claim in three statements); the PE backlink is a DIFFERENT evidence chain, and never-merge-evidence-kinds governs — forcing it NULL would buy cosmetic consistency with real information. The implementer verifies the ladder's independence rather than inheriting my assumption.** Leg 4: closure 89→53, `barrier_installed()` now pins SIX triggers (a wrong-acceptance on the structural proof closed), eleven findings carried as RELAYED-unverified, the S3.8 audit itself had a wrong entry (caught by measurement).**
- **The A+ spot-check cadence** continues to N=10 priced (currently 3: NVCR −1.0 / AMN 0.0 / FTRE's shadow twin +0.4433). **THE PAIR HAS MATURED — trade 19 CLOSED 08-04 at −0.092R vs the twin's +0.4433R; see §2 for the attribution (STRATEGY, not execution).** *(This line said the pair "matures when trade 19 closes" until 2026-08-11 — stale for two days against §2 of the same file.)*
- **OPEN — THE `pivot < zone_cap` MANIFEST: SEVEN sites, THREE outstanding, awaiting a CHARC scope call.** *(Corrected 2026-08-08 — my sweep claimed six sites and that nobody had asked the manifest question. **Both false.** Plan §0.3 (`5aecf5ff`, 29 hours EARLIER) enumerated SEVEN with per-site claim text and scope column, under the heading "found by checking the symbol rather than reading the prose" — the implementer asked and answered it before I did. Worse: **the grep I PUBLISHED as my method returns five and does not find my own site 4** — `test_order_intent.py:289` says `cap`, not `zone_cap` — so my stated method could not produce my own table, in a message whose thesis was method rigor. **THE RULE, which is the orchestrator's: a token pattern encodes the claim's WORDING, and a false invariant propagates by PARAPHRASE — a token grep bounds the family from BELOW; only a READ establishes the manifest. Report the number WITH the method that produced it.** Fourth instance of existence-vs-completeness (`_PRICE_DP` 2→3→4; recipe residual 1→2→4; D29 2→4; this 5→6→7) — every one short until someone stopped matching and started reading, and **the feeling of having swept is identical to having swept.** Ask "how did you arrive at this count" of your OWN numbers first.)* §0.3 also proved CONSTRUCTIVELY what I only argued in principle: `zone_cap_for_pivot` = `round(p*1.03,4)` exceeds `p` for ordinary pivots but `round(1e-305*1.03,4)` is `0.0`, and `_usable_price` admits any finite `pivot > 0.0` — so a `Latch` with `zone_cap == 0.0 < latched_pivot` is constructible from a real `candidates` row. **The invariant is a property of the ORDINARY DERIVATION, not of the constructor; stating it as a constructor guarantee is what let `compute_prepared_order` ship without the refusal.** CORRECTED STATE — `build.py:139`+`:150` corrected (`c46efcf4`); `order_intent.py:374-375` + `tests/latches/test_order_intent.py:289` corrected BY the arc; **OUTSTANDING after item 4 merges = THREE: `web/view_models/dashboard.py:756` (production; its code is correct, only the attribution is false, and false in the direction that would justify deleting a downstream cap check) + `tests/recommendations/test_sizing_basis_is_the_limit.py:614` + `tests/web/test_view_models/test_sizing_basis_is_the_limit.py:236`.** My recommendation to CHARC: ONE comment-only sweep commit over the three, `b81da672`/`c46efcf4` precedent, rule 2 in full, and **binding: re-derive the manifest by READING each site, trusting neither grep.** Detail below is the original find: `Latch.__post_init__` does **NOT** guarantee `pivot < zone_cap` (`models.py:279` puts `zone_cap` in the finiteness loop only; the sole ordering check is `:281` `latched_initial_stop >= latched_pivot`) — yet **SIX** sites assert it. Corrected: `build.py:151` (`c46efcf4`). In item-4 scope: `order_intent.py:375` (uses it to assert a branch "cannot fire"). **STILL UNNAMED:** `web/view_models/dashboard.py:756` (a THIRD production file, out of every arc's scope — its code is correct, only the attribution is false, and false in the direction that would justify deleting a downstream cap check as redundant) + three test docstrings (`tests/latches/test_order_intent.py:289`, `tests/recommendations/test_sizing_basis_is_the_limit.py:614`, `tests/web/test_view_models/test_sizing_basis_is_the_limit.py:236`). **The test at :289 is SOUND — it monkeypatches `compute_shares` to raise, so it does not depend on the geometry; only its docstring's production-unreachability claim is false. NOT a false green.** **The discipline this earned: when a false claim is found in a note, grep for the CLAIM, not just the note** — the discharged-deferral rules are per-NOTE and say nothing about REACH; a plausible invariant propagates by being quoted, so an instance is evidence of a FAMILY, never a singleton. **My filtered grep missed two of the six; the unfiltered pass found them — treat 6 as a FLOOR.**
- **OPERATOR-RATIFIED FOR PHASE 22 (2026-08-13) — and the live render is WORSE than the banked version: THREE ANSWERS ON ONE PAGE.** His screenshot (ORKA + CADL, both `pre_+1.5R`): the Open Positions advisory says **act now — "Trail stop up to $10.56 — 0.3% below 10MA"**; the SAME advisory block says **"recommended trail-MA: 20MA"**; the Daily Management tile says **"NOT YET ELIGIBLE, candidate $10.18"** (the 21-day MA). Three surfaces, three instructions, three price levels, simultaneously. **Verified: `advisory.py:483-485` fires `suggest_trail_ma` for BOTH the 10MA and 20MA and NEITHER call consults the eligibility flag or maturity stage** — the render fix (deliberately scoped) gated only the TILE; the advisory was never gated, and the which-MA hint and the act-now line disagree WITH EACH OTHER inside one block. **INTERIM PRIORITY RULING (operator asked, 2026-08-13, restating my 08-10 guidance): THE ELIGIBILITY GATE GOVERNS — the spec'd §3.5 rule beats the un-adjudicated advisory; FTRE's pair (+0.44R → scratch via trail-early) is the only paired evidence and points the same way; the T4 withdrawal showed winners need WEEKS, so trail-early forfeits the right tail the strategy exists for.** "NOT YET ELIGIBLE" has priority; "Trail stop up to" is the defect speaking. The §VII.F doctrine read remains the real answer.
- **OWED BY ME, AHEAD OF THE WATCH-STANDARD AMENDMENT (it has LIVE exposure; the amendment does not) — THE TRAIL-DOCTRINE CONTRADICTION.** The framework ships **two incompatible trail rules in one instrument**: `suggest_trail_ma` + `_MATURITY_STAGE_TRAIL_MA` (`swing/trades/advisory.py:352-356`) recommend trailing at the MA from **`pre_+1.5R`** — i.e. from entry — while `trail_MA_eligibility_flag` (spec §3.5, `swing/metrics/maturity.py:12`) says eligible only at **`open_MFE_R_to_date >= +2.0R`**; the stage vocabulary even contains `>=+2R_trail_eligible`. Both were specified deliberately by different arcs and **nobody noticed they disagree.** **Which is correct is a METHODOLOGY question (when should trailing begin) → routes through the V2.1 §VII.F source-of-truth correction protocol, NEVER a direct patch.** **And I can answer it with DATA rather than argument: the shadow engine can run trail-from-entry vs trail-later across the whole priced cohort at signal pace — the right instrument already exists. Scoping that read.** **SEPARATE AND CHEAPER — THE RENDER DEFECT, which needs no doctrine ruling and should ride NOW:** `daily_management_tile.html.j2:86-94` renders **"TRAIL ELIGIBLE @ $18.39"** when the flag is 1 and a **BARE `$18.39`** when it is 0 — same number, minus a badge whose absence means nothing to a reader who has never seen it present. **NOT an unconsumed flag (4 render consumers exist; the orchestrator's "zero consumers" was wrong in the direction that UNDERSTATES it) but an ASYMMETRIC one: it can say *eligible* and structurally CANNOT say *not yet*. SILENCE BECAME PERMISSION** — my own zero-data-renders-NOT-YET-never-clean family, inverted, on the surface he reviews daily. **Live exposure: trade 20 (AMN) is the ONLY position with size, is at `pre_+1.5R` by construction, and hits the identical sequence at its next daily record.** *(NOT established: what was actually on his screen — no rendered-advisory audit was found, and that absence would mean no past decision can be reconstructed against what it was made against.)*
- **TRADE 20 (AMN) — NOT AN A+ ENTRY; THIS ENTRY IS THE RECORD (operator-corrected 2026-08-09; the DB has nowhere to hold it, see Demand B).** **My "6th A+ fire ever, 07-31" was WRONG TWICE.** AMN's A+ fires are action sessions **2026-07-01 and 2026-08-03 — the only two ever**; "07-31" is the *data-as-of* date of the 08-03 fire and the 07-31 action-session row is `watch`. *(Inherited from the predecessor and restated by me as fact — same class as the VSTS fire-date error already at the top of §6.)* **The framework AGREED with the operator:** aplus (08-03) → watch → watch → **`skip` on 08-07, the entry session**, pullback 5.98→7.74→8.3→**13.89%**, pivot revised DOWN 36.27→35.73, and the close **30.80 BELOW the latch's own close-based invalidation 30.85**. **OPERATOR'S ACCOUNT (unprompted):** the stop-limit buy was placed while A+ was indicated; **the de-latching work then in flight left the order resting; a large intraday SPIKE hit the stop and it executed.** He has a sell stop in place and manages per doctrine; the entry was non-standard *(his word)*. **INTENT RULING (operator concurred): leave `entry_intent` NULL — none of the three values is TRUE.** `standard` would contaminate the edge-measuring cohort (**one bad sample = 5% of a 2/20 decision target**); `hypothesis_test_by_design` is FALSE and would corrupt the label the training-epoch declaration pins; NULL renders "Unclassified" (`cli.py:1102`) so it is visible in the UI — *I over-stated "invisible"; it IS absent from the D29 cohort readers, which is the part that matters*. **EXCLUDED from the standard cohort. NOT an H1 row — "H1's most precious sample" WITHDRAWN.** **The doctrine gap this exposes: the banked "latch fills label from the FIRE, not the flickered bucket" ruling was written for a FLICKER. This was MONOTONIC DECAY ending below the latch's own invalidation — a fire-labelled fill would have entered H1 as an A+ sample. THE RULING NEEDS A BOUND: a latch does not survive its own invalidation, and a fill after that point does not label from the fire. MINE TO WRITE.** **UNRESOLVED — `entry_price` 36.43 on a session whose framework close was 30.80** (intraday bars NOT queried; the spike is the operator's account and is consistent, but the pair is unreconciled in the record).
- **TWO DEMANDS ROUTED 2026-08-10 (operator-authorized), one cause — the record cannot express what happened to trade 20.** **A: a THIRD `entry_intent` value** for an entry executed against a mandate that outlived its own invalidation (`0027_entry_intent.sql:13` allows only `standard`|`hypothesis_test_by_design`). **Scoped as a GOVERNANCE amendment in the V2.1 §VII.F family, NOT merely a CHECK widening — those two values ARE the training-epoch contract's vocabulary (charter §7), so a third changes what the contract can SAY and silently re-writes what every past `standard` row meant.** Operator-ratified, prior text preserved, like the H1 0034 amendment. **Explicitly NOT to ride item 5's open CHECK machinery as a passenger.** Value NOT named by me — naming is design; the operator's own word is "non-standard." **B: an APPEND-ONLY, TIMESTAMPED post-entry annotation surface.** `trades.notes` is **WRITE-ONCE — verified, no `UPDATE` anywhere in `swing/`** — and the only post-entry free-text paths (`trade_events.notes` via stop-adjust/exit) require a REAL event, so fabricating one to carry a note would record a false event (the same refusal as `mark_unmatched` on disc 95). **MY BINDING CONSTRAINT — RESTATED 2026-08-12, because its premise was false:** `trades.notes` is ALREADY mutable via the audited corrector. **Demand B must not add an UNAUDITED write path to `notes` or the premortem/why_now block.** The entry-time belief is still evidence; what protects it is not writer-absence but that every write leaves a `reconciliation_corrections` row — **which is exactly what makes the tier-2 admissibility check possible. A surface writing those fields outside the audited corrector would destroy the test at the same stroke.** *(So the Demand-B annotation guard now has TWO independent grounds: annotations post-date entry (test (a)), AND an unaudited path would blind the tier-2 check.)* Same asymmetry as append-only `reconciliation_corrections`.
- **ARMING EVIDENCE — CLAIM NARROWED BY ME (2026-08-10).** I first called AMN "the first live instance of the harm arming `criteria_lapsed` would have prevented." **Too strong; withdrawn as a rung-5 attribution.** The latch's close-based **invalidation (rung 4, ALREADY LIVE)** was breached at 30.80 vs 30.85 — under R6 rung 4 sits ABOVE rung 5, so rung 5 may never get a turn; and latches are DERIVED (no `latches` table), so I have **not run the derivation** to establish the actual state at the fill and will not assert it from the ladder. **What the instance DOES establish is sharper: WHICH RUNG CLEARED THE LATCH MAY NOT MATTER, BECAUSE CLEARING A LATCH DOES NOT CANCEL A RESTING BROKER ORDER.** The mandate died in the framework and the ORDER stayed alive at the BROKER — the order-vs-mandate gap item 4's cancel-decoupling addresses, same family as LQDA. **So: evidence for the CANCEL/RETIREMENT path, NOT (yet) for arming rung 5** — the derivation must run first. **The asymmetric arming discipline is UNCHANGED; this adds a third category it never contemplated — the COST OF NOT RETIRING — to fold in when I write the read.**
- **D31 EXIT-SIDE ARC: MERGE HELD 2026-08-11 ON MY RULING — RESOLVED: MAJOR 2 fixes IN the arc.** The orchestrator's independent B (the operator's B-reassignment ruling, first use) found the arc's date-grain cutover leaves fills WITHOUT `schwab_order_id` keyed on stored date alone — a pre-fix row (ENTERED grain) no longer matches its own post-fix candidate (EXECUTION grain) → **re-offered clean → double-record invitation; 10 of 21 non-entry fills are anonymous (verified live), all on closed trades — forward risk, no live exposure.** Worse: the arc's own test BLESSED it (*"a recorded fill dated 08-03 is a DIFFERENT fill"* on a fixture carrying no identity — a docstring asserting what its fixture cannot establish, the sweep's class in ASSERTIVE form). **MY RULING: identity is NEVER asserted from a date alone, IN EITHER DIRECTION** — not entered-date-fallback exclusion (silent omission = MAJOR 1's shape added deliberately), not clean re-offer (silent double-record). **The two-grain match is ALARMED: candidate OFFERED with a possible-duplicate flag naming the anonymous row; the operator adjudicates** (alarm-never-assert + recording-vs-alarming + the ambiguity-surface precedent, same rule as D-2a's `undetermined` one arc over). Blessing test re-fixtured with a DIFFERING order id (claim becomes provable); anonymous-case test added pinning the FLAG. **An introduced regression is not citable** — MAJOR 1 (pre-existing set-multiplicity over-merge) stays cited with the operator, implementer's revert endorsed; eventual fix composes with this flag machinery. **CHARC's verdict-line anchor: demonstrated necessary** (a killed B run's only convergence strings were CLAUDE.md prose echoed back). **SUPERSEDED 2026-08-11 16:58Z — MY STATE-2 WAS WRONG and the orchestrator's second B found it: state 2 assumed an anonymous row's stored-date GRAIN is knowable. Queried live: ALL 10 anonymous rows are `operator_typed` — the grain is unknowable IN PRINCIPLE, forever, not just pre-D31; my model distinguished match-GRAINS when the honest line was always PROVENANCE.** **THE STANDING RULE: silent exclusion requires PROVEN identity (order id); anonymous value-tuple matches FLAG at EITHER grain — never exclude, never offer clean; the operator adjudicates.** Cost owned: flagged re-offers of manually-typed exits while the fill sits in the lookback window — bounded, visible, labelled. **By-product: MAJOR 1's anonymous half DISSOLVES (scale-out candidates all offered, each flagged — silent omission becomes visible adjudication), which DELIBERATELY supersedes the prior arc's shipped contract test (`test_exit_form_auto_fill.py:859`) + the `1625` pin — REWRITTEN to the flag contract, not deleted; the implementer's mid-arc revert was correct THEN, a director ruling is the instrument that supersedes a shipped contract. Operator override available on the MAJOR-1 half; the MAJOR-2 half is measurement integrity and I hold it.** **ROUND 2 (the fresh orchestrator generation's B, operator-directed second opinion 2026-08-11): merge HELD again — 3 NEW majors on the tree A converged clean at R23, ALL on my instrument, all adjudicated by me:** MAJOR 1 (`routes/trades.py:2448`) = a PROVENANCE VIOLATION — the corrected branch keeps the form-render default's order id through a reachable third case its two-case comment forecloses; ruled: an id asserts VALUES→ORDER linkage (operator affirmatively LINKED the row — selection or unedited acceptance), residue is DROPPED and the row enters the flag channel; **STATED SEMANTICS required, not a widened condition** (the false-exhaustive comment is what survived 23 rounds). MAJOR 3 (`exit_auto_fill.py:1043`) = IN SCOPE (the arc built a float-precise comparison atop a pre-existing truncation; its own R16 comment condemns the line left) but the fix is NARROW — the comparison consumes the untruncated quantity (in hand at `:1036`, the signature hashes it); the dataclass int→float migration stays BANKED. **Live check run by me: 43 fills, ZERO fractional.** MAJOR 2 (`view_models/trades.py:1051`) = CLOSE, `.strip()` — whitespace-as-proof is the ruling's own boundary condition failing; the loop re-opens on 1+3 anyway so zero marginal cost. MINOR 1 = the FOURTH `assert_canonical_fill_datetime` site one commit after the sweep swept 'the last three' — the sweep's own bounds-from-below caveat proving itself; the unwired assertion is MERGE-BLOCKING for me at the future `swing/data` carve-out arc (a malformed datetime silently removes a row from BOTH dedupe and flagging). **BANKED FOR §5.1 (posted to CHARC): both B rounds were DELTA-BLINDNESS — every finding sat on a line no late round changed; A reviews what MOVED, B reads what IS; the iterative loop's blind spot is the UNCHANGED line.** Single fix dispatch over all five endorsed; A re-run; B on the fixed tree; then my gate. **Q1 RULED 2026-08-12 — CONFIRMED, and a SECOND shipped contract is superseded ON MY AUTHORITY:** my MAJOR-1 semantics reach the DEFAULT-RADIO OVERRIDE case (old sub-case (a)) too — verified on `0ce45b3c`, where the rewritten test's own fixture IS the case (price edited to 121.25 matching no candidate, id still `ord-1`). **The extension is REQUIRED, not permitted:** carving it out would rebuild the false-exhaustive comment **AS CONTROL FLOW** (a stated rule plus an exception contradicting it — the orchestrator's phrasing, adopted). Sub-case (b) correctly KEEPS the id (non-default radio, no rebinding → the persisted values ARE the default's), which is the proof the rule is real and not a slogan: both halves fall out of ONE question. Encoding verified as the shape I demanded — one `_derives_from` predicate (`:2557`) applied uniformly (`:2616-2618`), meaning stated at the site (`:2520-2527`), no case list. `test_h_r3m1_regression_default_radio_manual_edit_top_level_unchanged` REWRITTEN with the supersession + lineage name preserved. **Two shipped contracts now superseded by this ruling (859, then this) — both mine to supersede: an implementer must not, an orchestrator should not, a director ruling is the instrument that does.** **B ROUND 3 (fixed tree, 2026-08-12) — MAJOR A: FINDING REAL, REMEDY REJECTED, AND THE FAULT WAS MINE.** I wrote that an id asserts *"the persisted values derive from the candidate this id names"* — that was the TEST for establishing the link AT POST, and I stated it as an INVARIANT the row must hold forever. **CORRECTED SEMANTICS: a top-level `schwab_order_id` asserts a HISTORICAL FACT — at recording time the operator LINKED this row to that order. The id names WHICH order; the values describe HOW it executed. Drift between them is a DATA-QUALITY question, NEVER an identity question, and a later correction makes the row a MORE accurate record of the SAME order.** **The live query is what refuted the remedy** (11 envelope-bearing non-entry fills; 1 drifted: **fill 40**, FTRE's exit, current `('2026-08-04',18.4,10.0)` vs candidate `('2026-08-03',18.4,10.0)` — **and its id is CORRECT**, the operator corrected the date toward truth): **the read-path re-derivation would have DEMOTED fill 40 and re-offered its order — the exact double-record my own ruling forbids, caused by my own remedy, on the row this whole programme was built around.** So: **read path owes NO re-derivation; correction writers owe NO id-drop; pre-fix residual population QUERIED = NONE (zero ids naming no candidate), FORWARD-ONLY on evidence; scope reaches no reconciliation writer.** **The real hazard the finding exposes is INVERTED — that a future reader or B round sees the drift and "fixes" it into the re-derivation. Take a COMMENT naming fill 40 as the reason.** **MAJOR B — BANKING REVERSED on new information: a sub-1-share execution 500s the trade-detail page** (`int(0.9)`→0; the dataclass guard rejects `quantity <= 0`; none of `_build_candidate`'s three refusal reasons catches it) — reachable from input both `fills.quantity REAL` and the Schwab model permit. **Remedy is a FOURTH REFUSAL REASON (visible degradation, not a crash), NOT the dataclass int→float migration — which stays BANKED with the sub-1-share limitation named IN it.** The narrow MAJOR-3 fix is unaffected and still ships. **MERGE-BLOCKING for me: a 500 from permitted input.** **B ROUND 3 SCOPE RULING (2026-08-12): the `SchwabSchemaParityError` 500 is BANKED, not fixed in-arc.** `exit_auto_fill.py:699` catches `(SchwabAuthError, SchwabRateLimitError, SchwabApiError)`; the MRO is `SchwabSchemaParityError → _RedactedMessageError → RuntimeError`, so a deliberately-raised parity error (`trader.py:665`) escapes and 500s the page. **`git log -S` on that symbol in that file is EMPTY — the arc never touched it.** **Evidence I ran rather than delegating: 7,795 production calls, ZERO schema-parity failures EVER; 3 lifetime errors on `accounts.orders.list` (1,278 of 1,289 are `marketdata.pricehistory`).** **THE BOUNDARY IS INTRODUCED-vs-PRE-EXISTING AND SEVERITY DOES NOT OVERRIDE IT** — the sub-one-share crash was fixed because the arc BUILT that machinery; this is untouched pre-existing. **If identical operator experience sufficed to pull a pre-existing defect in, the boundary would have NO CONTENT — every pre-existing defect produces some operator experience, so the exception swallows the rule and every bounded arc ends where its reviewer's imagination did.** The answer to "pre-existing is the excuse this project distrusts" is to make the banked item REAL, not to abandon the boundary: **BANK ≠ FORGET.** The item carries file:line, the handler tuple, the MRO (found only BY MRO, not by reading the except clause), and the zero-in-7,795 incidence WITH its query. **Scoped to the CLASS — *which handlers catch that tuple, and does the parity error escape each?* — not to the one line;** the right fix is wider than the arc could have taken. **The sub-one-share ADVISORY WORDING is MINE:** an advisory telling the operator to record by hand while the form's `min="1"` forbids it instructs an IMPOSSIBLE ACTION — strengthened beyond the orchestrator's scoping: it must say what the operator CAN do. **BANKED FOR THE RECIPE (orchestrator's, and it is worth more than the error that produced it): a reviewer not told what has been ACCEPTED re-finds it every round forever.** Declaring accepted contracts WITH their reasons — and explicitly INVITING challenge rather than forbidding the subject — suppressed re-litigation while B still returned 3 majors (round-2/round-3 pair is the evidence). **MY CAUTION: the list is mostly MY rulings and I have been wrong twice this week, both times corrected by a B round reading something I declared settled — so each entry carries its REASON, never a bare conclusion, or the list suppresses the challenge that corrected me on MAJOR A.** **CONDITION ATTACHED on the banked client-editable-envelope item:** its eventual brief must ENUMERATE the dismissals resting on it and re-adjudicate each — **a banked item silently governing other findings' dismissals is a load-bearing artifact, not a queue entry** (currently one live dependant, the `candidate_index` finding; MAJOR 2 is moot, closed not cited). **OPERATOR RULED 2026-08-11: "Flag noise is fine" — the MAJOR-1 override DECLINED, the FULL provenance rule stands UNQUALIFIED; the labelled floor is MOOT (do not ship it as a halfway house). Both contract tests (859 + 1625) rewrite to the flag semantics; MAJOR 1 leaves the cited list for the anonymous population. Nothing operator-pending remains on this arc; my merge gate re-runs at the fix's return.**
- **THE MERGE GATE — RESOLVED 2026-08-12, and MY boundary was creating the contradiction.** B is a WHOLE-ARTIFACT reviewer (that IS the delta-blindness cure), so on a file carrying real debt it returns real findings indefinitely — **"merge when B returns clean" is UNREACHABLE**, and held literally it forces either never-merge or the arc absorbing the surrounding surface, **the unbounded-arc failure my own boundary exists to prevent.** **ADOPTED (orchestrator's proposal, my concession): B IS CLEAN FOR MERGE WHEN IT RETURNS NO UNBANKED CRITICAL/MAJOR THAT THIS ARC INTRODUCED; pre-existing findings are BANKED — file:line, mechanism, reachability evidence — never merely dismissed.** The banking obligation IS the answer to "pre-existing becomes forgotten"; a gate reading INTRODUCED without it would be strictly worse than the unreachable one. **BINDING (their caution against their own proposal, the load-bearing half): IF AN ARC CANNOT SHOW THE EVIDENCE, THE FINDING DEFAULTS TO INTRODUCED** — the cheap failure is fixing what you did not break, the expensive one is shipping what you did, so the burden sits on whoever claims PRE-EXISTING. **Standard: a `git log -S` on the changed symbol or a `git diff --stat` proving the region untouched, QUOTED in the adjudication — never "this looks pre-existing."** **MY CLARIFICATION: CONTESTED provenance = INTRODUCED — where the symbol predates the arc but the arc changed its MEANING or made it REACHABLE (the truncation-under-a-new-float-comparison and the sub-one-share crash both). PROVENANCE IS ABOUT THE DEFECT, NOT THE LINE.**
- **THE TOLERANCE — I WAS WRONG IN ROUND 3 AND ALMOST WRONG AGAIN CORRECTING IT (2026-08-12).** I endorsed *"the tolerance is fine; the justification is the defect."* **Both were defective.** Reproducing the orchestrator's numbers, `math.isclose(a,b,abs_tol=1e-9)` returns **True** — `rel_tol` DEFAULTS to 1e-9, which at magnitude 1e7 is a tolerance of 1e-2 — so I nearly filed a correction against a correct finding. **Then I read the CALL: `:1418` passes `rel_tol=0.0` EXPLICITLY.** Against the real signature: large case residual **1.862645e-9 → False → a genuine duplicate offered CLEAN** (the silent miss the provenance rule forbids, reintroduced by the fix that closed its exact-equality version); small case 5e-10 → True → **different quantities flagged as the same row**. IN SCOPE (`git log -S "abs_tol"` → this arc's `5203caa9`). **MY LESSON: a comment defending a value is a claim ABOUT that value — auditing the claim's HONESTY while taking the VALUE on faith is HALF AN AUDIT**, the same shape as the paraphrase failure: inspecting the description instead of the thing. **And: reproduce against the REAL CALL SIGNATURE, never a plausible reconstruction of it — the default argument I did not check would have inverted the ruling.** The hybrid closes the large-magnitude miss and correctly does NOT tighten the small case (a spurious flag is adjudicated; a silent miss is not) — **the fix must SAY so, or the surviving looseness reads as an oversight.**
- **MY FORWARD TEST WAS CONFOUNDED — 96/97/98 CLOSE ON OTHER GROUNDS (2026-08-12).** I ruled the next reconciliation run would DECIDE their disposition. **It could not have.** Item 5 merged / correction 38 witnessed **2026-08-11 ~05:40Z**, and the FTRE `entry_price_mismatch` chain stopped at **run 97 (08-07)** — FOUR DAYS BEFORE THE FIX EXISTED. Runs 97, 98 and 99 (08-11T03:44, ~2h pre-correction) all ran on UNFIXED code; **only run 100 (08-12) is genuinely post-correction. Post-fix evidence = ONE run, not four.** *(The orchestrator read the sequence as four confirmations and concluded the fix took; the evidence is right and the conclusion does not follow — and it was MY test's premise that failed, not their diligence: they re-derived every state from the live DB, which is the only reason the sequence was in front of me.)* **DISPOSITION, on the DIRECT fact I reached past: the rows describe a condition CORRECTION 38 DEMONSTRABLY FIXED** (`entry_date` now 07-31, three coupled rows moved atomically, verified live) — whether the mapper also stopped re-emitting is a DIFFERENT question from whether these rows still describe something true. **Close all three `journal_corrected`, each reason citing CORRECTION 38 as the fixing event, NEVER the forward test; 97/98 keep their own ambiguous-match text and must not inherit 95's zero-delta wording.** **THE UNEXPLAINED CESSATION IS THE WORSE FINDING AND IS BANKED AS MINE: a reconciliation check that stops emitting for an unknown reason is a candidate SILENT FAILURE OF THE CHECK ITSELF** — the instrument going quiet instead of alarming, the class I have ruled on all week. Two benign explanations exist (the 7-day `lookback_days` window aging the entry out; the 08-06 exit-date correction changing the match) and **NEITHER IS ESTABLISHED.** Until one is, we do not know whether the check works or has stopped looking at FTRE. **It must never be recorded as "the fix took."** **THE LESSON: I built a test on the premise that a signal would PERSIST until my fix removed it, and never checked that the signal's absence had only one cause. A silence test needs its confounds enumerated BEFORE it is relied on** — inference from absence is exactly what I forbid everywhere else.
- **DISC 99 (AMN `untracked_broker_position`, material, unresolved) — THE CHECK WAS RIGHT WHEN IT FIRED, and I nearly closed it as stale.** Trade 20's entry fill carries `auto_fill_audit_at = 2026-08-09T06:17Z`; disc 99 was raised by run 98 at **2026-08-08T03:38Z**. **The position genuinely was not in the journal when the check ran — it was recorded a day later.** Not a false positive, not a timing artifact. **Close `journal_corrected`, with a reason stating the finding was CORRECT WHEN RAISED** and the position was subsequently journaled as trade 20 (recorded 08-09, entry dated 08-07) and has since closed. `mark_unmatched` would be false. **A row closed without that sentence reads as a false positive and teaches the next reader to distrust the check.**
- **TOOLING, MINE (2026-08-12): I posted a fenced code block via inline `--body` and BASH ATE IT** — the delivered message carried an EMPTY fence where the run table was, i.e. the one load-bearing piece of a message about my own instrument being unsound was the piece that did not arrive. **The `--body-file`-always rule was banked by the orchestrator this week and I said I adopted it. Write the body to a file and post it in the SAME STEP — never inline, never reach back for a path.**
- **MEASUREMENT TRAP — `pre_trade_locked_at` IS NOT EVIDENCE (banked 2026-08-09).** It equals `entry_date + 'T16:00:00'` on **20/20 trades, zero exceptions** — a synthetic restatement of the column it appears to corroborate, not a lock timestamp. Same family as gotcha #30 (a stamp standing in for per-row provenance), except here the stamp is DERIVED from the very value it seems to independently confirm. **Any decision-to-execution LAG analysis built on it would measure exactly ZERO on every trade, by construction, forever — and would look like a clean finding.** Build nothing on it until it is fed a real lock time or retired.
- **Watches:** `invalid_ohlc` **52** (§1 carries the trajectory + the stoplight-cannot-escalate finding; *this line said 38 until 2026-08-11 — my own file disagreeing with itself, the corroboration-failure class at home*); the D27 naming fix (Phase-21-close/22 scope; my trace `docs/rd-d27-provenance-trace.md` — the straddle unifies D27 with hit 7, 0/140 measured); hits 4/5 = a precondition on any re-use of the dormant V1↔V2 harnesses; truncate-at-last-bar (kills the delisted-window class, P22).

## 6. BEHAVIOURAL — load-bearing, earned (inherit the whole list; these are this generation's additions)

- **THE DOC IS A LEAD, THE CODE IS THE EVIDENCE** — unchanged and still the top item. This generation's instances: the VSTS fire date (quoted rd-state instead of the candidates row); the "can never exceed clock-derived" overstatement (corrected by my own trace's asymmetry); the unrounded-vs-production-path incidence (7 vs the true 43 per 100k); the epoch-contract design-note ambiguity a blanket filter would have turned into zeroing H2. **Rulings stated as SEMANTICS survive corrections that would kill rulings stated as numbers — state the semantic, let the numbers be illustrations.**
- **WHEN DIRECTING AN EXCISION, QUOTE THE TEXT TO PRESERVE VERBATIM — OR DELEGATE THE SEAM AND SAY SO. NEVER PARAPHRASE IT** (added 2026-08-07, earned within the hour of writing the direction it broke). I bounded the `service.py:140` deletion by naming the keeper in MY OWN WORDS. The artifact's sentence carried TWO propositions joined by an em-dash; my paraphrase named one and silently dropped the false one. **A keep-list stated in the director's words bounds the director's SUMMARY, not the TEXT — and the gap is exactly where a surviving falsehood lives.** Executing my line numbers would have preserved a fourth falsehood in the authority position, now carrying a fresh commit certifying the block as reviewed: **a partial cleanup LAUNDERS what it leaves behind.** The orchestrator's replacement rule supersedes my own and is credited to them — *when an arc discharges a recorded deferral, re-verify EVERY claim the note makes against the symbols it names, INCLUDING the claims you intend to keep; the keepers are the dangerous ones because they survive the cleanup that was the safety net.* **Detection method belongs IN the rule: neither of us caught it by READING** (that prose is engineered to read plausibly) — it fell out of checking the named symbol against its CONSUMERS. Four lines of grep. Same family as the grep-proves-what-it-names line below.
- **A HANDOFF RECONCILES AGAINST THE RECORD; IT DOES NOT ENUMERATE FROM MEMORY** (added by the incoming generation, 2026-08-07 — earned against the handoff that produced this very file). The orchestrator posted the line-38 correction at **19:57:32Z**; the handoff commit landed at **20:04:44Z without it**. Seven minutes, and the outgoing generation wrote a false fact into its successor's boot surface. Same class as the H1 amendment that was acked and never executed — **the acked-vs-actioned gap**, which costs most at exactly the moment nobody is watching for it. **Before overwriting this file, drain the inbox and diff every claim against the artifact that owns it.** The corollary the same incident earned: **when an arc discharges a recorded deferral, deleting the deferral note is PART of the fix** — otherwise the note and the state file corroborate each other and the check that should break a false belief confirms it instead (sibling of CLAUDE.md gotcha #31; routed to CHARC for the catalog, not self-authored — harness architecture is their lane).
- **A RULING RECORDS ITS REASON AT RULING TIME — a disposition without its discriminator gets re-litigated forever, including by the standard I set for others** (added 2026-09-02, earned on B's P1). My leg-6 ruling approved the PE-anchor guard's mechanics ("ordinary path only — APPROVED") without recording WHY it is not cohort bookkeeping; B read the surviving entry-refusal as a principle violation and the question came back around one full round-trip later. I had attached exactly this rule to the accepted-limitations list ("the list must carry each item's REASON, not just its disposition") and did not apply it to my own ruling. The claim-vs-absence discriminator now travels with the disposition. **Extended same-week to ENDORSEMENTS (clause 2, 2026-09-02): endorsing another director's rule without stating its admissibility preconditions is the same omission — I called resolve-by-read fail-closed while "present" silently required visibility + attempt-identity, and round 10 paid the round-trip.**
- **AN INSTRUMENT'S SILENCE IS EVIDENCE ONLY THAT IT LOOKED — verify the check EXISTS before reading its silence as the world** (added 2026-09-02, earned within hours of the read). I told the operator "the broker still holds ORKA — no stop fired" from three reconciliation runs' silence; the position check is one-directional and structurally cannot see a sold-but-journaled position. The stop had fired six days earlier. Same family as the FTRE cessation rule I wrote a month ago ("a check going quiet is a candidate silent failure of the check itself") — I had the rule and did not apply it to a check whose coverage I had never verified.
- **POST IT WHEN YOU SAY IT.** A commitment stated in chat is NOT routed — chat is not even a mailbox. Three unposted rulings sat for hours while the orchestrator held a witness on one. Post in the same turn, or record the deferral in this file.
- **Cross-branch investigation reads via `git show`, NEVER `cd`** — a persistent shell CWD silently re-points every relative path (bit me twice; the second time mid-QA).
- **The grep-proves-what-it-names ledger:** state what a grep PROVES, not what you infer. My instance: D29's reader count (my grep's scope guaranteed my undercount). Six holders in one week; the method belongs in the doc beside the number.
- **A ruling is PROVISIONAL ON ITS PREMISES; a load-bearing number arriving later RE-OPENS it.** And when both cost and value move against a conclusion, change it — I reversed the scoped-all-clear ruling on exactly that.
- **QA the divergence, never defend the brief — including my own rulings.** The implementers beat my framing repeatedly this tenure (the three-name view split; the data-check that saved the epoch-contract note; the unreachability argument better than my lean). Adopt what is better; own what was mine.
- **Blunt over sycophantic; verify intent before labelling a mistake; every number freshly queried.** Run `role_mail` with a `cd`-prefix in the same command; long bodies via `--body-file`; briefs crossing a §3 tripwire route through CHARC pre-dispatch; RD is merge-blocking on measurement integrity.
