# RD — Current State (single source of truth)

> **OVERWRITE this file each session/handoff — do NOT append.** This is the one always-current state pointer for the RD (Research Director / CIO) role. The dated session log in [`docs/research-director-context.md`](research-director-context.md) §7 is APPEND-ONLY history; current state lives HERE only. Bootstrap reads this file FIRST. Convention: [`docs/harness-architecture.md`](harness-architecture.md) §6.

---

**Last overwritten:** 2026-07-27 (session resume; all numbers FRESHLY QUERIED this date — glance + monitor + live DB + artifact `20260725T033855Z`). **Phases 19 AND 20 are CLOSED** (audits clean at `0d560822` / `e3b6570f`); main HEAD `b25ab488`, 65 ahead of origin (local-first; operator pushes, do NOT push); schema **v31** (two consecutive zero-migration phases); ~9115 fast green.

**Program state:** **T4 = CONFIRMED-NEGATIVE, FINAL — A+ gate selectivity validation BANKED** (study + §8: `research/studies/2026-07-03-broad-watch-baseline-t4-decision-read.md`; its 4 cited artifacts are git-tracked under the study's `artifacts/` per watch-standard **v2.1** cite=commit). Both measurement chains have now been forensically hardened and witnessed: the RESEARCH chain in Phase 19 (shadow-engine risk-unit floor + epsilon clamp; my T4 confirmatory re-read discharged it) and the TRADING LEDGER in Phase 20 (the D25 tier-1-corrector corruption — 3 fills + a phantom trade — fixed, corrected, RD-witnessed to the cent, and proven live against its own causal geometry by run 78). **Posture: STOP-ENGINEERING + MARKET TIME.**

## Live state (fresh 2026-07-27)
- **Research health: ALL SEVEN GREEN — second consecutive all-green read.** `invalid_ohlc` holds at EXACTLY baseline 23 (the 19-D clamp's attrition fix is stable, not a one-off); `insufficient_forward_depth` cleared to 0. Substrate 51,327 observations, zero post-baseline non-finite.
- **Drumbeat healthy + unattended:** 687 unique signals, `total_unattributed = 0` throughout; runs 139/140 (`trigger='scheduled'`) landed on schedule. Newest artifact 2 days old = the weekend gap (Fri 07-24 data), correct — Monday's run fires tonight.
- **H1 LIVE = 2/20 closed:** VSTS +$1.80 (+0.05R) · AMN +$1.17 (D25-corrected). Both scratch-wins.
- **H1 SHADOW now has real evidence — A+ closed cohort n=2, mean −0.50R:** NVCR 06-18 **−1.0000R** (`initial_stop` — it DID eventually trigger; was 'untriggered' at read #1) · AMN 07-01 **0.0000R** (`breakeven_stop`) · VSTS 06-25 EXCLUDED `degenerate_risk` (the 19-D floor catching the collapsed risk unit, exactly as designed) · **FTRE 07-20 OPEN at +1.2165R** (`horizon_mtm`). Meaningless at n=2 — but H1 is finally accruing measured outcomes.
- **THE FTRE GAP IS NOW QUANTIFIED: shadow +1.2165R, live nothing.** The shadow entered 18.34 on 07-22; the live order was dead (vacation). This is the measured price of the entry-window gap — and the strongest possible argument for the latch dashboard.
- **VSTS RE-FIRED A+ 2026-07-27** (5th fire ever): pivot **16.90**, framework stop 13.40, close 15.58 — order standing above market from fire time = the latch's first clean-from-the-start application. NOTE the geometry: pivot−stop = $3.50 = **20.7% of entry** (wide); the mechanical ruleset would use the entry-bar LoD instead — **decide the plan-of-record stop BEFORE fill** (it drives both size and every future R).

## Open watch items
- **Broad-watch closed cohort has grown 100 → 267 signals and the mean R has drifted −0.637 → −0.224** (still negative; the banked verdict's DIRECTION holds). The magnitude softening as N grows is a real observation to examine at read #2 — NOT grounds to reopen a frozen criterion, and the positive branch is untriggered (mean is still < 0).
- **PGNY-class floor-boundary marks** (risk/ADR ratio 0.15–0.2 band still amplifies): revisit ONLY on realized evidence, no ad-hoc re-tune.
- **T9 pattern-count gate**: needs a deliberate operationalization (matured-forward-walk reading vs raw counts) at a future read.
- **Shadow-twin divergence** (live day-3 partial vs engine day-4 fire point): n=1, revisit only if it systematically biases live-vs-shadow at scale.
- **Banked semantics:** `equity_delta_dollars` stores the RAW OOF-INCLUSIVE gap — never read it without OOF-netting (the badge/emit path nets).

## Phase-21 in flight (as of 2026-07-28)
- **21-D COMPLETE + LIVE** (`57e4e797`): singular orchestrator inbox, 67 files migrated sha256-matched, zero deleted.
- **21-A at merge gate:** operator witness PASSED both states (run against a v32 SNAPSHOT — migrating live would hard-fail the scheduled pipeline's exact-match schema guard). Multiplicity guard + regime label LANDED (boundary verified by me on disk — count-only, nothing iterates the matched set). Outstanding: the scoped-all-clear refinement + the two-reason wording split (a label that fires every pre-run evening must not read as a warning), then rebase + merged-head suite. **My merge QA is HELD for the post-rebase head** (9409 is pre-rebase; this arc produced three self-inflicted interaction defects). My rulings shipped structurally (no zone clear-reason; horizon derived from config; two-form shape check live-verified — FTRE's GTC LIMIT 18.89 now reads MATCH under the PULLBACK regime).
- **21-F (operator-originated: surface latches on the dashboard) — RULED option (b)-sharpened**: telemetry records WHICH LATCHES were rendered + the surface (the `surface` column rides **21-B's** migration per CHARC — I conceded my before-0032 timing). Sequencing: with-or-after 21-B; the telemetry contract must settle BEFORE any dashboard surface ships. CHARC FORBADE a broker call on the dashboard path (order cache promoted V2→PREREQUISITE); **my rider: a cached-order-derived ALL-CLEAR must never be asserted** (alarms from cache OK, coverage claims not). Traced: the latch MANDATE half is fully LOCAL-derivable (candidates + OHLCV + local fills) — only order-agreement needs the broker, so my telemetry is independent of the cache design.

## Pending RD
- **T5 FTRE: ANSWERED + VERIFIED 07-23** (vacation; order never fired — RIGHT through 07-21 incl. a ONE-CENT miss; but 07-22 CROSSED 18.34 with the order dead → no live entry while the SHADOW window will trigger it). **LATCH POSTURE ADOPTED 07-23 (operator):** A+ fire → GTC buy-stop-limit at the fire's pivot + buy-zone limit cap; latch clears ONLY on fill / setup invalidation / horizon (~20 sessions, the shadow-parity bound) — never bucket flicker. **FTRE latch: STILL ARMED, UNFILLED.** The GTC limit 18.89 never filled — price GAPPED away 07-23 (O 19.26 / L 19.01) and has not returned to the zone (07-24 L 19.25; now ~19.52, +6.4% over the latched pivot). No invalidation — **the FIRE-TIME invalidation level is 14.88** (evaluation_run 121), NOT the 16.51 I earlier quoted from a drifted later row (my own freeze-at-fire constraint, violated by its author; conclusion unchanged — price ~19.5 is far above both — but 16.51 would cancel ~11% early). Base intact; **horizon CORRECTED to 2026-08-31** (30 sessions — my earlier '~20 sessions / 08-17' was an untraced invention that the 21-A plan then back-solved from, circularly; the shadow's effective entry window is the 30-session observe bound, so 30 is the parity number). Two DISTINCT items, do not conflate: the missed 07-22 entry was an ORDER-LIFECYCLE failure (the latch fixes that class going forward — cost now +1.2165R and rising); the 07-23 refusal to chase at 19.475 was a CORRECT discipline call (the zone cap working). FTRE's pivot has since risen 18.34 → 18.59 → **20.19** — a re-fire would open a NEW latch at the new pivot; the old latch does not entitle entry at today's price. **The latch dashboard is now an OPERATOR-DECLARED Phase-21 input** (posted CHARC `20260723T161518Z`; semantics settled, RD gates the derivation rules).
- **Catch-up read: operator-sequenced 07-23 to FOLD INTO monthly read #2** (first trading week of August). Read-#2 agenda carries: the broad-watch mean drift (−0.637 → −0.224 @ n=267), the FTRE shadow-trigger once depth suffices, T9 operationalization, the §3.2 format.
- **Phase-21 latch execution: OPERATOR DESIGN ADOPTED (supersedes my three-tier split — his line 'a human decides every order' beats mine 'compute vs act').** Filled form + PLACE/CANCEL buttons, staged log-only → preview_order → live (CHARC scope `81244576`). **My MEASUREMENT RULING posted `20260727T164346Z`: the separation is PRESERVED and the instrument IMPROVES** (removes mechanical noise = contamination; preserves decision variance = the real signal; adds fill quality) — **and the form makes a non-entry LEGIBLE as a decision rather than ambiguous with forgetting (an information gain).** BINDING (amended 07-27, operator-sharpened): action is a THREE-state space — accept / decline(+reason) / **no-action, which must NOT default to 'away'** (that launders indecision into noise and biases the discipline signal optimistic). Loop-closer: objective view-telemetry (panel viewed while armed) + an attestation prompt ONLY for the ambiguous viewed-but-no-action cell; **an unattested viewed-no-action defaults to DISCIPLINE LAPSE, not away** — the instrument must not flatter its subject. Bonus: the AWAY RATE becomes measurable = the quantified business case that will justify or kill stage-3 auto-place. Acceptance test: FTRE must retro-classify AWAY (+1.22R to the away bucket, not against his judgment). Stage 1 = an EXECUTION-PARITY LEDGER (framework-computed vs operator-actual per fire, end-to-end linked to the shadow twin) — RD reads it monthly; it is also what EARNS stage 2. **Telemetry FOLDED INTO 21-A by RD ruling** (gap fires would be the instrument's first observations; unclassifiable = lost, not neutral; possible 0032 migration cost-share). Linkage: store BOTH the candidate fire identity AND the detection identity (different id spaces; exactness now is unrecoverable later). **21-A PLAN GATE PASSED 07-27** with five rulings (`20260727T210759Z`): fire-identity resolved as SUPERSEDE-on-different-pivot / re-confirm-on-same (no fourth clear condition; zone escape is a display attribute); horizon = 30 sessions bound to the config source; dual anchor, terminal precedence, and the base-break deferral confirmed; 21-B must telemetry-health-check the away-rate. **Constraint 3 RULED three-ways 07-27** (same-session duplicates collapse / re-fire-while-armed = re-confirmation with NO re-freeze / re-fire-after-clear = new latch; re-confirmations recorded as an attribute; drift-while-armed surfaced never silently re-frozen) — it conflicted with constraint 1 as originally written; the ledger's unit is the LATCH not the fire, which protects the away-rate denominator. **Six derivation constraints issued for 21-A** (freeze-at-fire — a LIVE trap, FTRE's pivot drifted while armed; sessions-not-calendar; per-fire identity; latch-specific fill detection; clear-reason recorded; invalidation on closes). Stages 2–3 remain behind the operator-signed L2 endpoint diff. Phase-21 scoping comes via the fresh CHARC generation. Inputs now: **the latch dashboard + this amendment (operator-declared 07-23/07-27)** + D5 suite-runtime (the audit's headline candidate) + my riders (the deferred taxonomy discrepancy_type — NO number; 21-A takes 0032 — plus execution↔fill identity and the bound-b stats choke point). RD gates: the latch derivation rules + anything measurement-touching.
- A+ spot-check cadence continues to N=10 priced (currently 2 priced).

## Behavioral load-bearing (pointers into the charter)
- Blunt over sycophantic; evidence before assertion (every number freshly queried — never carried forward). Verify intent before labeling a mistake, then be direct.
- **Adjudicate residuals by DECOMPOSITION, never prior plausibility** — D25's headline: the $10 equity_delta instrument caught a real corruption BOTH directors nearly dismissed as deposit drift. A monitor that fires gets a decomposition before it gets an explanation.
- **THE DOC IS A LEAD, THE CODE IS THE EVIDENCE** (banked 2026-07-28, orchestrator-framed, aimed at me). Four times this phase-pair a live-code check overturned one of my assertions — the pruner, the correction-chain grain, write-on-GET, and the horizon (where my untraced '~20 sessions' became its own citation via a date I recorded from it). **I am reliable on SEMANTICS and unreliable on REMEMBERED CONSTANTS: any figure I state that a plan will build on must be TRACED at the moment I state it, never recalled.** Cite `file:line` or a live query, never a director doc, for anything numeric.
- QA the DIVERGENCE, never defend the brief — **including my own rulings** (Option-C; the #33 stamp withdrawal). **Trace the neighboring system / the key structure before ruling on a mechanism** (the pruner miss; the per-discrepancy chain-grain miss).
- Weight REALIZED (closed-only) over marks in every expectancy read; when primary scenarios disagree in sign, the pre-committed realized read binds (T4 precedent).
- Run `role_mail` posts with a `cd <main-repo> &&` prefix in the SAME command — never trust a prior cd (2 misdelivery instances banked).
- Briefs crossing a CHARC §3 tripwire route through CHARC pre-dispatch; RD is merge-blocking on measurement integrity; deviations from stop-engineering need written justification.
