# 18-H.6 — `untracked_broker_position` — orchestrator QA'd return report (NOT yet merged)

**To:** CHARC (QA-on-disk + the §3 condition + B-[P1] adjudications requested) · **From:** orchestrator · **Date:** 2026-06-16
**Arc:** Phase 18 §18-H.6 (TRIPWIRE: schema 0031 + `swing/trades`+`swing/data` carve-out). **Brief:** `docs/phase18-arc-h6-dispatch-brief.md` (`3c5c1ec6`).

## 0. Status — code QA PASS; merge PENDING (CHARC QA-on-disk + 2 operator live-witnesses)
Branch tip `5a401f84` (3 commits on `ef1ec09d`; main now `9346f739` — advance is docs-only harness-backlog, ZERO carve-out overlap → clean rebase + no schema collision). Suite **8588 passed, 5 skipped** (before-review 8586), `ruff check swing/` clean, trailers all `[]`. NOT merged — two adjudications below + the gate sequence (CHARC QA-on-disk → operator migration witness → operator §5.10 recon witness → merge).

## 1. The build (verified on disk)
- **Task 0 (#11 atomic, C1)** — commit `9c15020d`: grep-all-`swing/` confirmed the only full-enum mirrors are `DISCREPANCY_TYPES`/`_DISCREPANCY_TYPES`/`MATERIAL_BY_TYPE`/the migrations; ALL updated in one commit (+ the `len==11` + MATERIAL-covers-all tests). Verified.
- **Migration 0031 (C2)** — current `reconciliation_discrepancies` confirmed byte-identical to the 0019 def (0019 was the last rebuild; 0029 only comments it); rebuild stayed **well within the 0019 precedent**; all 20 cols / 5 indexes (incl. `pending_ambiguity` partial) / FKs / cross-column CHECK preserved; #9 BEGIN/COMMIT; strict `pre==30` backup-gate; `EXPECTED_SCHEMA_VERSION=31`; read-path widened. 10 migration tests (round-trip, run-twice no-op, backup-gate-at-pre==30). db.py edited (version + expected-tables + gate) — within the brief's allowance.
- **Orphan pass (§3.1, C3)** — commit `018198fb`: step 5.1 in `run_schwab_reconciliation`, reverse of step 5 (`journal_open_tickers` set + the same `instrument.symbol` match), `_emit untracked_broker_position` (field_name `broker_position`, trade_id NULL, `{journal_qty:0}` vs `{qty,market_value}`, ASCII); qty = `longQuantity-shortQuantity`, MV = the top-level `marketValue` (None→"MV unknown"); `math.isfinite` guards (R1 Major 2 fix, commit `5a401f84`). Additive-only — existing checks byte-identical.
- **Warning removal (refinement 2)** — the journal-flat `orphan_broker_position` cash-warning removed; the both-flat `equity_delta` suppression preserved structurally.

## 2. ADJUDICATION 1 — C4 sandbox gate (implementer deviated from the brief's literal C4 test text; I VERIFIED the deviation is correct)
**The brief C4 / §5 test text says "the new `_emit` writes domain rows ONLY under production / under sandbox the orphan pass writes NO domain row."** On disk that premise is slightly wrong:
- `_emit` (schwab_reconciliation.py:368) does **NOT** gate on environment — it inserts the discrepancy row UNCONDITIONALLY via `repo.insert_discrepancy`. EVERY existing emit (entry_price/position_qty/…) does the same. Discrepancy rows are recon **output**, not a sandbox-gated domain mutation.
- The sandbox short-circuits in the file gate the actual DOMAIN MUTATIONS — tier-1 corrections (:706) + cash_movements inserts (:1025-1031 "cash_movements is a DOMAIN row → short-circuited under sandbox").
- The production gate for the WHOLE recon is at the **caller**: `swing/integrations/schwab/pipeline_steps.py` invokes `run_schwab_reconciliation` only under `environment=='production'` (sandbox skips the domain writes).
**→ The orphan emit correctly mirrors every existing emit; it adds NO new contamination surface (identical posture to all 10 existing discrepancy types).** The implementer flagged the tension + resolved it by "mirror every other `_emit`" + a sandbox test matching the established `test_run_schwab_reconciliation_sandbox` semantics. **My recommendation: BLESS the caller-gate interpretation** (C4 satisfied in spirit; the literal test wording was premised on a wrong model of where the gate lives). It is your §3 condition — your call to ratify.

## 3. ADJUDICATION 2 — the orphan-attribution V2 gap (BOTH Reviewer A R1-M1/M3 AND gating Reviewer B [P1] independently)
Reviewer B (codex-auto-review, repo-access, effort=high) ran a LIVE repro: emit an orphan → `count_unresolved_material` = **0**, banner-link = **None**. B's [P1]: *"the all-FK-null orphan row is not integrated with the material alert + resolution flows — it does not show in the global alert, and the FK-requiring acknowledge/resolve handlers raise on it, making the core finding effectively impossible to surface/clear for broker-only holdings."* Reviewer A's R1 Major 1/3 + R2 are the same concern; A's R3 + B both confirm it is the **PRE-EXISTING, documented V2 boundary** (`swing/metrics/discrepancies.py:14-21` excludes orphan `trade_id IS NULL` by construction, "orphan attribution is a V2 candidate"), NOT introduced by this arc, and the fix touches `reconciliation_auto_correct.py` / `repos/reconciliation.py` / `cli_schwab.py` — ALL outside the 4-file carve-out. The brief §2 explicitly scoped 18-H.6 to **"V1 = visibility"** (the raw `reconciliation_discrepancies` row + the run summary), deferring correct attribution to V2.
- **Per the B gate ("a B [P1]/major resolved-OR-CITED before merge"): this is CITED** — documented boundary + brief deferral + out-of-carve-out. Gate-satisfiable by citation.
- **BUT the V1 "visibility" is narrower than one might assume:** the orphan shows in the recon RUN SUMMARY + the raw table, but does NOT light the global material banner/count, and is not clearable via the normal resolve UI. The brief §5.10 routes "acceptable live VISIBILITY" to the **operator's recon live-witness** — that is where this should be decided.
- **Decision needed (CHARC + operator):** (a) accept the run-summary-level V1 visibility as sufficient and merge on the citation, OR (b) commission **18-H.6.1** — a small carve-out-extended follow-up: a no-FK-safe `untracked_broker_position` terminal resolver + banner/count inclusion for orphan rows. My lean: merge V1 on the citation IF the operator's recon witness confirms the run-summary visibility is enough to act on, and bank 18-H.6.1 for the banner/clear integration.

## 4. Reviewer A / convergence / mechanics
A: review-strong, `-p review-strong` (profile present), repo-access, **`reasoning effort: high` confirmed in every raw transcript header**, converged R3 `NO_NEW_CRITICAL_MAJOR` (R1 Major 2 fixed in-carve-out; M1/M3 = the §3 V2 boundary above). B: clean except the cited [P1] (§3). #11 atomic; migration within 0019; 8588 green; trailers `[]`. Pre-review tree `018198fb`; A/B artifacts: `.worktrees/18-H.6-untracked-broker-position/.copowers-findings.md` + `.codex-review-r1..r3.txt`; B output `.codex-B-18-H.6.txt`.

## 5. Gate remaining
CHARC QA-on-disk (ratify C4 + adjudicate the B-[P1] citation + the V2-boundary disposition) → operator MIGRATION live-witness (v30→v31 on the live DB) → operator §5.10 RECON live-witness (orphan fires for the real IPO shares + acceptable-visibility call) → merge + no-false-green merged head. No RD merge-block.
