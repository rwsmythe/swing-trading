# Orchestrator Handoff — 2026-06-16 PM (Phase 18 §18-H batch: H.4/H.4.1/H.2/H.3/R1/H.1/H.6/H.6.1 all SHIPPED+CLOSED)

**Audience:** a fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). **Read [`docs/orchestrator-context.md`](orchestrator-context.md) end-to-end first** (durable role framing), then this doc (the delta this generation), then `git log --oneline -20` + `git status`. Bootstrap via `scripts/orchestrator_bootstrap.md`.

## 1. Clean boundary (verify: `git log --oneline -10` + `git status -sb` + `git worktree list`)
- main HEAD **`3ebbbcc8`** (the 18-H.6.1 close-out; code at `6b7db700`). **origin is ~23 behind — NOT pushed** (operator cadence; push only when asked). **Single `main` worktree** (all this-gen worktrees ff-merged + swept).
- **~8618 fast green** on `main` (re-confirmed on the merged head at the 18-H.6.1 close). **Schema v31** (0031 `untracked_broker_position` CHECK widen, shipped this gen + operator-witnessed live-migration). Ruff clean. **ZERO `Co-Authored-By`** holds (every commit `%(trailers)` = `[]`).
- Untracked/modified drift (NOT the orchestrator's lane): `.codex/` + `AGENTS.md` (Codex-context mirror) + `docs/charc-state.md` (modified) — all the **concurrent harness/director arc's** artifacts; LEAVE them. Plus the `exports/research/shadow-expectancy-*` dirs.
- Merge discipline: rebase the branch onto CURRENT main → `merge --ff-only` → re-run the suite ON the merged head + READ the actual `N passed` summary line.

## 2. What this generation shipped (all SHIPPED+CLOSED, gate-passed, ff-merged)
- **18-H.4** (`4d8b92bb`, superseded) + **18-H.4.1** (`1a916375`) — Schwab web re-auth self-lock (WinError 32). 18-H.4's release-before-replace was a PARTIAL fix; the §7 codex-auto-review A/B's Reviewer B caught a real production-defeating defect review-strong's diff-only pass missed (the ladder closures retained the client by value). 18-H.4.1 = the replaceable-holder + drainable in-flight barrier. **Operator real-re-auth witnessed.**
- **18-H.2 + 18-H.3 + R1** (`e53b0886`) — web-polish: 404 renders base+stoplights via the context processor; health drill-down stoplight dots; `requests` declared in pyproject. **Operator browser gate 3/3.**
- **18-H.1** (`1a2a774d`) — Schwab token-health: configured-but-tokens-absent → YELLOW. **Operator live-witness 3/3** (green valid / yellow after logout / green after setup).
- **18-H.6** (`2817b299`) — first-class `untracked_broker_position` discrepancy + **migration 0031 (v30→v31)** + the Schwab-driven orphan pass (carve-out: schwab_reconciliation/reconciliation/models + 0031). **Both operator live-witnesses PASS** (migration + recon caught the operator's real SPCX IPO shares).
- **18-H.6.1** (`6b7db700`) — orphan attribution (3 parts: banner/count inclusion + a no-FK-safe resolver reusing `acknowledged_immaterial` [no new schema] + route the orphan to `unresolved` not the pending-ambiguity limbo). **CHARC QA-on-disk PASS + §5.10 witness 3/3.**

## 3. Disciplines banked THIS generation (durable — apply them)
- **codex-auto-review (Reviewer B) is ADOPTED as a GATING complementary second-eye on production-code arcs** (CHARC, at 18-H.4). The ORCHESTRATOR runs it: `wsl … codex exec review --base main -c model_reasoning_effort=high` on the merge-candidate tree (detached-checkout the final tip in the MAIN repo via `git -C`, run, re-attach). A B `[P1]`/major is **resolved-or-cited before merge**. It is complementary, not a replacement — finding sets are often disjoint from Reviewer A.
- **The binding review (Reviewer A) now runs WITH REPO ACCESS** — NOT diff-only (the 18-H.4 blind spot: a defect in un-changed surrounding code, e.g. closures/reference-graphs, is invisible to a diff-only review). Recipe §3 updated.
- **The `-p review-strong`-absent fallback MUST force `-c model_reasoning_effort=high`** (fixed in the recipe at `da22b9d8`; the 18-H.4.1 review accidentally ran at effort=none). **At QA, verify Reviewer A's raw transcript header shows `reasoning effort: high`** — make the implementer report it.
- **codex-auto-review WSL-CRLF phantom false-positive** (noise mode): B run via WSL against the Windows CRLF working tree can report a phantom "repository-wide line-ending rewrite" / mass-modified files. Cross-check any line-ending-churn finding against the Windows-side `git diff --numstat` before treating it as real (it was a verified-false [P2] on the web-polish arc). A `.gitattributes` normalization would kill it at the source (candidate -H item).
- **CWD-DRIFT (cost me a bogus B run + a detached-checkout snag on 18-H.4.1):** the Bash tool's cwd silently drifts into worktrees. Use `git -C <abs>` for ALL worktree git ops; for the B detached-checkout, drive it with `git -C <MAIN>` (re-attach `git -C <MAIN> checkout main` works because main owns the main worktree — running `checkout main` from inside a worktree FAILS).
- **v30→v31 live-migration window:** the operator MIGRATION witness applies the migration to the LIVE DB via the WORKTREE code (`PYTHONPATH=. python -m swing.cli db-migrate`), which puts the live DB AHEAD of main-code's `EXPECTED_SCHEMA_VERSION` → the operator's normal main-code `swing` chokes until the merge lands. Keep that window short (migrate → witness → merge promptly). Closed at merge (main = v31).
- **Schwab CLI self-lock vs a running web server:** a CLI `swing schwab logout`/`fetch`/`db-migrate` hits WinError-32 if `swing web` is holding the tokens/DB — stop the server first (the 18-H.4.1 web fix only covers the WEB `/schwab/setup` path, not external CLI processes). This is documented 18-H.4 behavior.

## 4. Phase 18 state — §18-H queue (operator-sequenced; do NOT commission until "go")
Live tracker: [`docs/phase18-todo.md`](phase18-todo.md). **18-H NEXT per the operator: 18-H.5.**
- **18-H.5 (briefed `f3b95d3e`, HOLD) — dead-code-hardening audit** (the retroactive twin of the #39 schema-boundary corollary): AUDIT-FIRST (Phase 1 read-only catalog with CITED+READ constraints, classify remove/replace/KEEP — default KEEP → CHARC+operator review → Phase 2 scoped remediation). Brief: `docs/18-H.5-dead-code-hardening-audit-dispatch-brief.md`. **The operator wants this next.**
- **18-G** (dead-brief sweep) · the **RD watch-standard amendment** (RD's own action) · **18-H.7** (nightly research-health ATTENTION → rd-push; 3 design points deferred).
- **Live-DB note:** SPCX orphans id 69 (legacy pending) + id 70 (new unresolved) remain OPEN — the operator can clear them via the normal `swing web` (18-H.6.1 banner/resolver is live on main) or by journaling the SPCX trade.

## 5. Directors + comms (unchanged)
CHARC (tool/architecture) + RD (research/measurement). POST status/return_report via `python scripts/role_mail.py post --from orchestrator --to charc[,rd] --type … [--body / --body-file]` from the MAIN repo dir. **No orchestrator inbox** — direction comes FROM the operator (who relays director rulings). The IMPLEMENTER never posts (you post post-QA). For -H web-layer/recon arcs RD does NOT merge-block (released post-18-D); CHARC owns the §3 / TRIPWIRE gate + QA-on-disk on carve-out/schema arcs.

## 6. The dispatch model (unchanged; full detail in orchestrator-context §Dispatch execution + the recipe)
Orchestrator spawns the implementer as a **sub-agent via the Agent tool** (`.claude/agents/implementer-<model>-<effort>` cells; SPOF [`docs/implementer-dispatch-recipe.md`](implementer-dispatch-recipe.md)) on the operator's "go": commit the brief to main FIRST → select the cell by TASK (announce, vetoable) → `Agent(run_in_background=true)` (NO model/isolation override — the cell makes its own `.worktrees/<name>` + carries model+effort). Await; **QA-against-disk** (diff, trailers `[]`, convergence from the REAL `.copowers-findings.md` + raw transcript [effort=high], the C1/Cx conditions, data-shapes-vs-live-code). Run gating Reviewer B. Post the QA'd return to CHARC → CHARC gate (if TRIPWIRE) → operator authorizes/witnesses → rebase + `merge --ff-only` + no-false-green merged head + ruff → close-out (status docs + worktree sweep + stray-B-file rm) → post merge confirmation to CHARC. Web/UI → operator browser gate; schema/monitor read-path → operator live-witness; pipeline-step → operator real-run gate.

## 7. The concurrent harness-scaffold arc (be aware, do NOT touch)
A SEPARATE generation/CHARC is building a generic harness scaffold (`.codex/`, `AGENTS.md`, `docs/charc-state.md`, the `docs/harness-*` briefs/plans, the `docs(harness-backlog) B-1..B-8` commits). It ADVANCES main frequently (docs-only) — so ALWAYS branch worktrees from CURRENT main HEAD + rebase before merge (every -H arc this gen rebased 1-3× onto an advancing main; all clean, no overlap with swing code). Leave its artifacts alone.

## 8. FIRST ACTION
Greet the operator; confirm the clean boundary (main `3ebbbcc8`, ~8618 green, v31, single worktree, **Phase 18 §18-H — H.1/.2/.3/.4/.4.1/.6/.6.1 + R1 all CLOSED**; remaining: **18-H.5 [next], 18-G, 18-H.7, the RD watch-standard amendment**). Announce the new generation to both directors (`role_mail … --to charc,rd --type status`). NO active dispatch, NO in-flight work. **The operator has signaled 18-H.5 is next** — await the operator's "go" before commissioning it (brief `f3b95d3e` is ready). Do NOT push origin unless asked.

*End of handoff. The entire 18-H bug/feature batch shipped this generation; the codex-auto-review A/B graduated to a gating second-eye + the binding review gained repo access (the review harness is materially stronger than at gen start).*
