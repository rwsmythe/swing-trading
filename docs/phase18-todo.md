# Phase 18 — Data-Collection Integrity (TODO)

**Opened:** 2026-06-13 (scoped with the operator; CHARC-drafted per charter §2.3, operator-approved). **Theme:** the instrument's *inputs*. Phase 17 consolidated the code; Phase 18 makes the data the tool collects/stores trustworthy and its health visible. Motivated by the RD's 2026-06-13 data-collection audit (one real defect + the monitoring gap) + the operator's request for at-a-glance health.
**Demand-side:** RD (Briefs 1+2) + operator (tool-health monitor, GUI stoplights). **Supply-side:** CHARC (write-path audit, yfinance audit, boundary hygiene).
**Status:** ACTIVE 2026-06-13 (Phase 17 closed; operator opened Phase 18). **18-A SHIPPED (`c45d8752`, 8146 green) + 18-B SHIPPED (`5cce72db`, 8153 green) 2026-06-13 + 18-C SHIPPED (`2b381461`, 8258 green, schema v29→v30) 2026-06-14 — all 3-eye-gate-passed ff-only merges (18-B first executing-via-sub-agent; 18-C first schema arc + full library round + operator-witnessed v30 migration verified); next arc (18-D/18-E monitors, or per operator sequencing) awaits operator go.** The operator sequences per-arc; architecture passes for the tripwire-crossing arcs are pre-done or owed by CHARC at commissioning.
**Scope reconciliation (operator-confirmed 2026-06-13):** the Phase-17 closeout's "riders R1 + R3 yfinance-audit waived" is CORRECT within the orchestrator's Phase-17 swimlane — they are not Phase-17 work. The cross-phase fact (directors' scope): **18-C (yfinance call audit) is a Phase-18 arc, and R1 (declare `requests`) carries into Phase 18.** No contradiction — two correct statements at different scope levels; the orchestrator's record needs NO amendment (injecting Phase-18 context into a Phase-17 closeout is the context-creep the swimlane design exists to prevent). Reconciling across phases is the director's job, not the orchestrator's.
**Baseline at open:** main `98dff618` · schema v29 · (test count confirmed at first-arc branch).

---

## Tripwire posture
Most arcs cross a §3 tripwire (new schema, new module, new standing process, base-layout change) → their commissioning briefs route through CHARC. RD's two briefs already have the architecture pass (`docs/phase18-rd-briefs-charc-architecture-pass.md`, conditions C1-C5). CHARC authors the passes for the write-path audit, the yfinance audit, the tool-health monitor, and the stoplight GUI at commissioning.

## Backfill decision (LOCKED, on record)
The temporal-log 103-NaN-row **backfill is WITHDRAWN** (operator-locked 2026-06-13; three independent reads converged — RD/Codex/CHARC — payoff ~1 unique priced name, recoverable by time, not worth eroding the anti-#26-drift reproducibility guarantee). Only the **writer fix** (Arc 18-A) proceeds. Full record + override-guards: the architecture-pass doc.

---

## Arc 18-A — Temporal-log NaN writer fix + shared finiteness predicate [RD Brief 1; HIGH]
**Brief:** `docs/temporal-log-nan-writer-fix-commissioning-brief.md` (architecture-passed; conditions C1-C3).
**Scope:** add the finiteness guard to `build_ohlc_today_json` (skip-with-warning, mirroring Arc-8), extracting a SHARED finiteness predicate (C1) into `swing/data/` that BOTH this writer and `ohlcv_archive`'s trim consume — `temporal_metadata` (pipeline) imports FROM data, never the reverse (layer rule). NO backfill, NO migration, normal cycle. The skip→`warnings_json` partially closes the going-forward observability; the historical surface is the monitor (18-D, C3 dedup).
**Tripwire:** passed. **Couples with 18-B** (produces the shared predicate 18-B audits against).
**SHIPPED 2026-06-13** → merged `main` ff-only at `c45d8752` (plan `daebb6bf`; impl `c12a50ec` predicate · `82981a6d` archive-trim→C1 · `b0f0562f` caller skip · `c6ca75f7` serializer belt-raise · `36505bbb` ASCII · `c45d8752` Codex-R1 test-lock). Shared predicate `swing/data/ohlcv_finiteness.py:is_finite_ohlc(*values)` consumed by all three sites (archive trim + serializer + caller; pipeline→data). **Decision #3 = KEEP** the serializer belt-raise (3-eye-ratified). Codex converged R2 `NO_NEW_CRITICAL_MAJOR`. **Three-eye gate passed** — orchestrator QA + RD measurement-integrity + CHARC C1-C3, each verified against the shipped diff. 8146 fast green on the merged head; ruff clean; schema v29 unchanged; trailers `[]`. The caller skip emits the structured `warnings_json` {step,ticker,observation_date,reason=`non_finite_ohlc`} for 18-D to consume; RD watch-items a/b/c satisfied. The 103 NaN rows stay withdrawn/age-out (locked).

## Arc 18-B — OHLC write-path finiteness audit & consolidation [CHARC; HIGH]
**Why:** Brief 1 is ONE instance of a recurring class — Arc-8 guarded `ohlcv_archive` but the temporal-log writer was missed (the #24-#26 two-path-divergence family). Grounded 2026-06-13: ≥3 OHLC persistence paths (archive, temporal log, candidates/pattern_forward_observations) and finiteness guards are ad-hoc across ~25 files, NOT systematic at persistence boundaries.
**Scope:** enumerate EVERY OHLC-bar persistence boundary; verify each routes through the 18-A shared predicate; close the gaps. Catches the class so it can't recur a third time. First task = the enumeration (audit-to-confirm; no pre-asserted count, per §5.7).
**Tripwire:** CHARC pass at commissioning (touches multiple write paths + the shared predicate). Sequence AFTER/with 18-A (consumes its predicate).
**SHIPPED 2026-06-13** → merged `main` ff-only at `5cce72db` (plan `c784e96a`; impl `8900d6a7` `_trim_trailing_ragged` gains a behavior-preserving `columns=` param · `5cce72db` the ladder GAP closure). The exhaustive audit (20 rows: 4 GUARDED / **1 GAP** / 15 OUT) found EXACTLY ONE gap — the Schwab market-data ladder's archive-ingest (`marketdata_ladder._persist_window_to_archive` → `write_window`) missing the Arc-8 trailing-ragged barrier its sibling legacy fetch paths carry — closed by routing through the ONE shared `is_finite_ohlc` via the generalized `_trim_trailing_ragged` (no third copy; `integrations→data`). **The #24-#26 two-path-divergence class is killed at the ingest boundary.** Three-eye gate passed (orchestrator QA + RD measurement-integrity + CHARC C1/parity/scope, all disk-verified); 8153 fast green on the merged head; ruff clean; schema v29 unchanged; trailers `[]`. **First EXECUTING-via-sub-agent dispatch (`implementer-opus-high`) — the sub-agent dispatch model proven end-to-end** (writing-plans pilot + executing). Plan: `docs/superpowers/plans/2026-06-13-phase18-arc-b-ohlc-write-path-finiteness-audit.md`.

## Arc 18-C — yfinance call audit [CHARC; deferred Arc-1c]
**Why:** confirmed on disk — NO yfinance-call audit exists (Schwab calls audited via `schwab_api_calls`; yfinance blind). Direct collection observability + feeds both monitors (18-D/18-E read it).
**Scope:** a `yfinance_calls`-style audit (timings, errors, empty-results) paralleling `schwab_api_calls`. **NEW SCHEMA (a table)** — CHARC tripwire; migration discipline (in-file BEGIN/COMMIT #9, backup gate strict-equality, migrate-twice test, #11 mirrors).
**Tripwire:** CHARC pass at commissioning. Sequence BEFORE the monitors that consume it.
**SHIPPED 2026-06-14** → merged `main` ff-only at `2b381461`; **schema v29→v30** (migration 0030 `yfinance_calls`). The audit table mirrors `schwab_api_calls` with the two design divergences (status incl. `'empty'` first-class; NO `environment` column) + the #1-approved enum extensions (`call_type='download_intraday'`, `surface='web'`) + the §9 LOCKED **FK ON DELETE SET NULL** (the plan's RESTRICT reverted by director decision). Recording mirrors `record_call_start/finish` AROUND the raw `yf.download` at all 4 sites (single/batch/labeling/intraday) via a process-global audit context (`busy_timeout=0`, never delays the fetch); always-on, no sandbox gate; light redaction. Three-eye gate (orchestrator QA + RD measurement-integrity + CHARC C1-C4 on the shipped diff, discharge `fd7c75d3`) + the operator-witnessed v30 live-migration + backup gate (independently verified: v30, integrity ok, FK SET NULL, backup taken). 8258 fast green on the merged head; ruff clean; trailers `[]`. Built via the library (writing-plans `implementer-opus-xhigh` 17-round-converged; executing `implementer-opus-high`). Plan: `docs/superpowers/plans/2026-06-14-phase18-arc-c-yfinance-call-audit.md`. Feeds 18-D/18-E.

## Arc 18-D — Research data-collection health monitor [RD Brief 2; HIGH]
**Brief:** `docs/data-collection-health-monitor-commissioning-brief.md` (architecture-passed; conditions C4-C5).
**Scope:** read-only integrity monitor (the audit mechanized) — temporal-log non-finite scan, excluded-reason breakdown, coverage gaps, orphans/look-ahead, drumbeat liveness. **Build the read-only SCRIPT first; DEFER the nightly pipeline-step half** (C4); if nightly, it MUST use the 17-B `step_guard`. **Emit structured JSON status** (for the 18-F stoplight). Reads the engine manifest (no funnel fork). RD owns the watch-standard amendment post-build.
**Tripwire:** passed (new standing process). RD-lane; RD QAs.

## Arc 18-E — Operational tool-health monitor [operator + CHARC; focused subset]
**Why:** operational signals exist piecemeal (pipeline_runs heartbeat/state, Schwab token TTL, archive/weather freshness, recon backlog, cash-coherence badge) but there's no aggregated early-warning — the same "rode invisibly" failure applies operationally (a dying 7-day token, a silently-failing best-effort step, stale data).
**Scope:** an AGGREGATING read-only roll-up of EXISTING signals — NO new instrumentation, NO new schema — same shape as `harness_probe`/`weekly_glance` (stdlib, ASCII, ATTENTION-on-threshold). **Scoped to the data-collection-ENABLING subset** (pipeline-run health, token TTL, data freshness) — these failing = no data collected, upstream of 18-D. Broader operational coverage (perf, all subsystems) is Phase 19+. **Emit structured JSON status** (for 18-F).
**Tripwire:** CHARC pass at commissioning (new standing process). Operator-facing; build via orchestrator.
**SHIPPED 2026-06-14** → merged `main` ff-only at `65de5a7f`. New `swing/monitoring/` package: `tool_health.py` with `compute_tool_health(conn, *, cfg=None, prices_cache_dir=None, now=None) -> ToolHealthStatus` (the CHARC-owned §3 JSON envelope — `monitor`/`generated_ts`/`overall`=worst-of/`checks[]`; frozenset-validated status enum; `grey` is 18-F render-only, never monitor-emitted) + a `scripts/tool_health.py` operator probe (ASCII `ATTENTION (N)` / `all clear` + `--json`; exit-1-on-attention; own `mode=ro` conn). Three check families — pipeline two-read freshness + wedged + recent-failures · Schwab token TTL reusing the `cli_schwab` severity tiers · OHLCV-mtime + weather data-freshness — all read-only, aggregate-don't-instrument, stdlib + ASCII, no new schema, no measurement-chain touch. ONE ratified touch-set extension: the additive pure `sessions_behind(reference, candidate)` NYSE helper in `swing/evaluation/dates.py` (Codex drove pandas OUT of the monitor → the canonical session-math home; CHARC-ratified; no existing function changed). **The operator live-DB witness caught a false-RED weather-check defect the byte-tests + 4 Codex rounds missed** (`get_latest(conn)` defaults to ticker `"QQQ"`, but the live system records weather under `cfg.rs.benchmark_ticker="SPY"` → false RED on a current system) — fixed at `65de5a7f` (the weather check now passes the benchmark ticker; cfg-None → n/a green) + a distinguishing SPY-only regression test (the data-shapes-vs-live-DB lesson: the operator-witness leg is what caught it). **Codex usage-cap interlude:** the post-fix Round-4 review was blocked by an account cap (reset 6/17); the implementer honestly flagged NOT-CONVERGED rather than fabricate; re-run post-cap-raise → `NO_NEW_CRITICAL_MAJOR`. **Three legs discharged:** orchestrator QA-against-disk + CHARC C-E1..C-E6 (re-discharge `94c0d547`) + operator re-witness (weather flipped false-RED → GREEN, overall all-clear, on the live DB). 8326 fast green on the merged head; ruff clean; schema v30 unchanged; trailers `[]`. Built via the library (executing `implementer-opus-high` + the weather-fix re-dispatch). Plan: `docs/superpowers/plans/2026-06-14-phase18-arc-e-tool-health-monitor-plan.md`. Emits the §3 envelope 18-F consumes (the contract 18-D conforms to).

## Arc 18-F — GUI health stoplights [operator; web; downstream consumer]
**Scope:** TWO top-row stoplights (green/yellow/red = worst-of each monitor's checks) — **tool-health (18-E)** + **research-measurement (18-D)**; harness-hygiene stays CLI-only (operator decision 2026-06-13). Top row (with date + theme toggle), visible on ALL pages, each clickable to a read-only drill-down page showing which checks flipped.
**Architecture (CHARC-settled, the tripwire content):**
- **The base-layout 500-risk is the load-bearing constraint.** "All pages" = `base.html.j2` → the documented gotcha (a base field missing from any base VM 500s unrelated routes). Bind: a single shared `health_stoplights(conn)` helper called in EVERY base-VM construction with a safe default + a regression test that every base route renders with the stoplights. (Option to weigh in the plan: a base-context consolidation so base-wide fields stop being hand-duplicated across 5 VMs — not mandated.)
- **Data sources differ (reinforces independence):** tool-health computes at render from the DB (like the cash badge — precedent); research reads 18-D's structured artifact. Separate computations.
- **Refresh:** compute-at-render to start; HTMX poll deferred.
- **NO new schema.**
**Tripwire:** CHARC pass (base-layout change). **Operator-witnessed browser gate BINDING** (all-pages HTMX/base-layout — the full web gotcha family; TestClient won't catch a base-VM 500 on an unrelated route). Sequence LAST — depends on 18-D + 18-E emitting structured status.
**SHIPPED 2026-06-15** → merged `main` ff-only at `f075e7b5`. The base-wide stoplight framework: a NEW `swing/monitoring/stoplights.py` (`Stoplight` dataclass + `health_stoplights(conn, cfg)` aggregator + two independent providers + the `RESEARCH_HEALTH_ARTIFACT_PATH` = `exports/research/health/latest.json` + `RESEARCH_MONITOR_ID` = `"research_measurement"` contract constants + the identity+staleness-gated research reader) injected into EVERY render via a **Starlette context processor** registered inside `_build_templates` (orchestrator-verified live: `context_processors=` is a standalone keyword on Starlette 1.0.0, NOT mutually-exclusive with `env=`; the per-VM-field FALLBACK was NOT taken → the every-base-VM-or-500 gotcha + the D15 ~15-VM duplication are SIDESTEPPED, not paid). TWO topbar stoplights (tool-health = 18-E `compute_tool_health` at render; research = the §3 artifact, GREY until 18-D writes it — provider-driven auto-light) + two read-only drill-downs (`/health/tool`, `/health/research`). DEFENSIVE-never-raises → grey (triply-guarded: provider→aggregator→processor), safe on the error page too. **review-strong CONVERGED (R1-R5; FOUR research false-green vectors closed — future-date, overall-vs-worst-check, full-per-check-schema, + the cfg-override consistency)**; the FIRST **codex-auto-review A/B** ran (built-in `codex exec review` on the converged diff = no escaped major, but **NOT adopted** — default effort=none, 1.37MB unstructured prose, no parseable verdict; review-strong gpt-5.5/high stays the binding gate). Three legs discharged: orchestrator QA + CHARC C-F1..C-F5 (discharge `698e44d8`) + the BINDING operator browser gate (5/5: dashboard, both drill-downs, every base route no-500-with-stoplights, the validation-400 error page renders stoplights). 8380 fast green on the merged head; ruff clean; schema v30 unchanged (NO new schema/dep); 404 handling untouched (18-H.2). Built via the library (writing-plans `opus-xhigh` review-fast; executing `opus-high` review-strong). Plan: `docs/superpowers/plans/2026-06-14-phase18-arc-f-gui-stoplights-plan.md`. Trailing: 18-H.1/.2/.3. **18-F is the last consumer; 18-D (research monitor) is the only remaining Phase-18 monitor arc.**

## Arc 18-H — OPEN bug / small-feature catch-all [operator-added at approval]
Standing container for defects + small features identified WHILE Phase 18 is worked (the 17-D pattern). Each gets its own focused dispatch (TDD; review depth proportional to blast radius — the orchestrator's call; tripwire rules apply as everywhere, so anything crossing §3 still routes through CHARC). Entries accumulate here with commit SHAs so the close audit has one place to look. Empty at open — that's the goal state at close too. (Sub-items registered as 18-H.1, 18-H.2, … as they arrive.)
- **18-H.1 (candidate; NOT dispatched) — Schwab "configured-but-tokens-absent → yellow":** 18-E shipped the ratified contract where a configured Schwab client (`client_id` set) with NO tokens DB → green/"n/a" (absence is not a tool-health failure). Codex (18-E R2) + the orchestrator flagged a plausible operational case that a configured-but-tokens-GONE state (Schwab WAS set up, tokens now missing) could warrant YELLOW instead — a dead/missing refresh token is exactly the hazard `schwab_token_ttl` exists to catch. A small `_check_schwab_token` contract tweak; CHARC product-decision. Banked 2026-06-14 (operator-directed at 18-E close); revisit if the operator wants it.
- **18-H.2 (flag-not-fix; NOT dispatched) — error-surface inconsistency (web error pages):** a validation-400 (`RequestValidationError` → `_handle_validation_error` → `page_error.html.j2`, which extends base) renders base + the 18-F stoplights, but a plain `StarletteHTTPException` 404 (`_handle_http_exc`, `swing/web/app.py:500`) falls to the minimal Starlette default (no base, no stoplights). Surfaced by Codex review-strong R1 at 18-F executing; CHARC-ruled FLAG-NOT-FIX (pre-existing error-handling architecture the arc never touched; fixing = scope expansion into the 404 path; same degraded-surface philosophy as the bare unhandled-500). Evaluate whether to UNIFY the two full-page error surfaces (route 404s through `page_error.html.j2` too). Banked 2026-06-15 (CHARC nod at 18-F executing).
- **18-H.3 (enhancement; NOT dispatched) — stoplight icon (not the word) in the health drill-down status column:** the `/health/tool` (+ `/health/research`) drill-down lists each check's status as the WORD (green/yellow/red); the operator (18-F browser gate) requested rendering the colored `.stoplight-<color>` DOT instead, for faster visual scanning — keeping the status word as the `title`/`aria-label` (a11y + test-greppable). Small change to `health_tool.html.j2`/`health_research.html.j2` + their tests + a CSS tweak; reuses the existing topbar `.stoplight-*` classes. Banked 2026-06-15 (operator-directed at the 18-F browser gate; deferred out of 18-F to keep the gated arc as-is).
- **18-H.4 (BUG, root-caused + briefed; NOT dispatched — operator deferred to the -H commissioning) — Schwab web re-auth self-lock (WinError 32):** `swing web`'s LONG-LIVED schwabdev client (`_construct_web_schwab_client`, `swing/web/app.py:191`, gated on `_is_ladder_active` = Schwab active) holds the tokens DB open (`BEGIN EXCLUSIVE`) for the whole session → `/schwab/setup`'s rename-aside (`os.replace`, `auth.py:543`) hits WinError 32; `swing web` deadlocks its OWN re-auth (consistent whenever Schwab is active). **Fix = release-before-replace** (close the `app.state` client + `del` + `gc.collect()` to free the handle → run the setup flow → reconstruct via the EXISTING `_construct_web_schwab_client` factory; L2 LOCK preserved, no new construction call site). NO tripwire (web-layer). Brief: `docs/18-H.4-schwab-setup-self-lock-dispatch-brief.md` (`d353783e`). **Operator WORKAROUND (VERIFIED 2026-06-15, reset the 7-day window): stop `swing web` → re-auth via the CLI → restart** (the lock is the running server's client; web down → nothing holds the DB). Banked 2026-06-15 (operator-reported; CHARC root-caused + briefed; HOLD dispatch until -H commissioned).
- **18-H.5 (AUDIT, briefed; NOT dispatched — operator-directed, the treadmill's retroactive twin) — "hardening into dead code" audit:** find + remove existing defensive branches that guard a state an upstream constraint VERIFIABLY prevents (schema `CHECK`/`UNIQUE`/`NOT NULL`/FK / migration / constrained writer) → unreachable dead code (its test just skips; the R11 cascade surface). The retroactive twin of the recipe §3 corollary (`94ac2905`). **AUDIT-FIRST:** Phase 1 read-only catalog (each candidate with a CITED + READ constraint; classify remove / replace-with-general / KEEP — **default KEEP**) → CHARC+operator review → Phase 2 scoped remediation (replace per-case branches with a GENERAL graceful-degrade + LOGGING; preserve the safety net, do not strip the defense). **Citation rigor is load-bearing** — never remove a non-verifiably-dead defense (that converts a real hole into a removed guard). Scoped to readers/consumers/monitors. Brief: `docs/18-H.5-dead-code-hardening-audit-dispatch-brief.md` (`f3b95d3e`). Banked 2026-06-15 (operator-directed at the 18-D treadmill; HOLD until -H commissioned).
- **CHARC-lane experiment (rides an -H review, NOT a bug-fix item): the codex-auto-review A/B re-test** — PULLED OFF 18-D + re-homed here 2026-06-15 (operator-directed; 18-D's mid-arc regime change — the #39 corollary + scope-bound introduced DURING the review — broke apples-to-apples vs the corollary-naive auto-review). Rides the first qualifying **production-code `review-strong`** arc — nominee **18-H.4** (Schwab self-lock), fallback 18-H.2; **NOT 18-G** (docs → `review-fast`; no production code → can't test `review-strong` recall). Design: BOTH reviewers single-pass on the same pre-review tree, matched HIGH effort, scored head-to-head (recall + noise + ergonomics). Runs when -H is commissioned; CHARC owns the arc pick. Charter-banked 2026-06-15 (the same-day 18-F/A-B entry).

## Boundary hygiene + riders
- **18-G — broad brief-corpus sweep (D10):** ~246 dead briefs from phases 3e-15 + research lanes beyond the 24 archived at Phase-16 close (departing P16 orchestrator flagged it). `git mv` closed-arc briefs to `docs/archive/phase<N>/`; specs/plans stay. Off-theme but phase-boundary-appropriate + overdue. Operator: include or defer.
- **R1 (D7):** declare `requests` in `[project] dependencies` — fold into the first arc touching pyproject (likely 18-C); fast suite required (inline-edit memory).
- **R2 (D9):** frozen-clock convention line in `orchestrator-context.md` — land with the first Phase-18 brief; pairs with any test-determinism work.
- *(R3 from Phase 17 — the yfinance call-audit — was PROMOTED to full arc 18-C when Phase 18 was scoped; it is no longer a rider.)*
- ~~**Conditional carry — D14** (`-n auto` web-route polluter)~~ — RESOLVED: 17-D.4 (`0867337d`) fully closed D14 (polluter healed at the source, 8132/0 ×2 verified). NO carry to Phase 18.

## Sequencing (CHARC recommendation; operator's call)
Dependency chain: **18-A+18-B** (coupled; shared predicate) · **18-C** (yfinance audit; before the monitors) → **18-D + 18-E** (monitors; emit structured JSON) → **18-F** (stoplights; consume both). 18-G + riders slot anytime. D14 conditional on 17-D.4.
Recommended order: 18-A/18-B → 18-C → 18-D / 18-E (parallel) → 18-F → 18-G. Nothing dispatches until Phase 17 closes + operator go.

## Out of scope (considered, deprioritized)
- The temporal-log backfill (withdrawn — locked above).
- Broader operational-dashboard coverage beyond the data-collection-enabling subset (Phase 19+).
- D1 runner.py infra relocation (navigability, off-theme, downgraded — 17-B took the high-value part).
- D8 (form anchor-ladder) / D12 (latest-eval guard) — trigger-gated watch items; no trigger.
