# Phase 17 — Consolidation & Parity-Drift Elimination (TODO)

**Opened:** 2026-06-12, operator-approved at the Phase 16 close (proposed by CHARC — [`docs/phase16-close-audit-charc.md`](phase16-close-audit-charc.md) §7; operator added 17-D). **Theme:** the tool is feature-complete for the current trading program and the RD lane is in measurement-awaits-market-time posture; the highest-risk open debt is drift-class, not capability-class. This phase pays it down. **Demand-side:** none anticipated from the RD at commissioning (operator, 2026-06-12); any later demand routes through the operator as usual.

**Schema:** NO migrations anticipated. Any arc that discovers a schema need crosses the §3 tripwire → CHARC architecture pass before dispatch.
**Tripwire posture:** 17-A and 17-B are each expected to introduce NEW modules under `swing/` → their commissioning briefs route through CHARC (cheap pass; CHARC co-authors the scopes, so largely by-construction).
**Baseline at open:** main 8053 fast green · schema v29 · ruff clean · zero open discrepancies · pipeline 2m20s.

---

## Phase 16 trailing items (tracked HERE — their tracker, the P16 orchestrator, was decommissioned 2026-06-12)

> Each item stays until checked off with evidence; the Phase 17 close audit sweeps this section. Verification specs live in [`docs/archive/phase16/orchestrator-handoff-2026-06-12-phase16-close.md`](archive/phase16/orchestrator-handoff-2026-06-12-phase16-close.md).

- [ ] **T16-1 TROX frozen-not-removed check** — after the NEXT nightly. Expectation (handoff, stated precisely): screen-absent + unpinned → the absent-skip contract; NO injection/suppression audit entries; streak holds at 3; removal fires only on a screen re-entry. Owner: operator or any role with DB read access (read-only query); the first Phase 17 orchestrator session inherits it if still open.
- [ ] **T16-2 Dividend-marker capture** — ORGANIC trigger: the account's first `DIVIDEND_OR_INTEREST` transaction. Action: capture the real description string(s), seed the marker frozensets, UNSKIP `tests/trades/test_schwab_cash_ingestion.py:246` (`test_dividend_marker_set_is_real_payload_sourced`) and make it assert the real-sourced markers. Until then every such transaction tier-2 flags (the safe default — a flag arriving IS the trigger firing).
- [ ] **T16-3 Monthly cadence look** — ORGANIC: when the first monthly review triggers, witness the Arc-9 monthly reference text renders. Owner: operator ritual.
- [ ] **T16-4 RD QAs at next read** — the 0026 §ADDENDUM language · Arc 9's rendered text · the measurement-universe note. Owner: Research Director.

---

## Arc 17-A — Single evaluation orchestration (D6) [HEADLINE] — SHIPPED + GATED 2026-06-12

**SHIPPED + GATED 2026-06-12** (merged `bc05958c`, pushed). One shared `swing/evaluation/orchestration.py:orchestrate_evaluation` consumed by both `eval_cmd` and `_step_evaluate`; the comment-enforced mirror is dead. Cycle: writing-plans (incl. the CHARC seam ratification + amendment) → executing; Codex converged at each phase. 14 exec commits, all trailers `[]`; main **8074 green** (no-false-green re-run on the merged head); ruff clean. The §4 Task-C checkpoint surfaced SIX divergences (4 beyond the 2 pre-grounded); operator rulings: **D1/D2 intentional** (held-union + pin-injection pipeline-only via the augmentation seam), **EQUITY/ERROR-DEDUP/EXCLUDED-CLOSE unify**, **SPY-GUARD intentional** → `EvaluationBehaviorPolicy` pruned to one field `spy_failure_mode` (CHARC C1/C2/C3 honored; ratification at [`docs/phase17-arc-a-charc-ratification.md`](phase17-arc-a-charc-ratification.md)). **Arc dividend:** ERROR-DEDUP was a latent CLI **crash** (`UNIQUE(evaluation_run_id,ticker)` IntegrityError on a blocklisted+fetch-failing ticker) — fixed by the unify, exactly the production parity-drift D6 predicted. **§6 live gate PASSED** (orchestrator-run, operator-authorized): pipeline run #102 (eval 88) vs `swing eval` #89 on the same `finviz15Jun2026.csv` → **110 rows byte-identical across all 15 columns**, P-but-not-C=0 / C-but-not-P=0 (empty augmentation this session = full identity). Per-arc detail: the plan + the executing return report (mailbox).

**The hazard:** `swing/cli.py:369-374` says it outright — `eval_cmd` hand-mirrors `_step_evaluate`'s plumbing "so standalone `swing eval` and the pipeline persist classification identically." The criterion core (`evaluate_batch`) IS shared; the orchestration around it (CSV parse, sector/industry passthrough, RS universe, SPY benchmark, OHLCV fetch, context assembly, persistence) is a comment-enforced parallel copy. This is the V1↔V2 parity-drift family (#24–#26) living in PRODUCTION: drift means `swing eval` and the nightly silently classify differently.

**Scope:** extract ONE shared orchestration path consumed by both entry points; the mirror comment dies because the mirror dies. Likely a new module (name at commissioning; e.g. `swing/evaluation/orchestration.py`) → tripwire pass at commissioning.
**Binding test posture:** characterization FIRST — a golden-parity harness pinning that both entry points produce identical persisted rows on the same inputs BEFORE the extraction, kept green through it. The parity tests must exercise the PRODUCTION derivation path, not stub fixtures (the byte-parity-insufficient gotcha).
**Gate:** one nightly + one manual `swing eval` on the same session producing identical persisted classification rows, operator-witnessed via a diff query.

## Arc 17-B — runner.py step-wrapper extraction (D1) — SHIPPED + GATED 2026-06-13

**SHIPPED + GATED 2026-06-13** (merged `5c86c254`, pushed). The ~9× best-effort step-wrapper boilerplate collapsed into one tested context manager [`swing/pipeline/step_guard.py`](../swing/pipeline/step_guard.py) (runner.py −85 net lines); zero behavior change. Cycle: writing-plans (CHARC ratified a SECOND fatal site as-is — finviz site-1; precondition resolved in-plan) → executing; Codex converged each phase. 7 exec commits trailers `[]`; ruff clean. §4 characterization matrix (16 sites: 9 wrapped BS/B + 7 explicit incl. both fatal sites, charts 3-way, shadow `run_warnings`-in-handler, complete, review_log_cadence) green pre+post. §5 byte-identical locks (#25/#27/#16/LeaseRevoked-propagation; the explicit set untouched). §6 wrapper-only (no infra relocation). §7 smoke: a real pipeline run completes. **Suite (honest):** isolation-green (31/31) + 2-of-3 `-n auto`-green (8117); the 3rd run surfaced 17 spurious failures in `test_hyp_recs_expand_route.py` — a PRE-EXISTING process-global `-n auto` pollution flakiness (the file untouched by 17-B; the no-false-green re-run caught it), proven non-deterministic (same commit → 8117 / 17-fail / 8117), tracked as **17-D.4**, NOT a 17-B regression. Per-arc detail: the plan + the executing return report (mailbox).

**Scope:** the outer `lease.step()`/try-except wrapper boilerplate repeated ~11× (runner.py L820-1065 region) becomes a decorator/context-manager; optionally relocate the non-step infrastructure (finviz CSV select L4264-4576, shadow-expectancy helpers, chart/briefing composers) into their own modules (new modules → tripwire pass). Behavior-preserving: #27 warnings semantics, fence hygiene (the #16 fetch-hoist locus), per-step timing emission, and failure-state transitions all byte-identical.
**Sequencing note:** AFTER 17-A — the extraction then wraps the new shared evaluate call rather than the old inline body (one churn pass over that region, not two).

## Arc 17-C — exports retention (D3 / P5) — CLOSED 2026-06-13 (NOT BUILT: premise already-solved)

**CLOSED as already-solved — no code shipped.** The founding premise ("exports never got retention") was FALSE. `swing/rendering/retention.py:archive_old_exports` already retains dated `exports/<YYYY-MM-DD>/` dirs: on every pipeline run ([`_step_export`, runner.py:3947-3951](../swing/pipeline/runner.py#L3947)) it zips each dir older than `cfg.export.retention_days` (90; swing.config.toml:105) into `exports/archive/<YYYY-MM>.zip`, then `shutil.rmtree`s it. **D3/P5 = resolved-by-pre-existing-mechanism.** The proposed `swing exports cleanup` (hard-delete dated dirs >N days) would be REDUNDANT at 90d (the archiver already cleared them → zero candidates in steady state) and DATA-UNSAFE at shorter windows (it hard-deletes what the archiver zip-PRESERVES). Caught at the writing-plans phase — the implementer grounded the brief against live code (Expansion #2 brief-vs-actual). The orchestrator's commissioning brief + pre-dispatch investigation MISSED `archive_old_exports` (it lives in `swing/rendering/`, not the `swing/pipeline/` region grepped; the investigation only surfaced the adjacent `_prune_shadow_expectancy_artifacts`). Owned; lesson banked ([`orchestrator-context.md`](orchestrator-context.md) §Lessons + memory `feedback_brief_premise_check_existing_mechanism`).

**Deferred trivia (NOT an arc; on record):** `archive_old_exports` zips but never prunes the `exports/archive/*.zip` files — they accumulate ~1 small zip/month. Currently EMPTY (oldest dated dir 2026-04-20 < 90d; first zip ~2026-07-19). Revisit only if the zips ever become material; not worth commissioning now.

**Artifacts:** the writing-plans plan (`docs/superpowers/plans/2026-06-13-phase17-arc-c-exports-retention.md`) was DISCARDED (never executed; superseded by this close). The commissioning brief ([`docs/phase17-arc-c-exports-retention-commissioning-brief.md`](phase17-arc-c-exports-retention-commissioning-brief.md)) carries a CLOSED banner. Codex had converged R3 (`NO_NEW_CRITICAL_MAJOR`) on the plan before the premise issue dispositioned the arc; the plan + `.copowers-findings.md` were a sound product of a wrong premise.

## Arc 17-D — OPEN bug-fix container (operator-added at commissioning)

Standing arc for defects found between Phase 17 commissioning and closeout. Each fix gets its own focused dispatch (TDD; review depth proportional to blast radius — the orchestrator's call, tripwire rules apply as everywhere); entries accumulate here with commit SHAs so the close audit has one place to look. Empty at open — that's the goal state too.

**DISPATCH POSTURE (operator, 2026-06-13):** 17-D items are REGISTERED here as found (the durable-landing convention continues) but are **NOT dispatched piecemeal as they arrive** — they are batched and dispatched together when the operator **activates the 17-D arc**. Orchestrator registers + drafts the fix brief on report; it holds the dispatch until arc activation.

**ARC ACTIVATED 2026-06-13** (operator: "let us move forward with 17-D"; CHARC concurred on 17-C close + took recurrence-prevention action). Plan: **quick wins first** — .3 (LeaseRevoked one-liner), .5 (weather chart), .2 (comms dark mode) briefed + dispatched as focused fixes; **.4 (the `-n auto` polluter hunt, investigation-first) follows** once the quick wins land. Briefs: [.3](phase17-17d3-review-log-cadence-leaserevoked-fix-brief.md) · [.5](phase17-17d5-weather-chart-declutter-fix-brief.md) · [.2](phase17-17d2-comms-ui-dark-mode-brief.md).

### 17-D.1 — CLOSED (not a defect): `swing eval` displacement is already structurally closed in both reader surfaces

Surfaced 2026-06-12 at the Arc 17-A Task-C divergence sitting (a side-find of the D6 parity work, not 17-A scope). With the operator's rulings **D1 (held-ticker union) and D2 (Arc-7 pin injection) = INTENTIONAL pipeline-only**, an ad-hoc `swing eval` persists an `evaluation_run` *without* the held-union or pin-injection enrichment. If such a run can become "latest" and feed the dashboard / watchlist "latest `evaluation_run`" reads, an interactive `swing eval` could transiently displace the nightly's enriched run (stale held closes; pins not refreshed). **Pre-existing behavior, NOT introduced by 17-A.** Open question for triage before any fix is scoped: should `swing eval` runs be "latest"-eligible, or marked non-displacing (e.g. a run source/kind flag the "latest" query filters on)? Operator-directed landing 2026-06-12; fyi sent to CHARC (who may reclassify or veto — "let CHARC say no").

**CLOSED 2026-06-12 — CHARC veto, orchestrator-verified on disk (no fix needed).** A re-discovery of a concern adversarial review already caught and closed (Round 3 Major 1). Both reader surfaces structurally prevent a standalone `swing eval` from displacing the nightly's enriched run in steady state: (1) the dashboard recommendations + CLI `trade entry --hypothesis` pre-fill route through `latest_evaluation_run_id()` ([dashboard.py:139](../swing/web/view_models/dashboard.py#L139)) — a two-step select where the most-recent COMPLETE pipeline_run's eval ALWAYS wins; the standalone-eval fallback fires ONLY when no pipeline-bound eval exists (legacy NULL-FK / fresh install). (2) the watchlist criteria/chart binding ([watchlist.py:359](../swing/web/view_models/watchlist.py#L359)) anchors on `latest_completed_pipeline_run()` and DELIBERATELY leaves `eval_run_id=None` rather than falling back to latest-eval on linkage failure — the [:372-380 comment](../swing/web/view_models/watchlist.py#L372-L380) documents this exact concern + the Round-3-Major-1 closure; the raw latest-eval read at :395 is the fresh-install/no-pipeline branch only. (3) the sector/industry passthrough read (candidates.py:196) reads CSV-identical fields, unaffected by the D1/D2 enrichment. Verified against the live tree 2026-06-12. No fix, no run-kind flag, no "latest"-query filter needed; not to be raised a fourth time. **Adjacent register watch item (CHARC's register, NOT a 17-D item):** the invariant is enforced PER-READER (the dashboard two-step + the watchlist binding are separate mechanisms); a future THIRD "latest eval" reader added without one of these guarded paths would re-expose it — CHARC logged it as a watch item, not commissioning anything.

### 17-D.2 — Dark mode for the comms GUI (enhancement, not a defect) — MERGED 2026-06-13 (`7b7dc169`..`991430c8`); operator visual gate PENDING

**MERGED 2026-06-13** (ff-merge of branch `17d2`, pushed). 2 commits trailers `[]`, `scripts/comms_ui.py` + its test ONLY: CSS custom properties on `:root` + a `:root[data-theme="dark"]` override + `color-scheme` (so native form controls theme too) + a header toggle button + an early no-flash `<head>` script (localStorage `comms-theme`; `removeItem` on light so "no stored value == light" stays canonical). Locks verified on disk: no new route, no new dependency, no `swing/` touched, L1–L5 + OriginGuard unchanged, theme survives the 5s pane polls (vars on `:root`). 6 TDD tests added; merged-head fast suite 8123 passed / 0 failed; ruff clean. Codex R1 (2 major resolved — `color-scheme` + clear-on-light; 1 accepted: test edits are brief-mandated TDD) → R2 NO_NEW_CRITICAL_MAJOR. **STILL OWED: the operator-witnessed visual gate** (`python scripts/comms_ui.py` → toggle, reload-persist, poll-survives, accent legibility) — binding before this is fully closed.

Operator-requested 2026-06-12. Add a dark-mode theme to the comms mail UI (`scripts/comms_ui.py` — the on-demand localhost FastAPI/HTMX view at 127.0.0.1:8765). Scope at dispatch: a CSS dark theme + a toggle (client-side-persisted); NO new dependency. `scripts/comms_ui.py` is CHARC-custody harness tooling, but this crosses no §3 tripwire (existing script; no new module/schema/dependency/standing process) so it dispatches without an architecture gate.

### 17-D.3 — `review_log_cadence` swallows `LeaseRevokedError` (latent bug — CHARC-found 2026-06-13) — SHIPPED + MERGED 2026-06-13 (`45d9feae`..`aab2c5e4`)

**SHIPPED + MERGED 2026-06-13** (ff-merge of branch `17d3`; HEAD `aab2c5e4`, pushed). 3 commits, trailers `[]`: the runner re-raise ([runner.py:1018](../swing/pipeline/runner.py#L1018), mirrors the shadow_expectancy block) + the coupled characterization flip (`Site(review_log_cadence).revoke_propagates` False→True; the data-driven `test_planted_lease_revoked_propagation` now asserts `force_cleared` for it; the `else` branch retained as a future-contract slot) + a stale `step_guard.py` docstring fix (it had described review_log_cadence as deliberately swallowing revoke). Codex R1 (3 minor, resolved) → R2 NO_NEW_CRITICAL_MAJOR. QA'd against disk; merged-head fast suite = 8114 passed / 3 failed where the 3 are the **17-D.4 `-n auto` pollution** (see below — `test_schwab_setup_route`, pass 23/23 in `-n 0`), NOT a 17-D.3 regression. Brief: [phase17-17d3-...](phase17-17d3-review-log-cadence-leaserevoked-fix-brief.md).

CHARC-found during the 17-B architecture pass; orchestrator-verified on disk (`runner.py:1063-1070`, the `lease.step("complete")` block): `_step_review_log_cadence(lease=lease)` is wrapped in a **bare `except Exception: log.warning(…continuing…)`** with **no** preceding `except LeaseRevokedError: raise`. Every other guarded step re-raises revoke first; this one does not → a `LeaseRevokedError` raised at the cadence step is **swallowed** and the run proceeds to `lease.release(state="complete")`. Likely an oversight (the comment justifies swallowing ordinary auxiliary failures but is silent on revoke), not deliberate; impact NARROW (the last auxiliary step before completion). **Fix:** add `except LeaseRevokedError: raise` before the bare `except Exception` — one line; make it behave like every other step. **Cross-arc coupling (governs the order):** 17-B's Phase-0 characterization PINS `review_log_cadence` as the one site where revoke is swallowed (`test_step_failure_characterization` planted-revoke test asserts `revoke_propagates=False` for this site). The 17-D.3 fix MUST flip that assertion to `revoke_propagates=True` in the same change — so it lands AFTER 17-B (or updates 17-B's characterization test as part of the fix). Operator-relayed from CHARC 2026-06-13.

### 17-D.4 — Pre-existing `-n auto` test-pollution flakiness (process-global polluter → `test_hyp_recs_expand_route`) — QUEUED (investigation-first; dispatched after the quick-wins batch)

Surfaced 2026-06-13 by the 17-B no-false-green re-run, proven **NON-DETERMINISTIC**: same commit `5c86c254`, three `-n auto` runs → 8117 passed / **17 failed** / 8117 passed; the failing file passes **31/31 in isolation** (`-n 0`). **NOT caused by 17-B** — `test_hyp_recs_expand_route.py` was last touched `dd31227d` (pre-17-B), and 17-B is a pipeline refactor that cannot deterministically break web route tests; 17-B's +43 tests shifted the xdist worker distribution and SURFACED a pre-existing latent polluter. The file is temp-DB-isolated (`seeded_db`) + per-test `monkeypatch`, so the corruption is a **process-global** mutated by some co-worker test and not restored (candidates: the schwab logging `setLogRecordFactory` / the `install_logging` composition root / a module-level singleton), affecting the test's fills/current-balance/hypothesis-recs reads when xdist co-locates them on a worker. The documented "`-n auto` perturbs worker balance, surfaces latent instances" family (CLAUDE.md §Windows/tooling). **Fix:** identify the polluter (reproduce with `-n 0` + explicit ordering, NOT by re-running `-n auto`) + make it hermetic, OR make `test_hyp_recs_expand_route` resilient to it. Operator chose **path A** 2026-06-13 (close 17-B as a correct refactor; track + hunt this separately).

**BROADER THAN ONE FILE (observed 2026-06-13 at the 17-D.3 merge):** the same `-n auto` pollution failed **3 tests in `tests/web/test_routes/test_schwab_setup_route.py`** on the 17-D.3 merged-head full-suite run (8114 passed / 3 failed) — all of which pass **23/23 in isolation** (`-n 0`). 17-D.3 is pipeline-only and cannot touch a Schwab OAuth web route, so this is the SAME process-global polluter surfacing on a different worker arrangement (not a 17-D.3 regression). So the victim set is "web route tests that read the corrupted global," NOT just `test_hyp_recs_expand_route`; the hunt should look for the process-global mutated-and-not-restored (the schwab `setLogRecordFactory` / `install_logging` composition root / a module-level singleton are the standing candidates) and the fix must restore it. **Reinforces 17-D.4's priority** — it makes every parallel dispatch's full-suite QA noisy (each merge must isolation-confirm the failures). Operator sequencing still quick-wins-first; 17-D.4 follows.

### 17-D.5 — Weather mini-chart polish: axis-label declutter + 20MA line (operator-reported 2026-06-13) — ACTIVE (quick-wins batch)

Operator screenshot: the `render_market_weather_svg` mini-chart (400x150, web dashboard "Market weather (SP500 daily)") has its right-side axis labels overlapping the rotated x-axis date labels in the cramped figure — the mplfinance DEFAULT price ylabel **"Price"** (top-right) and the volume panel ylabel **"Volume  $10^{6}$"** (bottom-right; the `$10^{6}$` is mpf's auto scale-factor suffix, part of the ylabel STRING, NOT a separate offset-text). **Operator-specified fix (FIX-DIRECT, known mechanism):** remove "Price", "Volume", and "10^6" from the weather chart ONLY; leave everything else (candles, MA line, trend badge, price ticks, volume bars, dates). Mechanism: `render_market_weather_svg` ([swing/web/charts.py:827](../swing/web/charts.py#L827)) sets NO ylabels — they're the mpf defaults from the shared `_render_candles_fig` builder, which already resolves+returns `vol_ax` (so clearing labels post-return is safe; the builder's internal `_resolve_volume_ax` already ran). Clear them post-builder-return: `price_ax.set_ylabel("")` + (None-guarded) `vol_ax.set_ylabel("")` (the latter drops BOTH "Volume" and the "$10^{6}$" suffix in one call). **Scope LOCK:** weather renderer ONLY — `render_ticker_detail_svg` + `render_position_detail_svg` explicitly set "Price (USD)"/"Volume" and those are PRESERVED (guarded by `test_render_ticker_detail_preserves_volume_ylabel`). **Test LOCK:** the regression test MUST exercise the PRODUCTION `render_market_weather_svg` path (e.g. spy on `_svg_bytes_from_fig` to capture the fig before close), NOT rebuild via `_render_candles_fig` directly — a builder-rebuild test bypasses the fix and FALSE-PASSES (the byte-parity-bypasses-production-path gotcha). **Plus (operator 2026-06-13): add the 20-day MA line** to the weather chart — change `ma_windows=(50, 200)` → `(20, 50, 200)` in `render_market_weather_svg`. The `_MA_COLORS` palette already carries a 20 entry (`render_ticker_detail_svg` uses 10/20/50/150/200), and `_render_candles_fig` skips windows longer than the series, so this is a one-line addition + a test asserting the 20MA addplot is drawn. Operator-witnessed visual gate is BINDING (chart text). Brief: [`docs/phase17-17d5-weather-chart-declutter-fix-brief.md`](phase17-17d5-weather-chart-declutter-fix-brief.md). SHIPPED: _(pending — dispatch held until the 17-D arc is activated)_.

## Riders (fold-ins, not arcs)

- **R1 (P4/D7):** declare `requests` in `[project] dependencies` — fold into the FIRST arc that touches pyproject; fast suite required on the change (the inline-edit memory).
- **R2 (P6/D9):** one frozen-clock convention line in `docs/orchestrator-context.md` (NEW tests touching dates use a frozen-clock fixture; no retrofit). Orchestrator-lane content; land with the first Phase 17 brief.
- **R3 (optional, on explicit operator want only):** the Arc-1c yfinance call-audit deferred from Phase 16.

## Sequencing (operator's call; CHARC recommendation)

**17-A → 17-B → 17-C**, riders per trigger, 17-D as-needed. 17-A first while the phase is young — it is the risk item and the reason the phase exists; 17-B second so the wrapper extraction happens once over the post-17-A shape; 17-C is disjoint filler for any gap. **UPDATE 2026-06-13:** 17-A + 17-B SHIPPED; **17-C CLOSED as already-solved** (not built). Remaining: 17-D items (registered + briefed, dispatch HELD until the operator activates the 17-D arc) + the riders. With 17-C closed, the next active work is whatever the operator commissions next — likely the 17-D arc activation.
