# Orchestrator Handoff — 2026-06-08 (Phase 15 CLOSED → Phase 16 OPEN: Observability & Logging)

**Audience:** A fresh Claude Code instance taking the ORCHESTRATOR role for Swing Trading (`c:\Users\rwsmy\swing-trading`). You drive the copowers cycle + QA + merge + housekeeping; you do NOT implement. **Read the durable role framing in `docs/orchestrator-context.md` + the prior handoffs `docs/orchestrator-handoff-2026-06-07-phase15.md` (and -06-04/05)** for the disciplines, the WSL-Codex form, and the merge mechanics. This doc is the Phase-15-close → Phase-16-open delta.

## 1. Clean boundary (verify: `git log --oneline -8` + `git status -sb` + `git worktree list`)
- **main HEAD ≈ `b86cc289`** (advances via the operator's parallel research commits — re-verify; origin in sync, pushes authorized at QA-pass throughout). Working tree clean; only the `main` worktree.
- **~7268 fast tests green** on main; **schema v24**; ZERO `Co-Authored-By` (~720+ commits); ruff clean; busy_timeout 30000.
- 3 known xdist co-residency flakes (`test_ohlcv_reader_re_export_identity`, `test_read_cohort_csv_against_committed_v2trf`, `test_prices_refresh_uses_pipeline_eval_anchor`) pass `-n0`.
- **The operator commits research-data/docs to main frequently in parallel** (the Minervini exemplar-recall arc etc.) — expect main to have advanced since any branch point. **Divergence discipline: rebase the branch onto main, then `merge --ff-only`** (happened on essentially every merge in the 2026-06-08 session). History stays linear.

## 2. Phase 15 — CLOSED (what shipped; full per-arc records in `docs/phase3e-todo.md` §A + `#20`-`#23`)
Schwab-integration-hardening + operational-correctness phase. All arcs SHIPPED+CLOSED: schwabdev v2.5.1→3.0.5 + Fernet (`#20`, first L2-LOCK re-anchor) · B-7 failure-mode (`#21`, the v24 migration) · PGT-redesign + nav-date (`#22`) · pattern-observation pool-widening (`#23`) · and the **data-integrity arc** with its full descendant chain — regular-session/completed-day (`b237412b`) → the `database is locked` deadlock fix (`ffb5fdc6` + `4f0b4010`) → bad-bar accept-and-document → **daily-management #16 fetch-hoist** (`42c15caa`) → **Issue #3 `_count_open_at_run`** (`7bd51750`) → **Gate-4 quote-cassette recorder** (`af36157d`) → **Slice-B OQ-3 A-lite quote-mapper fix** (`a74a41a2`) → **Gate 4 PASSED + DATA-INTEGRITY ARC FULLY CLOSED** (cassette `56e14988`; Schwab now LIVE for quotes). **The mandate (the 2026-06-07 handoff's top-3 non-research bullets) is fully discharged.**

Phase-15 disciplines added in-session (in CLAUDE.md §Gotchas/Schwab): the Schwab `regularMarket*`-lives-in-the-`regular`-sub-block fact + the synthetic-quote-fixture-vs-real-emitter drift (a dead code path the live recorder caught).

## 3. Phase 16 — the active phase (Observability & Logging). THE LIVE LIST IS [`docs/phase16-todo.md`](phase16-todo.md)
Do NOT treat the arc list as fixed — **`docs/phase16-todo.md` is the authoritative, GROWING todo** (the operator is adding items; more may land before you commission anything). As of 2026-06-08 it holds:
- **Arc 1 — pipeline-run observability** (highest-leverage, smallest): the web subprocess runs `stdout/stderr=DEVNULL` with no `pipeline.log`, and there's no per-step duration — so ~570s of a ~10-min Run #96 was unloggable. Fix the DEVNULL → `pipeline.log` (+ Schwab redaction) + per-step timing. Prerequisite to a perf fix for the #23-widened yfinance fetch load.
- **Arc 2 — general logging overhaul**: centralized `configure_logging` (web/CLI/pipeline) + a config level knob (none today) + rotation **retention** (225MB logs dir; 83-97MB rotated web.logs never cleaned) + redaction coverage + web↔subprocess correlation.
- **Arc 3 — XMAX watchlist-thumbnail bug**: sparse thumbnail vs rich detail chart, SAME run/day → a live Shape-A parallel-archive truncation (the 15-row `XMAX.schwab_api.parquet` shadows the 1260-row legacy archive in the thumbnail's `ohlcv_cache.get_or_fetch` path).
- **Arc 4 — account-equity reconciliation**: the ACCOUNT tile = journal-computed `current_equity` (NOT the Schwab snapshot); it drifted from the broker because a recurring monthly deposit went unrecorded. **4a DONE** (recorded the 5/28 deposit; tile reconciles to ~$2027). **4b/4c remain**: wire the EXISTING `cash_movement_mismatch` reconciliation + Schwab-cash ingestion to run/surface (so the ledger can't silently drift), + the cash-vs-NLV `kind` discriminator (the one schema-touching item) + cash_movements date-format normalization.

Sequencing is the operator's call (see phase16-todo §Sequencing). Arc 1 is the cleanest first commission (a focused executing-with-Codex); Arc 4 carries a schema touch.

## 4. CLOSEOUT TASK FOR YOU (do this FIRST, with full context — deferred from the 14%-context session)
**Re-compact CLAUDE.md line-3** (the phase rollover). It still leads with "PHASE 14 CLOSED (2026-06-06) at schema v23" and carries the full interwoven Phase-15 saga — bloated + stale. Replace line-3 with this drafted pointer-summary (verify the SHAs/counts against `git log` + the suite before committing; this is a docs-only suite-exempt edit):

> **Current state (2026-06-08):** **PHASE 15 CLOSED** at **schema v24** — Schwab-integration-hardening + operational-correctness (NOT ruleset-deployment, per the 2026-05-27 applied-research arc closure). ALL arcs SHIPPED+CLOSED: schwabdev v2.5.1→3.0.5 + Fernet (`#20`; first L2-LOCK re-anchor) · B-7 failure-mode (`#21`; v24 migration 0024) · PGT-redesign + reviews nav-date (`#22`) · pattern-observation pool-widening aplus→aplus+watch (`#23`) · the **data-integrity arc** (regular-session/completed-day `b237412b` + the `database is locked` deadlock fix `ffb5fdc6`+`4f0b4010` + bad-bar accept + the daily-mgmt #16 fetch-hoist `42c15caa` + Issue #3 `_count_open_at_run` `7bd51750` + the Gate-4 quote-cassette recorder `af36157d` + the Slice-B A-lite quote-mapper fix `a74a41a2`) — **FULLY CLOSED 2026-06-08** (Gate 4 passed, cassette `56e14988`, Schwab live for quotes). ~7268 fast tests green; ruff clean; production ZERO open discrepancies. **Canonical live state: `git log --oneline -20` + [`docs/phase3e-todo.md`](docs/phase3e-todo.md) §A + `#20`-`#23`** (full per-arc history). Cumulative code gotchas: §Gotchas below; process/brief disciplines: [`docs/orchestrator-context.md`](docs/orchestrator-context.md). **PHASE 16 (IN PROGRESS — Observability & Logging):** [`docs/phase16-todo.md`](docs/phase16-todo.md) is the LIVE list (Arc 1 pipeline observability · Arc 2 logging overhaul · Arc 3 XMAX thumbnail bug · Arc 4 account-equity reconciliation [4a done]; the operator is still adding items). **Streaks to preserve:** ~720+ commits ZERO `Co-Authored-By`; **Schema v24** (B-7 migration 0024 = nullable `trades.failure_mode`; v23 at SB3 `edd098d`); **L2 LOCK RE-ANCHORED ONCE** (schwabdev v3; `docs/schwab-v3-endpoint-diff.md`).

*(The Phase-15-detail collapse is the same move the Phase-14 close made; the per-arc detail it removes already lives in `phase3e-todo.md`. The `*CLAUDE.md restructured…*` italic note below line-3 should get a "Line-3 re-compacted at Phase-15 close 2026-06-08" addendum.)* Also re-confirm the **"Current baseline"** bullet in §Invariants reads ~7268 / v24 / Phase-16-active (it was updated in-session — verify it's coherent). **orchestrator-context.md was already refreshed this session** (the in-flight section now says Phase 15 CLOSED / Phase 16 active).

## 5. Binding disciplines (unchanged; full detail in the 2026-06-04/05/07 handoffs + orchestrator-context.md)
Merge is an orchestrator action at QA-pass/convergence (don't ask). **QA every product against disk + AWAIT the operator's return signal before QA.** NEVER false-green (re-run the fast suite ON the merged head, read the actual count; isolate the 3 known flakes; docs-only merges are suite-exempt — state it; config/code inline commits are NOT). copowers Codex runs to convergence via the WSL prefix+stdin form (`wsl.exe bash -ilc 'export PATH="$HOME/.local/node22/bin:$PATH"; codex …'`; liveness `codex --version`→`codex-cli 0.135.0`; MCP dead); implementer persists prompts AND responses to `.copowers-findings.md`; pre-generate the diff on Windows + tell Codex not to run git. Operator gates are binding + witness the UNSEEDED default. NO Co-Authored-By / NO --no-verify / final `-m` paragraph plain prose / verify `%(trailers)` is `[]` before every push. Commit the dispatch brief BEFORE the inline prompt + always provide a paste-ready inline prompt. **Divergence (operator commits land mid-flight): rebase the branch onto main then `merge --ff-only`.** For a tiny, fully-scoped fix, folding writing-plans into a focused executing-with-Codex is sanctioned (Issue #3, the recorder, A-lite all did this). The operator paces deliberately; "pause" = stop.

## 6. READ FIRST
1. `CLAUDE.md` line-3 (re-compact it per §4 first) + §Gotchas (esp. yfinance partial-bar/session-anchor, the SQLite/transaction families, the Schwab `regular`-sub-block + synthetic-fixture-drift entries) + §Invariants.
2. [`docs/phase16-todo.md`](phase16-todo.md) — the live Phase-16 list (Arcs 1-4 + whatever the operator has added).
3. `docs/phase3e-todo.md` §A — Phase-15 per-arc records + the banked items (reconciliation trade-field allowlist; OhlcvBar bad-bar accept; the Schwab since-inception statement ingestion).
4. `docs/orchestrator-context.md` (durable framing) + the `memory/` directory (esp. `[[project_recurring_monthly_deposit]]`, `[[project_capital_risk_floor]]`, `[[feedback_no_false_green_claim]]`, `[[feedback_await_return_before_qa]]`, `[[feedback_codex_round_limit_suspended]]`, `[[feedback_wsl_native_codex_invocation]]`).

## 7. FIRST ACTION
Stand by. Greet the operator; confirm the clean boundary (main HEAD, origin in sync, ~7268 green, v24, Phase 15 closed). **Do closeout-task §4 (the CLAUDE.md line-3 re-compaction) as your first action** (docs-only; commit + push). Then state Phase 16 is open with the live list at `docs/phase16-todo.md`, and ask the operator which arc to commission first (Arc 1 is the cleanest start; Arc 4b/4c the account-reconciliation follow-through) — do NOT commission unprompted beyond the operator's go.

*End of handoff. Phase 15 fully closed (the data-integrity arc + its 6-descendant chain landed, Gate 4 passed, Schwab live for quotes). Phase 16 (Observability & Logging) is open and growing in `docs/phase16-todo.md`.*
