# Orchestrator Context — Persistent Handoff File

**Audience:** Future orchestrator-role Claude sessions for the Swing Trading project. Also useful as a reference when the current orchestrator's context window is compacted.
**Purpose:** Provide enough context to bootstrap an orchestrator role without re-reading conversation history. Not a complete project spec — pointers to authoritative sources are throughout.
**Last updated:** 2026-05-10 (Polish bundle 2026-05-10 SHIPPED at `44ac760` — 3-item bundle: 3e.4 hyp-rec current price line + 3e.7 entry-form example asides extended to all 8 textareas with HTML5 native `<details>`/`<summary>` collapsibility + Ruff N818 mechanical 8-class rename. 9 commits = 5 task-impl + 2 Codex-fix + 1 operator-gate I1 + 1 merge. Codex R1 0/3/2 → R2 0/0/2 NO_NEW_CRITICAL_MAJOR (2 rounds). Operator-witnessed gate: 6 surfaces; S1+S4+S5+S6 PASS; S2+S3 SKIPPED-with-test-coverage. Operator-gate I1 caught two brief-author errors mid-verification: (a) undercount of textareas in Pre-trade thesis fieldset (locked '5' but actual is 8) + (b) operator-iterated reversal of "visible always" lock to "default collapsed, individually expandable." Test count 2121 → 2140 (+19); ruff baseline 26 → 18 (8 N818 cleared; 18 E501 remaining). Adversarial review correctly delegated to implementer per orchestrator-context "Executing-plans dispatch convention" 2026-05-02 (initial brief had this backwards — orchestrator-runs-Codex; corrected via brief revision `596420e`). Previous: Polish bundle 2026-05-09 SHIPPED at `b4bb9dd` — 5-item bundle: 3e.5 daily-mgmt logged-today badge + 3e.6 auto-return to dashboard + 3e.11 strip Phase/Tranche from CLI help + 3e.13 top-nav Reviews link + 3e.14 cadence card "Complete review" inline link. 16 commits = 13 task-impl + 3 adversarial-fix; 3 Codex rounds → NO_NEW_CRITICAL_MAJOR; 2099 → 2121 fast tests (+22); ruff baseline 78 preserved. Mid-dispatch implementer caught a brief-locked predicate error — `action_session_for_run` vs writer's `last_completed_session` — and corrected inline; new round-trip integration test pins read/write alignment. New CLAUDE.md gotcha promoted: session-anchor read/write mismatch family (third instance with weather + this).).

---

## How to use this file

If you are a fresh orchestrator session: read this file end-to-end before engaging with the developer. Then check `git log --oneline -20` to see what's landed recently, then check `git status` to see what's untracked or modified.

If you are the same orchestrator post-compaction: skim the **Currently in-flight work** and **Recent decisions** sections to recover state, then continue.

This file is project-specific and lives in the repo. Update it (small commits) whenever you make a meaningful framing decision, capture a new operating process, or accumulate a lesson worth carrying forward. Avoid bloat — pointers to authoritative documents beat duplicated content. Older content migrates to `docs/orchestrator-context-archive.md` per §"Maintenance: retention discipline" below.

---

## Role and operating pattern

You are the **orchestrator instance** for the Swing Trading project. Your collaborator is the developer (Reid Smythe). The actual implementation work is done by **separate Claude Code implementer instances** that the developer dispatches with paste-ready prompts you provide.

**The pattern is:**

1. Developer raises a need (bug, strategic question, feature, study).
2. You and the developer discuss tradeoffs and decide on scope.
3. You draft a comprehensive **dispatch brief** as a Markdown file in `docs/`.
4. You produce a paste-ready **initial prompt** that points the implementer at the brief.
5. Developer dispatches a fresh Claude Code instance with that prompt.
6. Implementer executes the brief (TDD, adversarial review, etc.) and returns a structured **return report**.
7. Developer relays the return report to you.
8. You triage the return: validate decisions, flag follow-ups, capture lessons, propose next moves.
9. Loop.

**Key principle: the developer drives, you serve.** They set goals, methodology choices, gating decisions, scope boundaries. You provide recommendations, surface tradeoffs, draft artifacts, capture decisions in durable form. You do NOT decide on the developer's behalf when they haven't yet decided. When in doubt, present options with your recommendation and ask.

This principal-agent framing is captured in detail at `reference/Future Work/QuantEcon/2026-04-24-quant-econ-companion-ai-inference-benchmark.md` §"AI's role in this project — operator drives, agent serves."

---

## Governing strategy (binding)

Three documents govern. Read these before engaging on strategic questions.

1. **`reference/Future Work/2026-04-23-bifurcated-strategic-implementation-proposal-v2.1.md`** — the V2.1 governing strategy. Bifurcated architecture (Operational + Research-and-Verification), minimum-viable governance, bootstrap-first data, tranche sequencing.
2. **`reference/Future Work/2026-04-23-rebuttal-response-for-implementors.md`** — binding clarifications on V2.1. Anti-patterns list (strategy inflation, registry maximalism, infrastructure displacement, parity absolutism, bootstrap drift, priority flattening, document worship) is binding.
3. **`CLAUDE.md`** at repo root — current-state context, project conventions, gotchas. Auto-loaded by Claude Code on session start.

Forward-looking strategic content (deferred until V2.1 tranches mature):

- `reference/Future Work/QuantEcon/2026-04-24-quant-econ-future-research-program.md` — quant-econ integration program (Themes 1–4).
- `reference/Future Work/QuantEcon/2026-04-24-quant-econ-companion-trigger-purpose-three-branch.md` — Path A vs Path B framing + three-branch architecture refinement.
- `reference/Future Work/QuantEcon/2026-04-24-quant-econ-companion-ai-inference-benchmark.md` — AI-era crowding metric + operator-drives framing.
- `reference/Future Work/QuantEcon/external-references.md` — pointer file for external resources.

---

## Three-branch architecture (key refinement to V2.1)

V2.1 specifies two branches: Operational + Research-and-Verification. A 2026-04-24 refinement distinguishes two sub-branches within Research:

| Branch | Activity | Per-study horizon | Canonical example |
|---|---|---|---|
| **Operational** | Daily decision-making with promoted methods | Continuous | The `swing/` codebase |
| **Applied Research** | Testing rules developed elsewhere | Weeks–months per study | Earnings-proximity-exclusion (Sessions 2a/b/c) |
| **Basic Research** | Discovering/refining rules at first principles | Months–years per investigation | QuantEcon program (Themes 1–4) |

**Branches are permanent capability, not time-bounded projects.** Idle ≠ failure ≠ dismantle. Branch infrastructure (harness scaffolding, method-record format, cache directories, study templates) persists across idle periods. Time budget is allocated dynamically — 100% to whichever branch has an active study/investigation, idle when none.

Promotion path: Basic Research → Applied Research → Operational.

Full detail: `reference/Future Work/QuantEcon/2026-04-24-quant-econ-companion-trigger-purpose-three-branch.md`.

---

## Currently in-flight work

> **This section decays fastest. Update on every meaningful change.**

**As of 2026-05-17 PM (Phase 12.5 #1 BRAINSTORM SHIPPED + merged at `a1582c0`; 7 Codex rounds NO_NEW_CRITICAL_MAJOR; ZERO ACCEPT-WITH-RATIONALE across 15 Major + 10 Minor — cleanest brainstorm chain in project history; writing-plans dispatch UNBLOCKED pending operator decision on 3 §15.B open items):**

**Phase 12.5 #1 brainstorm SHIPPED 2026-05-17 at `a1582c0`** (integration merge of `phase12-5-bundle-1-oqf-brainstorm` via `--no-ff`; 8 implementer commits = 1 draft + 6 Codex-fix + 1 return-report; **7 Codex rounds → NO_NEW_CRITICAL_MAJOR** convergent monotonic-Major taper (5M+2m → 3M+2m → 2M+2m → 2M+2m → 2M+1m → 1M+1m → 0); **ZERO ACCEPT-WITH-RATIONALE banked across all 15 Major + 10 Minor findings — cleanest brainstorm chain in project history**; ZERO Critical findings entire chain; ZERO Co-Authored-By footer drift across 8 commits. **Spec doc** at `docs/superpowers/specs/2026-05-17-phase12-5-bundle-1-oqf-multi-leg-auto-redirect-design.md` (1236 lines; mirrors Sub-bundle C spec format §0-§17). **4 operator-locked decisions** (§2.1-§2.4 verbatim binding clauses; preserved through all 7 rounds): auto-redirect ON; all-match-within-tolerance threshold; reuse `apply_tier2_resolution(choice_code='split_into_partials', resolved_by='auto_tier1_multi_leg', applied_by_override='auto', correction_action_override='auto_applied')`; banner advisory only. **7 brainstorm-locks at §15.A** (Codex chain resolved during 7 rounds): §6.5 n=1 single-order multi-leg path via ambiguity_kind reclassification; §8.6 `--resolved-by` CLI filter IN-BUNDLE at T-1.10; §7.6 sandbox short-circuit gated-on-auto-redirect + SAVEPOINT ROLLBACK; §7.4 service API uses `operator_custom_payload` + new override kwargs (`applied_by_override` / `correction_action_override` / `resolved_by_override`); §11.2 briefing.md +1 line for `tier1_multi_leg_redirected_count`; §12.3 canary observability for empty-executions; §13.3 brief §2.4 amended (free TEXT `resolved_by`; no `_RESOLVED_BY_VALUES` constant). **Schema v19 UNCHANGED LOCK** (CHECK enums already accommodate values). **3 still-open §15.B items** for writing-plans handoff (brainstorm defaults recommended): §4.4 price_tolerance $0.01 absolute vs proportional-override; §6.3 qty_tolerance asymmetry LOCK 1e-9 predicate / 1e-6 handler; §6.4 defensive N-legs cap NO V1. **Single sub-bundle ship recommended** (NOT 2-3 originally projected): ~+320 LOC across 11 tasks; ~+85 fast tests + 1 slow E2E; 3-5 Codex rounds projected for executing-plans. **12 forward-binding lessons** for writing-plans (8 from spec §16 + 4 chain-surfaced in return report §8): recipe-field discipline; override-parameter threading; free-text vs CHECK-enum distinction; cross-column CHECK invariants; sandbox short-circuit ALWAYS in inner (C.C lesson #2); helper invocation completeness; ASCII-only banner text (cp1252 gotcha pre-emption); discriminating-test patterns for predicate edge cases.

**Writing-plans dispatch UNBLOCKED** — operator-paced; consumes locked spec + 12 forward-binding lessons + 3 §15.B items + brainstorm-default recommendations. Orchestrator's next deliverable: surface §15.B items to operator (4-decision recommended-defaults question) + draft writing-plans dispatch brief encoded with operator-locked decisions + inline implementer-dispatch prompt.

**Post-Phase-12 mapper-widening ARC CLOSED** (Sub-bundles 1 + 1.5 + 2 all shipped 2026-05-17; ~49 commits / 12 Codex rounds / +215 fast tests; ZERO footer drift). Phase 12.5 #2 (Web Tier-2 discrepancy-resolution surface) + #3 (Project hygiene maintenance pass — absorbs CLAUDE.md+orchestrator-context archive-split + V2.1 §VII.F amendment batch + Phase 8 walkthrough failing-test triage + Ruff 18 E501 cleanup) dispatches UNBLOCKED post Phase 12.5 #1 close. Phase 13 scope LOCKED at `docs/phase13-scope-brainstorm.md` §0.5 pending Phase 12.5 close.

**Previous current-state pointer (As of 2026-05-17 PM post-Sub-bundle-2-merge; historical):**

**Post-Phase-12 Sub-bundle 2 SHIPPED 2026-05-17 at `690aed0`** (integration merge of `schwab-mapper-bundle-2` via `--no-ff`; 13 implementer commits = 7 task-impl T-2.0..T-2.6 + 3 Codex-fix + 1 return-report + 1 merge; **3 Codex rounds → NO_NEW_CRITICAL_MAJOR** convergent (R1 1C/1M → R2 0C/1M → R3 0C/0M/1m); **ZERO ACCEPT-WITH-RATIONALE banked** — all 4 findings resolved with code-content fixes; ZERO Co-Authored-By footer drift; +52 fast tests (4523 → 4575); ruff 18 unchanged; schema v19 unchanged. **Deferred Phase 12 Sub-bundle B T-B.7 task CLOSED**: read-only `/schwab/status` web counterpart to `swing schwab status` CLI mirroring 3-state LIVE/PROVISIONAL/DEGRADED output 1:1. NEW `SchwabStatusVM` + `SchwabCallSummary` dataclasses + `GET /schwab/status` route + `schwab_status.html.j2` template + `/config` nav-link + `POST /schwab/setup` HX-Redirect retarget. **5-surface orchestrator-witnessed gate ALL PASS** via curl + grep on worktree web server port 8081: S1 inline pytest 4575 fast pass; S2 `/schwab/status` HTTP 200 with state-ok LIVE badge + masked tokens_db_path + 6-row recent-calls table + ZERO error leaks; S3 `/config` shows both `/schwab/setup` AND `/schwab/status` nav-links; S4 SKIPPED per brief default (refresh-token clock healthy ~5 days remaining); S5 ruff 18 unchanged. Codex chain absorbed R1 Critical #1 (error_excerpt sentinel leak — two-layer fix dropping render + adding read-time re-redactor) + R1 Major #1 (status enum widened 4→6 schema-CHECK values) + R2 Major #1 (tokens_db_path masked via Path.relative_to → ~/<path>) + R3 Minor #1 (template sentinel audit extended to error_message). **4 V2.1 §VII.F amendments banked** + **2 CLAUDE.md gotcha promotion candidates banked** for Phase 12.5 #3 maintenance pass triage.

**Post-Phase-12 mapper-widening arc CLOSED** — Sub-bundles 1 + 1.5 + 2 all shipped 2026-05-17; arc-cumulative ~49 commits / 12 Codex rounds / +215 fast tests / 2 ACCEPT-WITH-RATIONALE (both Sub-bundle 1.5) / ZERO Co-Authored-By footer drift / schema v19 unchanged. V2 mapper widening's positive lift confirmed firing on production; system EXITS safe-degraded mode; web `/schwab/status` page operational.

**Phase 12.5 dispatches UNBLOCKED** (operator-locked 2026-05-17 sequencing): (1) OQ-F multi-leg tier-1 auto-redirect (direct V2-mapper-widening successor; consumes Sub-bundle 1's `_compute_execution_price` + `_resolve_match_quantity` helpers + Sub-bundle 1.5's confirmed-firing extraction path) → (2) Web Tier-2 discrepancy-resolution surface (Sub-bundle C plan §I.3 V2; web counterpart of C.D CLI) → (3) Project hygiene maintenance pass (5 sub-items per operator-expanded 2026-05-17 scope: CLAUDE.md archive-split + orchestrator-context archive-split + V2.1 §VII.F amendment batch + Phase 8 walkthrough failing-test triage/fix + Ruff 18 E501 cleanup).

**Post-merge housekeeping completed** (Sub-bundle 2 ship + merge + push 2026-05-17 PM): CLAUDE.md status-line append + phase3e-todo.md Sub-bundle 2 SHIPPED entry at top + orchestrator-context.md current-state pointer refresh (this update). **Worktree husks cleaned** by operator post-merge (`schwab-mapper-bundle-1` + `schwab-mapper-bundle-1.5` + `schwab-mapper-bundle-2` all removed via `cleanup-locked-scratch-dirs.ps1 -DeregisterFirst`). **S2 `/schwab/status` visually verified by operator post-merge** (browser-level confirmation closed the curl+grep gate caveat — page rendering OK end-to-end). System CLEAN.

**Post-Phase-12 Sub-bundle 1.5 SHIPPED 2026-05-17 at `a7c1016`** (predecessor — CLOSES Sub-bundle 1's validator-drop defect; system EXITS safe-degraded mode after S3 production fetch confirmed positive lift firing).

**Post-Phase-12 Sub-bundle 1.5 SHIPPED 2026-05-17 at `a7c1016`** (integration merge of `schwab-mapper-bundle-1.5` via `--no-ff`; 13 implementer commits + 1 return-report + 1 merge; **4 Codex rounds → NO_NEW_CRITICAL_MAJOR** convergent tapering (R1 0C/6M/2m → R2 0C/2M/2m → R3 0C/0M/2m → R4 0C/0M/1m); **2 ACCEPT-WITH-RATIONALE banked** (R1 M#2 T-1.5.4 sequence-by-design; R2 M#1 canary intentionally minimal — `price > 0` strongest signal); ZERO Co-Authored-By footer drift; +48 fast tests (4475 → 4523); ruff 18 unchanged; schema v19 unchanged. **CLOSES Sub-bundle 1's validator-drop defect**: 12-line additive early-exit gate at `_extract_executions_from_order_raw` returns None when `filled_qty == 0` (EXPLICIT zero; preserves legacy "absent: permissive" stance) + NEW `_has_non_placeholder_leg` canary helper emits WARN inside gate when any leg in `activityType=EXECUTION` activity has `price > 0` (future-Schwab-regression sentinel). **Root cause confirmed via T-1.5.1 diagnostic** (`scripts/diagnose_schwab_executionlegs.py` operator-executed mid-dispatch): Schwab emits placeholder `executionLegs[]` on STOP-typed orders that NEVER executed (REPLACED/CANCELED/PENDING_ACTIVATION status; `filledQuantity=0` + `leg.price=0.0`); 12 of 17 production orders matched placeholder family; 5 real FILLED LIMIT orders flow through (CVGI/LION/VIR/YOU x2). H1-H5 from brief all falsified; root cause was UNANTICIPATED H1-extended family. **5-surface operator-witnessed gate ALL PASS**: S1 4523 fast pass; S2 23/23 regression tests across 4 new files; **S3 production fetch PASS** — reconciliation_run #14 (state=completed; tier1_applied_count=0; tier2_pending_count=2; 30 orders checked vs Sub-bundle 1's 18; ZERO validator-drop warnings; architectural fix HOLDS both negative AND positive sense — executions flow through extraction; 4 FILLED LIMIT orders matched cleanly via Shape A/B without Shape C tier-1 corrections because operator's journal prices already align); S4 Phase 10 banner cleared to 0 (correction_ids 15+16 acknowledge for DHC+VSAT run #14 ids 50+51); S5 ruff 18 unchanged. **Post-Phase-12 architectural arc CLOSED** — system EXITS safe-degraded mode; V2 mapper widening's positive lift now confirmed firing on production data.

**Sub-bundle 2 (Phase 12.5 #2 web Tier-2 surface) + Phase 12.5 #1 (OQ-F multi-leg auto-redirect) + Phase 12.5 #3 (CLAUDE.md + orchestrator-context maintenance pass) all UNBLOCKED.** Operator-recommended sequencing per phase3e-todo Phase 12.5 RESCOPED entry: 1 first (direct V2-mapper-widening successor; consumes Sub-bundle 1's helpers + Sub-bundle 1.5's confirmed-firing extraction path) → 2 second (smaller scope; consumes shipped C.C/C.D surfaces) → 3 third (cap-drift cleanup; opportunistic).

**Post-merge housekeeping completed by this orchestrator** (Sub-bundle 1.5 ship + merge + push 2026-05-17 PM): CLAUDE.md status-line append + phase3e-todo.md Sub-bundle 1.5 SHIPPED entry at top + orchestrator-context.md current-state pointer refresh (this update). Worktree husk at `.worktrees/schwab-mapper-bundle-1.5/` pending cleanup-script `-DeregisterFirst` pass.

**Post-Phase-12 Sub-bundle 1 SHIPPED 2026-05-17 at `120c992`** (predecessor — GATE PASS-WITH-FINDING; routed validator-drop defect to Sub-bundle 1.5 which closes it) (integration merge of `schwab-mapper-bundle-1` via `--no-ff`; 26 files / 5225 insertions / 4 architectural surfaces — `SchwabExecutionLeg` dataclass + `SchwabOrderResponse.executions` field + mapper widening + `_compute_execution_price` + `_resolve_match_quantity` + `_is_execution_bearing_candidate` + Shape C classifier predicate + Path B `execution_unavailable=true` sentinel + housekeeping FOLDED). 5 Codex rounds NO_NEW_CRITICAL_MAJOR; ZERO ACCEPT-WITH-RATIONALE; ZERO Co-Authored-By footer drift; +115 fast tests (4360→4475); ruff 18 unchanged; schema v19 unchanged. **GATE PASS-WITH-FINDING (S1+S2+S3+S5+S8 PASS; S4+S9 skipped; S6+S7 deferred async).** S3 production fetch (`python -m swing.cli schwab fetch --orders` from worktree) emitted reconciliation_run #13 (state=completed; tier1_applied_count=0; tier2_pending_count=2; ZERO false-positive entry/close_price_mismatch). **CRITICAL FINDING (routes to Sub-bundle 1.5):** ALL 18 production orders had `orderActivityCollection[0].executionLegs[0]` uniformly rejected by `SchwabExecutionLeg.__post_init__` validator at mapper drop+warn. Positive lift NEVER FIRED on production despite all 6 E2E cassette + hand-rolled tests passing. Root cause unknown — `schwab_api_calls` audit table does NOT capture `response_body_json`. Hypothesis: synthetic-fixture-vs-production-emitter shape drift (C.D-arc lesson #2 + #4 inheritance). 4 dispositions at gate (correction_ids 11+12+13+14) — DHC 48 + VSAT 49 from run #13 + DHC 46 + VSAT 47 from earlier run #12 (same fills lacking Schwab matches; per-run-vs-per-fill re-emission family); all `acknowledge` per C.D-precedent for `ambiguity_kind='unsupported'`. Banner cleared to 0. **System operating in safe-degraded mode** — V2 architectural infrastructure shipped + no false-positive emissions + positive lift gated on Sub-bundle 1.5 fix.

**Phase 12.5 RESCOPED to 3 items** (operator-locked 2026-05-17; phase3e-todo.md):
1. OQ-F multi-leg tier-1 auto-redirect (direct V2-mapper-widening successor; consumes `_compute_execution_price` + `_resolve_match_quantity`)
2. Web Tier-2 discrepancy-resolution surface (Sub-bundle C plan §I.3 V2 candidate)
3. CLAUDE.md + orchestrator-context.md maintenance pass (cap-drift cleanup; may fold ~17 cumulative V2.1 §VII.F amendments)

Original Phase 12.5 #2 (fill auto-population at trade-entry) MOVED to Phase 13 Theme 3 per operator decision — Phase 13 owns auto-fill end-to-end (entries + exits + reviews + period reviews).

**Phase 13 scope LOCKED — 4 themes / 10 sub-bundles** (`docs/phase13-scope-brainstorm.md` §0.5; operator-decided 2026-05-17). Anchored on operator-authored v2 chart pattern detection brief at `reference/Future Work/Chart Pattern Detection/stock_chart_pattern_detection_ai_ingestion_v2.md` (901 lines; 2026-05-08; AI-ingestion-ready). Themes: **Theme 1** chart rendering deepening (watchlist + hyp-rec + active list + market weather mini-chart) + **Theme 2** pattern recognition (5 buy-side patterns VCP/FB/CWH/HTF/DBW; rule-based PRIMARY + template matching SECONDARY + closed-loop surface; sell-side BANKED Phase 14; ML re-ranker BANKED indefinitely per v2 brief §16.6) + **Theme 3** auto-fill deepening (absorbs old Phase 12.5 #2; entries + exits + reviews + period reviews) + **Theme 4** usability triage closer. **Phase 13.5** drift detection split out (brief's Phase 5; 4 surfaces; benefits from baseline data accumulation). **Phase 14 candidates**: sell-side detector module + ML re-ranker gate revisit + intraday + tax-lot + multi-cohort. Sub-bundle decomposition: 10 SB total in dispatch sequence (T2.SB1 + T3.SB1 concurrent → T2.SB2 → T2.SB3 → T3.SB2 → T2.SB4 → T2.SB5 → T3.SB3 → T2.SB6 [Theme 1 lands here] → T4.SB closer). Dispatch UNBLOCKED post Sub-bundle 2 + Phase 12.5 close. Phase 13 brainstorm dispatch brief at `docs/phase13-brainstorm-dispatch-brief.md`.

**Sub-bundle 1.5 follow-up dispatch UNBLOCKED — NEXT ORCHESTRATOR FIRST MAJOR DELIVERABLE.** Validator-drop defect investigation + fix. Scope: diagnostic phase (capture raw Schwab response shape — options: temp debug logging at mapper drop-point + re-run; OR one-shot direct API call bypassing mapper; OR schema extension to capture response_body_json) → fix phase (amend validator OR mapper pre-coercion OR field-extraction to match actual Schwab production shape; discriminating cassette regression test) → re-verification (re-run S3; verify tier1_applied_count > 0 OR at minimum NO validator-drop warnings). Branch `schwab-mapper-bundle-1.5`; worktree `.worktrees/schwab-mapper-bundle-1.5/`. Codex chain budget 2-4 rounds. Gate 3-5 surfaces. Schema impact likely v19 unchanged.

**Handoff state**: outgoing orchestrator (this session) ships housekeeping = orchestrator-context.md (this update) + CLAUDE.md status-line append + phase3e-todo.md Sub-bundle 1 SHIPPED entry + handoff brief. New orchestrator picks up at: drafting Sub-bundle 1.5 dispatch brief + inline implementer-dispatch prompt. Handoff brief at `docs/orchestrator-handoff-2026-05-17-post-sub-bundle-1.md` (`54c7b9d`).

**3 NEW memory entries banked this session** (load-bearing for new orchestrator):
- `feedback_pause_means_pause.md` — operator's "pause" means STOP all forward motion immediately, even items appearing independently confirmed.
- `feedback_worktree_cli_invocation.md` — at worktree-side gates, `swing` routes to editable-install path NOT worktree. Use `python -m swing.cli` from worktree cwd.
- `feedback_time_estimates_overstated.md` — orchestrator wall-clock estimates are 3-5x too long; divide naive estimates by 3-5x for actual operator-paced wall-clock.

> **CURRENT STATE POINTER (2026-05-17 PM — post-Sub-bundle-1.5-merge maintenance shortcut):** the narrative entries below are progressively older (2026-05-11 + earlier). For AUTHORITATIVE current-state read **CLAUDE.md status line** (single-paragraph; updated with Sub-bundle 1 + 1.5 SHIPPED entries) + **`docs/phase3e-todo.md` top entries** (Sub-bundle 1.5 SHIPPED at top above Sub-bundle 1 + Phase 12.5 RESCOPED + Phase 13 scope brainstorm). Treat the narrative below as historical narrative only.
>
> **Lessons captured cap drift (2026-05-17 PM post-Sub-bundle-1.5):** active section now ~52 entries vs ~30 cap (no NEW since Sub-bundle 1 entry — Sub-bundle 1.5's 5 forward-binding lessons banked in return report §11 + phase3e-todo SHIPPED entry are durable but not yet promoted to active "Lessons captured" section to avoid further cap drift; Phase 12.5 #3 maintenance-pass dispatch will absorb them with full archive-split). Default-conservative until then: keep fresh lessons in return-report + phase3e-todo only.

**As of 2026-05-17 AM (pre-Sub-bundle-1-dispatch baseline; historical):**

**As of 2026-05-11 (post-3e.8-Bundle-1-ship; Bundles 2+3 brief drafting queued for fresh orchestrator):**

**3e.8 Bundle 1 (advisory parity §4.E + §4.F) SHIPPED 2026-05-11 at `b535cb2`** (worktree dispatch on `3e8-bundle-1-advisory-parity`; 9 commits = 5 task-impl + 1 ruff-style + 3 Codex-fix; 4 Codex rounds → NO_NEW_CRITICAL_MAJOR convergent shape; each round caught real issues incl `_CachingFetcherWrapper` for transient-failure cache-divergence trap). Wires existing advisory rules into pipeline briefing + trade detail page + open-positions expanded HTMX partial. NO new advisory rules. Operator-witnessed gate via Chrome MCP: 5/6 PASS + 1 SKIPPED-with-test-coverage. +23 tests (2183 → 2206); ruff baseline 18 unchanged. 2 V2 watch items banked (advisory composition hand-duplication across 5 paths; cache I/O during SQLite read-snapshot in expand route). Bundles 2 + 3 dispatch briefs deferred to fresh orchestrator (operator handoff per session-context-budget management).

**3e.8 INVESTIGATION SHIPPED 2026-05-10 at `63350ad`** (worktree dispatch; 746-line analysis doc at `docs/3e8-sell-side-advisories-investigation.md`; 4 commits; 3 Codex rounds → NO_NEW_CRITICAL_MAJOR). 11 recommendations spanning advisory-message-only + classification-altering classes + 1 operator-action prerequisite (§4.G transcribe Minervini SEPA + DST sell-side into `reference/methodology/`). Critical structural finding: `reference/methodology/` covers entry criteria only; sell-side rules require operator transcription before V2.1 §VII.F routing on §4.A/§4.C/§4.H. DHC-applicable three-field decision matrix surfaced for current trail-MA decision moment. 27 operator-decision items pending in §6 of analysis doc. Orchestrator will provide per-item recommendations + walk through with operator one at a time; each commissioned recommendation banks as separate phase3e-todo entry with own brainstorm/writing-plans/executing-plans cycle.

**3e.10 SHIPPED 2026-05-10 at `8488bf0`** (worktree dispatch on `3e10-dark-theme`; 7 commits = 3 task-impl + 3 Codex-fix + 1 orchestrator pre-gate I1). Dark theme via CSS-variable-driven theming + localStorage-persisted nav-bar toggle (🌙/☀️). Light default; operator opts in. Codex chain 3 rounds → NO_NEW_CRITICAL_MAJOR (convergent shape). 4 Major findings ACCEPTED. Orchestrator-side pre-gate I1: `color-scheme: dark` hint added to html.dark/body.dark block, pre-empting Windows-browser white-on-white form-input regression on Config + cadence-complete + entry forms (per implementer's discovery note). Operator-witnessed gate via Chrome MCP browser automation — Surface 1-7 PASS or covered. Test count 2166 → 2183 (+17); ruff baseline 18 unchanged. 8 V2 watch items banked in phase3e-todo 3e.10 entry. Operator-facing: dashboard now shows 🌙 button at right end of nav; click toggles dark theme; localStorage persists across sessions.

**3e.16 SHIPPED 2026-05-10 at `1b43efb`** (worktree dispatch on `3e16-cadence-review-trade-summary`; 8 commits = 4 task-impl + 4 Codex-fix). Adds "Trade activity during this period" section to `/reviews/{id}/complete` form view with state-tagged rows ([OPENED] / [CLOSED] / [OPENED+CLOSED] / [EVENT]). New `list_trades_with_activity_in_period` data-layer helper. Codex chain 5 rounds → NO_NEW_CRITICAL_MAJOR (R1 0/4/3 → R2 0/2/2 → R3 0/2/2 → R4 0/2/1 → R5 0/0/2; convergent shape; each round caught real edge cases including a brief-author error on `was_closed_in_period` predicate that would have mis-tagged partial-trim fills as `[CLOSED]`). 3 Major findings ACCEPTED with sound rationale (data-layer adapter re-paste + V1 closing-fill proxy + [EVENT] semantic for fill-fallback). Operator-witnessed gate via Chrome MCP browser automation: S1+S3+S4+S5 PASS; S2 SKIPPED-with-test-coverage. Test count 2142 → 2166 (+24); ruff baseline 18 unchanged. Three V2 watch items banked in `docs/phase3e-todo.md` 3e.16 entry.

**3e.15 SHIPPED 2026-05-10 at `d1aed5a`** (option (a) — narrowed badge predicate to event_log only). Single-commit inline orchestrator ship; ~30 min impl. Empirical premise re-verified at code before design lock. Operator-facing impact: open trades show ⚠ pending after pipeline runs unless operator submits an event_log entry via daily-management form (badge now means "operator engaged" not "pipeline ran"). +2 net tests; full suite 2140 → 2142 GREEN.



**Post-2026-04-28 6-phase operator sequence — current status:**

| Phase | Item | Status | Final HEAD |
|---|---|---|---|
| 1 | Sector/industry capture + display | SHIPPED + production-verified 2026-04-29 | `09ad4bd` |
| 2 main | Hyp-recs trade-prep expansion | SHIPPED 2026-04-29 (`a29a592`); production verification surfaced 3 bugs |
| 2 cleanup | Hyp-recs success-path fix | SHIPPED 2026-04-29 (`39ff313`); production verification surfaced 3 MORE bugs |
| 2 bug-fix | Production-verification investigation dispatch (Bugs A/B/C) | SHIPPED 2026-04-29 (`bc56ea1`); operator-witnessed PASS on steps 1-3 |
| 3 | OHLCV archive consolidation | SHIPPED 2026-04-30 (`3335d6c`); 696 tickers consolidated; migration ran cleanly | `3335d6c` |
| 4 | Cleanup-remainder bundle (Bug 7 durable closure + 4 follow-ups) | SHIPPED 2026-04-30 (`4cfe1a9`); structural-guard test as durable invariant | `4cfe1a9` |
| 4.5 | Hypothesis_label web-form gap fix | SHIPPED + operator-verified 2026-04-30 (`f9a07bf`); 4 tasks + 1 R1-minor cleanup |
| 4.5 follow-up | Hyp-recs panel in-flight count UX bundle | SHIPPED 2026-04-30 (`94355e6`); +4 tests; renders `N/T (+K in flight)` |
| TOS-import bug | TRD-as-withdrawal fix + Excel-quoted REF cleanup | SHIPPED 2026-04-30 (`c9159c7`); +1 test; verified against operator's 4/30 Schwab/TOS export (2 mismatched movements → 1 correct) |
| 5 | Configuration page for operator-tunable settings | SHIPPED 2026-05-02 at `3a4195c` (merge of `phase5-config-page-redispatch` worktree branch); 1472 fast tests + operator-witnessed 6-step browser gate PASS; 1 lost-update-race Major ACCEPTED with rationale (single-operator scope) | `3a4195c` |
| 6 | Post-trade review surface (journal v1.2 sub-bundle: Mistake_Tags + Process Grade + mistake_cost_R / lucky_violation_R + Review_Log) | SHIPPED 2026-05-04 at `51c79ed` (merge of `phase6-post-trade-review` worktree branch); 1587 fast tests + operator-witnessed 6-step browser gate PASS (1 mid-verification fix landed inside the worktree — review POST HX-Redirect target `/trades` was unrouted, fixed to `/reviews/pending`); writing-plans 5 Codex rounds → NO_NEW_CRITICAL_MAJOR; executing-plans 2 Codex rounds → NO_NEW_CRITICAL_MAJOR; 0 rogue commits | `51c79ed` |
| 7 | Trade lifecycle state-machine + Fills first-class | **SHIPPED to main 2026-05-05 at `c617777`** (integration merge of `phase7-sub-c-web` chained branch; 56 commits incl 3 sub-dispatches + 2 hotfixes). Sub-A `78c7005` (14 cmts/2 rounds); Sub-B `71ddb95` (15/6); Sub-C `b867f00` (24/3); migration-runner hotfix `283d4fa` (FK CASCADE-on-DROP wiped fills+trade_events under foreign_keys=ON; 2 discriminating tests); operator-gate hotfix `5e9981c` (entry duplicate trade_events + missing review state-transition; 2 discriminating tests). 11/11 operator-witnessed gate surfaces PASS post-hotfix. Production DB at v14; 4 trades (VIR/DHC/CC/YOU) + 5 fills + 11 trade_events; GPRE test trade cleaned post-gate. Worktrees + branches cleaned 2026-05-05 via cleanup-script extension (cleanup-locked-scratch-dirs.ps1 worktree-extension landed at `5430c1c` after `pytest-of-rwsmy/` ACL-lock recurrence trigger fired 5/5; 4-step takeown+icacls/reset+icacls/grant+Remove-Item resolved Phase 6 leftover too). |
| 8 | Daily_Management + MFE/MAE precision | **SHIPPED to main 2026-05-07 at `ddfdfcb`** (integration merge of `phase8-daily-management` worktree branch; 35 commits = 18 task-impl + 16 Codex review-fix + 1 mid-verification I1 fix). Brainstorm `c954eef` (875-line spec; 5 rounds + R5 confirm); writing-plans `17b1845` (4140-line plan; 9-round Codex chain new high-water mark for any single dispatch — convergent shape per Phase 7 Sub-B lesson family); executing-plans `a1c9849` worktree HEAD (Critical-1 pipeline_run_id wiring + 5 R1 majors + 5 R2 majors + 4 R3 majors + 4 R4 majors + 3 R5 advisory all fixed). Mid-verification I1 fix `2d0ed56` (operator-witnessed gate Surface 2 — emotional_state JSON-text input changed to multi-checkbox dropdown matching Phase 7 entry-form precedent; reuses canonical `DAILY_MGMT_EMOTIONAL_STATES` 8-value vocabulary). Production DB at schema_version 16 with `daily_management_records` table + `trades.planned_target_R` column. All 6/6 operator-witnessed gate surfaces PASS including Surface 6 stop-change FK chain (event_log id=7 + trade_events id=24 + trades.current_stop atomic on trade 4/YOU 53.08→54.06; §A.1 single-transaction contract verified in production). Test count 1940 → 2079 (+139); ruff baseline 78 preserved. 19 total adversarial rounds across the phase (brainstorm 5 + writing-plans 9 + executing-plans 5) — new high-water mark for any single phase in this project; healthy convergent shape. Worktree teardown left empty husk `.worktrees/phase8-daily-management/` ACL-locked (Phase 6/7 cosmetic-leftover pattern; needs elevated `cleanup-locked-scratch-dirs.ps1` run). | `ddfdfcb` |
| 9 | Risk_Policy entity + reconciliation depth | **brainstorm SHIPPED 2026-05-06 at `31ee51c`** (1090-line spec at `docs/superpowers/specs/2026-05-06-phase9-risk-policy-reconciliation-design.md`; 4 Codex rounds + R5 confirmation → NO_NEW_CRITICAL_MAJOR; convergent chain matches Phase 7 Sub-B + Phase 8 R2-R5 family). Three locks: per-policy-snapshot versioning with `is_active`+`superseded_by_policy_id` dual-col + Phase 7 state machine UNTOUCHED (reopen via query-side JOIN, not flag) + `risk_policy` canonical with cfg as startup-mirror. 5 new tables; TOS reconciliation depth bundle SUBSUMED with 4 discrepancy_types; sector tamper hardening BOTH schema+route; Schwab API Phase A boundary contract specified. Writing-plans dispatch queued. | n/a (brief commits) |
| 10 | Metrics dashboard | **brainstorm SHIPPED 2026-05-06 at `fe6cb45`** (641-line spec at `docs/superpowers/specs/2026-05-06-phase10-metrics-design.md`; 5 substantive Codex rounds + R6 confirmation → NO_NEW_CRITICAL_MAJOR). Three locks: capital-denominator split-policy + global statistical-confidence floor (n=20) + tier-comparison text-only descriptor. RESEARCH-posture (no schema/code). Sequencing locked: brainstorms 10→8→9; execution 8→9→10. | n/a (brief commits) |
| Finviz API V1 (standalone) | Finviz Elite API integration; new `swing/integrations/` namespace establishes pattern for Schwab API integration | **SHIPPED 2026-05-06 at `002338a`** (merge of `finviz-api-integration` worktree branch); 17 commits + 1 mid-verification I1 fix (`0e02ed6` — `insert_call` no longer commits internally; lease.fenced_write contract restored); writing-plans 5 Codex rounds + executing-plans 5 Codex rounds → both NO_NEW_CRITICAL_MAJOR; +63 fast tests + 2 slow live tests; ruff baseline 78 preserved; production DB at schema_version 15 with `finviz_api_calls` audit table; all 8 operator-witnessed gate surfaces PASS (S5 drift WARNING tested via prior-signature DB tamper since Finviz API URL params fully define the screen — no saved-screen-handle to edit on UI side per writing-plans research). | `002338a` |

**24-phase track record (17 ZERO-rogue + Phase 5 with rogue + recovery + Phase 6/7/Finviz-V1/Phase-10-brainstorm/Phase-8-brainstorm/Phase-9-brainstorm/Phase-8-writing-plans/Phase-8-executing-plans/Phase-8-V1-polish/3e.12-tos-import-diagnostic/polish-bundle-2026-05-09 ZERO-rogue first-attempt clean). 2121 fast tests at HEAD `fbb8fc5` (1 skipped — Phase 7 operator-only fixture test). Polish bundle ship contributed +22 tests; ruff `swing/` baseline **78 → 26** via 2026-05-10 sweep (`e99047f` safe auto-fixes 78→44 + `33338f7` unsafe auto-fixes 44→34 + `9c9b57c` manual B904+E741+SIM115-noqa 34→26; 2121 preserved across all three; remaining 26 = 8 N818 + 18 E501 banked at `fbb8fc5` for bundling per phase3e-todo entry). claude-mem upgraded 12.7.5 → 13.0.1 (three local edits re-applied: PreToolUse:Read deletion + PostToolUse:* deletion + .mcp.json direct-node launcher); subsequently disabled by operator 2026-05-10 for ~1-week token-usage-vs-cross-session-performance experiment (memory file `feedback_claude_mem_hook_blocks_disabled.md` documents the window + re-enablement criteria). Chart pattern detection v2 research docs tracked at `6b40292` + backlog entry at `138d3ab` (greenfield expansion beyond shipped flag-v1; brainstorm-needed; sequence-locked after Phase 9 + Phase 10). Phase 9 writing-plans queued next per locked sequencing 8 → 9 → 10.**

**Polish bundle 2026-05-09 — SHIPPED to main 2026-05-09 at `b4bb9dd`** (integration merge of `polish-bundle-2026-05-09` worktree branch; 16 commits = 13 task-impl + 3 adversarial-fix). Brief at `docs/polish-bundle-2026-05-09-brief.md` (`192cde1`); no separate writing-plans plan (small-scope dispatch followed tos-import-diagnostic shape). 5-item bundle: 3e.5 daily-mgmt logged-today badge on open-positions row + 3e.6 auto-return to dashboard after daily-mgmt event submit + 3e.11 strip Phase/Tranche from operator-facing CLI help text (4 audit-found bonus fixes beyond the 2 locked) + 3e.13 top-nav Reviews link between Journal and Pipeline + 3e.14 cadence card "Complete review" inline link. 3 Codex rounds → NO_NEW_CRITICAL_MAJOR; convergent shape (R1 0/2/2 → R2 0/1/2 → R3 0/0/0). **Mid-dispatch brief-side defect caught by implementer + Codex R1 Major #1:** brief locked the 3e.5 read predicate to `action_session_for_run(now())` but writers stamp `last_completed_session(now()).isoformat()` per Phase 8 spec §4.5; implementer corrected predicate inline + added `test_post_event_log_then_dashboard_badge_flips_to_logged` round-trip integration test pinning read/write alignment. Brief labels for the badge (`✓ today / ⚠ not yet`) updated mid-dispatch to `✓ logged / ⚠ pending` per Codex R2 Major #1 — original labels were temporal lies on weekends/holidays. Test count 2099 → 2121 (+22; over projection of +13-17 due to 4 in-scope C.3 audit fixes + 1 R1-driven round-trip test). Worktree teardown attempted but ACL-locked (Phase 6/7/8 husk pattern recurrence at `.worktrees/polish-bundle-2026-05-09/`); operator-elevated `cleanup-locked-scratch-dirs.ps1` covers this. New CLAUDE.md gotcha promoted: session-anchor read/write mismatch family (third instance with the existing weather lookup gotcha).

**Phase 8 V1 polish — SHIPPED to main 2026-05-08 at `24b3e9a`** (integration merge of `worktree-phase8-v1-polish` branch; 16 commits = 12 task-impl + 4 adversarial-fix). Brief at `docs/phase8-v1-polish-writing-plans-brief.md` (`188d1ea`); plan at `docs/superpowers/plans/2026-05-07-phase8-v1-polish.md` (`5aa7829`; 1256 lines; 13 tasks A.1-A.4 + B.0-B.9 + Z.1-Z.3; writing-plans 2 Codex rounds; executing-plans 3 Codex rounds → both NO_NEW_CRITICAL_MAJOR; convergent shape). 4-surface operator-witnessed gate PASS via Chrome MCP walkthrough — all programmatic + visual checks confirmed: Detail anchor renders on every open-position row with correct href + stopPropagation; Phase 7 stop-adjust trade_events surface as "Stop adjustment (legacy quick-adjust)" with chronological merge alongside Phase 8 daily_snapshots + event_logs; dedup via `linked_trade_event_id` correctly suppresses Phase 8-form-written trade_events from double-displaying as orphans; no Jinja UndefinedError from VM extension; existing forms (entry/exit/stop) intact. **Three V2 advisory items banked** in `docs/phase3e-todo.md` "Phase 8 V2 advisory items" subsection: audit-chain symmetry (legacy /trades/{id}/stop also writes Phase 8 event_log row); `data-trade-event-id` template attribute for orphan rows; read-side snapshot consistency (transient race window between two SELECTs in `build_daily_management_timeline_vm`). Test count 2079 → 2090 (+11; within projected +10-15); ruff baseline 78 preserved.

**Phase 4.5 — hypothesis_label web-form gap (shipped + verified).** Brief `ec93af2`; plan `719c35e`; executing-plans chain `f2df573..f9a07bf` (5 commits: Tasks 1-4 + R1 minor 2 import cleanup). Codex 3 rounds → NO_NEW_CRITICAL_MAJOR (R1: 0/2/2 — both M ACCEPTED with §2.4 + sector-industry-precedent rationale; 1 minor fixed; R2: 0/1/1 — major ACCEPTED helper byte-identical + chain coverage; R3: 0/0/1 advisory). Helper extracted to `swing/recommendations/hypothesis_prefill.py:lookup_active_recommendation_label` (public; CLI + web-VM consumers; 6 existing CLI prefill tests still pass). Multi-path data ingestion audit closed: 4 writers to `trades.hypothesis_label` (CLI; web routes NEW; `swing/trades/entry.py` canonicalize boundary; `swing/data/repos/trades.py` repo writer). Operator-witnessed verification confirmed 2026-04-30: hypothesis row renders correctly on `/trades/entry/form`; hidden input carries matcher's label; KALV smoke-test trade auto-filled correctly. Advisory follow-up (R3 minor 1): no end-to-end GET→extract-fragment→POST→assert-DB happy-path test; current coverage pins the seams via snapshot-trust (POST→DB) + GET-form template assertions (GET→hidden input) separately. Defer.

**Phase 4.5 follow-up — hyp-recs panel in-flight count UX bundle (shipped).** `94355e6`. Operator-surfaced UX gap: dashboard rendered "1/5" without distinguishing closed-evidence count from in-flight (open) attribution. Now renders `N/T (+K in flight)` when open prefix-matching trades exist; `N/T` alone when none. Tripwire arithmetic untouched — in-flight is display-only via `list_open_trades` + `_label_matches_hypothesis`. 4 layers: `HypothesisProgress.in_flight_sample` (journal-stats) + `compute_hypothesis_progress_breakdown` + `HypothesisRecommendation.hypothesis_in_flight_n` (dashboard VM) + `_build_active_recommendations` plumb + `hypothesis_recommendations_row.html.j2` decoration. Single-commit feature with TDD discipline (3 RED tests across 3 layers + 1 omit-when-zero discriminator → all GREEN; +4 fast tests; ruff baseline preserved at 91).

**Phase 6 — post-trade review surface (SHIPPED 2026-05-04 at `51c79ed`).** Brainstorm-skipped per operator + orchestrator in-thread design lock (Q1-Q9 + Q-bonus answered + locked); writing-plans brief at `docs/phase6-post-trade-review-writing-plans-brief.md` (`441e22a`); plan at `docs/superpowers/plans/2026-05-02-phase6-post-trade-review-plan.md` (commit chain `1be4622..e976d64`; 5 Codex rounds → NO_NEW_CRITICAL_MAJOR; 1 R1 Critical fix added Task 12b cadence-completion path); executing-plans brief at `docs/phase6-post-trade-review-executing-plans-brief.md` (`a7c4bda`). 10 nullable trade-row fields + new `review_log` entity (slim 14 always-populated + 7 frozen-at-completion aggregates per Q4=B) + Mistake_Tags vocabulary v1.2 §7.10 verbatim + Process Grade v1.2 §9.2 computation + cost/violation derivation per v1.2 §8.8 (Q3=A: counterfactual `realized_R_if_plan_followed` stored; cost+violation derived on read) + soft-warn at trade close (Q5=B; cfg-driven `cfg.review.review_window_days` default 7) + dashboard "needs review" badge + dashboard cadence cards (Q6=B: 3 UI-wired daily/weekly/monthly; quarterly + circuit_breaker schema-supported) + pipeline `_step_review_log_cadence` (Q9=A: pre-create idempotent; uses `last_completed_session(now)` per §A.8 brief-defection orchestrator-pre-approved) + CLI `swing trade review` + cadence-completion CLI + web. **Executing-plans dispatch (worktree-isolated `phase6-post-trade-review` branch from base `a7c4bda`)** — 24 commits including in-cycle review fixes (16 task-impl + 1 code-review I2 ruff baseline restore + 4 R1 Codex fixes + 1 R2 Codex minor fix + 1 mid-verification I3 fix for HX-Redirect-target-unrouted) — 2 Codex rounds → NO_NEW_CRITICAL_MAJOR (R1: 0/3/2 all FIXED; R2: 0/0/3 — 1 FIXED + 2 ACCEPTED-with-rationale on docstring drift + ReviewsPendingVM.window_days stale field) — operator-witnessed 6-surface browser gate PASS 2026-05-04 (1 mid-verification fix for review POST HX-Redirect target `/trades` was unrouted, fixed to `/reviews/pending` in worktree; freezing-aggregates verification done via direct DB query because `/reviews/{id}/complete` 404s for completed Review_Logs — 2nd V1 follow-up). Merged via `git merge --no-ff` at `51c79ed` (10 docs commits ahead of base prevented fast-forward) + pushed to origin. Test count 1472 → 1587 (+115; ~3x plan's +30-45 estimate due to Codex compound-ROI on discriminating-test-discipline; lessons-captured below). Ruff baseline 98 → 80 (net −18 from incidental cleanup of pre-existing items in touched files). Phase 6 was the **first all-clean executing-plans dispatch since Phase 4** — worktree+marker-file workflow held; ZERO rogue commits.

**Phase 5 — configuration page (SHIPPED 2026-05-02 at `3a4195c`).** Brainstorm-skipped per operator + orchestrator in-thread design lock; brief at `docs/phase5-configuration-page-writing-plans-brief.md` (`3fde496`); plan at `docs/superpowers/plans/2026-05-01-configuration-page-plan.md` (5 Codex rounds → NO_NEW_CRITICAL_MAJOR; HEAD `e8c6396`). 3 V1 fields (`cfg.web.chase_factor`, `cfg.pipeline.chart_top_n_watch`, `cfg.account.risk_equity_floor` — risk_floor de-scoped from constant-promotion to no-op after empirical audit). Separate user-config file at `%USERPROFILE%/swing-data/user-config.toml`; per-request read refactor; CLI parity (`swing config show|set|reset`). 1381 → 1472 fast tests (+91 — exceeds the +75 plan estimate). **Executing-plans first attempt** (HEAD `dff70ca..2278e97`) landed Tasks 1.0/1.1/1.2 (plan-aligned) + Task 2.0 (plan-aligned) + a rogue Task 1.2-rewrite commit `2278e97` that overwrote the plan-aligned `swing/config_overrides.py` with a non-plan API (extended-time-window subagent self-collision; see Lessons captured 2026-05-01). Rogue commit reverted via `git reset --hard e42f5be` + `git push --force-with-lease origin main`. **Re-dispatch (2026-05-02)** from Task 2.1 onward used worktree isolation (`superpowers:using-git-worktrees`) + global PreToolUse Codex-blocking hook + marker-file workflow (revised brief at `docs/phase5-configuration-page-executing-plans-brief.md`); 7 commits onto `phase5-config-page-redispatch` branch (`f86eafd..4d3174d`); 2 Codex rounds → NO_NEW_CRITICAL_MAJOR (R1: 0/3/1, 2 Majors fixed in `4d3174d` for HTMX HX-Request header reset + HX-Redirect for HTMX success — both browser-only failure modes; 1 Major ACCEPTED for lost-update race with rationale of single-operator scope; R2: 0/0/1 advisory). Operator-witnessed 6-step browser gate PASS 2026-05-02 (after working around the editable-install vs worktree path mismatch via `$env:PYTHONPATH = "."; python -m swing.cli web`). Merged via `git merge --no-ff` at `3a4195c` (4 docs commits ahead of base prevented fast-forward).

**Operator-paced items (deferred-with-tracking):**

- **Tier-3 #5 lightning icon trigger logic re-evaluation.** LOW operational urgency; cosmetic. Detailed orchestrator-thread review provided 2026-04-30 (current logic, concerns, options A-F enumerated); operator deferred.
- **Tier-3 #6 advisory state-machine + trade-maturity gating.** MEDIUM-HIGH operational urgency; DHC active and approaching trail-MA decision territory. Operator framing captured 2026-04-27 (verification-results doc §#6) + reaffirmed 2026-04-30 (Minervini "trade has to prove itself"). Trade-maturity stages: new (~0R) → maturing → mature (+1.5R-2R, default 20MA) → well-mature (+2R+, upgrade to 10MA). Operator decision per timing: after Phase 4.5 + Phase 5; OR pre-empt if DHC matures into the +1.5R region.
- **Chart-scope policy v3 (4th tier `hypothesis_rec`).** Original Q2 lock 2026-04-28; reaffirm-deferral 2026-04-30 after operator hit "Chart unavailable" on CC hyp-rec entry. Trigger near-hit; tracking signal accumulating. Captured in `docs/phase3e-todo.md` 2026-04-28 chart-scope policy v2 follow-ups → "Future deferrals" subsection.

**Chart-pattern flag-v1 V1-ship gates (operator-paced; long-horizon; no progress this session):**

- Task 7.3 (operator-paced fixture labeling, ≥15 fixtures) — operator's earlier framing: blocked more by external constraints (figuring out best way to label) than by orchestrator-side bandwidth. No urgency.
- Task 7.4 (FP-biased classifier tuning checkpoint, gated on Task 7.3).

**Notable bugs surfaced + addressed this session (2026-04-29 → 2026-05-01):**

- Journal divide-by-zero (`_losers_held_too_long` when all winners same-day; surfaced via smoke-test trades) — fixed `bed682f`.
- HTMX `<tr>`-leading `makeFragment` table-wrap pathology (Bug B from production verification) — fixed via pure-OOB response architecture.
- `exclude_tickers` invariant breadth (Bug C from production verification; previously ACCEPTED-with-rationale, confirmed live in production) — fixed via build_dashboard inline path + `/hyp-recs/refresh` route.
- Bug 7 mixed-anchor family (third resurfacing) — DURABLY closed via two-helper-per-contract pattern + structural-guard invariant test (Phase 4).
- TOS-import TRD-as-withdrawal (operator-surfaced 2026-04-30 via 4/30 Schwab/TOS export with CC purchase) — fixed `c9159c7`. `extract_cash_movements` skipped only `BAL` + empty TYPE; trades settle in the Cash Balance section under TYPE=`TRD` with negative amount. Denylist now includes `TRD`. Co-located fix: Excel-style force-as-text REF parsing (`="..."` left a stray leading `"` from old `.strip('"').replace("=", "")` ordering; new `.strip("=").strip('"')` ordering handles cleanly).

**Surface-level bug observations captured but not actioned (defer-investigate per operator):**

- Entry form stop-value populated incorrectly during CC entry (2026-04-30; Take-this-trade button on hyp-recs expansion). Operator instruction: do not investigate further until reproduces. Captured in `docs/phase3e-todo.md` with possible-mechanisms enumeration.

**Active orchestrator-side housekeeping (cosmetic; not blocking):**

- **Leftover `.worktrees/phase6-post-trade-review/` directory after worktree-merge cleanup — partially cleaned 2026-05-04** — `git worktree remove` originally failed with `error: failed to delete ... Directory not empty` (Permission denied on `.tmp/pytest-of-rwsmy/` subdirectory; same Windows ACL pattern as Phase 5's `.worktrees/phase5-config-page-redispatch/`). Worktree REGISTRATION removed + branch `phase6-post-trade-review` deleted; on-disk directory remained. **2026-05-04 cleanup attempt** (operator-run): `takeown /F ... /R /D Y` + `icacls ... /reset /T /C /Q` + `Remove-Item -Recurse -Force` succeeded on 1196/1198 files; **2 files in `.tmp/pytest-of-rwsmy/` remained ACL-locked** despite takeown+icacls. Working tree at HEAD `aa2dd60` has `?? future/` (intentional) + `?? .worktrees/phase6-post-trade-review/.tmp/pytest-of-rwsmy/` (2 stuck files; cosmetic). The 2026-05-02 `cleanup-locked-scratch-dirs.ps1` extension covered Codex-sandbox patterns but does NOT cover `pytest-of-rwsmy/` patterns. **Backlog item added to `docs/phase3e-todo.md` (2026-05-04 entry "Worktree cleanup script: pytest-of-rwsmy ACL-lock pattern recurrence check") with explicit post-Phase-7 verification trigger** — if the same lock pattern recurs after Phase 7's worktree teardowns (Sub-A + Sub-B + Sub-C), the cleanup script needs the pattern extension durably.

**Ad-hoc DB cleanups this session:**

- 2026-04-28: Removed 4/27 smoke-test trades (GPRE/ALTO/FLEX) — backup `swing-pre-smoketest-removal-20260429T214129.db`.
- 2026-04-29: Removed 4/29 smoke-test trades (GPRE/SGML/GFS) — backup `swing-pre-smoketest-removal-r2-20260429T222558.db`.
- 2026-04-30: Backfilled CC hypothesis_label via SQL UPDATE — backup `swing-pre-cc-label-backfill-20260430T070438.db`.
- 2026-04-30 (post-Phase-4.5): VIR/DHC/KALV cleanup — backup `swing-pre-vir-dhc-kalv-cleanup-20260430T204013.db`. Operator clarified VIR was pre-engine and should NOT carry the hypothesis-3 prefix; moved label to DHC where it belongs. KALV was a Phase 4.5 smoke-test entry (auto-filled correctly via the new web-form pre-fill); deleted along with its trade_events row + watchlist_archive entry.
- 2026-05-04 (post-Phase-6): Removed SPY test entries (trades 4 + 5 + 2 exits + 4 trade_events) created during operator-witnessed verification gate (S2 soft-warn at close + S6 backdated-trade-for-freezing-test) — backup `swing-pre-spy-cleanup-20260504T022932.db`. Review_Log table preserved (3 rows: daily pending, weekly completed with `n_trades_reviewed=1` consistent with VIR-only post-cleanup, monthly pending). VIR's Phase 6 review fields preserved (real operator data — operator's actual VIR review). **Production DB at HEAD `51c79ed`:** VIR (closed; label `inaugural trade test`; reviewed 2026-05-04 via Phase 6 surface — `realized_R_if_plan_followed ≈ 0`, derived `mistake_cost_R = 0.33`) + DHC (open; entry 2026-04-27 @ $7.58 × 39 shares; label `sub-A+ VCP-not-formed test (proximity_20ma + tightness fails)`) + CC (open; entry 2026-04-30 @ $26.97 × 5 shares; label `Sub-A+ VCP-not-formed (watch); failed: proximity_20ma, tightness`). Hypothesis 3 progress: **0/5 closed (+2 in flight)** — VIR no longer attributes; DHC + CC are open prefix-matchers and will increment to 1/5 then 2/5 as they close.

**Journal v1.2 incorporation (2026-05-01).** Operator commissioned external research on a swing-trading-journal spec (3-revision chain at `reference/Future Work/Trading Journal/swing_trading_journal_*.md`; current target v1.2 at 1953 lines). Orchestrator reviewed the chain + mapped against our platform's actual shape; the journal v1.2 is a discretionary-trader's spec while OUR platform is a framework-research-loop, so direct adoption requires substantial modification. Operator decision 2026-05-01: decompose v1.2 incorporation into **Phases 6-9** in `docs/phase3e-todo.md` "2026-05-01 Journal v1.2 incorporation" section; ship Phase 6 (post-trade review surface — Mistake_Tags + Process Grade + mistake_cost_R / lucky_violation_R + Review_Log) FIRST after Phase 5, re-evaluate before committing to Phase 7's larger schema disruption (state machine + Fills first-class). Pyramiding R-views, Setup_Playbook DB entity, and Screen_Definitions versioning explicitly DROPPED as platform-mismatched. TOS reconciliation depth bundle (close-price + stop + position-qty mismatch detection) sits queued standalone OR can subsume into Phase 9 when sequenced.

**File rename decision (2026-05-01).** Operator surfaced the irony that `docs/phase3e-todo.md` now tracks Phases 6-9 too. Decision: leave filename to preserve ~46 cross-references in shipped briefs; retitle the H1 to `# Cross-Phase Operational Backlog` with explanatory note. Filename is path-stable; readers see the canonical title.

**Operational branch outcome data: n=1 as of 2026-04-25 (durable state, preserved across in-flight resets).** First closed trade (VIR, -$2 / -0.33R loss; framework-recommended in `watch` bucket on 2026-04-20; failed VCP-layer criteria `proximity_20ma` (+11.69% to +15.38% above 20MA — extended) AND `tightness` (0-day streak — base not formed); operator entered ~5% above recommended pivot; tightened stop mid-trade; clean stop-hit exit at -0.33R). hypothesis_label backfilled 2026-04-25 to "sub-A+ VCP-not-formed test (proximity_20ma + tightness fails); inaugural trade test". **Per-criterion analysis confirms the framework correctly flagged this as watch (not A+) for the right reasons** — tightness is a doctrine-incompatible miss (base not yet formed), so VIR was NOT a near-A+ defensible candidate per Finviz study D1 frozen set. Outcome played out as the framework would predict; -0.33R loss is the cost-of-development for an evidence-gathering trade test. Production DB is source-of-truth for trade outcome details. Statistically nothing; case-study informative.

**Active operator constraints (refined 2026-04-25):**

- Solo developer, ~4–8 hrs/week sustained (V2.1 §III.7 time-budget anchor); operator confirms time is non-binding for current workflow scale.
- Operator capital: $7,500. **Capital tie-up is the primary forward constraint** (~50 trade/year ceiling at full deployment under Minervini sizing; ~14% per position × ~5 concurrent × ~10 cycles/year). At Minervini-typical expectancy, ceiling translates to 20-40% annual return — well above operator's 10% target.
- Universe: SPX+NDX baseline; S&P 1500 adoption decision pending operator call.
- Pipeline cadence: daily; no trade-execution friction.
- Manual chart-pattern review: non-binding for throughput at current capital ceiling; **chart-pattern algorithm is for ENCODING qualitative input into structured feedback-loop data, not for throughput acceleration.**
- A+ identification rate on operator's actual Finviz pool: order-of-magnitude ~40-100/year per Finviz-pool study extrapolation + Session 2a anchor (NOT the ~2/year baseline I had been using; that was on broad SPX+NDX universe, not operator's filtered pool).

---

## Recent decisions and framings (don't re-litigate)

These have been settled with the developer's explicit approval. Don't reopen unless they ask.

- **Bifurcated architecture per V2.1.** Settled.
- **Three-branch refinement** (Operational, Applied Research, Basic Research). Settled 2026-04-24.
- **V2.1 Addendum additions adopted in V2.1** (time-budget anchor, demotion pathway, source-of-truth correction protocol). Merged in place.
- **Adversarial review on every code-shipping session** (standing convention). Adopted after Tranche B-ops Session 2 demonstrated value (caught the `open_risk_position_count` bug). Implementer invokes `copowers:adversarial-critic` on the combined diff after task commits land; iterates to `NO_NEW_CRITICAL_MAJOR`; fixes findings in a new commit per no-amend rule.
- **Path A vs Path B framing for QuantEcon program.** Path A = practitioner extension; Path B = quant-rigor overlay. Path B gated on operational system producing above-market returns. Path B is QuantEcon program; not actionable until V2.1 tranches mature AND operational system is proven.
- **Operator-drives, agent-serves principle.** Operator defines problem framing; agent provides knowledge-retrieval + execution. The recursive crowding concern about AI-aided development is narrowed by this discipline.
- **Pre-registration discipline for research studies** (Session 2c). Decision tiers + thresholds committed before viewing data.
- **Survivorship-bias interpretation protocol** (study-level amendment). Absolute metrics treated as lower bound; relative metrics as direction-trustworthy-magnitude-uncertain; weak/null signals → defer not reject.
- **Filter-rule activation-rate sanity check** (forward-looking lesson from Session 2c). Future filter-rule studies pre-register a sanity check that the most aggressive variant must filter ≥10% of signals.
- **Tranche 2 of V2.1 §X satisfied formally by Session 2c's defer outcome.** Discipline held; question-level inconclusive. Bifurcated architecture proven end-to-end.
- **Russell 3000 as primary broader-universe variant** for the candidate-sparsity diagnostic; S&P 1500 as fallback if sourcing has friction.
- **(2026-04-25) Capital-sensitivity finding interpretive disposition: informational, not workflow-changing.** Diagnostic showed structural risk_feasibility blocking shrinks 18.6% → 1.3% on SPX+NDX (and 6.9% → 0.3% on Russell) when capital scales 1× → 5×, but the deterministic A+ count change (5 → 10 SPX, 112 → 123 Russell) doesn't reach workflow-relevance threshold. Operator's framing: "the amount of money available is the amount of money available; without proven history, doesn't make sense to raise capital 2 orders of magnitude to go from 5 months to 2.5 months per A+ candidate."
- **(2026-04-25) Next-move post-Tranche-C: parallel operational + applied-research parity check.** Continue operational work (Phase 3e items, `build_watchlist` mixed-anchor fix, etc.); when research branch reactivates, the cheapest applied-research follow-on is hypothesis 5 (production-vs-replay parity check) to investigate the residual ~50× gap between matrix's most-permissive cell and production rate.
- **(2026-04-25) Production-gating-aware instrumentation as standing pattern.** When instrumenting production logic for diagnostic measurement, mimic production's gating order, not criteria emission order. Caught as R1 Critical in candidate-sparsity diagnostic; would have made primary hypothesis appear 3.5× weaker than reality if uncorrected.
- **(2026-04-25) Hypothesis 5 closed.** Harness-vs-production parity check returned Tier 1 result on n=80 production candidates from eval_15 (action_session 2026-04-25). Drift between research-branch harness and production pipeline is NOT the explanation for the residual ~50× rate gap from Russell-3000-5× to Session 2a anchor. Bound on claim: parity verified at watch/skip classification level; A+ classification parity unverified empirically (n=80 produced zero A+ candidates).
- **(2026-04-25) Path 1 selected for residual-gap question.** Accept "anchor noise + universe selection" as the explanation for the residual ~50× rate gap. Session 2a's CI [0.137%, 1.806%] is consistent with true rates as low as 0.05%; combined with Finviz pre-screening narrowing the universe, the gap is at least partially expected. No further study commissioned. Hypothesis 6 (Finviz universe reconstruction) and hypothesis 4 (regime) remain available but not pursued under current capital constraints.
- **(2026-04-25) Bug 7 family confirmed closed in web layer.** Survey query (`grep -rn 'ORDER BY run_ts DESC LIMIT 1' swing/web/`) on the post-`77877c1` tree confirms every primary read path that joins candidates by ticker now binds via `pipeline_runs.evaluation_run_id` (FK-direct or heuristic). Class durably closed in this layer.
- **(2026-04-25) Framework framing.** The framework is what we are building, informed by Minervini and Disciplined Swing Trader as known-good priors. Research branches exist to test, refine, and — where evidence justifies — depart from those references. Doctrinal fidelity is not the constraint; evidence quality is. Implication: research-branch findings can propose framework changes (criteria modification, allowed-miss extension, bucket logic, universe choice); the V2.1 source-of-truth correction protocol governs the formal change process, but the framework is not bound by the references absent that process.
- **(2026-04-25) Evidence gap framing.** Current operational rate (~2 trades/year confirmed by operator; harness-derived ~2.5 A+/year on SPX+NDX 1×) is too low to produce trade-outcome data sufficient for framework evaluation. Research-derived rate-uplift candidates (universe broadening, allowed-miss extension, criteria refinement) are tools to escape the rate-vs-evidence recursive bottleneck. As of 2026-04-25, operational branch has produced n=1 trade outcome (VIR); the loop has begun to break but evidence accumulation will take time.
- **(2026-04-25) S&P 1500 universe expansion: Tier 2 — Mixed.** 2.39× rate uplift point estimate over SPX+NDX 1× baseline (Wilson CIs overlap, so the difference is suggestive not formally significant on this single window); 48.6% absent earnings data; clean sector + liquidity profile. Capital-fit sub-finding: risk_feasibility blocking is LOWER on mid-cap universes than large-cap at operator's actual capital, contrary to naive expectation. Operator-decision pending on whether to adopt the lever; not re-litigated absent new evidence.
- **(2026-04-25) Sub-A+ trading is in operator's actual practice.** VIR trade (2026-04-20) was framework-recommended in `watch` bucket — passed all 8 trend-template criteria, failed two VCP-layer criteria (`proximity_20ma` extended above 20MA; `tightness` zero-day-streak base not formed). Operator took it anyway as a "trade test" per entry notes. Practice precedes principle: the "willing to relax absolute A+ doctrine" framing post-dates the actual deviation. Future workflow discussion should treat sub-A+ trade-taking as a practice-supported reality, not a hypothetical. Specific category for this trade (now backfilled in `hypothesis_label`): "sub-A+ VCP-not-formed test (proximity_20ma + tightness fails); inaugural trade test."
- **(2026-04-25) Operational branch as evidence-generation surface.** Operator is willing to take hypothesis-tagged sub-optimal trades within risk discipline, treating losses as cost-of-development rather than investment loss. Each trade requires a frozen pre-trade hypothesis label (free-text initially via the `hypothesis_label` column shipped 2026-04-25). Pre-registration discipline applies — label is set at entry and frozen; outcome-driven re-labeling is anti-pattern. This shifts operational posture from "execute proven framework" to "execute candidate framework variations to generate evidence" — V2.1 promotion path running in reverse to escape the rate-vs-evidence bottleneck.
- **(2026-04-25) A+ identifications ≠ trades (statistical analysis discipline).** A+ is a production classification (bucket assignment); trade is an operator decision to enter a position. They should be analyzed as separate measurements; do not conflate. Operator's intent is to trade every A+ but that's not a hard rule, AND going forward trades will outpace A+ identifications because of hypothesis-tagged sub-A+ trade evidence collection. Statistical aggregations (rate, expectancy, etc.) should preserve the distinction.
- **(2026-04-25) Identification rate recalibration.** A+ identification rate on operator's actual Finviz pool is plausibly ~40-100/year per Finviz-pool study extrapolation (8-day snapshot produced 2 unique A+ decisions on 6 distinct CSV-days). NOT ~2/year. The earlier "2/year" framing came from candidate-sparsity diagnostic on broad SPX+NDX universe; Finviz pool is a much tighter pre-filter that implicitly enforces TT criteria, raising the conditional A+ rate substantially. The "2/year" framing is retired.
- **(2026-04-25) Binding constraint is capital tie-up, not identification rate.** Capital ceiling at $7,500 × ~14% per position × ~5 concurrent × ~10 cycles/year ≈ 50 trades/year. With identification volume ~40-100 A+/year + ~470/year near-A+ defensible candidates from Finviz study, candidate volume already exceeds throughput capacity. Time, pipeline cadence, manual chart-review, and trade-execution friction are all non-binding at current scale. Implication: rate-uplift levers (universe broadening, allowed-miss extension) matter less than I previously framed; operational use of existing infrastructure matters more.
- **(2026-04-25) Chart-pattern algorithm is for encoding, not throughput.** Operator's manual chart assessment is fast enough to saturate capital today. The algorithm's value is structuring the qualitative chart-pattern dimension of trade decisions into the feedback-loop's analyzable data. Without it, hypothesis-label free-text absorbs chart-pattern info qualitatively (interim solution). When chart-pattern algorithm exists, it becomes a structured field; hypothesis-label holds remaining qualitative dimensions. Chart-pattern algorithm is important but NOT urgent; multi-session copowers cycle when ready. Phase 3e §3e.6 captures the original scope.
- **(2026-04-25) Next-horizon priority: operational use of newly-built infrastructure, not additional development.** Hypothesis-label infrastructure ships; Finviz-pool study identifies near-A+ defensible candidates (SLDB, UCTT). The actually-urgent next move is operational — take hypothesis-tagged trades, accumulate evidence, let the feedback loop run. Watch-staging UI is a small operational change with immediate value. S&P 1500 adoption decision is operator-pending. Chart-pattern algorithm and other development work are not urgent.
- **(2026-04-25) 10% return target math.** With $7,500 capital, ~50 trade/year ceiling at full deployment, and Minervini-typical expectancy (~0.5-0.8% account return per trade), the math gives 20-40% annual return ceiling. The 10% target is well within range, not the ambitious target it was earlier framed as. Compounding + capital injection to ~$100K extends absolute returns linearly to $20K-$40K/year at the same percentage ceiling.
- **(2026-04-25) Hypothesis investigation plan v0.1 longer-horizon framing: implicit, revisit at first closure gate.** Operator and orchestrator agreed that beyond the 4 starting hypotheses (A+ baseline 20-sample target; near-A+ defensible extension 10-sample; sub-A+ VCP-not-formed 5-sample; capital-blocked smaller-position 5-10-sample), there is no longer-horizon plan, and that's the correct posture for now. Reasoning: data from the 4 starting hypotheses will inform what to test next; trade pace is unknown (3-18 months to fill all 4 targets); committing further hypotheses now risks document-worship anti-pattern OR vague aspiration. **Action: when the first hypothesis closes (whichever hits its target sample size or escapes via tripwire first; likely Sub-A+ VCP-not-formed at 5 samples or A+ baseline at 20), explicitly revisit the longer-horizon planning question with operator.** Decision-cadence framing (Phase A: collect; Phase B: review-and-iterate at first closure; Phase C: longer-horizon strategic review at multi-hypothesis closure) is captured here for that future moment but not yet committed as a roadmap document.
- **(2026-04-25) Hypothesis investigation plan v0.1 OPERATIONAL.** Migration 0008 seeded 4 hypotheses (A+ baseline 20-target; Near-A+ defensible: extension test 10-target; Sub-A+ VCP-not-formed 5-target; Capital-blocked: smaller-position test 10-target). Tripwire values per-hypothesis: consecutive-max-loss N (3 for n=5; 4 for n=6-10; 5 for n=11-20) AND absolute-loss 5% of starting equity. Per-hypothesis status mutation only via `swing hypothesis update --status --reason "..."`; tripwire/sample-size/decision-criteria are immutable post-data (require formal new migration). VIR is sample 1 of 5 in Sub-A+ VCP-not-formed. Dashboard surface and CLI pre-fill operational; operator can take hypothesis-tagged trades from Monday onward.
- **(2026-04-25) Prefix-label convention (operator-facing).** Hypothesis-label matching against the registry uses **case-insensitive PREFIX** match (not substring; chosen by hyp1 R1 to prevent double-counting). **When operator manually composes a `--hypothesis` value** (e.g., for off-pipeline trades or override of pre-fill), the label MUST start with the canonical hypothesis name (e.g., `"Sub-A+ VCP-not-formed test ..."`, NOT `"my custom test for sub-A+"`) for tripwire/progress aggregation to correctly attribute it. The CLI pre-fill emits matcher's `suggested_label_descriptive` which already follows this convention; manual labels are operator-responsibility.
- **(2026-04-25) Hypothesis-recommendation engine framing: dashboard PROPOSES, operator DISPOSES.** The dashboard's Hypothesis-driven recommendations panel is an active recommendation surface — not just a listing. It tells the operator "if you take this trade, it advances hypothesis X (currently N/M samples)." Operator validates the recommendation (chart pattern, risk, sector preference) and either takes the trade or declines. Tripwire-fired hypotheses surface visually (red row); operator evaluates whether to pause/escape via `swing hypothesis update --status paused --reason "..."`. Pre-registration discipline applies — hypothesis plan and tripwires are frozen at migration 0008; only `status` is mutable via CLI.
- **(2026-04-25) Entry discipline for hypothesis trades: wait for pivot (stop-buy at pivot).** All hypothesis-tagged trades enter at-pivot via stop-buy order (or stop-limit for higher-volatility names where slippage control matters). Do NOT chase more than ~1% above pivot; if price gaps significantly above pivot at open, skip that day. Rationale: (a) pivot is the framework's prescribed entry trigger; (b) entry-execution is a confound on per-hypothesis expectancy aggregation — pre-committing to at-pivot eliminates that confound from the per-hypothesis statistics. Applies to ALL four hypotheses uniformly, including Sub-A+ VCP-not-formed (where the "pivot" is somewhat placeholder since base isn't formed; entry-at-pivot is still the cleanest discipline). The framework's initial_stop discipline caps the downside if pivot turns out to be the day's high → pullback. Operator workflow: review hyp-tagged dashboard listings → manual analysis (chart pattern, sector, risk) → if proceeding, stop-buy at recommended pivot → accept that pivot-as-day's-high is a known failure mode budgeted by the initial_stop.
- **(2026-04-26) Watchlist sort uses four-key composite ordering.** Sort key: tag count DESC → tag precedence DESC (`A+`=4, `VCP✓`=2, `TT✓`=1, summed) → abs(% to pivot) ASC → ticker ASC for determinism. Hypothesis-recommendations table sort untouched per scope discipline (its existing prioritizer is hypothesis-aware: progress, target distance, tripwire). Operator decision recorded; revisit hyp-recs sort separately if needed.
- **(2026-04-26) `/prices/refresh` anchor consistency closed Bug-7 family in this layer.** The route's cache-prewarm path was using `MAX(run_ts) FROM evaluation_runs` (the pre-Tranche-C mixed-anchor pattern) while `build_dashboard` and `build_watchlist` use pipeline-eval-first via `latest_evaluation_run_id`. Session 2 R1 caught the divergence as part of sort-anchor consistency review; the route now consumes the same pipeline-eval-first anchor. Survey query (`grep -rn 'MAX(run_ts) FROM evaluation_runs' swing/web/`) confirms no remaining occurrences in the web layer. Class durably closed.
- **(2026-04-26) Phase 3 dispatch discipline: disjoint-task-partitioning, no worktree isolation initially.** Phase 2 execution surfaced internal subagent collision (see Lessons captured). Operator decision: try task-partitioning brief discipline first (cheaper, prevention rather than containment) before falling back to worktree isolation (heavier ceremony). Phase 3 brief specifies: each task assigned to exactly one subagent (multiple tasks per subagent allowed; task sets must be DISJOINT across agents); subagents must verify task deliverable doesn't already exist before starting; abort + report if it does. Worktree isolation is the fallback if collision recurs in Phase 3+.
- **(2026-04-26) Phase 4+ dispatch discipline: continue partitioning + ADD observable verification.** Phase 3 produced one rogue task duplicate (b080da9 + revert 132142c) despite single-subagent dispatch. Net code state was correct; noise bounded (~20% of chain vs Phase 2's ~30%). Operator decision: continue brief discipline; ADD observable verification — subagent MUST include `git log --grep="Task X.Y" --oneline` output in commit body BEFORE each task commit. Makes duplicate-detection observable to Codex review (Codex can flag commits whose body shows existing task implementation). If Phase 4 sees another rogue, escalate to worktree isolation in Phase 5+.
- **(2026-04-26) Review-fix commit message convention formalized.** Task implementation commits MUST include task ID (e.g., `feat(pipeline): Task 3.2 — ...`). Adversarial review-fix commits SHOULD include round + finding ID (e.g., `fix(pipeline): Codex R1 Major 2 — summary-log denominator semantics`). Format-only cleanup commits (ruff, comment-only) no task ID needed (e.g., `style(pipeline): ruff UP037 cleanup`). Phase 2 precedent (`115c96b`) and Phase 3 R2 M1 finding settled this. Captured in §"Binding conventions" below.
- **(2026-04-26) Observable-verification subject-only grep refinement.** Phase 4 surfaced false positives in the `git log --grep="Task X.Y"` rule when commit bodies cross-referenced future task IDs in narrative prose. Phase 5+ briefs adopt: `git log --pretty='%s' --grep='^[a-z]+\([a-z]+\): Task X.Y'` — subject-only, anchored to the conventional-commit prefix structure. Doesn't constrain commit-body prose. Single-subagent + observable-verification (Phase 4 evidence) successfully prevented rogue duplicates; worktree isolation NOT escalated.
- **(2026-04-26) Phase 4 scope-deviation acceptances (R1 Major 2).** Two scope deviations from Phase 4 brief §1 file allowlist were accepted as in-scope-by-spirit: (a) `.gitignore` modification for `.tmp-phase4/` — brief §5 mandated `pytest --basetemp=<gitignored-relative-dir>`; gitignore entry is the natural corollary. (b) `tests/pipeline/test_step_charts_classification.py` — Task 4.8 closed Phase 3's OPEN QUESTION on date deserialization by removing `.isoformat()` workarounds that became broken assertions once Task 4.0a parsed dates as `date` objects; downstream consumer of authorized Task 4.0a carve-out extension. **Pattern for future briefs:** when a brief authorizes a Phase 2 carve-out extension touching upstream behavior, downstream tests of the modified file are naturally in-scope; brief should explicitly list them OR brief should explicitly say "downstream tests of carve-out file are in-scope by extension."
- **(2026-04-26) Base-layout 5-VM rule scope.** CLAUDE.md gotcha applies only when `base.html.j2` dereferences the new field. Phase 4 brief blanket-required all 5 VMs to gain `pattern_tags`; implementer correctly scoped to 2 (`WatchlistVM`, `DashboardVM`) per spec §3.5 because flag-tag rendering lives in `partials/watchlist_row.html.j2` (consumer-scoped). Spec scope was narrower and correct. **Future briefs:** don't blanket-require the 5-VM rule unless `base.html.j2` is actually being touched. Verify the gotcha applies before requiring its mitigation.
- **(2026-04-26) Phase 6 mathtext fix landed as standalone tiny commit (`2fd0ecc`).** Phase 6 visual verification surfaced a pre-existing matplotlib mathtext quirk in the chart title format (`pivot $110.00 stop $95.00` — paired `$..$` triggered math mode, italicizing "stop"). Pre-existing in legacy title format; Phase 6 didn't introduce it but every Phase 6 chart shipped with the artifact. Operator approved as standalone tiny commit before Phase 7 dispatch (Q1, post-Phase-6 triage). Fix: `\$` escape in raw f-string; matplotlib renders `\$` as literal `$` glyph without entering math mode. Test assertions updated to match (`r"... pivot \$110.00 ..."`).
- **(2026-04-26) Internal-Codex `(internal)` qualifier convention** (Q2, post-Phase-6 triage). Phase 6 surfaced commit-message tag collision: subagent-dispatched internal-Codex round (`c215c79`) and orchestrator-wrapper Codex round (`13d0deb`) both labeled "Codex R1 Major 1." Convention: internal-Codex commits append `(internal)` qualifier; orchestrator-Codex commits stay as-is. Subject-only grep regex still matches both forms (when the grep is invoked correctly per the 2026-04-27 ERE refinement below). Captured in §"Binding conventions" above. Phase 7+ briefs adopt the convention.
- **(2026-04-27) Subject-only grep regex amended to ERE + POSIX digit class** (Q1, post-Phase-7-implementer triage). Phase 7 implementer-side discovered empirically that the convention's regex `^[a-z]+\([a-z]+\): Task X.Y` returns empty under git's default BRE because `+` is literal in BRE (not a quantifier) and `\d` is a Perl-style class POSIX doesn't recognize. The "expected empty" output then matched for the wrong reason — the regex was non-functional. Amended convention: subagent invokes `git log -E --pretty='%s' --grep='^[a-z]+\([a-z]+\): Task X.Y'` with the `-E` flag (ERE mode); for Codex round labels, use `[0-9]` instead of `\d`. Captured in §"Binding conventions" above. **Lesson worth surfacing in past briefs:** prior phase briefs that included the BRE-incompatible regex were technically non-functional; the discipline still worked because (a) the implementer typically reads commit subjects directly anyway, and (b) ZERO-rogue track record was real (other partitioning rules carried the load). Going forward, briefs use the ERE form.
- **(2026-04-27) Manual verification round 1 complete; Tier-1 mathtext fix dispatch queued.** Operator + orchestrator walked verification doc step-by-step; ~75% coverage achieved; ONE production regression confirmed (mathtext title fix #1) + 3 UX gaps + 2 design questions + 7 doc fixes surfaced. Findings consolidated in `docs/chart-pattern-flag-v1-manual-verification-results.md`. Operator instruction during walkthrough: "do not take action on feedback until we finish or hit a significant blocker" — discipline held; all findings logged as observations, none fixed mid-walkthrough. Tier-1 mathtext fix dispatched ahead of Task 7.3 fixture labeling (legible chart titles needed during labeling work). All other findings tier-prioritized for post-V1 backlog OR natural-workflow exercise.
- **(2026-04-27) Phase 7 FP/FN aggregator: deferred to operator-manual classification** (Q2, post-Phase-7-implementer triage). Spec §3.1.4 + §4.2 require FP-biased tuning at Task 7.4 checkpoint based on FP/FN tally over labeled fixtures. Phase 7 implementer-side did NOT add an automated session-scoped FP/FN aggregator — operator does manual classification from pytest output (`pytest -v` reports per-fixture PASS/FAIL with label + actual pattern in the error message). ACCEPTED for V1 personal-use scope; if Task 7.4 reveals operator needs aggregation tooling, add as a small standalone follow-up. Captured in `docs/phase3e-todo.md` Phase 7 → operator handoff items.
- **(2026-04-26) Chart-pattern shape estimator V1 scope locked (six decisions).** Brainstorm output at `docs/superpowers/specs/2026-04-26-chart-pattern-flag-v1-design.md` (commit `081f689`). Locked: (1) **One pattern only — `flag_pattern`**; other patterns (pennant, base, cup-handle, tight channel) are V2+ additions. (2) **Governance: display + persist on trades + confidence metric; production scoring/bucketing UNTOUCHED.** Promoting any aspect to production decision logic requires V2.1 §VII.F. (3) **Compute timing: pipeline-time on chart-scope tickers** (extends `_step_charts`, shares in-hand OHLCV — zero scope expansion). (4) **Display surface: watchlist rows + trade-entry form + chart overlay.** (5) **Trade-entry consumption: cached-only.** Out-of-chart-scope manual trades have no override surface in V1. (6) **Operator override: algo and operator values stored separately** on trade row (4 columns: chart_pattern_algo, chart_pattern_algo_confidence, chart_pattern_operator, chart_pattern_classification_pipeline_run_id audit anchor). Effective-pattern-for-analysis = COALESCE(operator, algo). Algorithm approach: rule-based geometric (deterministic; 11 gates; min-of-clearances confidence). Watchlist tag rendering: parallel `pattern_tags` VM field — `_sort_watchlist` byte-for-byte UNCHANGED (sort-neutrality structurally guaranteed). Spec passed 5 adversarial Codex rounds (22+ findings dispositioned).
- **(2026-04-25) Vocabulary for confirmed hypotheses: "promoted" (per V2.1 promotion-path).** A hypothesis whose investigation closes positively (target sample met AND decision criteria evaluated positive AND operator decides to retain as ongoing recommendation surface) is **promoted**, not "lemma" / "validated" / "confirmed" / other generic terms. Reuses V2.1 three-branch promotion-path language (Basic Research → Applied Research → Operational; here: under-investigation → promoted-to-operational). Behavioral implications of `promoted` status: (a) continues to appear on dashboard recommendation surface but with different visual treatment (no "N/M progress" since target was met; instead lifetime stats like "promoted; lifetime: N trades, mean R X.XX"); (b) tripwire stays armed — if performance degrades post-promotion, the tripwire surfaces it for operator re-evaluation; (c) per-trade attribution continues so we can monitor for degradation. **Implementation deferred until first closure approaches:** a future small migration `0009_hypothesis_status_promoted.sql` will add `'promoted'` to the `hypothesis_registry.status` CHECK constraint enum (currently `active`/`paused`/`closed-escaped`/`closed-target-met`). Pre-registration discipline preserved — status enum widening is a formal migration, not a CLI-mutable operation.
- **(2026-04-27) Orchestrator-vs-implementer brainstorm-pattern decision.** For substantive design work (≥3 architectural decisions of medium complexity OR spec output ≥500 lines OR orchestrator context approaching 60%+), DISPATCH the brainstorming workflow to a fresh implementer instance rather than running it in the orchestrator thread. Reasoning: spec-by-construction self-contained (implementer has no conversation history to leak in); discipline isolation (single-role focus on the rigid brainstorming workflow); orchestrator context preserved for cross-dispatch triage; pattern parity with writing-plans + executing-plans (also implementer dispatches). For smaller-scoped brainstorms (1-2 sections, well-bounded scope), orchestrator-thread is more efficient (avoids ~10-15 min context-reload overhead). The chart-scope policy v2 brainstorm (~6 sections, schema migration + signature change + policy redesign) was borderline — finished in orchestrator-thread because we were ~75% done when the question came up. Future substantive brainstorms default to dispatch.

---

## Operating processes

### Brief drafting

Briefs live in `docs/` with naming pattern `{tranche-name}-{session-name}-brief.md`. Examples: `tranche-a-brief.md`, `tranche-b-ops-session-2-brief.md`, `tranche-c-pipeline-linkage-brief.md`.

A brief MUST include:

- Audience statement ("Fresh Claude Code instance with no prior conversation context").
- Mission paragraph.
- Expected duration estimate.
- §0 "Read first" — list of files the implementer must read, with rationale.
- §0 "Skill posture" — which superpowers/copowers skills to invoke or NOT invoke.
- Strategic context section (compressed; what they need that isn't in linked references).
- Scope section with explicit "out of scope" sub-list.
- Binding conventions (commit style, no-amend, TDD, test discipline, etc.).
- Per-task specifications with acceptance criteria.
- Adversarial review section (target + watch items).
- Done criteria.
- Return report format.
- "If you get stuck" section.

Briefs typically run 200–500 lines. Tight is better than padded. Self-contained — the implementer should be able to execute end-to-end from the brief + linked references without your conversation context.

### Paste-ready initial prompt

Always provide alongside the brief. Format:

```
You are dispatched as the {session-name} implementer for the Swing Trading project in this repo.

Step 1 — Read `docs/{brief-filename}.md` in full. {one sentence on what's in it}.

Step 2 — Read the references §0 of the brief points at, particularly {key references}.

Step 3 — Execute the brief directly. {Skill posture summary; key disciplines; key out-of-scope reminders.}

Step 4 — Produce the return report per §{N} of the brief as your final message.
```

### Triage of return reports

When a return report comes back, triage in this order:

1. **Verify the work matches the brief.** Commits landed? Tests green? Adversarial review verdict?
2. **Validate the substantive decisions.** Did the implementer make sensible calls on judgment-call items?
3. **Triage adversarial-review findings.** Each ACCEPTED-with-rationale finding deserves a sentence of acknowledgment; each FIXED finding deserves verification that the fix is correct.
4. **Flag follow-ups.** Items the implementer flagged for future work go to `docs/phase3e-todo.md` or appropriate backlog at the next housekeeping opportunity.
5. **Capture lessons.** Process insights go into this file or into memory.
6. **Propose next moves.** Don't decide unilaterally; offer options with recommendation.

### Bug-fix briefs and operator-confirmation gate

For bug-fix briefs where the mechanism is not yet diagnosed (scope: "Investigation comes first; fix comes second"), require an explicit **operator-confirmation gate** between the investigation phase and the fix phase. This prevents the failure mode demonstrated by Bug 2 (2026-04-25) — implementer assumes a plausible mechanism, builds a fix that's internally correct but addresses a different path than the one operator is hitting.

**Brief language template (insert between investigation phase and fix phase):**

```
### Investigation-phase operator-confirmation gate (before §X fix phase)

Before designing the fix, draft a "mechanism candidate" message back to
the operator containing:
  - The mechanism you believe is causing the bug
  - The reproduction sequence you used to confirm it (concrete steps)
  - Specific evidence (network trace, response body, DOM state, etc.)
  - Explicit confirmation request: "Does this match what you see?"

Wait for operator confirmation before proceeding to design the fix. If
the operator says "that's not what I see," repeat investigation with the
new information. Do NOT design the fix until the mechanism is
operator-confirmed.
```

For FIX-DIRECT briefs (known mechanism, specified fix), the gate is not needed.

**Adversarial-review watch items for ALL bug-fix briefs:**

Standing watch items the brief should pass to `copowers:adversarial-critic` for any bug-fix dispatch:

- "Did the investigation empirically reproduce the operator's EXACT symptom (not a plausible-but-different mechanism that produces a similar-looking failure)?" Bug 2 (2026-04-25) demonstrated this failure mode.
- "Did the fix address the root cause, or only the surface symptom?" Sometimes the symptom can be made to go away without understanding why — that's brittle and likely to recur.
- For UI bugs specifically: the project lacks a JS test harness (see `docs/phase3e-todo.md` JS-execution test harness gap). String-match assertions on rendered HTML confirm structure but NOT runtime JS behavior. Operator manual verification is the actual confidence source for JS-behavior fixes; document the verification steps in the return report.

### Housekeeping commits

Periodically, accumulate untracked drift + small backlog updates + minor documentation corrections into a single small "housekeeping" commit. Pattern: `docs/{phase}-housekeeping-brief.md` brief; one-session implementer work; landing commits like `docs: track {description}`. Examples shipped: `4f74493` (Tranche B cleanup), `b03f66a` (B-ops cleanup), `2df7adb..6c179de` (post-2c housekeeping).

**Don't let housekeeping accumulate.** When 5+ untracked artifacts exist or 3+ small follow-ups have been deferred, dispatch a housekeeping commit.

---

## Binding conventions (project-wide)

These come from CLAUDE.md but are restated here because you'll be drafting briefs that enforce them:

- **Branch:** `main`. No feature branches.
- **Commits:** conventional-commits. **No Claude co-author footer. No `--no-verify`. No amending.** Commit-message conventions (formalized 2026-04-26):
  - **Task implementation commits** MUST include task ID: `feat(area): Task X.Y — <description>`. Partitioning-prevention surface; makes duplicate-task detection trivial.
  - **Adversarial review-fix commits** SHOULD include round + finding ID: `fix(area): Codex R1 Major 2 — <description>` or `test(area): Codex R3 Minor 1 — <description>`. Audit-trail surface; not partitioning-relevant.
  - **Internal-Codex review-fix commits** (subagent-driven within-task review BEFORE orchestrator-wrapper Codex round) use the `(internal)` qualifier: `fix(area): Codex R1 Major 1 (internal) — <description>`. Distinguishes from orchestrator-wrapper Codex commits without breaking the subject-only grep regex (matches both forms when invoked correctly per the regex syntax note below). Phase 6 surfaced the disambiguation need (commit `c215c79` was internal-Codex; `13d0deb` was orchestrator-Codex; both labeled "Codex R1 Major 1" caused audit confusion).
  - **Subject-only grep observable verification** (regex amended 2026-04-27 post-Phase-7-implementer Q1): subagent invokes `git log -E --pretty='%s' --grep='^[a-z]+\([a-z]+\): Task X.Y'` (ERE flag `-E` + POSIX digit class) BEFORE each task implementation commit. **The `-E` flag is required** — git's default Basic Regular Expression mode treats `+` as a literal character, not as a quantifier; without `-E`, the grep silently returns empty even when matching commits exist (the "expected empty" output then matches for the wrong reason). For matching Codex/internal-Codex round labels, the canonical regex form is `^[a-z]+\([a-z]+\): Codex R[0-9]` (POSIX `[0-9]` instead of `\d` because POSIX regex doesn't support Perl-style character classes). Phase 7 implementer-side discovered the BRE incompatibility empirically (commit chain `528d38b..ca66216`); convention amended to specify ERE + POSIX explicitly.
  - **Internal code-review fix commits** (per Phase 5 precedent) use `code-review` prefix: `fix(area): code-review I1 — <description>` or `fix(area): code-review T6.1 — <description>`. Distinguishes from Codex review by review source.
  - **Format-only cleanup commits** (ruff, comment, whitespace) no task ID needed: `style(area): ruff UP037 cleanup`.
- **TDD:** failing test first, minimal implementation, pass, commit. One red-green cycle per logical change.
- **Phase isolation:** during Phase 3 work, `swing/trades/` and `swing/data/` are read-only unless an explicit carve-out is granted in the brief. Carve-outs require justification and listing of specific files touched.
- **DB location:** `%USERPROFILE%/swing-data/swing.db` — outside the Drive-synced folder. Never violate this; SQLite + Drive sync = corruption.
- **Tests:** `python -m pytest -m "not slow" -q` is the fast suite; must stay green. Slow suite (`-m slow`) is network-dependent; don't require it for routine validation.
- **Ruff:** `ruff check swing/` baseline is 18 errors (E501 only) as of the 2026-05-10 polish-bundle ship (`efd3e15` cleared 8 N818 via mechanical exception-class rename batch; `9c9b57c` had previously brought the broader baseline 78 → 26 — see `docs/phase3e-todo.md` 2026-05-10 entry for residual breakdown). Earlier baselines for historical reference: 26 post-sweep / pre-N818; 98 pre-2026-05-10 sweep; 91 pre-Phase-5; 81 pre-Phase-7 (drift between historical anchors was pre-existing-legacy or ruff-version drift, not introduced by dispatches). Briefs forbid introducing new violations; the residual 18 E501 are explicit banked-for-bundling items per the phase3e-todo entry, not free territory for incidental fixes outside the bundle. Verify via `ruff check swing/ --statistics` not `wc -l` (default ruff output is multi-line per violation).
- **Adversarial review** on code-shipping sessions is mandatory (standing convention).
- **Executing-plans dispatch convention (formalized 2026-05-02 post-Phase-5).** Direct invocation of `superpowers:subagent-driven-development` followed by `copowers:adversarial-critic` (NOT the `copowers:executing-plans` wrapper, which bundles both phases without marker-file management between them). Workflow: (1) `superpowers:using-git-worktrees` to create isolated worktree (REQUIRED per `subagent-driven-development` skill docs); (2) `touch .copowers-subagent-active` to activate the global PreToolUse Codex-blocking hook (`~/.claude/hooks/block-copowers-during-subagent.sh`, registered in `~/.claude/settings.json`) which physically prevents subagents from invoking `copowers:adversarial-critic`, `copowers:review`, or `mcp__plugin_copowers_codex__codex*`; (3) invoke `superpowers:subagent-driven-development` to execute tasks; (4) `rm .copowers-subagent-active` after all tasks complete; (5) invoke `copowers:adversarial-critic` directly with PHASE/SPEC_PATH/PLAN_PATH/BASELINE_SHA; (6) operator-witnessed verification gate; (7) merge worktree to main. Hook is global (active for ALL Claude Code sessions across ALL projects on this machine); subagents physically cannot bypass. See `docs/phase5-configuration-page-executing-plans-brief.md` (`671451f`+, revised) as the canonical brief template for this workflow.
- **Worktree + editable-install verify-command (formalized 2026-05-02 post-Phase-5).** When a worktree-isolated dispatch needs runtime/browser verification via a CLI entry point (e.g., `swing web`), the verify-command MUST point at the worktree's package, not the editable-install path. PowerShell: `$env:PYTHONPATH = "."; python -m swing.cli web` from inside the worktree dir. Bash: `PYTHONPATH=. python -m swing.cli web`. Pytest is not affected (cwd-based discovery); CLI entry points ARE affected (editable-install resolver). Specify in any brief that uses worktrees + needs runtime verification.
- **Worktree directory path MUST be `.worktrees/<branch>/` at repo root (formalized 2026-05-09 post-polish-bundle-ship + cleanup-script extension).** Worktree-isolated dispatch briefs MUST specify the worktree directory path explicitly in §3 binding conventions OR §8 dispatch metadata. Required path: `.worktrees/<branch>/` at repo root — NOT `.claude/worktrees/<branch>/` (the `superpowers:using-git-worktrees` skill default). Rationale: `.worktrees/` is the project-precedent location aligned with the elevated-cleanup script `cleanup-locked-scratch-dirs.ps1` (which scans both paths as of 2026-05-09 but `.worktrees/` is the canonical naming) AND aligned with Phase 5/6/7/8 ship history (cleanup recurrence patterns + ACL-handling discipline). The 2026-05-08 lesson on worktree directory path discipline is now elevated to a binding convention; new briefs MUST include the explicit path. Failure mode (from Phase 8 V1 polish dispatch 2026-05-07): brief specified branch name only; implementer's `using-git-worktrees` invocation chose `.claude/worktrees/phase8-v1-polish/` per skill default; husk required separate `git worktree remove --force` to clean up because cleanup-script targeting was `.worktrees/` only. Cleanup-script extension landed 2026-05-09 covers both paths; brief discipline still required.

---

## Anti-patterns to avoid

These have caused real problems; resist the impulse:

- **Drafting briefs that reference "uncommitted" files without verifying current `git status`.** I made this mistake on the post-2c housekeeping brief (claimed Bug 7 in Bugs.txt was uncommitted; it had been committed by Session 2b's mid-session catch-up). Always verify before asserting tracking state.
- **Padding triage responses with structure that doesn't earn its space.** Headers + bullets + tables when a few sentences would do is noise. The developer reads carefully; respect their time.
- **Introducing new strategic framings without operator approval.** This is the operator-drives discipline. If you have a new framing in mind, present it as "want to discuss?" not as fait accompli.
- **Proposing action when only triage is needed.** Sometimes the right response is "clean session, standing by." Don't manufacture next steps just because the developer asked for triage.
- **Re-litigating decided framings.** The decisions in §"Recent decisions and framings" are settled. Don't reopen them unless the developer does.
- **Vacuous regression tests.** A test that passes under both pre-fix and post-fix code is worse than no test. Memory file `feedback_regression_test_arithmetic.md` captures the canonical example.
- **Mid-session scope expansion.** Bug-class issues discovered mid-session in OTHER surfaces should be flagged in return reports, not fixed inline. The pipeline-linkage session correctly flagged `build_watchlist` per this discipline.
- **Treating "diagnose, don't decide" as soft.** When a study or diagnostic is scoped as descriptive, sneaking in implicit recommendations through "should" framings or threshold suggestions violates scope. The reviewer will catch this; better to write the discipline in correctly the first time.
- **Re-fetching expensive data when it can be cached.** yfinance has rate limits. Diagnostic studies should use cache-warm patterns; never burn yfinance quota for re-runs of already-fetched data.
- **Brief internal inconsistency between "mirror canonical" and prose-asserted counts.** When a brief points the implementer at a canonical template AND prescribes an independent count of cases, the count must match the canonical OR the deviation must be called out explicitly. The build_watchlist mixed-anchor fix brief had §0 say "mirror canonical" (3 tests) and §4.1 say "add a second test" (2 implied); implementer correctly chose canonical-template fidelity but had to do judgment work I should have done at draft time. Always cross-check prose counts against any canonical references the brief points at.
- **Bug-fix investigation that tests plausible mechanisms instead of operator's actual reproduction.** Bug 2 (trade entry form vanishes mid-typing, 2026-04-25) demonstrated this failure mode: implementer interpreted operator's ambiguous symptom report ("when I adjust the price") as form submission with stop≥entry; built TestClient probe of POST /trades/entry with stop=entry; got 500 + bare-div response that gets hoisted out of `<tbody>` by HTML parser; declared mechanism identified; built fix for that path. Adversarial review approved because the fix was internally correct. Operator manual verification revealed the actual mechanism was different (sizing-hint span hx-target inheritance from parent form). First fix `04ef355→20d2cab` was correct for ITS mechanism but wasn't operator's bug; required follow-up `2a167d1` for the actual cause. **Pattern to avoid:** treating implementer's interpretation of operator's symptom as ground truth. For UI bugs especially: TestClient confirms server-side behavior but doesn't verify the bug fires through the path the operator is hitting. **Mitigation:** see §"Operating processes" §"Bug-fix briefs and operator-confirmation gate" — INVESTIGATION-FIRST bug-fix briefs must include an operator-confirmation gate between investigation and fix.

---

## Maintenance: retention discipline

> Added 2026-05-05 as part of the handoff-document structural separation refactor. This section governs ongoing curation of this file + `docs/phase3e-todo.md` to keep fresh-orchestrator bootstrap consumption bounded over time.

### Active vs archive boundary

Two pairs of handoff docs:

- `docs/orchestrator-context.md` (active; canonical filename) + `docs/orchestrator-context-archive.md`
- `docs/phase3e-todo.md` (active; canonical filename) + `docs/phase3e-todo-archive.md`

Bootstrap discipline: fresh-orchestrator session reads only the active files (this one + `docs/phase3e-todo.md`) + `CLAUDE.md` + `git log -20` + `git status`. Archive companions are searchable on demand via Grep / Read.

### Cooldown rules (when to migrate)

Migration triggers, in order of frequency:

1. **End of phase ship.** When a phase merges to main, migrate that phase's SHIPPED `phase3e-todo.md` entries to the archive. One-phase cooldown (don't archive the same phase that just shipped — give one ship-cycle of "still warm" context). Worked example: Phase 7 shipped 2026-05-05 at `c617777`; Phase 6 SHIPPED entries (Phase 6 sub-bundle of journal v1.2) migrated to archive at this dispatch. Phase 7 SHIPPED entry migrates to archive at end of next phase ship (Phase 8 or Phase 9, whichever lands first).
2. **Lessons-captured cap.** Active `Lessons captured` section maintains last ~30 entries. When a new lesson lands and pushes the count past 30, the oldest lesson migrates to archive (or promotes to CLAUDE.md if it's durable code-failure prevention; see below).
3. **Recent-decisions supersession.** When a "Recent decisions and framings" entry is fully superseded by a later decision, archive the old entry with a cross-ref to the superseding one. When refined (not fully superseded), keep both with cross-ref. Default-conservative: when in doubt, KEEP in active. Re-litigation risk dominates.
4. **Tripwire-fired entries.** When a trigger-gated `phase3e-todo.md` entry resolves (e.g., 2026-05-04 worktree-cleanup-script entry RESOLVED 2026-05-05), migrate to archive with `TRIGGER FIRED + RESOLVED YYYY-MM-DD` footer. Mirror the worktree-cleanup-script + handoff-document-growth precedents.

### Lesson promotion: archive vs CLAUDE.md

For lessons being aged out of the active section:

- **Promote to CLAUDE.md gotchas** if the lesson durably prevents code failure for future sessions (e.g., yfinance API regressions; HTMX failure surfaces; Windows ACL gotchas; SQL/Python idiom collisions; matplotlib mathtext quirks). CLAUDE.md is auto-loaded so the gotcha fires every session, not just when grep'd.
- **Archive (no promotion)** if the lesson is process-only / single-incident-historical / discipline-applied-to-orchestrator-thread (e.g., commit-message convention refinements; subagent-collision diagnosis history; specific phase-handoff items).

Most older lessons (the 96 archived 2026-05-05) fall into "archive only" — their durable code-failure prevention has already landed in CLAUDE.md gotchas. Re-promotion would duplicate.

### Archive-split trigger (hierarchical decomposition deferred)

If an archive file exceeds **~80k tokens** (mirrors the active-file pressure point that motivated the original split), revisit hierarchical decomposition. Likely categorization at that point: SHIPPED-by-phase + lessons-by-domain (HTMX / yfinance / Windows / SQLite / dispatch-discipline / brief-drafting) + decisions-by-quarter. Defer category invention until the trigger fires — data informs categories better than upfront design.

### Invocation cadence

- **At session-end after each ship:** quick check whether SHIPPED items in `phase3e-todo.md` should be migrated; whether the lessons cap has been exceeded; whether any "Recent decisions" have been fully superseded.
- **Operator-explicit:** operator can request retention-discipline pass at any time (e.g., "do a housekeeping pass on the handoff docs").
- **Quarterly or longer:** revisit the active-vs-archive boundary holistically; archive entries that are now firmly historical even if they didn't fire a per-incident trigger.

### What this section does NOT govern

- The structural-separation refactor itself (one-time work; SHIPPED 2026-05-05).
- Removing content from `CLAUDE.md` (that file is curated by its own discipline; project-conventions + gotchas are append-mostly).
- Memory entries (`MEMORY.md` index + per-memory files at `~/.claude/projects/.../memory/`); those have their own retention semantics.

---

## Lessons captured (with cross-references)

> **Cap:** ~30 most-recent entries retained here; older lessons migrate to `docs/orchestrator-context-archive.md` per §"Maintenance: retention discipline" above. As of 2026-05-09, 119 older lessons have been archived (96 baseline + 18-entry post-Phase-8-ship batch + 5-entry post-2026-05-09-session batch covering Phase 6 cfg-cascade / closed_date-derived / plan-template-fixture-defects / Python-`or ""`-vs-SQL-CHECK / test-count-projection-bias lessons; Phase 6 + Phase 7 era lessons; chart-pattern flag-v1 Phase 1-7 lessons; Phase 5 first-attempt incident triage lessons; chart-scope policy v2 lessons; OHLCV archive consolidation lessons; sector capture; hyp-recs trade-prep expansion; Phase 4.5 hypothesis-label web-form gap lessons; etc.). Durable code-failure-prevention lessons (yfinance regressions; HTMX failure surfaces; Windows ACL gotchas; SQL/Python idiom collisions; matplotlib mathtext quirks; sqlite3 executescript implicit COMMIT; in_transaction safety-guard anti-pattern; server-stamping client-trust pattern) live in CLAUDE.md gotchas. Most-recent 30 retained below for active reference.

Process insights from this project's history, with examples and pointers:

- **Operator-architectural-pushback mid-gate triggers STOP-and-recover, not push-through.** Lesson from Phase 12 Sub-sub-bundle C.D operator-witnessed gate (2026-05-17; S3a CVGI tier-1 apply + S3b LION tier-1 apply pre-cursor). Mid-gate at S3b, after S3a wrote `fills.price = $5.30` for CVGI, operator pushed back: "$12.75 was the limit price; actual fill was $12.6999 per TOS — auto-correcting fills.price to $12.75 would CORRUPT the journal." Orchestrator pattern was to STOP, query operator's actual TOS data for CVGI (revealing limit-vs-fill $5.30/$5.2244 divergence applied to CVGI too), surface the architectural finding (Pass-1 tier-1 entry_price_mismatch inherits limit-vs-fill defect from Pass-2-tier-1-FORBIDDEN family), then recover via tier-3 `override-correction` (correction chain heads 3 + 4 + 6 restored fills to operator's TOS Net Price values $5.23 CVGI + $12.70 LION). Recovery path through the new C.D `override-correction` CLI surface, NOT direct DB edits or `acknowledged_immaterial` (per orchestrator-context anti-pattern). **Generalization:** when operator pushes back on a production-write surface output during an operator-witnessed gate, STOP + investigate the architectural assumption (don't trust that the implementer's code is correct just because tests pass — see "Synthetic-fixture-vs-production-emitter shape drift" CLAUDE.md gotcha for the test-passing-vs-production-shape gap); recover via the architecturally-correct tier-3 override-back path that preserves audit-trail integrity. **Brief-drafting checklist update:** for any production-write operator-witnessed gate, document expected output values up-front + verify against operator's source-of-truth (TOS, broker statement) per surface; surface architectural-assumption violations as orchestrator action items, not deferred. Pattern complement to existing "Operator architectural pushback supersedes orchestrator scope assumptions" lesson — same family applied to mid-gate production-write surfaces.

- **Production-write classifier soft-block fires PER-INVOCATION even after AskUserQuestion authorization.** Lesson from Phase 12 Sub-sub-bundle C.D operator-witnessed gate (2026-05-17; S3a + S3b + S4a + S4b + S6b + S6b.dup + S7a + S7b = 8 production-write invocations across the gate). AskUserQuestion-confirmed authorization with explicit preview-block enumeration of all 4 invocations (CVGI / LION / DHC / VSAT) was insufficient to authorize the classifier's per-invocation scope check — the classifier interpreted "yes" as scoped to specific KNOWN cases from the handoff brief (CVGI/DHC/VSAT) and BLOCKED the LION invocation as "agent-inferred scope expansion." Pattern is to surface for plain-chat operator "yes" per blocked-invocation, citing the specific command + reason. **Generalization:** AskUserQuestion authorization is sufficient for the auto-mode classifier ONLY when the authorized scope exactly matches the classifier's interpretation; for scope expansions, novel targets, or per-invocation production-writes, plain-chat operator "yes" per-invocation is the recovery pattern. **Brief-drafting checklist update:** for any operator-witnessed gate with N production-write invocations, expect N soft-block prompts; surface to operator with command + auth-rationale per invocation; do NOT batch-authorize via single "yes" assuming classifier will scope across all N. Pattern complement to existing "Production-write classifier soft-block: surface to operator for plain-chat yes confirmation if blocked" durable preference (`feedback_orchestrator_vs_implementer_execution.md`) — refines the pattern from "if blocked" to "expect to be blocked per-invocation."

- **Orchestrator-inline gate-fix is a durable Phase-12-arc pattern (3 cumulative instances).** Cumulative lesson from Phase 12 inline gate-fixes (2026-05-15 to 2026-05-17): Sub-bundle A `e2c0384` (T-A.3 schwab_client=None hardcoded wiring); Sub-bundle B `7b75d4a` (`/config` page nav link to `/schwab/setup` UX gap); Sub-sub-bundle C.D `a542f65` + `34d74f7` + `32812f7` (U+2192 → ASCII swap + synthetic-field_name audit-helper fix + UTF-8 stdout reconfigure). Pattern: operator-witnessed gate surfaces a defect that the implementer's pytest captured-stdout / mocked-fixture tests didn't catch; orchestrator inline-fixes on the worktree branch BEFORE integration merge, committed with `fix(<phase>): orchestrator-inline gate-fix #N — <description>` prefix; merged via the same `--no-ff` integration commit. Saves a separate dispatch cycle for what would otherwise be a follow-up bug-fix dispatch. **Generalization:** when an operator-witnessed gate surfaces a defect that is (a) clearly within the dispatched scope, (b) trivially fixable in <5 lines or via a localized helper change, (c) covered by an existing test pattern that just needs production-shape values — inline-fix on the worktree branch is appropriate. Threshold: if the fix requires schema changes, new modules, or new test fixtures, escalate to a follow-up dispatch instead. Brief-drafting checklist update: dispatch briefs MUST anticipate the inline-gate-fix possibility (`§4 OUT OF SCOPE` should NOT block trivial inline gate-fixes; clarify that "inline-fix at gate" is a recognized path for browser-only or stdout-encoding-only defects). Pattern complement to existing pre-Codex review + implementer self-report accuracy lessons — three-piece orchestrator-quality-gate family (pre-Codex review + return-report verification + gate-fix-then-merge).

- **Pass-1 tier-1 entry_price_mismatch inherits limit-vs-fill defect from Pass-2-tier-1-FORBIDDEN family (V2 mapper widening blocker resolved at C.D gate).** Lesson from Phase 12 Sub-sub-bundle C.D operator-witnessed gate S3a + S3b (2026-05-17; CVGI + LION both empirically falsified the Pass-1 "order/limit ≈ execution" operator-locked assumption). The V1 Schwab mapper at `swing/integrations/schwab/mappers.py:223-230` reads `order.price` (LIMIT or stop TRIGGER); reconciliation comparator at `swing/trades/schwab_reconciliation.py:693` compares against journal `f.price` (EXECUTION). When `limit ≠ execution` (slippage / VWAP / partial-fill aggregation), emits false `entry_price_mismatch` / `close_price_mismatch` that tier-1 auto-corrects to the LIMIT — CORRUPTING the journal. CLAUDE.md `Pass-2-tier-1-FORBIDDEN` gotcha AMENDED at C.D housekeeping commit `4bab6ee` to cover Pass-1 family. **V2 mapper widening priority BUMPED**: was operator-locked next-architectural-dispatch slot per OQ-4 + plan §I.1; today's gate evidence demonstrates the assumption breaks operationally + bumps from "queued" to "next dispatch unblocked + scope recommended." Pre-V2 operator workflow: `reconcile-backfill --apply` then manual TOS-audit + tier-3 `override-correction` on any wrong tier-1 corrections; the new C.D `override-correction` CLI is the operational safety valve. **Standalone post-Phase-12 dispatch scope** (operator-approved 2026-05-17): mapper extension to surface `orderActivityCollection[].executionLegs[].price` + classifier consumer updates (lift Pass-2-tier-1-FORBIDDEN) + reconciliation comparator update + backward-compat fall-through + T-B.7 `/schwab/status` web counterpart + 2 housekeeping micro-fixes (stale CVGI date attribution + historical audit row backfill OR leave-as-is operator-decision). Brainstorm dispatch pending operator commission. **Generalization:** when an operator-locked V2 assumption breaks operationally during a production-write gate, the gate evidence is sufficient evidence to bump V2 priority from "deferred" to "next dispatch unblocked"; document the empirical falsification cases in the brainstorm brief as binding context. Pattern complement to existing "V2.1 §VII.F amendment routing" lesson family — same family applied to V2-deferred architectural items where operator-locked assumptions are testable.

- **Schema-coverage constant ≠ manual-resolver allowlist — after widening a Python enum to mirror schema CHECK, audit every existing manual callsite that validates against the constant.** Lesson from Phase 12 Sub-sub-bundle C.C Codex R1 Major #4 + R2 Major #1 (2026-05-16; return report §10 lesson #1). When C.A widened the schema `resolution` CHECK 5→9 values, the C.B + C.C consumers correctly used the widened `_RESOLUTION_VALUES` for dataclass validation. But the existing Phase 9 Sub-bundle B `resolve_discrepancy` CLI surface validated operator input against `RESOLUTION_TYPES` (a SECOND Python-side mirror in the repo module); Codex R1 M#4 caught that this constant was still 5-value and lacked the 4 new service-owned states. Codex R2 M#1 caught the follow-up: even after widening `RESOLUTION_TYPES` to 9, the manual `resolve_discrepancy` CLI should NOT accept the service-owned states (`auto_corrected_from_schwab` / `pending_ambiguity_resolution` / `operator_resolved_ambiguity` / `operator_overridden`) — those route through canonical service entries (C.C `apply_*` functions). Resolution: introduce a separate tighter `_MANUAL_RESOLVE_ALLOWED_RESOLUTIONS` allowlist for the manual path with routing-hint substring in rejection error message. **Generalization:** when widening any schema-mirror Python constant, grep for ALL callsites; service-owned values that require additional invariants or routing through specific service entries get a SEPARATE tighter allowlist for the manual path. Pattern complement to existing "Schema-CHECK + Python-constant + dataclass-validator MUST land in the same task" C.A lesson — same family applied to MULTIPLE Python-side mirrors in different repo modules. **Brief-drafting checklist update:** for any dispatch that widens a CHECK enum or Python constant, enumerate every existing Python-side surface that validates against the schema-mirror constant + audit which surfaces are "manual" (operator-input) vs "service" (transitions through canonical service entries) + add a discriminating test per service-owned value asserting the manual surface rejects with routing-hint substring.

- **Outer transaction discipline UNIFORM regardless of sandbox — short-circuit MUST live in the inner (caller-tx) function, NOT the outer (own-tx) function.** Lesson from Phase 12 Sub-sub-bundle C.C Codex R1 Major #1 (2026-05-16; return report §10 lesson #2). Initial C.C implementation had `apply_tier1_correction(env='sandbox')` short-circuit at the OUTER function before opening `BEGIN IMMEDIATE`. Codex R1 caught two failure modes: (a) outer-skip bypasses the in_transaction caller-held-tx check (a caller holding an open tx + invoking under sandbox would silently no-op instead of raising `CallerHeldTransactionError`); (b) outer-skip lets nonexistent `discrepancy_id` succeed as a no-op (silent error — caller's bug not surfaced). Resolution: outer ALWAYS issues `BEGIN IMMEDIATE` → calls inner → `COMMIT` (or `ROLLBACK` on exception). Inner short-circuits sandbox cases internally (returns no-op `CorrectionResult(correction_id=None, notes='sandbox: ...')`). Discriminating test pattern: `_ExecuteSpyConn` wrapper that records `conn.execute` calls + asserts `"BEGIN IMMEDIATE"` was issued under sandbox + caller-held-tx still raises under sandbox. **Generalization:** transactional discipline (reject caller-held / BEGIN IMMEDIATE / COMMIT / ROLLBACK) MUST be uniform regardless of environment. Environment-specific behavior (sandbox short-circuit; production journal mutation) belongs inside the inner function or service-call boundary, NOT at the transactional entry point. Pattern complement to existing "in_transaction safety guard anti-pattern" Phase 8 lesson — both about transactional-boundary discipline; this lesson is about not skipping the boundary based on environment.

- **SELECT-first idempotency must precede payload validation — terminal-state discrepancy returns existing correction_id even with stale/malformed/None payload.** Lesson from Phase 12 Sub-sub-bundle C.C Codex R1 Major #2 (2026-05-16; return report §10 lesson #3). Initial C.C `_apply_tier1_correction_inner` validated `classification` payload BEFORE SELECTing the discrepancy's current resolution. Codex R1 caught the failure mode: a terminal-state discrepancy (`resolution='auto_corrected_from_schwab'`) re-invoked with stale/malformed/`classification=None` would raise on payload validation instead of returning the idempotent existing-correction lookup. Resolution: reorder to (1) sandbox short-circuit (no state needed); (2) SELECT discrepancy + None-check; (3) terminal-state idempotent return; (4) payload validation; (5) remaining atomic flow. Discriminating test: invoke with terminal-state discrepancy + `classification=None`; assert existing `correction_id` returned + no exception raised. **Generalization:** for any service with an idempotency contract (terminal-state → return existing audit row WITHOUT new write), the SELECT + terminal-check MUST precede input-payload validation. Idempotency is about consistency on re-invocation; payload validation should only fire when state actually requires a new write. Pattern complement to existing service-layer discipline lessons (Phase 8 `record_event_log` + Phase 9 `supersede_active_policy` precedents) — same family applied to ordering within the inner-function steps.

- **Counter staleness after inline state mutation requires post-loop recompute.** Lesson from Phase 12 Sub-sub-bundle C.C Codex R1 Major #3 (2026-05-16; return report §10 lesson #4). The Phase 9 `run_*_reconciliation` flow emits discrepancy rows + increments `summary_json.unresolved_discrepancies_count` per emit. C.C's flow pivot then mutates those rows' states inline (`unresolved` → `auto_corrected_from_schwab` / `pending_ambiguity_resolution`) during the dispatch loop. The emit-time counter is now stale. Codex R1 caught the failure mode: post-pivot, `summary_json.unresolved_discrepancies_count` reflected pre-mutation emit-count not actual final state. Two resolution patterns work: (a) decrement counter per-mutation inside the loop (matches legacy `resolve_discrepancy` pattern); (b) recompute via `SELECT COUNT(*) WHERE resolution = 'unresolved'` post-loop (simpler + more robust; C.C R1 M#3 choice). **Generalization:** any flow that emits rows incrementing counters AND later mutates those rows' states (resolve / dispose / forward-to-another-state) MUST either decrement counters per-mutation OR recompute via SELECT COUNT post-loop. Discriminating-test pattern: plant N discrepancies (mix of tier-1 + tier-2 outcomes) → run pivot → assert run's counter equals actual post-pivot count via independent SELECT, NOT N. Pattern complement to existing "Service-layer with conn: opens its own transaction" Phase 8 gotcha — both about state-coherence-after-multi-step-flow discipline.

- **DRY helper extraction across pivot mirror sites with lazy import — when plan says "mirrors T-X verbatim" + the mirror is non-trivial (100+ lines), extract a private helper rather than duplicating.** Lesson from Phase 12 Sub-sub-bundle C.C T-C.6 design (2026-05-16; return report §10 lesson #5). Plan §D.6 stated "T-C.6 mirrors T-C.5 verbatim with TOS-CSV source semantics per OQ-2 PIVOT BOTH." Naive implementation would duplicate the 100+ line Step 2 classify+dispatch+savepoint pivot loop. Implementer correctly extracted `_pivot_classify_and_dispatch_for_run` as a private helper in `swing/trades/reconciliation_auto_correct.py` + consumed it from both `swing/trades/schwab_reconciliation.py` + `swing/trades/reconciliation.py` via **lazy in-function import** to break circular dependencies (auto_correct.py imports classifier; classifier doesn't import auto_correct; but schwab_reconciliation imports auto_correct lazily within the function body, not at module top-level). **Generalization:** for "mirrors verbatim" plan instructions where the mirror exceeds ~50 lines, extract a private helper at writing-plans phase OR at implementation phase. Lazy import is the standard pattern to break circular dependencies between service modules. Watch item: if the helper lives in module A but is consumed by module B (asymmetric direction), V2 candidate to relocate to a neutral utility module. **Brief-drafting checklist update:** writing-plans-author "mirrors X verbatim" instructions for non-trivial code should be reframed as "helper-extraction candidate" + the helper's module location decided up-front (preferred: neutral utility module).

- **Pre-Codex orchestrator-side review catches LOCK divergences cheaply — saves an estimated 1-2 Codex rounds per dispatch.** Lesson from Phase 12 Sub-sub-bundle C.C 2-Major-pre-Codex absorption (2026-05-16; return report §10 lesson #6 + SC-1 + SC-2 commits). Before invoking adversarial-critic, the orchestrator dispatched a focused reviewer subagent with the plan §D acceptance criteria + brief §0.5 BINDING contracts as anchors, asked for a deviation list ≤600 words. The review caught 2 Major findings: (a) **SC-1 sandbox-threading**: implementer claimed "SC-1 fix threaded `environment` through pivot + apply functions" but actual code did NOT thread `environment` into `_apply_tier1_correction_inner` per plan §D.5 step 3 LOCK — instead short-circuited the entire pivot block; (b) **SC-2 T-C.11 E2E scope**: T-C.11 slow E2E test invoked `apply_tier1_correction` directly via service composition rather than the full `_step_export` invocation per plan acceptance criteria. Both fixed pre-Codex; the subsequent Codex chain converged in 3 rounds (R1=4M + R2=1M + R3=1m) instead of an estimated 5-6 rounds if those findings had emerged at Codex time. **Generalization:** for any executing-plans dispatch where the implementer's return report claims fix completion, an orchestrator-side review subagent with the plan acceptance criteria as anchors catches LOCK divergences cheaply. Reviewer subagent prompt template: "review file:line evidence for each fix claim in the implementer return report; flag any cited line that doesn't match the claim; output a deviation list ≤600 words." Pattern complement to existing "Implementer self-report accuracy gate" lesson — pre-Codex review IS the orchestrator's accuracy gate. **Brief-drafting checklist update:** add pre-Codex orchestrator-review step between implementer-completion-claim + adversarial-critic invocation; dispatch focused reviewer subagent with plan acceptance criteria + brief BINDING contracts as anchors.

- **Implementer self-report accuracy gate — cite file:line evidence for each fix claim; orchestrator-side review MUST verify cited lines match claim, not just check that tests pass.** Lesson from Phase 12 Sub-sub-bundle C.C SC-1 pre-Codex review catch (2026-05-16; return report §10 lesson #7). The implementer chain's final report claimed "SC-1 fix threaded `environment` through pivot + apply functions" but the actual code did NOT thread `environment` into `_apply_tier1_correction_inner` per plan §D.5 step 3 LOCK — instead it short-circuited the entire pivot block. The implementer's regression test passed because the test pinned the (wrong) short-circuit behavior. Pre-Codex orchestrator review caught the divergence by re-reading the cited file:line evidence + cross-checking against the plan LOCK text. **Generalization:** regression tests pinning the wrong behavior can pass while violating the spec/plan LOCK; test-passing is necessary but insufficient evidence of fix correctness. Implementer self-report MUST cite specific file:line evidence (e.g., `swing/trades/schwab_reconciliation.py:432-451 now threads environment via apply_tier1_correction(env=environment) per plan §D.5 step 3`); orchestrator-side review MUST verify the cited lines actually match the claim — not just check that the test passes. Pattern complement to existing "Plan-template fixture defects are normal at executing-plans phase" Phase 8 lesson — both about distinguishing "tests pass" from "fix matches LOCK". **Brief-drafting checklist update:** implementer return-report shape MUST include per-fix file:line citations; orchestrator pre-Codex review verifies the citations match the cited LOCK text.

- **Classifier output is the C.B → C.C boundary; lifecycle invariants are service-layer responsibility, NOT classifier output.** Lesson from Phase 12 Sub-sub-bundle C.B return report §11 #1 + Codex R1 Major #4 disposition (2026-05-15; preserved as a binding architectural seam). Phase 12 Sub-sub-bundle C.B ships `classify_discrepancy(...) → ClassificationResult`. Lifecycle invariants on `reconciliation_corrections` rows (e.g., `correction_action='auto_applied'` implies `applied_by='auto'`; `correction_action='operator_overridden'` requires non-null `operator_truth_value_json`; tier-1 cannot land with non-NULL `ambiguity_kind`) are NOT enforced by the classifier — that's C.C `apply_tier1_correction` INSERT-time responsibility. Codex R1 Major #4 caught this seam framing question (would a schema cross-column CHECK enforce the invariant at C.A schema-time? Answer per C.A return report lesson #6: schema is defense-in-depth; service is primary path; C.B does neither — classifier produces ground-truth for service to consume). **Generalization:** when designing a pure-function module that feeds a service-layer transactional consumer, the pure function emits a `Result`-shaped dataclass; lifecycle invariants live at the consumer's INSERT-time enforcement layer (with rich error messages); schema CHECKs are defense-in-depth. Do NOT push lifecycle enforcement upstream into the classifier — it owns classification, not commits. Pattern complement to existing "Cross-column CHECK precedence" lesson family. Brief-drafting checklist update: for any classifier/service split dispatch, brief §0.5 must enumerate which layer owns which invariants + add discriminating tests at BOTH layers (classifier output shape test + service INSERT-time rejection test).

- **Validator chain MUST be re-invoked at service apply-time even when classifier already ran it.** Lesson from Phase 12 Sub-bundle C spec §4.6 + §5.5 BINDING + Sub-sub-bundle C.B return report §11 #2 (2026-05-15). The classifier in C.B accepts an optional `validator_chain` for tier-1 dry-run downgrade; on `(False, reason)` returns `(tier=2, ambiguity_kind='validator_rejected')`. But the C.C `apply_tier1_correction` service MUST re-invoke validators at INSERT time regardless of whether classifier already ran them — schema state may have shifted between classifier run + apply call (backfill scenario; concurrent state mutation by another surface; FK shift via DELETE elsewhere). Defense-in-depth via re-validation; classifier-side validator is an optimization (early-downgrade to avoid wasting service time on rejectable tier-1 candidates), NOT the enforcement boundary. **Generalization:** when a pure-function pre-validates + a service-layer apply re-validates, the SERVICE re-validation is the enforcement boundary; the pre-validation is optimization. Pre-empt the false-economy "we already validated, skip the service-side check" anti-pattern via discriminating test: inject a state-shift between classifier run + service apply + assert service rejects the now-invalid correction. Pattern complement to the Phase 8 R3→R4 `in_transaction` lesson family ("reject + simple contract" over "auto-detect + complicate").

- **`functools.partial` composition between two pure-function modules with mismatched signatures is non-obvious — pre-empt in dispatch brief.** Lesson from Phase 12 Sub-sub-bundle C.B plan §C.13 + return report §11 #3 (2026-05-15). C.B ships `default_validator_chain(conn) → Callable[(correction_target, *, affected_table, affected_row_id), tuple[bool, str | None]]`. C.B ships `classify_discrepancy(... validator_chain=Callable[(correction_target,), tuple[bool, str | None]])`. The two callable signatures diverge — chain needs `affected_table` + `affected_row_id` partial-application before it can be passed as `validator_chain`. The composition lives at C.C `apply_tier1_correction` step 4 where caller-context (`discrepancy.affected_table` + journal row PK) is known: `validator_chain = functools.partial(default_validator_chain(conn), affected_table=..., affected_row_id=...)`. Codex R1 didn't catch this as an issue because the un-skip at T-B.14 cross-bundle pin test exercises both callables separately. C.C dispatch brief MUST document the `functools.partial` composition explicitly + add an integration test that exercises end-to-end composition. **Generalization:** when two pure-function modules have signature divergence intentionally (different consumers need different invocation contracts), document the bridging composition pattern at brief-drafting time + add a discriminating integration test at the consumer-dispatch landing. Brief-drafting checklist: any "module A composes over module B" architectural seam, enumerate the composition pattern + verify signatures match OR document the bridge.

- **Free-form-string convention in `correction_reason` for cross-bundle signaling (e.g., `_pass_2_required=True`) is C.B → C.D interface — document the exact substring in consumer dispatch brief.** Lesson from Phase 12 Sub-sub-bundle C.B plan §C.4 + return report §11 #4 (2026-05-15). The `unmatched_open_fill` sub-classifier emits `(tier=2, ambiguity_kind='unsupported', correction_reason=f"...; _pass_2_required=True; ...")` when Pass-2 re-fetch is needed. This is NOT a typed field on `ClassificationResult` — it's a substring-match contract between producer (C.B classifier) and consumer (C.D backfill). Banked as plan §I V2 candidate to formalize as a typed `metadata: dict` field on the dataclass; V1 keeps the substring convention to scope C.B without retrofitting the dataclass. **Generalization:** when cross-bundle interface emits structured signaling via free-form-string fields (vs typed fields), the consumer-bundle dispatch brief MUST document the EXACT substring contract + add a discriminating substring-search test at the consumer landing. Brief-drafting checklist: any string-typed field carrying machine-interpretable signal needs explicit substring contract documentation OR a dataclass-field refactor banked as V2. Pattern complement to existing "Brief enumeration of shipped CHECK enums needs empirical verification" lesson (which is about producer-side enum value lists; this is about cross-bundle string contracts).

- **Shape predicate tightening discipline: when Codex tightens a discriminator predicate iteratively across rounds (R1 C → R2 M → R3 M → R4 M), the lesson is "implement input-shape checks EXPLICITLY at handler entry" — NOT just for this discriminator, but as a pattern for ANY operator-supplied or upstream-supplied payload that drives classification.** Lesson from Phase 12 Sub-sub-bundle C.B Codex R1 C#1 → R2 M#1 → R3 M#1 → R4 M#1 sequence (2026-05-15; entry_price_mismatch Shape A vs Shape B predicate). The classifier started with a permissive "any non-None price differs" check; R1 caught (ticker, date, quantity) consistency missing; R2 caught partial-tuple ambiguity; R3 caught source-side date-vs-fill_datetime contradiction; R4 caught journal-side same. 4 rounds of cascade because the predicate was being tightened reactively to specific cases Codex surfaced, not designed defensively from the start. **Generalization:** for any handler classifying a payload with multiple plausible shapes, BEFORE writing the happy-path: enumerate every recognized key set + reject anything else (`{'price'}` OR `{'ticker', 'date', 'quantity', 'price'}`; partial set → tier-2 unsupported); enumerate every cross-field consistency rule (date vs alternate-date-shape MUST agree; source-vs-journal MUST agree); add explicit reject branches for each. C.C handlers that classify operator-supplied `--custom-value` payloads will face the same scrutiny; pre-empt the 4-round cascade by designing defensively up-front. Pattern complement to existing "Brief-recommended technical micro-decisions should be empirically pre-tested" lesson — same family applied to predicate-shape design at writing-plans time.

- **Same-source-keys-on-source-and-journal evidence convergence: when both `source_payload` AND `journal_row` carry the same information field in MULTIPLE forms (e.g., `date` + `fill_datetime`), determinism requires each side's internal forms must agree AND both sides must agree with each other.** Lesson from Phase 12 Sub-sub-bundle C.B Codex R3 M#1 + R4 M#1 (2026-05-15). `source_payload` may carry both `date` (ISO-prefix) and `fill_datetime` (full timestamp); journal_row mirrors. R3 caught: if `source.date == '2026-04-27'` but `source.fill_datetime == '2026-04-28T...'`, the source-side has internal contradiction → tier-2 unsupported. R4 caught the journal-side mirror: same contradiction shape. The fix is symmetric input-validation at both sides. **Generalization:** when a payload schema has redundant representations of a logical field (date vs datetime; price vs total/qty; ticker vs full instrument symbol), determinism principle requires per-side internal-consistency check AND cross-side consistency check. 4 distinct rejection paths per redundant-field: (a) source internal-inconsistency; (b) journal internal-inconsistency; (c) source-vs-journal disagreement on form A; (d) source-vs-journal disagreement on form B. Discriminating-test pattern: 4-case parametrize. Pattern complement to existing "Session-anchor read/write mismatch" gotcha family — both about value-stability across representations.

- **Co-Authored-By footer drift requires EXPLICIT suppression in dispatch prompts; CLAUDE.md passive inheritance is insufficient because subagents have isolated context.** Lesson from Phase 12 Sub-sub-bundle C.B Codex R1 fix-bundle deviation #3 + operator-decided rebase-strip pre-merge (2026-05-15). R1 fix-bundle 4 commits (`6e4bd30` / `fcff4d3` / `180838e` / `78d98b2`) accidentally carried `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` footer despite CLAUDE.md `No Claude co-author footer` convention. Implementer subagent interpreted system Bash tool's default footer template as authoritative; CLAUDE.md context wasn't loaded into the subagent because subagents have ISOLATED context windows. Discovered at R1 verification + corrected at R2/R3/R4 dispatch (those fix commits correctly omit). Operator-decided rebase-strip pre-merge at integration time (`git rebase -i fdb4276` with `GIT_SEQUENCE_EDITOR` + `GIT_EDITOR` Python message filter; SHAs shifted to `a14401c`/`63909f5`/`1cd5951`/`69a2cc8`; content unchanged; 26-commit count preserved). **Generalization:** any binding convention in CLAUDE.md that subagents could violate (commit-message format, no-amend, no Claude co-author footer, specific commit-prefix conventions) MUST be explicitly cited in the dispatch prompt + reinforced in the dispatch brief §1.3 Commits section. Passive inheritance from CLAUDE.md is insufficient — subagent context starts fresh. Brief-drafting checklist update: enumerate every CLAUDE.md binding convention relevant to the dispatch + cite EACH explicitly in the dispatch prompt's "Key disciplines" section AND in brief §1.3. Pattern complement to existing "Orchestrator MUST commit the executing-plans dispatch brief to main BEFORE providing the inline dispatch prompt" lesson — both about dispatch-prompt content discipline; one is about timing (commit-before-prompt), this is about content (explicit-convention-citation).

- **Backup-gate equality form: `pre_version == (target - 1)` strict equality, NOT `pre_version <= (target - 1)`.** Lesson from Phase 12 Sub-sub-bundle C.A Codex R1 + dispatch-brief deviation 2026-05-15 (`354b6c0` merge; banked at plan §I + dispatch-brief §0.5 amendment candidate). My C.A dispatch brief §0.5 used `pre_version <= 18 AND post_version >= 19` for the backup gate; Phase 9 Sub-bundle A precedent + plan §A.12 LOCK use strict equality `pre_version == 18`. The `<=` form is overly broad: it fires for any pre-version up to and including 18, which means a v17 → v19 jump would ALSO fire the backup-gate (and miss the v17 → v18 boundary if that boundary's own gate had different naming convention). Strict equality fires the gate ONLY at the v18→v19 boundary; v17→v19 jumps must be a separate two-step migration. **Generalization:** future migration dispatch briefs MUST use `pre_version == (target - 1)` strict equality on the backup gate. Brief-drafting checklist update: copy the Phase 9 Sub-bundle A backup-gate clause verbatim; do NOT paraphrase. Same family as the brief-empirical-verification lesson family — applies at brief-drafting time too, not just at writing-plans.

- **Schema-CHECK + Python-constant + dataclass-validator MUST land in the same task for atomic consistency.** Lesson from Phase 12 Sub-sub-bundle C.A writing-plans plan §B.2 deviation + return report §11.#2 (2026-05-15). Plan §B.2 originally proposed widening the Python `_RESOLUTION_VALUES` constant in a separate task from the schema CHECK enum widening (T-A.1). Codex would have caught the split as a non-atomic-consistency risk: schema CHECK widens at T-A.1, but Python constant + dataclass validator widening landing in a later task means tests between the two have an inconsistent system state (schema accepts new value; Python rejects it OR vice versa). The widening should land in T-A.2 same commit as the schema change OR be folded inline into T-A.1. **Generalization:** any new schema element with a corresponding Python-side constant or validator MUST land atomically. Pattern: 1 task = (a) schema CHECK + (b) Python constant + (c) dataclass `__post_init__` validator + (d) all three discriminating tests. Cross-reference to existing CLAUDE.md gotcha "Python `... or ''` idiom collides with SQL CHECK-constraint nullability" — both about schema-vs-Python consistency.

- **Cross-column CHECK precedence: schema-defended is defense-in-depth; service-layer is primary path.** Lesson from Phase 12 Sub-sub-bundle C.A schema design + C.C scope split 2026-05-15 (spec §3.2 + plan §B.A LOCK + return report §11.#3). The cross-column CHECK between `(ambiguity_kind, resolution)` on `reconciliation_discrepancies` enforces tier-1 / tier-2 / tier-3 invariants schema-side (NULL kind + auto_corrected_from_schwab vs non-NULL kind + pending_ambiguity_resolution vs NULL kind + operator_overridden). C.A ships the schema CHECK; C.C ships the app-layer service-time enforcement (e.g., classifier output validation; auto-correction service refusing invalid state transitions). Both layers enforce the same invariant — schema is defense-in-depth (catches direct-SQL or test-fixture mistakes); service is the primary enforcement path (catches business-logic violations with rich error messages). **Generalization:** any future schema-design dispatch that introduces tier/state invariants across multiple columns should: (a) ship the schema CHECK at the foundation sub-sub-bundle; (b) ship the service-time enforcement at the service-layer sub-sub-bundle; (c) ensure both layers reject the same set of invalid states; (d) discriminating tests at BOTH layers (schema-CHECK rejection test + service-layer rejection test for each invariant). Cross-reference to existing Phase 9 §3.1 R1 Minor #4 precedent.

- **Plan-author schema additions DURING the writing-plans Codex chain need explicit spec-amendment-or-escalation BEFORE landing — not bank-after-write.** Lesson from Phase 12 Sub-bundle C writing-plans Codex R3 architectural-revert (2026-05-15; plan §I.19 SUPERSEDED record). Plan-author LOCKED a `metadata_json` column addition at R2; Codex caught at R3 framing the addition as a violation of spec §15.1's 13-item BINDING inheritance lock (schema design is one of the 13 BINDING items; plan-author cannot add to it without spec amendment). Plan-author REVERTED at R3; chain cascade through R4/R5 cleaned up references; stabilized at R6 convergence. **Generalization:** when a writing-plans dispatch consumes a Codex-locked spec (`NO_NEW_CRITICAL_MAJOR` at brainstorm-output time), the plan author MUST NOT propose schema additions or material design changes within the writing-plans chain. If the plan author identifies a need (e.g., a new column would simplify the implementation), the correct path is: (a) flag as orchestrator escalation; (b) orchestrator triages with operator; (c) if accepted, spec amendment via copowers:brainstorming partial-dispatch OR explicit operator approval to amend spec inline; (d) plan author re-converges. The bank-after-write path (plan author writes the addition + banks as a V2 candidate post-revert) is wasteful — costs 2-3 Codex rounds of cascade cleanup. **Brief-drafting checklist update:** writing-plans briefs MUST include a §3 OUT-OF-SCOPE entry "schema additions or material spec deviations require pre-dispatch orchestrator escalation; do NOT bank-after-write." Adversarial review watch-item: "Plan author proposes a schema element NOT in spec §3 → STOP + escalate." Pattern complement to existing brief-empirical-verification lesson family — same family applied to plan-author additions vs brief-author assertions.

- **Plan-size budget for architectural-pivot writing-plans: 3000-3700 lines for 4 sub-sub-bundles × ~50 tasks; 25% over schema-design-plan budget.** Lesson from Phase 12 Sub-bundle C writing-plans (2026-05-15 at `008dfe4`; 3621 lines; 51 tasks across 4 sub-sub-bundles; 6 Codex rounds; 5 brief-vs-shipped-schema deviations banked). Prior precedents: Phase 9 writing-plans 2257 lines / 5 sub-bundles / 30 tasks / 5 rounds; Phase 10 writing-plans 2008 lines / 5 sub-bundles / 33 tasks / 6 rounds. Sub-bundle C is the new high-water mark because architectural-pivot plans require: (a) per-task acceptance criteria for 51 tasks vs ~30 for schema-design plans; (b) §15.3 pre-verification verbatim grep results pasted (per Lesson #5 brief-vs-shipped-schema empirical verification); (c) Codex-fix cascade naturally accretes content (R1-R6 added +189 net lines per cascade); (d) integration-pin matrix between sub-sub-bundles surfaces explicitly. **Generalization:** orchestrator brief line-target for architectural-pivot writing-plans should be **3000-3700 lines** (not the 2200-2900 lines I projected for Sub-bundle C). Schema-design writing-plans stays at 2000-2500. Polish-bundle writing-plans 800-1500. Cross-reference: brief-line-target lesson family at orchestrator-context.md (Phase 9 brainstorm + Phase 8 brainstorm + Phase 10 brainstorm precedents).

- **9-substantive-round Codex chain is the new project high-water mark (Phase 12 Sub-bundle C brainstorm 2026-05-15 at `d682c25`); chain shape healthy when finding-count taper is monotonic-with-cascade-cleanup-spikes; 4C+26M+15m total findings; ZERO ACCEPT-WITH-RATIONALE.** Phase 12 Sub-bundle C brainstorm extended the convergent-chain lesson family with a 9-round dispatch (prior max: Phase 8 writing-plans 8 substantive + R9 confirm). Shape: R1 3C+6M+2m → R2 1C+3M+1m → R3 1C+2M+1m → R4 0C+2M+1m (Critical-free since R4) → R5 0C+4M+1m (cascade cleanup of R4 architectural rename — finding-count uptick FLAGGED in implementer return report as "cascade cleanup" not "adversarial thrash") → R6 0C+3M+2m → R7 0C+3M+2m → R8 0C+3M+2m → R9 0C+0M+2m → NO_NEW_CRITICAL_MAJOR. Spec landed at 1444 lines (above 800-1300 projection by ~110). 5 minor (out of 15 total minor) were R5-R8 wording polish cleanups + 1 from architectural rename cascade; this is the "convergent chain extending into wording-precision territory" pattern. **Generalization:** orchestrator budget for architectural-pivot brainstorms (vs schema-design brainstorms) should expect 7-10 substantive rounds; this is the upper bound of "healthy" per the convergent-chain lesson. Architectural pivots have more wording-precision surface area than schema designs because each operator-locked constraint (Sub-bundle C had 4) interacts with multiple subsystems (classifier + service + audit + tier-2 surface + backfill + flow pivot + lifecycle integration), and Codex finds wording-cascade-from-fix iteratively. Cross-reference: this is the same lesson family as Phase 8 writing-plans 8-round / Phase 10 writing-plans 6-round / Phase 9 brainstorm 4+R5-confirm lessons. **Brief-budget update:** architectural-pivot brainstorms should plan 7-10 round budgets; schema-design brainstorms 4-6 round budgets; polish dispatches 2-3 round budgets.

- **Brainstorm-time composition-source claims need empirical verification BEFORE spec encoding; brief-author "spec will compose validators from shipped repos" is unfounded if shipped repos don't expose callable validators.** Lesson from Phase 12 Sub-bundle C brainstorm Codex R2 Major #3 (2026-05-15; LOCKED at spec §5.5 + §14.OQ-14; introduces NEW `swing/trades/reconciliation_validators.py` shim module). Earlier brainstorm wording assumed shipped `swing/data/repos/fills.py` + `trades.py` + `cash_movements.py` exposed dedicated callable validator functions the auto-correction service could compose against. Codex R1 + R2 verification empirically established that these files do NOT expose validators — they expose schema CHECK + FK constraints + indirect `_recompute_aggregates` invariants only. The spec was forced to introduce a NEW shim module mirroring schema invariants as importable Python predicates for dry-run validation. **Generalization:** when brainstorming an architecture that composes over shipped surfaces, the brief AND the brainstorm output should empirically verify EACH composition source against the shipped codebase BEFORE encoding as binding §1 / §5. Grep for `^def` definitions; read the actual public surface; don't assume composition convenience. **Brief-drafting checklist addition:** for any composition-over-shipped-surfaces architecture, brief §1 lists each composition source + verifies the callable API matches the brainstorm's assumed shape. Pattern complement to existing brief-empirical-verification lesson family (Phase 5 / Phase 6 / Phase 10 / Phase 9 Sub-bundle B / Phase 12 Sub-bundle B all had brief-vs-shipped-code mismatches caught at Codex time).

- **Persisted-JSON-tier-1 vs re-fetched-tier-1 asymmetry: data shape constrains classifier determinism — when V1 mapper exposes less than the source provides, the missing-detail case MUST tier-2 even if the operator-locked tier-1 model would naively apply.** Lesson from Phase 12 Sub-bundle C brainstorm §8.4 Pass-2-tier-1-FORBIDDEN lock (2026-05-15). CVGI 41 persisted `actual_value_json` from Pass-1 with execution-level price; classifier can tier-1 redirect with confidence (operator-locked architectural intent preserved). DHC 39 + VSAT 40 require Pass-2 re-fetch via `get_account_orders`; V1 mapper exposes only order-level fields (no per-execution-leg detail). Even when the order count + qty sum match the journal fill, the classifier CANNOT tier-1 redirect because price-discrepancy detection requires execution-level data; the order-level fields are insufficient. Spec locked: Pass-2 re-fetch ALWAYS tier-2 in V1 regardless of payload shape. The asymmetry preserves operator-locked CVGI 41 tier-1 path (Pass-1 persisted) while honoring the determinism principle for new Pass-2 data. **Generalization:** when the source-data shape constrains classifier determinism — e.g., V1 mapper exposes a strict subset of what the source supplies — the classifier must DOWNGRADE tier-1 → tier-2 with explanatory `ambiguity_kind` for the data-shape-insufficient case, not assume the shipped-mapper coverage matches the source coverage. Spec must EMPIRICALLY verify the mapper's exposed fields against the source's full schema before encoding tier-1 redirect paths. V2 candidate: expand the mapper to expose execution-leg detail (Schwab API supports this; `marketdata_executions` endpoint); then the V2 classifier can tier-1 redirect. Cross-reference: this is the brief-empirical-verification family applied to MAPPER coverage, not just shipped-code symbols.

- **Synthetic-fixture-only acceptance test for production-write-contract surfaces: don't contaminate production audit trail when payload-required choices exist in a multi-choice resolution menu.** Lesson from Phase 12 Sub-bundle C brainstorm §15.5 C.D gate Codex R6 M#3 + R7 M#3 + R8 M#1+M#2 cascade (2026-05-15). The C.D gate originally required exercising the `--custom-value` payload-required CLI contract against operator's REAL production DHC 39 + VSAT 40 cases. R6-R8 refined: this would force operator into contrived dispositions (the operator's REAL data may not warrant `consolidate_using_operator_vwap` or `operator_truth`; operator wants `keep_journal_as_is` for V1; forcing the payload-required choice contaminates the audit trail with non-real operator intent). Final mechanic: synthetic-fixture isolated-DB pathway exercises the payload-required CLI contract against a planted synthetic discrepancy (`ambiguity_kind='unknown_schwab_subtype'` so `operator_truth` choice is valid); production case is dispositioned per operator's REAL data without contortion. **Generalization:** when a gate scenario would require contrived operator dispositions to exercise a contract surface, use a synthetic-fixture isolated-DB pathway that exercises the contract in isolation + separately validates the production case per operator's real intent. The audit-trail integrity of the production database is a binding constraint; gate verification routes around it via fixture isolation, not by forcing operator into contrived choices. **Brief-drafting checklist update:** for any operator-witnessed gate that exercises a payload-required CLI contract OR a multi-choice resolution menu, enumerate (a) whether the contract MUST be exercised against operator's REAL production case (almost never — the operator's real disposition stands), and (b) the synthetic-fixture pathway that exercises the contract in isolation if (a) is no.

- **Brief enumeration of shipped CHECK enums needs empirical verification against migration files BEFORE encoding as binding §1.5 / §0.7.** Lesson from Phase 12 Sub-bundle C brainstorm Codex R1 verification (2026-05-15; §14.OQ-11 carries forward as informational deviation). The brief enumerated `reconciliation_discrepancies.resolution` enum as 5 values including `'mistake_corrected'`; actual shipped migration 0017 has 5 values including `'source_treated_canonical'` + `'manual_override'` (not `'mistake_corrected'`). The brief enumerated `fills.reconciliation_status` enum as `('unreconciled', 'reconciled')`; actual shipped migration 0014 has 5 values. Codex R1 verification caught both via empirical grep of migration files. Spec uses shipped schema; §1.5 documents divergence as a brief-vs-shipped-schema discrepancy. **Generalization:** any brief that references shipped CHECK enums for purposes of widening / extension / cross-reference MUST `grep -E "CHECK.*IN.*\(" swing/data/migrations/*.sql` to verify the shipped enum values before encoding. This is the brief-empirical-verification family applied to CHECK-enum membership. Pattern complement: Phase 9 Sub-bundle A precedent surfaced the same family for `risk_policy` field count (brief said 28; actual was 34). **Brief-drafting checklist update:** for any brief that references a shipped schema element by enumerated values, grep the migration file + verify each value before locking; if any divergence found, document explicitly in brief §0 reading order so implementer doesn't waste a Codex round.

- **Orchestrator MUST commit the executing-plans dispatch brief to main BEFORE providing the inline dispatch prompt to operator — recurring procedural gap (2026-05-15 instance + prior unspecified instances; operator-surfaced 2026-05-15: "This is not the first time").** Workflow LOCK: (a) Write the brief file to `docs/<bundle>-executing-plans-dispatch-brief.md`; (b) `git add` + `git commit` the brief to local `main` SAME orchestrator turn as the Write; (c) ONLY THEN provide the inline dispatch prompt as a fenced code block in chat. Reason: worktrees created via `git worktree add .worktrees/<branch> -b <branch>` branch from local `main` HEAD at creation time. When the brief is untracked (only in parent working tree), the implementer's worktree branches from pre-brief HEAD — the brief is absent from their working tree at the path the prompt references. Recoverable (operator can pull main into the worktree OR re-create) but adds friction at a time-sensitive handoff moment + signals process noise to the operator. **Fix is procedural-discipline, not architectural** — the Write tool succeeds + the inline prompt is the visible "deliverable" in chat, so the commit step gets forgotten. The commit is invisible-but-load-bearing. Generalization complement: this is the same family as the 2026-05-08 "merge + push is orchestrator action" lesson — durable artifacts that the dispatched implementer references on disk are orchestrator commit responsibility BEFORE the dispatch prompt leaves orchestrator chat. **Brief-drafting checklist update:** add explicit commit step between brief Write and inline prompt; checklist verifier `git log --oneline -1 | Select-String <bundle>-brief` if uncertain. Cross-reference saved in auto-memory at `feedback_commit_brief_before_inline_prompt.md` for cross-session persistence.

- **Operator-paired-gate-caught implementation gap → orchestrator-inline gate-fix (precedent: 11B `34be84e` + 12A `e2c0384` + 12B `7b75d4a` — now 3 instances cumulatively).** When the operator-witnessed gate surfaces a real defect (cassette tests passed but real-world execution failed) OR a real UX gap (TestClient + helper-return-contract tests can't see what the operator sees in the browser), the orchestrator may make a small, well-understood, structural fix INLINE on the worktree branch (not request a re-dispatch). Pattern: (a) defect/gap must be small (one or two callsite changes; one regression test); (b) discriminating signal must be clear (pre-fix vs post-fix behavior cleanly distinguishable); (c) regression test added in same commit pinning the post-fix shape (often source-level pattern matching for structural fixes, OR template/route-table assertion for UX gaps); (d) commit message explicitly framed as "orchestrator-inline gate-fix"; (e) integration merge folds the gate-fix in alongside the implementer's commits via `--no-ff`. Examples: 11B `34be84e` (camelCase kwarg `max_results→maxResults` + 5 signature-pin tests); 12A `e2c0384` (snapshot/orders callsites passed `client=None` hardcoded; wired `client=schwab_client` + source-level regression test); 12B `7b75d4a` (operator-surfaced UX gap — `/schwab/setup` had no nav link from `/config`; added "External integrations" section with link + TestClient template-render regression test asserting `href="/schwab/setup"` + "External integrations" substrings). All three fixes were defects/gaps that the implementer's pre-existing tests couldn't catch — cassette stubs accept any kwargs; helper-return-contract tests don't exercise integration; route-level integration tests don't exercise operator-discoverability. **Generalization:** when operator-paired gate catches a real defect OR UX gap AND the fix is mechanical + well-understood, prefer inline-fix to re-dispatch — saves a full implementer cycle. When fix is architectural OR impacts unfixed surfaces broadly, re-dispatch is correct. **Discoverable-UX-gap variant (new with 12B):** TestClient tests can verify route registration + response status but cannot verify whether the operator can FIND the route from their normal workflow — operator-witnessed gate is the canonical surface for navigation/discoverability gaps; UX-gap inline fixes should add a discriminating test asserting the link/button/nav-entry is present in the rendering of the operator's likely entry-point page.
- **Operator architectural pushback supersedes orchestrator scope assumptions; reframe before bandaging.** Lesson from 2026-05-15 reconciliation pivot conversation. Orchestrator surfaced 3 fresh discrepancies post-pipeline #63 + recommended operator-action paths (direct DB edit OR `acknowledged_immaterial` resolution OR a CLI command "that may or may not exist"). Operator pushed back: those recommendations bypass audit/validators; "immaterial" framing is wrong when broker data IS authoritative; the system should auto-correct journal-from-Schwab, not surface for operator-triage. The orchestrator's recommendations were tactical patches that would have re-introduced fiction by asking operator to manually mediate truth. **Reframe was the right move:** acknowledge the architectural failure of the recommendations + bank the correct model (three-tier resolution: tier 1 unambiguous auto-correct = most common; tier 2 ambiguity surfaced for operator decision with type-specific resolution choices = operationally important; tier 3 operator override of an applied tier-1 correction = rare edge) + propose Phase 12 Sub-bundle C as the architectural pivot. **Generalization:** when orchestrator's recommendation feels tactical-but-incomplete + operator pushes back framing-wise, do NOT defend the recommendation — listen for the architectural model the operator is describing + reframe forward. Tactical patches that "fix the surface symptom" can re-introduce the deeper anti-pattern. **Brief-drafting checklist update:** when an existing system has a well-defined operator-action loop (Phase 9 reconciliation discrepancies → operator triage), and a new authoritative external data source becomes available (Phase 11 Schwab API), the brief-author MUST consider whether the operator-action loop is still the right design — OR whether the authoritative source obsoletes it. Asking "should this still require operator action?" is a binding §0 design question for any integration brief that introduces an authoritative external source.
- **Once operator-witnessed verification gate passes, integration merge is an orchestrator action — operator's gate-pass confirmation IS the trigger; do NOT also ask "shall I proceed with merge."** Lesson from Phase 8 V1 polish gate walkthrough (2026-05-08; corrected by operator post-Surface-4-PASS). Orchestrator's default of presenting "ready to merge — shall I proceed?" treats merge as a second confirmation gate after the operator-witnessed gate passes. Operator correction: gate-pass = ship authorization; orchestrator just executes the merge + push + post-merge housekeeping. **Generalization:** integration-merge-to-main + `git push origin main` + post-merge housekeeping commit are orchestrator actions, NOT operator-confirmation-gated actions. Authorization for ship was given at three earlier points: (a) brief approval, (b) executing-plans dispatch confirmation, (c) "gate passed" / "all good" / equivalent confirmation per operator-witnessed surface check. Asking again at merge time is friction. Pattern complement to existing operator preferences ("Operator confirms decisions with short responses ('yes', 'concur', 'looks good'); take as full approval") — gate-pass is the analog for ship-flow. Add to brief-drafting checklist: brief §6 "Done criteria" + §7 "Return report format" should describe merge as an orchestrator post-gate action, NOT a "shall I proceed" prompt. Save as feedback memory in `~/.claude/projects/c--Users-rwsmy-swing-trading/memory/` so the preference carries forward.
- **Worktree-isolated dispatch briefs MUST specify the worktree directory path explicitly, not just the branch name; `superpowers:using-git-worktrees` skill default may diverge from project precedent.** Lesson from Phase 8 V1 polish dispatch (2026-05-08; operator surfaced post-merge). Brief §3 specified branch name `phase8-v1-polish` but not the directory path; implementer's `using-git-worktrees` invocation chose `.claude/worktrees/phase8-v1-polish/` (skill default), while Phase 5/6/7/8 precedent uses `.worktrees/<branch>/` at repo root. Both work — `.claude/` is gitignored (project `.gitignore:35`); `.worktrees/` is not gitignored but is the documented Phase 6/7/8 husk-cleanup target (cleanup-locked-scratch-dirs.ps1 expects this path). **Generalization:** brief §3 "Binding conventions" or §8 "Dispatch metadata" MUST specify the worktree directory path explicitly. Recommended path: `.worktrees/<branch>/` for project consistency with Phase 5/6/7/8 ACL-handling + cleanup-script discipline. Add to brief-drafting checklist for worktree-isolated dispatches: "specify worktree path: `.worktrees/<branch>/` (matches project precedent + cleanup-script targeting)."
- **Read/write predicate symmetry on session-anchored UI surfaces MUST be verified against the writer's actual code BEFORE locking in brief; orchestrator mental-model "today's session" inference is not authoritative.** Lesson from polish bundle 2026-05-09 Codex R1 Major #1 (commit `cfacbc5`; brief-side defect caught + corrected during executing-plans dispatch). My polish-bundle brief §0.3 #1 locked the 3e.5 daily-management badge predicate to `review_date == action_session_for_run(now())` — the canonical "next-session" anchor I assumed was correct for "today's session" semantics. Implementer's investigation found that BOTH writers (pipeline `record_snapshot` + web route `daily_management_event_post`) stamp `review_date = last_completed_session(now()).isoformat()` per Phase 8 spec §4.5 / R3 Major #2. The two date functions diverge on weekends, holidays, evenings, and pre-market — using the brief-locked predicate would leave the badge stuck at "pending" immediately after a successful operator submit (the operator's daily-mgmt entry IS recorded, but the read predicate looks at the WRONG date and finds nothing). Implementer fixed inline + added round-trip integration test `test_post_event_log_then_dashboard_badge_flips_to_logged` pinning read/write alignment. **Generalization:** when an orchestrator brief specifies a UI predicate against any session-anchored column (`weather_runs.data_asof_date`, `daily_management_records.review_date`, `evaluation_runs` / `pipeline_runs` anchors), the brief MUST cite the writer's session-anchor function as a binding §0 Read step ("verify writer at `<file>:<line>` uses `<session_function>` to stamp the column being queried"). Forward-looking `action_session_for_run` (next session being prepped) and backward-looking `last_completed_session` (most recent session that closed) are NOT interchangeable. **Brief-drafting checklist update:** for any new UI predicate on a session-keyed audit-trail or daily-snapshot table, brief §0.3 lock MUST quote the writer's exact predicate verbatim, NOT the orchestrator's mental-model inference. Pattern complement to existing 2026-05-04 lesson "Exchange-session helper family has multiple members; brief author MUST specify forward vs backward semantically" (now archived) AND the 2026-05-08 lesson "Brief-recommended technical micro-decisions should be empirically pre-tested" — same family of brief-side empirical-verification gaps. Promoted to CLAUDE.md as the THIRD instance in the session-anchor-drift gotcha family (joining the existing weather-lookup gotcha + this one).
- **Brief-recommended technical micro-decisions should be empirically pre-tested against multi-row + same-second cases before plan adoption; "clever value-mangling" patterns (sign-flip, negative IDs, sentinel encoding) are fragile under concurrent insertion + tiebreak edge cases.** Lesson from Phase 8 V1 polish executing-plans Codex R1 Major 1+3 (2026-05-08; fix at `7c08f12`). The phase8-v1-polish writing-plans brief §0.3 #7 recommended `management_record_id := -trade_events.id` as the sort-tiebreak for cross-table ordering of orphan rows alongside `daily_management_records` rows. Plan §A.2 adopted the recommendation verbatim. Codex R1 Major 1 + Major 3 caught two failure modes the recommendation missed: (a) **negative-id reverses insertion order** — for two orphan stop_adjusts at the same second, `-trade_events.id` ASC put the LATER-inserted orphan first (because the later one has higher id, hence more-negative tiebreak); operator's natural mental model is "earlier-inserted first." (b) **DMR-vs-orphan same-(date,ts) tiebreak was untested** — the recommendation assumed positive ints sort after negative ints in the canonical sort, but the test fixtures didn't exercise this. Plan author replaced with explicit `(review_date, created_at, source_rank, abs_id)` 4-tuple where `source_rank=0` for orphans / `1` for DMRs; orphans sort by `trade_event_id ASC` (preserves insertion order). Original `-trade_events.id` value retained as `data-timeline-record-id` template attribute (informational; no functional collision). +2 discriminating tests. **Generalization:** orchestrator briefs SHOULD NOT recommend "clever" tiebreak schemes (sign-flip, negative IDs, sentinel encoding); explicit discriminator columns / source_rank fields / dedicated cross-table comparators are the robust pattern. **Brief-drafting checklist addition:** when recommending a sort-tiebreak across multiple data sources, prefer explicit `source_rank` discriminator over value-mangling; if value-mangling is the locked recommendation, the brief MUST also enumerate the multi-row same-second + cross-source same-key tiebreak cases as discriminating-test requirements. Pattern complement to existing 2026-05-04 Phase 6 lesson on "brief assertions about column-vs-derived status need empirical verification" — same family (orchestrator brief inferring micro-decisions from secondary mental models rather than empirical data-structure verification).
- **Writing-plans dispatch's plan file MUST be committed before approving for executing-plans dispatch; brief should explicitly require implementer to commit, AND orchestrator should verify commit landed at triage.** Lesson from Phase 8 V1 polish writing-plans dispatch (2026-05-07; orchestrator-fix-up commit `5aa7829`). The implementer wrote the plan to `docs/superpowers/plans/2026-05-07-phase8-v1-polish.md` but did NOT commit it; return report read "no worktree created — writing-plans dispatch produces a doc, not code; the executing-plans dispatch will branch" — interpreting "doc, not code" as "no commit needed". Plan remained untracked in working tree post-dispatch. Orchestrator caught at triage via `git status --short` + committed the plan in-thread. Two prior writing-plans dispatches landed plan as a commit (Finviz at `734ba6f`; Phase 8 at `17b1845`); that was implicit precedent rather than explicit brief requirement. **Brief gap analysis:** the phase8-v1-polish writing-plans brief §6 "Return report format" asked for "HEAD" + "Tasks landed" but did NOT explicitly require the plan commit as the final dispatch step. The orchestrator's mental model assumed implementer would mirror prior writing-plans precedent; the implementer's mental model interpreted the brief literally + the "doc, not code" framing reinforced no-commit. **Generalization — two-piece discipline:** (a) **Brief discipline:** writing-plans briefs MUST include an explicit final step in §6 acceptance criteria — "Commit the plan via `git add docs/superpowers/plans/<plan-file>.md && git commit -m 'docs(<area>): writing-plans plan from <dispatch>'`; report committed SHA in return report HEAD section." (b) **Orchestrator-triage verification:** every writing-plans triage MUST run `git log --oneline -5 && git status --short` BEFORE drafting the executing-plans paste-ready prompt; if plan is untracked, either commit in-thread (this dispatch's pattern) OR dispatch a follow-up commit task to the implementer. Pattern complement to existing 2026-05-04 Phase 6 lesson "no main-side commits while executing-plans dispatch is in flight" — that lesson is about commit-cadence DURING dispatch; this lesson is about commit-completeness AT dispatch end. Add to brief-drafting checklist: writing-plans briefs require explicit commit-step language in §6.
- **Python sqlite3 `executescript()` issues an implicit COMMIT before running its script; each statement runs in autocommit mode and `conn.rollback()` cannot undo successful intermediate statements.** Lesson from Phase 8 executing-plans T1.1 (2026-05-07; plan-template defect surfaced + accepted-with-rationale during dispatch; substantiated by Codex R3 verification path). The plan-template asserted "probe_table absent after rollback" which is empirically incorrect — `executescript()`'s implicit COMMIT means the executed-so-far state cannot be rolled back. Implementer corrected the discriminating test to assert `conn.in_transaction == False` + error-re-raised invariants instead, with inline NOTE explaining the deviation. **Generalization:** when testing migration-runner rollback semantics, the discriminating test must use the runner's actual rollback path (per Phase 7 hotfix `283d4fa`'s wrapper that issues explicit `BEGIN`+`executescript`+`COMMIT` with try/except rollback) — NOT bare `executescript`+`rollback`. Relevant for Phase 9 + Phase 10 migration tests; Phase 7 hotfix's runner-level discipline IS the rollback boundary, not `executescript` itself. Promote-to-CLAUDE.md candidate (durable code-failure prevention).
- **The `in_transaction` "safety guard" anti-pattern: speculative defensive guards in transactional code can re-introduce the very race the explicit lock was meant to close.** Lesson from Phase 8 executing-plans Codex R3.M1 → R4.M1 cascade (2026-05-07). Initial T3.2 implementation used Python's `with conn:` (deferred transaction); R3 added explicit `BEGIN IMMEDIATE` to close the stale-prior_stop race; R4 caught that the `in_transaction` guard added in R3 actually re-introduced the race because the guard's "auto-detect outer transaction" logic could deceive a caller into thinking they were inside the lock when the connection's deferred transaction had not yet escalated. Final contract: caller MUST NOT hold an open transaction; function ALWAYS owns BEGIN IMMEDIATE / COMMIT / ROLLBACK; reject (don't auto-detect) caller-held transactions. **Generalization:** prefer "reject + simple contract" over "auto-detect + complicate" for transactional boundaries. Future writing-plans dispatches that introduce single-transaction services must specify the rejection-on-caller-tx contract explicitly + add discriminating test that asserts function raises if called while caller has open transaction. Pattern complement to existing 2026-05-06 Finviz I1 lesson "Repo functions must NOT call `conn.commit()`" + 2026-05-07 Phase 8 §A.1 "service-level wrapper opens its own transaction" CLAUDE.md gotcha.
- **Five client-trust holes closed via server-stamping in Phase 8 — pattern for any V1 single-operator form.** Lesson from Phase 8 executing-plans Codex R2.M2 + R3.M2 + R4.M2 (2026-05-07). The daily-management event-log POST handler started with hidden form inputs for `created_at`, `review_date`, `data_asof_session`, and `mfe_mae_precision_level`. R2 caught that a stale form (page open across sessions) or tampered POST would persist incorrect timestamps; R3 caught the same for session anchors; R4 caught it for the precision tier (a tampered POST could persist `intraday_exact` audit metadata that doesn't match the actual data source). Fix pattern: SERVER-STAMP at handler entry; remove hidden inputs from form; expose computed values as display-only `<span class="muted">` text (operator sees what will be persisted; cannot tamper). **Generalization:** any V1 single-operator form that exposes hidden audit fields is a tampering surface; default to server-stamping unless operator-override is explicit + intentional. Add to writing-plans §5 watch items: "for any new form-driven route, enumerate every hidden input + per-field disposition (operator-supplied / server-stamped / display-only / removed)." Pattern complement to existing Phase 5 R1 M1+M2 HTMX failure surfaces.
- **Plan-template fixture defects are normal at executing-plans phase — implementers SHOULD detect + accept-with-rationale; orchestrators SHOULD NOT auto-amend the plan mid-dispatch.** Lesson from Phase 8 executing-plans (2026-05-07; 5 plan-template defects across T1.1 / T4.0 / T5.0 / T4.1 / T3.2 deviations documented in return report). Plan-author drafts test fixtures based on best-effort code-state knowledge; some assertions break against actual SQLite/FastAPI/pytest semantics that surface only at execution time (executescript implicit COMMIT, pipeline_runs CHECK enum, caplog API, SWING_DB_PATH non-existence, monkeypatch.setattr signature). The right discipline: implementer accepts-with-rationale + corrects the fixture inline + flags as deviation in return report. Orchestrator banks the deviations + does NOT amend the plan retroactively (per existing 2026-05-04 lesson "no main-side commits during executing-plans"). **Generalization:** plan-author fixture-template-correctness is unverified until execution-time exercises real semantics; expect 3-7 fixture defects per executing-plans dispatch with discriminating-test discipline. Pattern complement to existing Phase 6 lesson "Plan templates can encode assertion errors that surface only at fixture instantiation" — same family, broader applicability.
- **Process discipline: prefer explicit-path `git add <file>` over `git add -A` to prevent stray-file inclusion + accidental amend-to-remove.** Lesson from Phase 8 executing-plans R1 Critical 1 commit `a52ac1a` AMEND incident (2026-05-07; technical brief violation of "no amending" rule from binding conventions). Implementer used `git add -A` to stage the pipeline_run_id wiring fix; that pattern accidentally staged the `.copowers-subagent-active` marker file (which should never be committed). Implementer realized + amended the commit to remove the marker file. The amend-to-remove fix was correct content-wise but violated the "no amending" binding convention. **Generalization:** `git add -A` is convenient but dangerous — it stages ALL changes including stray files (markers, .DS_Store, editor temp files, generated artifacts). Per existing CLAUDE.md staging convention "When staging files, prefer adding specific files by name rather than using `git add -A` or `git add .`, which can accidentally include sensitive files (.env, credentials) or large binaries." Phase 8 surfaced this as a binding-convention-violation pattern. Future dispatches: brief explicitly forbids `git add -A`; subagent-driven-development includes per-task `git add <specific-files>` discipline. Pattern relevant for Phase 9/10 executing-plans + any future dispatch.
- **19 adversarial rounds across a single phase is a new high-water mark; chain-shape diagnostic is "tapered finding count" + "fix-introduced regressions are healthy".** Lesson from Phase 8 phase-total (2026-05-07): brainstorm 5 substantive + R5 confirm + writing-plans 9 substantive + executing-plans 5 substantive + R5 confirm = 19 substantive Codex rounds. Each chain individually exhibited tapered finding-count + convergent shape per existing Phase 7 Sub-B + Phase 8/9 brainstorm lessons. Whole-phase total was 19 — significantly higher than Phase 6 (10 rounds) or Phase 7 (~12 across all sub-dispatches). Discrimination-discipline compound-ROI is real but expensive in round-count. **Generalization:** orchestrator brief-budgets at PHASE level should expect 15-25 substantive Codex rounds for a schema-design phase with 5 new tables + 15 tasks. Phase 9 (5 new tables + reconciliation depth + sector-tamper hardening + Schwab API source-enum coordination) likely sees comparable round-count when its writing-plans + executing-plans dispatches fire. Pattern complement to existing chain-shape lesson + brief-line-target lesson — round-count, line-count, test-count all scale with scope×integration multiplier when discriminating-test discipline is binding.
- **SQLite REPLACE has quirky semantics; SELECT-then-UPDATE-or-INSERT is the safer pattern for upsert.** Lesson from Phase 8 daily-management brainstorm (Codex R4 Major fix, 2026-05-06; spec at `docs/superpowers/specs/2026-05-06-phase8-daily-management-design.md` §4.2). REPLACE in SQLite is `DELETE old + INSERT new` semantically — not a true UPDATE — which means: (a) all FK constraints with ON DELETE CASCADE on the replaced row's child tables CASCADE-WIPE child data, (b) auto-increment PK gets a new value (history-rewrite via PK-change), (c) any index entries for the replaced row are reconstituted with new PK references. For an audit-trail-with-uniqueness pattern (Phase 8's daily_management_records same-tier reflow case), REPLACE will silently destroy referential integrity. Use SELECT against the unique-key-tuple to find existing row, then UPDATE if found OR INSERT if not. Same lesson family as Phase 7 Sub-A R1 Major 3 (`executescript()` partial-failure rollback) — both about SQLite-runner-level discipline that needs explicit code, not implicit assumptions about ORM-style atomicity. **Generalization:** any future Phase 9 (reconciliation_runs / reconciliation_discrepancies) or Phase 10+ (account_equity_snapshots) audit-trail schema with uniqueness constraints inherits this discipline. Add to writing-plans §5 watch items: "for any UPSERT against a TEXT-keyed unique constraint, specify SELECT-then-UPDATE-or-INSERT semantics; reject SQLite REPLACE proposals with rationale."
- **`is_superseded` flag column + `superseded_by_record_id` FK pattern decouples uniqueness slot from audit pointer.** Lesson from Phase 8 daily-management brainstorm (Codex R2 Major fix, 2026-05-06). Initial Phase 8 design used `superseded_by_record_id IS NOT NULL` as both the "superseded" boolean test AND the FK audit pointer to the replacement row. Codex R2 caught the conflation: when later (R3) the tier-upgrade sequence revised to capture predecessor by exact PK in step 3, the FK pointer needed to be set in a LATER transactional step (step 5) — but the boolean test ("is this row superseded?") needed to be settable IMMEDIATELY in step 3 to lock out concurrent writes. Conflating them forced an impossible ordering. R2 fix introduced `is_superseded INTEGER` flag (0/1) as a separate column from `superseded_by_record_id INTEGER REFERENCES daily_management_records(management_record_id)`. Boolean is settable per row mid-transaction; FK pointer is set after the new row's PK is known. **Generalization:** any future audit-trail schema with uniqueness slots (Phase 9 reconciliation_runs auto-supersession; risk_policy versioning supersession) should follow the dual-column pattern. Pattern complement to Phase 7 lesson "State-bearing entity validation must distinguish operation enforcement from data invariant" — both about separating concerns that conflate naturally but produce ordering bugs.
- **Per-row stamp of policy-versioned values prevents historical-row reinterpretation under risk_policy changes.** Lesson from Phase 8 daily-management brainstorm (Codex R1 Major #5 fix, 2026-05-06). Phase 8 captures `trail_MA_candidate_price` as REAL using a 21-day SMA reference period as V1 default. Phase 9's risk_policy entity will version this default (e.g., switch to 10-day post-+2R per Tier-3 #6 doctrine). Without per-row stamping of which period was used at capture time, a Phase 9 policy change would silently re-interpret all historical rows (operator looking at a 2026-06-01 snapshot would see "21-day SMA" but Phase 9 risk_policy active 2026-08-01 says "10-day SMA" — confusing without provenance). R1 M5 fix added `trail_MA_period_days INTEGER` per-row stamp; Phase 9 versioning changes the DEFAULT for new rows but cannot retroactively change historical rows. **Generalization:** any future risk_policy-versioned default that gets captured into a daily/per-trade record needs a corresponding per-row stamp of the value used at capture-time. Phase 10 metrics dashboard's `position_capital_denominator_dollars` is a similar case (would need per-row stamp if `live_capital_denominator_dollars` becomes versioned via Phase 9). Add to writing-plans §5 watch items: "for any risk_policy-versioned value consumed in per-trade or per-snapshot capture, add per-row stamp of the value-as-used."
- **Convergent multi-round Codex chains are healthy at 4-9 rounds when fix-introduced regressions are the failure mode; R-final confirmation pass with no new findings is a valid stopping pattern; tapered finding count is the diagnostic.** Lesson from Phase 8 brainstorm (5 substantive + R6 confirmation) + Phase 9 brainstorm (4 substantive + R5 confirmation) + Phase 7 Sub-B (6 rounds) + **Phase 8 writing-plans (8 substantive + R9 confirmation, 2026-05-07; finding-count taper 5→5→3→5→2→1→2→1→0; new high-water mark in this project)**. All four exhibited the same shape: each round's findings were R-N-1-fix-introduced regressions, freshly-exposed gaps, or detail-cascade follow-ups — NOT adversarial thrash. Phase 9 R4 had Codex explicitly note "chain health: healthy. Round 4 findings are narrow fix-introduced wording/implementation-risk issues, not basic schema misses" — a useful diagnostic phrase to look for. **Generalization:** orchestrator brief budgets should specify 4-6 substantive rounds for schema-design dispatches AND 6-9 substantive rounds for writing-plans dispatches with high task counts (15+ tasks × discriminating-test specifications compounds the surface). Expect a final 0/0/0 or 0/0/N-minor confirmation pass to lock approval. Phase 8 executing-plans + Phase 9/10 writing-plans dispatches may see similar 6-10 round patterns. Pattern complement to existing Phase 6 lesson "Test count projections should bias high when discriminating-test discipline is in play" + brief-line-target lesson — all three (round count, test count, line count) bias high together when discriminating discipline is binding.
- **Brief drafting touching schema migrations MUST verify next-available migration number empirically before writing the brief.** Lesson from Phase 8 writing-plans Critical R1 (2026-05-07; spec §A.0 migration rename 0015→0016). Phase 8 brainstorm spec §8.1 + Phase 8 writing-plans brief both asserted migration filename `0015_phase8_*.sql` without verifying 0015 was already shipped as `0015_finviz_api_calls.sql` from the Finviz V1 ship (2026-05-06). Implementer caught as Critical R1 + correctly renamed to 0016. Schema bump v15→v16 was preserved per spec intent; the filename collision was orchestrator-brief-side. **Pattern complement to existing 2026-05-04 Phase 6 lesson "Brief assertions about column-vs-derived status need empirical verification" + 2026-05-06 Phase 10 lesson "Brief-premise empirical-verification extends to brainstorm-phase".** This is the THIRD instance in the brief-empirical-verification family; cumulative pattern: orchestrator-brief drafting touching ANY schema/migration/code-state assertion MUST verify against actual repo state BEFORE writing. **Concrete pre-brief-draft checklist additions:** (a) `ls swing/data/migrations/` + identify next-available number for migration-touching dispatches; (b) `git log -E --pretty='%s' --grep='^[a-z]+\([a-z]+\):' -- swing/data/migrations/` to spot-check recent migration ships; (c) `grep -rn "<asserted-shipped-symbol>" swing/` for any code-state assertion the brief encodes as binding §1 strategic context.
- **Two functions can share the same name across repo-level and service-level layers; transactional discipline differs.** Lesson from Phase 8 writing-plans §A.1 (2026-05-07). Phase 7 shipped TWO `update_stop_with_event` functions: REPO-level `swing/data/repos/trades.py:update_stop_with_event` (atomic; caller controls transaction) AND service-level `swing/trades/stop_adjust.py:update_stop_with_event:105` (opens its own `with conn:` block; commits on context exit). Phase 8's `record_event_log` needs single-transaction contract per spec §4.4 (its outer transaction owns BEGIN IMMEDIATE / COMMIT). Service-level call would prematurely commit the outer transaction. Plan §A.1 routes Phase 8 to REPO-level + adds defense-in-depth validator boundary rejecting no-op stops (`new_stop == current_stop`) AND stale `prior_stop` (`req.prior_stop != trade.current_stop` re-read inside transaction). Pattern complement to existing 2026-05-06 Finviz I1 lesson "Repo functions must NOT call conn.commit()" — both about caller-controlled transaction scope, but at different layers (Finviz lesson = repo functions don't commit; Phase 8 lesson = service functions DO commit, so caller-controlled-transaction work routes around service to repo). **Generalization:** when two functions share a name across layers, audit the transactional discipline of each before routing a new caller. Future schema-design dispatches that need single-transaction contracts: prefer repo-level callees + add defense-in-depth validators inside the transaction boundary; reserve service-level callees for HTTP-route-style atomic boundaries that own their own transaction.
- **Defense-in-depth validators inside transactions: re-read live state + reject stale operator-supplied snapshots.** Lesson from Phase 8 writing-plans §A.1 (2026-05-07). The validator boundary at Phase 8's `record_event_log` REJECTS two cases inside the transaction: (a) no-op stops (`new_stop == current_stop` — operator submits stop change but value is unchanged; rejection prevents spurious audit-trail rows) AND (b) stale-prior-stop (`req.prior_stop != trade.current_stop` re-read inside transaction — operator's form may have stale state if another concurrent write modified `current_stop` mid-form-fill; rejection prevents lost-update race). Together these prevent two distinct failure modes that pure outer-validation can't catch (race-condition-stale-snapshot AND no-op-spurious-row). Generalization: any audit-trail-with-event-emission boundary that takes operator-supplied prior-state from a form should re-read live state inside the transaction + reject stale snapshots. Pattern complement to existing Phase 5 R1 Major lost-update-race ACCEPTED-with-rationale (single-operator scope) — Phase 5's race was form-driven cfg writes; Phase 8 hardens against the same family for trade-stop writes. Future writing-plans on form-driven event-emission surfaces should adopt the same pattern.
- **State-machine integration via query-side JOIN, not schema flag, when grain mismatches.** Lesson from Phase 9 brainstorm R1 Major #4 (2026-05-06; spec §5.1). Initial Phase 9 design proposed `review_log.reopened_at` flag column to surface review-reopen due to material discrepancy; Codex caught that review_log is **cadence-period-grain** (one row per daily/weekly/monthly cadence period, frozen-at-completion aggregates) — NOT trade-grain. A flag column on review_log couldn't accurately attribute "which trade triggered the reopen." Final design: TWO operator-action queries built from `reconciliation_discrepancies.material_to_review = 1 AND resolution = 'unresolved'` JOINed to `trades.state` — active-trade alerts (state IN ('entered','managing','partial_exited'), HIGH urgency) + closed/reviewed re-review attention (state IN ('closed','reviewed'), NORMAL urgency). Phase 7 state machine UNTOUCHED. Phase 6 review_log gets ONE per-row policy stamp column (`risk_policy_id_at_review_completion`) but no behavioral flag. **Generalization:** when integrating new functionality with an existing state machine, FIRST check whether the integration data already exists for query-side construction vs requires schema flag. Grain mismatches between integrating tables (cadence-period vs trade vs daily-snapshot) make flag columns inaccurate; query-side JOINs preserve schema invariants. Pattern complement to Phase 7 lesson "Status→state predicate rewrite is not uniform substitution" — both about pre-empting schema modifications when query construction suffices.
- **Canonical-truth promotion: existing config becomes startup-mirror; new entity becomes authoritative.** Lesson from Phase 9 brainstorm §3.1.3 (2026-05-06). Phase 9 promotes `risk_policy` table to canonical post-Phase-9 source-of-truth for risk + threshold + classification fields. `swing.config.toml` (the Phase 5 user-config infra) becomes a startup-time MIRROR — values import to seed the entity but the file is NOT auto-written. Phase 5 config-page edits cascade to a new `risk_policy` row (audit-trail-preserving); hand-edits to TOML produce a divergence banner with explicit `swing config policy import-from-toml` ratification path (operator-only; no auto-write). Preserves "policy versions only via explicit operator action" + audit-trail integrity. **Generalization:** when lifting a config block to a versioned entity, the existing config remains as a startup-time mirror (not deleted; not auto-written-to). Hand-edits surface visibly via divergence-detection. Future-applicable for any future cfg-block → versioned-entity migrations.
- **`material_to_review` (or any urgency-classification field) is CLASSIFICATION not workflow trigger; operator-overridable; computed via type-lookup.** Lesson from Phase 9 brainstorm §3.2.4 (2026-05-06). The Phase 9 `material_to_review` flag on reconciliation_discrepancies drives downstream queries (active-trade alerts; review-reopen attention) but does NOT trigger workflow state changes. Computed via `MATERIAL_BY_TYPE` lookup at insert-time; operator-overridable on a per-row basis. Within-run dedup binding (no duplicate rows in same reconciliation_run); cross-run dedup explicitly NOT done (re-running reconciliation is a legitimate audit workflow; dashboard collapses by `(trade_id, type, field)` at READ-time). **Generalization:** for urgency-classification fields on audit-trail tables, prefer (a) classification-not-workflow semantics, (b) operator-overridable per-row, (c) type-lookup computation at insert-time, (d) within-run dedup but cross-run audit preservation, (e) READ-time collapse for dashboard surfaces. Future-applicable for Phase 10+ metrics-dashboard reconciliation-status badge design + any future audit-trail urgency flag.
- **Brief line-target should bias high when dispatch scope-of-tables × integration-surface multiplies.** Lesson from Phase 9 brainstorm 2026-05-06 (1090 lines spec vs 600-900 brief target; ~190 over). Phase 9 had 5 new tables + integration with shipped Phase 6 (review_log) + Phase 7 (trades.state, fills, trade_events) + queued Phase 8 (daily_management_records) + Schwab API Phase A coordination. Phase 8's spec landed at 875 lines for 1 new table + integration with Phase 7 + Phase 10 capture-needs. Locked-content-per-major-locked-decision was comparable across both phases; the line-count difference is proportional to the table × integration-surface multiplication. **Generalization:** brief line-target should scale roughly linearly with `(new_tables × integration_surfaces)`. Phase 8 (~1×N) → ~875 lines acceptable. Phase 9 (~5×N) → ~1090 lines acceptable. Future writing-plans / executing-plans briefs on similar scope multipliers should set targets accordingly. Pattern complement to existing Phase 6 lesson "Test count projections should bias high when discriminating-test discipline is in play" — both about expected-vs-actual variance widening with scope multiplication.
- **Brief-premise empirical-verification extends to brainstorm-phase, not just writing-plans-phase.** Lesson from Phase 10 metrics-design brainstorm 2026-05-06 (orchestrator-flagged §1.5 mistake-cost formula premise was empirically wrong; implementer caught by reading `swing/trades/review.py:157-174` and verifying Phase 6 already ships the correct formula — v1.1-alternate's `max(0, plan_followed_R - actual_realized_R)` — not v1.1-main's `max(0, abs(realized_R) - plan_followed_R)` that the brief asserted). Pattern complement to existing 2026-05-04 Phase 6 writing-plans lesson "Brief assertions about column-vs-derived status need empirical verification" + 2026-05-04 Phase 6 lesson "Brief-speculation about consumer code state should be empirically verified at brief-draft time" — same surface (orchestrator inferring code-state assertions without verifying), now extended to BRAINSTORM-phase briefs (not just writing-plans-phase briefs). **Generalization:** when drafting any brainstorm brief that asserts shipped-code state (Phase 6 ships X formula; Phase 7 ships Y schema field; etc.), the orchestrator MUST verify the assertion empirically — read the code OR run a query — before encoding it as binding §1 strategic context. Otherwise the implementer wastes a Codex round catching the brief's own incorrect premise.
- **Repo functions must NOT call `conn.commit()` — caller controls transaction scope.** Lesson from Finviz API integration code-review I1 (operator-witnessed S3, 2026-05-06; fix at `0e02ed6` merged in `002338a`). `swing/data/repos/finviz_api_calls.py:insert_call` called `conn.commit()` internally; this violated `lease.fenced_write()`'s documented contract (callers own BEGIN IMMEDIATE / COMMIT). Inside fenced_write, insert_call's internal commit prematurely closed the transaction; outer COMMIT raised `sqlite3.OperationalError("cannot commit - no transaction is active")` → except clause's ROLLBACK raised identically → bubbled up as `finviz_fetch programming error (continuing)` log on every pipeline run. Audit row was persisted (insert_call's own commit succeeded), so functional behavior was correct — but every pipeline run leaked the spurious log. **Generalization:** the project repo convention is caller-controlled transaction scope. `grep -rn "conn.commit()" swing/data/repos/` should return ZERO occurrences (confirmed pre-existing convention; insert_call was the only outlier). Adversarial review didn't catch this because all unit tests exercised insert_call via raw conn (where internal commit is benign + necessary for default-isolation_level autocommit-after-INSERT semantics); pipeline-path usage with REAL fenced_write was first exercised in operator-witnessed gate. **Pattern for future repo functions:** brief-drafting checklist + adversarial-review watch item — verify no `conn.commit()` calls in new repo files. CLI raw-conn callers commit explicitly in CLI body. **Forward-looking:** Schwab API integration's repo functions inherit this discipline; brief should pre-empt by enumerating the contract.
- **Subprocess cfg-propagation: child-process CLI body is the binding override point, NOT the parent process that spawns it.** Lesson from Finviz API integration writing-plans Codex R2 (2026-05-05) + executing-plans confirmation (2026-05-06). Web route `/pipeline/run` spawns `python -m swing.cli pipeline run`; cfg overrides applied to the web-route's process do NOT propagate to the child via in-memory state. Override-load must live in the child CLI's body. Phase 7e (Finviz API plan) Task 2.5 codifies the pattern (`apply_overrides()` call added to `swing pipeline run` CLI body); discriminating test pins the contract via source-text inspection of the `pipeline_run_cmd` body. **Forward-looking relevance:** Schwab API integration (queued at `docs/phase3e-todo.md` 2026-05-04 entry) will hit the same surface (web → CLI → broker API). Add to writing-plans phase as adversarial-review watch item: "any cfg override surfaced via web route that consumes a CLI subprocess MUST verify override is loaded in the child process, not assumed-inherited from parent."
- **HTMX form-driven endpoints have two browser-only failure surfaces TestClient cannot detect: HX-Request header reset on embedded forms + HX-Redirect for success-path response.** Lesson from Phase 5 configuration page executing-plans re-dispatch (Codex R1 Major 1 + Major 2, fixed in `4d3174d`, 2026-05-02). Two related findings on the new `/config` route: (1) embedded forms inside HTMX-rendered fragments must include `hx-headers='{"HX-Request": "true"}'` on the form element — without it, OriginGuard strict-mode rejects subsequent submits with 403 because the parent page's HTMX header context doesn't propagate to nested forms by default. TestClient passed because it sent the header explicitly; real browser submissions without the propagation hint failed. (2) Success-path response to HTMX POST should be 204 + `HX-Redirect: <url>` header (browser re-navigates via htmx.js), NOT a 303 → swap-target — real browsers using htmx.js swallow 303 transparently and the swap target receives nothing. TestClient verified the 303 status code but not how htmx.js processes it. Pattern complement to the JS-test-harness gap lesson family (2026-04-25 Bug 1; 2026-04-29 hyp-recs success-path; 2026-04-29 entry_post Bug B `<tr>`-leading makeFragment). **Generalization:** when adding ANY new HTMX-driven route handler with form submission, the plan's per-task acceptance criteria MUST enumerate both: (a) embedded-form `hx-headers='{"HX-Request": "true"}'` propagation under OriginGuard strict-mode, and (b) success-path `HX-Redirect` emission instead of 303 swap-target. Operator-witnessed browser verification gate is BINDING for these — both failure modes are invisible to TestClient. Captured in CLAUDE.md gotchas in the same housekeeping commit.
- **Worktree-isolated dispatches + editable installs need verify-command pointed at the worktree, not the editable-install path.** Lesson from Phase 5 configuration page executing-plans re-dispatch operator-witnessed verification (2026-05-02). Operator launched the web server from inside the worktree dir via `python -m swing.cli web`; got 404 on `/config` despite the route being correctly registered in the worktree's `app.py`. Root cause: `pip install -e` registers the package with a path-pointer to the original install path (the main repo dir); subsequent `python -m swing.cli` invocations resolve `swing` via that pointer regardless of cwd. So the running web server was loading the OLD code from main (which had no `/config` route), not the worktree code. Implementer's TDD tests passed because pytest does cwd-based discovery; the CLI entry point doesn't. **Workarounds:** single-command — `PYTHONPATH=. python -m swing.cli web` (PowerShell: `$env:PYTHONPATH = "."; python -m swing.cli web`) prepends worktree path to `sys.path`, beating the editable-install pointer. Persistent — `pip install -e ".[dev,web]"` from inside the worktree dir re-points the editable install at the worktree (revert with the same command from main after merge). **Generalization:** for any dispatch using `superpowers:using-git-worktrees` AND requiring runtime/browser verification via a CLI entry point in the package being modified, the brief MUST specify the verify-command with the worktree-pointing prefix. Not relevant for pure-pytest verification (pytest already does cwd-based discovery). **Add to brief-drafting checklist for worktree-isolated dispatches:** enumerate which verification commands route through the editable-install resolver vs through cwd discovery; specify `PYTHONPATH=.` (or `$env:PYTHONPATH = "."` for PowerShell) or temporary re-install for the former.
- **Exchange-session helper family has multiple members; brief author MUST specify forward (`action_session_for_run`) vs backward (`last_completed_session`) semantically.** Lesson from Phase 6 post-trade review writing-plans dispatch (§A.8 audit-finding orchestrator-pre-approved 2026-05-04). Brief literally specified `action_session_for_run(datetime.now())` for cadence-step anchoring (§2.9 + §0 #5 + §6.2 watch item 5). `action_session_for_run` returns the NEXT (forward-looking) session — correct for "what session am I prepping for?" but wrong for "what period just closed?" Cadence pre-create needs `last_completed_session(now)` (backward-looking; both helpers wrap the same NYSE-calendar + tz infrastructure at `swing/evaluation/dates.py`). Same lesson family as the CLAUDE.md weather-lookup gotcha ("Weather lookup must NOT query by `action_session`" — `action_session` is forward-looking; weather is keyed on `data_asof_date` = last completed session). My brief conflated the gotcha's "use the exchange-session helper, NOT `date.today()`" with "use `action_session_for_run` specifically." **Generalization:** when invoking the exchange-session helper family, brief author MUST specify forward vs backward semantically. **Pattern complement to** "Brief-speculation about consumer code state should be empirically verified at brief-draft time" (Phase 5 writing-plans, 2026-05-01) — same surface (orchestrator inferring code-state assertions from secondary sources rather than verifying empirically); applied here to helper-API choice rather than field-paths.
- **Brief-scoped read-only consumer modules force scope-vs-DRY tradeoff at writing-plans time.** Lesson from Phase 6 post-trade review writing-plans dispatch (§A.1 carve-out audit-finding 2026-05-04). Brief §3.3 listed `swing/journal/` as read-only for Phase 6; plan author needed `_trade_r` + `_trade_closed_date` private-symbol formulas for Review_Log aggregate computation. Plan §A.1 chose to **re-paste 4-line byte-identical formulas** into `swing/trades/review.py` as `_share_weighted_r` + `_trade_closed_date_for_review` rather than (a) expanding the carve-out OR (b) importing private symbols across the boundary. Defensible for tiny helpers (private-prefixed → no API contract; drift risk bounded). **Generalization:** when scoping read-only adjacent modules in briefs, anticipate the scope-vs-DRY tradeoff: either pre-authorize a narrow private-promotion carve-out (lift `_trade_r` → `trade_r` public in the same dispatch) OR accept the re-paste explicitly. Codex review can only ratify the chosen path; the brief's scope discipline forces it.
- **State-bearing entities require enumeration of ALL state-transition UI surfaces in the brief, not just the creation path.** Lesson from Phase 6 post-trade review writing-plans (R1 Critical 1, fixed by adding Task 12b cadence-completion path 2026-05-04). Brief specified Review_Log pre-create (cadence step) + completion semantics (frozen aggregates, atomic transaction) but NOT the completion-path UI (CLI command + web form for marking a Review_Log row complete with persisted aggregates). Codex Critical caught the gap: an entity with pending → completed state machine NEEDS UI for the transition or operators cannot complete it. Implementer added Task 12b (`swing review complete <review_id>` CLI + `/reviews/{id}/complete` web GET/POST + `CadenceCompleteVM`). **Generalization:** for future briefs introducing entities with state machines (created → completed → revised), enumerate every state-transition surface explicitly (CLI invocation path; web form route + view-model; API endpoint if applicable). Pattern complement to "Spec/plan silence on form-driven success-path response shape is a recurrent failure class" (hyp-recs trade-prep R1 M1, 2026-04-29) — both lessons surface that briefs leaving response/transition paths implicit produce predictable Codex findings.
- **Production-write classifier soft-block under auto-mode: AskUserQuestion responses are NOT visible to the auto-mode classifier; only chat-text "yes, run X" authorizations are.** Lesson from Phase 9 Sub-bundle A operator-witnessed gate (2026-05-12; encountered twice in one session). When the orchestrator drives a production-mutating action (`swing db-migrate`; HTMX entry-form submit; any Bash/javascript_tool call that mutates the production DB or user-facing state) AND the operator pre-authorized via `AskUserQuestion`, the auto-mode classifier may STILL soft-block on "high-severity production state change" because the AskUserQuestion answer is not part of its decision input. **Workaround:** when the classifier blocks an action the operator pre-authorized via AskUserQuestion, surface back to the operator with the exact action description + ask for plain-chat "yes" confirmation; on the second invocation the chat-text authorization is visible + the action proceeds. Do NOT try to bypass the classifier in any other way (no `dangerouslyDisableSandbox`; no rewording the action). **Add to brief-drafting checklist for any dispatch that surfaces a production-write operator-witnessed gate:** brief reviewer notes that AskUserQuestion-gated actions may require a second plain-chat confirmation if the classifier soft-blocks; do NOT treat the AskUserQuestion authorization as load-bearing for the classifier's decision. Pattern complement to existing Phase 8 V1 polish "gate-pass = ship authorization" lesson — same surface (operator authorization signal), different consumer (classifier vs orchestrator).
- **`tomli_w.dump` is a one-way TOML serializer: comments + key-order + whitespace are dropped on write.** Lesson from Phase 9 Sub-bundle A mid-Codex-R1 incident (2026-05-12; banked from Sub-bundle A return report §7 item #1). `swing/config_user.py:write_user_overrides()` uses `tomli_w.dump()` to write `~/swing-data/user-config.toml`; any Phase 5 cfg-cascade write OR Phase 9 TOML-divergence repair flow that triggers the write path strips operator comments from the file. This is a pre-existing structural limitation of the tomli_w library (not Phase 9-specific). Operator's restoration after the Sub-bundle A test-pollution incident pulled comments back from a known-good source. **V2 candidates:** (a) switch `write_user_overrides` to `tomlkit` (round-trip preserving — keeps comments + whitespace + key-order); (b) document the constraint in the operator manual + add a stderr advisory line when the write path fires ("TOML write: comments will be stripped; re-add manually if needed"). **Pre-empt for any future dispatch that touches `write_user_overrides`:** brief MUST acknowledge the comment-stripping behavior + propose a disposition (preserve V1 behavior; migrate to tomlkit; add stderr advisory). Pattern complement to the Phase 5 lost-update-race ACCEPTED-with-rationale (single-operator scope) — both about pre-existing config-write surface limitations that V1 tolerates + V2 should harden.

- **Operator's "pause" means STOP all forward motion immediately, even items appearing independently confirmed.** Lesson from post-Phase-12 Phase 13 scope conversation 2026-05-17. Operator said "Pause for additional discussion on charts" mid-flow + "The questions tool does not allow for choosing an option and providing additional input..."; orchestrator interpreted as pause on chart discussion only + continued committing Phase 12.5 rescope (which had operator-confirmed answer from earlier AskUserQuestion). Operator's response: "So now you have immediately jumped past where I was ready to go." **Generalization:** when operator says "pause" / "stop" / "hold off" / "wait" mid-multi-step workflow, halt ALL commits, doc edits, dispatch drafts, AskUserQuestion calls immediately. Do NOT cite "but this part was already operator-confirmed" as license to proceed on that part. If a commit has already landed before the pause signal: note it; offer revert; leave decision to operator. **Banked to auto-memory at `feedback_pause_means_pause.md`** for cross-session persistence. Pattern complement to existing "Once operator-witnessed gate passes, integration merge is orchestrator action" — orchestrator's default-toward-action posture needs to YIELD when operator interrupts.

- **At worktree-side operator-witnessed gates, the `swing` console-script routes to the editable-install path (main repo), NOT the worktree's code. Use `python -m swing.cli <subcommand>` from worktree cwd.** Lesson from post-Phase-12 Sub-bundle 1 S3 gate proposal 2026-05-17. Operator caught: "should I invoke via python rather than swing? I believe swing points to the production code not the worktree code." Orchestrator's initial S3 proposal (`swing schwab fetch --orders` from inside worktree) would have invoked main-repo code, not Sub-bundle 1's widened mapper + classifier + comparator — S3 verification would have been meaningless. Root cause: `pip install -e` places `swing.exe` entry point at `%APPDATA%\Python\Python314\Scripts\`; worktrees share the editable install (only ONE active at a time); operator's 2026-05-17 `pip install -e ".[dev,web]" --force-reinstall --no-deps` recovery pointed it at main repo. **Generalization:** for any gate surface exercising CLI command paths against worktree-local code, propose `python -m swing.cli <subcommand>` invoked from `cd .worktrees/<branch>` cwd, NOT `swing <subcommand>`. Don't propose `python swing/cli.py <subcommand>` either — puts `swing/` on `sys.path[0]`, breaks internal `from swing.X import Y` imports. `python -m swing.cli` is the canonical pattern. Pytest works fine without prefix (filesystem-walk discovery). **Banked to auto-memory at `feedback_worktree_cli_invocation.md`.** Cross-references existing 2026-05-02 "Worktree-isolated dispatches + editable installs need verify-command pointed at the worktree" lesson — extends to interactive operator-driven gate commands, not just brief-specified verify-commands. **Pre-empt in any future executing-plans dispatch brief §3 gate surfaces that propose CLI commands** MUST use `python -m swing.cli ...` form.

- **Orchestrator's wall-clock duration estimates for dispatched work are routinely 3-5x too long. When estimating Phase work, divide naive estimate by 3-5x to land on operator's actual experienced wall-clock.** Lesson from post-Phase-12 Phase 13 scope conversation 2026-05-17. Orchestrator estimated Phase 13 Theme 2 alone at "~30-50 sub-bundle weeks operator-paced"; operator calibration: "generally 3-5 days of work as noted by you is done within one day, but that is fine." Likely sources of the bias: implementer subagents run multi-hour bursts (faster than human-paced engineering); TDD + Codex review + dispatch infrastructure parallelizes verification + implementation; operator dispatches efficiently with mature workflow; orchestrator estimates anchor on naive software-engineering-team duration norms which don't apply to single-operator + AI-implementer setup. **Generalization:** when estimating durations in dispatch briefs, scope-brainstorm docs, sub-bundle decomposition recommendations: take naive estimate (anchored on team-pace assumptions), divide by 3-5x for actual operator wall-clock. Phrase as a range explicitly noting the calibration. For sub-bundle-grain estimates (~1-3 days naive): expect ~half-day to full-day actual. For phase-arc-grain estimates (~6-12 months naive): expect ~2-4 months actual. **Banked to auto-memory at `feedback_time_estimates_overstated.md`.** Pattern complement to existing brief-line-target lesson family — both about calibration of orchestrator estimates against operator-observed actuals.

- **Sub-bundle architectural fix can hold in negative sense (no regression) while positive lift fails to fire — synthetic-fixture-vs-production-emitter shape drift family.** Lesson from post-Phase-12 Sub-bundle 1 S3 production gate 2026-05-17. Sub-bundle 1 shipped V2 Schwab mapper widening with 6 E2E tests passing (3 cassette-recorded + 3 hand-rolled). At S3 production `swing schwab fetch --orders`, ALL 18 production orders had `orderActivityCollection[0].executionLegs[0]` uniformly rejected by `SchwabExecutionLeg.__post_init__` validator at mapper drop+warn → executions=None everywhere → comparator fell through to Path B sentinel emit → reconciliation_run #13 completed cleanly with ZERO false-positive entry/close_price_mismatch (architectural fix HOLDS in negative sense) BUT `tier1_applied_count=0` (positive lift NEVER FIRED on production). Root cause unknown without raw Schwab response shape — `schwab_api_calls` audit table does NOT capture `response_body_json`. **Generalization:** an architectural fix's gate criteria should include BOTH negative-sense verification (no regressions; no false-positives) AND positive-lift verification (the new code path actually fires on production data). For Schwab/external-API integrations, this requires checking that the new processing path produced expected non-zero outputs OR explicit "no eligible inputs" semantics. Without positive-lift verification, a fix can ship in safe-degraded mode indefinitely without anyone noticing. Pattern complement to C.D-arc lessons #2 + #4 (synthetic-fixture-vs-production-emitter shape drift; Pass-1 limit-vs-fill defect family) — same family extended to "fix shipped but doesn't fire" scenarios. **Pre-empt in any architectural-fix executing-plans dispatch:** §3 gate surface MUST include explicit positive-lift verification with binding pass criterion (e.g., "tier1_applied_count > 0 IF eligible inputs present"; "executions field populated on at least 1 production order"). For Schwab integration specifically: V2 candidate to add `response_body_json` column to `schwab_api_calls` schema OR add diagnostic logging at mapper drop-point that captures the rejected shape (banked at Sub-bundle 1.5 dispatch).

- **Per-run-vs-per-fill re-emission family: each fresh reconciliation_run that finds no Schwab match for an open fill emits a new `unmatched_open_fill` discrepancy on that same fill, regardless of prior resolutions.** Lesson from Sub-bundle 1 S3 gate 2026-05-17 — run #12 + run #13 both emitted DHC fill_id=2 + VSAT fill_id=6 unmatched_open_fill (discrepancy ids 46+47 + 48+49) despite operator's prior C.D-era dispositions of the same fills. Underlying fills are pre-Phase-11 (operator-typed-from-memory; reconciliation_status='unreconciled'); Schwab order history doesn't capture them; each run re-discovers the mismatch. **Generalization:** reconciliation framework emits per-run discrepancies based on current state; operator resolutions are per-discrepancy not per-fill. Operationally, this means re-emission is expected on any fill that remains unreconciled (typically pre-Phase-11 entries). For `ambiguity_kind='unsupported'` family, the operator-action menu allows only `acknowledge` or `operator_truth` (NOT `mark_unmatched` which is reserved for `multi_match_within_window` family). C.D-precedent for unmatched pre-Phase-11 fills is `acknowledge` (with reason explaining "re-emission family of C.D-era X; Schwab unmatched; journal canonical"). **Pre-empt in any future reconciliation-flow design:** V2 candidate to add per-fill suppression mechanism (e.g., `fills.reconciliation_suppress_until` column OR `acknowledged_immaterial` global flag) so dispositioned fills don't trigger re-emission on subsequent runs. V1 workflow accepts re-emission + operator dispositions each run. Pattern complement to existing "Counter staleness after inline state mutation" lesson family — both about discrepancy-state-vs-run-state semantics.

- **Bash tool's persistent cwd across invocations CAN drift away from the primary worktree if earlier commands `cd` into a subdirectory. Use `git -C "<absolute-path>"` for cross-worktree git operations rather than relying on cwd.** Lesson from Sub-bundle 1 merge attempt 2026-05-17. Orchestrator had `cd` into `.worktrees/schwab-mapper-bundle-1` for inline gate commands (S1/S2 pytest, dispositions); subsequent merge attempt `git checkout main && git merge --no-ff schwab-mapper-bundle-1` from same shell failed with "fatal: 'main' is already used by worktree at 'C:/Users/rwsmy/swing-trading'" because cwd was the SECONDARY worktree (not primary), where checking out main is forbidden (main is checked out elsewhere). Recovery: `git -C "/c/Users/rwsmy/swing-trading" merge --no-ff schwab-mapper-bundle-1 -m "..."` (absolute path bypasses cwd ambiguity). **Generalization:** for orchestrator git operations that affect a specific worktree, use `git -C "<path>"` explicitly when there's any ambiguity about cwd. Especially relevant for cross-worktree merges where the primary worktree is the merge target but the orchestrator's Bash tool shell has drifted into a secondary worktree from earlier inline commands. Pattern complement to existing 2026-05-02 "Worktree + editable-install verify-command" lesson — both about explicit-path discipline when worktrees + cwd interact non-obviously.

---

## External tools available

Capabilities outside the repo that orchestrator/implementer sessions may consult during conversation. Listing them here so future sessions know they exist; details and governance status live in the linked docs.

- **`qullamaggie` MCP server** — knowledge-base wrapper around Kristjan Kullamägi's trading commentary (437 stream sessions, Oct 2019 – Dec 2021; ~2.5M words; 3,980 rules; 84 setup types; 1,214 tickers). Eight tools surfaced as `mcp__qullamaggie__*`. Configured user-global in `~/.claude.json`; runs as a daemon at `http://localhost:9871/mcp`. Source repo: `C:\Users\rwsmy\qullamaggie-mcp\`. **Reference-only; not a source-of-truth.** Promoting any aspect to production criteria or methodology requires V2.1 §VII.F. Full tool inventory, invocation patterns, and gotchas: `docs/qullamaggie-mcp-capabilities.md`.

---

## Memory entries (cross-session persistence)

The Claude memory system has these durable entries relevant to this project:

- **`MEMORY.md`** index lists project memories (3 new banked 2026-05-17):
  - `project_references.md` — Disciplined Swing Trader PDF + Minervini physical-only book.
  - `project_refactor_intent.md` — refactor later, not now.
  - `feedback_regression_test_arithmetic.md` — verify test arithmetic distinguishes pre-fix from post-fix.
  - `feedback_pause_means_pause.md` (NEW 2026-05-17) — operator's "pause" means STOP all forward motion immediately, even items appearing independently confirmed.
  - `feedback_worktree_cli_invocation.md` (NEW 2026-05-17) — at worktree-side gates, `swing` routes to editable-install path NOT worktree; use `python -m swing.cli` from worktree cwd.
  - `feedback_time_estimates_overstated.md` (NEW 2026-05-17) — orchestrator wall-clock estimates 3-5x too long; divide naive estimates by 3-5x for actual operator-paced wall-clock.

If you make a meaningful process discovery worth carrying across sessions, save it as a memory entry AND update this file's "Lessons captured" section.

---

## Key file locations

| Location | Contents |
|---|---|
| `CLAUDE.md` (root) | Current-state, conventions, gotchas. Auto-loaded. |
| `docs/Bugs.txt` | Operator-reported bug list. |
| `docs/phase3e-todo.md` | Operational backlog (active; canonical filename). |
| `docs/phase3e-todo-archive.md` | Archived SHIPPED + closed entries (grep on demand). |
| `docs/cycle-checklist.md` | Daily/weekly/monthly operator routine. |
| `docs/*-brief.md` | Dispatch briefs (one per implementer session). |
| `docs/superpowers/specs/` | Phase-design specs (e.g., Tranche B-ops session 1 design). |
| `docs/orchestrator-context.md` | This file (active; canonical filename). |
| `docs/orchestrator-context-archive.md` | Archived older lessons + superseded framings (grep on demand). |
| `docs/qullamaggie-mcp-capabilities.md` | Reference for the qullamaggie MCP server (tool inventory, governance status, suggested invocation patterns). |
| `reference/Future Work/` | V2.1, rebuttal-response, archived predecessors. |
| `reference/Future Work/QuantEcon/` | Forward-looking strategic content (program + companions + external references). |
| `reference/methodology/` | Source-of-truth methodology references (e.g., Minervini Trend Template transcription). |
| `research/README.md` | Research branch intro. |
| `research/method-records/` | Method records per V2.1 §IV.B format. |
| `research/studies/` | Study designs and evidence summaries. |
| `research/notes/` | Research notes (data-source evaluations, decision memos). |
| `research/harness/` | Research code (e.g., earnings_proximity replay harness). |
| `swing/` | Production codebase (consumed read-only by research branch). |
| `~/swing-data/swing.db` | Production SQLite DB (outside Drive). |
| `~/swing-data/research-cache/` | Research-branch OHLCV + earnings caches (outside Drive). |

---

## Session-start checklist for fresh orchestrator sessions

1. Read this file end-to-end.
2. Check `git log --oneline -20` to see recent commits.
3. Check `git status` to see untracked files or modified files.
4. Read `CLAUDE.md` (auto-loaded but worth re-skimming for the gotchas list).
5. Skim `docs/phase3e-todo.md` for current operational backlog.
6. Ask the developer: "What's currently in flight or recently dispatched? What's today's question?"
7. If the developer is mid-decision, present options with recommendation; don't decide for them.
8. If the developer raises a new strategic question, propose where in the existing framing it fits before drafting anything.

---

## Session-end checklist (when wrapping up a working session)

1. Update §"Currently in-flight work" with current state.
2. If a meaningful framing decision was made, add to §"Recent decisions and framings."
3. If a process insight was captured, add to §"Lessons captured."
4. If a new file or convention was introduced, update §"Key file locations" or §"Operating processes."
5. **Retention discipline check (per §"Maintenance: retention discipline" above):** end of phase ship → migrate that phase's SHIPPED `phase3e-todo.md` entries to archive (one-phase cooldown); lessons-captured cap exceeded → migrate oldest to archive (or promote to CLAUDE.md if durable code-failure prevention).
6. Don't update for trivia. Bias toward fewer, higher-quality updates.

---

## How to update this file

Small commits via the regular implementer-dispatch pattern, OR direct orchestrator edit during conversation (the developer will commit later as part of housekeeping). Either is fine.

When updating: keep sections in their current order. Add new sub-bullets rather than restructuring sections. The next orchestrator's mental model is shaped by the current organization; preserve it unless the developer agrees to a reorganization.

If active sections grow large enough that bootstrap becomes a token-budget issue, see §"Maintenance: retention discipline" — the structural-separation pattern is established (active vs archive companion); migrate aged content to `docs/orchestrator-context-archive.md` rather than splitting active into multiple files.
