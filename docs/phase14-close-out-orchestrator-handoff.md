# Phase 14 Close-Out -- Orchestrator Handoff (all sub-bundles SHIPPED; polish batch onward)

**Audience:** Fresh Claude Code instance taking on the Phase 14 orchestrator role at the **close-out tail**. ALL Phase 14 feature sub-bundles (SB1-SB5 + SB5.5) are SHIPPED end-to-end. The prior orchestrator handed off (operator-directed 2026-06-01) to start the **close-out polish batch** in a clean context.

**Clean boundary:** **main = `480c5a5`** (0/0 with origin; working tree carries only pre-existing untracked scratch — `*.tmp.txt`, `exports/diagnostics/*`, `docs/phase3e-todo-scrub-inventory-20260530.md` — leave them). ~700+ commits ZERO `Co-Authored-By`. **Schema v23 LOCKED. L2 LOCK green (no re-anchor — the FIRST-ever re-anchor is the Phase-15 v3 item).** Your first action: **author the close-out polish-batch dispatch brief + inline prompt** (the operator will start it).

---

## Bootstrap (read in order)
1. **CLAUDE.md** line-3 "Current state" (all SB1-SB5 + SB5.5 SHIPPED; the close-out tail) + the compressed Gotchas. The "Expansion #N" process disciplines live in [`docs/orchestrator-context.md`](docs/orchestrator-context.md) §"Pre-Codex review + brief-authoring disciplines" — read BOTH.
2. **`docs/phase3e-todo.md`** top entries: **#12** (SB5.5 executing-plans SHIPPED end-to-end — the freshest) + **#5** (the consolidated close-out punch-list — THE canonical tail tracker; the polish-batch item list lives here) + **#9** (the Phase-15 schwabdev-v3 upgrade, DEFERRED). Then #10/#11 (SB5.5 brainstorm/writing-plans) for precedent.
3. **Memory** at `C:\Users\rwsmy\.claude\projects\c--Users-rwsmy-swing-trading\memory\` — esp. the orchestration set: `feedback_orchestrator_performs_merge`, `feedback_orchestrator_qa_implementer_product`, `feedback_no_false_green_claim`, `feedback_commit_brief_before_inline_prompt`, `feedback_always_provide_inline_dispatch_prompt`, `feedback_await_return_before_qa`, `feedback_codex_round_limit_suspended`, `feedback_copowers_codex_mcp_windows_launcher`, `feedback_wsl_native_codex_invocation`, `feedback_implementer_persist_codex_responses`, `feedback_degraded_harness_sequential_tool_calls`, `feedback_visual_gate_both_render_and_browser`, `feedback_seeded_gate_masks_default_state`, `feedback_taskstop_does_not_kill_detached_server`, `feedback_commit_message_trailer_parse_hazard`, `feedback_pause_means_pause`, `feedback_handoff_briefs_only_when_context_actually_exhausting`.

## Phase 14 state (all features SHIPPED)
| Sub-bundle | Ship |
|---|---|
| SB1 data-wiring · SB2 temporal-log (v22) · SB3 chart-surface (v23) · SB4 review+journal · SB5 metrics-overview | `e323339` / `27f8007` / `edd098d` / `31da4a5` / `6206fb6` |
| **SB5.5 Schwab (A-3 web ladder + P14.N7 checker resilience + web health badge)** | **`16b3366`** (operator S6 badge gate PASS; 6976 green on merged main; R2 Codex convergence via plan-grounded rebuttal) |

**Phase 14 lands at v23.** The schwabdev v2.5.1→3.0.5 upgrade + Fernet is the **PHASE 15** item (operator-chosen 2026-05-31; pre-drafted brief `docs/schwabdev-v3-upgrade-brainstorming-dispatch-brief.md` [marked DEFERRED] + investigation `9d4f6a4`; NOT dispatched; it will DELETE P14.N7 + perform the first L2 baseline re-anchor).

## First action + the close-out tail (operator-LOCKed sequence; phase3e-todo #5/#12)
1. **Author the close-out polish-batch dispatch brief** + commit it BEFORE the inline prompt (`feedback_commit_brief_before_inline_prompt`) + provide the inline prompt. The batch (canonical list in phase3e-todo #5 "close-out polish batch"):
   - **P14.N1 (dashboard portion)** — open-positions + hyp-rec TABLE thumbnails (reuse `render_trade_window_thumbnail_svg` `swing/web/trade_charts.py:87`; mirror SB4's journal-listing VM→row pattern).
   - **A-1** market_weather 200MA fetch-window · **A-2** theme2 vcp 5-contraction label crowding (cosmetic) · **A-4** `_bulz_*`→general rename (cosmetic).
   - **A-6** process-grade-trend drill-down chart dark-mode invisibility (the chart's `<polyline>`/`<circle>` use SVG-default colors; give them theme-aware stroke/fill).
   - **A-7 (freshest; operator-reported 2026-06-01)** — the Schwab **web health badge does not render in normal operation** (it hides via `build_schwab_checker_badge -> None` when no liveness sidecar exists, and no checker runs because production tokens are degraded). The fix-investigation must resolve TWO questions: **(i) DESIGN** — render a visible "no checker / UNKNOWN" state (the `Schwab?` warn entry is currently web-unreachable because build returns None first) instead of hiding, so the dashboard always surfaces Schwab health; **AND/OR (ii) WIRING** — confirm whether the checker SHOULD be running under the operator's valid production tokens (if so and it isn't, that's a real A-3/P14.N7 wiring bug). NOTE per `feedback_seeded_gate_masks_default_state`: SB5.5's S6 gate witnessed the badge render only via orchestrator-SEEDED sidecars, which masked this live-absence — so A-7 is a genuine gap the gate didn't surface.
   - **Group (a) minors** (C-1/C-2/C-3/C-5/C-6/C-19; small SB1-era advisories — see #5).
   - **Decomposition:** likely ONE copowers cycle (brainstorm may be light or skip to writing-plans given the items are small + well-specified). The operator may want to triage which items are in/out at brainstorm. The browser-render items (P14.N1 thumbnails, A-6 dark-mode chart, A-7 badge) need an **operator-witnessed render gate** — and per A-7's lesson, witness the UNSEEDED/default state, not just a seeded one.
2. **B-7 operator failure-mode classification** — the Phase 14 FINAL TOUCH (extend the CR.1 review surface for operator-annotated failure reasons; assess schema impact at its brainstorm — may add a nullable review column → v24 STRICT `pre_version`, or reuse existing). Its own small cycle, AFTER the polish batch.
3. **Phase 14 close-out review (Sec 9.1 Q6)** — operator browser-witnessed cross-sub-bundle integration (charts + review/journal + metrics overview + the Schwab surface coherent together) → then refresh CLAUDE.md status-line to **"Phase 14 CLOSED"** at v23.

## Operating disciplines (binding — the same set carried all phase)
- **Merge:** QA-pass / Codex-convergence / operator-gate-pass IS the trigger across all copowers phases; the next-phase dispatch brief + inline prompt are orchestrator actions. Do NOT ask "shall I merge" (`feedback_orchestrator_performs_merge`).
- **Never false-green:** after every merge, RUN `python -m pytest -m "not slow" -q` ON THE MERGED HEAD + READ it before claiming green (`feedback_no_false_green_claim`). The known pre-existing flake `tests/research/.../test_ohlcv_reader_re_export_identity` (xdist co-residency) PASSES IN ISOLATION — isolate it from genuine failures; it's banked as a group-(a) polish item. Baseline: **6976 fast tests** green on main `16b3366`.
- **QA every product against disk** before merge (branch/trailers ZERO Co-Authored-By; merge-base; docs-vs-code scope; schema verdict; LOCKs; spot-check Codex catches; **verify any withdrawn-via-rebuttal Codex finding against the plan on disk** — SB5.5's R1 critical was a legitimate plan-accepted-residual rebuttal, verified). **Await the implementer's RETURN before QA** — touch nothing on an in-flight worktree (`feedback_await_return_before_qa`).
- **Trailer hazard:** keep the FINAL `-m` paragraph plain prose; verify `git log -1 --format='%(trailers)'` is `[]` before every push. NO `Co-Authored-By`; NO `--no-verify`.
- **Codex transport (copowers v2.0.3; MCP DEAD in the VS Code ext):** WSL fallback — `wsl bash -ilc` OR `export PATH="$HOME/.local/node22/bin:$PATH"`; **VERIFY `command -v codex` → `/home/<wsluser>/.local/node22/bin/codex`** before the chain (the bare-shim trap dies `node: not found`). `resume` REJECTS `-s`/`-C`; pre-generate the diff on Windows. v2.0.3 PERSISTS prompt+response per round to `.copowers-findings.md` (read it at QA for the verdict). **Run chains to CONVERGENCE** (the ~5-round cap is suspended). Briefs still state the persist-responses requirement (`feedback_implementer_persist_codex_responses`).
- **Degraded harness:** if mid-batch tool cancellations recur, switch to SINGLE SEQUENTIAL tool calls + re-Read before each Edit + verify each commit; a fresh session clears corrupted read-state (`feedback_degraded_harness_sequential_tool_calls` — this bit SB5.5's first executing-plans session hard).
- **Operator-witnessed gate:** the rendered surface in a real browser is BINDING for UI work. Run the BRANCH server from the worktree (`python -m swing.cli web --port 8081`) against the live v23 DB (read-mostly safe); kill by PID via `Get-NetTCPConnection -LocalPort 8081` → `Stop-Process -Force` + verify the port frees (`feedback_taskstop_does_not_kill_detached_server`). **Witness the UNSEEDED/default state, not just seeded data** (`feedback_seeded_gate_masks_default_state`).
- **Worktree CLI:** `python -m swing.cli` (NOT bare `swing`). After an executing-plans merge, reinstall `swing` from main (`pip install -e . --no-deps`).
- **Codex artifacts are gitignored** (`.copowers-findings.md`, `.codex-*`, `.copowers-session-*.json`) — read them from the worktree to verify a chain; they are NOT committed/merged.
- **Inline prompts:** every dispatch brief gets a paste-ready inline implementer prompt in chat; commit the brief BEFORE the prompt.
- **Pace:** the operator is highly engaged + paces deliberately at boundaries; present milestones + the forward path + let them set pace; honor `feedback_pause_means_pause`.

---

*End of handoff. Clean boundary: ALL Phase 14 feature sub-bundles SHIPPED (SB5.5 at `16b3366`); main = `480c5a5`; v23 LOCKED; L2 green (no re-anchor). First action: author the close-out polish-batch dispatch brief + inline prompt (items P14.N1-dashboard + A-1 + A-2 + A-4 + A-6 + A-7 + group-(a); canonical list in phase3e-todo #5). Then B-7 (final touch) → Phase 14 close-out review (Sec 9.1 Q6) → "Phase 14 CLOSED" at v23. The schwabdev v3 upgrade + Fernet is the tracked Phase-15 item (pre-drafted, NOT dispatched). A-7 (the Schwab badge not rendering in normal use) is the freshest item + carries a design-vs-wiring fix-investigation.*
