# 18-H.4.1 — orchestrator return report (replaceable-holder fix-forward) + Reviewer A/B outputs

**To:** CHARC · **From:** orchestrator · **Date:** 2026-06-16
**Arc:** Phase 18 §18-H.4.1 (fix-forward of 18-H.4 per CHARC direction). **Brief:** `docs/18-H.4.1-replaceable-holder-dispatch-brief.md`.

## 1. Outcome
Merged to `main` at **`1a916375`** (rebased onto `fadef861` — main had advanced via the harness-scaffold arc, docs-only, zero overlap; clean rebase). Merged-head no-false-green: **8559 passed, 5 skipped** (270s), `ruff check swing/` clean. Trailers all empty. L2 lock preserved (zero new `schwabdev.Client(` sites — grep verified). Cell: implementer-opus-high.

## 2. The fix (the verified defect from 18-H.4 is now closed)
Replaceable-holder refactor in `swing/web/app.py`: a new `_SchwabClientHolder` (thread-safe via a `threading.Condition`) holds the live client; the two ladder closures (`_quote_hook`/`_bars_hook`) no longer capture the constructed `client` by value — they `borrow()` the CURRENT client from the shared `app.state.schwab_client_holder` at call time. `/schwab/setup`'s `_release_long_lived_schwab_client` now `drain_and_release()`s the holder (clear the slot → new borrows resolve None→yfinance; wait bounded for in-flight borrows to drain) BEFORE `del`+`gc.collect()`, so NO closure ref AND NO in-flight local ref survives → the client finalizes → the tokens-DB handle releases → the rename succeeds. Reconstruct sets both `app.state.schwab_client` and the holder slot, so the closures auto-pick-up the new client. None-window → yfinance. L2 preserved.

## 3. Reviewer A (review-strong, implementer-run) — WITH REPO ACCESS (the protocol change worked)
Converged R4 `NO_NEW_CRITICAL_MAJOR`. The repo-access change paid off immediately — A found 3 genuine concurrency MAJORs that depend on surrounding (un-diffed) code, exactly the class diff-only review was blind to:
- **R1**: an in-flight ladder fetch on an executor worker holds a transient local ref during release → WinError 32 survives → fixed with the drainable holder + in-flight counter + `drain_and_release` barrier.
- **R2**: the drain timeout (hard-coded 10s) was SHORTER than the 30s Schwab request timeout → a routine slow call outlives the drain → fixed by coupling the drain timeout to `schwab.timeout_seconds + margin`.
- **R3**: the borrow wrapped the L9 scope-bypass yfinance branch → a long yfinance fetch held the client → fixed by scope-gating before the borrow.

**⚠️ FLAG (recipe gap — your lane): Reviewer A ran at `reasoning effort: none`, not the spec'd review-strong/HIGH.** The `-p review-strong` profile is absent in the sub-agent's `$CODEX_HOME`; the recipe fallback (omit `-p`) silently fell back to the DEFAULT effort = `none` — the same disqualifier as the 18-F codex-auto-review. A still found 3 real majors at effort=none (not a no-op), but the binding review did not meet the HIGH-effort spec. **Recommended recipe fix:** the `-p review-strong`-absent fallback MUST also pass `-c model_reasoning_effort=high` (or provision the profile in the cell's `$CODEX_HOME`). The high-effort scrutiny for THIS arc was supplied by the gating Reviewer B (below).

## 4. Reviewer B (codex-auto-review) — GATING, run by the orchestrator, HIGH effort — CLEAN
Ran `codex exec review --base main -c model_reasoning_effort=high` on the rebased cumulative diff (the merge candidate). Verdict verbatim: *"The substantive changes are limited to the Schwab web client holder/release flow and associated tests. I did not identify any discrete correctness issues introduced by the patch."* → **no [P1]/major; gate satisfied.** (B's exploration emitted git "trailing whitespace" diff-noise on the NEW lines of `tests/web/test_routes/test_schwab_setup_self_lock.py` — cosmetic, outside `ruff check swing/`'s scope, NOT a codex finding. Flagged as a trivial nit; do you want `ruff` extended to `tests/` or a cleanup pass?)

## 5. The A/B read for this arc
- Repo-access Reviewer A (even at effort=none) caught the 3 concurrency majors — the repo-access protocol change is validated (the 18-H.4 blind spot is closed).
- Gating Reviewer B at HIGH effort on the final tree found nothing → independent confirmation of the shipped code.
- Net: the two-reviewer model (repo-access A + gating high-effort B) gave strong, complementary coverage. The one process defect (A at effort=none) was fully backstopped by B-at-high.

## 6. Binding witness PENDING
Per the brief, the binding witness is the operator's next REAL Schwab web re-auth (the file-lock is Windows-environment-specific; the regression test + the in-flight-drain tests are the automated proxies). The fix should now actually release the handle in the ladder-active production case.

## 7. Direction / flags for CHARC
- (a) **Recipe gap** (§3): the review-strong-absent fallback drops effort to `none` — fix the fallback to force `-c model_reasoning_effort=high`. Do you want Reviewer A RE-RUN at HIGH on `1a916375` for the record, or is B-at-high-clean sufficient (my read: sufficient — fix-forward posture makes a later top-up cheap)?
- (b) The trailing-whitespace nit in the test file (ruff doesn't cover `tests/`) — extend ruff to `tests/`, or leave it?

## 8. Artifacts (on disk)
- Reviewer A: `.worktrees/18-H.4.1-replaceable-holder/.copowers-findings.md` + `.codex-review-r1..r4.txt`.
- Reviewer B: `.codex-B-final-18-H.4.1.txt` (repo root).
