# Research Director — Context Doc ARCHIVE (section 7 session log, verbatim)

> The FULL verbatim section-7 session log (2026-06-08 -> 2026-06-21), archived from `docs/research-director-context.md` at the Phase-18 close (2026-06-21) for harness-weight compaction (CHARC FORM-flag; the `harness-architecture.md` section-6 director-state convention). Current state: `docs/rd-state.md`. The live context doc keeps sections 1-6 + a chronological digest of the below. Append-only history; future archive passes append here.

## 7. Session log (append-only)

### 2026-06-08 — Session 1 (role established)
- Operator commissioned the strategic-evaluator role and this charter.
- Delivered the big-picture evaluation (§3–§6). **Key miss, corrected by operator:** initially framed the 16 live trades as undisciplined "chasing"/execution failure. Operator corrected — they were the pre-registered 4-hypothesis program (§4); negative R on H3 is success-by-design; cost-of-learning was acknowledged and worth it. Retracted and recalibrated; the correction *strengthened* the P1 shadow-engine thesis (it's the way to answer H1, which is starved).
- Operator confirmed strong preference for blunt, non-sycophantic feedback and explicit call-outs of his own mistakes (saved as memory).
- Open thread handed forward: brainstorm the P1 shadow-expectancy engine; spec the P0 tuition-vs-error instrumentation fix.

### 2026-06-09 — Session 1 (cont.): P1 brainstorm converged; young-name closed
- **P1 shadow-expectancy engine brainstormed end-to-end + Codex-converged** (6 rounds, 3 crit / 20 maj / 13 min, all resolved, zero accepts; spec `03bc562f`). Two structural wins from the review: the original `[realistic, optimistic]` bracket was *unsound* (negative R denominator on gap-up entries) → redesigned to a fixed single entry + exit-only `favorable_reprice` bracket; the multi-detection collapse hardened (canonical-detection rule + identical OHLC/`triggered_open` + input validation + two-level funnel + `unattributed` bucket). **Parked at the writing-plans gate** per operator (young-name WIP first). Transcript `.copowers-findings.md`.
- **Young-name primary-base recall arc CLOSED** (a separate orchestrator the operator ran in parallel; I QA'd the return — **PASS**: zero co-author trailers across the arc, the single `swing/` change is the CLI reg `66fcd7c9`, 7373 fast tests live-verified by operator). Finding in §5. **My evaluator read:** mechanism-validated + specific but **depth-uncalibrated → zero near-term performance impact**; an unvalidated funnel-widener that routes into P1 for eventual edge-pricing — consistent with the program's pattern (rigorous + honest + no deployable edge yet). The cumulative signal is unchanged: the bottleneck is *measuring edge* (P1), not *adding screens*.
- Phase 16 (Observability) Arc-1 brainstorm was commissioned in parallel (`2160bc92`); pure plumbing, **no shadow-engine impact** (verified: Phase 16 touches none of the temporal log / candidates / matcher / engine read-path).

### 2026-06-09 — Session 1 (cont.): P1 SHADOW-EXPECTANCY ENGINE SHIPPED
- **Writing-plans converged** (6 Codex rounds; 4 crit / 17 maj / 8 min, all resolved; plan + spec reconciliations `2ee03c1b`). Round 1 caught **four real correctness bugs** — wrong initial-stop basis, per-leg R double-counting partials (3.2R vs 1.6R), four-scenario means dropping closed trades, pivot-only collapse consistency; rounds 2–6 hardened the funnel contract to airtight (reconciliation invariant + raise-on-malformed both sides).
- **Executing-plans dispatched to an independent implementer** (brief `2348be57`, committed before the inline prompt). Ran copowers:executing-plans in an isolated worktree off v24; converged 3 Codex rounds (0 crit / 5 maj / 1 accepted minor). Notable catch: `validate` still gated eligibility on `candidate.initial_stop` (a field the simulator never uses) → removed, *strengthening* the lock.
- **Orchestrator QA: PASS + MERGED** (`e8d03036`). L2 held (only `swing/cli.py`), zero co-author trailers across 19 commits, no schema from the arc. `main` had advanced to **v25 (Phase 16 Arc 1)** off the v24 branch — the 3-way merge auto-resolved `swing/cli.py` cleanly. Binding suite on the merged head: 7503 passed + the **1 pre-existing** minervini em-dash failure (independently confirmed failing on clean `main` pre-merge); zero new failures, all 95 harness tests green. Operator-authorized in-scope fix (ASCII-transliterated the young-name study doc, `df402810`) → **7504 passed / 0 failed**. Worktree removed, branch deleted.
- **P1 is SHIPPED but NOT YET RUN.** Next move belongs to the operator: run `swing diagnose shadow-expectancy --db <live>` to produce the first H1 scorecard, then let it accumulate signal-pace samples. That run is the measurement the whole research program has been routing toward.

### 2026-06-09 — first live run of P1: BLOCKING join bug + H1-substrate reality
- Pushed the session's work (origin/main synced) and ran the engine against the live DB. **Invocation gap:** bare `swing.exe` fails `ModuleNotFoundError: research` (research/ isn't an installed package + repo root not on sys.path); ran with `PYTHONPATH=<repo root>`. Affects ALL research diagnostics (pre-existing pattern), worth a fix.
- **Result: 42 unique signals, 100% → `no_canonical_detection`, ZERO priced trades, empty scorecard.** The funnel honesty surfaced it cleanly (no fabricated result).
- **Root cause (blocking design bug):** the canonical-detection rule `detection.pivot == candidate.pivot` is a FALSE PREMISE. `detection.pivot` (structural_anchors.evidence.pivot_price) is the PATTERN-geometric pivot — per pattern class, often `0.0` (double_bottom_w / cup_with_handle); `candidate.pivot` is the SCREENING pivot. Different quantities (WULF 25.86/0.0 vs 27.47), never coincide. Synthetic fixtures forced them equal → passed 6+3 Codex rounds; only the live run falsified it (the **synthetic-vs-real-emitter drift** gotcha — already in CLAUDE.md). **Fix:** re-adopt the R3 direction — recompute entry from `candidate.pivot` over the shared frozen forward bars (all (run,ticker) detections share the OHLC; no look-ahead since forward obs are post-detection). A focused correction arc.
- **Two substrate realities the run exposed:** (1) all 42 signals are `watch` — ZERO aplus — so even fixed, the engine prices H2/H3/H4, NOT H1; **H1 stays gated on rare aplus fires.** (2) The log is tiny/young (4 runs, 2026-06-05→09, 219 forward obs) → ~5-session walks → nearly everything open-at-horizon; needs months. **Refined thesis:** P1 accelerates the WATCH hypotheses fast; H1 accrues only as aplus fires (still faster than hand-trading, but NOT signal-pace as I'd framed it). Correcting my own earlier overstatement.

### 2026-06-09 — entry/join correction spec: Codex-converged + QA PASS
- Dispatched the correction brainstorm to a fresh instance (brief `c84369cc`); it returned a Codex-converged spec (`6fd664f7`; 3 rounds, 7 major resolved, `NO_NEW_CRITICAL_MAJOR`). **QA PASS** — independently re-verified the load-bearing claim against the live DB: no-look-ahead holds (**0/219** obs violate `observation_date > data_asof_date`; first obs is ON `detection_date`, which correctly corrects my brief's wrong "strictly after detection_date" framing). Trailers clean.
- **Design (surgical, sound):** entry recomputed as first forward bar `high >= candidate.pivot`; canonical detection demoted to bar-source-only (longest chain, prefix-invariant-checked); removed the falsified `detection.pivot==candidate.pivot` match + `no_canonical_detection` + a SECOND latent bug (`inconsistent_trigger_state` — per-pattern pivots legitimately differ, 8/42 groups); new `no_candidate_pivot` reason. Honest V1 limitation: no close-confirmation admits intraday-touch entries → realistic arm slightly optimistic, mitigated by a reported `entry_bar_weak_close` diagnostic. Invocation-gap fix folded in → ALL research diagnostics now turnkey from the installed entry point. Spec: `2026-06-09-shadow-expectancy-entry-join-correction-design.md`.
- **Substrate reality (restated honestly in the spec):** even fixed, the current log prices ≈zero — every `candidate.pivot` sits above its forward highs; nothing has broken out in the thin 2–3 session windows. The fix unblocks the MECHANISM; priced samples accrue as the log matures + breakouts occur. Success criterion correctly reset to *funnel-routes-correctly + a breakout golden-fixture prices end-to-end*, NOT immediate H1 numbers. **Next: writing-plans.**

### 2026-06-09 — correction writing-plans: Codex-converged + QA PASS
- Dispatched (brief `67a4fe30`); returned a Codex-converged TDD plan (`b59541c7`; 4 rounds, `NO_NEW_CRITICAL_MAJOR`). **QA PASS** — verified on disk: trailers clean; L2 held (only `swing/cli.py`); the plan's edits target the REAL shipped signatures (`collapse_detections`, `_DetView`/`_entry_and_forward`, `validate_candidate_levels(*, pivot)`, the three constants tuples + the to-be-deleted `no_canonical_detection`/`inconsistent_trigger_state` branches all confirmed present). Codex verified against the live DB + shipped code (the discipline the original chain lacked) and caught real defects (a look-ahead test JOIN on a non-existent column; the BULZ fixture's invalid OHLC). 7 tasks; honest caveats accepted (Task-3 collapse/run signature coupling; testkit stays v24). **Next: executing-plans.**

### 2026-06-09 — correction SHIPPED + the attribution-coverage finding
- Executing-plans returned (brief `5a5e3b8c`), Codex-converged round 2, **QA PASS + MERGED `31e7441c`** (L2 held — only `swing/cli.py`; clean 3-way merge onto Phase-16-Arc-2 main; zero co-author trailers; **7516 fast tests green** on the merged head). Codex caught a real honesty bug (attributed zero-observation signals mis-routed to `never_triggered` → `missing_observations`). Worktree removed. Re-ran the engine **turnkey** — the invocation fix is confirmed (runs from the installed entry point without `PYTHONPATH`).
- **The fix WORKED + surfaced the next binding constraint.** Live funnel: 210 detections → 42 unique signals, **`no_canonical_detection` wall GONE**, all 42 join+collapse+reach attribution — but **100% `matched_no_hypothesis`**. Bottleneck moved from the collapse bug (fixed) to **attribution coverage**: all 42 are `watch` (zero aplus); the active hypotheses are NARROW cohorts (H1 aplus / H2 watch∩proximity_20ma-only / H4 watch∩risk_feasibility-only) that don't cover the BROAD #23-widened watch pool. Honest output, not a defect.
- **Three independent reasons the engine prices ≈zero today** (all substrate/coverage, none a defect): (1) attribution coverage gap (above); (2) no breakouts — every `candidate.pivot` sits above its forward highs in the thin 2–3 session windows; (3) no aplus → H1 starved.
- **My recommendation (research-director call):** add a **broad-watch-baseline hypothesis** (bucket==watch, any miss-set) so the engine prices the population the log ACTUALLY contains — which is also the operator's actual trading practice (12/16 live trades were watch). Highest-value next step (the narrow cohorts almost never occur); answers "does the widened watch pool have mechanical edge?" without risking capital. Routes through V2.1 §VII governance (a registry amendment / new migration). Caveat: necessary-not-sufficient — even with coverage, prices ≈zero until the log matures (breakouts). **The instrument is DONE; the measurement now awaits a registry decision + market time, not more engineering.**

### 2026-06-09 — Session 2 (fresh instance): operator GO — both arcs commissioned
- Operator approved P1-NOW and added a second commission: the engine must run **automatically in the operational drumbeat**. Division of responsibility (operator-set): research director finishes tool development (the hypothesis amendment arc) + recommends placement; the **Phase 16 orchestrator implements** the operational integration as a new Phase 16 arc.
- **Pre-dispatch grounding surfaced two load-bearing design hazards** (verified in code, not carried from the handoff): (1) the engine's attribution is a thin wrapper over the PRODUCTION matcher (`research/harness/shadow_expectancy/attribution.py` → `swing/recommendations/hypothesis.py:match_candidate_to_hypotheses`), and the engine routes **>1 match → `multi_match` → unattributed** (`run.py` ~L121) — a naive `bucket==watch` rule would cannibalize H2/H4 attribution the moment their signals appear → the brief mandates **fallback semantics** (baseline = watch not otherwise hypothesized); (2) the same matcher feeds the LIVE recommendation surfaces (`hypothesis_prefill.py`, `dashboard.py` ×2) — an always-matching hypothesis would flood them and outrank H2/H4 via distance-to-target → the brief proposes opt-in containment (`include_baseline=False` default; shadow engine passes True). Also verified live: registry v25 has H1/H2/H4 active, H3 `closed-target-met`; matcher's documented fifth-hypothesis extension point at `hypothesis.py` ~L124; latest export (`20260610T063533Z`) re-confirms 42/42 `matched_no_hypothesis`.
- **Briefs authored + committed:** `docs/broad-watch-baseline-hypothesis-brainstorming-dispatch-brief.md` (migration 0026 v25→v26; proposed pre-registration: name 'Broad-watch baseline', target 30 shadow-priced, baseline criteria with both outcomes pre-committed; tripwires 5/5.0 schema-mandated) and `docs/phase16-shadow-expectancy-drumbeat-integration-commissioning-brief.md` (Phase 16 **Arc 5**). **Placement recommendation:** new best-effort step `shadow_expectancy` AFTER `_step_export`, BEFORE `complete` (data final after `pattern_observe`; last slot protects operator-facing outputs; lease serialization + Arc-1 step-timing/pipeline.log observability for free); **subprocess** of the installed CLI, not an in-process `research/` import (bifurcation boundary + crash isolation); rejected: pre-run (one session stale) and external scheduler (no lease coordination, drift). Arcs are independent — land in either order.
- Next: operator dispatches both inline prompts; research director QAs the brainstorm returns.

### 2026-06-09 — broad-watch brainstorm returned: QA PASS (one process flag) → writing-plans dispatched
- **QA PASS** on the brainstorm return (spec `99e0608f`, trailers clean). Independently verified — not carried from the self-report: (1) the live-DB cohort composition **reproduces exactly** (my own read-only query: 400 watch rows, H2-exact=1, H4-exact=0, dominant sets 110/61/60/36/15/15/10 matching the spec §3.1 table); (2) `tier.py` IS allowlist-locked (`TAXONOMY_COHORTS` 4-tuple + `__post_init__` raises on ≠4 — the R1-C1 inversion is real, the no-touch decision sound); (3) `PHASE16_PRE_MIGRATION_EXPECTED_TABLES` aliases the v24/B7 set → the spec's R2-M2 expected-tables correction (`| {"pipeline_step_timings"}`) is correct; (4) testkit pins `target_version=24` and `test_no_match_returns_empty` (watch+`{orderliness}`→`[]`) WILL flip under the wrapper change — the R2-M3 catch is real; (5) `closed-target-met` appears in migrations only as a CHECK enum value → the spec's fixture-vs-live H3-status distinction (fresh DB = 5 active, live DB = 4 active) is correct and load-bearing for the #11 sweep; (6) `hypothesis_status_history` + the open-interval partial-unique index exist in 0017 → the §2 history-seed requirement is real.
- **Design verdict:** the spec is STRONGER than my commissioning brief — it found the registry-reading surfaces my containment framing missed and split them correctly (progress/process accept the 5th cohort coherently because live-labeling is permitted; tier/deviation self-contain via the allowlist), and rejected the `measurement_only` column with sound YAGNI reasoning (operator-confirmed). Fallback semantics implemented as a structural gate on `not matches` (order-robust), exactly the §2.2 protection.
- **One process flag:** `.copowers-findings.md` persisted Rounds 2–3 + the convergence verdict but left **Round 1's response section empty** (prompt only) — violates `feedback_implementer_persist_codex_responses`. Convergence itself is evidenced (R3 `NO_NEW_CRITICAL_MAJOR` persisted; R1 findings reconstructable from the spec's inline R1-C1/R2-M* citations), so non-blocking — but the writing-plans brief explicitly mandates all-rounds persistence.
- **Writing-plans dispatched:** brief `docs/broad-watch-baseline-hypothesis-writing-plans-dispatch-brief.md` committed; spec is BINDING; locks restated (tier.py untouched; frozen rows; no funnel changes; no column). Phase 16 Arc 5 (drumbeat) accepted by its orchestrator in parallel (`36a7a955`) — independent, no coordination needed.

### 2026-06-09 — writing-plans returned: QA PASS (no flags) → executing-plans dispatched
- **QA PASS** on the plan (`75b6d291`, 821 lines, trailers clean; Codex R1 0-crit/0-maj/2-min → R2 clean, BOTH rounds' full responses persisted including R1 — the prior process flag was addressed). Independently re-verified every material implementer claim: (1) `test_db_v8.py` DOES migrate to head via `ensure_schema` and pins `EXPECTED_SCHEMA_VERSION == 25` at `:112` — the spec's STAY-4 premise was wrong, the implementer's escape-clause bump is correct; (2) the version-pin family is real (~40 grep hits across 15+ migration-test files) — the plan's expansion beyond the spec's §7.1 table is justified and necessary for a green landing; (3) `run_migrations` reads `_current_version` ONCE (`db.py:1153`) and passes it to every gate → the 0025-clamp inversion analysis (`:107` 25→26, broad-watch gate inert on a v24 walk) is correct; (4) `test_repos_hypothesis.py` `== 4` (rows + active) → 5 under fresh-DB-H3-active; (5) Task-3 fixture logic sound (the §9.1.1 fixture correctly moves to the real `{adr}` shape because the v26 testkit seeds H3 ACTIVE — a sharp catch the spec missed; H2-exact anti-cannibalization fixture asserts `[H2]` exactly). Task ordering verified safe (Task-2 testkit bump can't fire the baseline pre-Task-3: attribution still defaults `include_baseline=False`).
- **Executing-plans dispatched:** brief `docs/broad-watch-baseline-hypothesis-executing-plans-dispatch-brief.md` committed. Worktree off main HEAD; migration-number collision check at branch time (Arc 2/Arc 5 in flight); live DB read-only (the v25→v26 live migration fires post-merge on the operator's first `ensure_schema` touch, backup-gated); implementer returns WITHOUT merging — research director QAs the worktree, merges, owns the post-merge suite run.

### 2026-06-09 — executing-plans returned: QA PASS → MERGED `54bd8b94`; P1-NOW SHIPPED
- **QA PASS on the worktree** (branch `bwb-impl`, base `599a0444`, 4 commits): trailers all `[]` (my own audit); production diff = EXACTLY the four planned files (lock-grep verified); migration 0026 text matches the frozen spec §2 verbatim; the two-phase gate (`if include_baseline and not matches:` + active+present check) and the single opt-in caller verified in the diff. Codex evidence on disk (`.codex-review/`, both rounds incl. the 9.4k-line R1; converged `NO_NEW_CRITICAL_MAJOR`); notable: the implementer REJECTED Codex's terse R1 clean verdict and forced a deeper R2 trace — the right instinct. All three plan deviations independently verified sound (the `test_no_schema_change_v3` `<=26` guard; the index-overview headline "5" — `_extract_hypothesis_progress` at `index.py:251-256` counts the registry-driven progress card, NOT the tier allowlist, so the old "4 TAXONOMY_COHORTS" comment was stale; the 5th name appended to the ordered names list).
- **MERGED `54bd8b94`** (clean ort merge onto `ca671850` — Arc 5 had landed underneath; zero file overlap as the implementer reported). **Binding suite on the merged head: 7599 passed / 0 failed / 3 pre-existing skips** (read off the actual run); `ruff check swing/` clean; pushed to origin. Worktree removed (Codex evidence preserved at `.codex-review-bwb-executing/`), branch deleted. CLAUDE.md updated to schema v26 (line 3 + baseline).
- **The arc is complete: instrument + hypothesis + drumbeat all SHIPPED.** Live state: the live DB is still v25/4-rows (verified untouched, read-only probe) — the v26 migration + backup fires on the operator's next write-path touch; Arc 5's nightly step then attributes the watch pool to the baseline each session. Two operator gates remain (the v26 live migration witness + the Arc-5 5b live-run go), then it's market time, not engineering.

### 2026-06-10 — both operator gates PASSED: the measurement program is FULLY LIVE
- Operator reported the Arc-5 5b live-run go complete. **Independently verified** (read-only probes, not the report): live DB at **v26**, 5 registry rows (`Broad-watch baseline` id=5 active), exactly 1 status-history row (the migrate-twice invariant holding in production), backup snapshot `swing-20260609T234259.db` landed. Run #98 artifact (`shadow-expectancy-20260610T095430Z`): **255 detections → 51 unique signals → 0 unattributed** — the `matched_no_hypothesis` wall (100% two sessions ago) is GONE; all 51 attribute to the baseline. Scorecard honestly zero: trigger rate **0/36**, all four censoring scenarios n=0, suppression floors annotating correctly, `entry_bar_weak_close=0`. Exactly the spec's predicted first-live shape.
- **Program state:** temporal log → engine → hypothesis → nightly drumbeat, all live and autonomous. Engineering on the measurement chain is COMPLETE. The remaining research-director-lane engineering item is **P0 (tuition-vs-error instrumentation)** — still open, still worth commissioning when the operator wants it. The run-#98 perf finding (`evaluate=522s` ≈91% of wall) is Phase 16's lane (commissionable on data per CLAUDE.md); it does not affect measurement correctness.
- **Posture:** stop engineering; weekly funnel glance + monthly evaluator read; decision point N≥30 priced shadow signals; ~6-month calendar checkpoint for B-1..B-8 stands (P2).

### 2026-06-10 — P0 commissioned; the labeling question answered
- **Operator commissioned P0** (tuition-vs-error instrumentation). Brief authored + committed: `docs/p0-tuition-vs-error-instrumentation-brainstorming-dispatch-brief.md`. Core design direction: a trade-level `entry_intent` nullable enum via migration 0027 (v27), set-at-entry/correctable-at-review, operator-driven backfill of the 16 historical trades, surfaces FACETED not regraded (the semantic contract: `process_grade` measures execution-given-intent, so the historical "A on designed losers" grades are correct — the fix is display conflation). Orthogonality lock: a designed trade with a slip (VIR `STOP_NOT_PLACED`) still counts as a violation. Measurement chain untouched. The Phase 16 orchestrator is running the run-#98 perf arc in parallel — not my lane.
- **Labeling answer (operator Q):** yes, `Broad-watch baseline` on live watch trades is MANUAL — the entry-form prefill (`lookup_active_recommendation_label` → matcher default `include_baseline=False`) will never suggest it, by the containment design (the frozen 0026 notes: "live recommendation surfaces never surface it"). Operational rule given: take the narrow prefill when one fires (H1/H2/H4 — narrower attribution wins, matching the fallback complement), else hand-type `Broad-watch baseline` (exact name, then optional ` ...`/`; ...` suffix per the 3-rule prefix contract). If manual proves annoying, opting the ENTRY-FORM PREFILL ONLY into the baseline is a one-line + tests mini-amendment — but it touches the frozen notes' "never surface it" language, so it routes as a deliberate governance edit; recommended living with manual at the current ~trade-per-week rate.
- **CORRECTION (2026-06-10, web-GUI mechanics):** my "hand-type the label" guidance was WRONG for the web — the entry form's `hypothesis_label` is a display-only span + HIDDEN input, server-resolved at form-render via `lookup_active_recommendation_label` (`trade_entry_form.html.j2:147-148`, `view_models/trades.py:542`; the V1 server-stamp discipline). No free-text hypothesis field and no post-hoc label editor exist in the web. With the prefill contained (`include_baseline=False`), web-entered watch trades persist `hypothesis_label=NULL`. The ONLY manual labeling path is the CLI `swing trade entry --hypothesis "Broad-watch baseline"`. Identification tell in the GUI: a watch name absent from the hyp-recs panel / entry form showing "Hypothesis: (none)". Consequently the prefill mini-amendment (opt `lookup_active_recommendation_label` ONLY into the baseline; fallback gate gives narrow-first for free) is now RECOMMENDED rather than optional — it is the only way the operator's web-first workflow yields labeled broad-watch trades; routes as a documented micro-arc separate from P0 (it amends the 0026 spec's prefill-containment letter; argument: the entry-form prefill is an ATTRIBUTION surface, not a recommendation surface). Awaiting operator go.
- **Labeling-effectiveness requirements elicited + Phase 16 arc commissioned (2026-06-10):** operator proposed pairing the missing watchlist-tagging feature with the labeling fix. Four operator decisions captured via structured Q&A: (1) tag shape = **pin flag + note** (not free-form tags, not an enum); (2) label source = **matcher-driven** (the prefill amendment; tags do NOT drive labels — labels always mirror shadow attribution); (3) pin semantics = **blocks removal only** (streaks keep counting; unpin → accumulated state applies); (4) the prefill amendment **folds into the Phase 16 arc** (not a separate micro-arc), with the research-director governance authorization granted in the brief (the 0026 spec §5.1 letter is amended by a dated addendum re-classifying the single-ticker entry-form/CLI prefill as an ATTRIBUTION surface; dashboard recommendation surfaces stay contained, regression-tested). Brief: `docs/phase16-watchlist-pin-and-labeling-effectiveness-commissioning-brief.md` (binding R1–R6; ground truth verified in code: no tag columns exist on `watchlist`; the absent-from-candidates skip at `service.py:70-71` flagged for the stale-pinned-row design). Coordination noted: P0 (in flight) and this arc both touch the entry form; migration numbers taken at branch time.
- **P0 brainstorm returned: QA PASS → writing-plans dispatched (2026-06-10).** Spec `0c1efe71` (520 lines, trailers clean; Codex 4 rounds — R1 6 maj / R2 1 / R3 1 / R4 clean — ALL rounds persisted incl. R1). Independently verified: the §1 live-record table matches my own read-only query row-for-row (16 trades; 13 A / 2 B / 1 unreviewed; VIR carries `NO_STOP`+`STOP_NOT_PLACED` at grade B; `disqualifying_process_violation=0` on ALL 15 reviewed rows — the existing flag does no separating work, load-bearing for the column's justification); the `MISTAKE_TAGS` category premise behind the §7.1 always-on execution-discipline panel holds on disk (`NO_STOP` in `risk`, `STOP_NOT_PLACED` in `reconciliation`, `CHASED`/`NO_SETUP` in `entry` — the panel includes real slips, excludes probe-noise); `FAILURE_MODES`-in-models.py colocation + the 0024 ALTER template + all §7 file:line anchors land exactly. Design judgment: the spec's probe-vs-practice reframe (D1) is RIGHT and better than my brief's framing; the Codex-driven execution-discipline panel (separate unfiltered denominator) is the real orthogonality guarantee — accepted as V1 load-bearing. One reviewed-and-accepted boundary: the panel covers risk∪reconciliation only (management-category violations like `MOVED_STOP_AWAY` stay facet-visible but not panel-pinned) — defensible, matches the record's actual failure family, V2-widenable. Writing-plans brief: `docs/p0-tuition-vs-error-writing-plans-dispatch-brief.md` (spec BINDING; D1–D6/L1–L7 closed; migration-number check at executing time — Phase 16 perf + Arc 7 may contend for 0027).
- **P0 writing-plans returned: QA PASS → executing-plans dispatched (2026-06-10).** Plan `92ccf13d` (1763 lines, trailers clean; Codex R1 2 maj [the soft-warn `is not None` discriminator — a real truthiness bug that would have masked an explicit "Unclassified"; the normalized PGT CSS map] → R2 clean; BOTH rounds persisted incl. the 1.5MB raw R1). Independently verified the implementer's beyond-spec claims: (1) `failure_mode` is absent from BOTH `INSERT INTO trades` column lists (`repos/trades.py:216,280`) → the new v27 INSERT branch + four-projection-era resolution is correct (failure_mode is review-only; entry_intent is set-at-entry — a genuine structural difference from the 0024 template); (2) three projection eras confirmed on disk, failure_mode at `_row_to_trade` index 54; (3) the no-hypothesis-count-sweep simplification is sound (0027 adds a column, not a registry row). Judgment calls ruled: Task-1 size accepted (forced by #11 atomicity + the pin sweep — the 0026 precedent); the two-call execution-discipline fetch accepted as primary (≤16-row cohort, exercises the spec-mandated predicate); precedent-derived form-VM internals accepted with executing-time re-anchoring mandated. Executing brief: `docs/p0-tuition-vs-error-executing-plans-dispatch-brief.md` (worktree; migration-number preflight — Phase 16 perf/Arc-7 may contend; live DB read-only; implementer returns WITHOUT merging).
- **Arc-7 commission corrected + pre-Codex spec QA'd (2026-06-10).** Operator surfaced a miscommunication in MY commissioning brief: R2's absent-from-candidates "stale-display" framing missed his intent — the pin must keep a ticker TRACKED (fetched + evaluated) even after it falls off the finviz screen (the use case: future companies to keep an eye on that aren't currently tracked). The Arc-7 pre-Codex spec already carries the corrected model (pinned tickers unioned into the `_step_evaluate` universe at the held-ticker seam, `runner.py` ~1389-1400 — verified on disk; real criteria/bucket/streak, NOT bucket='excluded'). Rulings issued via a dated AMENDMENT in the commissioning brief: deviation (a) `_step_evaluate` carve-out APPROVED (operator-authorized); deviation (b) render-time cohort hint via `include_baseline=True` APPROVED with two riders — the 0026 addendum must enumerate BOTH opt-in call sites, and an opt-in call-site inventory guard test (only prefill + hint builder + engine attribution.py). Plus the measurement-universe ruling nobody had flagged: pin-injected watch-bucket tickers enter the #23 pool → temporal log → shadow broad-watch population — ACCEPTED (pins are the operator's intentional universe; the frozen statement covers it) with riders: the universe-composition note (screen + pinned) in the spec/addendum + per-run pin-injection audit line (tickers + count) for provenance decomposition. Edge flagged for Codex: delisted/no-data pinned ticker routes through F6/error without blanking the row.
- **P0 executing-plans returned: QA PASS → MERGED `cc9a2b46` (2026-06-10); the instrument is BUILT.** Worktree QA verified independently: 12 commits all trailers `[]`; production diff = exactly the plan's File Map (the one beyond-brief file, `swing/journal/analyze.py`, is the plan-specified Task-8 `TradeAnalysis.entry_intent` carrier — 2 lines); ZERO locked files touched (hypothesis.py/tier.py/review.py/progress-card/journal-stats/research all clean); migration 0027 is the exact 0024 shape; the R1-Major-2 presence-gate verified on disk (`"entry_intent" in raw_form` at trades.py:2842 — absent=preserve vs ""=clear). Codex chain properly persisted INCLUDING a VOID round logged + discarded with cause (the `codex exec resume --last` cwd bug read the main repo and false-alarmed "implementation not on disk" — diagnosed, re-run fresh with `-C <worktree>`, corrected R2 = NO_NEW_CRITICAL_MAJOR). **Binding suite on merged head: 7716 passed / 0 failed / 3 pre-existing skips; ruff clean.** Merge pushed (note: the Phase-16 orchestrator shares this checkout and pushed their Arc-7 brief `60da9936` atop my merge — docs-only, green claim unaffected; Arc 7 is moving to writing-plans in parallel). Worktree removed (Codex evidence preserved at `.codex-review-p0-executing-findings.md`); CLAUDE.md → schema v27. **Post-merge operator gates pending:** (1) the live v26→v27 migration (backup-gated, next write-path touch); (2) the `docs/p0-tuition-vs-error-operator-gate.md` browser/CLI witness — facet render, panel invariance under facet toggling, PGT markers light+dark, entry/review set/correct, and the 16-trade `swing trade backfill-intent` walk (the one needing real operator time; he is the only source of truth for intent).
- **P0 both operator gates PASSED (2026-06-10) — FULLY CLOSED; first instrument read taken.** Verified live: schema **v27**, backup `swing-20260610T201507.db`, all 16 trades carry `entry_intent`. **The classification IS the finding: 16/16 `hypothesis_test_by_design`** — the operator overrode the prefill's `standard` suggestions for YOU (the A+ trade), VSAT, and PTEN, declaring the ENTIRE pre-instrument record tuition. Consequences: (1) the `standard` facet starts EMPTY — execution-quality-on-intended-practice accrues from the operator's NEXT standard-intent trade against a zero-noise background (a clean epoch boundary at 2026-06-10); (2) the negative program expectancy is now formally 100%-attributed to the learning program by the operator's own classification; (3) the always-on discipline panel still pins the real slips (VIR `NO_STOP`/`STOP_NOT_PLACED`, SGML `STOP_TOO_WIDE`/`GAP_RISK_IGNORED`, VSAT `STOP_TOO_TIGHT`) regardless — working as designed. **Open verification question put to the operator (D1 divergence — verify intent before labeling):** D1's semantic line maps H1-A+ → `standard`; classifying YOU as by_design deviates. If deliberate epoch-thinking ("everything before the instrument was tuition, incl. the A+ trade"), the charter adopts the epoch framing; if over-broad brush in the walk, the fix is `swing trade backfill-intent --trade-id <id> --force` for the three. Awaiting answer; either way non-blocking.

### 2026-06-10 — TRAINING EPOCH DECLARED COMPLETE (operator-confirmed; the D1 question resolved)
- **Operator confirmed: deliberate epoch thinking.** The initial months were a learning period by intent; the first trades identified the need for the hypothesis system itself; YOU (the lone A+ fire) was treated as a learning event because it was the FIRST hit at that level on a system he didn't yet trust. Now: higher comfort monitoring the field + entering/exiting + higher system trust → **training epoch complete as of 2026-06-10** (learning + data collection continue). Operator-stated behavioral deltas: internal acceptability for low-likelihood wins is RETIRED, and process adherence tightens.
- **The 16/16 by_design record is SETTLED — no relabeling, ever.** The −1.33R program total is formally 100% tuition by the operator's own classification. The `standard` cohort starts empty at the epoch boundary.
- **Forward intent contract (the checkable shape of "epoch complete"):** (1) A+ fires → take per program, `standard` intent; (2) H2/H4 narrow-cohort fires → the ONLY legitimate `by_design` entries remaining (pre-registered program, not retired tuition; H3 is closed — its question is answered); (3) everything else discretionary → either DON'T enter (the shadow engine prices the watch pool free) or enter as `standard` and be graded as practice. **Epoch-integrity tripwires for my future reads:** a new `by_design` trade that isn't an H2/H4 program fire = the declaration failing; any risk/reconciliation tag on a `standard` trade = immediate review (the discipline panel pins it); `CHASED`/`NO_SETUP` recurring on `standard` trades = the old pattern leaking through.

### 2026-06-10 — WATCH STANDARD v1 adopted (operator-commissioned; the contract's final item)
- Operator asked for a monitoring periodicity + a durable standard binding future research-director instances to a consistent watch with consistent action tripwires. **Authored + committed [`docs/research-director-watch-standard.md`](research-director-watch-standard.md) (v1).** Cadence: **weekly operator glance** (mechanical 5-min checklist — drumbeat alive, unattributed=0, accrual moving, intent labels clean) · **monthly evaluator read** (the binding research-director cadence; first trading week of each month; first read due early **July 2026**; fixed checklist + a standard log-entry format appended to this §7) · **quarterly strategic** (every 3rd read, first early **2026-09**) · **ten event tripwires T1–T10** (immediate, calendar-independent: drumbeat-dead, unattributed>0, the first-priced-trade golden gate, the N≥30 broad-watch decision read, A+ fire handling, the epoch-integrity set, registry loss-tripwires, the N≥100 B-gate, the 2026-12 P2 checkpoint). Consistency rules bind instances to fresh-evidence reads, append-only logging, the stop-engineering default posture, and deliberate-only amendments. **Future instances: read the standard at session start alongside this charter.**
### 2026-06-10 — weekly-glance tooling shipped; T3 FIRED + GOLDEN GATE PASSED (the first priced shadow evidence)
- Operator asked for the §2 weekly-glance execution process → built **`scripts/weekly_glance.py`** (read-only: last ~7 artifacts table + newest-age + this-week entries/intents + standard-trade tag scan; ATTENTION lines pre-mapped to T1/T2/T3/T6/T7; ASCII). Verified on real data; first run surfaced a T2 false-positive on the pre-0026 historical artifacts → fixed (T2 = newest-run-only) + documented in the standard §2.1.
- **The first run also fired T3 for real:** run `20260611T041306Z` priced the FIRST 5 signals — trigger 5/28, all broad-watch. **Golden gate executed + PASSED:** hand-walked BOTH unique names against raw forward bars + candidate pivots from the live DB. WULF (pivot 27.47): no-trigger ×2 bars, entry bar 06-09 (H 27.78 ≥ pivot), fill max(27.47, 26.94)=27.47, stop=entry-low 24.095, next-bar low 23.06 → stop-out → **−1.0000R exact**; `weak_close=True` correct (close 25.3 < pivot). VECO (pivot 65.03): entry 06-09 (H 73.03), fill 65.03, stop 61.86, no stop touch, horizon mtm 65.94 → **+0.2871R exact**; `weak_close=False` correct. Entry rule, fill, stop basis, R-arithmetic, and diagnostics ALL verify. Per T3, next obligation: spot-check ~1-in-5 new priced trades until N=10.
- **Statistical caveat recorded into the standard (§3.1.3 + the log format):** the 5 signals are 2 unique names — cross-run re-detections walk identical bars → identical R → correlated samples (WULF −1.0×3, VECO +0.2871×2). Monthly reads + the eventual T4 decision read track signal-level AND name-level N. First scorecard glimpse (meaningless at this N, floors suppress correctly): 2 names, one −1R closed, one +0.29R open.
- **Cadence-references arc commissioned to Phase 16 (2026-06-10, SMALL):** operator asked that the weekly/monthly review cadence pages carry static references to the watch-standard activities (weekly → run `scripts/weekly_glance.py`; monthly → the research-director read + the every-3rd quarterly note). Brief: `docs/phase16-cadence-watch-references-commissioning-brief.md` (R1–R5; static text only, pointer-not-fork discipline so the standard stays the single source of truth; grounded on `cadence_complete.html.j2` / `trades.py:2939`).
### 2026-06-11 — fourth-role question DECIDED: no harness director; CHARC retains custody
- Operator floated a fourth peer role to manage the team-harness artifacts themselves (CLAUDE.md, todos, briefs corpus, charters — context-growth + staleness management, a CHARC-equivalent for the coordination layer). My assessment: the FUNCTION is real (context weight taxes every instance every session and compounds; staleness corrupts evidence-grounded reads) but a fourth role is process-managing-process — CHARC's register already half-owns it (D3 exports, D4 CLAUDE.md weight), the gap is cadence-not-ownership (a standard + executable staleness/weight probe beats a charter), and shared-file custody would need a hard custodian-of-form vs owner-of-content boundary. Recommended: extend CHARC's register to harness artifacts + a mechanical hygiene standard/probe; reserve the role for evidence that phase audits under-serve harness hygiene. **Operator CONCURRED ("process managing process is an accurate criticism") — CHARC maintains harness custody, for now.** Reopen bar: evidence-based, same as any CHARC debt item. If reopened and created, my one hard ask stands: this charter's §7 + the watch standard CONTENT stay mine; form/retention can be custodied.

### 2026-06-11 — peer role established: Tool Development Director (CHARC)
- Operator established the engineering-strategy peer role (`docs/tool-director-context.md`): I originate capability demand (research → trading performance); CHARC owns supply (engineering soundness, sequencing, tool health, debt register). My charter header + §1 updated with the boundary and the operational consequence: **my future commissioning briefs crossing a CHARC §3 tripwire (new schema / new module / new dependency / new standing process / phase-isolation carve-out) get a CHARC pre-dispatch architecture pass** — which my 0026/0027/drumbeat arcs would all have tripped. Cycle contention is operator-arbitrated; mutual lock-respect is in both charters (their §5.5 ↔ my measurement-chain posture). Pointer memory updated.
- **Operator follow-up clarified (important):** broad-watch-baseline trades are NOT intended for actual entry — the hypothesis is SHADOW-measured by design; its decision criterion counts shadow-priced signals ONLY, so a live entry contributes ZERO progress toward N≥30. The labeling guidance applies only to watch trades the operator chooses to take anyway on his own discretion (his pre-existing practice) — label those for live-record coherence + tripwire coverage, but the program does not ask for them. Evaluator guidance restated: with edge unproven and the engine now measuring the watch pool risk-free nightly, discretionary watch entries are tuition spend, not evidence generation; the program-directed live entries remain the RARE narrow-cohort fires (H1 aplus, H2/H4 prefills) per the frozen registry.

### 2026-06-12 — T3 spot-check #1: PBI hand-walk PASSED (priced N = 6 signals / 3 names)
- Run `20260612T105643Z` (nightly #100): 325 detections → 65 signals → 0 unattributed; trigger 6/31. The 6th priced signal is the first NEW unique name since the golden gate — **PBI** (detection 2026-06-10, pipeline run 98, all 5 pattern classes collapsed to one signal; shared forward bars verified identical across detections 226–230).
- **Hand-walk vs live DB (operator-directed, read-only): EXACT MATCH on every field.** Candidate pivot 17.13 (eval run 84). Entry bar 2026-06-10: H 17.13 ≥ pivot — an **exact-touch trigger** (bit-identical floats); fill = max(pivot, open 16.73) = 17.13; stop = entry-bar low 16.44 (R-unit 0.69); `weak_close=True` correct (close 16.91 < pivot). Bar 2026-06-11: no stop touch (low 16.73), day 2 — no partial/trail; open at horizon → mtm at close 17.14 → **+0.014493R → +0.0145 exact** (`horizon_mtm`, `open_at_horizon=True`, realistic==favorable correct since no gap-up). per_session row (mtm 17.1400 @ 2026-06-11) matches. Bonus re-check: VECO's drift +0.2871→+2.0568 re-verified ((71.55−65.03)/3.17).
- **Evaluator note:** PBI is the marginal case of the documented V1 intraday-touch limitation — entry on an exact high==pivot touch a real order might not have filled; the `weak_close`/`entry_bar_ambiguous` diagnostics flag it correctly (4 of 6 priced signals are weak-close entries; worth watching as a realism caveat when the T4 read comes). T3 ledger: golden gate (2) + spot-check #1 (PBI) done; next spot-check due ~5 new priced trades or at N=10. Scorecard still floor-suppressed (closed-only n=3, all correlated WULF).

### 2026-06-13 — session bootstrap + T3 spot-check #2: UNIT hand-walk PASSED (priced N = 10 signals / 4 names → T3 CLOSED)
- **Fresh RD instance bootstrapped** (charter + watch standard + inbox drain + `weekly_glance.py` + git). Inbox = 22 orchestrator FYIs, all Phase-17 ENGINEERING (Arc 17-A single-eval orchestration SHIPPED+GATED `bc05958c`; 17-B step-wrapper extraction `5c86c254`; 17-C closed-as-already-solved [premise error]; 17-D UI quick-wins + the `-n auto` polluter hunt 17-D.4) — CHARC/orchestrator lane, ZERO research bearing. Load-bearing for me: **Arc 17-A explicitly re-verified the measurement chain untouched** (`evaluate_batch` byte-identical, #25 timings intact, no schema/dep) — my measurement-chain locks held through the refactor.
- **Weekly glance fired a T3 ATTENTION** (trigger rate nonzero). Characterized: NOT a new first-priced event (golden gate done 06-11) — the heuristic re-fires on any nonzero trigger. Treated per §4 T3 (read-only hand-walk of the new name).
- **Funnel (newest, run `20260613T091809Z`, 3 runs landed 06-13):** 385 detections → 77 signals → **0 unattributed**; trigger **10/33**. The 10 priced signals are only **4 unique names**: WULF (×3, all −1.0R `initial_stop` **closed**), VECO (×2, +3.9274R **open**), PBI (×2, +0.2754/+0.4750R **open**), UNIT (×3, **open**). The new 4th name since PBI is **UNIT**.
- **UNIT hand-walk vs live DB (read-only): EXACT MATCH on all 3 signals.** Three detections, three SESSION-OWN candidate pivots (the engine correctly uses each detection's own eval-session candidate, NOT a single pivot): det 06-09/run96 pivot **12.385** → bars 06-09 (H 11.71) + 06-10 (H 11.74) below → entry 06-11 (H 12.645≥pivot), fill=max(12.385,open 12.25)=12.385, stop=entry-low 11.92 (R-unit 0.465), horizon 06-12 close 12.75 → (12.75−12.385)/0.465 = **+0.7849R** ✓ (`weak_close=False`, close 12.52≥pivot). det 06-10/run98 pivot **11.88** → entry 06-11 first bar, fill=max(11.88,open 12.25)=**12.25** (gap-open above pivot), stop 11.92 (R-unit 0.33), horizon 12.75 → (12.75−12.25)/0.33 = **+1.5152R** ✓. det 06-11/run99 pivot **11.88** → identical entry/fill/stop/horizon → **+1.5152R** ✓. Entry rule, fill, stop basis, R-arithmetic, weak-close diagnostic all verify. **T3 spot-check #2 = PASS.**
- **T3 CLOSED:** priced N reached the 10-signal "trust the machinery" threshold; ledger = golden gate (WULF+VECO) + spot-check #1 (PBI) + spot-check #2 (UNIT), all exact. Subsequent priced trades trusted without per-trade hand-walk (monthly read still re-derives the funnel/scorecard fresh).
- **Evaluator caveats carried to the T4 read:** (1) **signal-level N=10 grossly overstates evidence** — independent name-level events ≈ 4, and **only WULF (1 name) is terminal/closed — and it lost (−1.0R ×3 correlated).** VECO/PBI/UNIT are all OPEN, UNREALIZED marks; the +0.942R `mtm_at_horizon` headline (n=10) is entirely unrealized open-position marks. **Zero realized edge; the only realized outcomes are losses.** (2) VECO's open mark keeps drifting up across reads (+0.2871 → +2.0568 → **+3.9274R**) — a single volatile unrealized position, not banked edge. (3) `entry_bar_weak_close=4` of 10 — the intraday-touch realism caveat stands. Floors suppress correctly; **meaningless at this N, as designed.** No T1/T2/T5/T6/T7; no live trades this week; epoch intact. **Posture unchanged: stop-engineering + market time;** binding next event is T4 at N≥30 (we're at 10, mostly correlated). First monthly read due first trading week of July 2026.

### 2026-06-13 — operator-directed data-collection framework audit: ONE defect found (temporal-log NaN writer) + two Phase-18 commissions to CHARC
- **Operator commissioned a thorough read-only integrity audit** of the data-collection framework ("is everything being collected, analyzed, and stored as expected" — NOT outcome analysis). Ran the full chain pipeline→candidates→detections→forward-observations→shadow-artifacts via `mode=ro` probes.
- **Clean bill across the chain:** pipeline runs complete (all 14 failures historical Apr–mid-May dev runs); `warnings_json` richly populated (#27 discipline working; warnings are expected-by-design — Arc-6 warm serial fallback, Arc-7 TROX pin-injection); candidates `null_pivot=0`/`error=0`; detections `null_score=0`; **forward-obs coverage = 210 mature detections, ZERO observation-date gaps**; 0 orphans; 0 look-ahead violations; attribution `unattributed=0`; weather_runs recording per session. The 60 zero-obs detections are ALL the future 06-15 session (forward-anchor, expected). `aplus=0` everywhere = documented H1 starvation, not a defect.
- **THE DEFECT (writer-side, sample-attrition not corruption):** `build_ohlc_today_json` (`swing/pipeline/temporal_metadata.py:149`) — the temporal-log construction barrier — guards completed-session + key-presence + provider but **NOT value finiteness**. A completed PAST session whose yfinance adjusted `Close` returned `NaN` (Arc-8 trailing-ragged artifact; O/H/L/V present, Close=NaN) was serialized into the immutable log. **103 forward obs / 19 tickers, ALL the 2026-06-10 session, all Close=NaN** (bounded — full 1287-row scan found non-finite OHLC ONLY on 06-10). Arc-8's `_trim_trailing_ragged` lives ONLY in `swing/data/ohlcv_archive.py` — never mirrored to this 2nd write path (two-path divergence, the recurring parallel-archive gotcha family).
- **Consequence + the engine's saving grace:** the engine's `validate_bars` (`run.py:147`) honestly rejects any chain containing a non-finite bar → `excluded/invalid_ohlc` BEFORE the simulator → **no R is ever poisoned** (today's 4 priced names hand-verified clean; the engine is robust — the defect is purely collection/storage). But the cost is throughput: **23 of 77 attributed signals (30%) permanently excluded** (manifest: `invalid_ohlc` 06-10:0 → 06-11:22 → stable 23) in a sample-starved measurement. **Observability gap:** `invalid_ohlc` is manifest-only — absent from `summary.md` and `weekly_glance.py` — so operator surfaces read all-clear (trigger 10/33, unattributed=0) while 30% silently dropped out. This is exactly the blind spot that hid the defect 06-10→06-13.
- **Posture deviation justified (watch-standard §6.4):** a correctness defect in the instrument that attrits ~30%, recurs, and is operator-invisible warrants fixing now (operator concurred, flagged HIGH).
- **Two RD→CHARC commissioning briefs authored + committed `c9205d1d` (pushed) + posted to CHARC** (`query`, thread `phase18-data-collection-integrity`, `20260613T205703Z`): (1) `docs/temporal-log-nan-writer-fix-commissioning-brief.md` — finiteness guard at the write barrier (mirror Arc-8 via a shared helper), LOCK validate_bars unchanged, open Qs = skip-vs-reject / the 103 existing rows (leave-vs-governed-backfill, cost it) / surface excluded reasons; (2) `docs/data-collection-health-monitor-commissioning-brief.md` — mechanize this audit as a read-only integrity monitor (the layer weekly_glance omits), key open Q = nightly-flag vs spin-up-only cadence, LOCKS read-only/don't-fork-funnel/ASCII/no-dep, RD owns the post-build watch-standard amendment. Both crossed the charter-§1 CHARC tripwire (temporal-log writer / new standing process); approval + Phase-18 sequencing stays operator-carried. RD will QA the returns.

### 2026-06-13 — CHARC backfill RFI answered: independent position = DON'T backfill (the writer fix is unambiguous)
- CHARC's architecture pass on both briefs = PASS w/ conditions C1–C5 (`docs/phase18-rd-briefs-charc-architecture-pass.md`, `bb4e72a3`); the writer fix is clear; the **103-row BACKFILL track** is where the operator wanted a careful multi-perspective read. CHARC RFI'd my independent reasoning (deliberately withholding his + a parallel Codex read to avoid anchoring). Position doc: `docs/phase18-temporal-backfill-rd-position.md` (`580be9d1`, pushed); posted on-thread `phase18-temporal-backfill` (`20260613T213724Z`).
- **Verified the payoff instead of assuming it (read-only):** (1) 18 of 19 NaN-tickers do NOT re-detect cleanly (fell off screen after 06-08; only UCTT re-detects clean) — so the loss is NOT redundant copies (corrected my own first assumption); BUT (2) only **2 of 19 would ever TRIGGER** (entry rule keys on HIGH = present, so exact: COHU 61.80≥58.47, UCTT 110.25≥94.95); the other 17 never reach pivot = 0R `never_triggered`; (3) the gate counts TRIGGERED signals (0026 frozen "N≥30 priced shadow signals"; scorecard priced = `triggered and realized_r is not None`); so (4) **net recoverable toward N≥30 ≈ 1 genuinely-unique priced name (COHU)** — UCTT is redundant (clean re-detections capture it). 10 priced/4 names → ~12/~5. Immaterial.
- **Recommendation: ship the writer fix; DON'T backfill.** The asymmetry decides: not-backfilling costs ~1 name and is recoverable by time (the gate is market-time-gated, not attrition-gated); backfilling-wrong erodes the immutable log's anti-drift reproducibility guarantee (the project's answer to gotcha #26) and is NOT recoverable. Default-if-ambiguous = DON'T (substrate integrity > marginal N). If the operator ever wants COHU-class recovery, the correct lever is an ANALYSIS-layer single-interior-NaN tolerance (preserves immutability), NOT mutating the log — but not worth it for ~1 name. **Q4: DELAYING marginally, not blocking.**
- **Independent-of-decision caveat banked for the T4 read:** the excluded cohort triggers ~2/19 (11%) vs included 10/33 (30%) — invalid_ohlc exclusion mildly INFLATES the observed trigger rate (older fizzles dropped). Note at T4. CHARC reconciles RD+Codex+his read into an operator synthesis; approval stays operator-carried.

### 2026-06-14 — Phase 18 opened; Arc 18-A (NaN writer fix) writing-plans QA'd; RD decision-#3 ruling (RD is merge-blocking)
- **Phase 17 CLOSED (orchestrator + CHARC clean-to-close);** the measurement chain held (17-A verified `evaluate_batch`/timings untouched). **Phase 18 (data-collection integrity) opened** — Arc 18-A = my temporal-log NaN-writer-fix commission. Writing-plans returned Codex-converged (plan `b82c5abb`, worktree `.worktrees/phase18-arc-a`), orchestrator-QA PASS. **Three-eyes merge gate: orchestrator QA + CHARC (C1–C3) + RD (measurement-integrity) — RD is MERGE-BLOCKING.**
- **Design (sound):** TWO layers — caller `_step_pattern_observe` SKIPs the non-finite bar with a `warnings_json` `reason="non_finite_ohlc"` (the primary, graceful path, mirroring the shipped `bar is None` precedent) + serializer `build_ohlc_today_json` ALSO raises (belt, unreachable in prod). Shared predicate `is_finite_ohlc` in new `swing/data/ohlcv_finiteness.py` (`math.isfinite`, OHLC-only, volume-exempt) — matches `validate_bars` exactly → **writer↔engine finiteness PARITY** (the property I most require; prevents a new two-path divergence).
- **Plan caught more than my brief (QA the divergence, directive #10):** a NaN-close bar ALSO drives a phantom `('triggered_open','entry_fired')` status transition at `_advance_status` (`NaN < x` False → `high ≥ pivot`) — corrupting the forward-obs STATUS field, not just the OHLC json. My audit missed this; the caller-skip prevents BOTH. Stronger than the commission.
- **RD decision-#3 ruling = KEEP the serializer raise** (posted charc,operator `20260614T023147Z`, thread `phase18-arc-a`). Reasoning: unreachable in prod (caller pre-skips) → ZERO effect on recorded data → can't threaten integrity by construction; fires ONLY on a future bypass, where fail-LOUD is the correct backstop for an immutable substrate (silent-skip would hide the bug). No lock conflict — my §4.1 skip-not-raise governs the CALLER (satisfied); LOCK 2 "add finiteness" satisfied by the raise. Concur w/ orchestrator lean; fallback (delete the raise) is acceptable but inferior (single-point protection, no fail-loud backstop).
- **Forward-looking improvement named:** post-fix a future ragged fetch yields a one-session HOLE (engine tolerates gaps, walks the chain, PRICES it) instead of a NaN (whole-signal invalid_ohlc exclusion) — strict improvement on the attrition I found. **Plan-level measurement gate CLEARED** (LOCK 1 validate_bars untouched, LOCK 3 no backfill, C2a no migration). **Executing-plans GO from the RD seat.** Final merge-blocking sign-off at the executing return, QA'd vs the shipped diff. Watch-items carried: (a) skip must not corrupt `sessions_since_detection`/horizon counting on a hole; (b) re-confirm writer↔`validate_bars` predicate parity in the shipped diff; (c) the skip warning carries ticker+date+reason for 18-D (health monitor) to consume.
- **18-A EXECUTING RETURN — RD measurement-integrity sign-off = PASS (merge-unblocked from the RD seat).** Verified independently on disk (branch `phase18-arc-a` @ `b3e7b911`, 6 commits trailers `[]`, +313/-6, 8 files): LOCK 1 (`validate.py` 0-line diff) · LOCK 3/C2a (no migration/backfill; 103 rows untouched) · **watch-item (b) parity** — `is_finite_ohlc = all(math.isfinite(v))` OHLC-only/volume-exempt/finiteness-only, matching the engine's `_finite_nonneg` finiteness dimension exactly (non-negativity stays the engine's; no over-rejection) — self-documented in the predicate docstring · **watch-item (a)** — caller skip (runner.py:2995) precedes `_advance_status` (3003) so a NaN bar can't drive a phantom `triggered_open`; `sessions_since` date-derived (3002) so a hole can't misnumber; regression test present · **watch-item (c)** — exact 4-key warning dict `{step,ticker,observation_date,reason=non_finite_ohlc}` + `continue`, list-equality-locked · DECISION #3 — serializer raise built AFTER the existing guards (LOCK 2 nothing removed) · LOCK 4 — archive trim trailing-only walk, interior preserved, +inf superset, Arc-8 green. **CONCUR with the Codex R1-Major-1 ACCEPT** (trim raises on pd.NA/None, not fixed): broadening would break the writer↔engine parity lock (validate_bars is equally pd.NA-intolerant) + stdlib-only/C1; prod OHLC is float64 — keeping the predicate byte-identical to the engine matters more than a hypothetical object-dtype provider. No pushback. Posted charc,operator (`20260614T035815Z` corrected copy; the first `035724Z` lost an inline snippet to a shell-backtick substitution). Merge proceeds on CHARC final C1–C3 + operator auth; orchestrator owns the binding green-on-MERGED-head re-run.
- **18-A SHIPPED + CLOSED** (orchestrator merge `65c62083` housekeeping; arc commits `c12a50ec`/`b0f0562f`/`c6ca75f7` on main — RD-verified present on origin/main; `ohlcv_finiteness.py` present; `validate.py` still untouched on main = LOCK 1 held post-merge). Three-eye gate fully discharged (orchestrator QA + RD measurement-integrity + CHARC C1–C3, all diff-level) + operator-authorized; ff-only linear merge; **binding no-false-green: full fast suite 8146 passed / 4 skipped / 0 failed ON THE MERGED HEAD**; ruff clean; schema v29 unchanged; trailers `[]`. **The temporal-log NaN writer defect is fixed in production** — future ragged fetches skip-with-warning (one-session hole the engine prices) instead of poisoning the immutable log; the 103 existing rows stay withdrawn/age-out (locked); the structured skip warning feeds 18-D. The first Phase-18 arc — and the first defect from my 2026-06-13 audit — is closed end-to-end. Next Phase-18 arc (incl. the 18-D health monitor) awaits operator sequencing.
- **18-B (OHLC write-path finiteness audit & consolidation) writing-plans — RD-lens read (binding sign-off deferred to executing).** CHARC-lane consolidation arc (find/kill remaining write-path finiteness gaps), measurement-adjacent. Plan `dbadd0ae` QA'd PASS by orchestrator (first sub-agent-dispatch pilot — PASS). **Confirmed on disk:** the gap is real — `_trim_trailing_ragged` (shared `is_finite_ohlc`) guards ohlcv_archive.py:264/697 but NOT `write_window` (:833); `marketdata_ladder._persist_window_to_archive` (:165) routes a freshly-fetched Schwab window through it unguarded → Shape-A parquet → `read_or_fetch_archive` → candidates → `candidate.pivot` → shadow engine (measurement-relevant). Fix = generalize `_trim_trailing_ragged` with a `columns=` param, reuse the ONE predicate (no third copy), preserves parity. **`migrate_prices_cache.py` OUT — CONCUR (measurement-integrity):** verified `_consolidate_ticker` = read_parquet→concat→to_parquet (moves existing cached data, never fetches fresh → can't introduce new ragged NaN); no measurement hole leaving it OUT (engineering-exhaustiveness final call is CHARC's). My executing-gate locks restated (validate_bars untouched, parity on the new path, gap actually closed, no third copy, no schema, interior preserved). Posted charc,operator `20260614T053345Z`.
- **18-B EXECUTING — RD measurement-integrity sign-off = PASS (merge-unblocked).** Verified on disk (branch `phase18-arc-b` `fc602d8d`→`9ceccf41`, 2 code commits trailers `[]`, +166/-5, 4 files): GAP CLOSED — `marketdata_ladder._persist_window_to_archive` now runs the shared `_trim_trailing_ragged(df, columns=("open","high","low","close"))` before `write_window` (integrations→data); C1 exactly ONE def each (`is_finite_ohlc`/`_trim_trailing_ragged`), gap closes by REUSE not copy; PARITY preserved — Task 1 only parameterizes column NAMES (`columns=` default legacy capitalized; finiteness logic unchanged: math.isfinite/OHLC-only/volume-exempt/no `>=0`); LOCK 1 validate.py 0-diff; LOCK 3 no schema; LOCK 4 `write_window`/`_write_archive_atomic` untouched (trailing-only/interior preserved); discriminating tests confirm real-06-10-shape trim pre→post, valid+volume-only-NaN unchanged. Hardens the Schwab-ladder archive-ingest boundary (Shape-A parquet → `read_or_fetch_archive` → candidates → `candidate.pivot` → shadow engine); kills the #24-#26 two-path divergence; correct regardless of Schwab activation (sandbox→yfinance). No pushback. First executing-via-sub-agent went clean (Codex R1 converged, real transcript verified) — the QA discipline my topology clearance depends on held. Posted charc,operator `20260614T072715Z`. Merge on CHARC C1/parity/scope + operator auth; orchestrator owns the green-on-merged-head re-run.
- **18-B SHIPPED + CLOSED** (merge housekeeping `cf7681d1`; arc commits `8900d6a7`/`5cce72db` — RD-verified on origin/main; `validate.py` untouched + `ohlcv_finiteness.py` still the single predicate post-merge = LOCK 1 + C1 held). Three-eye gate discharged (orchestrator + RD + CHARC, diff-level) + operator-authorized; ff-only linear; **binding no-false-green: 8153 passed / 4 skipped / 0 failed on the MERGED HEAD**; ruff clean; v29 unchanged; trailers `[]`. **SUBSTANCE: the audit found EXACTLY ONE gap (Schwab-ladder archive-ingest) and closed it via the ONE shared predicate — the #24-#26 two-path-divergence class is now killed at the ingest boundary.** Both write-barrier arcs (18-A writer + 18-B consolidation) from my 2026-06-13 audit are closed end-to-end. MILESTONE: first executing-via-sub-agent proven (QA discipline held). Open Phase-18: the 18-D health monitor (my 2nd commission) + 18-C/others await operator sequencing.
- **18-C (yfinance call audit; CHARC-lane, feeds both monitors) writing-plans — RD measurement read, plan CLEARED from the measurement seat.** New `yfinance_calls` audit table (migration 0030, v29→v30) recording every `yf.download` (status/timing/rows/ticker/call_type/surface/run-linkage), mirroring `schwab_api_calls`. Codex-converged 17 rounds; orchestrator-QA PASS; 3 decisions for CHARC+RD. **Resolved decision #3's RD asks (grounded in the plan text on disk):** (b) busy_timeout=0/best-effort-drop is measurement-SAFE — records AROUND the raw fetch, never alters call/kwargs/result (plan:5,130), the lock is sub-ms and never held across `yf.download` so the fetch is NEVER delayed (plan:125-128); cost = audit-row incompleteness under contention, never measurement OHLC; correct priority (protect the hot fetch path that feeds candidates+temporal-log over audit completeness). (a) captures the monitors' TRANSPORT dimension (empty-vs-error first-class = F6 signal; dead `rate_limited` correctly removed). **LOAD-BEARING CLARIFICATION on the record for the 18-D build:** `status='success'` = TRANSPORT success (raw rows returned), NOT data usability — an all-NaN-Close frame records `success` (literally the 18-A defect shape), so yfinance_calls CANNOT catch the non-finite class; 18-D's own temporal-log non-finite scan stays the usability authority. **18-C and 18-D are COMPLEMENTARY (transport health vs data usability), not redundant** — I'll hold the 18-D build to consuming `yfinance_calls.status` as a transport indicator only + best-effort (drops/stale-`in_flight` = incomplete, plan:129). Proportionality (#3): the recorder complexity each traces to a Codex CRITICAL under LOCK 1 (never perturb the hot fetch path = a measurement-protection req) → justified from my lens; minimal-design call is CHARC's. #1 enum (download_intraday/surface=web) HELPS the monitors filter measurement-feeding vs display fetches — support; #2 FK RESTRICT-vs-SET-NULL = no measurement impact (audit table, not measurement-consumed) → defer to CHARC. **Executing GO from the RD seat** once CHARC rules #1/#2/#3-arch; binding sign-off at the executing return (records-around + busy_timeout=0 + migration 0030 discipline on the shipped diff; standard v29→v30 live-migration+backup operator gate at merge). Posted charc,operator `20260614T180834Z`.
- **18-C EXECUTING — RD measurement-integrity sign-off = PASS (merge-unblocked).** Verified on disk (branch `phase18-arc-c` `30a4b838`→`0206f9d9`): RECORDS-AROUND confirmed — `_record_yf_download` (yfinance_audit.py:220) returns the EXACT `fetch_fn()` result unchanged on every path (start-fail→direct fetch:248; success→269; classification-fail→266), fetch called ONCE, classification wrapped; `busy_timeout=0` (start+finish) so the hot fetch path that feeds candidates+temporal-log is NEVER delayed; `ohlcv_archive` (measurement-feeding) keeps DB-free-at-import (lazy recorder import, no-context fast path imports nothing). MEASUREMENT-CHAIN UNTOUCHED — diff name-list has NONE of validate/hypothesis/evaluation/candidates/pattern_forward/temporal_metadata/pattern_detection; runner.py is a pure reindent wrapping the post-lease body in the non-throwing `_pipeline_yfinance_audit_scope` (step LOGIC lives in untouched modules → identical compute; orchestrator's 474 pipeline tests + R4 LeaseRevoked false-positive rebuttal corroborate). MIGRATION 0030 PURELY ADDITIVE — `0030_phase18_yfinance_call_audit.sql` in-file BEGIN/COMMIT (#9), CREATE TABLE yfinance_calls only, version→30, ON DELETE SET NULL, NO ALTER/DROP of any measurement table; strict pre_version==29 backup-gate mirror. Transport-vs-usability boundary preserved (success=transport, not usability; 18-D non-finite scan stays authority). ZERO Co-Authored-By across the range (streak intact). **MANDATORY pre-merge (streak hygiene, in the orchestrator merge plan): commit `1bf9f511` body begins "Runner:" → git parses it as a `%(trailers)` (the trailer-parse-hazard; NOT a co-author) → reword to prose before merge.** No measurement pushback. Posted charc,operator `20260614T194035Z`. Merge on CHARC C1-C4 + the trailer reword + operator-witnessed v29→v30 live-migration+backup gate.
- **18-C SHIPPED + CLOSED** (arc `2b381461`; housekeeping `fbb77a48`). Three-eye gate discharged (orchestrator + RD + CHARC C1-C4) + operator-authorized; trailer reword applied (tree byte-identical, range trailers `[]`); ff-only; 8258 green on merged head; ruff clean. **RD-verified the LIVE DB post-migration (read-only, my lane — the migration touched the measurement substrate's DB): schema_version=30, integrity_check=ok, yfinance_calls present, and every measurement table INTACT with row counts identical to my 06-13 audit (candidates 7888, detections 385, forward-obs 1287, eval_runs 90, pipeline_runs 103) — purely additive, measurement data undisturbed; validate.py untouched post-merge (LOCK 1).** Backup `swing-pre-phase18-arc-c-migration-20260614T202039Z.db`. yfinance fetches are now observable (transport-layer blind spot closed) — feeds 18-D/18-E. ZERO Co-Authored-By holds; schema v30. **All three write-barrier/observability arcs from my 06-13 audit are closed (18-A writer · 18-B consolidation · 18-C audit). NEXT IN MY LANE: 18-D (my 2nd commission — the health monitor that mechanizes the audit + consumes yfinance_calls as a TRANSPORT indicator alongside its own non-finite usability scan); awaits operator sequencing.**
- **18-E (operational tool-health monitor; CHARC/operator lane) CLOSED `65de5a7f`** — FYI, not my lane (operational, not research-measurement). Emits the section-3 health envelope. **Notable cross-lane lesson:** the operator-witness leg caught a false-RED weather defect (`get_latest(conn)` defaults `ticker='QQQ'`; the live system records weather under `cfg.rs.benchmark_ticker='SPY'` — 39 SPY rows, zero QQQ) that byte-tests + 4 Codex rounds BOTH missed — the exact `feedback_adversarial_review_verify_data_shapes` failure mode (synthetic fixture seeded under the default ticker). Supports the proposed CLAUDE.md gotcha (read-path over a ticker-keyed table must pass the CONFIGURED benchmark + verify vs live-DB shape).
- **18-F (GUI health stoplights; CHARC/web lane, NOT RD-merge-blocking) writing-plans converged — RD contract review for 18-D fitness: CONFIRMED.** 18-F locks the research-artifact contract my future 18-D commission MUST conform to: PATH `exports/research/health/latest.json` (shared `RESEARCH_HEALTH_ARTIFACT_PATH`), section-3 ENVELOPE `{monitor, generated_ts, overall∈{green,yellow,red}, checks:[{key,status,summary,detail}]}`, IDENTITY gate `monitor=="research_measurement"` (greys non-conformant objects → the existing shadow-expectancy exports can't false-green it), STALENESS gate `generated_ts ≤ 7d`. All sound for 18-D (my check set maps 1:1 to the checks array). **ONE interaction flagged: the 7-day staleness gate constrains 18-D's cadence (latest.json must refresh ≥ weekly) → reinforces my standing recommendation that 18-D needs the NIGHTLY best-effort artifact-write (via 17-B step_guard), not spin-up-only** — now doubly-motivated (catch defects same-day + keep the stoplight lit). 18-D will IMPORT the shared constants (C1 single-source), set `monitor="research_measurement"`, write nightly. Posted charc,operator `20260615T004416Z`. **18-D is the last research-lane Phase-18 arc + my 2nd commission; awaits operator sequencing.**
- **18-D COMMISSIONED (2026-06-15) — my 2nd commission, operator-sequenced NEXT; routed to CHARC for the sec-3 confirmation.** 18-F merged (`f075e7b5`) → the research-artifact contract is LIVE on main; CHARC confirmed my import dependency satisfied. Finalized the health-monitor brief (`09499109`, pushed): the 2026-06-13 problem statement + §4 locks stand; a §6 ADDENDUM brings it current for the 3 shipped developments (18-C `yfinance_calls`; 18-E `compute_tool_health` precedent to mirror; 18-F live contract) and resolves §3 per CHARC's **C4** (script-first, defer the nightly STEP to a fast-follow; nightly MUST use 17-B `step_guard` B-shape) + **C5** (role_mail-on-ATTENTION rides the nightly half). **Design:** `compute_research_health(conn)→ResearchHealthStatus` in `swing/monitoring/` (sibling of compute_tool_health) emitting the section-3 envelope (monitor="research_measurement", overall=worst_of, fresh ts, IMPORT the 3 constants) + `scripts/research_health.py` (mirror tool_health.py); 7 checks = the audit probe set + **NEW `fetch_transport_health`** (yfinance_calls error/empty RATE as a TRANSPORT indicator ONLY — best-effort/stale-in_flight=incomplete; success≠usability; `temporal_log_finiteness` stays the usability authority — the 18-C boundary). **ONE refinement flagged for CHARC's sec-3 ruling (a C5 sharpening forced by 18-F, which post-dates the C4/C5 pass): the SCRIPT-first half must ALSO write `latest.json` (not ASCII-only per C5) or the live stoplight never lights — requested APPROVE; does not change C4's deferred nightly STEP.** RD merge-blocking at the executing return (read-only/mode=ro, no funnel fork, transport-vs-usability boundary, nightly step_guard never perturbs the pipeline). Posted charc,operator `20260615T060546Z` (query). Awaiting CHARC sec-3 confirmation + the §6.3 ruling → operator dispatches writing-plans. Post-build: RD amends watch-standard §3.1 to reference the monitor.
- **18-D CLEARED for dispatch (2026-06-15).** CHARC sec-3 envelope pass CONFIRMED + the C5 refinement APPROVED (script writes BOTH ASCII + latest.json; artifact-write, read-only LOCK holds). Two CHARC writing-plans reinforcements folded into the brief (`46d40169`): (a) `ResearchHealthStatus/Check` mirror 18-E's `__post_init__` frozenset enum validation (reject grey) → structurally-conformant envelope (all 5 false-green gates by-construction); (b) atomic `latest.json` write (tmp + `os.replace` same-dir). Staging CONFIRMED: SCRIPT-first is SUB-tripwire (no further arch gate — dispatch now); NIGHTLY half = new pipeline step → its OWN sec-3 pass at that dispatch (17-B step_guard B-shape), fast-follow; separate dispatches. Notified operator it's their move to dispatch the orchestrator for writing-plans (`20260615T061521Z`). RD merge-blocking at the executing return (read-only, no funnel fork, transport-vs-usability boundary, frozenset envelope, atomic write).
- **18-D writing-plans QA-PASS + merged (`8b5ef510`, docs-only); RD confirmed the one flag.** Codex converged R8 (script-first plan). Orchestrator QA shows my measurement dimensions satisfied IN THE PLAN (read-only + ro-conn test; no funnel fork — #2/#5 read the manifest; transport-vs-usability PROVEN by `test_transport_does_not_substitute_for_finiteness` = #7-green-while-#1-red on a NaN DB; the live 103-non-finite defect will RED #1 at the operator gate). **RD flag resolved — CONFIRMED the #7 transport LOCK ("never alarm on a low row count"):** the implementer correctly REJECTED Codex R6-M2's rate-driven color-override (yfinance_calls is best-effort → a sparse-sample rate is unreliable/drop-artifact; #7 is a transport indicator, #1 is the usability authority; a false-alarming stoplight is cry-wolf) and adopted ONLY detail-surfacing. **V2 candidate noted (not now):** my brief barred alarming on a LOW count, not ALL coloring — a SAMPLE-SIZE-GATED color is a V2 once yfinance_calls accrues volume (don't tune a threshold on a sparse young table; it only began filling 06-14). Binding sign-off at the executing diff. Posted charc,operator `20260615T072828Z`. Executing dispatch awaits operator sequencing.
- **18-D EXECUTING — RD review: 5 dims CLEAN, HOLDING the merge-block sign-off on ONE finding (operator intent decision needed).** Converged R15; operator live-DB/browser gate PASSED (check #1 RED on the 103 live non-finite @06-10 = the 18-A motivating-defect proof; stoplight grey→red; latest.json written). Verified on disk (branch `phase18-arc-d-exec`@`0881ca42`): read-only (ro-conn test), no funnel fork (#2/#5 read manifest), transport-vs-usability boundary (#7 detail-only/excludes in_flight), envelope (3 constants imported, frozenset `__post_init__` rejects grey + enforces worst_of + aware-UTC non-future ts, atomic write), diff fenced to monitoring/scripts/tests, ZERO Co-Authored-By. **THE FINDING (measurement-utility, NOT a code defect — the implementer built exactly to my brief §6.2 #1 "red on any"; the gap is MY brief spec):** check #1 reds on ANY non-finite with no baseline → the immutable, withdrawn-backfill **103-row 06-10 cohort holds #1 (and overall) RED PERMANENTLY** → the 18-F research stoplight is red-from-day-one-forever → a NEW non-finite regression (a future writer-barrier failure, the monitor's whole purpose) is INVISIBLE (already red; 103→104 changes no color). The monitor re-creates the invisibility it was built to eliminate. Asymmetry: #2 (rate) self-heals as clean signals dilute the fixed-23 (yellow→green ~weeks; honest+temporary); #1 (binary) does NOT — permanent. **Decision put to operator (verify intent):** (A) permanent-red = intended honest signal (sign off as-is), or (B) **RD-recommended:** baseline-out the accepted cohort — #1 reds only on non-finite NEWER than the 18-A writer-fix date (2026-06-13; barrier prevents all post-fix non-finite writes, so obs_date>that = a genuine regression), surface the known 103 in detail, NEW non-finite→red distinctly (small date-gated fix; detail structure already exists). Posted charc,operator `20260615T103533Z` (query). On (B): small #1 fix + re-verify → I sign off; on (A): sign off now. CHARC C-conditions + the codex-auto-review A/B are the remaining non-RD legs.
- **18-D finding RESOLVED = (B) + construction-barrier guard (operator-decided 2026-06-15).** Operator probed my (B) premise — "have we verified non-finite can never be written again?" — a sharp question that surfaced a real gap. **Verified on disk:** the SOLE writer to `pattern_forward_observations` is `_step_pattern_observe → build_ohlc_today_json → insert_observation` (18-A-guarded ×2; 18-B confirmed no other OHLC path unguarded), BUT `PatternForwardObservation.__post_init__` validates status/event/sessions and **NOT ohlc finiteness** — the F6 construction-barrier gap 18-B's path-enumeration didn't reach (a future direct `insert_observation` caller or a guard regression could still introduce non-finite). So "never" was a writer-discipline claim, not structural. **Two coordinated fixes (brief §6.6, `50fb37d8`; RD merge-blocking on both):** FIX 1 = #1 baseline (in 18-D's fence, `swing/monitoring/`; reds only on non-finite `observation_date` > a cutoff [18-A merge 06-13 / empirical 06-10 boundary], surfaces the accepted 103 in detail; no new tripwire → no fresh CHARC pass; re-dispatch in-fence) — **lifts my held sign-off to (B)**; FIX 2 = construction-barrier guard (`PatternForwardObservation.__post_init__` RAISE on non-finite; `swing/data/models.py`; 18-B-class write-barrier completion; crosses CHARC tripwire) — **routed to CHARC for the architecture pass** (`20260615T104527Z`); CHARC scopes (expand 18-D vs companion 18-B.1) + sequences. **Combined steady-state:** #1 GREEN (103 baselined + no new non-finite possible by construction), reds ONLY if the FIX-2 guard regresses — an actionable tripwire, and the 18-F stoplight reflects real health instead of permanent-red. The operator's premise-check produced the structural-prevention upgrade (charter directive #3: the verification question itself produced clarity). Other 5 measurement dims of the 18-D diff already verified clean.
- **FIX 2 CHARC arch-pass = PASS w/ REQUIRED RELOCATION — ACCEPTED (CHARC caught a read-path regression in MY recommendation; verified on disk).** My `__post_init__` location would ship a read-path regression: `__post_init__` fires on EVERY construction including `_row_to_observation` (the read mapper; passes `ohlc_today_json` through; 3 readers, one feeds the nightly `_step_pattern_observe`), and the 103 non-finite rows have VALID status (06-10: pending=10, triggered_open=243) → they read clean today → a finiteness raise in `__post_init__` would NEWLY crash their reads. **Relocate to `insert_observation`** (the SOLE `INSERT INTO pattern_forward_observations`, verified) — same structural prevention (every insert funnels through it), no read-path touch. My miss: anchored on the F6 "construction barrier" gotcha, missed that `__post_init__` is construction-UNIVERSAL (read-mapper too). Accepted cleanly (directive #10: don't defend the brief). Brief updated `8b11b931`. **Scope = NEW arc 18-B.1** (keeps 18-D's "NO swing/data" fence clean; 18-B write-barrier lineage; CHARC carve-out scoped to `insert_observation` + reuse `is_finite_ohlc` + 2 required tests incl. read-preservation encoding the finding). **CHARC arch-pass already PASSED → 18-B.1 ready for dispatch. DECOUPLING:** 18-D merges on FIX 1 + the C-PASS + my lifted sign-off — NOT gated on FIX 2; 18-B.1 on its own track (independent; the date-gate is correct regardless). RD merge-blocking on BOTH diffs. Posted charc,operator `20260615T105927Z`. Operator's move: relay FIX 1 re-dispatch (in 18-D worktree) + 18-B.1 (fresh dispatch) to the orchestrator.
- **BOTH RD merge-blocking sign-offs = PASS (2026-06-15), verified on the shipped diffs.** **18-D FIX 1** (diff `0881ca42..a5997e22`): fence held (research_health.py + tests only; no swing/data/schema/dep/__post_init__); `_FINITENESS_BASELINE_CUTOFF=date(2026,6,13)`, strict `>` → post-cutoff red / ≤cutoff accepted-historical (detail, not red) / only-≤cutoff GREEN. The two review catches sound + BETTER than my spec (credited): corrupt JSON ALWAYS red (never baselined); non-canonical obs_date gated to `^YYYY-MM-DD$` (genuinely unconstrained — TEXT NOT NULL no CHECK — correctly in-scope). Operator re-witness PASSED (#1 RED→GREEN on the 103; topbar red = worktree-path #5 artifact, not a defect; real overall confirmed on merged head). **18-D CLEAR TO MERGE** (FIX 1 + 0881ca42 C-pass + this sign-off; not gated on 18-B.1). **18-B.1** (diff `ef517b25..233745da`): fence held (`pattern_forward_observations.py` + 2 tests; **models.py NOT in diff** — relocation held; no schema/dep; append-only/caller-tx, no commit); `_assert_finite_ohlc_for_write` at the TOP of `insert_observation` before INSERT, reuses `is_finite_ohlc` (C1) + json.loads + per-key guard + OverflowError→ValueError wrap, volume-exempt; **the structural lock `test_read_path_preserves_accepted_nonfinite_historical_rows` VERIFIED** (raw-inserts a non-finite row bypassing the barrier, reads via all repo readers, asserts NO raise — proves write-path-only + reads of the 103 preserved; a __post_init__ guard would fail it) + write-barrier both-ways + 11-case rejection suite. **RD = PASS; remaining gate = CHARC C-conditions** (independent of 18-D). **COMBINED STEADY-STATE achieved on both merges:** #1 GREEN (103 baselined + no new non-finite possible by construction at insert_observation), reds only on a barrier regression — the answer to the operator's "never?" made structural. **The full chain from my 06-13 audit is closing: defect → 18-A writer fix → 18-B consolidation → 18-C observability → 18-D monitor (baselined) + 18-B.1 structural prevention.** Posted charc,operator `20260615T174611Z`.
- **18-D+FIX-1 (`2f13f0f5`, 8514 green) + 18-B.1 (`8022d45d`/`a272c7da`, 8529 green) MERGED + CLOSED — the audit chain is COMPLETE.** The 18-B.1 merged-head no-false-green suite caught a cross-arc regression (18-D monitor tests seed non-finite via `insert_observation`, which 18-B.1's barrier now rejects) — fixed test-only (`e7c61f98`: seed via raw INSERT = the read-preservation principle); production correct (barrier blocks new writes, monitor detects existing). **Merged-head REAL state:** #1 GREEN (103 baselined), #5 GREEN; overall RED (honest) via two RD-lane signals → **my 2 post-build CALIBRATION decisions** (posted `20260615T234110Z`; same alarm-fatigue principle as FIX 1): **(A) #3 coverage_gaps — ADD a TRAILING 1-session grace** (reds every post-close→pre-nightly evening on the expected newest-session-unobserved lag = recurring false-red; red only on ≥2-session trailing OR interior gaps; HIGHER priority — recurs, self-resolves each night but noisy); **(B) #2 invalid_ohlc — BASELINE the known 06-10 cohort** (~30% = the engine rejecting the SAME 103 I baselined in #1; fixed/permanent post-18-A+18-B.1; a baseline-count constant → red only on NEW invalid_ohlc; baseline ONLY invalid_ohlc, not insufficient_forward_depth/missing_observations). **Packaging:** a small monitor follow-up — fold into the deferred 18-D nightly-half dispatch OR standalone; CHARC/operator sequence; RD merge-blocking. **Watch-standard amendment (my remaining post-build action): sequenced AFTER the calibrations** so §3.1 cites the final thresholds/semantics. Tonight's nightly should flip #3 GREEN; I re-glance after.
- **Operator FOLDED the 2 calibrations into the deferred 18-D nightly-half (2026-06-15)** — one coordinated monitor-touching dispatch. Brief §6.7 (`b8fc3177`) specs the THREE-part scope: (1) the nightly pipeline STEP (17-B `step_guard` B-shape running `compute_research_health` + writing `latest.json` nightly + `role_mail`-on-ATTENTION; C4/C5; the tripwire-crossing piece → CHARC sec-3 pass AT dispatch); (2) calibration A #3 trailing 1-session grace; (3) calibration B #2 invalid_ohlc baseline-count (baseline ONLY invalid_ohlc). Parts 2-3 monitor-internal (no new tripwire, like FIX 1). RD merge-blocking on the diff. **Watch-standard amendment sequences after this lands.** Planning fold, not a dispatch — operator sequences. Posted charc,operator `20260615T234943Z`. Phase-18 research-lane remaining: this nightly-half dispatch (+calibrations) → then the watch-standard amendment. The audit chain itself (18-A/B/C/D/B.1) is CLOSED.
- **Operator ready to start the nightly-half (2026-06-16).** Routed the sec-3 architecture-pass request to CHARC on PART 1 (the new nightly pipeline step — the tripwire piece; parts 2-3 calibrations are monitor-internal riding it) — `20260616T001426Z`. Pre-drafted the orchestrator dispatch prompt (writing-plans→executing; 3-part scope; reuse the shipped `compute_research_health`; step_guard B-shape never perturbs the pipeline; read-only DB / only latest.json+role_mail writes; baseline ONLY invalid_ohlc; the distinguishing tests) — handed to operator, **to relay the instant CHARC's sec-3 pass confirms.** RD merge-blocking at the executing return. After it lands → the watch-standard §3.1 amendment (final RD research-lane Phase-18 action, citing the complete monitor + nightly cadence + calibrated thresholds).
- **CHARC sec-3 PASS on the nightly step (2026-06-16, C-NH1-5) — CLEAR TO DISPATCH.** Conditions: C-NH1 step_guard B-shape (LeaseRevoked propagates, never fails the run); C-NH2 read-only `mode=ro` URI conn (physical read-only enforcement, not the runner's RW connect); C-NH3 placement immediately AFTER `_step_shadow_expectancy` (runner.py:~1051); **C-NH4 SINGLE-SOURCE the artifact write** — extract `write_research_health_artifact()` consumed by BOTH the script + the step (the #24-#26 divergence guard on the file 18-F trusts; a genuine catch I under-specced — agreed); C-NH5 write-NOTHING-on-failure (retain prior; staleness→grey). **role_mail-on-ATTENTION DEFERRED to new arc 18-H.7** (sender-taxonomy: a pipeline step isn't a role / `VALID_FROM` enum-locked) — Part 1 ships the artifact-refresh; the 18-F stoplight + staleness gate are the passive surface until 18-H.7. The **codex-auto-review A/B does NOT ride this arc** (stays on 18-H.4). Parts 2-3 (calibrations) cross no tripwire (RD-gated at executing). Brief §6.7 updated `2071bc1e`; updated dispatch prompt (C-NH1-5, no role_mail) handed to operator — clear to relay. RD merge-blocking at the executing return.
- **18-D nightly-half EXECUTING — RD measurement-integrity sign-off = PASS (RD leg cleared; merge on CHARC C-NH + operator pipeline-run gate).** Converged R2 (R1 caught a real exports_root coupling — the step read the default root vs the cfg root; prod coincides but a non-default cfg would read the wrong manifest → stale artifact; fixed + regression test; CREDITED as a measurement-freshness catch). Verified on disk (branch `18-d-nightly-exec` `bed684ac..09acb748`): **C-NH1** bare `step_guard(lease,"research_health",logger=log)` at runner.py:1060 (never-fails, no run_warnings perturb); **C-NH2** separate `mode=ro` URI conn (write raises "readonly database" test); **C-NH3** placement between `_step_shadow_expectancy` (1038) and `complete` (1063); **C-NH4** `write_research_health_artifact` defined ONCE (research_health.py:232), script's local `_write_latest_json_atomic` DELETED — single-sourced (the #24-#26 guard on the 18-F file); **C-NH5** compute-before-write + atomic, failing-compute retains prior artifact byte-identical. **Calibrations:** #3 `_COVERAGE_TRAILING_GRACE_SESSIONS=1` (red on ≥2 trailing OR interior/leading); #2 `_INVALID_OHLC_BASELINE_COUNT=23` with the **SCOPE LOCK HELD** (baseline applied ONLY to invalid_ohlc — the `if reason=="invalid_ohlc"` branch; the other 2 reasons NOT baselined, verified). Fence held (scripts + research_health.py + runner.py + tests; no schema/dep/data/trades); trailers []; role_mail absent (18-H.7). Posted charc,operator `20260616T044923Z`. **After this merges the 18-D monitor is COMPLETE → my final research-lane Phase-18 action = the watch-standard §3.1 amendment** (nightly cadence + the calibrated #2/#3 thresholds).
- **18-D nightly-half + 2 calibrations MERGED + CLOSED (`4d17492b`, 8550 green) — THE 2026-06-13 AUDIT CHAIN IS FULLY CLOSED + AUTOMATED.** Operator pipeline-run gate witnessed live (run 104): the research_health step fired after `_step_shadow_expectancy` + refreshed latest.json from within the run; **CALIBRATION A live — #3 coverage_gaps GREEN** (trailing-1 evening false-red gone); **CALIBRATION B live — #2 invalid_ohlc=23 baselined-not-red, while the UN-baselined `missing_observations` (16.3%) correctly drives the honest overall YELLOW** (the SCOPE LOCK working — baseline only the 06-10 cohort; the maturity signal stays honest). Chain: 18-A→18-B→18-C→18-D+FIX-1+18-B.1→nightly half+calibrations; the artifact refreshes every pipeline run; the GUI research stoplight reads the calibrated state. **(Shared checkout: the arc + a concurrent non-18-D `94859ba1` harness-scaffold commit are local-only, unpushed per operator cadence — main ahead 8.)**
- **WATCH-STANDARD v2 amendment AUTHORED (2026-06-16) — `docs/research-director-watch-standard.md`, pending operator acknowledgment (§6.5).** A new dated version section (supplements v1; cadence/tripwires unchanged) integrating the research-health monitor: §2 supplement (run `scripts/research_health.py` alongside `weekly_glance.py`; the 18-F stoplight is the passive surface; role_mail-on-ATTENTION is 18-H.7-deferred); §3.1 supplement (the 7 checks mechanize the data-integrity dimension, still cross-checked vs the live DB); **CALIBRATION SEMANTICS a future RD MUST read the monitor through** (#1 GREEN = zero POST-baseline non-finite, the 103 @≤06-13 are accepted-historical NOT a failure; a #1 RED = a write-barrier regression = T2-class; #2 invalid_ohlc baselined at 23, the other 2 reasons honest; #3 trailing-1 grace). **Committed local-only (held for the operator's push cadence — the unpushed code arc sits below it).** My final research-lane Phase-18 action; **the audit-chain saga is complete.**
- **WATCH-STANDARD v2 OPERATOR-ACKNOWLEDGED + ADOPTED 2026-06-16.** The monitor integration + calibration semantics are now binding on future RD instances. **The full 2026-06-13 data-collection audit arc is CLOSED end-to-end — defect → 6-arc remediation (18-A/B/C + 18-D/FIX-1/18-B.1 + nightly half/calibrations) → the monitor automated + calibrated → the watch standard updated.** Posture returns to STOP-ENGINEERING + market time. Remaining Phase-18 items are NOT RD-lane (role_mail-on-ATTENTION 18-H.7; 18-G/R1/18-H.1-.6; the codex-auto-review A/B — CHARC/orchestrator). RD-lane forward: the standing watch cadence (weekly glance + monitor; first monthly read due first trading week of July 2026; the broad-watch N≥30 / T4 decision gate at ~10 priced signals now).
- **Sub-agent-dispatch automation (orchestrator spawns implementers) — RD topology clearance.** CHARC invited a taxonomy concern-or-clear before operator ratification. CONCUR no erosion: authority (dispatch-go/merge-go/decision_request-operator-only/implementer-never-posts-to-director) unchanged; only the mechanical info-relay moves. ONE invariant noted (proposal preserves it): my merge-blocking diff-level gate must stay, and it depends on the orchestrator's QA remaining a REAL against-disk verification (not a rubber-stamp of the sub-agent self-report) — held in the 18-B pilot (C-c convergence verified from the real `.copowers-findings.md`). Cleared from the RD seat.

### 2026-06-17 — RD session: glance all-clear; SPCX path-B carve-out commissioned to CHARC
- **Fresh RD instance bootstrapped** (rd-state pointer [FIRST real overwrite, retiring the CHARC FORM scaffold] + charter + watch-standard v1/v2 + harness-architecture; inbox drain [13 msgs, all CHARC/orchestrator harness-scaffold + 18-H-batch FYIs, zero research bearing]; `weekly_glance.py`; git). main HEAD `516c24f3`, schema **v31**, ~8618 green. (Current state now lives in `docs/rd-state.md`; entries here are append-only history.)
- **Weekly glance = all-clear.** Drumbeat alive (run `20260617T070016Z`, age 0d); unattributed 0 across 7 shown runs; accrual 51→114; no live trades this week (epoch intact, no T5/T6/T7); `invalid_ohlc=23` stable (baselined). Lone `ATTENTION` = the known **T3 heuristic re-fire** (trips on any nonzero trigger; T3 CLOSED since 06-13, N past 10) — not actionable. **Broad-watch priced N ≈ 26 triggered signals** (was 10 on 06-13) — numerically approaching the **T4 N≥30** gate but **zero realized edge**: closed-only **0/7 wins, −0.714R**; the +0.382R `mtm_at_horizon` headline is entirely UNREALIZED marks on 19 open positions; 14/26 weak-close intraday-touch; signal-count overstates (correlated re-detections). **At the T4 read, weight closed-only realized, not the blended open-mark number.** Meaningless at this N by design.
- **SPCX out-of-framework holding — operator chose PATH B (ignore-entirely), 2026-06-17.** CHARC routed the categorization question (operator bought 2 SPCX IPO shares ~$412 to hold long-term OUTSIDE the framework → a per-run `untracked_broker_position` orphan via 18-H.6/.6.1). **Verified in code (not from memory):** the orphan only stops when the ticker has a journal OPEN trade (`schwab_reconciliation.py:1304-1367`; no cross-run suppression belt for this type); `equity_delta` is both-flat-gated so SPCX fires NO false delta while held (`:1714-1722`); strategy stats are closed-only with no `entry_intent` filter (`journal/stats.py:179-204`), and `entry_intent` (standard/by_design/NULL) does not fit an out-of-framework hold. **Rejected** journal-as-normal (fabricates stop/R-basis + contaminates the headline on eventual close) and acknowledge-nightly (cry-wolf). **B = carve declared out-of-framework holdings out at the recon boundary; the position never enters the `trades` table → zero measurement contamination by construction.** My lane leaned B over A (total-account mirror); operator concurred B.
- **Commissioning brief authored + committed `78d6d5e6`** (`docs/out-of-framework-holding-carveout-commissioning-brief.md`; measurement locks L1–L4 RD merge-blocking; open Qs = registry mechanism [config/table/CLI] / equity-coherence refinement now-vs-defer / existing-orphan-row disposition [RD lean: resolve to `acknowledged_immaterial` on landing]). **Posted to CHARC + operator** (`query`, thread `spcx-out-of-framework-carveout`, `20260617T093619Z`) requesting the pre-dispatch architecture pass (crosses the new-standing-concept + `swing/trades` carve-out tripwires). **Status: awaiting CHARC arch pass → operator sequences the orchestrator writing-plans dispatch;** RD QAs the returns + gives the merge-blocking sign-off at the executing return. Posture otherwise unchanged: STOP-ENGINEERING + market time (the carve-out is justified as instrument-integrity, not research).

### 2026-06-17 (cont.) — SPCX carve-out ran the full arc same-day; RD L1–L4 sign-off = PASS (at the merge gate)
- The path-B carve-out advanced end-to-end while the RD session was idle: **CHARC arch-pass GO** (`docs/out-of-framework-holding-carveout-charc-architecture-pass.md`) — Ruling 1 registry = a `[reconciliation] out_of_framework_tickers` config-list in user-config.toml (NO schema/table); Ruling 2 the §2.4 coherence refinement **DEFERRED** (L2-delicate, narrow-window value; registered fast-follow; tradeoff on record = the coherence check stays suppressed while SPCX is held); Ruling 3 resolve existing rows to `acknowledged_immaterial` — **all three as RD leaned**; operator-confirmed; conditions C1-C5. Then writing-plans (`52fc3ade`, QA-PASS, Reviewer-A R1 NO_NEW_CRITICAL_MAJOR) → executing (branch `spcx-carveout-exec` tip `cb69e59f`, 5 commits trailers `[]`).
- **Reviewer-A R1 caught a REAL MAJOR that is squarely an L3 violation:** the first normalizer accepted ANY iterable, so a `[reconciliation.out_of_framework_tickers]` TOML *table* would iterate its KEYS into a silent carve-out (+ a `tuple()` pre-wrap char-split a bare string). FIXED to a strict list/tuple-of-strings normalizer (rejects dict/str/bytes/set with TypeError) + `apply_overrides` degrades-on-TypeError to the empty base. Converged R2. (The cross-arc + dual-review machinery earned its keep — a diff-only review would have missed the silent-carve-out vector.)
- **RD L1–L4 merge-blocking sign-off = PASS** (posted charc,operator `20260617T172727Z`), verified INDEPENDENTLY on the shipped diff (43a6b767...cb69e59f; the 11-file scope): **L1** no trades/fills row by construction (carve-out is a pure `continue`; resolve is UPDATE-only via `resolve_discrepancy`; no stat-module touched) · **L2** the `equity_delta` both-flat block (~`schwab_reconciliation.py:1690-1739`) has ZERO diff hunks (nearest 1435 + 1897); §2.4 deferred · **L3** strict normalizer (the R1 MAJOR fix; malformed user-config degrades to NO carve-out = everything banners, the safe direction) + declared→SKIP+#27 `out_of_framework_excluded` line/counter/log, undeclared→`_emit` (still banners) · **L4** file scope excludes every measurement module. **C5 corroborated** (migration/module guard clean); **C3 resolve** scoped/audited/idempotent/TOCTOU-guarded/tx-disciplined.
- **Production wiring verified on disk (not trusted from the report) — the 18-E silent-false-negative class:** the carve-out integration test seeds `cfg.reconciliation` directly (SimpleNamespace), so it would NOT catch an `apply_overrides` break. Confirmed `run_pipeline` (the production entry, `cli.py:3296`) applies `apply_overrides` BEFORE the steps, and the resolve CLI applies it too → both read the override-applied `cfg.reconciliation` → the operator's user-config.toml SPCX declaration is honored (no production caller bypasses via `run_pipeline_internal` with a bare cfg).
- **Status: at the merge gate.** Remaining (NOT RD): CHARC C1-C5 + the operator §5.10 live-witness (declared→no orphan + #27 line; undeclared still banners; existing SPCX banner clears) → orchestrator merges --ff-only + re-runs the suite on the merged head. RD gate discharged; I re-engage only if a live-witness surprise reopens a measurement question. (Also closed this session, NOT RD-lane: **18-H.5** dead-code-hardening audit CLOSED Phase-1-only — operator+CHARC declined Phase 2; the lone verified-dead branch A-1 [reconcile.py 409-guard `ambiguity_kind is None` disjunct] stays as documented belt-and-suspenders.)

### 2026-06-18 — SPCX path-B CLOSED (merged + live-witness PASS); §2.4 swing-NLV coherence COMMISSIONED (RD merge-blocks L2)
- **SPCX out-of-framework carve-out (path B) is CLOSED end-to-end.** Operator authorized; merged `--ff-only` to main `5b24ef68` (the 5 commits, trailers `[]`); **binding no-false-green = 8638 passed on the MERGED head**, ruff clean; convergence transcript tracked at `docs/reviews/spcx-carveout-executing-codex-findings.md`. **§5.10 operator live-witness COMPLETE + PASS** (orchestrator-verified read-only on the live DB): C3 — `swing schwab resolve-out-of-framework` cleared 2 SPCX orphans (id 69 run 56 + id 70 run 57 → `acknowledged_immaterial`, audited reason), banner gone; C1 — post-declaration recon run 58 emitted ZERO new `untracked_broker_position` for SPCX (skip fired); C2/L3 — run 58 `out_of_framework_excluded_count=1` + cash_warnings `{reason: out_of_framework_excluded, detail: 'SPCX: +2.00 sh @ $+392.51 excluded'}` (pipeline_runs 106 carries the same in `warnings_json`); not-a-blanket covered by the scoped-set test (operator has no second untracked holding). **Every L3 property I was merge-blocking on is confirmed on live data.** My RD L1–L4 sign-off (PASS, `20260617T172727Z`) held through the rebase replay (no changes beyond replay). The ~$392.51 SPCX MV now sits outside the framework by design.
- **§2.4 swing-NLV coherence refinement COMMISSIONED (the deferred SPCX fast-follow).** CHARC FYI'd a heads-up (`20260618T051402Z`, thread `swing-nlv-coherence`): operator-commissioned 2026-06-17; CHARC-lane (equity machinery — I placed §2.4's mechanics there at SPCX §4 Q2); CHARC §3 arch-pass GO; writing-plans dispatched (`implementer-opus-xhigh`). Brief/settled design `docs/swing-nlv-coherence-commissioning-brief.md` `3033c0f8`. **Design (drift-only, operator-chosen):** `broker_flat_swing := (positions − declared) empty`; `swing_nlv := source_nlv − Σ(declared MV, same payload)`; fire `equity_delta` on `journal_flat AND broker_flat_swing AND |ledger − swing_nlv| > tol`; NO positive flat+coherent surface; reuse `equity_delta`; NO schema/module; `swing/trades` carve-out scoped to `schwab_reconciliation.py` step 8 only. **Net:** restores swing-drift detection while SPCX is held (today the held position forces `broker_flat=False` → the check is suppressed entirely → a real swing-ledger-vs-broker drift goes undetected).
- **RD merge-blocks L2** (no false equity signal, either direction). Pre-loaded + acked to CHARC (`20260618T051746Z`): the design is **L2-sound on its face** — drift-only means the only L2 failure mode is a MISSED drift (false coherence = failing to fire), and CHARC's C1 (same-snapshot MV) + C2 (suppress-on-missing-MV) hit the two canonical vectors; **no net-new L2 concern** (won't manufacture one). **My executing-return L2 checklist (front-loaded so writing-plans builds to the gate):** (1) C1 on disk — `source_nlv` + the subtracted `marketValue`s come from the SAME account-details call/object (one snapshot); (2) C2 — a declared-and-held position with `marketValue=None` SUPPRESSES, not treat-as-0 (treat-as-0 → swing_nlv = full NLV vs swing-ledger → a FALSE DRIFT of ~the declared MV); test (c) proves the degrade path ran; (3) Σ iterates HELD declared positions only (un-held declared ticker contributes 0, no error); (4) `account_equity_source_dollars` keeps the RAW broker NLV so the equity tile doesn't under-report the operator's true account value by the declared MV (the subtlest L2-adjacent misreport vector; CHARC flagged it at brief §2) — swing_nlv/declared_oof_mv/basis ride additively in `actual_value_json`; (5) C3 byte-identical when nothing declared/held. L1+L4 hold trivially (nothing journaled, step-8-only). **No RD action until the executing return** (orchestrator posts it to charc+rd; route any L2 question via the operator).
- **§2.4 writing-plans QA-PASS + realigned to my §9.4 (`3f6bd6e6`).** My posted L2 checklist landed in the brief as §9 (CHARC, `e9f3db3d`) DURING the writing-plans run; **§9.4 (my point 4 — keep the run-row columns RAW) resolved the run-row-recording design point OPPOSITE the plan's first cut** (which had made `equity_delta_dollars` swing-scoped + persisted a `summary_json.equity_coherence` key). The orchestrator caught the stale-brief conflict at QA + re-tasked to align: now `equity_delta_dollars`/`account_equity_source_dollars` stay RAW, the swing values (`swing_nlv`/`declared_oof_mv`/`basis`) ride additively in the `equity_delta` `actual_value_json` ON FIRE only, and the coherent no-fire case persists NOTHING + emits a `log.info` (C5-consistent — a pipeline.log audit line, NOT a persisted positive-coherence surface); test (a)'s distinguisher = the caplog line. **The early front-load worked as intended — it caught a design divergence before executing.** Codex review-fast converged (prior 5 rounds → NO_NEW_CRITICAL_MAJOR; the realignment R1 NO_NEW_CRITICAL_MAJOR + 2 minors → R2 clean). NEW executing watch-item: the folded-in `__post_init__` NaN/inf fix sets `account_equity_source_dollars=None` on a non-finite NLV — verify the None degrade doesn't misreport the `dashboard.py:82-95` banner consumer. **Awaiting CHARC C1-C5 plan-QA + operator auth; RD L2 QA at the executing return.**
- **§2.4 EXECUTING QA-PASS + RD L2 sign-off = PASS (`20260618T074716Z`; branch `swing-nlv-coherence-exec` `2d2e0b40...7a283683`).** Verified independently on the shipped diff (production touch = `schwab_reconciliation.py` ONLY + 3 tests; models.py untouched; no schema/module). Reviewer A converged R2 after fixing TWO MAJORs — **MAJOR-1 was squarely L2:** a non-finite ledger made `|NaN| > tol` always-False → a FALSE COHERENCE (silent no-fire hiding a real state) + a `get_run` crash; the fix normalizes `finite_ledger_equity`→None and gates BOTH the fire AND the coherent log on it → SUPPRESS (no fire, no log, None stamp), not a false coherence. My L2 checks all verified on disk: **C1** same-snapshot at the caller (ONE `get_account_details` → `details` → `schwab_account`; both NLV + `positions[].marketValue` from that one object); **C2** suppress-never-treat-as-0 (the MV loop breaks on None/non-numeric/non-finite → `swing_nlv_computable=False`); **RAW columns** (None only on a non-finite degrade — the MAJOR-2 latent-crash fix, never under-reporting a valid NLV); **Σ over HELD declared only**; **C3** byte-identical (`broker_flat_swing` reduces exactly to legacy `len(positions)==0`); **[P2]** `equity_dollars=swing_nlv` preserved → both legacy consumers render swing_nlv (correct — the compared-against value, not a misreport). The tests pin the wrong-impl false-fires (test (c): a None→0 impl FALSE-FIRES; the MAJOR-1 test: a non-finite ledger spurious-coherent) — genuine discriminators, real-payload-shaped. **Scope-extension** (the `finite_source_nlv` pre-BEGIN normalization + INSERT stamp, beyond literal step 8): NO L2 objection — L2-PROTECTIVE (prevents persisting a non-finite NLV / read-back crash on a preserved failed-run row), zero finite-value change; the formal in-scope-by-extension ratification is CHARC's. **RD gate DISCHARGED.** Remaining: CHARC C1-C5 + the scope-extension ratification + operator §5.10 live-witness → orchestrator merges --ff-only + the merged-head no-false-green (8652 on-branch).

### 2026-06-18 (cont.) — §2.4 MERGED; research-monitor YELLOW assessed = BENIGN (DINO 06-15 fetch-gap)
- **§2.4 swing-NLV coherence MERGED** to main `e1017b1e` (operator merge-then-witness; 8652 green on the merged head; both director gates PASS; 5 commits trailers []). The §5.10 live-witness is operator-owned / in-progress; orchestrator relays + CHARC closes the arc on a clean witness. **The swing-ledger-vs-NLV coherence check is restored while holding a declared out-of-framework holding — the SPCX→§2.4 chain is complete.**
- **Operator ran a pipeline + flagged a YELLOW research monitor; RD assessed (read-only) = BENIGN, no action.** Ran `scripts/research_health.py --json` + cross-checked the live DB (§3 discipline). The defect-class check `temporal_log_finiteness` is **GREEN** (0 post-baseline non-finite of 2706; the 103 @ ≤06-13 accepted-historical) — **no write-barrier regression**; structural_integrity / drumbeat / candidate_completeness / fetch_transport all GREEN. Overall YELLOW from TWO checks:
  - **`excluded_reason_breakdown` YELLOW** = `missing_observations` 17.5% (the 10–25% yellow band; >25% red); `invalid_ohlc`=23 = baseline (06-10 cohort, not red). The self-healing log-maturity signal (recent detections still accruing forward windows; 143 signals / 2706 obs) — falls as the drumbeat runs. The calibrated yellow documented in watch-standard v2.
  - **`coverage_gaps` YELLOW** (5; thresholds yellow≥1 / red>10) = **5 DINO detections (det186-190, the 5 pattern classes from 06-09) each missing the 2026-06-15 observation** — an INTERIOR one-session hole (`sessions_since_detection` 4→6; TNA same detection-date got 06-15 fine). **Root cause confirmed** (run 104 warnings_json, the 06-15 nightly): 5× `{step:pattern_observe, ticker:DINO, observation_date:2026-06-15, reason:"no bar for observation_date"}` — DINO had no OHLC bar for 06-15 at observe time, so the step correctly SKIPPED (skip-don't-poison; the 18-A family + #27 audit) rather than writing a NaN. A one-off transient per-ticker fetch gap. **Benign — but PERMANENT** (the observe step never backfills), so this coverage_gaps yellow PERSISTS for these 5 DINO detections (a stable benign yellow, not a transient maturity one).
- **Calibration observation (banked, NOT commissioned — stop-engineering holds):** `coverage_gaps` yellows on ANY non-graced hole (threshold 1) and counts permanent ACCOUNTED holes (explained by a recorded `pattern_observe` `no bar`/`non_finite_ohlc` skip-warning) identically to unexplained ones → as one-off transient fetch gaps accumulate (each permanent), the count creeps toward red=10 → eventual cry-wolf RED on benign accounted holes (now 5/10, finite headroom). The principled V2 (mirrors the #1/#2 baseline philosophy): a hole EXPLAINED by a recorded skip-warning for that ticker+date is ACCOUNTED → surface in detail, don't count toward the threshold; an UNEXPLAINED hole stays the real signal. CHARC-lane to engineer IF the operator commissions it; flagged, not urgent.

### 2026-06-18 (cont.) — §2.4 live-witness FIRED a TRUE drift; RD L2 ruling + owned a PATH-B premise miss
- **The §5.10 live-witness is NOT clean (run 59, journal-flat, holding declared SPCX): §2.4 fired a persistent swing-scoped `equity_delta` +$372.30 where coherence was expected.** Numbers: source_nlv 2039.76; ledger (current_equity) 2027.26; SPCX MV 384.80; swing_nlv = 2039.76−384.80 = 1654.96; ledger−swing_nlv = **+372.30** → FIRES (tol ~8.27). RAW `equity_delta_dollars` = −12.50 (raw, §9.4 honored ✓). Fired discrepancy id 71, routed to `pending_ambiguity_resolution` (tier-2 limbo). Orchestrator HELD the arc open for adjudication.
- **RD L2 RULING: NOT an L2 false-drift — a TRUE drift / true positive; §2.4 is L2-correct + stays merged.** The system is genuinely NOT coherent — the swing ledger really overstates swing capital by the unrecorded SPCX cash outflow (SPCX bought with ~$385 swing cash; path B records NOTHING for SPCX incl. that outflow → the ledger still counts the ~$385 as swing cash while swing_nlv excludes SPCX → a real +$372 gap fired every run). My code-level L2 sign-off STANDS; the finding is a DESIGN-PREMISE flaw, not a code defect.
- **Owned the miss (mine): the gap is in PATH B (my commission) — it handled the carve-out's MARKET-VALUE side and OMITTED the CASH side.** The §2.4 brief §2 "math sanity (NLV−SPCX≈ledger)" was FALSE on live data and self-contradicted by its own cited witness numbers (2066.86−392.51 = 1674.35 ≠ ~2027 ledger; the ~$353 gap = the SPCX cost); I had them at L2 pre-load and didn't run the subtraction; the pinned-arithmetic tests constructed swing_nlv≈ledger so couldn't catch a premise false on live data — the **byte-tests-vs-live-witness pattern (18-E; the shadow-engine pivot premise), THIRD instance, this time on MY premise.** Lesson banked (charter directive #2 + `feedback_adversarial_review_verify_data_shapes`): a brief premise/"math-sanity" relating two independently-derived production quantities MUST be verified against LIVE values, and discriminating tests MUST use real-derived inputs, never values constructed to satisfy the premise.
- **Recommended path (a): record the SPCX cash outlay (the PURCHASE COST / cash that left, NOT the current MV) as a swing `cash_movement` transfer-out.** It is the correct reconciliation (completes path B's missing cash side — buying an OOF holding with swing cash IS a swing-capital withdrawal; a future sale back = a transfer-in; symmetric); L1-CLEAN (a cash_movement touches only the equity ledger + cash-recon, NOT compute_stats/cohort/hypothesis; no trade/fill row → SPCX still never pollutes strategy stats); resolves the $372 phantom (ledger → ≈ swing_nlv) leaving only the genuine residual ~$12.50 drift for §2.4 to correctly surface. **REJECT (b)** [add the holding's cost basis back into swing_nlv] **on L2 grounds — it manufactures a FALSE COHERENCE** (distorts swing_nlv to mask a real ledger overstatement = the L2 violation in the OTHER direction). **REJECT (c)** (every-run cry-wolf + limbo). **Corrected the (a) justification on record:** the position-sizing impact is MASKED by the $7,500 sizing floor today (sizing = max($7500, actual); actual ~$2k) → NOT a present sizing error; (a) is correct-accounting + a real incoherence + correctness-at-scale. **Concurred** (CHARC/orchestrator lane): route the swing-scoped `equity_delta` OUT of `pending_ambiguity_resolution` (18-H.6.1-style limbo fix) regardless of path. Posted charc,operator `20260618T083445Z`. **Arc HELD OPEN** until (a) recorded + re-witnessed; operator decides the path, CHARC owns the (a) mechanism (the cash-recon SKIPs TRADE-type txns assuming trade-cash enters via realized P&L — false for a non-journaled OOF buy, exactly where the ~$385 fell through) + the limbo-routing.
- **§2.4 live-witness now CLEAN (operator did path (a)); RD L2 ruling CONFIRMED; arc closeable.** Operator recorded `cash_movements` id 5 = withdraw **$372.48** (the SPCX purchase cash outlay; note "swing capital reallocated out of the swing sleeve … not a swing trade") -> ledger 2027.26 -> 1654.78. **Run 60 (pipeline 108) COHERENT, NO fire:** `swing_coherence_delta=-0.18` (within tol), the coherent log line emitted, RAW columns held (source_nlv 2041.56 raw; equity_delta_dollars -386.78 raw). **L2-confirm posted `20260618T091737Z`:** the run-59 fire was a TRUE, actionable drift (not false); §2.4 fired ONLY on a real divergence and went quiet ONLY when it was genuinely RESOLVED (not suppressed) -> **L2-sound in BOTH directions** (no false drift; no false coherence — the strongest validation of the L2 sign-off). The -0.18 residual confirms the entire ~$372 was the SPCX cash phantom — no separate genuine swing drift under it (my earlier "~$12.50 residual" estimate was WRONG; I conflated the raw ledger-vs-NLV delta — the swing-scoped residual is ~0). **Path-B cash-side practice = operator doctrine** (endorsed; CHARC places it in a cycle-checklist / path-B note): buy an out-of-framework holding with swing cash -> record a swing `cash_movement` WITHDRAW of the purchase cost (not the MV); sell it back into swing -> record a DEPOSIT of the proceeds — keeps it out of trades (L1) AND the swing capital base accurate (the cash side path B omitted); the operational COMPLETION of path B. **Arc CLOSEABLE** on RD-confirm + CHARC practice-bless + 2 registered follow-ups (i: clear the stale id 71; ii: route swing-scoped `equity_delta` out of `pending_ambiguity_resolution` 18-H.6.1-style so a future REAL swing drift is cleanable). No RD block on close. **Memory `feedback_adversarial_review_verify_data_shapes` updated** with the premise corollary (a brief's "≈" between two production figures is the highest-risk line; verify against live values; tests must use real-derived inputs — 3rd instance). The SPCX→§2.4 chain is complete + validated.

### 2026-06-18 (cont.) — equity_delta limbo fix MERGED (follow-up #2); id-72 cash-mismatch assessed = NON-ISSUE
- **equity_delta limbo-routing fix (§2.4 follow-up #2) MERGED + witness-CLEAN (`8fee6a1d`, 8657 green; RD = fyi/no-L2-block — disposition-routing, CHARC C1-C4 lane).** The `_pivot_classify_and_dispatch_for_run` skip-set widened to include `equity_delta` → a fired equity_delta stays unresolved/cleanable (run-level-counted, material=0), NOT the tier-2 limbo; the stale id 71 cleared via the existing resolver. CHARC §0=(A) (no `MATERIAL_BY_TYPE` flip; equity_delta material=0 correct-by-design since Phase 9). Clear to close from the RD seat.
- **id-72 `cash_movement_mismatch` (a path-(a) downstream finding the witness surfaced) — RD assessed = NON-ISSUE, no follow-up** (posted `20260618T174409Z`). Path (a)'s manual SPCX withdraw (cm 5, $372.48) has no Schwab cash counterpart → a material=0 `cash_movement_mismatch` — the SAME pattern as the operator's RECURRING MONTHLY DEPOSIT (cm 4, $100; cash isn't Schwab-auto-synced — `project_recurring_monthly_deposit`). **Verified on the live DB it SELF-SUPPRESSES on resolution:** cm 4 flagged on runs 48+49 while pending, resolved 2026-06-12, then ZERO re-emits on runs 50-60 despite staying in-window throughout. So id 72 = a ONE-TIME acknowledge (SPCX is one-time, not recurring); material=0 (no banner); zero measurement impact (the §2.4 ledger fix holds). **Path (a) is SOUND as the OOF-cash-side model.** The "satisfies-both-checks" clean model (an OOF-reallocation cash_movement KIND the recon auto-excludes from the Schwab cash-match) would save ONE acknowledge for a RARE event → NOT worth a schema-adjacent arc vs stop-engineering; DON'T commission. Broader pre-existing note (flagged, not commissioned): ALL manual cash_movements flag this (deposits included) because cash isn't Schwab-auto-synced → a general recon improvement could address deposits + OOF reallocations together IF the monthly acknowledge becomes a chore (CHARC-lane ops, predates path (a), low priority). **Action: operator acknowledges id 72** (same as the monthly deposit). Both §2.4 + its 2 follow-ups now resolved; back to standing watch cadence.

### 2026-06-19 — OOF-buy cash-coherence arc COMMISSIONED (CHARC); RD measurement gate pre-loaded
- **Orchestrator generational handoff** (saga closed; main `08236be8`, ~8657 green, schema v31, no live arc) → a fresh orchestrator generation online. The whole SPCX/§2.4 cluster (carve-out path B + §2.4 coherence + equity_delta limbo fix) is CLOSED + witnessed.
- **CHARC commissioned the OOF-buy cash-coherence arc** (`swing journal oof-buy`; brief `27a0347f`; §3 pass GO, NO schema, ONE authorized additive step-7 matcher carve-out) — Phase-18 deferred-follow-up #1, the cash-recon ROOT fix shaped as design option B. **This is the clean "satisfies-both-checks" model I recommended NOT building last turn** ("not worth a schema-adjacent arc for one acknowledge on a rare event"). **Override is well-founded — owned, not re-litigated:** my framing undervalued (1) removing the manual `--withdraw` amount as an operator-error surface, and (2) that it is the PREREQUISITE for the deferred #2 equity_delta materiality bump (can't land while benign cash drifts fire monthly at the ~$5-8 tol). §0 cluster disposition: #1→THIS arc; #2 (materiality) DEFERRED; #3 (general manual-cash ergonomic) →D16 WATCH; #4 (id 72) = operator one-time ack.
- **It is measurement-core (cash ledger → equity coherence) → my L-checklist is MERGE-BLOCKING; the §5 composition test is the binding artifact.** Pre-loaded + posted to CHARC (`20260619T053533Z`, thread `oof-buy-cash-coherence`): L1 (cash_movement only, no trade/fill → no expectancy contamination) · **L2/§5 composition** (eval_delta 0 at cost==MV AND still 0 after a later OOF price move — declared_oof_mv from POSITIONS [current MV] vs the ledger debit [fixed cost] are orthogonal, no double-count; the fee nets out; real-derived test inputs, never premise-constructed) · idempotency (sentinel `ref` + `ux_cash_ref`, no double-debit, can't collide with a real `transaction_id`) · L3-analog (the matcher excludes ONLY OOF-sentinel rows of declared-OOF tickers, non-OOF byte-unchanged, a genuine unmatched cash_movement STILL fires, registry guard rejects non-OOF) · L4 (no measurement-chain/schema). **1 concern front-loaded:** idempotency-on-collision must FAIL-LOUD, not a silent no-op (a silent no-op would drop a legitimately-distinct second same-ticker/same-day OOF reallocation from the ledger → an un-audited coherence drift = the #27 silent-skip smell, the exact class this arc prevents). **Complementary-checks note (strength):** the matcher waiving `cash_movement_mismatch` for OOF rows does NOT hide a wrong `--cost` — that check is counterpart-EXISTENCE (correctly waived); §2.4 `equity_delta` is the ledger-CORRECTNESS backstop on the cost. **RD merge-blocking at the executing return** (the §5 composition test + L1-L4 on the shipped diff). I re-verified the §2.4 composition arithmetic independently last turn; CHARC's §2 on-disk walk is sound.
- **OOF-buy arc: writing-plans CLOSED + plan merged (`308b4a35`, docs-only); EXECUTING dispatched (opus-max, measurement-core).** 3 production tasks TDD-first (the OOF sentinel-ref helper; the step-7 self-reconcile matcher branch [the authorized `swing/trades` additive carve-out]; the `journal oof-buy` CLI). Binding RD artifact = the plan's **test C1** (the both-sides-exclude composition, real-derived inputs). **My front-loaded idempotency concern landed PARTIALLY:** the plan's idempotency (plan :34/:40) is a SELECT-first **"clean no-op message + return"** on a **ticker+date** sentinel (`_build_oof_ref(ticker,date)`) — the no-op path I cautioned against, NOT the fail-loud error. **QA-the-divergence (directive #10, not defending my flag): ACCEPTABLE** for the realistic use case (OOF holdings aren't bought as multiple same-day lots → the distinct-same-day-buy collision is near-zero-probability AND a visible no-op, not a silent drop). **Converted to an executing-return check:** verify the no-op message unmistakably states nothing-was-recorded (so it can't mislead the operator into thinking a 2nd lot was captured). Not escalated mid-executing (can't change the in-flight task spec; my binding gate is the return). **No RD action until the orchestrator posts the QA'd executing return** → then the merge-blocking L-checklist QA (test C1 + L1-L4 + the idempotency-message check on the shipped diff). Await-then-QA: I touch nothing on the in-flight worktree.
- **OOF-buy idempotency: my front-loaded concern CONFIRMED on disk + ADOPTED by CHARC** (`20260621T012359Z`, thread `oof-buy-cash-coherence`). The merged plan resolved the brief's "no-op/error" ambiguity the WRONG way (SELECT-first no-op on a ticker+date sentinel `oof:<TICKER>:<DATE>`; I1 only covered a bare re-run) → a legitimately-distinct same-ticker/same-day OOF buy collides on the key and is SILENTLY DROPPED from the ledger (my #27 ledger-loss hole). **CHARC ADOPT** (well-founded, in-scope, cheap; silent ledger loss is the drift class this arc exists to kill): fail-loud `ClickException` on collision + a `--force`/disambiguator escape that records a genuinely-distinct 2nd buy under a DISTINCT `oof:` ref + bare-re-run rejected loudly (no double-record) + I1 updated to distinguish silent-no-op from fail-loud; brief §4 tightened (`e4ae1459`); lands BEFORE my merge-blocking gate. **Self-calibration (banked):** the front-load was right (it surfaced the hole the writing-plans pass missed — CHARC credited it), but my divergence-QA last turn leaned "acceptable for the realistic use case" (near-zero-probability + visible no-op) when CHARC's "fix it now" was the better call. Lesson: when a CHEAP fix closes the EXACT silent-drift class an arc exists to kill, push for the fix — don't rationalize the residual as low-probability ("low-probability" is how small holes slip through). Connects to `feedback_blunt_no_sycophancy` (push at a low threshold, including on my own too-lenient calls). **Executing-return check upgraded:** verify the FULL fail-loud fix (collision→ClickException, `--force`/distinct-ref escape, bare-re-run-rejected, I1 distinguishes) on the shipped diff, alongside test C1 + L1-L4. **(Course-correction DISPATCHED `20260621T014119Z` — fresh opus-max reusing the `oof-buy-exec` worktree, re-converge review-strong + codex-auto-review on the delta.) Added sub-check: the `--force` distinct ref must STILL self-reconcile (match `_is_oof_sentinel_ref`) AND stay unique — else `--force` re-introduces the recurring `cash_movement_mismatch` the arc exists to kill (a measurement-relevant interaction; on the orchestrator's QA radar + mine).** Still no RD action until the orchestrator posts the QA'd executing return.

### 2026-06-21 — OOF-buy arc: RD measurement-integrity L-checklist = PASS (merge-unblocked)
- **Executing returned + the idempotency course-correction returned, both orchestrator-QA-PASS; RD ran the merge-blocking L-checklist independently on disk (branch `oof-buy-exec`, base `308b4a35` → HEAD `a21fd42d`, 7 files) = PASS** (posted charc,operator `20260621T025638Z`).
  - **L4 (the load-bearing one):** the §2.4 coherence computation has **ZERO diff lines** in `schwab_reconciliation.py` (grep-verified) — my prior §2.4 L2 sign-off preserved by construction; no measurement module touched (evaluation/patterns/hypothesis zero diff); NO schema (sentinel reuses the free-form `ref`).
  - **L2 / C1 (binding artifact):** `test_c1_composition` is real-derived (built via the production `insert_cash` + `run_schwab_reconciliation` path, not premise-constructed) + the **tolerance is COMPUTED from the resolved `swing_nlv`, not assumed**; PRE-fix eval_delta=500>tol fires (swing-scoped basis), POST-fix=0 no-fire + self-sourced, the **price-move arm (M'=750,N'=1700) STILL 0** — the both-sides-exclude property (declared_oof_mv from POSITIONS vs the ledger debit from fixed COST, orthogonal). Correct.
  - **Idempotency FAIL-LOUD (my front-loaded concern) — fully resolved, no silent-drop path:** a collision without `--force` raises an actionable `ClickException` ("nothing was recorded") at BOTH the SELECT-first path AND the `IntegrityError` race-belt; `--force` records a distinct self-reconciling `oof:T:D#seq` ref (re-allocate-once on a race, then fail-loud); I1 distinguishes.
  - **L1** cash_movement only (kind='withdraw'; no trade/fill → no expectancy contamination). **L2-hardening** non-finite `--cost` rejected before the write; registry via `apply_overrides` (18-E gotcha cited+avoided). **L3-analog** triple-gated matcher (kind==withdraw AND canonical-shape AND currently-declared), non-OOF byte-unchanged (C3 exact-set), branch-disabled belt proves load-bearing, `oof:` namespace reserved at journal-cash + TOS-import (oof-buy the SOLE producer).
- **V1 residual** (a raw out-of-band DB write of a forged `oof:` withdraw would be self-sourced) — **CONCUR it's a V2 CHARC decision, not a V1 blocker:** consistent with the all-writes-through-repos trust model, COUNTED via `cash_oof_self_sourced_count`, and fully closing it needs a schema origin column (a forbidden tripwire). No V1 measurement objection.
- **Arc at the FINAL gate (NOT RD):** operator §5.10 live-witness (oof-buy → recon → coherent ledger / no recurring cash_movement_mismatch / registry rejects non-OOF) + the orchestrator pre-merge housekeeping (reword 2 `Test:`-trailer commits — the same trailer-parse hazard I hit this session; streak intact). On the witness + auth → ff-merge + the binding merged-head no-false-green. **RD gate DISCHARGED.**
- **OOF-buy arc MERGED + CLOSED (`5855c760`).** All three gates passed: operator §5.10 live-witness PASS (throwaway ZZTEST ticker, recon run #63, **5/5 checks green** — write lands / fail-loud collision / `--force` distinct self-reconciling ref / registry-guard reject / real recon self-reconciles no discrepancies; ledger cleaned + registry restored — **my front-loaded fail-loud-idempotency concern was witnessed LIVE**, 2 of the 5 checks); CHARC architecture QA PASS; RD L-checklist PASS. ff-merged; the 2 `Test:`-trailer commits reworded via filter-branch (trees byte-identical); all 18 commits trailers `[]`; ZERO Co-Authored-By; **8691 green on the merged head** (binding no-false-green); schema v31 unchanged; worktree swept. **This closes the entire SPCX/§2.4/OOF cash-side saga** (carve-out path B → §2.4 coherence → equity_delta limbo fix → OOF-buy root cash-side fix). Posture returns to STOP-ENGINEERING + market time; RD-lane = the standing watch cadence (rd-state compacted to a lean current pointer this turn). **No live arc / nothing in flight.**
- **SPCX re-record left a DOUBLE-DEBIT — RD caught it on a post-action read-only verify + corrected; cash-correction gap flagged to CHARC.** The operator (on RD's optional-cleanup recommendation) re-recorded the existing SPCX position via `swing journal oof-buy` to get the self-reconciling `oof:` ref — but the command only ADDS; the old manual path-(a) row (`cash_movements` id 5, ref `spcx-oof-buy-2026-06-15`, withdraw $372.48) could not be removed (NO cash-delete command exists). Result: id 5 + id 6 both live = a **$372.48 double-debit** of the swing ledger (~$1282 vs the correct ~$1655) that would have fired a REAL §2.4 `equity_delta` on every recon. **RD verified post-action (not assuming "completed"), found the dup, and corrected it** with a backed-up (`swing-pre-id5-dupe-delete-20260620T183303.db`), scoped raw DB `DELETE` of id 5 — one SPCX withdraw remains (id 6, self-reconciling); ledger restored. **My optional-cleanup recommendation was flawed** — it presumed a cash-delete path that doesn't exist (`swing/data/repos/cash.py` is insert/list/find only; the ledger is append-only) → owned. **Gap flagged to CHARC (operator-requested, `20260621T043434Z`, thread `cash-ledger-correction-gap`):** no supported cash-correction/void/delete path → an erroneous cash_movement can only be fixed by a raw out-of-band DB write (the same vector as the OOF-buy V1 residual); it corrupts the swing ledger (current_equity → §2.4 + sizing), so it's measurement-relevant, not just UX. Asked CHARC to weigh a `journal cash void/delete <id>` (pairs with the deferred origin-column V2). Lesson reinforced: **verify a measurement-touching "completed" against the live DB — don't assume; and verify the supported surface EXISTS before recommending an operational action** (directive #9).

### 2026-06-21 — cash-void command (D19) commissioned from my flag; RD L-checklist = PASS (merge-unblocked)
- **My cash-correction-gap flag → CHARC concurred + registered D19, and the operator fast-tracked the `swing journal cash-void <id>` command** (brief `7c1a5809`). CHARC owned their part (the OOF-witness note glossed there's no supported reversal — exactly why the operator's SPCX re-record double-debited). Design mirrors OOF: a `void:<original_id>` sentinel + an additive step-7 matcher self-source branch + a REVERSING cash_movement (NOT a delete — the ledger stays append-only, mirroring `reconciliation_corrections` discipline); 2 carve-outs (the void: matcher branch in `swing/trades` + a `find_by_id` READ helper in `swing/data`; the write reuses `insert_cash`); NO schema.
- **RD measurement-integrity L-checklist = PASS** (posted charc,operator `20260621T181459Z`), verified independently on the shipped diff (base `6872606a` → HEAD `e2e1baf8`, 9 files):
  - **L4:** §2.4 coherence chain ZERO diff AND `swing/trades/equity.py` UNTOUCHED — the void's net-to-zero correctness relies on the unchanged `net_cash_movements` ADD/SUB partition, byte-identical. No schema/module.
  - **L2 / COMP-V (binding):** real-derived (production `current_equity` over `insert_cash`'d void rows) + BOTH directions (void-deposit reverses a withdraw error; void-withdraw reverses a deposit error) + dual-surface (current_equity + recon equity_delta) + tolerance-computed + X=372.48 (my live double-debit). Restores exactly both ways.
  - **Kind-negation SINGLE-SOURCED** from `equity.py._CASH_ADD_KINDS` (the reversing kind = opposite sign-class from the SAME partition that computes the ledger → provably nets to 0; no divergent hardcode). **Kind-AGNOSTIC matcher gate** (`if cm.ref and _is_void_sentinel_ref(cm.ref): continue` — ref-only; the load-bearing OOF-vs-void difference; S-V + branch-disabled belt prove it). **Idempotency FAIL-LOUD flat, NO `--force`** (the course-correction lesson baked in from the start; void-of-a-void + not-found rejected). **L1** insert_cash-only. **C3-V** non-void byte-unchanged; void: namespace reserved (cash-void the sole producer); `find_by_id` read-only.
  - **CONCUR** the flagged Schwab-`transaction_id`-not-`^[0-9]+$` collision-boundary is OUT-OF-SCOPE (a Phase-18-close hardening candidate; a single `^[0-9]+$` constraint closes BOTH oof: + void: at once) — NOT a merge blocker; it's the IDENTICAL assumption my merged+witnessed oof: sign-off accepted (real Schwab tx-ids are numeric; the threat needs a forged upstream string tx-id). **RD gate DISCHARGED.**
- **Remaining (NOT RD):** operator §5.10 CLI live-witness + ff-merge + merged-head no-false-green. **The full chain from my SPCX-double-debit catch is now: flag → CHARC D19 → cash-void command → the SPCX-double-debit class of error is operator-correctable without a raw DB write going forward.** (Also in flight, RD FYI-only / not merge-blocking: the web-acknowledge coverage arc — a web-layer allowlist + 409-copy fix, NOT measurement-core.)
- **D19 MERGED + CLOSED `be567712`** (8734 green; operator §5.10 live-witness PASS — $50 seed restored 1654.78→1604.78→1654.78, 3 guards fail-loud; the 1654.78 also re-confirms the SPCX double-debit fix landed). The flag→D19→cash-void chain is complete. **CHARC FORM-flag (`20260621T201449Z`): this context doc is ~154K, over the 120K harness threshold → on my phase-18-close list (archive the closed-saga §7 entries to a companion + collapse to a pointer; keep new §7 entries tighter — this session's saga logging was over-verbose).**

---

## Appended 2026-08-03 (Phase-21 all-merged compaction): the 2026-06-21 → 2026-07-29 generation's verbatim entries + the tenure-close record

> Moved verbatim from `research-director-context.md` §7 when the doc crossed the 120K harness-weight ceiling (probe ATTENTION, flagged by the orchestrator 2026-08-03). The digest remains in the live doc.

- **2026-06-21 (Phase-18 close) — this §7 ARCHIVED + context-doc compacted** (156K→28K; verbatim in the archive companion). **D18 research-output disposition** (posted charc,operator `20260621T221857Z`, verified on disk): (A) delete the gitignored pattern-cohort `results.csv` (~1.1 GB; closed-null W-pattern arc; the closure studies + per-dir `manifest.json`/`summary.md` are git-tracked); (B) delete the gitignored `evaluations.csv` ONLY (~459 MB; the diagnostic-out dir ALSO holds 23 git-tracked aggregates [`binding_constraints.csv` etc.] the report reads — a blanket dir-delete would've dropped them; the `.gitignore` itself documents `evaluations.csv` as regenerable); (C) defer the standing prune (CONCUR — closed non-growing arc). ~1.5 GB reclaim, zero tracked artifacts touched. **D20** (Schwab-txn-id `^[0-9]+$` collision-boundary constraint) converged + QA-PASS — RD **fyi/no-block** (validation-only, NO measurement-value change; closes the oof:/void: collision proofs at one construction barrier). RD-lane Phase-18 obligations discharged; posture unchanged (stop-engineering + market time; T4 approaching at ~26 priced signals, zero realized edge; first monthly read first trading week of July).
- **2026-06-24 — FIRST live research-health RED (the 18-H.7 push WORKS — surfaced it same-day); INVESTIGATED: a drumbeat OUTAGE, substrate INTACT.** Root cause (confirmed on disk + operator): a SINGLE missed manual run — **06-22 ONLY** (the operator confirmed he should have run it but didn't). **06-19 was Juneteenth, NYSE-closed** — the check CORRECTLY excluded it (`_NYSE.is_session('2026-06-19')=False`; det1's check-computed missing set = `['2026-06-22']` only; "1 missing" per detection ⇒ holiday-aware, NO bug). The gap looked multi-day only because it's bracketed by the 06-19 holiday + the 06-20/21 weekend (pipeline_runs all `state=complete`/none failed; the pipeline is operator-triggered). Drumbeat RESUMED 06-23/24 (drumbeat_liveness GREEN now; 06-24 fully observed = 945 obs; BULZ det1/2/3 sequences show the 06-19/06-22 hole, recent sessions present). **Substrate clean:** #1 finiteness GREEN (0 post-baseline non-finite of 5226), #4 structural GREEN. The two pushed REDs = ONE event: drumbeat-dead (T1, self-RECOVERED) + `coverage_gaps`=842 = 842 active detections each missing the ONE 06-22 observation; PERMANENT — observe never backfills. **Cause CLOSED:** operator confirmed 06-22 was a missed manual run (the pipeline is operator-run, not a cron → occasional missed days are EXPECTED). **Calibration gap REALIZED (the `coverage_gaps` cry-wolf-trajectory I flagged, now at scale):** a SINGLE missed manual run holds the stoplight RED for WEEKS (until the 842 detections age out) — and with a manual pipeline this RECURS. Principled V2 = red only on a CURRENT/ongoing gap (drumbeat not observing the latest sessions), ACCEPT historical gaps once the drumbeat provably moved past the window (latest observed) → flips #3 back to green now (06-24 fully observed). CHARC-lane monitor calibration (research_health.py #3, sub-tripwire like the 18-D calibrations), **RD-recommended; pending operator go to author/route.** Secondary: `invalid_ohlc` 23→26 (+3 new; #2 yellow; minor — archive-bar ragged, not a log-write regression since #1 GREEN).
- **2026-06-24 — coverage_gaps calibration: PLAN-STAGE SEMANTIC sign-off CONCUR** (`20260625T072355Z`; plan `96b582ff`, worktree; reviewed on disk). Brief routed `c2d7b3ac` → writing-plans converged (4 Codex rounds). The plan settled on my (b)-lean and went BEYOND my brief on two points, both genuine improvements I concurred: (1) whole-session-miss requires BOTH `S ∉ global_observed_sessions` AND `S ∉ run_observed_sessions` (completed-run `data_asof_date`) — my "no pipeline run" wording conflated "no run" with "no obs"; the quadrant partition keeps a zero-obs observe-FAILURE [run ran, wrote nothing] RED (Task 4b) + a globally-observed-but-no-ledger hole RED (Task 4c); (2) the per-ticker `(ticker,S)` skip-warning attribution is semantically correct (a no-bar is a per-ticker fact). My §3 mandates all met: still-reds (trailing≥2 / never-observed / unexplained-interior / 4b / 4c, exact-count controls); the accept-only-moved-past CRUX carried by clause-1 (an observed session AFTER S → a current trailing outage still REDs); conservative-degrade `run_observed_sessions→None` (not empty-set, never accept-everything); auditable detail (#27); real-derived. NO push-back (the plan is STRICTER than my brief everywhere). CHARC owns the monitor/§3 + tripwire self-cert; operator authorizes dispatch. At the executing return: post-build semantic review (binding 06-22→green on the LIVE DB + still-reds hold) + fold watch-standard §3.1 (plan §5 draft accurate).
- **2026-06-25 — coverage_gaps CALIBRATION C: EXECUTING-RETURN semantic review PASS, cleared for merge** (`20260625T082754Z`; HEAD `6511f891`; executing converged review-strong R1-R4, 8795 green). **BINDING LIVE CHECK PASS:** ran the shipped worktree monitor vs `~/swing-data/swing.db` → coverage_gaps flips the real 842-RED → GREEN ("0 gaps; 842 accepted historical (missed-run/skip)"; accepted gaps in the DETAIL, #27). Read the shipped accept-rule (`research_health.py:1063-1105`) — matches my sign-off exactly (clause1 ∧ (BOTH-signals is_whole_session_miss ∨ skip_index)). **Executing added 2 NEW MAJORs in my lane — R1/R2 canonicalization** (`_canonical_session`: obs/asof dates are TEXT w/ no format CHECK + `date.fromisoformat` accepts compact/week forms → a degraded-date false-green vector; harmonized + CONSERVATIVE-degrade everywhere: unparseable ledger date → `_run_observed_sessions`=None → clause-2a disabled → still reds) — CONCUR, sound. **Watch-standard §3.1 FOLDED** (`554619fe` — old "RED on any interior gap" superseded by CALIBRATION C). RD gate CLEARED; merge pends CHARC §3 code-QA + operator authorization; on merge re-confirm green on the merged head from MAIN. **Secondary (NOT this arc):** flagged drumbeat_liveness #5 is CWD-sensitive for artifact discovery (worktree→false-RED "no engine artifacts"; main repo→GREEN) — may explain the 06-24 #5 push; CHARC's call to look.
- **2026-06-25 — coverage_gaps CALIBRATION C MERGED `83c21305`** (merged-head 8795 green, both directors cleared; transcript `docs/reviews/coverage-gaps-calibration-executing-codex-findings.md`). My arc CLOSED; the first research-health RED's COVERAGE half resolved (the 842 missed-run gaps now read accepted-historical). **NEW RD-lane flag — drumbeat_liveness #5 recurring FALSE-RED** (re-flagged charc,operator `20260625T164541Z`; thread `drumbeat-liveness-false-red`): #5 RED "no engine artifacts" fired 06-24 08:12 + 06-25 16:34 (GREEN at run 112 06-25 06:12 → INTERMITTENT), but the drumbeat is provably ALIVE (16 artifacts, newest TODAY; main-repo monitor #5 GREEN). Writer (runner.py:1207) + reader (:1361) BOTH use `cfg.paths.exports_dir/research` → no static divergence (disproved the script-vs-pipeline-split hypothesis); the false-RED is LAUNCH-CONTEXT-dependent (a web-spawn cwd/config_path→project_root resolution reading an empty exports_root, + shadow-expectancy apparently not writing those runs — CHARC's trace to pin). **CORRECTED the merge-note conflation** (the orchestrator read my #5 flag as the Codex-review WSL /c/-vs-/mnt/c/ path thing; it's NOT — mine is the MONITOR CHECK's artifact discovery). WHY IT MATTERS: #5 is the PRIMARY drumbeat-death T1 alarm → a recurring false-RED makes it untrustworthy (+ keeps firing the 18-H.7 push). CHARC engineering lane; RD reviews the fix semantics; not a data emergency. Watch-standard §3.1 already FOLDED (`554619fe`).
- **2026-06-25 — drumbeat flag WIDENED: the broken-launch-context run poisons the WHOLE `latest.json`, not just #5** (CHARC FYI folded `20260625T165110Z`, same thread). CHARC observed coverage_gaps "842 mature → 0 mature (n/a)" in ~8h. RESOLVED in my lane: NOT a maturity-window-semantics issue — the live DB has 842 mature RIGHT NOW (1060 detections, 955 observed; live monitor green "842 accepted historical"). The 16:34 "0 mature (n/a)" = the SAME broken 16:34 run reading an EMPTY context (empty `pattern_detection_events` → "no detections (n/a)" [research_health.py:1171-1174 = per_det EMPTY, not a maturity calc] + empty exports → drumbeat "no artifacts"). ONE broken run, BOTH symptoms. **Escalation:** the broken run reads empty cfg paths for ALL DB/exports checks but WRITES a "successful" envelope to the repo-anchored production `latest.json` → POISONS the 18-F stoplight + 18-H.7 push source with empty-context garbage. RECOVERED at 16:49Z (a good run overwrote it; overall yellow, drumbeat green, 842) — TRANSIENT but recurs. Recommended CHARC fix scope: (1) the broken-launch-context cfg-path resolution AND/OR (2) a defensive "suspicious all-empty context → write-nothing" guard (don't overwrite a good `latest.json` with an all-n/a envelope). RD reviews fix semantics.
- **2026-06-26 — drumbeat false-RED recurred (now 4×, DAILY: 06-24, 06-25 16:34, 06-26 06:18); NEW evidence + de-escalation** (follow-up `20260626T072043Z`, same thread; will NOT ping per-recurrence). **Real drumbeat HEALTHY / not starved:** a fresh `shadow-expectancy-20260626T024023Z` artifact landed in the real exports; main-repo monitor #5 GREEN. **NEW: the broken context mis-resolves the DB PATH too, not just exports** — NO 06-26 row in live `pipeline_runs` (newest run 113/06-25) despite the 06-26 activity, and every broken push reports **"run id: unknown"** (vs the good coverage push's "run id: 112") → the broken run reads/writes a WRONG DB context (so the whole cfg is mis-rooted on those launches, not just `exports_dir`; resolves my earlier "is db_path affected?" uncertainty — it IS). "run id: unknown" + missing-run-row = a clean CHARC repro signal. Not urgent (real drumbeat healthy, latest.json transient-recovers) but daily-recurring.
- **2026-06-27 — db re-examination + OWNED a correction** (CHARC diagnosed #5; query `20260627T071545Z`, same thread). **CORRECTION I OWNED:** my "broken context mis-roots the WHOLE cfg incl db_path" was WRONG — `db_path="swing-data/swing.db"` is HOME-anchored (cwd-INDEPENDENT; CHARC right). I over-inferred from symptoms without tracing `_resolve_path`. **The exports/drumbeat half is CONFIRMED** (CHARC, on disk): web `/pipeline/run` passes a RELATIVE `--config` + no `cwd=` → `project_root=config_path.parent.resolve()` (cwd-dependent) → `exports_dir` mis-roots → drumbeat false-RED + the repo-anchored latest.json poison. **My grounded db re-exam:** empty per_det ⟺ empty DB (the coverage SELECT has NO WHERE clause); the push fires from EXACTLY the leased step (runner.py:1368, run_id=lease.run_id) — the standalone script writes latest.json but never pushes. So the db-symptoms (empty per_det + run-id-unknown + missing live row) require a DIFFERENT-**HOME** launch (re-roots the home-anchored db → empty db + lease→wrong-db), NOT cwd — a scheduled/service/elevated launcher, NOT the web-spawn (correct HOME → coverage fine + run-id known). **KEY FIX-SCOPE for writing-plans:** the commissioned ROOT fix (absolutize config_path) covers the cwd/exports half but does NOT cover the HOME-driven empty-db/coverage half (db isn't config_path-anchored) → the **GUARD must catch the empty-DB read too**, not just empty-exports. I review the guard's real-empty-vs-broken-empty discriminator at the fix (merge-relevant). Operator holds the fast pin: which launch path he used on the broken runs (scattered times argue interactive-wrong-environment, not a cron).
- **2026-06-27 — #5 OFFENDER PINNED (operator-confirmed) — manual WORKTREE launches; pause LIFTS** (`20260627T080901Z`). Operator: always manual, no scheduled launcher; he SWAPS between worktree branch-versions and main. The earlier diagnostics (script `diag_5_launch_context.py`) confirmed his interactive MAIN context is HEALTHY (USERPROFILE correct, db 1270, exports 18). The Drive "Daily Market Weather" scheduled task = a DEAD orphan (run_weather.bat missing; no Drive project copy) — ruled out. **VERIFIED:** MAIN exports/research = 18 artifacts, a WORKTREE's = 0 (artifacts GITIGNORED → main-only) → running `swing web`/pipeline from a WORKTREE reads EMPTY exports → drumbeat false-RED. db home-anchored → correct → coverage usually fine (drumbeat-only pushes). **THE BUG = READ/WRITE ANCHOR MISMATCH:** reads exports via `cfg.paths.exports_dir` (cwd→worktree) but writes latest.json + pushes via `__file__` (editable-install→MAIN) → a worktree run POISONS main's production artifact. **My db-home over-inference was thus DOUBLY wrong** — not a different HOME either; it's a worktree CWD reading empty exports, db always correct. **Fix REFRAME fed to CHARC:** absolutize-config_path masks worktree isolation; cleaner = anchor-CONSISTENCY (worktree run writes its OWN latest.json) + the write-nothing guard. RD reviews guard semantics. Practical: run from MAIN → no false-REDs (operator confirming now). Lesson re-banked: I twice over-inferred a mechanism (whole-cfg, then HOME) from symptoms before tracing the code path / asking the operator the launch question — the operator's "we swap worktree/main" answered it in one line.
- **2026-06-30 — T3 golden-gate spot-check CAUGHT a shadow-engine RISK-UNIT artifact** (operator's weekly_glance fired a T3 ATTENTION; flagged CHARC `20260630T012011Z`, thread `shadow-engine-risk-unit-artifact`). The A+ baseline mtm headline showed **+27.333R** (VSTS, n=1 → A+ per-signal expectancy 13.667). **Hand-walked vs raw bars (yfinance) — COMPUTATION ARTIFACT:** VSTS 06-25 close 13.74 → 06-29 close 14.83 = +7.9% over 4 sessions (the engine's exact mtm price), a NORMAL move; for +27.333R the effective risk unit must be ~$0.03-0.04 (~13-17× tighter than the $0.52 ATR; atr_pct=3.85%, close_at_detection=13.49 from the detection metadata) — un-tradeable. A sane stop → ~2.6R (≈ the normal winners CNTA 2.625R / VECO 2.664R). Both VSTS variants `entry_bar_ambiguous=True`; broad-watch VSTS = 5.137R (×6, ~$0.33 risk) — same artifact, less extreme; the A+ 06-25 was a 52w-high breakout (prox_52w_high_pct=0.0) with the stop collapsed to ~$0.04. **Scope:** all `horizon_mtm` (OPEN) → corrupts the mtm headlines (A+ 13.667 n=1; broad-watch mtm mildly) NOT the broad-watch closed-only realized (−0.968R n=65, clean) — but a latent T4 hazard when these CLOSE into realized. **Recommend CHARC:** a risk-unit FLOOR (≥ a fraction of ATR) and/or gate the `entry_bar_ambiguous=True` signals (the engine flags them but still counts the inflated R). RD reviews the fix's measurement semantics. CAVEAT (honest): the ~$0.04 risk unit is INFERRED from the raw move + reported R (the detection table doesn't store the engine's entry/stop) — the exact stop computation is CHARC's code-trace. NEW live `standard`-cohort trade noted: VSTS 06-25 (the operator's REAL position is a separate ~+8% winner on his own entry/stop, NOT the shadow's 27R — don't conflate).
- **2026-07-02 — coverage_gaps RED (run 117) = a CALIBRATION-C GAP in MY design (false-positive on a DELISTING); flagged CHARC `20260702T044056Z` (thread `coverage-gaps-calibration`).** Investigated (didn't assume): run 117 is a REAL leased run (run_id known, NOT the #5 worktree class); 60 NEW counted gaps + 842 accepted. Root: **CNTA delisted/acquired after 06-29** (yfinance flat O=H=L=C=40.50 06-26/06-29 = deal-price pin, then no bars; the observe step CORRECTLY logged "no bar" skip-warnings — 30 in run 116 + 35 in run 117). ~40 CNTA detections × the 2 trailing sessions (06-30/07-01) ≈ 2/3 of the 60. **THE GAP:** CALIBRATION C's clause-1 (interior/moved-past) gates BOTH 2a AND 2b (`if not clause1: continue` ~research_health.py:1092); a delisted stock's skip-warning holes are TRAILING (no later obs possible) → clause-1 FALSE → 2b acceptance skipped → COUNTED → false-RED. The DINO test was INTERIOR, so this trailing case was uncovered — **I OWN it** (my design applied the interior-gate to the skip-warning case too). **FIX:** accept 2b regardless of clause-1 (skip-warning = direct benign-no-bar evidence, valid interior OR trailing; clause-1 gates ONLY 2a). SAFE: a real drumbeat-behind trailing failure has NO skip-warnings → still counted. Alternative: truncate a delisted detection's expected window at its last bar. Recurs on every future delisting; data fine (drumbeat healthy, 1493 obs 07-01). RD reviews the refinement at the fix.
### 2026-07-03 — monthly watch read #1 (standard v2)
- Drumbeat: 10/11 sessions covered since 06-17; gaps: 06-22 only (missed manual run, adjudicated 06-24; accepted-historical under CALIBRATION C). Newest artifact 0d old.
- Funnel (newest, `20260703T020948Z`): 1765 -> 353 -> 0 unattributed. Delta since the 06-30 logged numbers: +390 detections / +78 signals.
- Pricing: trigger broad-watch 165/274, A+ 2/3; **priced N=165 signals / 48 unique names (gate 30 — CROSSED)**; scenarios closed_only 97 / mtm 165 / forced 165 / adverse 165.
- Log maturity: longest window 20 sessions (engine horizon cap); matured-through-full-horizon = 46 detections / 10 unique signals (gate 100 — T9 PENDING under the matured reading; definitional note: raw counts [1765 det / 353 signals / 615 ticker-class] already exceed 100, but the standing interpretation since 06-17 is matured-forward-walk — flagged for a deliberate operationalization at a future read).
- Live epoch record: 2 new trades (VSTS 06-25, AMN 07-01), both `standard` / A+ baseline, both `managing`; intent contract HELD; discipline panel clean.
- Hypothesis progress: H1 1/20 closed (+2 OPEN standard A+ — the first live H1 accrual since the epoch), H2 1/10, H4 0/10, broad-watch priced 165/30.
- Tripwires fired since last read: **T3** (AMN spot-check PASS, entry below); **T4** (gate crossed — **DECISION READ EXECUTED**, study `research/studies/2026-07-03-broad-watch-baseline-t4-decision-read.md`: **PROVISIONAL-NEGATIVE — bankable A+-selectivity validation**; closed-only mean R −0.648, n=97/36 names, win 10.3% Wilson-LB 5.7%; mtm +0.680 NOT bankable [survivorship + risk-unit artifact + 165/165 ambiguous-flag optimism all bias UP]; adverse ≈0; confirmatory re-read PRE-COMMITTED on the first post-Phase-19-fix artifact — engine is pure-recompute); **T5 ×2** (VSTS + AMN, contract honored); research-health REDs 06-24 (missed run, adjudicated) + 07-02 (CNTA delisting = CALIBRATION-C gap, mine, flagged CHARC); #5 false-REDs offender-pinned + PARKED. No T1 (the 06-22 gap was 1 session, under the >2 threshold), no T2 (unattributed 0 throughout), no T6/T7/T8.
- Posture: **unchanged** — stop-engineering + market time. The one queued engineering item (Phase-19 shadow-engine risk-unit/ambiguous-entry fix) predates this read and now also carries the T4 confirmatory re-read; no new engineering demand originated. Notable convergence: shadow watch-pool win rate 10.3% reproduces the pre-epoch live record (12.5%) — two independent paths agree the unfiltered watch pool loses; A+ selectivity validated at the gate boundary.
- **2026-07-03 — T3 spot-check AMN A+ (det 07-01, run 116): PASS — exact-match, risk unit SANE (not the VSTS artifact class).** Hand-walked vs the temporal log + an INDEPENDENT yfinance fetch (O/H/L/C match to the cent all 3 sessions; obs rows written post-close 02:00Z — no partial-bar capture; the 07-01 volume delta 426k→622k = the known yfinance same-evening-vs-consolidated revision, price-irrelevant to the walk). Walk: entry bar 07-01 (H 33.72 ≥ candidate pivot 33.48), fill = max(pivot, open) = 33.48, stop = entry-bar LoD 32.35 → **risk $1.13 = 3.38% of entry = 0.78×ATR (tradeable)**, mark = 07-02 close 34.75 → **+1.1239R — engine-exact to 4dp**. Flags consistent: `entry_bar_weak_close` (C 33.24 < pivot); `entry_bar_ambiguous` — the entry-bar low IS the stop, so the intra-bar path is unknowable from daily bars (engine resolves optimistically; if the low printed AFTER the fill it's a −1R same-bar stop-out — exactly the Phase-19 ambiguous-entry-gate class, but here the risk unit is sane, unlike VSTS's collapsed ~$0.04). Open `horizon_mtm` — a mark, not realized. A+ cohort = 3 signals total: NVCR 06-18 untriggered / VSTS 06-25 artifact (mtm decayed 27.33→17.78R) / AMN +1.12R sane. The operator's REAL AMN 07-01 `standard`-intent trade (T5, contract honored) aligns with this shadow signal.
- **2026-07-03 — Phase-19 Arc 19-A (coverage_gaps trailing-delisting fix) full RD chain same-day: plan-stage PASS + consequence-#2 BLESSED (two bounds) -> merge-blocking QA PASS -> MERGED `fadc193d` -> watch-standard §3.1 FOLDED.** Plan review (`20260703T082054Z`): truth table independently recomputed (only A=F∧C=T flips); T1/T5/T2/T3 arithmetic verified both paths; 3 code anchors verbatim on disk. Bless bounds: (1) attaches to the CURRENT `_observe_skip_index` admission rule (any loosening re-opens); (2) `N accepted historical` surfacing stays (#27 visibility = the 2b-trust compensating control). Merge QA (`20260703T091145Z`): loop=plan §2.2 on disk; both bounds verified IN THE DIFF (zero `_observe_skip_index` hits; surfacing locked by new assertions); Codex converged (review-strong 1 round + auto-review clean, transcript-read); **binding live-DB gate RE-RUN BY RD: coverage_gaps GREEN, 0 gaps, 947 accepted** (842+105 CNTA). Merged-head 8885/0-failed. §3.1 fold committed same-day (the asymmetric 2b-vs-2a rule + consequence-#2 semantics + still-reds). **D21 ruling** (`20260703T092145Z`): brief-coupled doc-assertions are ARC-LIFECYCLE-SCOPED — retire at arc close once banked (kept: byte-stability/L2/cohort locks — the frozen-regeneration-semantics predicate); executed as R4 `2d626b27` (main 8879/0-failed). **NEW #5-thread evidence:** 3 `pipeline` REDs (`run id: unknown`, 09:26/09:34/09:45Z) fired to the REAL RD inbox during the 19-A/R4 SUITE window with NO pipeline run after 118 and production latest.json GREEN + drumbeat alive — suite/worktree contexts can fire the production 18-H.7 push (test-isolation leak; alarm-fatigue on the primary T1 channel); flagged CHARC for the weekend #5 guard scope. Also: my worktree monitor probe live-reproduced the #5 read-side false-RED (wrote the worktree's OWN exports — script path anchor-consistent; the vector is pipeline/web-spawn + the push transport).
- **2026-07-04 — Arc 19-B (the #5 root fix) MERGED `3234f19a`; full RD chain logged.** Plan-stage PASS + 6 rulings (`20260703T161433Z`; the required 8.1(b) warnings_json-on-suppress SHIPPED verbatim) → merge-blocking QA PASS (`20260703T182123Z`; locks in-diff, discriminators re-run 43/43, zero-real-comms live-evidenced) → post-merge session-belt flake adjudicated (`20260704T031351Z`): NARROW-FIX blessed (signature-scoped ADDED-inbox detection; whole-tree-frozen was mis-designed for a multi-agent repo — the miss survived my plan review too, owned). Codex adjudications verified correct (R1 CRITICAL = my accepted 8.1(a) window; auto-review state='running' P2 = my 8.2(b) rejection). Pending: the binding operator witness (MAIN-run witness-seed + worktree no-poison). Detail in rd-state + the comms posts.
- **2026-07-04 — 19-B operator witness PASSED both halves → ARC FULLY CLOSED; the #5 saga ENDS** (run 119 seed: detection_count=1775, window closed, coverage GREEN live; run 120 worktree: main byte-identical, no post). Belt narrow-fix `44b3bbd9` verified on disk (imported-constant signature). **#2 invalid_ohlc 38 ADJUDICATED** (`20260704T052718Z`): NOT delisting churn — 16 ragged-SHAPE yfinance bars / 12 tickers (0.04–1.7%; flat delisting bars PASS validate.py) → 10.8% signal attrition, growing; calibration stands (true yellow); recommended epsilon-tolerant engine-reader clamp BUNDLED into the queued engine-fix arc (pure-recompute → recovers all 38 retroactively; one T4 confirmatory re-read covers all). Evidence: live-DB probes (zero duplicate sessions, zero post-baseline non-finite, per-bar magnitudes measured).
- **2026-07-05 — T4 CONFIRMATORY RE-READ EXECUTED (read #2, artifact `20260704T223859Z`, the first post-19-D full-corpus run): VERDICT CONFIRMED-NEGATIVE — FINAL.** closed_only −0.637 (n=100/39 names, win 11.0%, LB 6.3%) vs read-#1 −0.648; adverse flipped negative (−0.057); mtm dropped 0.680→0.551 as the bias-removal predicted; **the A+ headline healed to +1.124 n=1 = my hand-walked AMN value — measurement equals the honest walk.** degenerate_risk=5 (VSTS + the ARMK cluster caught); invalid_ohlc 38→22 (16 recovered, clamp census = my probe exactly); PGNY survives at ratio 0.164 (lawfully above the 0.15 floor — the 0.15–0.2 band watch item banked, no re-tune). Study §8 appended; the phase's terminal measurement gate DISCHARGED. 19-D/E/F all merged (f1add34c/d68ccfe6/1ff15898, per-merge suites green; belt-from-main clean). A+ selectivity validation now BANKED FINAL; posture: stop-engineering + market time.
- **2026-07-06 — PHASE-19 SIX-ARC LIST COMPLETE (triple witness).** 19-C stage-b (run 125, first true scheduled weeknight fire, clean) · **19-E: the natural witness DISCRIMINATED ruling-A live** — AMN (entry 07-01) fired the measured-ruleset partial advisory at exactly session-day 3; calendar-day counting would have read Monday as day 6 → window closed → NO fire (the ruling was load-bearing on its first real fire); unseeded-default half clean (VSTS out-of-window, silent) · 19-F: disc 73 (my June SPCX raw-delete residue) resolved through the supported GUI path, reason naming the missing row, badge gone. Remaining = CHARC riders + close ritual. RD Phase-19 ledger: 6 plan-stage reviews/rulings, 6 merge-blocking QAs, 2 calibration adjudications, the T4 read pair (provisional→FINAL), 1 falsified own-hypothesis (capture-timing) owned + corrected in docs.
- **2026-07-07 — VSTS CLOSED +0.05R (scratch): the epoch's FIRST closed `standard` trade — H1 = 2/20 closed.** Entry 13.61 (06-25, A+ signal) → trailed per plan → exit 13.73 (07-07) on signal. Clean discipline end-to-end: signal entry, ruleset trail, signal exit, scratch result. **AMN Day-3 partial TAKEN** (3 of 6 @ 35.65 at the 07-07 open, +5.9% banked, following the 19-E advisory's Monday-evening fire; remainder stop 31.84). **Shadow-twin divergence observed (record, not defect):** the engine's AMN A+ signal closed 0.0R `breakeven_stop` — its partial fire-point (session_index 3 = doctrine Day 4 = 07-07) collided with the breakeven stop (07-07 low 32.67 ≤ 33.48 entry; stop-before-EOD-partial ordering, arithmetic verified vs the bars) while the OPERATOR trimmed at day-3+1-open per the advisory — both within the doctrine 3-5 window; the 19-E-disclosed off-by-one showed up as real economic divergence (live ahead of shadow). A+ shadow closed cohort begins: n=1, 0.0R. Glance otherwise clear: scheduler fires Mon+Tue visible (19-C production-confirmed), unattributed 0, trigger 2/3→1/2 = the healed engine excluding VSTS-collapsed (expected), 5 new broad-watch priced accruing.
- **2026-07-11 — D25 MEASUREMENT ALERT: the tier-1 auto-corrector corrupted 3 fill rows (forensic broker reconciliation, to-the-cent); AMN's H1-feeding outcome recorded −$9.60 vs TRUE +$1.17.** Mechanism pinned from the system's own audit rows: same-qty leg-pair mismatch, no side/date guards, self-sealing; #34 RE-FIRED over its own correct #33 fix (07-10 — AFTER my 07-07 read, which used the true 35.65, so the divergence watch note SURVIVES as written). **The equity_delta instrument caught it within 24h ($10.94 > $10) while BOTH directors nearly prior-dismissed the badge as deposit drift — my recurring-deposit prior was the exact trap. Lesson banked: adjudicate residuals by DECOMPOSITION, never prior plausibility.** Phase-20 consult answered (`20260711T091206Z`): 20-A scope accepted + ONE added gate (RD-witnessed post-correction H1 ledger re-derivation, pre-August-read binding); **SATL phantom (trade 11) RULED: VOID-not-delete, excluded from all cohorts, audit-visible; tuition record restates 16→15 real**; **STRONG RECOMMEND: pause tier-1 fill-price auto-correction NOW** (3-for-3 corruption on the every-normal-trade geometry; re-fire-over-own-fix is disqualifying). AMN closed 07-09 (true ≈ +0.1R scratch-win pending correction); H1 = 3/20 closed pending the corrected re-derivation.
- **2026-07-11/12 — 20-A COMPLETE THROUGH B3: the witnessed Half-B landed; RD B3 witness PASS.** Full chain same-day: plan-stage rulings (B2 narrowed-V1+2 bounds / A2=2.0%+>1 session / ORDER / #33 stamp) → merge QA PASS + item-#1 ACCEPT (the Codex-driven 17-surface B2 sweep; D19 visibility test-locked; single-helper no-drift) → merge `d5999345` (9092/0) → Half-B executed (B1 ×3 overrides + SATL void; **#33 stamp WITHDRAWN on revised ruling** — the chains are per-DISCREPANCY and both post-B1 heads AGREE on 35.65; my premise-miss #2 owned [ruled from a summary without tracing the chain key]; the shipped A-4 guard documents the live double-head and resolves latest-applied-wins BY DESIGN) → **B3 WITNESS PASS: AMN +1.17 EXACT from corrected fills; H1 epoch honest (VSTS +$1.80 / AMN +$1.17); SATL 16→15 effective + detail-visible; LEDGER↔BROKER TO THE CENT (1200 initial + 425.38 signed movements − 69.77 corrected flows − 0.01 phantom + 0.20 penny-drift = 1555.80)**. Two in-stream arithmetic bugs during my own recompute (unsigned kinds; missing initial funding) owned + corrected — decomposition discipline applies to the witness too. REMAINING: operator badge-death witness; the LIVE-FIRE watch (the next scheduled run is the first reconciliation over corrected fills — the #34-class geometry; guards must hold silent; read at next glance).
- **2026-07-15 — PHASE 20 CLOSED (CHARC audit CLEAN at `e3b6570f`); the D22–D25 cluster retired badge-to-retirement in FIVE DAYS.** 20-B (D22 resolve-gate) + 20-C (D23/D24 coherence-UX) executed in parallel, witnessed (both PASS), merged (`ec348221`); riders R1-R3 done; ritual + audit clean (9115/5/0; v31 held a SECOND zero-migration phase; streak ~3983). My 20-B light-note design shipped as required (--force bypass RECORDED on the audit surface, never silent — no objection); 20-C displays the OOF-NETTED basis per my banked stored-vs-emit note. The audit §6 records both directors' owned errors + the headline lesson now in BOTH behavioral lists: **the instrument beat the directors** — a monitor that fires gets a decomposition before it gets an explanation. NOTHING WRONG REACHED A WITNESSED MERGE across Phases 19–20. Carried to Phase-21: D5 suite-runtime (5:14, crossed the 5-min line — headline candidate), the 0032 taxonomy type, the two V2 candidates (execution↔fill identity; my bound-b stats choke point), my floor-ratio + shadow-twin watches. CHARC generational handoff running. RD docs link-checked clean vs the archival sweep.
- **2026-07-23 — T5 FTRE story VERIFIED (mechanically) + a CONTRACT GAP found: the A+ entry-window is undefined.** Operator (on vacation): believed FTRE never hit the pivot; a buy-stop-limit never fired; cancel-before-07-20 not recalled. BARS: 07-20 H=17.84 (no touch), 07-21 H=18.33 vs pivot 18.34 — **missed by ONE CENT** — recollection RIGHT through 07-21; **07-22 H=18.59 CROSSED** (C=18.49) with no fill → the order was dead by then (canceled or DAY-expired). NOT a discipline breach: FTRE had lapsed to `watch` on 07-21 and the epoch contract never defined how long a fire's entry mandate lives. BUT the SHADOW window is detection-anchored full-horizon → the engine will trigger FTRE on the 07-22 cross once depth suffices (now excluded `insufficient_forward_depth` n=2) → a STRUCTURAL live-vs-shadow divergence: shadow H1 books an FTRE outcome, live H1 does not. **RECOMMENDATION (posted to operator): align live order posture to the measured semantics — A+ fire → GTC buy-stop-limit at the pivot, canceled ONLY on setup invalidation (close below the initial-stop level / base break), never by bucket-label flicker or expiry-by-forgetting.** Covers both directions of the stale-order hazard (07-11 note). FTRE 07-23: watch, failing ONLY `tightness`, 10c under the new pivot 18.59 — may re-fire imminently. Catch-up read folds into monthly read #2 next week (operator-sequenced). Fresh-read state: research health FIRST ALL-GREEN (invalid_ohlc back to baseline 23 — the attrition watch RESOLVED); scheduler unattended-clean; broad-watch closed mean drifted −0.637 → −0.224 at n=267 (direction holds; examine at read #2).
- **2026-07-23 — A+ LATCH POSTURE ADOPTED (operator, in-session): the epoch contract's entry semantics are now DEFINED.** An A+ fire LATCHES an entry mandate: a GTC buy-stop-limit at the fire's pivot with a buy-zone limit cap; the latch clears ONLY on (a) FILL, (b) SETUP INVALIDATION (close below the initial-stop level / base break), or (c) the detection HORIZON closing (~20 sessions — the shadow-parity bound). Bucket-label flicker never clears it. This closes the entry-window gap the FTRE T5 verification exposed and keeps the live cohort's entry semantics ≡ the measured (detection-anchored) window. APPLIED IMMEDIATELY: operator placing the FTRE order (stop 18.34, original limit) — fills at market if price ≤ limit; `standard` intent on fill. Banked (NOT commissioned): a GUI "latched signals" surface as a V2 capability candidate — raise at Phase-21 scoping only if the latch count grows beyond memory.
- **2026-07-23 (cont.) — FTRE latch operationalized + the LATCH DASHBOARD declared a Phase-21 input (operator-originated).** The re-placed stop-limit was broker-rejected (price above stop — expected mechanics; the strength-confirmation already occurred 07-22), converted to a resting **GTC buy-limit 18.89** (= pivot +3.0%, the zone's outer edge; size 10; risk ≈ $23.80 ≈ 1.2% at the 16.51 stop). The latch's first live application held the operator out of a +6.2% chase (price 19.475) — the zone cap working day-one in both directions. **Phase-21 demand posted to CHARC (`20260723T161518Z`):** implement the latch mechanic in the dashboard — read-only latch panel (fire/pivot/zone/price/days-to-horizon/invalidation level/state) with invalidation visibility first-class; semantics SETTLED (fill/invalidation/horizon clears; +3% band as config); RD gates the derivation rules (shadow-window parity), CHARC designs; sequencing vs D5 = CHARC's proposal.
- **2026-07-27 — VSTS RE-FIRED A+ (5th fire ever, 2nd for VSTS; pivot 16.90, framework stop 13.40, close 15.58) — the latch's first CLEAN application: order placed above market AT FIRE TIME, before any move (contrast FTRE, where the mandate was undefined and the order died on vacation).** Two live latches now (FTRE armed-unfilled, horizon ~08-17; VSTS fresh). **OPERATOR SCOPE AMENDMENT to the Phase-21 latch demand:** (i) latch surface should check for IN-PROCESS ORDERS; (ii) potentially let the FRAMEWORK PLACE/CANCEL orders on latched tickers. **RD posted a THREE-TIER SPLIT to CHARC (`20260727T160836Z`): TIER 1 order-state awareness (read-only) = ENDORSE + cheap — the order-read plumbing ALREADY EXISTS in production (`get_account_orders`/`_step_schwab_orders`/`map_orders_to_fill_candidates`, used for fill recon); it is a join+surface problem and yields the free ARMED-BUT-NO-ORDER alarm (the exact FTRE failure, cost +1.22R) plus its inverse. TIER 2 prepared-order + loud invalidation alert (framework computes, operator confirms) = the recommended sweet spot, applying the harness's own 'automate the transport, never the authority' principle to execution; no new write scope. TIER 3 framework places/cancels = NOT NOW** — four reasons: it is an **L2 WRITE-endpoint event** (place/cancel/replace = a second L2 re-anchor, operator-signed endpoint-diff required); the **D25 precedent is directly on point** (we just spent two phases fixing an automated writer whose matcher corrupted 3 fills for six weeks and re-fired over its own correct fix — the same bug class in an order path moves real money irreversibly); the **marginal value is narrow** (FTRE was lost to an undefined posture, not missing automation — a manual GTC-at-fire now survives vacations); and **specification risk** (n=2 latches, ZERO invalidation-cancels — designing automation off anecdote). Measurement-lane input flagged for the operator's decision: mechanized entry would CLEAN live-vs-shadow parity but partially COLLAPSE the charter's two-unknowns separation (live-H1 would become 'shadow with real fills' — arguably more valuable, but it ends the operator-discipline measurement; a deliberate call, not an automation side effect).
- **2026-07-27 (cont.) — MEASUREMENT RULING on the operator's latch-execution design (filled form + PLACE/CANCEL buttons, staged log-only → preview → live; CHARC scope `81244576`): the design BEATS MY OWN THREE-TIER FRAMING and is adopted.** My tiers drew the line at 'does the framework compute or act'; his draws it at 'does a HUMAN DECIDE EVERY ORDER' — the line that actually matters, which leaves my D25 objection nothing to bind on. **RULING (a) — two-unknowns separation PRESERVED and the instrument IMPROVES.** The live−shadow difference decomposes into three components: (1) MECHANICAL NOISE (unplaced/wrong-type/forgotten orders — FTRE's +1.22R measured exactly this; it is CONTAMINATION, not signal — removing it is a strict gain); (2) DECISION VARIANCE (whether/when to act, stop choice, exit + partial timing, sizing — the actual operator-discipline signal, mechanized by NOTHING in this design); (3) FILL QUALITY (added by going live). Removes 1, preserves 2, adds 3. **On CHARC's honest counter (trivial clicking → he always clicks → convergence):** observation correct, conclusion inverts — the entry decision was ALREADY non-discretionary under the epoch contract, so mechanizing it IMPLEMENTS a committed rule rather than collapsing a decision; and decisively, **today a non-entry is AMBIGUOUS (decision vs forgetting — FTRE needed a bar-by-bar forensic to resolve ONE instance), whereas with a filled form on screen a non-entry is UNAMBIGUOUSLY a decision. The form makes the decision LEGIBLE — an information GAIN.** **BINDING REQUIREMENT: the DECLINE must be capturable** (+reason), else 'no click' silently re-becomes 'never saw it'. **(b) Stage 1 shaped as an EXECUTION-PARITY LEDGER** — per fire: framework-computed order + its derivation inputs, operator actual action (click/decline+reason/manual), per-field delta, end-to-end linkage latch→order→fill→trade→shadow twin, order-validity outcome (the FTRE rejection class surfaces free); read monthly as agreement rate + the R cost of differences. **(c) SIX derivation constraints for 21-A:** FREEZE the latch at fire time (live trap — FTRE's pivot drifted 18.34→18.59→20.19 while armed; VSTS's stop 12.90→13.40); horizon in SESSIONS not calendar (the 19-E precedent); per-FIRE identity (VSTS has fired twice); latch-specific fill detection; clear-reason RECORDED not inferred; invalidation on CLOSES. Stages 2–3 stay behind the operator-signed L2 endpoint diff — and stage 1's ledger is what EARNS stage 2.
- **2026-07-27 (cont.) — BINDING REQUIREMENT AMENDED (operator-sharpened): latch action is a THREE-state space; NO-ACTION must NOT default to 'away'.** The operator identified the state I collapsed: accept / decline(+reason) / **no action** — and that no-action's 'I was away' implication itself needs post-facto verification. **Why it is load-bearing:** no-action is either (i) AWAY/never-saw = mechanical noise → EXCLUDE from the discipline signal, or (ii) SAW-IT-AND-DIDN'T-DECIDE = decision variance → a discipline DATUM (arguably a lapse under the epoch contract). Defaulting all no-action to 'away' LAUNDERS indecision into noise and biases the operator-discipline measurement OPTIMISTIC — the same failure class the T4 read guarded against (every bias pointed one way; I refused the flattering number). Same discipline, subject = the operator. **THE LOOP-CLOSER (posted `20260727T165400Z`, both parts small):** (1) objective VIEW TELEMETRY — was the latch surface viewed while armed (a panel-load timestamp; mechanical, no self-report — matters because FTRE proved recollection is imperfect in exactly this zone); (2) ATTESTATION prompt at terminal state ONLY for the ambiguous cell (viewed=YES, action=NONE) — never when telemetry already resolves it, since needless prompts train dismissal. **The load-bearing DEFAULT: an unattested viewed-but-no-action classifies as a DISCIPLINE LAPSE, not away — pessimistic toward the operator, because an honest instrument does not flatter its subject.** **Strategic bonus:** classifying away-fires makes the AWAY RATE measurable (N fires, how many lost to unavailability, what they cost in R) — **that is the quantified business case for stage 3 auto-place**, which today rests on one anecdote (FTRE +1.22R). Stage 1's ledger will justify or KILL the auto-place ambition on data. **Retro-validation acceptance test for 21-B: FTRE (vacation) must classify AWAY, be excluded from the discipline signal, and its +1.22R land in the away bucket — not scored against his judgment.**
- **2026-07-27 (cont.) — CHARC's three engineering findings RULED; one of them caught ME violating my own freeze-at-fire constraint in the same session I issued it.** **F1 VERIFIED on disk myself:** FTRE's fire (evaluation_run 121) carries pivot 18.34 / stop **14.88** frozen while later rows drift (15.19 → 15.25 → 16.51; pivot walks to 20.19); VSTS's two fires are separate rows by construction (run 99 = 13.56/11.62, run 126 = 16.90/13.40 — per-fire identity is FREE); `pattern_detection_events` carries NO pivot/stop → **the derivation keys on CANDIDATES, not detections** (CHARC's correction to my demand statement, accepted verbatim). **MY ERROR, operationally live: I told the operator FTRE's invalidation level was 16.51 — a DRIFTED value. The fire-time level is 14.88.** Conclusion unaffected (price ~19.5 far above both) but the number was ~11% wrong and would have cancelled his latch ~11% early. Corrected to him directly. The constraint validated against its own author. **F2 OWNED:** my 'view telemetry is essentially free' claim was wrong — writing on a GET is a seam the web layer lacks. Third mechanism-assertion-without-tracing this phase-pair (pruner → chain-grain → write-on-GET). **F3 (the sequencing trap — CHARC's question to me) RULED: FOLD minimal telemetry INTO 21-A.** Gap fires would be the instrument's FIRST observations (the ledger's baseline anchor); unclassifiable is NOT neutral but LOST under my do-not-flatter rule; ~weekly cadence = 1–3 permanently blind fires vs one small table. Noted a cost-share: the 0032 taxonomy migration is already a Phase-21 rider — if schema opens anyway the view-log rides at near-zero marginal cost. Fallback accepted if the operator wants 21-A no-schema: stamp PRE-TELEMETRY, exclude honestly, never default. **F4 linkage: CONCUR** — CHARC caught my ledger item 4 colliding with my OWN Option-C ruling (no durable link to a prunable artifact); their fix (store fire identity; pure-recompute means the current artifact always holds the twin) is right. **REFINEMENT: store BOTH identities** — (evaluation_run_id, ticker) freezes the values, but the artifact keys on (ticker, detection_date) and those are DIFFERENT id spaces; storing only one makes the shadow join derivable-by-convention rather than exact. Cheap now, unrecoverable later.
- **2026-07-27 (cont.) — CONSTRAINT-3 RULING: my constraints 1 and 3 CONFLICTED and the orchestrator was right to refuse to rule it.** A literal (evaluation_run_id, ticker) latch identity forks re-fires into separate latches, which RE-FREEZES the pivot at the newer row — the FTRE failure mode reintroduced inside the fix for FTRE. **Corpus verified myself and it is cleaner than briefed: 11 A+ fires / 6 tickers, and 3 of the 4 re-fires are SAME-ACTION-SESSION DUPLICATE RUNS** (SLDB 9+10 both 04-22; YOU 31+32 both 05-04; NVCR 94+95 both 06-18), plus one later-session re-confirm (SLDB 12, 04-24, IDENTICAL 8.87/6.40) and one post-clear new latch (VSTS 126, +27 runs, DRIFTED +3.34/+1.78). Every armed-window re-fire is value-identical; the only drift crossed a clear — supportive but n=4, so the rule handles drift-while-armed explicitly anyway. **THE THREE-WAY RULE:** (1) same-session duplicates COLLAPSE unconditionally (dedup at (ticker, action_session_date) BEFORE latch logic; the latch arms at the FIRST such row and freezes ITS values; horizon + telemetry window count from that arm; a same-session value change is a data-quality signal to surface, not to resolve silently); (2) later-session re-fire while ARMED = RE-CONFIRMATION — no new latch, **NO re-freeze** (symmetry with the settled semantics: if a lapse never CLEARS the latch, a re-confirm must not FORK it); (3) re-fire after CLEAR = a genuinely NEW latch (VSTS 99 → filled by trade 17 at 13.61 → cleared → 126 validates end-to-end). **PLUS:** record re-confirmations as an attribute (count + dates — plausibly a strength signal, testable later; must not fork, must not vanish); and on drift-while-armed, **never silently re-freeze and never silently hide** — surface it as an operator decision point (a flag, not a workflow, for a case never yet observed). **THE DECISIVE MEASUREMENT ARGUMENT (my lane, not style): the ledger's unit is the LATCH (opportunity), not the fire** — three SLDB rows would triple-count one opportunity and inflate every denominator including the AWAY RATE, the number that will justify or kill stage-3 auto-place. Fixture set specified: SLDB 9+10 + SLDB 12 + VSTS 99→126 (all three classes) alongside the FTRE freeze-at-fire fixture. **Linkage edge case flagged:** the latch corpus (candidates, from 2026-04) is BROADER than the shadow corpus (temporal log, from ~2026-06-05) — the April/May fires have NO shadow twin ever; a ledger row must tolerate a null twin.
- **2026-07-27 (cont.) — 21-A PLAN-STAGE GATE: PASS, five rulings; the plan caught me contradicting MYSELF and refused to resolve it (correct).** **G.1 — the conflict was real:** my 07-23 rd-state line ('a re-fire would open a NEW latch at the new pivot') vs today's ruling ('re-fire while armed = re-confirmation'). **Resolution: the plan's zone-escape DIAGNOSIS is right, but the fix is NOT a fourth clear condition.** Both my statements were partly right: 07-23 correctly saw FTRE's mandate had become UNACTIONABLE but mis-attributed the mechanism to re-firing; today correctly handled identical-value re-fires but over-generalized to all of them. **THE RULE: re-fire while armed → SAME pivot = re-confirmation (no fork, no re-freeze; SLDB 12); DIFFERENT pivot = the old latch CLEARS as SUPERSEDED and a new latch arms at the new frozen values.** The hazard in constraint 1 was never re-freezing per se — it was re-freezing SILENTLY under a resting order; a supersede is visible, and the panel must loudly show a stale resting order no longer matches. **No fourth clear condition: zone escape is a STATE ATTRIBUTE** ('ARMED — OUT OF ZONE'), because the resting order stays valid and fills on a pullback (clearing would desync from a live order — what tier-1 exists to prevent), and 'price won't return' is unknowable. **G.2 — MY ERROR, and a CIRCULAR one:** I invented '~20 sessions', recorded FTRE's horizon 08-17 from it, and the plan back-solved 20 from my own date citing my doc — my unverified assertion became its own evidence. Traced now: engine `HORIZON_SESSIONS=126` (trade-walk), entry search bounded by `observe_max_pending_window_sessions=30` → **the shadow's effective ENTRY window is 30; RULING: latch horizon = 30, BOUND AT THE SOURCE to the config value** (at 20, sessions 21–30 are a manufactured live-vs-shadow divergence window — the exact defect the arc kills). **FTRE's horizon moves 2026-08-17 → 2026-08-31.** Fourth tracing-beats-my-assertion this phase-pair. **G.3 dual session anchor CONFIRMED** (forward for horizon / backward for invalidation, both from ONE clock read — the strongest design in the plan). **G.4 fill > invalidation > horizon CONFIRMED** (facts beat signals beat deadlines). **G.5 base-break deferral CONFIRMED** with a labeling condition (must not present as full invalidation). **H.2 addition for 21-B: the away-rate must not be consumed without a telemetry-health check** — a silently broken beacon makes every fire look 'away', inflating the very number that justifies stage-3 auto-place.
- **2026-07-28 — 21-D COMPLETE (merged `57e4e797`, witnessed 3/3, live migration executed: 67 files relocated sha256-matched, ZERO deleted; the singular orchestrator inbox is LIVE and carrying director traffic).** RD no-action CONFIRMED by my own grep of all three RD docs (zero retired colon-sid occurrences). Note: the migration ADOPTED rather than archived the live gen's mail — the two rulings that the superseded five-tree list would have filed into `_archive` were mine. **21-A EXECUTING RETURNED** (23 commits, 9376/5/0 pre-rebase, 16 review-strong rounds never tiering down, 29 findings / 7 CRITICAL / 22 MAJOR / 28 fixed; the dominant class — NINE findings — was the FALSE ALL-CLEAR, i.e. the panel claiming coverage over an uncovered mandate: precisely the failure the arc exists to kill). Verified on disk by the orchestrator: `LATCH_CLEAR_REASONS` = fill/invalidation/horizon/superseded with **NO zone reason** — my zone-escape-is-an-attribute ruling honored STRUCTURALLY; the horizon DERIVED from `cfg.pipeline.observe_max_pending_window_sessions` per my G.2 ruling; models importing `LATCH_STATES` rather than re-declaring (multi-mirror drift impossible by construction). **MY THREE ITEMS ANSWERED (`20260728T061649Z`):** (1) the A.9 deviation ENDORSED — attributing an unattributable order to the most-recently-cleared latch was a FALSE STATEMENT; the truthful warning still fires the stale-order-hazard alarm I named 07-23. (2) Mandate-shape checking ENDORSED (FTRE was lost precisely BECAUSE the order was not GTC — shape-checking is the arc detecting its own founding failure) **WITH A REQUIRED WIDENING: the accepted set must contain BOTH GTC STOP_LIMIT (price below pivot — the breakout form) AND GTC LIMIT at the cap (price at/above pivot — the pullback form)**, because on 07-23 FTRE's buy-stop-limit was BROKER-REJECTED for exactly this reason; a STOP_LIMIT-only check would fire a FALSE MISMATCH on the arc's own first live subject. (3) Last-close-not-live-quote ACCEPTED — and a smaller compromise than it looks, since **invalidation is close-based by my own ruling, so last-close is the CORRECT input to the state machine**; only the intraday actionability hint lags. Non-binding note offered on CHARC's GET-write escalation (an instrument that mutates the substrate it measures is a category error; the cache is shared with charts/ATR/PriceFetcher — but the carve-out is his call). Supported making codex-auto-review standing (it found the 10-hour HST skew that would have fired a FALSE armed-no-order alarm — living in the interaction between changed and unchanged code). **DISCIPLINE BANKED, AIMED AT ME: 'THE DOC IS A LEAD, THE CODE IS THE EVIDENCE.'** Two doc-sourced premises overturned by live-code checks in one dispatch, both mine (FTRE's drifted 16.51 vs frozen 14.88; the horizon, where my untraced assertion became its own citation). Honest pattern across the phase-pair: **reliable on SEMANTICS, unreliable on REMEMBERED CONSTANTS** — any figure I state that a plan will build on must be TRACED at the moment I state it. Four instances is a pattern, not an accident.
- **2026-07-28 (cont.) — TWO RULINGS + the 21-A operator witness PASSED both states.** **(1) The multi-order FALSE ALL-CLEAR** (two GTC STOP_LIMITs, same stop, different caps → the panel prints AGREE over a real wrong-cap order): I rejected BOTH offered options and ruled a THIRD — **not a merge block, but not shipped-documented either.** Separate the expensive fix (per-order leg reporting = a reporting-model change → BANK as V2) from the cheap one (**do not make an affirmative claim the inspection cannot support**): when >1 order matches a latch, SUPPRESS the affirmative agree and state the multiplicity ("2 orders match; 1 shown — verify the others"). The join already knows the matched-set size. Converts a false all-clear into an honest partial. **Rarity argument REJECTED**: the geometry is rare under the CURRENT manual workflow, and 21-B is explicitly about making order placement routine — multi-order-on-one-latch becomes MORE likely, not less (sizing a defect against a workflow the next arc will change is the n=2-threshold error again). **Consistency is decisive for the channel**: nine instances of this class blocked during the chain; if the tenth ships because it was found late, AGREE means 'agree unless you're in the documented exception' — the Phase-19 drumbeat-false-RED erosion. An alarm channel is worth what its WORST known case is worth. **(2) 21-F dashboard surfacing vs my away-rate telemetry** (a naive dashboard = he sees latch state without visiting the panel → telemetry says never-viewed → away-rate inflates OPTIMISTIC → biases toward auto-place; a SECOND independent path to the H.1b corruption, but working as designed): **the operator's framing is RIGHT** ('the panel should come to me') — FTRE was an ATTENTION failure and a navigate-to surface is still attention-dependent. **Option (c) REJECTED outright** — crippling the surface that addresses the founding failure to protect a metric is the tail wagging the dog; the away rate serves the trading program, not the reverse. **RULED option (b) with a sharpening: record WHICH LATCHES were rendered with actionable detail, plus the surface, plus the timestamp** — because the question isn't 'did he open the panel' but 'was THIS MANDATE visible during its armed window'. Three properties: doesn't constrain CHARC's surface architecture at all; DEFERS the glance-vs-panel equivalence question instead of deciding it at n=0 (record both, analyze at read time); keeps the pessimistic default intact. **Sequencing requirement (same trap as 21-A): the telemetry contract must settle BEFORE any dashboard surface ships** — those fires wouldn't be merely unclassifiable but CONFIDENTLY WRONG optimistic, which is worse than unknown. Flagged (not blocking) that adding a `surface` column before 0032 lands is the cheapest it will ever be. **Also ruled: RENDER the undeterminable-regime state** (R14 precedent — an unlabelled reduction in what the panel asserts is a quiet all-clear by omission, same family as ruling 1); the dropped-off-screen inert check must be VISIBLY inert. Noted with approval: the witness ran against a v32-migrated SNAPSHOT because migrating live would have hard-failed tonight's 17:30 scheduled pipeline on the exact-match schema guard — the 20-A Half-B discipline applied unprompted.
- **2026-07-28 (cont.) — CHARC's four rulings answered: one CONCESSION, one boundary accepted, one measurement requirement added.** **CONCEDED ruling 3 (schema timing):** I recommended adding the `surface` column before 0032 lands as "the cheapest moment"; CHARC priced a step I missed — **21-B carries its OWN migration** (A1 answered one-table-in-the-negative), which sits between 0032 and any 21-F work, so the column rides there at zero marginal cost AND with a real consumer rather than landing today constant-valued with no reader. Plus the risk argument: 21-A has converged after 28 rounds and already produced THREE self-inflicted interaction defects — exactly the profile where a late non-blocking optimization is a bad trade. Withdrawn; ship 0032 as reviewed. **Second time this phase a CHARC ruling beat my design by pricing a neighboring step I hadn't traced** (Option-C pruner was the first) — same pattern as the constants problem: I reason well about what SHOULD be true, less well about what the surrounding machinery ALREADY does. **ACCEPTED the withhold-only boundary on ruling 1** — it states my intent more precisely than I did: withholding an unsupported claim changes what the VM CLAIMS; per-order legs change what the VM IS. First is a condition, second is a model. **ADDED to 2b (no broker call on the dashboard — concurred, the hard-dependency argument is decisive): A CACHED-ORDER-DERIVED ALL-CLEAR MUST NOT BE ASSERTED.** The error directions are asymmetric — cache says no-order-but-one-exists = a FALSE ALARM (safe, self-correcting); cache says order-exists-but-cancelled = a FALSE ALL-CLEAR, the arc's dominant defect class reached through staleness instead of partial inspection. Alarms from cache are fine; coverage claims are not. **Traced a decomposition that unblocks the design: the latch MANDATE state needs NO broker call** (pivot/stop from candidates, invalidation + zone from OHLCV closes, fill detection from local trades/fills — all local); only the order-agreement half touches Schwab. So a dashboard can render full mandate detail with zero broker dependency, and **my telemetry requirement is unaffected by however the order-cache question resolves.**
- **2026-07-28 (cont.) — 21-A pre-merge: boundary VERIFIED, two answers, merge QA held for the rebase.** **Withhold-only boundary held on disk (my own verification, not the report):** `matched_order_count` is a plain int (models.py:279 / orders.py:286), the VM branches `>1` (latches.py:732), NOTHING iterates the matched set — CHARC's line held exactly, and the multiplicity guard was a count-plus-string as I predicted. **JUDGMENT CALL (the label also withholds the PAGE-LEVEL all-clear): principle CONFIRMED, implementation OVERSHOOTS.** A page-level 'no alarms' is a completeness claim and isn't supportable when a per-latch check didn't run — same as my ruling 1 one level up, so do NOT revert to an unconditional all-clear. But blanket withholding costs the sentence its meaning given the frequency; **the consistent answer is ruling 1's own move — SCOPE the claim** ('No alarms among the N checked. M not checked — see labels'), same count-plus-string shape. If that's genuinely more work, ship the withholding and bank the scoped form — **honest silence over informative falsehood, but both are cheap here.** **FREQUENCY FACT — the mechanism is worse than 'materially more often':** the regime needs a derivation-session close, and before the nightly run TODAY's bar isn't in the archive yet → **the check is inert for EVERY latch in the pre-run evening window, which IS the operator's review window** (US close is mid-morning HST; the scheduled run is 17:30). It may be the DEFAULT state in his actual usage. **Two consequences ruled:** (1) WORDING — a label appearing on every latch every evening must NOT read as a warning, or it trains the exact dismissal reflex that made the Phase-19 drumbeat false-RED expensive, on the surface whose whole value is that its alarms mean something; and **the two reasons want DIFFERENT words** (pre-run = 'pending tonight's data', self-resolving; off-screen = will NOT self-resolve) — same state machine-wise, completely different operationally, which is where I push back on the one-label consolidation; (2) BANKED V2 applying this morning's cached-order asymmetry to PRICES: **from a stale close you may raise a MISMATCH alarm but may not assert a MATCH** — lets the evening window produce useful alarms while never producing a false all-clear. **Main went red overnight (pre-existing, a hard-coded date crossing a 90-day retention boundary with no commit behind it):** nothing owed by me; noted for CHARC's register that this is the THIRD instance this phase-pair of one class — **a test asserting against AMBIENT state the repo doesn't control** (D21 hard-coded list vs disk; 19-B session assertion over a live shared tree; now a hard-coded date vs the live clock) — a sweep framed that way would find more than a clock-specific one. **Merge QA deliberately HELD for the post-rebase merged head** — 9409 is a pre-rebase number and this arc's own history (three self-inflicted interaction defects) is exactly why I want the number that will actually be on main.
- **2026-07-28 (cont.) — 21-A MERGE-BLOCKING QA: PASS, CLEARED (`20260728T152634Z`).** Verified myself on the rebased head `3414a26f` (not from the report): 36 commits all trailer-empty (re-audited POST-rebase, correctly — a rebase rewrites SHAs); scope 22 files +3727/-2; migration 0032 present with the STRICT backup gate (`target_version < 32 or current_version != 31`). **SIX OF MY RULINGS ARE ENFORCED STRUCTURALLY RATHER THAN BY COMMENT** — the property that survives a future refactor by someone who never read the thread: `LATCH_CLEAR_REASONS` = exactly {fill, invalidation, horizon, superseded} so **no zone reason EXISTS to be misused**; `latch_horizon_sessions(cfg)` carries my bind-at-the-source rationale verbatim + a drift-pin test against `PipelineConfig` (#11 discipline); the fire enumeration selects `bucket='aplus'` with BOTH id spaces attached (finding-4); the two-form `MANDATE_ORDER_TYPES` frozenset (my widening); the withhold-only boundary (count-only, nothing iterates). **Outstanding yes/no CONFIRMED DECISIVELY: ship the withholding, do not revert** — scoped is the better end state but the withholding is HONEST, and a 36-commit arc with three self-inflicted interaction defects is not where I spend another cycle on a string; scoped form banks to 21-B. **Label wording banked, NOT a gate**, with one recommendation: a strings-only touch-up before 21-B is worth it if trivially safe, because **the dismissal-training clock starts at merge, not at 21-B** — the label is the DEFAULT state in the operator's evening window. **Explicitly disclaimed urgency:** merge + the live v31→v32 migration are ONE ATOMIC operator step (exact-match schema guard → main expecting v32 with a v31 live DB hard-fails the 17:30 pipeline); both are v31 and consistent right now, so if margin before tonight's run is short, HOLD — a failed scheduled run costs a real observation night and nothing in the arc expires. Endorsed the orchestrator's recommendation rather than letting a clearance read as pressure. Noted for CHARC's D5: 9436 tests at 4:37 vs the close audit's 9115 at 5:14 (more tests, less time, same box) — one sample isn't a trend, but a paydown arc shouldn't be scoped off a possibly non-reproducible tripwire number without a confirmation run.
- **2026-07-28 (cont.) — clearance HELD (correct); the inert-window mechanism MEASURED and it is worse than I framed; the banked V2 RE-PRIORITIZED.** The orchestrator probed `action_session_for_run` across the operator's local day and bisected the trigger: **it is the ACTION-SESSION ROLLOVER (10:00–10:30 HST), which COINCIDES with the US close rather than being caused by it — so the mandate-form check is inert for EVERY latch ~7 HOURS OF EVERY TRADING DAY (10:30→17:30), the entire post-close period.** My 'pre-run evening window' framing was directionally right and materially understated. **Noted about HOW that was obtained: this time it wasn't a stale doc standing in for evidence — it was MY OWN INFERENCE standing in for a measurement. Same failure shape, one step earlier.** **THE SUBSTANTIVE CONSEQUENCE (beyond wording): the inert window sits exactly where a REGIME CHANGE first becomes detectable** — the correct order shape depends on price vs the latched pivot, FTRE's shape had to change *because* price crossed, a crossing happens during market hours and is first observable after the close. So the check is absent in the period when the thing it detects most likely just happened, and present mainly overnight when nothing has changed. **That upgrades the banked V2 (from a stale close: raise a MISMATCH alarm, never assert a MATCH) from a refinement to a 21-B PRIORITY** — it would recover most of the check's value in exactly the hours it currently has none, under a rule already ruled for cached order state, so no new principle. **Explicitly NOT for 21-A** (at the merge gate with two changes in flight; I'm not adding a third). Clearance held pending the post-change merged-head number — clearing against a soon-stale 9436 would spend the pass on a state that never reaches main, which is why I asked for the post-rebase number to begin with. **CHARC's D5 RETRACTION CONFIRMED by measurement** (9140@4:33 vs the firing 9115@5:14 — matched-size pair 41s apart ⇒ a ~15% slow loaded box, not a threshold crossing; D5 stays WATCH, no paydown arc). Their consistency distinction affirmed as sound: the 0032 column was SPECULATIVE with a free deferral path; the wording split is a LIVE operator-facing defect accruing from merge with none — and their general test (a count-plus-a-string over an existing rendering path vs a MODEL change) is the right one.
- **2026-07-28 (cont.) — I REVERSED the ship-the-withholding ruling; the scoped all-clear goes into the in-flight dispatch.** The orchestrator flagged a TIMING CROSSOVER that would otherwise have passed silently: my ruling posted 15:26, their 7-hour frequency measurement 15:27, CHARC's opposite conclusion 15:29 — **so I ruled without the number and CHARC ruled with it.** Both terms of my stated rationale were falsified: **VALUE moved UP** (I argued for the scoped form because an all-clear usually absent stops being informative when absent — believing suppression was occasional; at 10:30–17:30 HST inert it is suppressed during essentially ALL of the operator's review window, so he would almost never see it: my original argument at full strength), and **COST moved DOWN** (the cycle I declined to spend was ALREADY in flight — the wording dispatch was open and had the scoped all-clear in it before being countermanded to honor me; adding a string to an open dispatch is not another cycle). When both sides of a trade move against the conclusion, the conclusion changes; holding would have been stubbornness. Same escape hatch retained (CHARC's risk test: count+string OK, model change STOPS and routes to 21-B). **PROCESS DISCIPLINE BANKED — second instance today** (the horizon was the first, where my untraced figure became a plan input before the code was checked): **when I rule on a cost-vs-value trade, the ruling is PROVISIONAL ON ITS PREMISES, and a load-bearing number arriving later RE-OPENS it rather than being inherited** — and the practical form falls on the reporter as much as me: flag the crossover rather than assuming the earlier ruling priced the later fact. The orchestrator caught this; I would not have. Recorded that CHARC's conclusion was RIGHT and mine STALE — we ruled on different evidence, not in disagreement.
- **2026-07-28 (cont.) — the crossing cascade resolved CONVERGENTLY, and it produced a durable comms convention.** CHARC's correction (15:36:15) and mine (15:36:23) crossed EACH OTHER by 8 seconds and said the identical thing (include the scoped all-clear) — no third position, thread closed, orchestrator unblocked. **The symmetry is the record-worthy part: CHARC wrote a message ABOUT a crossover and it crossed; I ruled on a cost-value trade and the number landed a minute later. Same failure, both directions, ninety minutes apart, each caught by the OTHER rather than by the author.** That is the peer net working — at a cost of three rounds on one string, which is what motivated the fix. **MY PROPOSAL ADOPTED BY CHARC AS COMMS CONVENTION: a message that SUPERSEDES an earlier position says so in the SUBJECT** (a reader deciding whether they are current must be able to tell from the inbox listing without opening and diffing two messages). **And I banked CHARC's sharpening of my own discipline, which is better than my formulation: a claim about what ANOTHER ROLE has already decided goes stale faster than a claim about code, and must be checked at POST time rather than READ time** — I stated the general rule (a conclusion is provisional on its premises); CHARC identified which premises decay fastest. Code sits still while you write about it; a live director thread does not.
- **2026-07-28 (cont.) — the orchestrator DIAGNOSED the crossing cascade better than either director, and their ask is now binding on me.** Both CHARC and I landed on 'cadence artifact'; the orchestrator identified the actual root: **the premise that mattered was DISPATCH STATE, which neither director can observe and only the orchestrator can supply** (they countermanded at 15:32:47; my message was 15:32:04 — I could not have known). My supersedes convention fixes stale POSITIONS and would NOT have prevented this, because the state in question was in nobody's message until they posted it. **Their structural commitment:** broadcast countermand/narrow/re-instruct as its own immediate status, so dispatch state is READ not INFERRED. **Their ask, ACCEPTED AS BINDING: when a ruling depends on what is in flight, rule against the orchestrator's LAST STATE STATEMENT, never another director's message; if unclear, ASK; and NAME the state statement being ruled against so a stale premise is visible in my own text.** **THE UNIFICATION — and it is why I adopted rather than merely agreed: all three disciplines this day produced are the same rule against different premise-owners.** A FACT is sourced from code, not a doc (the invented horizon, the drifted stop). A POSITION is sourced from the role that holds it, at post time (the crossings). DISPATCH STATE is sourced from the orchestrator (this). **General form: SOURCE EVERY PREMISE FROM THE ROLE THAT OWNS IT** — every miss I made today was sourcing a premise from something other than its owner. Also noted: the orchestrator is carrying the stale-close V2 re-prioritization into 21-B scoping as a COMMITTED artifact rather than leaving it in the mailbox, on the principle that a ruling existing only in a message is one that gets rediscovered later — the same mailbox-is-transport-not-tracker rule from harness-architecture §3.
- **2026-07-28 (cont.) — 21-A late-change return: both items ruled.** **ITEM 1 — the wording deviation: MY string was the inaccurate one.** codex-auto-review flagged 'N checked / M not checked' as over-claiming the reduction: the alarms, cap-leg, GTC-duration and stray-order sweep ALL RAN on those latches — only the two-form SELECTION was skipped. Shipped as 'form-checked'. **Worth naming since I've policed the opposite direction all week: 'the panel may say only what the data supports' cuts BOTH ways — saying LESS than the data supports is also a false statement about what was inspected.** **AND ITEM 1 REVEALED MY SCOPED CONSTRUCTION WAS BUILT ON A MISUNDERSTANDING:** if only form-selection is skipped, the alarm all-clear is COMPLETE, not scoped — the correct form is TWO separate claims ('No alarms.' full + 'Mandate-form check pending for N latches.'), which is simpler, better English, and **dissolves the vacuous zero-case** ('no alarms among the 0 latches form-checked') that would render through the whole 7-hour window. **NOT reopening** — shipped form is literally true with clarifying labels, code is landed+QA'd; banked for 21-B. **Consistency made visible:** I reversed on nearly this question 3 hours earlier because the COST had dropped (open dispatch, string nearly free); here the dispatch is closed so the cost has NOT dropped and honest-but-clumsy ships. Same standard, different circumstances, opposite answer. **ITEM 2 — the regime-gate provenance gap, VERIFIED MYSELF on disk:** `orchestration.py:231` sets `data_asof = max(max_dates)` (COHORT-MAX, persisted at :309) while `evaluator.py:56` takes `last_close = closes.iloc[-1]` (that TICKER'S OWN last bar, stored :94) → a lagging ticker is persisted with an older close under a fresher run-level stamp. **This is the item I banked earlier as a 'smaller flag' (staleness admission) — now UPGRADED to a FALSE-ALL-CLEAR PATH** (the shape check can bless the wrong order form off a stale price): the arc's dominant defect class reached through PROVENANCE rather than partial inspection. **21-B must inherit the escalated framing, not the mild one.** Agreed on all three orchestrator calls (don't fix here = measurement-chain/model change; the label-neutralizing partial move was right; scope it TOGETHER with the stale-close asymmetry — two routes to one failure, and ONE rule resolves both: **from a run-level stamp, as from a stale close, raise a MISMATCH alarm but never assert a MATCH**). **TWO ADDITIONS: (a) SURVEY the other `data_asof_date` consumers at 21-B scoping** — the panel is just the first to lean on it as close-provenance; I'm not asserting it's a class, I'm refusing to assume it isn't; **(b) the real fix (per-ticker close provenance at write time) is the SAME SHAPE as freeze-at-fire** — a run-level stamp standing in for a per-row fact; the arc has met this shape twice and the recognition is the transferable part. Third consecutive arc where codex-auto-review found the highest-severity item, this time after six review-strong rounds closed on 'Findings: none'.
- **2026-07-28 (cont.) — CHARC elevated a CODE-GOTCHA CANDIDATE from my addition (b) and assigned the authoring correctly (orchestrator's lane per §2.6, CHARC flags FORM only): 'A RUN-LEVEL STAMP STANDING IN FOR A PER-ROW FACT', met TWICE in one arc.** **My recommendation: CATALOG NOW, do not wait for the 21-B survey** — they answer different questions (the survey scopes REMEDIATION of existing instances; the gotcha is PREVENTION of the next one, and two independent instances already establish a recognizable shape). Project precedent supports it — gotchas here bank on first/second occurrence with forensic detail, not after a census (the trailing-NaN one from a single run; write-barrier-breaks-seeding-tests from a single cross-arc interaction). Let the survey UPDATE the entry's scope, not gate it. **Substance supplied for the orchestrator to shape:** TRIGGER = reading a per-row value alongside a run/batch-level timestamp and treating that stamp as provenance for the row; WHY = the stamp is an AGGREGATE (`max(max_dates)`), so any row lagging the batch max is stamped fresher than it is — nothing in the write path guarantees the coupling, the reader assumes it; INSTANCES = (1) freeze-at-fire (the latch must read the FIRE's candidates row: FTRE pivot 18.34@run121 → 20.19@run125, stop 14.88 → 16.51) and (2) `data_asof` cohort-max vs per-ticker `last_close`; FIX = the stamp is not provenance unless the writer guarantees it per-row — carry per-row provenance at write time, or treat the stamp as an UPPER BOUND from which you may raise a MISMATCH alarm but never assert a MATCH (**the fix-half is the SAME asymmetry already ruled for stale closes and cached order state — which is itself evidence the shape is real rather than two coincidences**). **One bound on my own framing so the entry doesn't over-claim: both instances are TIMESTAMP-shaped; the broader 'any batch-level attribute standing in for a per-element fact' is plausible but I won't assert it — anchor on the concrete case, widen on a third instance.** CHARC's closing framing accepted and better than mine: *holding a position when the facts change is stubbornness; changing one when they have not is indecision* — I demonstrated both directions correctly in one afternoon (reversed on the scoped all-clear when cost dropped; declined to reverse on the same question when it hadn't).
- **2026-07-28/29 — ARC 21-A MERGED (`1955aa59`) + LIVE MIGRATION v31→v32 EXECUTED (first migration in three phases; strict backup gate fired on the pre_version==target-1 clause; post-merge suite 9450/7/0 on the merged main head; production verified: GET /latches renders FTRE at pivot 18.34 / invalidation 14.88 / cap 18.89 / horizon 2026-08-31, VSTS present, dashboard 200).** **The orchestrator's closing observation, accepted and recorded as stated: TWO of those four live figures are ones I had WRONG when the arc opened** — 14.88 vs the drifted 16.51 I quoted the operator on 07-23 (would have cancelled his mandate ~11% early), and 2026-08-31 vs the 08-17 I recorded from an untraced 20-session horizon that then became its own citation in a plan. **Both corrected by TRACING rather than argument, and both now held by a panel rather than by my memory — the arc's thesis demonstrated on its own author.** **PROCESS NOTE raised once (not a grievance, no action asked): my merge clearance was HELD pending the post-change number and the merge landed in the same message that reported it.** Outcome correct (9450 green; my substantive QA passed at 15:26; I'd have cleared on sight) — **but I spent this week ruling that a known-benign exception is how a rule erodes (the multiplicity guard vs shipping a tenth false all-clear with a footnote), and I'd be inconsistent to apply that to a panel string and not my own gate. A merge-blocking gate compressed because it would have passed is advice with a ceremonial step.** Fix = the orchestrator's own dispatch-state remedy pointed at gates: the operator may absolutely authorize over a held director gate (his authority, undisputed), but it must be READABLE — the merge report says 'operator authorized with RD's clearance still held' so the record shows a deliberate override, not an omission. **WATCH ITEM (mine): tonight's 17:30 run is the first production exercise of v32 with merged code** — check the schema-version guard passes, research-health stays all-seven-green, the drumbeat artifact lands on schedule. Expecting nothing (rehearsed + integrity-checked); the point is that a first run on a new schema is exactly where an unrehearsed interaction surfaces. **Gotcha #30 banked** with the concrete run-level-stamp anchoring and the forensic archived, per both directors. **Phase-21 state: 21-A and 21-D CLOSED; 21-B next carrying a materially richer scope** (three-state capture + away-rate consumption + the TWO joint priorities + the data_asof consumer survey + the separated all-clear + the wording split + the telemetry surface column on its own migration); 21-F proposed with the order-cache prerequisite; 21-C deferred behind stage-1 evidence + a signed L2 diff; **21-E RETIRED (D5 retracted on CHARC's four-sample measurement).**
- **2026-07-29 — v32 WATCH CLEARED BOTH HALVES + the 21-G plan-stage gate (5 rulings), one of which is a defect in something I wrote.** **v32 watch:** research-health ALL SEVEN GREEN on the migrated DB (58,098 obs, zero post-baseline non-finite; **`invalid_ohlc` still EXACTLY baseline 23 — the 19-D clamp holds across a schema migration**); drumbeat artifact `20260729T033629Z` 0 days old. With the ops half (run 142 scheduled, schema guard passed, completed unattended, FASTER than both predecessors) the first v32 production exercise is clean. **MY VACUOUS ACCEPTANCE TEST — the orchestrator caught it by checking the data rather than accepting my framing:** `latch_view_events` was created by 0032 TODAY with ZERO rows; FTRE fired 07-20, so viewed=NO is true of it for a reason unrelated to operator behavior — **the test would pass identically if he'd been staring at the panel all week**, and it DEFAULTS a gap fire to away, which is exactly what my own 'unclassifiable is LOST not uncertain' rule forbids. **RULED:** (1) **PRE_TELEMETRY is a DISTINCT class, not a flavour of away** (CHARC's own scoping fallback applied — 'the instrument looked and saw nothing' ≠ 'the instrument did not exist'); (2) **partial-coverage asymmetry — a view record in the covered portion ESTABLISHES awareness; its ABSENCE over a partially-dark window establishes NOTHING** (positive evidence dispositive; absence from an uninstrumented period is not evidence of absence) — which also fixes the instability in the right direction: classification moves only toward a POSITIVE fact, never toward a negative inference from a dark period; (3) **FTRE RETIRED as the discriminating test** (a test passing under both hypotheses tests nothing) → seeded-telemetry fixture, **but KEEP a corrected real-data FTRE assertion** (must classify PRE_TELEMETRY, +1.22R not scored against his judgment). **OQ-1 RATIFIED** with a sharper principle than 'system-wide': **alarm from staleness only when it is CHARACTERISABLE (date PROVEN, not stamp-inferred — else the fix re-commits #30 inside itself) AND SELF-LIMITING (clock's fault, ends at the next run — a per-ticker lag may be permanent, and an unbounded alarm on a correct order forever IS the drumbeat class).** **OQ-2 NOT overruled** — the literal reading would flag his correct stopless pullback LIMIT for 7 hrs/day: a false positive by FREQUENCY destroys a channel as reliably as one by logic; my rule PERMITS alarms, doesn't require them — **conditioned on the reduction staying LABELLED** (an unlabelled under-alarm is a silent all-clear). **Item 1 APPROVED as a COMPLETION, not a reversal** — 21-A shipped SYMMETRIC behavior because the asymmetry needed a provenance ladder that didn't exist; the ruling never changed, its implementability did. Re-expressing (not deleting) the test is the right form. **OQ-3 FOLD IN** (a live #30 instance sitting inside the fix for #30). **SURVEY: 3 research-side hits (4/5/7) in MY chain with UNMEASURED consequences — accepted as RD-owned WITH A DEADLINE: read BEFORE the August monthly read, because that read CONSUMES that chain.** Survey answered my question honestly: not one bug, not established a class — neither resolved in either direction, which is what I asked for. **TWO EMPIRICAL FINDINGS: (a) ZERO observed per-ticker close lag over 12 runs → Route B is LATENT STRUCTURAL, not an observed defect; Route A (the 7-hr window) carries the justification — ENDORSED verbatim into 21-B scoping (don't-overstate applied to the arc's own justification, the hardest place to apply it). (b) THE NAMING COLLISION IS THE MOST CONSEQUENTIAL ITEM FOR MY LANE: `evaluation_runs.data_asof_date` is BAR-derived, `pipeline_runs.data_asof_date` is CLOCK-derived — two quantities, one column name, agreeing 25/25 only because nightlies are healthy, diverging exactly when the archive lags. Worse than latent: it LOOKS CORRECT — a reader conflating them isn't careless, the schema tells them they're the same. MY MONITORS READ BOTH** (CALIBRATION C's whole-session-miss keys on the clock-derived value; detection surfaces carry the bar-derived one). Not asserting a defect — not traced — but it is the FIRST thing I check in the three hits.
- **2026-07-29 — weekly glance adjudicated ALL CLEAR mechanically, but the broad-watch drift is now DIAGNOSED and it is read-#2's headline.** Glance: 730 signals, unattributed 0, artifact 0 days old (post-v32, healthy). **T3 line = the standing mechanical nonzero-trigger fire; NO spot-check owed** — the A+ funnel is now closed 2 / excluded 1 / never_triggered 1 / open 1 across all FIVE fires: **VSTS's 07-27 re-fire is attributed but NEVER_TRIGGERED** (price 15.58 vs pivot 16.90 — no R produced, nothing to walk). Priced A+ remains 3 (NVCR −1.0 / AMN 0.0 / FTRE open, mark risen +1.2165 → **+1.6598R**). **THE DIAGNOSTIC (mine, run today): the broad-watch closed mean has moved −0.6482 (n=97, 07-03) → −0.2016 (n=312, 07-29), and BOTH causes are real.** (1) **ETF DILUTION** — the leveraged slice (TNA/URTY/UDOW) is essentially unchanged in its own right (−1.0598 → −0.9613) but fell from **27% → 10%** of the closed cohort, which mechanically lifts the blend. (2) **THE NON-ETF COHORT GENUINELY IMPROVED: −0.4974 (n=71) → −0.1207 (n=282), with the win rate MORE THAN DOUBLING 10.3% → 22.1%.** **THE LIKELY MECHANISM IS THE UNWINDING OF THE SURVIVORSHIP BIAS I MYSELF IDENTIFIED AT READ #1** — I wrote there that the CLOSED cohort is biased NEGATIVE because losers realize −1R fast and leave to the closed set while winners linger open at marks; as the cohort matures and winners close, the closed mean must RISE toward the true value. That is precisely what happened, which means read #1 was conservative in exactly the direction I claimed — but it also means **the true baseline is materially less negative than the number I banked.** **HONEST CONSEQUENCE: the verdict's DIRECTION holds and the frozen positive-branch criterion (mean > 0) is NOT met, so nothing flips — but the margin has thinned by two-thirds, and the NON-ETF sub-cohort at −0.12 is near break-even.** That materially weakens 'the unfiltered watch pool loses money' as a claim; it may be closer to **'the watch pool roughly breaks even, and the leveraged-ETF slice loses'** — which would make the A+-selectivity validation a weaker result than banked, and would sharpen the population-design question I raised at read #1 (should the #23-widened pool contain leveraged ETFs at all). **AND I MUST OWN THE LABEL: I declared T4 CONFIRMED-NEGATIVE, FINAL on 07-05. 'FINAL' was about surviving the ENGINE FIX, not about the cohort being closed — the cohort is a LIVE, GROWING measurement, and a number that moves −0.637 → −0.202 in three weeks is not final in any useful sense. The label oversold stability and read #2 must re-state the verdict at the larger N rather than restate read #1.** NOT adjudicating now — read #2 does it properly (Wilson LBs, unique-name counts, the ETF split as a first-class cut, and the frozen criteria applied to the matured cohort).
- **2026-07-29 — H1 DECISION-CRITERIA AMENDMENT COMMISSIONED (operator-authorized in-session; brief `2150710b` at `docs/h1-criteria-amendment-commissioning-brief.md`; routed to the fresh CHARC gen `20260729T070916Z`).** Origin: the operator asked what 'Wilson lower bound' meant; explaining it surfaced that **H1's frozen win-rate leg requires 11 wins of 20 — a 55% raw win rate — which NO cohort this program has measured approaches** (broad-watch 22.1% @ n=312; A+ shadow closed 0/2; live 2/2 @ n=2, meaningless). **As written the criterion would likely REJECT a genuinely profitable trend system: its failure mode is a FALSE NEGATIVE ON SUCCESS.** The operator then argued mean-R is the primary edge metric and win rate secondary — **correct, and his strongest point was one he understated: win rate is MANIPULABLE BY EXIT POLICY (move the target nearer / stop further and it rises while expectancy falls), so it can move OPPOSITE to edge; mean R prices both legs and cannot be gamed that way.** I steelmanned the floor's real job (outlier-robustness on the mean-R claim at small n — live for us given the VSTS +27R artifact history) and reframed: **they are not competing measures — mean R measures edge, the floor GUARDS the mean-R claim.** He leaned pre-registered note; **I argued against his lean and he changed course**: the decisive argument is that **the split (mean-R pass / win-rate fail) is the MODAL SUCCESS CASE, not a contingency** — a note is right for an edge case, an amendment for the expected path. **I THEN STRESS-TESTED MY OWN PROPOSED GUARD AND REJECTED IT:** leave-one-out FAILS a genuinely profitable fat-tailed trend shape (30% win, +0.45 mean, one 10R winner → LOO −0.05) — reproducing the exact fat-tail penalty that disqualifies the Wilson floor. **Adopted instead: no single trade may contribute ≥50% of gross profit**, which passes every test case correctly. **AMENDED TEXT (operator-approved verbatim): mean R > 0 across the 20 closed labeled trades AND no single trade ≥50% of gross profit; no-winners → fails; win rate + Wilson LB REPORTED as diagnostics alongside median R and top-3 concentration, not gates.** **NON-NEGOTIABLE attached: the ORIGINAL criterion must remain readable in the record** (overwriting in place destroys the evidence of what was pre-registered — the whole point); mechanism is CHARC's, preservation is not. Its OWN migration (not bundled — a governance amendment must be independently auditable), next free number (the 0032-collision lesson). **TIMING is the load-bearing argument: H1 has 2 closed standard-epoch trades and no signal, so the motivation is the criterion's ARITHMETIC, not its outcome — unimpeachable now, indefensible in six months. The window is open precisely because nobody knows the answer.** Stated in the brief deliberately: **this does NOT make H1 statistically conclusive — at n=20 with fat tails no criterion can** (the Wilson floor couldn't either, it just failed differently); levers flagged-not-bundled (larger n, or shadow-cohort corroboration). **One open question left UNRESOLVED on purpose:** the criterion says '20 closed labeled trades' without pinning the cohort; the epoch contract settles it (standard-only; the pre-epoch `by_design` A+ trade must not count) but the text is silent — **flagged, NOT folded in, because he signed a specific text and adding downstream is what preserve-the-quote forbids.**
- **2026-07-29 (cont.) — COHORT clause ADDED to the H1 amendment on the operator's instruction; routed as a supersedes-flagged addendum (`20260729T071538Z`), brief updated in place (`aa122dd1`/`8246d386`).** The 20 are now pinned to **STANDARD-intent trades only** per the training-epoch declaration; pre-epoch `hypothesis_test_by_design` trades are settled tuition and don't count. **VERIFIED LIVE BEFORE WRITING IT IN — it changes a real number: three trades carry the `A+ baseline (aplus)` label — YOU (2026-05-04, `by_design`, PRE-EPOCH → excluded), VSTS (06-25, standard), AMN (07-01, standard). Without the clause a future reader could reasonably count 3/20; with it H1 stands at 2/20.** So the clause prevents settled tuition from being silently re-read as practice — exactly what the epoch declaration exists to forbid. It CITES the epoch contract, does not modify it; scope otherwise unchanged. **Process note worth keeping: this arrived as an ADDENDUM rather than in the original because I flagged the ambiguity and deliberately did NOT fold it in — the operator had signed a specific text, and adding to a signed text downstream is what preserve-the-quote forbids. He read the flag and ruled. That is the convention working as designed, not an oversight: the flag existed so he could make the call, and he made it.** Also fixed a brief internal inconsistency (§4 pointed at an open question §6 had since resolved).
- **2026-07-29 (cont.) — 21-B bucket questions RULED (tasks 5+9 unblocked).** Root cause first: `r_bucket_for`'s trailing `return decision_r` assigned unlisted dispositions **BY DEFAULT to the bucket that scores AGAINST the operator, silently** — two arrived that way and nobody ruled them. **RAISE-on-unlisted + a test asserting every disposition appears exactly once is worth more than either ruling, because the next unlisted disposition is one neither of us has thought of.** **Q1 `pending_live`: EXCLUDED from EVERY rate** (decision, away, discipline) and reported as its own visible category — an unterminated latch is NOT YET AN OBSERVATION; scoring it counts an unfinished observation as finished AND produces a number that moves underneath a monthly read without anyone editing it (the exact instability that retired FTRE as the acceptance case). Rates compute over TERMINAL, CLASSIFIABLE observations only; the ledger self-completes when a latch terminates. **Q2 `attested_was_away`: NEITHER bucket — it gets a THIRD.** Scoring as `decision_r` is clearly wrong (the away bucket exists to separate exactly this); but merging into `away_r` **reintroduces through the ATTESTATION door the flattering path I closed at the DEFAULT door** — a self-report about one's own diligence is biased toward the comfortable explanation, and 'I was away' is more comfortable than 'I saw it and didn't decide'. That would let **the away rate — the number that would justify automating his entries — be inflated by self-report**, the one number I least want softenable. **RULED: own category, excluded from the discipline signal (attestation honoured), and the away rate reported in TWO forms — OBJECTIVE (telemetry-only, GATES the stage-3 decision) and ATTESTED (+self-report, an explicit UPPER BOUND).** Bias direction is safe the other way (a self-flagellating attestation is conservative). **GENERAL PRINCIPLE NAMED (third application): never merge categories differing in EVIDENCE KIND even when outcomes agree.** Endorsed CHARC's lane: the coherence-pass finding (**a review-loop fix must REPLACE superseded text, not ANNOTATE it — annotation leaves the old rule in NORMATIVE voice, the next round reads it as live and files a finding, which is then also fixed by annotation: THE LOOP MANUFACTURES ITS OWN FINDINGS**) is a genuine gotcha, proven by round 21's CRITICAL surviving round 20's fix *because* round 20 annotated. And the no-scope-split call is right: 18 contradictions that were all PRESENTATION with the right answer already stated somewhere = a coherent design inside an unreadable document.


### 2026-07-29 — TENURE CLOSE: the generational handoff (this RD generation, Phases 19–21-partial)

Handing off at context capacity, mid-Phase-21, with nothing owed and nothing load-bearing living only in a mailbox — swept for it. Current state is NOT here: it is in [`docs/rd-state.md`](rd-state.md), overwritten as the fresh generation's boot surface at `edba7de6`. This entry is history only.

**What this tenure actually settled, in the order it will matter to a successor:**

**1. The A+ LATCH POSTURE — the largest doctrinal change, and it came from a live trade going wrong quietly.** The operator was on vacation when FTRE fired A+ on 07-20; his buy-stop-limit never triggered, the bucket flickered, and the setup went on to run +1.66R with no position. The diagnosis was not a bug — it was that the framework had no concept of a *standing mandate*. An A+ fire now LATCHES a GTC entry mandate at the fire's pivot plus a +3% cap, clearing only on fill / invalidation / horizon. Bucket flicker never clears it. Everything downstream — 21-A, 21-B, 21-F, 21-G, the whole Phase-21 shape — descends from that one adopted sentence, and it originated as an operator observation about his own missed trade, not as an engineering finding.

**2. FREEZE AT FIRE TIME, and I violated it within a day of issuing it.** I quoted FTRE's invalidation as 16.51 — the *current* value — when the fire-time value was 14.88, an error that would have cancelled a live mandate about 11% early. CHARC caught it by reading the row on disk. The rule stands (read the FIRE's `candidates` row, never the latest; pivots and stops drift while a latch is armed), and the irony is the useful part: the person who states a freeze rule is not thereby immune to it.

**3. THE STALE-DATA ASYMMETRY, generalized three times.** From a stale close, from cached order state, and from a run-level stamp: **you may raise a MISMATCH ALARM but you may never assert a MATCH.** Alarm permission is bounded to staleness that is *characterisable* (date proven, not stamp-inferred) and *self-limiting* (the clock's fault, ending at the next run) — without both bounds the alarm becomes the drumbeat class it was built to avoid. This rule now carries more Phase-21 weight than any other thing I wrote.

**4. THE THREE-STATE LATCH ACTION, sharpened by the operator against his own interest.** He asked that a decline capture its *reason*, and that no-action NOT be allowed to default to "away" without post-facto verification. That is the instrument refusing to flatter its subject, requested by the subject. The classification ladder (accept / decline+reason / away-attested / discipline-lapse / PRE_TELEMETRY), the exclusion of `pending_live` from every rate, and the objective-vs-attested away rate all descend from it.

**5. THE H1 CRITERIA AMENDMENT — governance, executed in the only clean window.** The frozen criterion required a Wilson-LB > 30% at n=20, which is 11 wins of 20 — a 55% raw win rate no measured cohort here approaches, and which would likely have REJECTED a genuinely profitable trend system. The replacement guard (no single trade ≥50% of gross profit) was chosen by stress test, not intuition: my own first proposal, leave-one-out, was tested and rejected because it fails a real fat-tailed trend shape. The operator argued the metric case independently and correctly — mean-R prices both legs and cannot be gamed by exit policy; win rate can move opposite to edge. He leaned toward a pre-registered note; I argued for a formal amendment; he changed course. The whole thing is only defensible because **H1 stands at 2/20 with no signal** — the window is open *because* nobody knows the answer, and it closes as the data arrives.

**6. T4 SOFTENED, AND I AM RECORDING IT AS A CAUTION AGAINST MY OWN LABEL.** The broad-watch closed mean moved −0.648 (n≈97) → −0.202 (n=312). Direction holds and nothing flips, but the margin thinned by two-thirds and the non-ETF slice is near break-even. The "FINAL" label attached on 07-05 meant *survived the engine fix*, not *cohort closed* — and it oversold stability to anyone reading the word rather than the caveat. Read #2 must RE-STATE the verdict at the larger N.

**7. THE PREMISE-SOURCING RULE** (`harness-architecture.md` §2) and its fifth row, PRESERVE THE QUOTE. Both were written because this generation kept meeting the same failure from different directions: a fact taken from a doc instead of code, a position taken from a stale message instead of the holder, a director's quoted words softened by a downstream editor into something that still *reads* like a quote. The last one is the dangerous one, because the failure is invisible from inside the loop.

**What I got wrong, since a successor learns more from this than from the rulings.** I invented a "~20 session" latch horizon, and it became its own citation — the plan back-solved 20 sessions from a date I had produced from nothing. Traced to `observe_max_pending_window_sessions = 30` and bound at the config source; FTRE's horizon moved to 08-31. I asserted view-telemetry was "essentially free" when writing on a GET is a seam the web layer lacks. I wrote an FTRE acceptance test that could not discriminate, because the table it queried had been created after the fire. I overstated an archive-lag clause and tightened it myself. I recommended a column before the migration that creates it had landed. Every one of these was caught by someone checking the artifact rather than accepting my framing — **the mechanism is the transferable part, not the list.** I am reliable on semantics and unreliable on remembered constants; a successor should assume the same of itself.

**On the working relationship.** The operator overruled me once (he was right), was convinced by me once (on the amendment route), and repeatedly sharpened proposals against his own convenience. CHARC beat my design twice this week by pricing a neighbouring step I had not traced, and I reversed both times. Neither of those is a failure mode to be reduced — it is the harness working. QA the divergence, including your own rulings.

**Open, with deadlines, in `rd-state.md` §5:** the three research-side gotcha-#30 instances inside my own measurement chain (before the August read — that read consumes that chain); D27, the bar-derived-vs-clock-derived `data_asof_date` collision; monthly read #2 in the first trading week of August; and three merge-blocking QAs (H1 amendment, 21-G, 21-B) with the binding 21-G-before-21-B ordering.
