"""Operational monitoring (Phase 18 Arc 18-E+).

Read-only health/stoplight aggregation over EXISTING signals -- distinct from
``diagnostics/`` (one-offs) and ``metrics/`` (dashboard surfaces). Houses the
tool-health monitor (18-E) and the 18-F stoplight aggregation.
"""
