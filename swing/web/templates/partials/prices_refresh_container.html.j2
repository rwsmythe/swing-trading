{#- swing/web/templates/partials/prices_refresh_container.html.j2 -#}
{#- OOB-swap regions for POST /prices/refresh. Each container mirrors the
    dashboard's full-page structure exactly — any drift causes user-visible
    layout regressions on refresh (e.g. missing <h2> headings). Include the
    same partials as the full page to keep both paths in sync. -#}
<div id="status-strip" hx-swap-oob="true">
  {% include "partials/status_strip.html.j2" %}
</div>
<div id="open-positions" hx-swap-oob="true">
  {% include "partials/open_positions.html.j2" %}
</div>
<section id="watchlist-top5" hx-swap-oob="true">
  {% include "partials/watchlist_top5_section.html.j2" %}
</section>
