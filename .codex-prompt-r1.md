You are an adversarial design-spec critic (Codex). Review a BRAINSTORMING design spec for a Python/FastAPI swing-trading app. Be rigorous and skeptical. Your job is to find CRITICAL and MAJOR defects that would mislead the downstream writing-plans phase. Do NOT rubber-stamp.

## What to review
Read this spec from disk (you are running with -C at the worktree root):
- `docs/superpowers/specs/2026-05-30-phase14-sub-bundle-5-metrics-overview-design.md`

## Original request (verbatim intent)
Phase 14 Sub-bundle 5 (P14.N5), the LAST Phase 14 sub-bundle: enhance the EXISTING text-only `/metrics` index into a graphics-driven overview dashboard — each of the 9 metric surfaces gets an at-a-glance headline stat + a sparkline WHERE genuine trend data exists, with drill-down to the existing per-surface routes preserved. Read-mostly: reuse the existing metric computations + the SB3/SB4 chart infra. Likely NO schema change. No-JS graphics (matplotlib-SVG OR inline-polyline — both static). The honesty floor forbids fabricated sparklines. Single Codex chain to convergence.

## Verify the spec against PRODUCTION reality (read these files from disk)
- `swing/web/routes/metrics.py` — confirm `metrics_index` route + `build_metrics_index_vm(conn)` call-site (the spec claims it must widen to `(cfg, conn)`).
- `swing/web/view_models/metrics/index.py` — confirm `MetricsIndexSurface{path,label,description}`, `MetricsIndexVM(BaseLayoutVM)`, `build_metrics_index_vm(conn)`, `_SURFACES`.
- `swing/web/view_models/metrics/shared.py` — `BaseLayoutVM` contract.
- `swing/metrics/capital.py` — `compute_capital_friction`, `CapitalFrictionResult.trend_runs`, `CapitalFrictionTrendPoint` fields, `TREND_MIN_RUNS` (spec claims 5).
- `swing/metrics/funnel.py` — `compute_identification_funnel`, `IdentificationFunnelResult.trend_runs`, `IdentificationFunnelPoint` fields, `TREND_MIN_RUNS` (spec claims 10).
- `swing/metrics/process_grade_trend.py` — `compute_process_grade_trend`, `ProcessGradeTrendResult.rolling_series` (spec claims `dict[str, RollingMetricSeries]`, 7 keys).
- `swing/web/view_models/metrics/process_grade_trend.py` + `swing/web/templates/metrics/process_grade_trend.html.j2` — the inline `<polyline>` precedent the spec cites for OQ-1.

## Focus areas (find real defects, not nitpicks)
1. Brief-vs-production signature / data-shape accuracy: are the cited route/VM/compute names, field names, line numbers, and the 3-of-9-series claim CORRECT? Flag any wrong field name, wrong threshold, or non-existent symbol.
2. Honesty floor: does the spec ever risk a fabricated/degenerate sparkline? Are the per-surface suppression thresholds (5 / 10 / line-band) applied correctly, NOT a uniform n<5?
3. OQ-1 (sparkline tech): is the matplotlib-vs-inline tradeoff laid out fairly and HELD for operator (not unilaterally pre-picked)? Is the render-lock-contention argument (L6) sound?
4. Schema: is the NO-migration verdict correct? Is the v24 escape-hatch (cached matplotlib) correctly gated (STRICT pre_version==23, gotchas #11+#9)?
5. The builder-signature-widening claim (`build_metrics_index_vm(conn)` -> `(cfg, conn)`): is it real and complete (route call-site + tests)? Does any per-surface builder actually need cfg?
6. BaseLayoutVM (L7): the spec claims new fields go on the leaf VM (MetricsIndexSurface/MetricsIndexVM), NOT BaseLayoutVM, so no base-contract fan-out. Verify.
7. Completeness for writing-plans: are the headline-stat sources (§6) actually present on the cited result/VM objects, or are some invented? Flag any headline stat whose source field does not exist.
8. Any missing risk, gotcha, or under-specified contract that would bite the implementer.

## Output format
For each finding: SEVERITY (CRITICAL / MAJOR / MINOR), a short title, the evidence (file:line or spec section), and the concrete fix. End with a one-line verdict: either "NO_NEW_CRITICAL_MAJOR" (converged) or a count of open criticals+majors. Be specific; cite real lines.
