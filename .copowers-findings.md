# copowers adversarial-critic findings — Phase 14 Sub-bundle 4 (review + journal UX) brainstorming

**Provenance / honesty note.** This file is a faithful transcription of the
adversarial Codex round log recorded in §15 of
`docs/superpowers/specs/2026-05-30-phase14-sub-bundle-4-review-journal-ux-design.md`.
The originating Codex chain ran to convergence under the copowers version that
preceded **v2.0.2** — that prior version did NOT auto-persist findings to this
canonical `.copowers-findings.md` path (the auto-persist behavior is the v2.0.2
feature). The operator has confirmed the chain genuinely ran to convergence. This
transcription materializes the audit trail at the canonical path so the
writing-plans phase inherits it; it is NOT a fresh v2.0.2 run and is not
represented as one. Each round below mirrors the spec §15 entry verbatim in
substance. Single chain (Sec 9.1 Q7; pure UX/wiring). Codex read-only sandbox.

---

## Round 1 — 0 critical / 12 major / 10 minor — verdict ISSUES_FOUND

All 12 majors resolved in-spec (none accepted-without-fix):

- **M#1** OHLCV-archive-I/O vs read-mostly → §2.3 scope clarification (read-mostly
  governs the trade domain; the OHLCV archive is the app's existing shared
  read-through cache; ZERO new chart_renders/trade rows).
- **M#2** review-page render slowness/flakiness → §4.3 failure-isolation +
  lazy-load (`GET /trades/{id}/review/chart`).
- **M#3** closed-trade-immutability / memo staleness → §4.2 freshness-keyed (or
  request-lifetime) memo; bare-`trade_id` cross-request memo forbidden.
- **M#4** exit VWAP underspecified → §4.1 explicit reducing-fill classification +
  long-only positive-qty semantics.
- **M#5** total_risk unsafe formula → §4.1 guarded (long-only; None on
  missing/invalid stop).
- **M#6** "archive depth ample" too optimistic → §1.2/§4.2 softened; no-coverage
  is a first-class state.
- **M#7** thumbnail request-storms → §5.1 pagination + §5.3 freshness-memo /
  semaphore / cacheable-GET bounds.
- **M#8** cache-rejected-without-profiling → §12 performance budget + v24 tripwire.
- **M#9** chronology is substantive → §5.4 elevated to a tested domain surface with
  explicit per-source contracts.
- **M#10** sort/filter swap unresolved → §8 DECIDED whole-`<table>` `outerHTML`;
  OQ-9 de-punted.
- **M#11** TradeDrilldownVM base fields → §8 shared `_base_banner_fields` helper.
- **M#12** reopened-ticker stale cache → §6 one-open-trade-per-ticker invariant +
  refresh + Slice-1 test.

High-value minors folded: m#2 (exit-date source), m#3 (`| safe` reserved for
generated SVG only), m#4 (in-page error fragment, not bare 400), m#8 (ASCII
governs generated strings only), m#10 (`filter_state` "reviewed" = `state=='reviewed'`).

## Round 2 — 0 critical / 6 major / 4 minor — verdict ISSUES_FOUND

All 6 majors were refinements of R1 resolutions; all resolved in-spec:

- **R2 M#1** chart_renders write contradiction → §4.1 write-contract precision.
- **R2 M#2** wrong concurrency control → §5.3 corrected (archive path bypasses the
  OhlcvCache semaphore; warm-parquet common case + explicit render-concurrency bound).
- **R2 M#3** partial freshness token → §4.2 request-lifetime memo is the V1 default.
- **R2 M#4** lazy-route HTMX disciplines → §4.3 explicit response contract.
- **R2 M#5** vague tripwire → §12 operationalized warm-archive operator-box medians.
- **R2 M#6** "reviewed" semantics → §5.1 confirmed coupled via `complete_trade_review`.

Minors folded: m#1 (focus/scroll/control persistence on swap gate), m#3
(`Cache-Control: private`), m#4 (exit-date = stored fill date prefix), m#2 (n/a
display text).

## Round 3 — 0 critical / 2 major / 3 minor — verdict ISSUES_FOUND

- **R3 M#1** BULZ row-expand staleness → §4.1/§6 corrected to the SAME read-only
  `get_cached_chart_svg` path `build_trade_detail_vm` uses (NO JIT, NO write).
- **R3 M#2** matplotlib thread-safety → §4.2 process-wide render lock (pyplot global
  state verified; no existing lock).

Minors folded: m#1 structured WARNING on every exception fallback; m#2 distinguish
missing-trade in fragment text + logs; m#3 record warm medians in merge notes.

## Round 4 — 0 critical / 2 major / 0 minor — verdict ISSUES_FOUND

- **R4 M#1** lock must be global, not SB4-only → §3/§4.2 render lock at the SHARED
  `charts.py` boundary so every web render path serializes.
- **R4 M#2** lock could queue ~50 renders / DoS → §5.3 thumbnails load on scroll via
  `hx-trigger="revealed"` + smaller default page size.

## Round 5 — 0 critical / 2 major / 0 minor — verdict ISSUES_FOUND

- **R5 M#1** lock self-deadlock risk → §4.2 single outer acquisition per render;
  `RLock` if nesting unavoidable; writing-plans no-deadlock test per chart path.
- **R5 M#2** `revealed` in overflow containers → §5.3 verify window-scroll layout or
  use `hx-trigger="intersect"` with explicit root.

## Round 6 — 0 critical / 0 major / 0 minor — verdict NO_NEW_CRITICAL_MAJOR — CONVERGED

The R5 resolutions drew no new findings; the chain reached a clean terminal verdict.

---

**Chain close:** R1 0C/12M/10m → R2 0C/6M/4m → R3 0C/2M/3m → R4 0C/2M/0m →
R5 0C/2M/0m → R6 CLEAN. Cumulative 0 critical / 24 major / 17 minor across R1–R6;
ALL 24 majors resolved-via-code, ZERO accepted-without-fix, ZERO unresolved at
close. Findings narrowed monotonically from design-level (R1) to
implementation-detail (R5) to none (R6) — the signature of a genuine converging
adversarial chain rather than a silent no-op.

---

# Re-review (copowers v2.0.2, codex-cli 0.135.0, WSL CLI, read-only) — 2026-05-30

copowers upgraded to v2.0.2 AFTER the original chain. At operator direction the
review was re-run from initial run through convergence, with Codex reading the
LIVE branch working tree (every spec claim independently verified against source).

## Re-Round 1 — 0 critical / 5 major / 3 minor (ISSUES_FOUND)

### Major
1. §5.4 chronology used `review_log` as a per-trade source, but `review_log` is
   a cadence/period table with NO `trade_id` (`0013_phase6...sql:44-70`);
   per-trade review lives on `trades` columns (`models.py:213-223`,
   `review.py:598-615`). → RESOLVED (source switched to `trades` columns).
2. `has_hyprec_link = candidate_id is not None` over-labels A+ + manual-watch
   (all candidate-backed); origin distinguishes them (`origin.py:67-72`,
   `entry.py:374-405`). → RESOLVED (derive from `trade_origin`).
3. HTMX swap contract contradictory (whole-`<table>` outerHTML vs `<tbody>`
   across §5.2/§8/Slice 3/V1). → RESOLVED (single whole-`<table>` outerHTML).
4. Thumbnail trigger (`revealed` vs `load`) + page size (~50 vs ~20-25)
   contradictory. → RESOLVED (`revealed` + single ~20-25).
5. `daily_management_records` `record_type` discriminator
   (`0016_phase8...sql:31-43`) lumped; supersession undecided. → RESOLVED
   (split daily_snapshot/event_log + decide + field-map table).

### Minor
1. ReviewVM "chart field" ambiguity vs §4.3 lazy-load. → RESOLVED.
2. Missing-trade path could hide broken links. → RESOLVED (fixture).
3. Preserve verified cache anchors in plan. → RESOLVED (§1.2 anchors added).

## Re-Round 2 — 0 critical / 2 major / 2 minor (ISSUES_FOUND)

### Major
1. Missing-trade fix over-corrected: full-page `GET /journal/trades/{id}` must
   404 (`routes/trades.py:2598-2602`); only HTMX FRAGMENTS return 200+unavailable.
   → RESOLVED (split the two contracts in §10.1).
2. daily-management resolution not concrete — supersession still "decide", no
   field map. → RESOLVED (§5.4 decided + enumerated table).

### Minor
1. Stale "~50 rows" in §12 budget. → RESOLVED (~20-25).
2. `period` FastAPI `Literal[...]` (`routes/journal.py:18`) 422s before the
   in-page error fragment. → RESOLVED (type table params `str`, allowlist-check).

## Re-Round 3 — 0 critical / 1 major / 2 minor (ISSUES_FOUND)

### Major
1. New field-map `event_log` kind wrongly "from `action_taken`"; stop changes
   live in `stop_changed`/`prior_stop`/`new_stop` (`daily_management.py:272-294`).
   → RESOLVED (precedence over real columns).

### Minor
1. daily_snapshot "MFE/MAE %" are R-multiples (`open_MFE_R_to_date`). → RESOLVED.
2. fills "side/role" — `Fill` has `action`, no side (`models.py:289-296`). → RESOLVED.

## Re-Round 4 — 0 critical / 0 major / 1 minor (NO_NEW_CRITICAL_MAJOR) — CONVERGED

### Minor
1. `{qty}` placeholder vs production `quantity` (`models.py:293`). → RESOLVED.

**Re-run chain close:** Re-R1 0C/5M/3m → Re-R2 0C/2M/2m → Re-R3 0C/1M/2m →
Re-R4 CLEAN. Cumulative 0 critical / 8 major / 8 minor; ALL 8 majors
resolved-via-code, ZERO accepted-without-fix, ZERO unresolved at close. The 8
new majors were source-of-truth errors (wrong table / column / enum) +
internal-consistency contradictions invisible to the original MCP-era chain
(which did not read the live tree). The re-run vindicated the operator's call to
re-run after the v2.0.2 upgrade.

---

# WRITING-PLANS review (copowers v2.0.2, codex-cli 0.135.0, WSL CLI, read-only) — 2026-05-30

Single chain (OQ-8 LOCK) over the writing-plans PLAN
(`docs/superpowers/plans/2026-05-30-phase14-sub-bundle-4-review-journal-ux-plan.md`)
against the SPEC. Codex ran READ-ONLY inside the worktree, reading the live
`swing/` source to verify every cited signature/column. Run to convergence
(5-round cap suspended for this project).

## WP-Round 1 — 0 critical / 6 major / 2 minor (ISSUES_FOUND)

### Major (all RESOLVED-via-code in the plan)
1. Task 4.1 thumbnail incompatible with live `_render_candles_fig` API: plan
   passed `title=None` and assigned a 3-tuple return to `fig`. Live sig
   `_render_candles_fig(df,*,ma_windows,figsize,volume=True,style) -> (fig,
   price_ax,vol_ax)` (`charts.py:412-419`), NO `title`. → RESOLVED: unpack the
   3-tuple, drop `title`, normalize `bars` via `_normalize_ohlc_for_mpf` first.
2. Render-lock test didn't prove no-deadlock per chart path; only 2 of 5 public
   renderers named. Live: `render_watchlist_thumbnail_svg:492`,
   `render_ticker_detail_svg:540`, `render_position_detail_svg:628`,
   `render_market_weather_svg:752`, `render_theme2_annotated_svg:898`. →
   RESOLVED: decorate ALL 5 (+ `_is_serialized_render` marker), add all-wrapped
   + no-undecorated-renderer guards + a PARAMETRIZED held-lock no-deadlock test.
3. BULZ row-expand cache-miss can render BLANK: unavailable branch keys on
   `chart_reason_message` (legacy PNG scope), so `chart_reason is None` +
   `position_chart_svg_bytes is None` → blank. → RESOLVED: terminal `{% else %}`
   fallback + a no-blank-on-cache-miss route test.
4. Task 0.4 open-trade exit test contradicts production: `build_review_vm`
   returns `None` unless `state == 'closed'` (`trades.py:1223`). → RESOLVED:
   test `_exit_vwap([])` directly; document CR.1 is closed-only.
5. Dropped spec §5.2 "has-A+ flag" filter. → RESOLVED: `_FILTER_APLUS` +
   `filter_aplus` param + include/exclude test.
6. Thumbnail DoS guard weaker than spec §5.3(c) (no explicit render-concurrency
   bound). → RESOLVED: `BoundedSemaphore(2)` render bound + 200+"busy" fallback.

### Minor (RESOLVED)
1. Non-ASCII em dash in the drill-down `<h1>` template literal. → RESOLVED:
   ASCII hyphen.
2. Thumbnail "request-lifetime memo dedupes a double render" claim is moot
   (one trade per request). → RESOLVED: removed the memo from that route.

Codex read the live tree and caught 4 production-signature errors (M#1-M#4) at
a granularity the orchestrator-verified anchors had not surfaced — the value of
the read-from-disk WSL chain. All 6 majors + 2 minors fixed before WP-R2.

## WP-Round 2 — 0 critical / 5 major / 1 minor (ISSUES_FOUND)

All NEW issues (no re-raises of WP-R1); all RESOLVED-via-code:
1. `journal_row.html.j2` (an {% include %}d partial) called the `state_badge`
   macro without importing it — macros from the parent are NOT visible inside
   an included partial → 500 risk. → RESOLVED: the partial now imports
   `{% from "partials/state_badge.html.j2" import render as state_badge %}`.
2. The "busy" thumbnail fragment had no retry trigger (the `revealed` trigger
   already fired), so a burst could STRAND thumbnails until a full rerender. →
   RESOLVED: the busy span self-retries via `hx-trigger="load delay:1500ms"`.
3. The busy response was cacheable under `Cache-Control: private` → browser
   could replay "busy". → RESOLVED: busy responses set `Cache-Control: no-store`.
4. The busy branch was untested. → RESOLVED: added a test that exhausts the
   `BoundedSemaphore`, asserts 200+busy body + no-store + self-retry trigger +
   WARNING + permit release (no leak).
5. Sort links carried only `sort`/`dir`/`period`, DROPPING active
   `filter_state`/`filter_pattern`/`filter_aplus` on click — contradicting the
   S4 persistence gate. → RESOLVED: a `query_state` dict on the VM + URLs built
   from the full current query state + a sort-after-filter preservation test.

### Minor (RESOLVED)
1. Task 3.2 "Files" bullet omitted `filter_aplus`. → RESOLVED: added.

NOTE (non-issue, verified): the `app.py:89` docstring claims "the codebase has
no `{{ foo | safe }}` usage", but the live SVG-render pattern IS
`{{ ...decode('utf-8') | safe }}` (`detail.html.j2:66`, `watchlist_row.html.j2:22`,
`hypothesis_recommendations_expanded.html.j2:87`). The plan's `| safe`-on-SVG-bytes
matches the established pattern and is correct under the forced `autoescape=True`;
the docstring is stale. No change needed.

## WP-Round 3 — 0 critical / 4 major / 0 minor (ISSUES_FOUND)

All NEW issues (no re-raises); all RESOLVED-via-code. Issues narrowing
6 -> 5 -> 4 across rounds:
1. Busy thumbnail self-retry swapped INTO the busy `<span>` (no `hx-target`,
   `innerHTML`) → nested spans / stale busy wrapper on repeat. → RESOLVED:
   `hx-target="this" hx-swap="outerHTML"` so the retry REPLACES the span; test
   asserts it.
2. Timestamp contract handled only EMPTY ts (`not bool(ts)`); a non-empty
   GARBAGE timestamp sorted lexicographically as if valid; fills never validated
   `fill_datetime`. → RESOLVED: a `_normalize_ts(raw, *, precision)` helper
   parses date/datetime → (sortable ISO key, ts_malformed); ALL four sources use
   it; a malformed-ts-sorts-last test added.
3. daily_management field map omitted spec-locked detail fields — snapshot
   trail-MA eligibility, event_log volume/RS/regime + management_notes (live
   cols `0016...sql:60-72,84-94`). → RESOLVED: SELECT + detail expanded to
   `maturity_stage`/`trail_MA_eligibility_flag`/`trail_MA_candidate_price`
   (snapshot) and `volume_behavior`/`relative_strength_status`/
   `market_regime_change`/`sector_condition_change`/`news_or_event_update`/
   `management_notes` (event_log); contract tests assert them.
4. The review source SELECTed `mistake_tags` but dropped it from `detail` (spec
   §5.4 locks full lesson + tags). → RESOLVED: detail now carries lesson + tags;
   test asserts the tag text appears.

Codex again read the live tree (cited `0016...sql:60-72,84-94`) and tightened
the chronology toward the spec's §5.4 detail contract + robust timestamp
handling. All 4 majors fixed before WP-R4.

## WP-Round 4 — 0 critical / 2 major / 1 minor (ISSUES_FOUND)

All NEW; all RESOLVED-via-code. Narrowing 6 -> 5 -> 4 -> 2:
1. `_normalize_ts` used `date.fromisoformat`/`datetime.fromisoformat` but the
   chronology module imported neither, and imported an unused `field` (would
   fail to run + trip ruff). → RESOLVED: `from datetime import date, datetime`;
   dropped the unused `field` import.
2. The supersession regression test (`getattr(e, "_src_id", None) != ...`) was
   NON-discriminating — `ChronologyEntry` has no `_src_id`, so the assertion was
   always true and would not catch a leaked superseded row. → RESOLVED: the
   fixture plants a unique marker on the superseded row + a unique marker on the
   active row; the test asserts the superseded marker is ABSENT and the active
   marker is PRESENT (fails on both over- and under-filtering).

### Minor (RESOLVED)
1. Task 4.2 acceptance still said `Cache-Control: private` for all four
   contracts while the busy branch uses `no-store`. → RESOLVED: acceptance now
   states success/unavailable/not-found = private; busy = no-store.

The two majors were a compile/lint bug and a non-discriminating test — exactly
the implementation-detail tier that signals an approaching clean verdict.
