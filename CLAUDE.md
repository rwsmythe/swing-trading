# Swing Trading — Claude Code Context

**Current state (2026-05-22 PM #3, HEAD `9b2a4db`):** Phase 13 T4.SB WRITING-PLANS SHIPPED post integration-merge of `phase13-t4-sb-writing-plans` via --no-ff at `9b2a4db` — the SECOND sub-bundle of the Phase 13 closer arc (T4.SB brainstorming → writing-plans → executing-plans). Plan at [`docs/superpowers/plans/2026-05-22-phase13-t4-sb-closer-plan.md`](docs/superpowers/plans/2026-05-22-phase13-t4-sb-closer-plan.md) (4184 lines NEW; 14 sections §A-§N with §A-§L per dispatch-brief done criteria + §M references + §N self-review; 6 sub-bundle tasks T-T4.SB.1..T-T4.SB.6 with 149 bite-sized TDD step-checkboxes); return report at [`docs/phase13-t4-sb-writing-plans-return-report.md`](docs/phase13-t4-sb-writing-plans-return-report.md). 6-commit dispatch (1 initial plan at `8ac4687` + 4 Codex MCP fix bundles at `7cc5775` + `0023df7` + `600313f` + `711637e` + 1 return report at `c8d21b9`). **Codex MCP adversarial-critic chain converged at R5 NO_NEW_CRITICAL_MAJOR after 5 rounds** (R1: 2C+6M+3m; R2: 0C+3M+3m; R3: 0C+3M+3m; R4: 0C+2M+3m; R5: 0C+0M+0m convergence; 2 CRITICAL + 14 MAJOR + 12 MINOR ALL RESOLVED in-place; ZERO advisory-only). **Codex caught real defects against actual code/schema**: placeholder variable enumeration (R1.C1); missing `allowed_miss_names` invariant in `bucket_for` mirror (R1.C2); non-existent `hypothesis_registry.description` column (R1.M1); wrong `ChartRender` field set (R1.M2); renderer signature mismatches (R1.M3); `pipeline_runs.state='completed'` not in CHECK enum→'complete' (R1.M4); undefined `_latest_completed_pipeline_run` import (R4.M1); nonexistent `request.app.state.db_conn` (R2.M3). **29th cumulative C.C lesson #6 validation NOTABLE — Expansion #10 (architecture-location 5-sub-discipline) ran CLEAN at writing-plans tier** (FIRST clean-on-arrival validation post-brainstorming; brainstorming spec's architecture-location discipline carried forward correctly; Codex R1-R5 surfaced ZERO architecture-location regressions). **NEW Expansion #11 CANDIDATE banked for 30th cumulative validation onwards**: taxonomy propagation audit — when an enum-typed field (`kind`/`status`/`type`) is added to one dataclass, audit all downstream dataclasses + serializers + test fixtures for consumption. Surfaced via R1+R3+R4 `kind` enum propagation 3-instance lesson (T-T4.SB.1 sensitivity-harness `{additive, gate, threshold}` taxonomy required 3 Codex rounds to fully scrub across `SweepEntry` dataclass + CSV header + markdown matrix Kind column + test fixtures). **1 NEW CLAUDE.md gotcha appended this housekeeping** (Expansion #11 candidate; taxonomy propagation audit). **10 V1 simplifications banked with V2 dependency cited** per return report §4 — notable: sensitivity-harness 15-of-17 threshold deltas-0 V1 (V2 OHLCV criterion-evaluator); allowed_miss_names tuple-set sweep excluded; benchmark_ticker string-identifier excluded; metrics_wiring_audit `_KNOWN_SURFACES` hand-maintained registry; corpus-all re-label operator-paired; OQ-5.1 R4 manual prune CLI; OQ-5.2 sync JIT no-timeout; `hyprec_detail` surface-name grandfathered; OHLCV cache validity at original asof_date V2-dependent; `_known_surfaces` R1 best-guess seed. **Sub-bundle dispatch decomposition LOCKED per plan §G (BINDING for executing-plans)**: T-T4.SB.1 (Item 1 sensitivity harness under `research/harness/aplus_sensitivity/` + Item 7 specific-defect diagnostic) + T-T4.SB.2 (Item 7 broader metrics audit + cross-bundle pin row 13 parametrize 4 surfaces) + T-T4.SB.3 (Item 5 architecture work with NEW `swing/web/chart_jit.py` module + chart_scope LOCKED read-only) + T-T4.SB.4 (Item 2 additive `rule_criteria` + envelope alias `narrative` key) + T-T4.SB.5 (Items 3 + 4 + 6 cosmetic/UX bundled) + T-T4.SB.6 (closer + Phase 13 FULLY CLOSED marker per spec §K + post-T4.SB triage agenda artifact per §1.5.2). **Phase 13 sub-bundle ship count: 11 of 11 SHIPPED** — T4.SB writing-plans SHIPPED THIS pass advances the closer arc but does NOT yet flip Phase 13 to fully closed (that fires at T-T4.SB.6 executing-plans SHIPPED per spec §K + §1.5.2 amendment triage-agenda artifact). Predecessor stack: T4.SB brainstorming at `4299340` (R5 NO_NEW_CRITICAL_MAJOR; 17 MAJOR ALL RESOLVED; Expansion #10 candidate banked; 18 OQs operator-triaged 2026-05-22 PM #2) + T2.SB6c executing-plans at `f30ceed` (R4 NO_NEW_CRITICAL_MAJOR; v20 → v21 LANDED at T-A.6c.1; 5670 fast tests baseline; §1.5.1/2/4 amendments shipped) + T2.SB6c writing-plans at `e26bb0a` + T2.SB6c brainstorming at `fb177e3` + T2.SB6b at `6ec989e` + T2.SB6a at `340f868` + T3.SB3 at `352bd83` + T2.SB5 at `409d209` + T2.SB4 at `3b28d92` + T3.SB2+hotfix at `cf3c489` + T2.SB3 at `e3d34a9` + T2.SB2+T-PT9 at `c15633d` + T2.SB1 at `b00597c` + T3.SB1 at `48c6bc6`. **Baseline 5670 fast tests** UNCHANGED (T4.SB writing-plans docs only) / **0 ruff E501** / **schema v21** UNCHANGED. **ZERO new Schwab API calls** (L2 LOCK preserved). Production ZERO open discrepancies. **Phase 13 dispatch sequence remaining**: T4.SB executing-plans → T-T4.SB.6 closer + Phase 13 FULLY CLOSED marker per spec §K + post-T4.SB triage agenda artifact per §1.5.2 amendment + post-T4.SB-SHIPPED operator-paired triage meeting (Phase 14 trigger / Applied Research focus / idle monitoring decision per §1.5.2). **Authoritative pointers**: [`docs/orchestrator-context.md`](docs/orchestrator-context.md) — [`docs/phase3e-todo.md`](docs/phase3e-todo.md) — [`docs/CLAUDE.md-archive.md`](docs/CLAUDE.md-archive.md) — [`docs/superpowers/specs/`](docs/superpowers/specs/) — [`docs/superpowers/plans/`](docs/superpowers/plans/) — [`docs/cycle-checklist.md`](docs/cycle-checklist.md). **Streaks preserved**: ZERO `Co-Authored-By` footer trailer drift (~385+ commits cumulative through T4.SB writing-plans merge + housekeeping); C.C lesson #6 cumulative validations 22x CLEAN through T3.SB3 → 23rd-28th NOTABLE through T2.SB6 + T4.SB-brainstorming arc → 29th NOTABLE T4.SB writing-plans (Expansion #10 CLEAN first clean-on-arrival; NEW Expansion #11 candidate banked). **Phase 13 11 of 11 sub-bundles SHIPPED**; T4.SB closer arc IN-FLIGHT (brainstorming + writing-plans both SHIPPED; executing-plans remains).

*Note 2026-05-22 PM #3: T4.SB WRITING-PLANS SHIPPED at `9b2a4db` — SECOND sub-bundle of Phase 13 closer arc. Codex R5 NO_NEW_CRITICAL_MAJOR (R1: 2C+6M+3m; cumulative 2 CRITICAL + 14 MAJOR + 12 MINOR ALL RESOLVED in-place). 29th C.C lesson #6 validation NOTABLE — Expansion #10 (architecture-location 5-sub-discipline) ran CLEAN at writing-plans tier (FIRST clean-on-arrival post-brainstorming); NEW Expansion #11 CANDIDATE banked (taxonomy propagation audit — `kind`/`status`/`type` enum field propagation across dataclass + serializer + fixture). 1 NEW gotcha appended (Expansion #11 candidate). 10 V1 simplifications banked with V2 dependency cited. Schema v21 UNCHANGED through writing-plans phase (docs only). Phase 13 sub-bundle ship count 11 of 11 — T4.SB closer arc IN-FLIGHT (brainstorming + writing-plans both SHIPPED; executing-plans remains). Predecessor: T4.SB brainstorming at `4299340` 2026-05-22 PM #2.*
*CLAUDE.md status-line restructured 2026-05-18 PM (Option B2 evaluator-pass; operator-decided): previously a single ~133K-char paragraph; replaced with the compact "Current state" summary above. Full historical SHIPPED ledger preserved at [`docs/CLAUDE.md-archive.md`](docs/CLAUDE.md-archive.md). Retention discipline + size-check trigger documented at [`docs/orchestrator-context.md`](docs/orchestrator-context.md) §"Maintenance: retention discipline."*

## Quick Start

```bash
pip install -e ".[dev,web]"
swing --help                         # CLI: trade, journal, pipeline, web, finviz, etc.
swing web                            # FastAPI + HTMX dashboard on 127.0.0.1:8080
python -m pytest -m "not slow" -q    # fast suite (~5670 tests as of 2026-05-22, ~2min with xdist)
python -m pytest -m slow             # pipeline/yfinance e2e (minutes, needs network)
ruff check swing/
```

**Windows PATH for `swing` CLI:** `pip install -e` places the `swing.exe` entry point in `%APPDATA%\Python\Python314\Scripts\` (user install, not `C:\Python314\Scripts`). Add that dir to the user PATH permanently via System Properties, or per-session: `$env:PATH = "$env:APPDATA\Python\Python314\Scripts;$env:PATH"`.

**Finviz inbox:** `data/finviz-inbox/` (configured in `swing.config.toml`). Pipeline now fetches the screen via Finviz Elite API automatically (`_step_finviz_fetch`); manual CSV drop remains a fallback override (file-collision skip in the pipeline step). Save manual exports as `finvizDDMmmYYYY.csv` (1-2 digit day, 3-letter month, 4-digit year — e.g. `finviz5May2026.csv`). API-emitted CSVs use the same filename pattern, anchored on `action_session_for_run(datetime.now())` per [`swing/pipeline/runner.py:_step_finviz_fetch`](swing/pipeline/runner.py). Validator requires 13 columns: `No., Ticker, Sector, Industry, Country, Price, Change, Average Volume, Relative Volume, Average True Range, 52-Week High, 52-Week Low, Market Cap`. Missing columns → CSV moved to `data/finviz-inbox/rejected/` with a sidecar `.rejected-reasons.json`. Pipeline outputs go to `exports/<action_session_date>/` (briefing.md + briefing.html + charts/).

**Pipeline lease-acquisition wait** (`web.pipeline_lease_wait_seconds`, default 5s): how long POST `/pipeline/run` waits for the spawned subprocess to insert its `pipeline_runs` row. Python 3.14 cold-start + heavy imports on Windows regularly cross 2s; bump if you see false "did not acquire lease" errors on a slow box.

## Strategy

The project follows a bifurcated architecture (research branch + operational trader-facing branch) per:

- `reference/Future Work/2026-04-23-bifurcated-strategic-implementation-proposal-v2.1.md` — governing strategy.
- `reference/Future Work/2026-04-23-rebuttal-response-for-implementors.md` — binding clarifications (minimum viable governance, toleranced parity, bootstrap-first data).

Source-of-truth methodology references live in `reference/methodology/`. These are reference-only; any production change driven by a methodology reference must route through the source-of-truth correction protocol (V2.1 §VII.F), not a direct patch.

The research branch lives at `research/` (created Tranche B-research, 2026-04-23). Method records in `research/method-records/` follow the V2.1 §IV.B minimum viable field list; studies in `research/studies/` follow the format established by `earnings-proximity-exclusion.md`. Phase 0 tasks in `research/phase-0-tasks.md`. No runtime code is under `research/` as of this writing — it is pure documentation and study artifacts.

Older strategy documents are archived at `reference/Future Work/archive/`.

## Architecture

`swing/` — application package:
- `data/` — SQLite schema (`migrations/*.sql`), dataclass models, repo functions
- `pipeline/` — nightly orchestrator, lease fencing, finviz CSV select+validate, step runners
- `trades/` — entry/exit/stop_adjust services + reconciliation flow + auto-correction service (Phase 12 C); advisory rules; equity math
- `recommendations/` — `compute_shares` / `SizingResult` (position sizing)
- `evaluation/` — A+ criteria, bucket rules, date semantics (`action_session_for_run`)
- `web/` — FastAPI + HTMX (`app.py`, `routes/`, `view_models/`, `templates/`, `middleware/`, `price_cache.py`)
- `integrations/` — Finviz Elite API (`finviz/`) + Schwab Trader/Market Data APIs (`schwab/`); per-vendor sub-packages (post-Phase-11)
- `metrics/` — Phase 10 dashboard metric surfaces (9 surface routes; Phase 13 T2.SB6b pattern-outcomes 9th tile shipped at `6ec989e`)
- `cli.py` — click-based `swing` CLI entry point

`tests/` mirrors `swing/`. `docs/superpowers/specs/` and `/plans/` hold per-phase design docs.

## Invariants

- **DB location:** `%USERPROFILE%/swing-data/swing.db` — **outside** the Drive dir (hard invariant; Drive syncing corrupts SQLite).
- **Phase isolation:** `swing/trades/` and `swing/data/` are consumed read-only unless the current-phase spec explicitly scopes a carve-out (historical: 3c touched `update_stop_with_event`; 3d touched `advisory.py`; 5 added `config_overrides.py`; 6 added `swing/trades/review.py` + `swing/data/repos/review_log.py` + 10 nullable trade-row fields + new `review_log` table; **7 introduced state-machine + Fills first-class** with new `swing/trades/state.py` + `origin.py` + `derived_metrics.py` + `swing/data/repos/fills.py` + 28 new trade columns + new `fills` table; status column dropped + exits table dropped + Exit shim deleted). Default posture for new phases remains read-only.
- **Current baseline:** ~5670 fast tests green on `main` (HEAD `2d266fa`, 2026-05-22 PM; Phase 13 11 of 11 sub-bundles SHIPPED — T4.SB remaining/paused; Phase 12.5 arc CLOSED). Schema v21; ruff 0 E501; production ZERO open discrepancies.

## Conventions

- **Branch:** all work on `main` (established for this project).
- **Commits:** conventional (`feat(web):`, `fix(web):`, `refactor(...)`, `test(...)`). **No Claude co-author footer, no `--no-verify`.**
- **TDD:** write failing test → see fail → minimal implementation → see pass → commit, per task.
- **copowers workflow:** `copowers:brainstorming` → `copowers:writing-plans` → `copowers:executing-plans` (wraps `subagent-driven-development`). Each adds adversarial Codex MCP review (2–5 rounds) before approval. Session state in `.copowers-session-*.json` at project root.
- **Starlette 1.0 signature:** `TemplateResponse(request, "name", {...}, status_code=...)`.
- **TestClient lifespan:** tests touching `app.state.price_fetch_executor` MUST use `with TestClient(app) as client:` (enters lifespan).

## Windows + gitbash

- Drive path has a space — always quote `"c:/Users/rwsmy/My Drive/Swing Trading"`.
- Unix shell syntax (`/dev/null`, not `NUL`); forward slashes in paths work in Python.
- Python 3.14 on this machine; `pyproject.toml` targets `>=3.11`.
- CRLF warnings from git are cosmetic — ignore.

## Gotchas

- **yfinance rate-limits.** Use `threads=False` on `yf.download()` ONLY. `yf.Ticker(t).history()` does NOT accept `threads=` and raises `TypeError: got an unexpected keyword argument 'threads'` on yfinance >= 1.2. Concurrency for `Ticker.history()` is bounded by the app-level executor; `PriceCache` + `OhlcvCache` sliding-window breakers are the real backpressure.
- **Test-count drift in plan docs.** Plans show stale expected counts — trust `pytest` output.
- **The auto-memory at `~/.claude/projects/c--.../memory/`** can go stale. Verify `project_refactor_intent.md` against current git log before assuming refactor is out-of-scope.
- **HTMX 4xx fragments need an explicit config override.** HTMX 2.x default is `{code:"[45]..",swap:false,error:true}` — 4xx responses fire the error event but don't swap. `base.html.j2` contains a `htmx.config.responseHandling` override that enables 4xx swapping; preserve it if you touch the base layout. TestClient-based tests won't catch a regression (they assert response body, not DOM state).
- **Starlette middleware is LIFO.** `add_middleware` call order is the REVERSE of request execution order — later calls become more-outer. Security-critical middleware (OriginGuard strict-mode, request-id stamping) must be added LAST so it wraps everything inside. A body-size check added after OriginGuard but before RequestId would fire 413 before OriginGuard's 403, silently weakening strict mode.
- **`os.replace` requires same filesystem.** On Windows with Drive-synced paths + `$TMP` on a different volume, `os.replace(tmp, final)` raises `OSError: [Errno 18] Invalid cross-device link`. For atomic-replace flows: create temp files in the destination directory (`tempfile.NamedTemporaryFile(dir=<dest_dir>, ...)`). Never use `shutil.move` expecting overwrite on Windows — it fails when the destination exists.
- **yfinance `history(interval="1d")` includes the in-progress bar during market hours.** The last row is today's PARTIAL session until the next session rolls over. If you compute a rolling-mean SMA or a "previous close" directly off `df.tail()`, you're sampling a partial intraday close — a "close below MA" rule turns back into an intraday rule. Strip it: `if df.index[-1].date() >= session: df = df.iloc[:-1]` where `session = action_session_for_run(datetime.now())`. Use the exchange-session helper, NOT `date.today()` — HST lags ET by 5h, so local midnight would incorrectly preserve today's partial bar for hours.
- **`base.html.j2` is shared — new `vm.foo` field requires adding to EVERY base-layout VM.** Every page that `{% extends "base.html.j2" %}` has a VM rendered as `vm` in the template context. If the base layout starts dereferencing a new field (banner flag, session date, etc.), EVERY base-layout VM must gain that field (with a safe default) or Jinja 500s unrelated routes with `UndefinedError`. Current set: `DashboardVM`, `PipelineVM`, `JournalVM`, `WatchlistVM`, `PageErrorVM`. Phase 3c hit this with `price_source_degraded` initially and caught it during adversarial review; Phase 3d hits it again with `ohlcv_source_degraded`. Same fix pattern each time.
- **Cache + executor race: workers must not write to shared state when the request thread cancels on deadline miss.** `Future.cancel()` is a no-op on a running worker. If the worker writes to `cache._store` from inside itself, a late-completing worker will poison the cache with a bundle the request thread already gave up on. Pattern: worker returns a pure value; request thread writes to `_store` ONLY for futures that completed in-deadline (`wait(..., timeout=deadline)` → iterate `done`, not `pending`). `OhlcvCache` follows this; future caches should too.
- **Weather lookup in read-only UIs must NOT query by `action_session`.** Weather is stored keyed by `data_asof_date` (last completed NYSE session); `action_session` is forward-looking (next session). On Sunday-evening → Monday-prep workflow (or any weekend/holiday gap) the query silently returns `None` → UI renders STALE despite a fresh weather row existing. Use `swing.data.repos.weather.get_latest(conn, ticker=...)` which returns the most recent record by `run_ts` regardless of date. The pipeline's own briefing generator uses `data_asof`; read-only UIs should use `get_latest`.
- **OHLCV fetch scope = open-trade tickers ONLY.** Advisories (trail-MA, exit-below-MA) only fire on open positions; watchlist rows never consume SMA data. Fetching OHLCV for watchlist tickers burns yfinance quota on every render and trips the `OhlcvCache` sliding-window breaker under normal load. `build_dashboard` computes `ohlcv_tickers = sorted({t.ticker for t in open_trades})` — do NOT union with watchlist or `active_tickers`. With zero open trades, skip the fetch entirely (guarded by `if ohlcv_cache is not None and ohlcv_tickers`).
- **yfinance `group_by='column'` now returns a MultiIndex column** (`Price × Ticker`) even for single-ticker calls. `df["Close"]` is a DataFrame, not a Series — `float(iloc[-1])` raises `TypeError: float() argument must be a string or a real number, not 'Series'`. Squeeze defensively: `close = df["Close"]; if hasattr(close, "ndim") and close.ndim == 2: close = close.iloc[:, 0]`. This and the `Ticker.history()` `threads` regression are examples of the same failure mode: pip-installed dependency updates silently break integration points that were working last quarter.
- **`exchange_calendars.is_open_at_time` requires `pd.Timestamp`**, not `datetime.datetime`. Passing a plain datetime raises `TypeError: \`timestamp\` expected to receive type pd.Timestamp`. The prior `market_hours_now()` caught all exceptions silently and returned `False`, permanently routing PriceCache to the last-close fallback during live trading hours. Wrap: `pd.Timestamp(datetime.now(timezone.utc))`. Same failure class as the yfinance API regressions above — when you catch broad exceptions around a dependency call, log them instead of silently returning a degraded default.
- **`PriceCache._last_close` only sees tickers in today's `candidates` table.** Open-trade tickers that rotate OUT of the finviz CSV get no fresh candidate row, so the after-hours fallback returns the close from whenever they were last in finviz (potentially days old). `_step_evaluate` unions `list_open_trades` into its fetch loop and writes them as `bucket='excluded'` with `notes='open position'` so the close stays fresh every run. If you add a new "priced" UI surface, verify its tickers actually appear in `candidates` — otherwise the fallback lies silently.
- **Queries ordered by `started_ts DESC` on `pipeline_runs` mask prior completes mid-run.** A new run with `finished_ts IS NULL` wins the ORDER BY and surfaces NULL where callers expect the last-known-good timestamp (dashboard "Last pipeline" rendered "never" the entire time a run was in flight). Two-read pattern: most-recent COMPLETED row's `finished_ts` for "when did we last have good data?", separately most-recent-started row's `state` for "what's happening right now?"
- **HTMX OOB-swap partials that hand-duplicate full-page markup drift silently.** The refresh-now `prices_refresh_container.html.j2` partial hand-duplicated the watchlist `<table>` markup without the `<h2>` heading and "Show all (N)" link that live in `watchlist_top5_section.html.j2`. On OOB swap the heading vanished and the watchlist butted up against the Open Positions table; a hard browser refresh restored the layout because full-page render used the canonical partial. Cosmetic symptom, structural cause. Fix: OOB-swap partials and the full-page render must go through the SAME `{% include %}` target, not hand-duplicated markup. If you find yourself copying JSX/HTML fragments between two partials to avoid an include, stop — the drift failure is a matter of when, not if.
- **External-API empty-result must be treated as transient when write-through-caching.** Failure mode discovered 2026-04-30 (OHLCV archive helper R2 Major 2; commit `3335d6c`). Write-through caches/archives backed by external APIs (yfinance, broker APIs, weather APIs) must NEVER replace existing cached content from an empty API response. yfinance occasionally returns empty for transient reasons — rate limit, network blip, weekend/holiday, ticker-symbol handling drift, upstream regression. Without the fallback, an established ticker's archive blanks; subsequent reads must re-fetch full history; historical context is permanently lost if the next call also returns empty. **Append-or-fall-back pattern:** on empty API response, RETAIN existing cached content; leave stale metadata (so next call retries the refresh); never blank. The only case where empty-as-authoritative is correct is when the API has explicit "no data found" semantics distinct from "transient failure" — and even then, prefer leaving stale data + flagging staleness over blanking. Applies to: `swing/data/ohlcv_archive.py` (now correctly handles); future weather caches; future broker-API caches; any write-through-cache backed by a flaky external API.
- **HTMX response leading with `<tr>` triggers `makeFragment` synthetic-table-wrap that drops `<table>` content inside OOB `<section>` chunks.** Failure mode discovered 2026-04-29 (entry_post Bug B; commits `398483d` + later). When an HTMX response's primary content starts with a `<tr>` element, the htmx.js `makeFragment` helper wraps the entire response in a synthetic `<table><tbody>` so the browser's HTML5 parser will accept the `<tr>` as valid (table-row content can't be parsed at fragment root). HTML5 nested-table parsing rules then DROP any inner `<table>` elements found inside `<section>` OOB chunks during the fragment parse — `htmx:oobBeforeSwap` and `htmx:oobAfterSwap` fire successfully, but the swap content has been MANGLED at parse time, leaving the OOB target with only the wrapping non-table elements (e.g., `<section><h2>Watchlist</h2></section>` with the `<table>` of rows silently gone). Server-side TestClient assertions on response body show fully-populated content; the bug only surfaces in a real browser's parser. **Fix: pure-OOB response architecture** — don't lead the response with a `<tr>` at fragment root; deliver new rows via OOB swap into the destination tbody instead (mirroring `partials/prices_refresh_container.html.j2`'s pattern). **Pre-empt the failure mode in any new HTMX endpoint that returns multiple swap chunks: keep the response's primary content table-row-free (use `<div>` or `<section>` at the root if you need primary content at all; otherwise return only OOB chunks).** This is the canonical example of the JS-test-harness gap (orchestrator-context "Lessons captured" 2026-04-29) — TestClient response-body assertions cannot catch this; only operator-witnessed browser DevTools verification (or a Playwright/Selenium harness) will.
- **Matplotlib mathtext fires on `$` (paired math mode), `^` (superscript), `_` (subscript), and unbalanced `\` — silently italicizes intervening text, consumes the metacharacters, and collapses surrounding whitespace.** String-equality test assertions verify the format STRING; they do NOT verify the RENDERED output through the mathtext interpreter. Commit `2fd0ecc` attempted to escape `$` with `\$` in the chart title format; matplotlib resolves `\$` to a literal `$` BEFORE math-mode parsing, so the paired `$..$` still triggered math mode (italics on "stop", `$` glyphs consumed, spaces collapsed in the rendered PNG) despite the test strings reading `\$110.00`. Real fix (`29c93f5`): omit the metacharacter from the title format entirely. Pattern: when adding/changing any rendered text that flows through matplotlib (`title=`, `xlabel=`, `ylabel=`, axes annotations, suptitles, legends), or any mathtext-aware renderer, **manual visual verification of rendered output is non-optional** — string-equality on the source string is insufficient. Future title-format additions (scientific notation `1e3`, exponents `x^2`, subscripts `x_i`, footnote markers) must visually re-verify on rendered PNG. Defense-in-depth alternatives if the threat model widens: `fig.suptitle(..., parse_math=False)` after `mpf.plot(returnfig=True)`, or a `_sanitize_for_mathtext()` helper that escapes/strips metacharacters at format-string time.
- **HTMX form-driven endpoints have two browser-only failure surfaces TestClient cannot detect: HX-Request header propagation on embedded forms + HX-Redirect for success-path response.** Failure modes discovered 2026-05-02 (Phase 5 config page, Codex R1 Major 1 + 2; commit `4d3174d`). (1) Embedded forms inside HTMX-rendered fragments must include `hx-headers='{"HX-Request": "true"}'` on the form element — without it, OriginGuard strict-mode rejects subsequent submits with 403 because the parent page's HTMX header context doesn't propagate to nested forms by default. TestClient passes because tests send the header explicitly; real browser submissions without the propagation hint fail. (2) Success-path response to HTMX POST should be `204` + `HX-Redirect: <url>` header (browser re-navigates via htmx.js), NOT a `303` → swap-target — real browsers using htmx.js swallow `303` transparently and the swap target receives nothing. TestClient verifies the `303` status code but not how htmx.js processes it. **Pre-empt in any new HTMX-driven route handler with form submission:** the plan's per-task acceptance criteria MUST enumerate (a) embedded-form `hx-headers='{"HX-Request": "true"}'` propagation under OriginGuard strict-mode, and (b) success-path `HX-Redirect` emission instead of 303 swap-target. Operator-witnessed browser verification gate is BINDING for HTMX work — both failure modes are invisible to TestClient. Pattern complement to the JS-test-harness gap lesson family (Bug 1 follow-ups 2026-04-25; hyp-recs success-path 2026-04-29; entry_post Bug B `<tr>`-leading makeFragment 2026-04-29).
- **HX-Redirect target route must be verified to exist; TestClient verifies the header but doesn't follow.** Failure mode discovered 2026-05-04 (Phase 6 review POST, operator-witnessed verification gate; fixed inline as code-review I3 on worktree). Phase 6 review POST returned `HX-Redirect: /trades` per plan but no `GET /trades` route was registered (route table had `/trades/entry/form`, `/trades/{id}/exit`, etc., but no bare `/trades` listing). HX-Redirect mechanic worked correctly (header propagated, browser navigated to `/trades`); destination 404'd with `{"detail":"Not Found"}`. TestClient verified status code (204) + header value (`/trades`) but didn't follow the redirect to verify target resolves. **Pre-empt in any new HX-Redirect-emitting handler:** plan tests should either (a) assert target route is registered in app's route table (e.g., `assert any(r.path == target for r in app.routes)`), OR (b) follow the redirect with a second TestClient call and assert 200. Operator-witnessed browser verification gate is BINDING — this is the third browser-only HTMX failure surface in the catalog (Phase 5 R1 M1 HX-Request header propagation; Phase 5 R1 M2 HX-Redirect-vs-303-swap; Phase 6 I3 HX-Redirect-target-unrouted).
- **Python `... or ""` idiom collides with SQL CHECK-constraint nullability when CHECK enum doesn't include empty string.** Failure mode discovered 2026-05-04 (Phase 6 `mistake_cost_confidence` form input; deviation #3 from executing-plans return report; corrected in implementer-side fix). Plan template had `value = form_value or ""` for nullable text column with CHECK constraint `mistake_cost_confidence IN ('high', 'medium', 'low')`. The `or ""` idiom produces an INSERT that fails the CHECK (empty string isn't in the enum) instead of writing NULL (which a nullable column allows). Fix: `value = form_value or None` for nullable text columns with CHECK constraints. Subtle Python-idiom-vs-SQL-nullability collision; recurs anywhere a form-input fallback feeds a nullable enum-CHECK column. Pre-empt in any new schema-with-CHECK + form-input combo: prefer `... or None` over `... or ""` for nullable text columns; add a discriminating test that submits an empty form field + asserts NULL persisted (not empty-string CHECK violation).
- **SQLite `INSERT OR REPLACE` (and the bare `REPLACE` shorthand) is `DELETE old + INSERT new` semantically — NOT a true UPDATE.** Failure mode anticipated 2026-05-06 (Phase 8 daily-management brainstorm Codex R4 Major fix; spec at `docs/superpowers/specs/2026-05-06-phase8-daily-management-design.md` §4.2). For any audit-trail-with-uniqueness pattern, REPLACE will: (a) CASCADE-WIPE child rows referenced via ON DELETE CASCADE FKs, (b) issue a NEW auto-increment PK (history-rewrite), (c) reconstitute index entries with the new PK. Symptom on Phase 8's daily_management_records: same-tier reflow via `INSERT OR REPLACE` would silently destroy any audit-FK chain pointing at the prior row's PK + cascade-wipe linked event_log entries that reference it. **Pattern: SELECT-then-UPDATE-or-INSERT** — query against the unique-key tuple to find existing row, UPDATE if found OR INSERT if not. Same family as the `executescript()` partial-failure rollback gotcha — both about SQLite-runner-level discipline that needs explicit code, not implicit assumptions about ORM-style atomicity. **Pre-empt in any new UPSERT design:** writing-plans phase enumerates SELECT-then-UPDATE-or-INSERT semantics; reject `INSERT OR REPLACE` proposals on tables with FK references or audit-trail intent.
- **Finviz Elite API token storage.** Token + screen-query live in `%USERPROFILE%/swing-data/user-config.toml` under `[integrations.finviz]`. Tracked `swing.config.toml` MUST NOT contain the token (sensitive); contains only `timeout_seconds`. Cassettes redact `auth=` query-param via pytest-recording's `filter_query_parameters`. `swing config show` does NOT surface Finviz fields (V1: `FIELD_REGISTRY` not extended; V2 may add masked surfacing).
- **Finviz API cassette staleness.** If `tests/integrations/test_finviz_api_live.py` (slow-marked) fails on response-shape diff after a Finviz upstream change, re-record cassettes per `docs/superpowers/plans/2026-05-05-finviz-api-integration-plan.md` §G runbook. Operator's saved screen MUST include all 13 canonical columns OR the slow test fails on `FinvizSchemaParityError` — operator-actionable via Finviz UI saved-screen edit.
- **File-collision precedence at `_step_finviz_fetch`.** If a manual CSV is present in `data/finviz-inbox/` for today's session date, `_step_finviz_fetch` skips API fetch + records `status='skipped_manual_override'`. This is the operator's manual-override knob — no other mechanism exists in V1. The shadow file path `*.api-pending` is an internal artifact; only canonical `finvizDDMmmYYYY.csv` triggers the skip.
- **Windows `%#d` vs POSIX `%-d` filename anchor.** API-emitted CSV filename uses `action_session.strftime('%#d%b%Y')` on Windows / `'%-d%b%Y'` on POSIX — produces `5May2026`, NOT `05May2026`. Matches the `finviz_select.py` regex `(\d{1,2})([A-Za-z]{3})(\d{4})`. Hard-coding `%d` (zero-padded) breaks roundtrip parsing on the day-of-month-1-9 days.
- **CLI vs pipeline concurrency exclusion for Finviz fetches.** `_perform_finviz_fetch_no_lease` (used by `swing finviz fetch` CLI) refuses execution if a pipeline run with `state='running'` exists in `pipeline_runs` — raises `FinvizPipelineActiveError` + CLI translates to friendly `ClickException`. Pipeline-internal `_step_finviz_fetch` does NOT check (runs WHILE the lease is held). V1 cross-surface coordination is V2-hardened (unify CLI under pipeline lease).
- **Service-layer `with conn:` opens its own transaction — DO NOT call from inside an outer single-transaction; call the REPO-level function instead.** Failure mode anticipated 2026-05-06 (Phase 8 daily-management plan §A.1; binding contract in `swing/trades/daily_management.py:record_event_log`). Phase 7 introduced two call paths for `update_stop_with_event`: the REPO-level function (`swing.data.repos.trades.update_stop_with_event`) which executes plain SQL inside the caller's transaction, AND the SERVICE-level wrapper (`swing.trades.stop_adjust.update_stop_with_event`) which opens its OWN `with conn:` block + does extra orchestration. Phase 8's `record_event_log` needs both a Phase 7 stop-adjust event AND a Phase 8 event_log row to commit atomically (or both roll back) under one outer `with conn:`. Calling the service-level wrapper from inside the outer transaction would PREMATURELY COMMIT the outer block when the inner `with conn:` exits (sqlite3 connection-level transaction semantics — there is no nesting, the inner context manager commits the connection's single transaction on success). Symptom: partial state where Phase 7 event committed but Phase 8 event_log row never inserted (or vice-versa on failure-path). **Fix: when composing multiple side-effects under one atomic boundary, call the REPO-level functions directly; do NOT route through service-level wrappers that own their own transaction.** The asymmetry is intentional + must be preserved — future maintainers MUST NOT consolidate the two call paths. Pattern complement to the `INSERT OR REPLACE` cascade-wipe gotcha above — both about SQLite-runner-level transaction discipline that needs explicit code, not implicit assumptions about ORM-style atomicity. **Pre-empt in any new service composing multi-step side-effects:** writing-plans phase enumerates "calls REPO-level function from inside outer `with conn:`, NOT service-level wrapper"; add a discriminating test that injects a failure between steps + asserts FULL rollback (no partial state).
- **Python `sqlite3` `executescript()` issues an implicit COMMIT before running its script; each statement runs in autocommit mode and `conn.rollback()` cannot undo successful intermediate statements.** Failure mode surfaced 2026-05-04 (Phase 7 Sub-A R1 Major 3, fixed at `78c7005`) + reaffirmed 2026-05-07 (Phase 8 executing-plans T1.1 plan-template defect). For migrations needing partial-failure rollback, the migration-runner wrapper MUST issue explicit `BEGIN`+`executescript`+`COMMIT` with try/except `rollback()`+re-raise — `swing/data/db.py:_apply_migration` is the canonical implementation post-`283d4fa` hotfix (which also added foreign_keys=OFF discipline at the runner level). Test rollback semantics through the actual runner path, NOT bare `executescript()+rollback`; a bare `executescript()` test will assert `probe_table absent after rollback` and fail because the implicit COMMIT already persisted the row. Discriminating test pattern: malformed migration with deliberate fail-mid-sequence; assert post-failure that the runner rolled back AND `conn.in_transaction == False`. **Forward-relevance:** Phase 9/10 schema migrations + Schwab API integration if it adds tables + any future migration-runner work inherits this discipline.
- **`in_transaction` "auto-detect outer transaction" guards in transactional services re-introduce the very race the explicit lock was meant to close — prefer "reject + simple contract" over "auto-detect + complicate".** Lesson from Phase 8 `record_event_log` Codex R3→R4 cascade (2026-05-07). Initial implementation used Python's `with conn:` (deferred transaction); R3 added explicit `BEGIN IMMEDIATE` to close the stale-prior_stop race; R4 caught that the `in_transaction` guard added in R3 actually re-introduced the race because the guard's "auto-detect outer transaction" logic could deceive a caller into thinking they were inside the lock when the connection's deferred transaction had not yet escalated. Final contract for any single-transaction service: **caller MUST NOT hold an open transaction; function ALWAYS owns BEGIN IMMEDIATE / COMMIT / ROLLBACK; reject (don't auto-detect) caller-held transactions.** Pre-empt in any new transactional service via writing-plans §5: rejection-on-caller-tx contract explicit + discriminating test asserts function raises if called inside open transaction. Pattern complement to the `Service-layer with conn:` gotcha above + the Finviz `Repo functions must NOT call conn.commit()` lesson — three-piece transactional discipline family (caller-controlled at repo layer; transaction-owning at service layer; reject-caller-held-tx at any new single-transaction service).
- **Session-anchor read/write mismatch (forward-looking `action_session_for_run` vs backward-looking `last_completed_session`) silently invisibles UI display.** Recurring family — three confirmed instances: (1) weather lookup on read-only UIs (existing CLAUDE.md gotcha "Weather lookup in read-only UIs must NOT query by `action_session`"); (2) Phase 8 daily-management badge predicate (Codex R1 Major #1 fix 2026-05-09 in polish bundle commit `cfacbc5`; brief-locked the read predicate to `action_session_for_run(now())` but writer stamps `last_completed_session(now()).isoformat()` per spec §4.5 — the two diverge on weekends/holidays/evenings/pre-market and the badge would have stuck at "pending" immediately after a successful operator submit); (3) the broader exchange-session helper family lesson. **The pattern:** writer uses backward-looking `last_completed_session(now())` (the session that just closed) to stamp `review_date` / `data_asof_date` / similar; reader on the dashboard / VM / route uses forward-looking `action_session_for_run(now())` (the next session being prepped); query silently returns None → UI shows STALE / not-yet / empty despite a fresh write moments earlier. **Pre-empt in any new UI predicate on session-keyed columns** (`weather_runs.data_asof_date`, `daily_management_records.review_date`, `evaluation_runs` / `pipeline_runs` anchors): orchestrator brief MUST cross-reference the writer's session-anchor function as a binding §5 watch item; do NOT lock the read predicate from orchestrator mental model alone — read the writer's code to verify. **Add a discriminating round-trip integration test** that writes a row + immediately reads via the UI predicate to verify visibility flips correctly (per the `cfacbc5` fix's `test_post_event_log_then_dashboard_badge_flips_to_logged` pattern).
- **For any V1 single-operator form with hidden audit fields (timestamps, session anchors, precision-tier metadata), default to SERVER-STAMPING at handler entry; hidden inputs are tampering surfaces.** Lesson from Phase 8 daily-management Codex R2.M2 + R3.M2 + R4.M2 (2026-05-07). The daily-management event-log POST handler started with hidden inputs for `created_at`, `review_date`, `data_asof_session`, `mfe_mae_precision_level`. R2 caught that a stale form (page open across sessions) or tampered POST would persist incorrect timestamps; R3 caught the same for session anchors; R4 caught it for the precision tier (a tampered POST could persist `intraday_exact` audit metadata that doesn't match the actual data source). **Fix pattern: server-stamp at handler entry → remove hidden inputs from form → expose computed values as display-only `<span class="muted">` text** (operator sees what will be persisted; cannot tamper). Phase 8 closed five such holes in one phase. **Pre-empt in any new form-driven route:** writing-plans §5 watch item — enumerate every hidden input + per-field disposition (operator-supplied / server-stamped / display-only / removed). Pattern complement to existing Phase 5 R1 M1+M2 HTMX failure surfaces — both about form-driven routes having browser-only failure modes that TestClient cannot detect.
- **Phase 9 risk_policy ratification is single-fire at v16→v17 transition; subsequent `swing db-migrate` runs SKIP to avoid clobbering operator's CLI policy edits.** Failure mode anticipated 2026-05-12 (Phase 9 Sub-bundle A Codex R3 Major #1; banked from Sub-bundle A return report §11). The post-migration helper `swing/trades/risk_policy.py:ratify_seed_from_cfg_on_v17_landing` supersedes the hard-coded SQL seed values with cfg-derived values (4 spec §3.1.3 SEED MAP fields: `capital_floor_constant_dollars`, `scratch_epsilon_R`, `low_sample_size_threshold_class_a_n`, `bootstrap_resample_count`) — but ONLY ONCE, gated on `pre_version <= 16 AND post_version >= 17`. Re-firing on a subsequent `db-migrate` would clobber operator's intentional `swing config policy set` edits made between the failed migration and the repair. **Repair path: per-field `swing config policy import-from-toml --field <name>`** covering all 4 ratifiable fields. **Pre-empt in any future migration that introduces a cfg-derived seed:** writing-plans §5 watch item — enumerate the ratification gate predicate explicitly + add a discriminating test that runs db-migrate twice + asserts second run is a no-op (no new policy_id; no WARNING fires). Pattern complement to the `INSERT OR REPLACE` cascade-wipe gotcha + the `executescript` implicit-COMMIT gotcha — three-piece migration-runner discipline family.
- **All four cascade emitters MUST do the no-op-skip check before invoking `supersede_active_policy`; future emitters MUST mirror it.** Failure mode anticipated 2026-05-12 (Phase 9 Sub-bundle A Codex R3 Major #3 + R4 Major #1; banked from Sub-bundle A return report §11). The four V1 emitters of risk_policy supersession are: (1) CLI `swing config policy set`; (2) CLI `swing config reset`; (3) web `POST /config`; (4) web `POST /config/reset/{field}`. EACH MUST compare the would-be value to the active policy's current value + skip supersession when identical — otherwise a no-op operator action creates a spurious policy_id with no audit value + clobbers `effective_to` on the predecessor. Codex caught one emitter at R3 (CLI set + reset + web reset) + a fourth at R4 (web config_save not yet covered). The pattern is per-emitter, not centralized — future emitters (V2 web bulk form; V2 import path; future scripted automation) MUST mirror it. **Pre-empt in any new cascade emitter:** writing-plans §5 watch item — enumerate the no-op-skip check + add a discriminating test that submits an identical-value POST/CLI + asserts no new policy_id was created. Discriminating test pattern: `test_<emitter>_no_op_value_does_not_supersede` (Phase 9 codifies 4 of them).
- **Form-render hidden anchors driving POST-time validation MUST round-trip through soft-warn confirm `form_values` dict — otherwise tampered `force=true` resubmits silently bypass the validation.** Failure mode discovered 2026-05-12 (Phase 9 Sub-bundle D Codex R3 Critical #1; recon doc at `docs/phase9-bundle-D-task-D0-recon.md` §6.3). Phase 9 Sub-bundle D added `sector_industry_evaluation_run_id` hidden form anchor (mirroring chart_pattern's `chart_pattern_classification_pipeline_run_id`) to anchor POST-time sector/industry tamper detection. The soft-warn confirm fragment that fires when operator hits the soft cap (per Phase 8 + Phase 4-7 cumulative discipline) iterates a `form_values` dict to emit hidden inputs that the operator's `force=true` resubmit replays. **If a new hidden anchor is added to the form-render path but NOT added to `form_values`, the confirm-fragment resubmit drops it → POST handler sees no anchor → tamper validation silently passes.** Cumulative round-trip requirement: every form-render-time hidden anchor that drives a POST-time validation (chart_pattern's `pipeline_run_id`; sector/industry's `evaluation_run_id`; hypothesis_label; origin; sector/industry; future cases) MUST be added to the soft-warn confirm `form_values` dict. **Pre-empt in any new hidden-anchor design:** writing-plans §5 watch item — enumerate every hidden form anchor + verify each is in the soft-warn confirm `form_values` dict; add a discriminating regression test that simulates a tampered `force=true` resubmit + asserts rejection. Pattern complement to the Phase 5 HTMX failure surfaces (HX-Request propagation + HX-Redirect-vs-303-swap + HX-Redirect-target-unrouted) — all in the same form-driven-route browser-only failure family.
- **POST-time recompute of "latest-of-something" creates a GET/POST TOCTOU window — prefer form-render-emitted hidden anchors over POST-time recomputes for any value the operator saw at form-render time.** Failure mode discovered 2026-05-12 (Phase 9 Sub-bundle D Codex R2 Major #1; recon doc at `docs/phase9-bundle-D-task-D0-recon.md` §6.1). Sub-bundle D's initial plan keyed POST-time tamper validation on `(ticker, action_session_for_run(now()))` — recomputing the action_session at POST time. Two failure modes: (a) **form-render anchor drift**: the form-render's anchor (`latest_evaluation_run_id`) may not be today's action_session (stale-pipeline scenario; no pipeline ran today yet); POST-time lookup on today's session yields no row; tamper check silently skipped. (b) **GET/POST TOCTOU drift**: a fresh pipeline run landing between GET (form render) + POST (operator submit) shifts the authoritative "latest" candidate; POST-time recompute compares against the NEW row not the one the operator saw. **Resolution: form-render emits hidden anchor; POST validates against that exact anchor's row.** Bundle D pivoted to chart_pattern-mirror pattern: `sector_industry_evaluation_run_id` hidden input on form-render → POST `WHERE evaluation_run_id = ? AND ticker = ?` lookup → tampered/stale anchor rejected with descriptive error. **Pre-empt in any future POST-time "latest-of-something" validation:** writing-plans §5 watch item — if the comparison target was visible to operator at form-render time, the anchor MUST be a form-render-emitted hidden input, NOT a POST-time recompute. Pattern complement to the "Session-anchor read/write mismatch" gotcha (forward-looking `action_session_for_run` vs backward-looking `last_completed_session`) — both about value-stability at write time vs read time.
- **Tests that exercise `swing/config_user.py:write_user_overrides` MUST monkeypatch BOTH `USERPROFILE` AND `HOME` env vars before invoking — `_user_home()` reads them unmonkeypatched and writes leak to the operator's REAL `~/swing-data/user-config.toml`.** Failure mode surfaced 2026-05-12 (Phase 9 Sub-bundle A mid-Codex-R1 incident; banked from Sub-bundle A return report §11 + implementer-side fix across 5+ test files). `_user_home()` (Windows + POSIX implementation in `swing/config_user.py`) consults `os.environ` directly. Any test fixture exercising the `write_user_overrides` path (T-A.5 cfg-cascade tests; T-A.6 CLI tests; T-A.5 web lifespan tests; any future Phase-5-cfg-cascade test; any future Phase-9-TOML-divergence-repair test) MUST `monkeypatch.setenv("USERPROFILE", str(home))` AND `monkeypatch.setenv("HOME", str(home))` BEFORE the test invokes the write path. Symptom of forgetting: operator's REAL user-config.toml accumulates test pollution (stripped comments + spurious field values) requiring manual restoration. **Pre-empt in any new test fixture:** brief / plan §5 watch item — when authoring tests for user-config.toml write paths, monkeypatch USERPROFILE + HOME at fixture setup; assert post-test that the path is the tmp-path, not the operator's home. Related V2 hardening: `_user_home()` could accept a dependency-injected `home_path` parameter to make the pattern less footgun-prone.
- **Schwab/TOS Account Order History uses multi-line order groups, NOT single-row entries — a header row carries `Type=MKT GTC WORKING/WAIT TRG` with `PRICE=~` (placeholder) + N continuation rows (blank `Time Placed`) supply the actual STP trigger price + `STP STD` qualifier.** Failure mode anticipated 2026-05-12 (Phase 9 Sub-bundle B operator-witnessed gate finding; closed Phase 9 Sub-bundle E T-E.3 at `2107df2`). Existing Bundle B parser at `swing/journal/tos_import.py:extract_stop_orders` was written against the single-row synthetic test fixture (one row with `Type=STP` directly) and missed every real-world working stop in operator's 4 sample exports — emitting 5 false-positive `stop_mismatch` discrepancies during Sub-bundle B + C gates (DHC/YOU/VSAT/CVGI/LAR — stops ARE placed at Schwab matching journal values exactly). **Pattern variants observed in operator's exports** (recon doc `docs/phase9-bundle-E-task-E3-parser-recon.md`): (a) simple absolute — header + `RE #<id> ... <price> STP STD`; (b) no-RE# — header + `,,,,,,,,,,,<price> STP STD,` (blank Notes); (c) `TRG BY` chain — header + `RE #<id> ... <price> STP STD` + `TRG BY #<other_id>` (no price); (d) `BASE-X.XX` conditional — header + `TRG BY #<id> ... BASE-6.74 STP STD` + `,,,,,,,,,,,20.51 STP,` (absolute trigger on second continuation; `BASE-` prefix is the conditional reference base — IGNORE it; the next NUMERIC STP price wins). **Status filter widening:** include both `WORKING` AND `WAIT TRG` (placed-but-not-yet-armed); exclude `CANCELED` and `FILLED`. **Pre-empt in any future broker-CSV parser work** (Schwab API V2; future broker integrations): writing-plans §5 — DO NOT assume single-row format; sample 4+ real-world exports + cover empty-section, `WAIT TRG`, `BASE-` conditional, `TRG BY` chain, no-`RE#` continuation, multi-section interleaving; preserve backwards-compat with single-row synthetic fixtures so existing Bundle B `test_stop_mismatch_*` tests stay green; sanitize account number (e.g., `27097300SCHW` → `<account>`) in any tracked fixture copies (privacy). Fixture corpus at `tests/fixtures/tos/schwab-real-world-*.csv` covers the 4 patterns + the empty-Account-Order-History edge case.
- **Sub-bundle B `34be84e` defect family — schwabdev camelCase kwarg discipline.** schwabdev's `Client.account_orders(maxResults=...)`, `Client.price_history(periodType=, frequencyType=, startDate=, endDate=, ...)`, `Client.account_details(fields='positions')`, `Client.transactions(types=[...])` all use camelCase kwargs (NOT snake_case as Python convention would suggest). Wrappers MUST pin the exact signature via `inspect.signature(schwabdev.Client.X)` discriminating tests landing BEFORE wrapper code. Cassette tests will NOT catch this — they stub the entire schwabdev call (any kwargs accepted). Reference fix at `34be84e`; pre-emption pattern at `tests/integrations/test_schwab_trader_kwarg_signatures.py` + `test_schwab_marketdata_kwarg_signatures.py`.
- **Typed `SchwabApiError` audit-row close discipline (per Sub-bundle B R1 M#3 + return report §9 #5).** All schwabdev API call wrappers MUST close `schwab_api_calls` audit rows via `record_call_finish(status='auth_failed' | 'rate_limited' | 'error', error_message=<redacted_excerpt>, ...)` BEFORE re-raising the typed exception. Pattern: classify the exception via `_classify_schwab_error(exc)` → call `record_call_finish` → re-raise. Audit log stays honest about both success + failure outcomes; degraded-health surfaces (briefing banner + status CLI) read these rows.
- **`swing schwab setup` requires clean tokens DB state** (Sub-bundle C operator-paired gate observation 2026-05-14). schwabdev's `Client.__init__` auto-attempts a refresh against any EXISTING tokens DB and hard-fails if that refresh dies (e.g., 7-day refresh-token expired) — never reaching the paste-back code path. **Recovery sequence is `swing schwab logout` → `swing schwab setup`, NOT `setup` standalone.** `logout` atomically renames the stale tokens DB to `*.deleted-<timestamp>` (24h recovery window) per A T-A.5 design even when revoke-best-effort fails; `setup` then runs against the now-empty path with clean paste-back. **V2 candidate (banked):** make `setup` self-healing — detect-and-rename stale tokens DB itself before invoking schwabdev.
- **7-day Schwab refresh-token clock requires periodic re-auth** (per `reference/schwabdev/client.md` L255-265 + Sub-bundle A T-A.0.b §6.bis). The refresh_token's TTL is fixed at 7 days from initial OAuth paste-back; no programmatic extension exists in schwabdev V1 (Q15 disposition). **Recovery sequence is the same `logout → setup` pattern.** `swing schwab status` surfaces days-remaining indicator with severity escalation (≤24hr WARN; ≤2hr ERROR + bold red); briefing.md banner emits "Schwab integration: degraded" if any V1-recent (e.g., last 3 calls) `schwab_api_calls.status != 'success'`. Cycle-checklist documents the weekly re-auth reminder.
- **schwabdev 2.5.1 `"Schwabdev"` capital-S logger prefix** (Sub-bundle A T-A.10 D1). The actual logger name in schwabdev 2.5.1 is `"Schwabdev"` (capital S), NOT lowercase `"schwabdev"` as plan §H.8 originally documented. Three-layer redaction (Layer 0 known-secret exact-replace + Layer 1 heuristic regex + Layer 2 `setLogRecordFactory`) MUST use `_SCHWABDEV_LOGGER_PREFIX = "Schwabdev"` for the Layer 2 prefix-check; without capital S, schwabdev logger records would silently slip through Layer 2 redaction. Discriminating test pattern: emit a sentinel via `Schwabdev`-named logger (NOT a parent/child); assert sentinel absent from caplog.
- **schwabdev silent-failure-mode discipline — `update_tokens()` does NOT raise on auth failure** (Sub-bundle A T-A.0.b phase-2 §6.bis + return report §8 lesson #1). schwabdev's `Client.__init__()` AND `update_tokens(force_access_token=True)` both PRINT-and-RETURN-silently on auth failure rather than raising an exception. Wrappers MUST verify post-call state (`client.tokens.access_token` populated AND rotated) + raise `SchwabAuthError` on silent failure. **Note:** `force_refresh_token=True` (NOT `force_access_token=True`) triggers a FULL OAuth dance via `input()` prompt — semantically different; the silent-rotation kwarg is `force_access_token=True`.
- **Schwab tokens DB stored at `~/swing-data/schwab-tokens.{env}.db`; plaintext OAuth state at rest V1.** Per-environment SQLite databases managed by `schwabdev`'s `Tokens` class hold `client_id` + `client_secret` + `access_token` + `refresh_token` as plaintext columns. ACL via Windows user-profile inheritance OR POSIX `chmod 600` is the only V1 protection. Filesystem-level threat model: anyone with read access to the file can impersonate the operator's Schwab API session until refresh_token TTL expires or operator revokes via Schwab Developer Portal. `.gitignore` patterns at `swing-data/schwab-tokens.*.db` (+ `-journal`/`-shm`/`-wal` SQLite-internal files) cover the file family. V2 hardening (post-Phase-11 dispatch): schwabdev's optional `encryption=<key>` constructor parameter wraps the DB in Fernet AES-128 encryption-at-rest; operator-paired key management.
- **Schwab CLI subcommands that write to `account_equity_snapshots` / `reconciliation_runs` REFUSE while pipeline is `state='running'`.** Mirror of Finviz `FinvizPipelineActiveError` precedent. Affected subcommands: `swing schwab fetch --snapshot|--orders|--all`; also `setup`/`logout` (without `--force`). Safe subcommands: `status` (read-only); `refresh` (tokens DB only); `fetch --verify-marketdata` (no cache writes). CLI surface checks `SELECT run_id FROM pipeline_runs WHERE state = 'running' LIMIT 1`; raises `SchwabPipelineActiveError` if non-empty. Discipline reason: concurrent pipeline + CLI snapshot UPSERTs both UPDATE the same `(snapshot_date, source='schwab_api')` row with `source_artifact_path` overwriting last-writer-wins; audit row pairings drift.
- **Schwab API integration writes domain rows ONLY when `cfg.integrations.schwab.environment == 'production'`.** Under `environment='sandbox'`, the pipeline + CLI `fetch` subcommands invoke schwabdev API calls + write `schwab_api_calls` audit rows + capture `signature_hash` for drift detection — but the `record_snapshot(source='schwab_api', ...)` + `run_schwab_reconciliation(...)` calls are SHORT-CIRCUITED (audit row written; domain row not written; audit row's `linked_*_id` remains NULL). Market-data ladder also short-circuits under sandbox — `PriceCache`/`OhlcvCache` fall through directly to yfinance. Rationale: sandbox-environment responses contain synthetic Schwab data; without the gate, the source-ladder would have Schwab winning over manual/TOS and silently contaminate production metrics (Phase 10 LIVE-badge equity), reconciliation discrepancies, and cohort analysis. Discriminating tests: `test_schwab_sandbox_gating.py:test_snapshot_under_sandbox_writes_audit_skips_domain` + `test_orders_under_sandbox_writes_audit_skips_domain` + `test_marketdata_cache_under_sandbox_falls_through_to_yfinance`.
- **schwabdev's own loggers must be covered by content-redacting `logging.setLogRecordFactory` wrapper (NOT level suppression and NOT a logger-attached `logging.Filter`).** Level-suppression via `_suppress_transport_debug_logs()` only mutes DEBUG-level urllib3 lines; schwabdev's `tokens.py` around line 338 logs `response.text` on auth failure at WARNING/ERROR level (per spec §A.2 caveat), which level-suppression does NOT catch. A `logging.Filter` attached to ancestor loggers does NOT fire for records emitted by child loggers during propagation (per Python `Logger.callHandlers()` semantics — only ancestor HANDLERS run on propagated records, not ancestor filters). The project installs `_schwab_record_factory` via `logging.setLogRecordFactory()` ONCE per process at SchwabClient construction; the factory mutates `LogRecord.msg` for records whose name starts with `schwabdev` at record-CREATION time, BEFORE any handler/logger sees it. The factory redactor scrubs the LogRecord via a process-global registry that (a) exact-replaces 5 long-lived known-secret slots: `client_id`, `client_secret`, `access_token`, `refresh_token`, `account_hash` (`authorization_code` covered separately by non-leak test since it is paste-back-only inside schwabdev's `manual_flow`); and (b) applies heuristic regex for 32+ hex-char and 24+ base64-char sequences as defense-in-depth. Thread-local recursion guard (`_FACTORY_DEPTH.in_call`) defends against third-party LogRecord factory chaining patterns that could otherwise infinite-recurse. `ensure_schwab_log_redaction_factory_installed()` is called by SchwabClient before every schwabdev API call to detect + recover from third-party factory replacements. Sentinel-leak audit test at `tests/integrations/test_schwab_token_redaction_audit.py` plants NON-token-shaped sentinels at all 5 long-lived slots + greps all log records (including schwabdev's WARNING/ERROR-level output) + all audit `error_message` columns + stdout + stderr; ZERO matches asserted. Separate authorization-code non-leak test asserts no `code=...` substring in any captured surface.
- **Schwab cassette runbook is V2 PLANNED — V1 ships mock-based tests only.** No Schwab cassettes are recorded as of Sub-bundle D ship — `tests/integrations/cassettes/` contains finviz fixtures only; existing Schwab tests use `MagicMock`-driven fixtures (see `tests/integrations/test_schwab_pipeline_steps.py` + `tests/integration/test_schwab_full_happy_path.py` patterns). When a future V2 dispatch lands cassette-recorded `test_schwab_*_live.py` slow-marked tests + cassette infrastructure (operator-paired session required for live Schwab API access), this entry expands to cover staleness recovery: re-record per the relevant V2 plan; cassette filter-list must redact `authorization` header + OAuth query/POST params (`code`, `refresh_token`, `client_id`, `client_secret`, `redirect_uri`) + response-body `access_token` / `refresh_token` / `accountNumber` / `accountHash` substrings + 32+ hex-char token-like sequences.
- **Schwab API source-artifact reference shape locked.** Every `account_equity_snapshots` row written via `source='schwab_api'` carries `source_artifact_path = "schwab_api:call/{call_id}"` (URI-style opaque reference to the corresponding `schwab_api_calls` audit row). Every `reconciliation_runs` row written via `source='schwab_api'` carries the same URI shape PLUS the FK column `schwab_api_call_id`. Reverse pointer: `schwab_api_calls.linked_snapshot_id` (FK to `account_equity_snapshots(snapshot_id)`; `ON DELETE SET NULL`) + `linked_reconciliation_run_id` (FK to `reconciliation_runs(run_id)`; `ON DELETE SET NULL`). Provenance chase is bidirectional. Do NOT use the raw Schwab API URL (contains `account_hash` segment).
- **Schwab OAuth web setup flow (Phase 12 Sub-bundle B T-B.4) — Option A paste-back form at `/schwab/setup`; Outcome B manual token exchange.** Web counterpart to `swing schwab setup` CLI lives at `GET /schwab/setup` + `POST /schwab/setup` (registered in `swing/web/routes/schwab.py`; mounted in `swing/web/app.py`). HTMX requirements: (a) embedded form MUST carry `hx-headers='{"HX-Request": "true"}'` to propagate the HX-Request header under OriginGuard strict-mode (existing Phase 5 R1 M1 gotcha precedent); (b) success-path response is `204 No Content` + `HX-Redirect: /config?schwab_setup=ok` per Phase 5 R1 M2 (will retarget to `/schwab/status` if/when a T-B.7 follow-up dispatch lands a web status page; this is V2 candidate). T-A.2 self-healing applies identically via the shared `setup_paste_flow_with_callback_url` helper in `swing/integrations/schwab/auth.py` (auto-rename of stale tokens DB to `*.deleted-<ts>` before token write; no separate web-side rename code). **Outcome B (manual token exchange)** chosen over Outcome A (schwabdev `Tokens.update_tokens` invocation): the route handler extracts `code=` query param server-side from the pasted callback URL + POSTs to schwabdev's `/v1/oauth/token` byte-for-byte mirroring `schwabdev.Tokens._post_oauth_token` (Basic auth header from `client_id:client_secret`; form body with `grant_type=authorization_code` + `code` + `redirect_uri`); tokens file is written atomically via `os.replace` from a same-volume `.tmp` sibling (Windows cross-device-link gotcha respected). Audit row records `surface='cli'` (same value the CLI path uses — V2 candidate banked for distinguishable `surface='web'`). Browser never sees token bytes after submit. Pre-empt in any new `/schwab/*` route: enumerate (a) HX-Request propagation on embedded forms; (b) HX-Redirect success path (NOT 303 swap-target); (c) HX-Redirect target route registered in app routes (Phase 6 I3 gotcha); (d) `surface='cli'` audit-row consistency unless intentionally diverging.
- **Schwab CLIENT_ID + CLIENT_SECRET storage (Phase 12 Sub-bundle B T-B.2).** Persistent storage lives in `%USERPROFILE%/swing-data/user-config.toml` under `[integrations.schwab]` (mirrors the existing `Finviz Elite API token storage` precedent). Tracked `swing.config.toml` MUST NOT contain the values (sensitive). Resolution cascade is **env vars (`SCHWAB_CLIENT_ID` + `SCHWAB_CLIENT_SECRET`) > cfg fields (`integrations.schwab.client_id` + `integrations.schwab.client_secret`) > interactive prompt** — three tiers, highest-wins. **Partial env-tier raises** by design (Sub-bundle A T-A.1 LOCK preserved — only one of the two env vars set raises `SchwabConfigMissingError` rather than silently falling through; prevents accidental shell mis-export from leaking into cfg/prompt path). **Partial cfg-tier falls through** to prompt (Sub-bundle B T-B.1 LOCK — file-tier is operator-friendly; one missing field of two doesn't poison the cascade). `swing config show` surfaces both fields masked (`****` redaction via `FIELD_REGISTRY` masked-entry metadata; Sub-bundle B T-B.2). `.gitignore` already covers via the broad `swing-data/` rule (line 11) — confirmed via `git check-ignore`. The OAuth `refresh_token` is a SEPARATE concern (rotates weekly; stored in `~/swing-data/schwab-tokens.{env}.db` per existing Schwab gotcha entry); only the app-level credentials are stable + cascade-eligible.
- **Migration runner backup-gate equality form: `pre_version == (target - 1)` strict equality, NOT `pre_version <= (target - 1)`.** Failure mode anticipated 2026-05-15 (Phase 12 Sub-sub-bundle C.A dispatch brief §0.5 deviation banked at plan §I; future migration dispatches inherit). The backup-gate condition that fires the `swing-pre-<phase>-migration-<ISO>.db` backup file must use STRICT EQUALITY on `pre_version`. The `<=` form fires for any pre-version up to and including `(target - 1)`, which means a v17 → v19 jump would ALSO fire the gate but skip the v17 → v18 boundary's own gate (if that boundary's gate had different naming convention). Strict equality fires the gate ONLY at the intended `(target - 1) → target` boundary; multi-version jumps must be a separate two-step migration. **Pre-empt in any new migration dispatch:** copy the Phase 9 Sub-bundle A backup-gate clause VERBATIM (`pre_version == 16 AND target >= 17` precedent shape; substitute current target); do NOT paraphrase to `<=`. Brief-drafting checklist: when authoring migration dispatch briefs, search for `pre_version` references + verify strict equality form is used. Pattern complement to the existing `executescript()` implicit-COMMIT gotcha + the `INSERT OR REPLACE` cascade-wipe gotcha — three-piece migration-runner discipline family.
- **Schema-CHECK + Python-constant + dataclass-validator MUST land in the same task for atomic consistency.** Failure mode anticipated 2026-05-15 (Phase 12 Sub-sub-bundle C.A writing-plans plan §B.2 deviation banked + return report §11.#2; corrected by folding `_RESOLUTION_VALUES` widening inline with T-A.1 schema CHECK widening). When a schema element widens (CHECK enum adds values; column type changes; new constraint added) AND the system has a corresponding Python-side constant OR dataclass validator OR repo-level type-narrowing, the schema change + Python change MUST land in ONE atomic task/commit. Splitting them across tasks creates an inconsistent-system-state window: the schema accepts the new value but Python rejects it (or vice versa); tests running between the two tasks have undefined behavior. **Pre-empt in any new schema-widening dispatch:** 1 task = (a) schema CHECK / column change + (b) Python constant or enum widening + (c) dataclass `__post_init__` validator + (d) all three discriminating tests landing in the SAME commit. Adversarial-review watch item: enumerate every Python-side surface that mirrors a schema constraint + ensure each is in the same task. Pattern complement to existing `Python ... or "" idiom collides with SQL CHECK-constraint nullability` gotcha + `SQLite INSERT OR REPLACE` gotcha — same family of schema-vs-Python consistency discipline.
- **Schema-coverage Python constant is NOT necessarily the manual-input allowlist — when widening a CHECK enum, audit every existing Python-side surface that validates against the constant + isolate the manual operator-input surfaces from service-only values.** Failure mode discovered 2026-05-16 (Phase 12 Sub-sub-bundle C.C Codex R1 Major #4 + R2 Major #1; orchestrator-context.md Lessons captured 2026-05-16). C.A widened the schema `reconciliation_discrepancies.resolution` CHECK 5→9 values. The C.A T-A.2 in-tree fix widened `_RESOLUTION_VALUES` (dataclass validator). But the shipped Phase 9 Sub-bundle B `resolve_discrepancy` CLI surface validated operator input against `RESOLUTION_TYPES` — a SECOND Python-side mirror in the same repo module. R1 M#4 caught the lag (CLI rejected the new service-owned values). R2 M#1 caught the FOLLOW-UP: even after widening `RESOLUTION_TYPES` to 9, the manual `resolve_discrepancy` CLI must NOT accept the 4 service-owned states (`auto_corrected_from_schwab` / `pending_ambiguity_resolution` / `operator_resolved_ambiguity` / `operator_overridden`) — those route through canonical service entries (C.C `apply_*` functions). Resolution: introduce a separate tighter `_MANUAL_RESOLVE_ALLOWED_RESOLUTIONS` allowlist for the manual path with routing-hint substring in rejection error message. **Pre-empt in any new CHECK-enum widening dispatch:** `grep -rn '<constant-name>' swing/` to find ALL Python-side mirrors; audit each callsite as either (a) manual operator-input (needs tight allowlist if service-owned values exist) OR (b) service-only (full widened constant); add discriminating tests per service-owned value asserting the manual surface rejects with routing-hint substring. Pattern complement to the existing schema-CHECK-vs-Python-constant gotcha above — applied to MULTIPLE Python mirrors in different surfaces.
- **Outer-tx transactional discipline MUST be uniform regardless of environment — short-circuit (sandbox / dry-run / read-only) MUST live in the inner (caller-tx) function, NOT the outer (own-tx) function.** Failure mode discovered 2026-05-16 (Phase 12 Sub-sub-bundle C.C Codex R1 Major #1; orchestrator-context.md Lessons captured 2026-05-16). Initial `apply_tier1_correction(env='sandbox')` short-circuit at the OUTER function (before `conn.execute('BEGIN IMMEDIATE')`) caused two failure modes: (a) outer-skip bypasses the `if conn.in_transaction: raise CallerHeldTransactionError` check — a caller holding an open tx + invoking under sandbox would silently no-op; (b) outer-skip lets nonexistent `discrepancy_id` succeed as a no-op (silent error; caller's bug not surfaced). **Fix pattern:** outer ALWAYS issues `BEGIN IMMEDIATE` → calls inner → `COMMIT` (or `ROLLBACK` on exception); inner short-circuits internally and returns no-op `Result(value=None, notes='sandbox: ...')`. Discriminating test pattern: `_ExecuteSpyConn` wrapper that records `conn.execute` calls + asserts `"BEGIN IMMEDIATE"` was issued under sandbox + caller-held-tx still raises. **Pre-empt in any new transactional service with environment-specific behavior:** the transactional boundary (reject caller-held / BEGIN IMMEDIATE / COMMIT / ROLLBACK) is UNIFORM; environment-specific behavior belongs inside the inner function. Pattern complement to existing `in_transaction` safety-guard anti-pattern + `Service-layer with conn:` gotcha — three-piece transactional discipline family.
- **SELECT-first idempotency MUST precede payload validation — terminal-state discrepancy returns existing audit-row id even with stale/malformed/None payload.** Failure mode discovered 2026-05-16 (Phase 12 Sub-sub-bundle C.C Codex R1 Major #2; orchestrator-context.md Lessons captured 2026-05-16). Initial `_apply_tier1_correction_inner` validated `classification` payload BEFORE SELECTing the discrepancy's current resolution. Re-invoking with a terminal-state discrepancy (e.g., `resolution='auto_corrected_from_schwab'` from a prior idempotent apply) + stale/malformed/`classification=None` payload would raise on payload validation instead of returning the idempotent existing-correction lookup. Idempotency is about consistency on re-invocation; payload validation should only fire when state actually requires a new write. **Fix pattern:** reorder service inner-function steps to: (1) sandbox short-circuit (no state needed); (2) SELECT discrepancy + None-check; (3) terminal-state idempotent return; (4) payload validation; (5) remaining atomic flow. Discriminating test: invoke with terminal-state discrepancy + `classification=None`; assert existing `correction_id` returned + no exception raised. **Pre-empt in any new service with an idempotency contract:** writing-plans §5 watch item — enumerate the inner-function step order explicitly + add a discriminating test asserting idempotent re-invocation works under stale-payload conditions. Pattern complement to the Phase 8 `record_event_log` transactional discipline lesson family — same family applied to step-ordering within inner-function flows.
- **Counter staleness after inline state mutation — any flow that emits rows incrementing counters AND later mutates those rows' states (resolve / dispose / forward-to-another-state) MUST either decrement counters per-mutation OR recompute via `SELECT COUNT(*)` post-loop.** Failure mode discovered 2026-05-16 (Phase 12 Sub-sub-bundle C.C Codex R1 Major #3; orchestrator-context.md Lessons captured 2026-05-16). The Phase 9 `run_*_reconciliation` flow emits discrepancy rows + increments `summary_json.unresolved_discrepancies_count` per emit. C.C's flow pivot then mutates those rows' states inline (`unresolved` → `auto_corrected_from_schwab` / `pending_ambiguity_resolution`) during the dispatch loop. The emit-time counter is now stale. Two resolution patterns work: (a) decrement counter per-mutation inside the loop (matches legacy `resolve_discrepancy` pattern); (b) recompute via `SELECT COUNT(*) WHERE resolution = 'unresolved'` post-loop (simpler + more robust; C.C R1 M#3 choice). Discriminating-test pattern: plant N discrepancies (mix of tier-1 + tier-2 outcomes) → run pivot → assert run's counter equals actual post-pivot count via independent SELECT, NOT N. **Pre-empt in any new flow that emits + mutates rows under the same outer transaction:** writing-plans §5 watch item — enumerate every counter incremented at emit time + verify post-mutation recompute OR per-mutation decrement is in place. Pattern complement to the existing 'Queries ordered by `started_ts DESC` on `pipeline_runs` mask prior completes mid-run' gotcha — both about state-coherence-after-multi-step-flow discipline.
- **Tier-1 auto-correct service inherits sandbox short-circuit gating from Phase 11.** Same pattern as `_step_schwab_snapshot` + `_step_schwab_orders` — under `environment='sandbox'`, domain rows are NOT written; audit rows ARE written. The `apply_tier1_correction` outer function accepts `environment` keyword + short-circuits at the inner function (NOT outer — per C.C lesson #2). Reconciliation flow pivot at `run_schwab_reconciliation` + `run_tos_reconciliation` honors this end-to-end. Discriminating test pattern: invoke service with `environment='sandbox'` + assert no journal mutation + assert WARNING log + assert `CorrectionResult.correction_id is None`.
- **`reconciliation_corrections` is APPEND-ONLY; tier-3 chains add NEW rows pointing back; never DELETE or UPDATE-in-place.** The override chain pattern (`tier-1 → tier-3 → tier-3-of-tier-3`) extends by INSERTing a new row + UPDATEing the PRIOR row's `superseded_by_correction_id` to point forward. The most-recent (chain-head) row has `superseded_by_correction_id IS NULL`. Operator-facing CLI rejects `override-correction` against an already-superseded row (per OQ-15 disposition); points operator at the current chain head's `correction_id`.
- **Pass-2-tier-1-FORBIDDEN + Pass-1-tier-1 — V2-RESOLVED for Pass-1 (Sub-bundle 1 SHIPPED 2026-05-16); Pass-2 STAYS tier-2-always (V2-DEFERRED OQ-F).** Post-Phase-12 Schwab mapper execution-grain widening dispatch (spec at `docs/superpowers/specs/2026-05-17-schwab-mapper-execution-grain-widening-design.md`; plan at `docs/superpowers/plans/2026-05-17-schwab-mapper-execution-grain-widening-plan.md`) closes the Pass-1 limit-vs-fill defect family. The architectural fix lands across 4 surfaces — (a) `swing/integrations/schwab/models.py` extends `SchwabOrderResponse` with `executions: list[SchwabExecutionLeg] | None`; (b) `swing/integrations/schwab/mappers.py:map_orders_to_fill_candidates` extracts `orderActivityCollection[].executionLegs[]` with defensive parsing + mapper-coherence-check; (c) `swing/trades/schwab_reconciliation.py` comparator switches the price-path to `_compute_execution_price(so)` (single-leg → leg price; multi-leg → VWAP) + quantity-path to `_resolve_match_quantity(so)` + Path B `execution_unavailable=true` sentinel emit when executions absent + candidate-pool widening via `_is_execution_bearing_candidate` admitting MARKET fills with `price=None` + partial-then-canceled CANCELED-with-`filledQuantity>0` orders; (d) `swing/trades/reconciliation_classifier.py` NEW Shape C predicate `frozenset(source_payload.keys()) == {"price", "execution_legs", "schwab_order_id", "schwab_order_price"}` at `_classify_entry_price_mismatch` + `_classify_close_price_mismatch` → tier-1 with `correction_target={'price': execution_price}`. Pass-2 `_classify_unmatched_fill_shared` ONLY adds Path B sentinel recognition emitting tier-2 `unsupported` with clearer reason — V1 LIFT scope LOCK preserved (spec §1.5 + §6.6 OQ-F V2). **`stop_mismatch` UNCHANGED** (spec §1.1 lock #2; trigger-vs-trigger comparison is apples-to-apples). **V1 HISTORICAL CONTEXT (preserved BELOW):** the original gotcha entry documented the limit-vs-fill defect family empirically falsified twice on 2026-05-17 at Phase 12 Sub-sub-bundle C.D operator-witnessed gate (CVGI BUY @ LMT $5.30 actual fill $5.2244 → journal correctly stored $5.23 + S3a auto-correct wrote wrong $5.30 → recovery via tier-3 override-back; LION BUY @ LMT $12.75 actual fill $12.6999 → journal correctly stored $12.70 + S3b auto-correct wrote wrong $12.75 → recovery via tier-3 override-back). The pre-Sub-bundle-1 operator workflow required manual TOS Net Price audit after `reconcile-backfill --apply`, with tier-3 override-back for any wrong tier-1 correction. **Sub-bundle 1 ship makes that audit step obsolete for Pass-1** — comparator emits execution-grain price → classifier auto-corrects to execution price → no operator manual audit needed. **The historical `reconciliation_corrections` rows 1-6 from C.D gate (CVGI fill 9 correction_ids 3+4 + LION fill 15 correction_id 6) remain leave-as-is per OQ-G** — those rows carry V1-era ORDER-grain prices in `schwab_said_value_json`; the V1 historical context is documented at the `swing journal discrepancy show-correction --help` epilog (T-1.12 generic ID-free addendum) for operators reviewing pre-Sub-bundle-1 correction chains. **Pass-2 still V1 tier-2-always**: V1 mapper's execution-grain widening at Sub-bundle 1 surfaces execution-grain data WHEN PRESENT in the response, but Pass-2 backfill scenarios (where the Pass-1 emitter persisted only `{"matched": null}` to discrepancy actual_value_json) still need a Schwab re-fetch to obtain execution-grain data — and `_classify_unmatched_fill_shared` MAY NOT auto-correct from the order-grain `so.price` of Pass-2 matches without operator broker-statement consultation. OQ-F V2 dispatch will lift Pass-2 by extending the classifier to consume re-fetched execution-grain data from the same `_compute_execution_price` helper. Commit chain: initial spec `8c1e5cb` through writing-plans `cc6fd2d` through Sub-bundle 1 ship.
- **Reconciliation flow pivot uses SAVEPOINT-per-discrepancy inside the outer run transaction.** Without the savepoint, an exception AFTER a partial journal UPDATE but BEFORE the audit row INSERT would leave a silent mutation when the outer transaction commits. Pattern: `SAVEPOINT correction_sp_<discrepancy_id>` / `RELEASE` on success / `ROLLBACK TO + RELEASE` on any exception. SAVEPOINT names use the discrepancy_id to guarantee within-tx uniqueness. The auto-correction service exposes a private `_apply_tier1_correction_inner` (caller-tx) for this nested usage; the public `apply_tier1_correction` (own-tx) is for standalone CLI / backfill invocations.
- **Classifier is a PURE function — no DB writes, no Schwab API calls, no transaction management.** All I/O is in the service layer (`reconciliation_auto_correct.py`) or the backfill orchestrator (`reconciliation_backfill.py`). The classifier consumes pre-fetched payloads + journal rows; service / backfill fetch them. This is the canonical retroactive-vs-prospective-symmetric architecture per spec §13.2: a future fill-auto-population-at-entry sub-bundle can reuse `classify_discrepancy` verbatim.
- **Tier-2 CLI `--custom-value` requirement is PER-CHOICE not suffix-based.** Per spec §6.2.1 Codex R5 Major #2 LOCK: choices like `consolidate_using_operator_vwap`, `split_into_partials`, `pick_schwab_record_<N>`, `operator_truth`, `operator_alternative`, `custom` REQUIRE `--custom-value`; choices like `keep_journal_as_is`, `mark_unmatched`, `acknowledge` do NOT. CLI validates via the helper-module `get_choice_menu(ambiguity_kind)`'s `requires_custom_value: bool` field on each `ChoiceMenuItem`. The §6.2.1 table is the binding contract.
- **Windows PowerShell stdout defaults to cp1252; non-ASCII glyphs (§, →, ↔, ←, ↑, ↓, ✓, ✗, em-dash, en-dash, fractions, etc.) in any code path that flows through `print()` / `click.echo()` / direct `sys.stdout.write()` will raise `UnicodeEncodeError` and crash the CLI** (pytest captures stdout via Python-level mechanisms that don't go through the Windows encoder, so synthetic-fixture tests pass against the same code that crashes in production). Failure family surfaced 2026-05-17 at Phase 12 Sub-sub-bundle C.D operator-witnessed gate twice — first at S2 dry-run (`→` in `_format_pass_2_line` at `swing/trades/reconciliation_backfill.py:512`) + then at S5 show-ambiguity (`§` in classifier reason text at `swing/trades/reconciliation_classifier.py:173+635` + CLI rendering at `swing/cli.py:2274`). **Two-layer fix shipped at C.D gate-fix #1 + gate-fix #3**: (a) avoid the unsafe character entirely in user-facing strings that must flow through stdout — same canonical pattern as the matplotlib mathtext gotcha ("omit the metacharacter from the title format entirely"); (b) reconfigure `sys.stdout` + `sys.stderr` to UTF-8 with `errors='replace'` at CLI entry (`swing/cli.py` module load) as defense-in-depth covering future cases. **Pre-empt in any new CLI output code path:** writing-plans §5 watch item — if a print()/echo() string contains non-ASCII glyphs (§/→/etc.), prefer ASCII swap; the stdout-UTF-8 reconfigure is the safety net but ASCII is the canonical project pattern. Discriminating-test gap: pytest's `capsys` captures via Python-level pipes that bypass the OS encoder; tests that pass with `capsys` may still crash in production on Windows. Add a discriminating test that constructs a subprocess invocation through PowerShell + captures stderr to validate stdout encoding works in the operator's actual terminal.
- **Synthetic-fixture-vs-production-emitter shape drift: tests that plant discrepancies with `field_name='price'` (a real `fills` column) pass the audit-helper happy path, but production-shipped reconciliation at `swing/trades/schwab_reconciliation.py:672` emits `field_name='fill_match'` for `unmatched_open_fill` / `unmatched_close_fill` discrepancies — and `fill_match` is NOT a real column.** Failure family surfaced 2026-05-17 at Phase 12 Sub-sub-bundle C.D operator-witnessed gate S6b — `_handle_no_mutation_audit` called `_read_journal_value(field_name='fill_match')` which constructed `SELECT fill_match FROM fills WHERE fill_id=?` and raised `sqlite3.OperationalError: no such column: fill_match`. **C.D gate-fix #2 shipped** at `swing/trades/reconciliation_auto_correct.py` — `_DiscrepancyInfo` extended with `expected_value_json`; `_handle_no_mutation_audit` branches on `discrepancy_type IN ('unmatched_open_fill', 'unmatched_close_fill')` to skip the column read + use the discrepancy's `expected_value_json` directly as the no-mutation audit shape per spec §6.2.1 Codex R1 Critical #1 LOCK. **Pre-empt in any new audit-helper or service-helper that constructs SQL using `field_name` as a column identifier**: enumerate every discrepancy_type that emits the helper-target field_name + verify it's a real column on the affected table; if synthetic (discrepancy-level label like `fill_match`), branch to use the discrepancy's persisted JSON envelope instead. Discriminating-test pattern: plant a production-shape discrepancy with the synthetic field_name + assert the helper path doesn't raise + records the correct audit shape. Pattern complement to the existing Phase 8 R2-R5 family — both about TEST FIXTURE SHAPE drifting from PRODUCTION EMITTER SHAPE silently.
- **Session-anchor inequality discipline depends on anchor directionality (backward-looking vs forward-looking).** Lesson surfaced 2026-05-18 PM at Phase 13 T1.SB0 Codex R3 family + R4 minor. The existing CLAUDE.md gotcha "yfinance `history(interval="1d")` includes the in-progress bar during market hours" prescribes the strip predicate `if df.index[-1].date() >= session: df = df.iloc[:-1]` where `session = action_session_for_run(datetime.now())`. **This `>=` inequality is correct ONLY for forward-looking anchors (`action_session_for_run` - the NEXT session being prepped).** For backward-looking anchors (`last_completed_session(now())` - the session that just closed), the predicate must be STRICT `>`: the archive's last legitimate bar can equal `end` (the boundary date is INCLUSIVE for backward-looking), so `>=` would incorrectly strip a legitimate bar. Pattern: when adopting the in-progress-bar strip in a new code path, identify which session-anchor function governs the boundary date (`action_session_for_run` vs `last_completed_session`); use `>=` for forward-looking, `>` for backward-looking. Discriminating-test pattern: plant a fixture with `df.index[-1].date() == anchor_date` + verify the predicate keeps the bar for backward-looking + strips it for forward-looking. Same gotcha family as the existing "Session-anchor read/write mismatch" gotcha (third instance now with weather + Phase 8 daily-management badge + this in-progress-bar inequality) - all about getting the anchor's directionality right.
- **Hook fallback window-completeness - shared-infrastructure cache hooks return FULL archive; consumers slice.** Lesson surfaced 2026-05-18 PM at Phase 13 T1.SB0 Codex R3 Major #1. The `_yf_window_fallback` fallback path in `swing/web/ohlcv_cache.py` initially truncated to 60 rows under Schwab-fallback (the minimum needed for SMA50 in the bundle-path consumer). But the chart-step path consumer needed ~145 business days (200 calendar) for chart rendering. The hook silently capped chart window depth for any code path consuming via the fallback. Resolution: rewrite the fallback to call `read_or_fetch_archive` directly returning the full archive; consumers slice per their own window needs. **Pattern**: any cache hook in shared infrastructure (OhlcvCache, PriceCache, future caches for chart_renders / pattern_evaluations / etc.) that returns "a window of bars" MUST return ENOUGH history to satisfy ALL downstream consumers - NOT pre-truncate to one consumer's minimum. Default to "full history, consumer slices". If a consumer needs a smaller window for performance, the consumer slices AFTER receiving the full archive. Pre-empt in any new cache layer: writing-plans Section 5 watch item - verify the hook's return contract by enumerating ALL downstream consumers + their window needs; pin via discriminating test that asserts the hook returns enough history for the MAX consumer window.
- **Schwab `price_history` API defaults to `(periodType=day, period=10, frequencyType=minute, frequency=1)` when called without explicit kwargs — returning 10 DAYS OF 1-MINUTE INTRADAY BARS (~2730-3900 candles), NOT daily bars.** Lesson surfaced 2026-05-19 at Phase 13 T1.SB0 gate-fix T-GF1 recon. Pre-Phase-13, `_bars_hook` did not invoke the ladder; T1.SB0 wired `_step_charts` through OhlcvCache.get_or_fetch → ladder → `get_price_history` → `client.price_history(symbol)` WITHOUT kwargs. The Schwab API server-side defaults silently delivered minute bars. The mapper at `swing/integrations/schwab/mappers.py:817-828` converts each minute candle to OhlcvBar with date-only `asof_date`; `write_window`'s `drop_duplicates(subset=['asof_date'], keep='last')` silently overwrote daily Shape A archive content with the last-minute-of-day intraday bar. Operator's CVGI.schwab_api.parquet had 2780 rows across only 10 unique dates (~278 minute-bars per date) instead of expected ~1260 daily bars. Chart rendered 2780 candles compressed into 10 x-positions with "00:00" time-of-day labels + raw-shares volume scale. **The CLI verify path at `swing/cli_schwab.py:1100-1111` ALREADY passes `period_type='month', period=1, frequency_type='daily', frequency=1` explicitly — proving architectural intent IS daily bars.** Fix: any Schwab Market Data API consumer that wants DAILY bars MUST explicitly pass `(year, N, daily, 1)` or `(month, N, daily, 1)` kwargs. **Pre-empt in any new Schwab API consumer** (T3.SB1 + T3.SB2 entry/exit auto-fill paths inherit this discipline; writing-plans §5 watch item): enumerate the period_type + period + frequency_type + frequency kwargs explicitly in every `client.price_history(...)` invocation; discriminating test pattern — assert kwargs are passed via `inspect.signature` or mock-verification.
- **Byte-parity test as algorithmic substitute for operator-visual gate is INSUFFICIENT when test fixtures bypass production data-derivation paths.** Lesson surfaced 2026-05-19 at Phase 13 T1.SB0 gate-fix post-mortem. The chart-bytes byte-parity test `tests/pipeline/test_chart_bytes_parity_through_ohlcv_cache.py::test_chart_bytes_match_between_ohlcv_cache_and_legacy_price_fetcher` was cited as "STRONG algorithmic substitute" for the S3 visual gate during T1.SB0 pre-merge orchestrator-side QA. The test passed but missed the Schwab minute-default footgun regression because BOTH test paths consume IDENTICAL stub fixtures via `monkeypatch.setattr("swing.web.ohlcv_cache.read_or_fetch_archive", _stub_read)` — exercising the bare `read_or_fetch_archive` branch ONLY. The ladder path is never invoked. Both paths trivially produce byte-identical PNGs from the same fixture data. **The test asserts "given identical inputs, identical outputs" — but the regression is in HOW INPUTS ARE DERIVED.** Same gotcha family as synthetic-fixture-vs-production-emitter shape drift (Phase 12 C.D + Phase 12.5 #2 + Phase 12.5 Q2) — except the failure mode here is FETCH SEMANTICS drift, not envelope shape drift. **Pre-empt in any new wiring change that touches production data-derivation**: production-path regression tests that exercise the actual wiring (NOT fixture-seeded equality between two paths); discriminating-test pattern — plant production-shape state that triggers the wiring's divergence + assert the new behavior + assert this test FAILS pre-fix when reverting the wiring locally. **NEVER characterize an algorithmic substitute as "STRONG" for visual gates without verifying the algorithmic test exercises the production data-derivation path.**
- **Synthetic-fixture-vs-production-emitter shape drift — FOURTH CUMULATIVE INSTANCE (T-A.1.5b CLI dict→str + T-A.1.8 cassette filter chain).** The T-A.1.5b dispatch closed a CLI dict→str shape drift where `_SilverLabelResponse.structural_evidence_json` was typed `str` but the subagent contract emits a JSON object; test fixtures used `json.dumps({...})` (pre-serialized string-in-a-string) masking the bug until production-shape input fired `sqlite3.ProgrammingError: type 'dict' is not supported`. The T-A.1.8 dispatch added a similar surface at the cassette filter chain — Codex R1 Major #3 caught that the cassette E2E test ran the sentinel-leak audit but NEVER actually exercised the `codex_mcp_vcr_config()` filter chain (`before_record_request` + `before_record_response`); a contributor recording a real-MCP-HTTP cassette without the filter chain attached would silently leak secrets. **The defense-in-depth pattern across all 4 instances**: (a) production-shape fixtures derived from real emitter output (T-A.1.5b: `tests/fixtures/pattern_labeler/silver_response_vcp_dict_shape.json` from `tmp/phase13-labeling/silver_1_SNAP_vcp.json`); (b) CLI-level explicit shape coercion + validation at parse time; (c) dataclass-level `__post_init__` coercion + validation; (d) filter-chain regression tests that exercise the filter against synthetic sentinels even when the upstream traffic-recording is V2-deferred. Pre-empt: in any new file-based parse + dataclass-construction code path OR any new cassette-filter chain, the discriminating test MUST exercise the production-shape input through the production filter chain, NOT a synthetic shortcut.
- **Brief-prescription-vs-schema-CHECK collision — closing a "preserve operator-set column" prescription may collide with the table's CHECK constraints.** Failure mode discovered 2026-05-20 (Phase 13 T2.SB1 T-A.1.8 Deficiency 2 closure). The closer dispatch brief §1.2 prescribed `preserve final_pattern_class as-is` for the relabel-to-gold workflow. The literal prescription is INCOMPATIBLE with `pattern_exemplars` Invariant #1 (CHECK constraint at migration `0020_phase13_charts_patterns_autofill_usability.sql:109-114` precludes `final_decision='confirmed' AND final_pattern_class IS NOT NULL`). The implementer's semantic-equivalent fix: COALESCE-into-related-column at state transition (`UPDATE ... SET proposed_pattern_class = COALESCE(final_pattern_class, proposed_pattern_class), final_pattern_class = NULL ...`); operator's class choice survives gold promotion + Invariant #1 holds + NO schema migration. **Pre-empt in any future "preserve column at state transition" prescription:** cross-check the table's CHECK constraints first; the literal preservation may be schema-incompatible. If incompatible, the semantic-equivalent fix (COALESCE-into-related-column; capture audit trail in a JSON field; etc.) may be the only schema-faithful path. Brief authors should cite the relevant CHECK constraint shape when prescribing column preservation.
- **Schema-version-aware INSERT for newly-widened columns** — when a migration widens a table with new NOT-NULL-DEFAULTED columns, the repo INSERT path must detect schema version via `PRAGMA table_info` AND branch between legacy-column-list and new-column-list INSERTs OR all pre-current-version test fixtures must migrate up. T3.SB1 chose the runtime branch at `swing/data/repos/fills.py:51-53` to preserve ~30 pre-v20 fixtures unchanged. Forward-binding lesson: any future migration that adds NOT-NULL-DEFAULTED columns inherits this pattern OR commits to a fixture-update sweep.
- **Hidden anchor 4-tier rejection ladder** — when a web form has hidden audit anchors driving POST-time provenance stamping, the canonical pattern is the 4-tier rejection: (a) malformed JSON → 400 + clear anchor on recovery; (b) non-dict JSON → 400 + clear; (c) dict missing required keys → 400 + clear; (d) dict with invalid value shapes (NaN, non-int, calendar-invalid date) → 400 + clear. The `_reject_anchor` helper pattern at `swing/web/routes/trades.py:899-910` is the reusable template. Plus a `claimed_auto_fill` consistency-check gate prevents anti-forgery (valid anchor without claim must NOT stamp provenance).
- **Recovery form anchor-clear discipline** — on anchor-rejection 400 responses, the recovery form MUST clear the bad anchor (pass `submitted_*=None` to the re-render helper, NOT the raw rejected anchor) — otherwise the operator gets trapped in repeated 400s when their next submit replays the same bad anchor. Forward-binding for any form-rejection path that emits a recovery form. Closes the Phase 13 T3.SB1 Codex R3 Major #2 finding.
- **`Literal[...]` type hints are NOT runtime-enforced.** Failure mode discovered 2026-05-19 (Phase 13 T-A.1.5b Codex R3 M#1). Codex flagged that `confidence: Literal["high", "medium", "low"]` did not validate at runtime — an invalid value would have persisted into `labeler_evidence_json` silently. Pattern for any future dataclass with `Literal[...]` field on the data-integrity path: add explicit `__post_init__` runtime validation against an explicit frozenset of allowed values. Defense-in-depth catches malformed external inputs (subagent emissions; CLI parse paths). Closes a recurring class of "stricter type hint than runtime check" bugs.
- **Service-layer ValueErrors must be wrapped at CLI boundary.** Failure mode discovered 2026-05-19 (Phase 13 T-A.1.5b Codex R4 M#1). Codex flagged that `_map_silver_evaluation_to_decision`'s `relabel:<same_class>` rejection escaped the CLI's construction-time except clause because it fires AFTER `_fire_claude_silver_label` invocation. Pattern: CLI's command-handler boundary must wrap ALL service-layer dispatch calls in `try: ... except ValueError as exc: raise click.ClickException(...)` so any future service-level ValueError (validation, invariant check) surfaces as a clean error rather than a raw traceback.
- **High-tight-flag (HTF) post-pole sub-window is named `consolidation_*`, NOT `flag_*`.** Surfaced 2026-05-20 at Phase 13 T2.SB1 T-A.1.8 Deficiency 3 audit. Spec §5.5 names the post-pole sub-window `consolidation_*` per criterion #3 + #4 lock strings + Structural evidence enumeration (e.g., `consolidation_start_date`, `consolidation_end_date`, `consolidation_pullback_pct`, `consolidation_width_pct`, `consolidation_duration_days`). Operator's colloquial "flag_start_date / flag_end_date" terminology is a misnomer that propagated to T-A.1.7 resume notes. Pre-empt in any future structural_evidence_schema audit or HTF-related work: read the spec section's criterion lock strings (NOT the colloquial pattern name) to identify the canonical sub-window field names. Locked at `tests/patterns/test_spec_static.py::test_high_tight_flag_consolidation_naming_matches_spec_5_5_not_flag_naming` for regression safety.
- **Cross-bundle pin fixture-shape mismatch silently extends the pin window beyond the schedule.** Surfaced 2026-05-20 at Phase 13 T2.SB1 T-A.1.8 cross-bundle pin disposition for `test_flag_classifier_integration.py:21`. The fixture loader at `tests/evaluation/patterns/_fixtures.py:load_labeled_fixtures` expects paired `<name>.csv` + `<name>.json` files at `tests/evaluation/patterns/fixtures/` with labels `"flag"` or `"none"` — that's the older Phase 3e/7 chart-pattern flag-v1 classifier infrastructure. The T-A.1.7 corpus at `data/phase13-t2-sb1-corpus/pattern_exemplars_dump.jsonl` has a DIFFERENT shape (JSONL with 5 V1 detector pattern classes) + does NOT carry paired OHLCV CSVs. **Pre-empt in any future cross-bundle pin disposition:** verify fixture shape compatibility BEFORE the pin promises an un-skip date. Shape mismatch silently extends the pin window beyond what the schedule documents. Banked as forward-binding for V2 dispatch: either (a) port the Phase 3e flag-v1 classifier to consume the Phase 13 corpus shape (likely T2.SB3+/SB4 territory); OR (b) retire `test_flag_classifier_integration.py` as superseded by Phase 13 detector test suite.
- **Schema-CHECK widening MUST audit ALL Python-side surface guards across the repo, not just the canonical constant.** Failure mode discovered 2026-05-20 at Phase 13 T3.SB2 operator-witnessed S2 gate (post-merge `7059945`; hotfix at `cf3c489`). T-A.1.1 v20 atomic landing properly widened `schwab_api_calls.surface` DB CHECK + `audit_service._SCHWAB_API_SURFACE_VALUES` constant (1 of 4 Python-side surface guards) — but MISSED 3 OTHER hardcoded `("pipeline", "cli")` surface guards: `swing/integrations/schwab/trader.py:527 _call_endpoint` (ACTIVE blocker — silently short-circuited BOTH T3.SB1 entry auto-fill AND T3.SB2 exit auto-fill in production for 1+ day; 0 audit rows written for either surface) + `swing/integrations/schwab/marketdata.py:453 _call_endpoint` (defensive parity; inert today) + `swing/data/models.py:1504 SchwabApiCallRecord.__post_init__` (dead-code drift defense). Pre-Codex orchestrator-side review + 5 Codex rounds across T3.SB1 + 5 Codex rounds across T3.SB2 + slow Schwab E2E (4 cumulative) + cross-bundle pin all MISSED the duplicate-guard pattern (slow E2E mocks at `trader.get_account_orders` boundary so never exercises `_call_endpoint` with the new surfaces against the real guard). **Pre-empt in any future CHECK widening dispatch:** writing-plans §5 watch item — when widening a constant that's mirrored elsewhere in the codebase, grep ALL `swing/` modules for hardcoded copies of the OLD tuple/enum/set AND add discriminating tests that exercise EACH downstream consumer with the new values through the production code path (NOT mocked at a higher boundary). C.C lesson #6 pre-Codex review scope expansion required — search for `not in ("a", "b")` / `frozenset({"a", "b"})` / etc. patterns matching the constant being widened. Pattern complement to existing schema-CHECK + Python-constant + dataclass-validator paired discipline gotcha — same family extended to N-mirror auditing. V2 candidate: hoist `_SCHWAB_API_SURFACE_VALUES` into a shared schema-constants module so all sites import from a single source (avoid duplication between `audit_service.py` and `models.py` introduced by this hotfix).
- **`selected_X_audit_id` is an AUDIT TRAIL, not a DEDUPE KEY.** Failure mode discovered 2026-05-20 at Phase 13 T3.SB2 Codex R2 M3 (RESOLVED at `1a79482`). When a form has hidden "what operator picked" anchors vs "what was persisted" anchors, dedupe MUST key off "what was persisted" (i.e., the value-source for the actually-stored row). Failure mode: VM dedupe over-excludes the picked-but-unrecorded candidate from future surfaces while leaving the actually-persisted candidate available for duplicate recording. Pattern recurs anywhere form-render presents N options + persistence reflects 1 outcome. Pre-empt in any future operator-selection-from-list flow: enumerate dedupe-key vs audit-trail fields; key dedupe off the field that reflects the row's value-source (e.g., envelope's top-level `schwab_order_id`, NOT envelope's `selected_candidate_order_id`).
- **Price-precision parity between template render + POST-time float comparison.** Failure mode discovered 2026-05-20 at Phase 13 T3.SB2 Codex R4 M2 (RESOLVED at `7348620`). When a template formats a float with fixed precision (e.g., `%.2f`) and the POST handler compares submitted-vs-anchor floats with a tight epsilon (e.g., `1e-9`), the comparison will silently flip to "operator edited" for execution-grain prices that round-trip through `%.2f`. Failure mode: authoritative `120.505` → template renders `120.50` → operator submits `120.50` → `abs(120.505 - 120.50) = 0.005 > 1e-9` → falsely flips fill_origin to `schwab_auto_then_operator_corrected` even though operator did NOT edit. Fix: compare with `round(anchor, N) != round(submitted, N)` where N matches the template's display precision. Pre-empt: in any new form rendering numerical floats, enumerate the template's display precision + use matching precision in the POST comparison.
- **`extended.pop(key, None)` for envelope keys that may not apply.** Failure mode discovered 2026-05-20 at Phase 13 T3.SB2 Codex R4 M1 (RESOLVED at `7348620`). When persisting a derived envelope where a key MAY OR MAY NOT apply (e.g., authoritative selected candidate has None for order_id), do NOT leave the stale form-render default in place — POP the key. Downstream dedupe will then fall through to alternate matching (e.g., fallback tuple). Failure mode: operator selects non-default radio + edits visible to match + selected candidate has None order_id → R3 rewrite skips (only fires on truthy order_id) → top-level stays at default order_id → future dedupe excludes default (never persisted) + does not add fallback tuple (order_id_found=True). Pre-empt: in any envelope-rewriting flow with optional fields, pop missing-data keys rather than leave stale defaults.
- **Evidence-tier vs composite-tier score cap distinction.** Failure mode discovered 2026-05-21 at Phase 13 T2.SB4 Codex R1 M1 + R2 Critical #1 + R3 Major #1 lesson family (RESOLVED across `6f735db` + `4768ee3` + `499c5ab`). When a spec defines BOTH an evidence-tier metric (rule-pass-fraction + bonus) AND a composite-tier metric (weighted blend + clamp), the implementation MUST: (1) allow evidence-tier to reach the spec'd upper bound (e.g., DBW `geometric_score` 1.10 per spec §5.8 line 718 + §10.5 line 1325 undercut bonus); (2) clamp composite-tier at the spec'd composite cap (e.g., 1.0 via `min(1.0, ...)` wrap per spec §5.8 line 712); (3) persist evidence in `structural_evidence_json` raw + DB evidence column unconstrained; (4) persist composite in DB composite column clamped; (5) validate the downstream `_composite_score_histogram` or similar histogramming functions consume the CLAMPED composite, NOT the raw evidence — otherwise `ValueError`s poison the entire emit loop for the run. Pre-empt in any future scoring-system implementation: explicitly enumerate evidence-tier vs composite-tier upper bounds + verify downstream consumers (histograms, normalizers, validators) consume the correctly-bounded tier. The dispatch brief got the cap wrong at T2.SB4 §1.1 #12 + §4.1 #4 ("undercut bonus capped at 1.0"); spec source-of-truth at §5.8 line 718 + §5.6 criterion #8 + §10.5 line 1325 was authoritative. See related new gotcha "Pre-Codex review must cross-check spec source-of-truth against dispatch brief sketches" below.
- **Bounded backward-slice search for anchor-relative detectors.** Failure mode discovered 2026-05-21 at Phase 13 T2.SB4 Codex R2 Major #1 (RESOLVED at `4768ee3`). When an algorithm walks back from an anchor_date to find a structural landmark (e.g., HTF pole peak; cup left edge; trough_1), bound the search window per the spec's max duration constraints (e.g., HTF pole = 4-8 weeks per spec §5.5 criterion #2). Picking "most recent" OR "absolute max" without bounding allows historical-data anomalies to capture the algorithm — a 6-month-old peak masquerades as a 6-week pole. Pre-empt: in any backward-search algorithm consuming `bars` + an anchor, explicitly bound the search window by the spec's max-duration constraint + add a discriminating test that plants an out-of-window candidate at the boundary + asserts the algorithm doesn't select it.
- **DBW anchor_date contract via `anchor_reason.startswith("zigzag_pivot")`.** Failure mode discovered 2026-05-21 at Phase 13 T2.SB4 Codex R1 Major #2 (RESOLVED at `6f735db`). Detectors consuming `CandidateWindow` MUST check the `anchor_reason` mode tag to determine `anchor_date` semantic: `zigzag_pivot*` = base START (the inferred swing-low anchor); `ma_crossover` / `high_low_breakout` = TRIGGER EVENT (different semantic). T2.SB3 detectors (VCP + flat_base + cup_with_handle) did not enforce this strictly + happened to work because `zigzag_pivot` was the dominant mode in tests; T2.SB4 DBW MUST enforce it explicitly because DBW's trough_1 identification depends on `anchor_date` being a swing-low. Pre-empt in any future detector authoring against `CandidateWindow`: guard `anchor_date` semantic via `anchor_reason.startswith("zigzag_pivot")` check + raise typed exception if non-zigzag mode supplied (or implement per-mode handling explicitly).
- **Pre-Codex review must cross-check spec source-of-truth against dispatch brief sketches.** Failure mode codified 2026-05-21 at Phase 13 T2.SB4 Codex R1 M1 lesson + 20th C.C lesson #6 validation (BANKED CLEAN on the scope-expanded surface-guard discipline; but a DIFFERENT class of defect surfaced via Critical R2 because the dispatch brief sketched the wrong cap). The dispatch brief's prescription of "DBW geometric_score = `min(base + bonus, 1.0)`" was WRONG per spec §5.8 line 718 + §10.5 line 1325 (1.10 evidence cap; composite at 1.0). Pre-Codex orchestrator-side review reading the brief alone wouldn't catch this; cross-checking against §5.8 + §10.5 BINDING text is required. Pre-empt: pre-Codex review templates SHOULD include "verify dispatch brief prescriptions against spec source-of-truth at the cited section" as a binding step. Pattern complement to the existing schema-CHECK-widening surface-guard scope expansion (T3.SB2 hotfix at `cf3c489`) — both about pre-Codex review SCOPE EXPANSIONS surfaced by Codex-caught defects.
- **Bad-exemplar isolation in retrieval functions.** Failure mode discovered 2026-05-21 at Phase 13 T2.SB5 Codex R1 Minor #1 (RESOLVED at `5534cc6`). `TemplateMatchExemplar.__post_init__` only validates type + ndim of `close_prices`; a NaN/inf array slips past construction. When `match_forward` invoked `_min_max_normalize` on the bad bundle, the `ValueError` propagated out of the function; pipeline caller's broad try/except buried the failure + stamped `template_match_score=None` for the row even though OTHER valid same-class exemplars in the cohort would have produced hits. Fix: per-exemplar try/except inside `match_forward` (skip bad exemplar; continue with cohort); symmetric defense in `match_reverse`; symmetric per-call defense on the candidate-side normalize call. Pre-empt in any future retrieval-style function that consumes a cohort + delegates per-element work to a normalization/transformation helper that can raise: per-element try/except around the helper (skip bad element; continue with cohort) rather than letting the exception bubble up + suppress the whole call. Aligns with existing "per-row failure isolation" discipline in pipeline step runners. Discriminating test pattern: plant one NaN element + one finite element in the cohort; assert finite element produces a hit (pre-fix: NaN raises + caller stamps None; post-fix: NaN skipped + finite element yields hit).
- **DTW Sakoe-Chiba band infeasibility on asymmetric series — correct skip-as-no-match semantic.** Surfaced 2026-05-21 during Phase 13 T2.SB5 T-A.5.4 integration testing; non-defect behavior documented at `swing/patterns/template_matching.py:202-208`. When candidate (N bars) and exemplar (M bars) lengths are asymmetric beyond the Sakoe-Chiba band's reach (e.g., 2-bar candidate vs 50-bar exemplar with 10% band), the DTW DP table's final cell remains `inf` because the path is infeasible within the band. `match_forward` + `match_reverse` skip such exemplars (treat as "no match found") rather than poisoning the hit list with non-finite distance. Correct V1 semantic — but worth documenting: a candidate window much shorter or longer than the exemplar corpus distribution may yield zero hits even when the candidate's pattern_class has exemplars planted. V2 candidates banked: length-stratified exemplar selection; adaptive Sakoe-Chiba band based on series-length ratio.
- **Universe histogram must reflect POST-template composite.** Discovered 2026-05-21 during Phase 13 T2.SB5 T-A.5.4 architecture design; honored at `swing/pipeline/runner.py:1964-1968`. When the pipeline's `_step_pattern_detect` Pass-2 layer adds a template-matching post-processing step that modifies `composite_score`, the downstream `universe_context["composite_scores"]` histogram MUST be seeded from the POST-template composites (the final persisted values), NOT the pre-template fallback (the Pass-1 `min(1.0, geometric_score)` placeholder). T2.SB5 dispatch brief watch items #11 + #12 anticipated this; implementation correctly builds `final_universe_scores` from `resolved_emit_list[i][8]` (the post-template composite_score). **Pre-empt in any future pipeline step that mutates a column AFTER the universe-snapshot point**: explicitly enumerate which column the universe histogram should reflect + verify the universe-build happens AFTER all mutations.
- **Read-path mapping must keep pace with write-path on widened columns.** Failure mode discovered 2026-05-21 PM at Phase 13 T3.SB3 Codex R1 MAJOR #1 (RESOLVED at `cab65d8`). v20 migration added `review_log.auto_populated_field_keys_json` column; write path (`complete_review_atomic`) persisted it correctly but the read-path `_row_to_review_log` mapper in `swing/data/repos/review_log.py` did NOT include the column in its row→dataclass projection. Result: persisted value invisible to `get()` / `list_recent()` / `list_pending()` consumers. Codex caught it; fix maps `row[23]` to the new field + adds 2 discriminating round-trip tests (persist via write path + read back via public reader; assert equality NULL + non-NULL). **Pre-empt in any future schema-widening dispatch:** writing-plans §5 watch item — when widening a dataclass with a new field, grep ALL `_row_to_<table>` mapper functions in the same module AND extend them in the SAME task with column-position comments. Pattern complement to the existing schema-CHECK + Python-constant + dataclass-validator paired discipline + schema-CHECK widening surface-guard auditing gotchas — same family extended to read-path mapper auditing.
- **"Server-stamped" hidden form inputs are STILL tampering surfaces unless the POST handler RECOMPUTES rather than ACCEPTS — semantic clarification of "server-stamping" LOCK.** Failure mode discovered 2026-05-21 PM at Phase 13 T3.SB3 Codex R1 MAJOR #2 (RESOLVED at `cab65d8`). Initial implementation of `cadence_complete_post` trusted the operator-submitted hidden `auto_populated_field_keys_json` form input value verbatim. The "stamping" only happened at GET render time (`build_cadence_complete_vm` set the hidden input value from canonical state); POST blindly accepted whatever value the operator (or a tampered curl invocation) submitted. The cumulative gotcha "For any V1 single-operator form with hidden audit fields, default to SERVER-STAMPING at handler entry" is NECESSARY BUT INSUFFICIENT — the POST handler MUST re-derive the audit envelope from canonical state at POST time, NOT consume the GET-side hidden input. Fix: drop the Form parameter at POST + recompute the envelope server-side via the same period helpers the GET-side VM builder uses + remove the hidden input from the template entirely. Discriminating test: submit a fabricated tampered envelope; assert persisted column is NULL matching the server-side recompute (NOT the operator-submitted value). **Pre-empt in any future hidden-audit-field design:** writing-plans §5 watch item — enumerate per-field disposition as (a) operator-supplied OR (b) recomputed-at-POST-from-canonical-state; BAN (c) "trusted-from-GET-render" patterns. Semantic clarification: "server-stamped" means the SERVER COMPUTES the value at POST time, not that the value RENDERED at GET time is signed/trusted-on-resubmit. Pattern complement to the existing Phase 8 R2-R5 server-stamping family — same family extended with the GET-vs-POST recompute discipline.
- **Audit envelope empty-state representation must be uniform across emit + persist paths.** Failure mode discovered 2026-05-21 PM at Phase 13 T3.SB3 pre-Codex MAJOR #1 (RESOLVED at `d3af86b`). `build_review_vm` emitted `json.dumps([])` (the string `"[]"`) while `build_cadence_complete_vm` emitted `None` on empty audit envelope. The string `"[]"` is TRUTHY under Python `... or None` defense (`"[]" or None` → `"[]"`), so any downstream POST persistence path using the `... or None` SQL-nullability gotcha-defense would accidentally persist the placeholder string `"[]"` instead of NULL on empty-envelope writes. The Phase 6 deviation #3 gotcha (`... or None` for nullable CHECK columns) only works if the upstream emitter cooperates by producing `None` (or empty-string `""`) on empty — not `"[]"`. Fix: emit `None` (not `"[]"`) on empty across all VM builders + writer paths; uniform empty-state representation. **Pre-empt in any future audit-envelope design:** writing-plans §5 watch item — discriminating tests asserting empty → `None` (not `"[]"` / not `""`) at BOTH the VM emit path AND the persist write path. Pattern complement to the existing `... or None` SQL-CHECK-nullability gotcha — same family extended to uniform-empty-state at the emit layer.
- **Schema-CHECK + Python-constant + dataclass-validator paired discipline (§A.14) EXTENDS to SEMANTIC contracts layered atop the schema CHECK.** Failure mode discovered 2026-05-21 PM #3 at Phase 13 T2.SB6a Codex R1 CRITICAL #1 (RESOLVED at `54fb531`). The `chart_renders` schema CHECK constrains theme2_annotated branch (`pattern_class` + `pipeline_run_id` non-NULL required) and non-theme2 branches (`pattern_class` NULL required). The substrate's `ChartRender.__post_init__` mirrored those schema CHECKs — but DID NOT mirror the plan §C.2 SEMANTIC cache key contract that extends BEYOND the schema CHECK: run-bound surfaces (`watchlist_row` / `hyprec_detail` / `market_weather`) require `pipeline_run_id IS NOT NULL`; `position_detail` requires `pipeline_run_id IS NULL`. The partial UNIQUE indexes constrain UNIQUENESS via predicates, not EXISTENCE. So `(surface='watchlist_row', pipeline_run_id=NULL)` PASSED schema + dataclass construction but was INVISIBLE to the canonical reader (whose run-bound branch matches `pipeline_run_id IS NOT NULL`). Symmetric: `(surface='position_detail', pipeline_run_id=42)` PASSED but was invisible. **Pre-empt in any future schema-CHECK + Python-validator paired discipline:** writing-plans §5 watch item — when the dataclass `__post_init__` mirrors a schema CHECK, enumerate ALL semantic invariants from spec/plan (cache key shapes; partial-index existence semantics; cross-column uniqueness only enforced by partial UNIQUE indexes) + mirror EACH in `__post_init__`, NOT just the schema CHECK. **Discriminating test pattern:** plant a row that satisfies the schema CHECK + violates the semantic contract; assert dataclass construction raises. **Pre-Codex review scope expansion #3 BINDING for future dispatches** — surface ALL semantic contracts on tables touched by the substrate; verify validator mirrors EACH. Pattern complement to the existing §A.14 schema-CHECK + Python-constant + dataclass-validator gotcha family + the T3.SB2 hotfix surface-guard widening gotcha — same family extended to semantic contracts beyond schema CHECKs.
- **F6 (write-through-cache transient empty) defense applies at CONSTRUCTION barrier, NOT cache-helper wrapper, when helper accepts a dataclass parameter.** Failure mode discovered 2026-05-21 PM #3 at Phase 13 T2.SB6a Codex R1 MAJOR #2 (RESOLVED at `54fb531`). `refresh_chart_render(chart_render: ChartRender)` accepts an already-constructed dataclass + unconditionally DELETEs the existing cache row then INSERTs whatever `chart_svg_bytes` the caller passed. A transient empty-bytes emit from an upstream renderer (matplotlib hiccup; pre-data-flow init; rare upstream regression) would BLANK a previously-good cache row. The cumulative F6 gotcha "External-API empty-result must be treated as transient when write-through-caching" was originally written for direct-API wrappers (yfinance / broker APIs); when the cache helper accepts a DATACLASS parameter instead of raw column values, rejecting empty bytes in the HELPER is too late (the dataclass was already-constructed). Fix: add non-empty validation to `ChartRender.__post_init__` — `ChartRender(chart_svg_bytes=b"")` raises at construction BEFORE `refresh_chart_render`'s DELETE can fire. Construction-time rejection achieves F6 by STRUCTURAL IMPOSSIBILITY — the bad input never reaches the cache helper. **Pre-empt in any future write-through-cache helper that accepts a dataclass parameter:** apply the F6 transient-empty defense at the dataclass `__post_init__` barrier, NOT at the helper. Discriminating test pattern: plant a known-good cache row + invoke helper with construction-rejected empty dataclass + assert existing row preserved verbatim post-rejection. **Pre-Codex review scope expansion #4 BINDING for future dispatches** — for each cumulative gotcha cited in a dispatch brief, walk a SPECIFIC failure scenario through the substrate code path (NOT generic "is the lesson applied" sufficiency check). Pattern complement to the existing F6 cumulative gotcha — same family extended to dataclass-accepting helper architecture.
- **Pre-Codex 5-expansion discipline catches schema-CHECK / Literal-validator / cross-section spec citations / hardcoded-duplicates / specific-scenario gotcha traces — but does NOT catch CONTENT-completeness vs spec text NOR cross-row semantic scope drift on operator-input flows.** Failure mode discovered 2026-05-21 PM #4 at Phase 13 T2.SB6b Codex R1 (7 MAJOR findings; no CRITICAL). Pre-Codex review applied all 5 SCOPE EXPANSIONS (#1 hardcoded-duplicate + #2 brief-vs-spec source-of-truth + #3 schema-CHECK-vs-semantic-contract + #4 specific-scenario gotcha trace + #5 cross-section spec inventory grep) and ran CLEAN at orchestrator-side audit; Codex still surfaced 4 CONTENT-completeness gaps (spec checklist items rendered as `"n/a"` stubs vs live data; queue weather-state V1 proxy vs spec criterion 3; metric tile reached_1r/hit_stop=None vs spec; Theme 1 chart integration partial completeness) + 1 cross-row semantic scope drift (`label_source='organic_trade_history'` was ticker-proxy not candidate-proxy per spec §5.10 lines 785-790) + 1 acceptance-criterion miss (cache-miss renderer-invoked path; arguably out of expansion #5 scope) + 1 ASCII em-dash slip. **NEW Expansion #6 candidate (BANKED for 25th cumulative validation): content-completeness audit** — for each spec data-surface checklist item (especially §5.10 8-item checklists; spec checklist data-surface enumerations; metric-tile field tables), pre-Codex review MUST enumerate the implementer's per-field disposition explicitly (LIVE / V1 PLACEHOLDER / V1 STUB) BEFORE Codex invocation. Walk each spec data-surface item + ask "does my code provide LIVE data or a STUB?" — banking V1 stubs is fine if intentional + return-report-enumerated; silently rendering a stub is the failure mode. **NEW Expansion #7 candidate (BANKED): cross-row semantic audit on operator-input flows** — for any new POST handler that consumes operator input AND looks up cross-row state (`trades`, `pattern_exemplars`, `reconciliation_*`, etc.), pre-Codex review MUST enumerate the SCOPE of the lookup (ticker / pattern_class / candidate / pipeline_run / etc.) + cross-check against the spec's wording for the lookup semantics. Discriminating test pattern: plant a same-ticker / different-candidate trade row + assert the POST persists per the spec's lookup-scope semantics, not the broader scope. Pattern complement to the existing 5 expansions + the §A.14 schema-CHECK + semantic-contract gotcha — same family extended to PROCESS DISCIPLINE for V1-completeness + semantic-lookup-scope at the route handler layer.
- **V1 simplification banking discipline — every "V1 placeholder" / "V1 stub" / "V1 simplification" the implementer ships MUST be enumerated in the return report §6 (or equivalent) WITH the V2 dependency cited.** Banked 2026-05-21 PM #4 at Phase 13 T2.SB6b Codex R1 lesson family. Failure mode: silently rendering a spec checklist item as `"n/a"` / `"(not available)"` / `None` without enumeration creates a deferred-work ledger that grows untracked + makes future arc-closure scoping impossible. Pre-Codex review's content-completeness audit (Expansion #6) is the gate; the return report §6 V1-simplifications-with-V2-dependency table is the permanent ledger. T2.SB6b banked 9 such V1 simplifications with V2 dependencies cited verbatim (trend-template stub → `current_stage()` weather-state read; volume profile → 30-session volume + 50d avg join; outcome distribution → trade backlink for 1R + stop bucketing; reached_1r/hit_stop=None → trades.candidate_id backlink V2 migration; closed_loop_review-only → trade.candidate_id backlink V2 migration; queue criterion 3 underrepresented_regime proxy → weather-state-aware variant per spec §5.10 line 799; hyp-rec/position detail VM chart bytes → per-page VM wire-up V2 sub-bundles; WatchlistVM watchlist_chart_svg_bytes plumbed-not-rendered → extend `partials/watchlist_row.html.j2`; exemplar cache-miss live render no write-through → pipeline-run-agnostic exemplar cache key shape V2). **Pre-empt in any future dispatch** where the brief scope is larger than V1 can land cleanly: writing-plans §5 watch item — enumerate every spec-bound surface that ships as STUB in V1 + cite the V2 dependency per row. Pre-Codex review verifies enumeration is complete + each disposition has a V2 dependency citation. Pattern complement to the existing V2-candidate-banking discipline at orchestrator-context "Lessons captured" + V2-dependency-cited cumulative gotcha (T3.SB1 schema-version-aware INSERT precedent) — same family promoted to required artifact in every return report.
- **Brief-vs-actual schema reality check + SQL skeleton column verification (Expansion #4 refinement BINDING).** Failure mode discovered 2026-05-21 PM #5 at Phase 13 T2.SB6c brainstorming. Two related defects surfaced — both originate in the orchestrator-side dispatch-brief authoring phase + must be caught at pre-Codex / pre-publish: (1) the orchestrator brief §2.3 proposed `watchlist_close_track_flags` as v21 delta C — OBSOLETE because the table ALREADY exists in v20 per migration `0020_phase13_charts_patterns_autofill_usability.sql:262-307` + Phase 13 spec §7.2 line 986 LOCK; brainstorm spec-write phase caught this via Expansion #2; (2) the orchestrator brief §2.1 + brainstorm spec sketched a SQL JOIN against `candidates.pipeline_run_id` — non-existent; the canonical column is `candidates.evaluation_run_id` per migration `0001_phase1_initial.sql:26` + `swing/trades/origin.py:10-11` BINDING text; Codex R1 CRITICAL caught this in brainstorm chain. **Pre-empt at dispatch-brief authoring (orchestrator-side)**: when proposing "NEW table X" / "NEW column Y" / "widen CHECK enum Z" in a brief, ALWAYS grep `swing/data/migrations/*.sql` for the proposed table/column name BEFORE publishing the brief; if the table/column already exists, note it explicitly + amend scope. **Pre-empt at brainstorm/spec-write phase (implementer-side)**: every SQL skeleton (especially JOIN ON clauses + WHERE clauses + subqueries) MUST have ALL column references verified against the actual migration files — grep `swing/data/migrations/*.sql` for the exact column name + table name combo before publishing the spec. **Pre-Codex Expansion #4 refinement (BINDING for 26th cumulative validation onwards)**: the existing "CLAUDE.md gotcha specific-scenario trace" expansion is extended with the binding sub-step "every SQL skeleton's columns verified against actual migration files". Discriminating test pattern at brainstorm phase: spec's SQL skeletons must compile / run successfully against a v<current>+ schema fixture. Pattern complement to the existing T3.SB2 hotfix surface-guard gotcha + Expansion #2 brief-vs-spec source-of-truth lesson — same family extended to schema/column existence + correctness at the brief-drafting + spec-write phases.
- **SQL aggregation UNIT audit (NEW Expansion #8 candidate BINDING for 27th cumulative validation onwards).** Failure mode discovered 2026-05-22 AM at Phase 13 T2.SB6c writing-plans Codex R2 MAJOR #1 + R3 MAJOR #1 + R4 MAJOR #1 lesson family. Pre-Codex Expansion #7 (cross-row semantic SCOPE audit per-candidate vs per-ticker) at writing-plans phase was CLEAN per the brainstorm-phase banking — but the writing-plans-phase pre-Codex review still missed 3 aggregation-UNIT-mismatch defects: B.5 denominator used COUNT(*) without DISTINCT, JOIN-inflating per-evaluation across the to-trade JOIN cardinality; B.5 numerator counted trades not evaluations; B.4 LIMIT applied to trade rows not evaluation rows. Scope audit (which rows participate) does NOT subsume unit audit (what is COUNT counting). **Pre-empt for any new SQL skeleton in writing-plans / executing-plans phases**: writing-plans §5 watch item — for EACH GROUP BY / COUNT / SUM / aggregation function in a SQL skeleton, enumerate (a) what unit the function is counting (trades / evaluations / candidates / exemplars); (b) whether DISTINCT is needed to prevent JOIN-cardinality inflation (multi-table JOIN with 1:N cardinality MUST have DISTINCT or CTE-then-aggregate); (c) whether LIMIT applies at the correct unit. Discriminating test pattern: cardinality-multiplied row fixtures (plant 1 evaluation with 2 trades; 1 candidate with 2 overlapping exemplars) + assert per-unit counts match expectations. Banked as Expansion #8 because it's distinct from #7 (semantic scope = which rows participate) and from #4 (column existence) — this is ARITHMETIC UNIT CORRECTNESS in aggregation. Pattern complement to existing Expansion #4 SQL-skeleton column verification + Expansion #7 cross-row semantic scope discipline — same family extended to per-aggregation-function unit correctness.
- **Existing-field reuse audit before claiming new dataclass fields.** Failure mode discovered 2026-05-22 AM at Phase 13 T2.SB6c writing-plans Codex R4 MAJOR #2. The plan claimed NEW `*_pct` fields on `PatternOutcomeRow` for Gap B.5 metric tile rendering — but the dataclass ALREADY has `reached_1r_n + reached_1r_ci + hit_stop_n + hit_stop_ci` fields populated as None per T2.SB6b V1 simplification. Claiming new fields would have created field-duplication + ambiguous downstream consumption (which field renders? which one persists?). **Pre-empt in any new dataclass extension dispatch**: writing-plans §5 watch item — BEFORE claiming new fields on an existing dataclass, run `Grep "_n: int | None\|_ci: " <dataclass module>` against the existing module to find fields matching the planned-new-field shape; verify whether the existing fields are populated-and-rendered, populated-but-not-rendered, or absent. If populated-but-not-rendered, the extension is a template-rendering surface change (NEW gotcha #11 below), NOT a dataclass field addition. Pattern complement to existing read-path mapping discipline (T3.SB3 R1 M#1) — same family extended to dataclass-field surface auditing.
- **Template-rendering surface audit before claiming "no template edit needed".** Failure mode discovered 2026-05-22 AM at Phase 13 T2.SB6c writing-plans Codex R5 MAJOR #2. The plan claimed "no template edit needed" for Gap B.5 metric tile WilsonCI surfacing — but the template at `swing/web/templates/metrics/pattern_outcomes.html.j2:35-45` only renders the ratio (`row.reached_1r_n / row.n`); the `_ci` fields are populated but NOT rendered. Plan was silently shipping a V1 STUB display contract while claiming closure. **Pre-empt for any V1 STUB → LIVE transition or dataclass-field consumption claim**: writing-plans §5 watch item — read the template's render path explicitly + enumerate which dataclass fields are rendered vs persisted-but-not-rendered; verify the V1 display contract matches operator expectation. If template surfaces only N of M populated fields, the extension scope MUST include template-render extension (NOT just dataclass field population). Discriminating test pattern: render-time test asserts the new field's value appears in template output (NOT just dataclass-construction round-trip). Pattern complement to Phase 8 server-stamping family + T3.SB3 server-recompute lesson — same family extended to render-surface-vs-data-surface discipline.
- **`date.fromisoformat()` discipline for cross-type-boundary calls.** Failure mode discovered 2026-05-22 AM at Phase 13 T2.SB6c writing-plans Codex R3 MAJOR #2. The plan invoked `current_stage(conn, ticker, asof_date)` (signature requires `date` per `swing/patterns/foundation.py:745`) but `pattern_evaluations.window_end_date` is TEXT (ISO-format string) per migration `0020_phase13_charts_patterns_autofill_usability.sql:230-250`. Missing conversion would raise `TypeError` at runtime. **Pre-empt in any cross-type-boundary call**: writing-plans §5 watch item — when a SQL TEXT column feeds a Python helper expecting `date`/`datetime`, cite the conversion explicitly at the callsite (`date.fromisoformat(row[N])`) + add malformed-input discriminating test (graceful fallback / typed exception, NOT 500). Discriminating-test pattern: plant a row with malformed ISO date + verify the consumer raises a typed error (e.g., `MalformedSessionAnchorError`) NOT `TypeError` deep in the Python stack. Pattern complement to existing schema-version-aware INSERT discipline (T3.SB1 precedent) — same family extended to TEXT-vs-date Python-type-boundary discipline.
- **Form-render anchor lifecycle audit (NEW Expansion #9 candidate BINDING for 28th cumulative validation onwards).** Failure mode discovered 2026-05-22 PM at Phase 13 T2.SB6c executing-plans Codex R1 MAJOR #1 + #2 + #3 + R2 MAJOR #1 + R3 MAJOR #1 cumulative family. When introducing a hidden form anchor that drives POST-time validation (e.g., `pattern_evaluation_id` hidden anchor at trade entry form per OQ-12 LOCK), the FULL lifecycle audit MUST include FOUR dimensions: (a) **soft-warn confirm `form_values` round-trip** — every hidden anchor that drives POST-time validation MUST be re-emitted in the soft-warn confirm fragment's `form_values` dict so the operator's `force=true` resubmit replays it (existing Phase 9 Sub-bundle D R3 Critical #1 gotcha "Form-render hidden anchors driving POST-time validation MUST round-trip through soft-warn confirm" extends here); (b) **GET-time query param consumption** — when an upstream UI surface emits a hidden query-param anchor on a link (e.g., hyp-rec card link to entry form with `?pattern_evaluation_id=N&pipeline_run_id_at_form_render=M`), the GET-time route handler AND VM builder MUST consume + honor it (form-defaults derive from the SAME explicit pipeline_run anchor, NOT the latest snapshot); (c) **candidate-snapshot consistency across pipeline runs** — when both `pattern_evaluation_id` and `candidate_id` backlinks land on the same trade row, they MUST resolve from the SAME `pipeline_run` snapshot; when `pattern_evaluation_id` is non-NULL, `candidate_id` resolves via the PE row's `pipeline_run_id → evaluation_run_id → candidates` chain (NOT `_latest_complete_evaluation_run_id`); (d) **explicit-anchor-vs-latest-snapshot validation order** — when both an explicit anchor (from GET-time query param) and an implicit fallback (latest snapshot) are available, the explicit anchor MUST win + the validation order MUST be enumerated explicitly + a mismatched-run fallback rationale comment MUST justify any permissive tamper behavior. **The cumulative 4-defect family escaped pre-Codex review** (T2.SB6c executing-plans 27th C.C lesson #6 validation NOTABLE; Codex R1+R2+R3 escalated through). **Pre-empt at writing-plans + executing-plans phases** in any future hidden-anchor design: writing-plans §5 watch item — enumerate ALL 4 lifecycle dimensions per anchor; discriminating tests per dimension (a) soft-warn `force=true` resubmit preserves anchor; (b) GET-time query-param anchor honored at form-default derivation; (c) candidate snapshot resolves from same pipeline_run as PE backlink; (d) mismatched-run fallback rationale documented. Pattern complement to the existing Phase 9 Sub-bundle D round-trip gotcha + T3.SB1 hidden anchor 4-tier rejection ladder + T3.SB3 server-recompute-at-POST gotcha — same family extended to form-render-time lifecycle dimensions distinct from POST-time rejection-ladder + persistence-recompute disciplines.
- **Architecture-location audit + 4 sub-disciplines (NEW Expansion #10 candidate BINDING for 29th cumulative validation onwards).** Failure mode discovered 2026-05-22 PM #2 at Phase 13 T4.SB brainstorming Codex R1 M#2 + M#3 + M#7 + R3 M#2 + R4 M#1 + M#2 + M#3 cumulative family. The brainstorming spec exhibited FIVE distinct sub-disciplines worth banking as a unified expansion: (a) **Architecture-location audit for cross-cutting helpers** — when a brief proposes wiring NEW logic into an EXISTING module, audit whether that module has the necessary dependency-context to host the helper. Codex R1 M#2 caught chart_scope.py being asked to host JIT side-effects when it's a pure availability-check module with no `conn`/`ohlcv_cache` access; R1 M#3 caught missing OHLCV-cache plumbing through to JIT callsites; R1 M#7 caught WatchlistRowVM being asked to host a `chart_svg_bytes` field that crossed VM-shape boundaries. **Resolution pattern**: NEW module for cross-cutting logic (e.g., `swing/web/chart_jit.py`) + explicit dependency-injection signature + invoke from route handlers / VM builders (NOT from the pure-query module). Pre-empt: any new helper wired into an existing module — verify the module's existing dependency surface (`grep` for imports + state-access) covers the new logic's needs. (b) **Template-vs-VM-parser-vs-emitter triangulation gap** — when a "feature doesn't render" gap exists, code-read ALL THREE LAYERS (template, VM-parser, emitter) to identify WHERE the data flow breaks. Codex R1 M#6 + R3 M#2 caught that Item 2 narrative-rendering "gap" was actually at emit/persist NOT at the template (template + parser already correct); fix required envelope-ALIAS pattern persisting both `geometric_evidence_narrative` AND `narrative` keys. Pre-empt: gap-location-triangulation enumeration in dispatch briefs — for any "rendering bug" or "feature missing", state explicitly which of the 3 layers is suspected + verify by reading each. (c) **Cache-key shape + renderer-kwargs uniformity LOCK** — when a `surface` enum value is reused across multiple callers with potentially different render kwargs, the cache key shape `(ticker, surface, pipeline_run_id)` becomes ambiguous; second caller's render-kwargs may differ from first; cache collision risk. Codex R4 M#3 caught `hyprec_detail` reused for dashboard-row + watchlist-expanded with potentially divergent kwargs. **Resolution pattern**: LOCK renderer-kwargs identical across callsites + add discriminating cache-collision test that mocks the renderer + asserts `call_count == 1` for the second caller (read from cache). Pre-empt: any time a `surface` enum is consumed by 2+ callers, verify renderer-kwargs uniformity OR introduce distinct surface names. (d) **SQL LIKE wildcard-escape + raw-vs-escaped binding-param asymmetry** — when a SQL helper produces BOTH an EQUALITY predicate AND a LIKE predicate against the same name parameter, the equality predicate must use the RAW name (escape sequences would break verbatim equality) and the LIKE predicates must use the ESCAPED name (or `%`/`_` wildcards in the name broaden the match unintentionally). Codex R4 M#2 caught that `count_per_cohort` helper would have used escaped name in equality, breaking exact-match for cohort names containing `%`/`_`. **Resolution pattern**: helper signature returns 3-tuple `(where_fragment, [raw_lowercased, escaped_lowercased, escaped_lowercased])` matching the 3 binding positions; discriminating test with synthetic registry name like `'cohort_X%'` + 4 persisted labels. Pre-empt: any SQL helper producing mixed equality + LIKE predicates against name params — enumerate per-binding-position raw-vs-escaped requirement explicitly. (e) **Orphan-label preservation when refactoring exact-match groupings to delimiter-aware** — existing exact-match GROUP BY surfaces (e.g., `count_per_cohort`) intentionally surface unregistered labels as orphan placeholders for operator visibility; a delimiter-aware refactor that ONLY counts registered-cohort matches LOSES this orphan-label behavior. Codex R4 M#1 caught this in the Item 7 broader audit refactor. **Resolution pattern**: second query for closed trades NOT delimiter-matching any registered hypothesis → orphan-label rows preserved; discriminating test plants 3 rows (registered-suffix + registered-exact + orphan) + asserts all 3 in output. Pre-empt: any refactor of exact-match → delimiter-aware grouping — enumerate existing "orphan / unmatched" behavior + preserve via second-query fallback. **Banked as Expansion #10 CANDIDATE** because the 5 sub-disciplines are sufficiently distinct from Expansions #1-#9 yet thematically unified (all 5 surfaced in a single brainstorming Codex chain; all 5 are "audit before lock" pre-Codex discipline; together they form an "architectural-decision-quality" audit cluster). Discriminating-test pattern across all 5: pre-Codex review checklist gains 5 explicit verification steps per sub-discipline before any architectural lock fires.
- **Taxonomy propagation audit (NEW Expansion #11 candidate BINDING for 30th cumulative validation onwards).** Failure mode discovered 2026-05-22 PM #3 at Phase 13 T4.SB writing-plans Codex R1 + R3 + R4 `kind` enum propagation 3-instance lesson. When a plan introduces a NEW dataclass with a `kind`/`status`/`type` enum field, the enum value MUST propagate to (a) related entry/result dataclasses; (b) the CSV/JSON serializer header column list; (c) the markdown matrix table column; (d) all test fixtures. Three Codex rounds were needed to fully scrub the T-T4.SB.1 sensitivity-harness `{additive, gate, threshold}` taxonomy: R2 MAJOR #2 introduced the 3-value taxonomy + binding Notes paragraph; R3 MAJOR #1 caught that `kind` was NOT propagated to `SweepEntry` dataclass / CSV header / markdown matrix Kind column (resolution: widened scope); R3 MAJOR #2 caught that gate-variable test fixtures still used legacy `kind="additive"` (resolution: fixture-sweep). **Pre-empt at writing-plans + executing-plans phases**: writing-plans §5 watch item — when a plan introduces a NEW enum field, the plan's own §F test scope MUST enumerate per-test-fixture per-dataclass per-serializer disposition of the enum field BEFORE Codex invocation. Enumerate (a) every dataclass that consumes the enum + every dataclass that derives from it + every serializer (CSV, JSON, markdown) header list; (b) every test fixture that constructs an instance + asserts on the enum; (c) propagation invariant assertion via discriminating test that round-trips the enum through dataclass → serializer → reader. **Discriminating-test pattern**: synthetic fixture with each enum value + assert serializer column list / dataclass field set / test fixture coverage matches. **Banked as Expansion #11 CANDIDATE** because the lesson is distinct from Expansion #4 (SQL-column verification) + Expansion #10 (architecture-location); this is ENUM-TAXONOMY PROPAGATION across dataclass + serializer + fixture surfaces. Pattern complement to the existing schema-CHECK + Python-constant + dataclass-validator paired discipline gotcha family + T3.SB2 hotfix surface-guard widening gotcha — same family extended to enum-field propagation across N-deep dataclass + serializer + fixture surfaces.
