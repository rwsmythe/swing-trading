# Phase 0 task list — Research branch

Per V2.1 §X tranche 1. Time-budget anchor: 4–8 hours/week total project time, 70/30 production/research. Realistic allocation: 1–2 hours/week on research during Phase 0.

Tasks are sized for completion in one evening each (2–4 hours). No task larger than 4 hours; split if it grows.

## Done

- [x] Adopt V2.1 as governing strategy (Tranche A, 2026-04-23).
- [x] Create research-branch scaffolding: directory, README, method-record template (Tranche B-research, 2026-04-23).
- [x] First method record: earnings-proximity exclusion (Tranche B-research, 2026-04-23).
- [x] First study design: earnings-proximity parameter sweep (Tranche B-research, 2026-04-23).
- [x] Evaluate ≥2 free earnings-calendar sources for date-precision accuracy (Tranche B-research 2a, 2026-04-24). See `notes/earnings-calendar-sources.md`. Decision: yfinance `get_earnings_dates()`; 5/5 primary-source spot-check matches.
- [x] Decide historical-candidate data source (Tranche B-research 2a, 2026-04-24). See `notes/historical-candidate-source-decision.md`. Decision: Option B (synthetic replay); repo has 2 A+ signals vs. ≥120 threshold; ~77% of replay logic reusable from `swing/evaluation`, `swing/recommendations`, `swing/trades`.

## Next

- [ ] **Operator: run pattern cohort detector evaluator harness against `tightness_1.005_flips_67` cohort** — `python -m swing.cli diagnose pattern-cohort-detect --cohort-csv exports/research/cohorts/tightness_1.005_flips_67.csv --db "$USERPROFILE/swing-data/swing.db" --output-dir exports/research/ --window-mode per-window --template-match on`. Capture output triple `{results.csv, summary.md, manifest.json}` + commit. Populate `Results` section at `research/studies/2026-05-24-pattern-cohort-detection.md` + cross-tabulate with V2 OHLCV backtest verdict at merge `e0a9edd` (17 patterns / 5 triggered / -0.18R). **Pattern cohort harness SHIPPED 2026-05-24** (SECOND applied-research method-record COMPLETED; status `research` v0.1.0 at `research/method-records/pattern-cohort-detection.md`).

- [ ] **Operator: run V2 OHLCV harness against swing-data/swing.db** — `swing diagnose aplus-sensitivity-v2 --db "$USERPROFILE/swing-data/swing.db" --eval-runs 63 --output-dir exports/diagnostics/` (acceptance target: <60 min per OQ-9). Capture output files + commit to `exports/diagnostics/`. Populate the findings table at `research/studies/2026-05-23-v2-ohlcv-criterion-evaluator.md`. **V2 OHLCV harness SHIPPED at `f467ff5`** (2026-05-23; first method-record COMPLETED per OQ-CL.3 LOCK; method-record at `research/method-records/aplus-criteria-calibration.md` bumped to 0.2.0).

- [ ] **Review V2 findings + pick binding variables** — after operator DB run, review the sensitivity matrix. If any of the 15 threshold dials produce non-zero `delta_aplus` / `delta_watch`, those are binding variables. Rank by marginal A+ per loosening unit. Propose cfg-policy candidates for shadow promotion per `research/method-records/aplus-criteria-calibration.md` promotion ladder §"shadow -> production" criteria. If all 15 are non-binding, the conclusion is no-near-threshold candidates; see V2 study §"Conclusion" for interpretation guidance.

- [ ] **Build the study harness** — minimum viable Python script (can live under `research/` as a notebook or plain `.py`; does not go under `swing/`). Reads historical candidates, applies each `blackout_trading_days` variant, emits expectancy/gap-rate/gap-loss/signal-volume metrics. (3–4 hours.) *Note: earnings-proximity exclusion study deferred while V2 OHLCV arc was prioritized; resumable per Path B sequencing.*

- [ ] **Run the study** — execute harness with variants ∈ {0, 3, 5, 7, 10}. Capture raw output. (1 hour.)

- [ ] **Write evidence summary** — `research/studies/earnings-proximity-exclusion-results.md`. Includes decision (reject/shadow/promote) and rationale. Triggers copowers adversarial review per Tranche-B-research session 2. (2–3 hours.)

- [ ] **Evaluate which A+-like indicators from source-of-truth references warrant method-records + studies** (promoted from "Later (deferred)" 2026-05-22 PM at Phase 13 T4.SB executing-plans T-T4.SB.1). Context: V1 sensitivity harness shipped under T4.SB answered the 2 gate dials; V2 OHLCV criterion-evaluator harness now answers the 15 threshold dials (SHIPPED 2026-05-23). Next-action sequence post-V2-findings-review: pick 1-3 indicators to advance → author method-record stub(s) per `method-records/_template.md` → design study per `studies/earnings-proximity-exclusion.md` precedent. **V2.5 candidates banked** (per spec §M.4 + brainstorming return report §4): promote `vcp.watch_max_fails` to cfg-derived; `old_criterion_failure` per-criterion attribution; parquet bulk-read via pyarrow; concurrent.futures parallelism; `allowed_miss_names` sweep (V3+); `benchmark_ticker` sweep (V3+); 2D cross-coupling (V3+). **Posture per V2.1 §IX**: minimum viable governance -- let V2 outputs inform the next applied-research arc + Phase 14 commissioning consideration per Path B sequencing.

## Applied research candidates (backlog; post-2026-05-27 arc closure)

New applied-research topics surfaced AFTER the W-pattern ruleset arc closed (`research/studies/2026-05-27-applied-research-arc-closure.md`). Not yet commissioned; each becomes a method-record + study when the operator schedules it.

- [x] **Minervini correct-entry exemplar recall / sensitivity validation — SHIPPED + CLOSED 2026-06-09** (full copowers cycle brainstorm `8efb33ce`->writing-plans `292fff17`->executing `0a4994c2`, each Codex-converged; harness `research/harness/minervini_exemplar_recall/` + CLI `swing diagnose minervini-recall`; study `research/studies/2026-06-08-minervini-exemplar-recall.md`; operator smoke run `exports/research/minervini-exemplar-recall-20260609T021301Z/`; headline: window-sweep screening recall **0.90 screenable [18/20]**, **7/27 young-name `skip_insufficient_history`**, the **Stage-2 8/8-TT detector gate** the binding constraint AND the source of detector specificity; methodological-infrastructure + diagnostic, NOT a deployable ruleset change). **The original entry's 'physical-book-only / operator must transcribe' premise was OBSOLETE** — exemplars were extracted IN-HOUSE from the transcribed agent-readable corpus (commissioning brief §2; memory `project_references` corrected 2026-06-04). _Original entry, for provenance:_ (operator-requested 2026-06-04; BACKLOG — NOT for action now). **Question:** given Minervini's documented *correct-entry* examples ("Trade Like a Stock Market Wizard" + the Disciplined Swing Trader references; physical-book-only per memory `project_references` — the operator must transcribe each as `ticker + approximate entry date + setup label`), would our **screening + filtering** pipeline have PASSED each exemplar as price crossed the entry point (i.e. would `bucket_for` [`swing/evaluation/scoring.py`] land it in `aplus`/`watch` at the entry-crossing session), AND would any of our **indications** have FIRED to flag the setup was reached (the 5 V1 geometric detectors in `_step_pattern_detect`; the A+ criteria / trend-template / VCP gates; the buy/sell advisory rules)? **This is a true-positive RECALL / sensitivity test** — does the pipeline CATCH known-good setups — complementary to (NOT the same as) the closed arc's expectancy test (which asked whether rulesets PROFIT). A pass means our gates would have surfaced the setup; a miss localizes which gate (trend/VCP/proximity/risk) silently rejected a Minervini-correct entry. **Method sketch:** point-in-time replay — fetch historical OHLCV (yfinance `period="max"` for pre-2021 examples per gotcha #29), evaluate each exemplar AS OF its entry-crossing session with NO lookahead, record per-gate pass/fail + which detectors fired + the bucket assignment. **Leverages existing infra:** the `pattern_exemplars` table (label_source/final_decision) could hold the Minervini set; the pattern-cohort detector harness (`research/method-records/pattern-cohort-detection.md`) + the 9-metric scorecard (arc-closure Sec 6 #1) are reusable. **Binding prerequisite (operator input):** the transcribed exemplar list (ticker, entry date, setup type) — the study cannot start without it. **Disciplines:** the #33–#37 brief-authoring family; strict point-in-time/as-of evaluation (no lookahead); #29 historical depth; #24/#26 archive-freshness. **NOT temporal-log-gated** (it uses Minervini ground truth, not forward-walk accumulation) — so it can run BEFORE the B-1..B-8 set, once the operator supplies the exemplar list.

- [ ] **VCP threshold calibration — BANKED (NOT recall-motivated; do NOT commission off the recall study).** Surfaced as a *non*-follow-up during the 2026-06-09 Minervini recall-arc debrief. The recall study found **no evidence VCP is mis-tuned**: with correct (window-sweep) timing the VCP gate passes **0.90** of screenable exemplars and only **1/20** screenable misses localized to VCP (the single-session VCP failures are the by-design breakout-bar artifact — the VCP proximity/tightness criteria measure the pre-breakout coil, which has released by the breakout bar — NOT a calibration signal). **A recall test on known-good setups can only detect a gate that is *too tight* (rejecting winners); it is structurally blind to a gate that is *too loose* (admitting losers).** "Is VCP too loose / mis-calibrated for profitability" is an **EXPECTANCY** question — the space the **closed 2026-05-27 applied-research arc** already worked (9 rulesets x 4 substrates -> no deployable positive finding), now living in the **temporal-log-gated B-1..B-8 set** (needs months of forward-walk data). So: do NOT commission a VCP calibration off the recall study; if revisited, it is an expectancy study gated behind temporal-log accumulation, not a recall arc. (Contrast the two recall-motivated follow-ups, which ARE warranted: `docs/young-name-screening-variant-commissioning-brief.md` + `docs/stage2-detector-gate-ratification-commissioning-brief.md`.)

- [ ] **B-1..B-8 strategic / applied-research set** (moved out of operational Phase 15 -> the research branch, operator-decided 2026-06-04). The continuation of the closed 2026-05-27 W-pattern ruleset arc (`research/studies/2026-05-27-applied-research-arc-closure.md` Sec 7 future-revisit predicates). **Temporal-log-accumulation-gated** -- these resume once the v22 `pattern_detection_events`/`pattern_forward_observations` log (now fed the ~83x watch population by the 2026-06-04 pool-widening) accumulates forward-walk data over months (the arc-closure predicate: N>=100 patterns + frozen regeneration semantics + multi-substrate consistency). The set: **B-1** substrate-size augmentation; **B-2** Finviz filter widening (operator declined the price-band widening 2026-05-25; revisit scope); **B-3** cohort-stability LOCK (gotcha #37 already eliminated-by-construction in the v22 log); **B-4** D2 `canonical_survival_rate` L4 remediation; **B-5** real-time prospective tracking (D+E hybrid); **B-6** multi-pattern composite signals; **B-8** other-gates market-regime investigation. Any deployable ruleset/signal a B-investigation surfaces spawns a FUTURE operational arc (routed through the source-of-truth correction protocol V2.1 §VII.F), but the investigations are research. Discipline family: arc-closure gotchas #33-#37.

## Later (deferred)

- [ ] Second method record authored (TBD — orchestrator will choose based on study outcomes and operator-branch priorities).
- [ ] Parity test harness — single pytest fixture + helper. Deferred until the first promotion package needs it; minimum viable is fixture-identity on a synthetic input.
- [ ] Cross-study shared utility code (if any pattern emerges across multiple studies). Deferred per V2.1 §V.B until a second study actually surfaces a shared pattern.

## Off the list (V2.1 §VIII deferred)

Explicitly NOT Phase 0 work:

- Full signal-registry platform features.
- Generated code from registry schema.
- Pre-commit enforcement of registry transitions.
- Generalized experiment framework.
- Broad evidence-tier bureaucracy.
- Broad vendor-equivalence frameworks.
- Generalized panel-data architecture.

Revisit these only when a concrete active study demands them and no cheaper alternative exists.
