"""Research harnesses — study-specific replay drivers and metrics aggregators.

Each subpackage is a standalone harness for one research study (see
`research/studies/`). Harnesses import READ-ONLY from `swing/` per the
CLAUDE.md Phase-isolation invariant.
"""
