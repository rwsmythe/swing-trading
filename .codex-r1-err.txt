OpenAI Codex v0.135.0
--------
workdir: /mnt/c/Users/rwsmy/swing-arc3
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019eb9c0-99bb-7812-b790-be3d4a6d4b9e
--------
user
You are an adversarial code reviewer. Do NOT run git or any VCS command. The
complete diff to review is in the file .codex-diff.txt (read it directly from
disk). The repo source is on disk under this directory — read any file you need
for context.

CONTEXT — Phase 16 / Arc 3 of a swing-trading app. The bug: ticker XMAX's
dashboard watchlist THUMBNAIL rendered ~16 sparse price points while its
ticker_detail chart rendered ~207 rich bars for the SAME data_asof_date. Root
cause (confirmed): the pipeline market-data ladder's `_bars_hook`
(swing/pipeline/runner.py, installed by `_install_pipeline_marketdata_caches`)
returned the freshly-fetched Schwab window VERBATIM on the schwab_api success
path (`window.to_dataframe()`). For tickers with short Schwab listing history
(XMAX = 16 Schwab daily bars, TDAY = 138, vs a ~1260-row legacy archive), that
truncated the thumbnail. The web ticker_detail path has no ladder and reads the
full archive via `read_or_fetch_archive`, so it stayed rich. This violated the
standing "cache hooks must return the FULL archive; consumers slice" contract.

THE FIX (3 commits):
1. `_bars_hook`: on the schwab_api success path, return the FULL archive (via
   `_yf_window_fallback` → read_or_fetch_archive — the SAME read the no-ladder
   path uses) instead of `window.to_dataframe()`. The ladder still fetches +
   persists the Schwab Shape-A archive (audit + provenance preserved). The
   yfinance fallback path already returned the full archive.
2. New `compute_chart_source_hash(bars)` helper in swing/web/charts.py keyed on
   (bar count + first/last asof_date).
3. Wire that hash into the chart_renders write sites (pipeline _step_charts
   _refresh_one × 3 surfaces + JIT get_or_render_surface), replacing the static
   literals "step_charts_v1" / "chart_jit_v1".

HARD CONSTRAINTS the fix must respect: NO schema change; swing/trades/ and
swing/data/migrations/ untouched; the Arc-6 warm + Arc-8 NaN barrier in
swing/data/ohlcv_archive.py untouched; the Schwab QUOTE ladder (PriceCache)
untouched; Shape-A persistence (write_window) must NOT be deleted/stopped.

ADVERSARIALLY PROBE (be skeptical; verify against the on-disk source, not just
the diff):
1. Does the `_bars_hook` fix actually make the pipeline thumbnail path and the
   web ticker_detail path converge on IDENTICAL bar counts for a ticker with a
   partial Schwab file + full legacy archive? Are there inputs where they still
   diverge?
2. Fidelity of the reproduction test to the REAL pipeline configuration — does
   it faithfully exercise the installed ladder hook, or does it stub so much
   that it proves nothing?
3. Does the fix hold for ALL affected tickers (XMAX, TDAY, and the ~47 other
   *.schwab_api.parquet tickers), not just XMAX? Any ticker where the new
   behavior regresses (e.g. a ticker with NO legacy archive, only Schwab data)?
4. Side effects of returning the full archive instead of the Schwab window:
   does any consumer of `_bars_hook`'s return value depend on the old
   schwab-shaped/short behavior? Is the provider_tag still correct/meaningful?
   Is `window.to_dataframe()` now dead code anywhere it matters?
5. The 3c hash: collision behavior (two genuinely different frames hashing the
   same?), determinism, the empty-frame sentinel, and whether the
   best-effort try/except in chart_jit could silently mask a real bug.
6. Does the 3c change actually cause "re-render on data growth", or is
   source_data_hash a write-only field never read for invalidation (in which
   case is the change merely cosmetic)? State the truth.
7. Any lock violation (schema, trades/, migrations/, ohlcv_archive.py warm/NaN
   barrier, the quote ladder).

Output format: a numbered list of findings. Each finding tagged
[CRITICAL] / [MAJOR] / [MINOR] / [NIT] with file:line and a concrete rationale.
If you find no critical or major issues, end your response with exactly:
NO_NEW_CRITICAL_MAJOR

codex
I’ll review from the provided diff and on-disk files only, and I’ll avoid all git/VCS commands as requested. I’m going to inspect the diff first, then read the relevant pipeline, chart, cache, and test code around the changed paths.
exec
/bin/bash -lc "sed -n '1,260p' .codex-diff.txt" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
diff --git a/swing/pipeline/runner.py b/swing/pipeline/runner.py
index f2864273..e532a826 100644
--- a/swing/pipeline/runner.py
+++ b/swing/pipeline/runner.py
@@ -109,6 +109,7 @@ from swing.trades.advisory import AdvisoryContext, compute_all_suggestions
 from swing.trades.equity import current_equity, sizing_equity
 from swing.watchlist.service import compute_watchlist_changes
 from swing.web.charts import (
+    compute_chart_source_hash,
     render_market_weather_svg,
     render_position_detail_svg,
     render_watchlist_thumbnail_svg,
@@ -473,14 +474,31 @@ def _install_pipeline_marketdata_caches(
             frequency_type="daily",
             frequency=1,
         )
-        # When provider='yfinance', `window` is whatever the yfinance
-        # fallback returned (bars DataFrame). When provider='schwab_api',
-        # `window` is a SchwabPriceHistoryWindow — convert to bars-shape
-        # via the T-C.1 mapper's `to_dataframe()` helper.
-        if provider_tag == "schwab_api" and hasattr(window, "to_dataframe"):
-            bars = window.to_dataframe()
-        else:
-            bars = window
+        # Full-archive-return contract (CLAUDE.md: "cache hooks must return the
+        # FULL archive; consumers slice"). Phase 16 Arc 3 fix — the symmetric
+        # sibling of the Phase 13 R3 Major #1 yfinance-fallback fix.
+        #
+        # On the schwab_api SUCCESS path `window` is ONLY the freshly-fetched
+        # Schwab sub-window. For a ticker whose Schwab listing history is short
+        # (XMAX = 16 daily Schwab bars; TDAY = 138 — vs a ~1260-row legacy
+        # archive), `window.to_dataframe()` returned just those bars, so
+        # `OhlcvCache._fetch_bars_window` sliced an already-short frame and the
+        # pipeline-rendered watchlist THUMBNAIL drew a handful of sparse points
+        # while the web `ticker_detail` path (no ladder → read_or_fetch_archive)
+        # stayed rich for the SAME data_asof_date. The ladder has ALREADY
+        # persisted the fetched Schwab bars to the Shape-A archive (audit +
+        # provenance preserved by `fetch_window_via_ladder`); here we return the
+        # FULL archive — the SAME read the no-ladder/ticker_detail path uses —
+        # so both surfaces converge on identical bar counts. The yfinance
+        # fallback path already returns the full archive (`_yf_window_fallback`
+        # → read_or_fetch_archive), so this unifies both provider paths.
+        # schwab_api → re-read the full archive (above); yfinance → `window` is
+        # already the full archive frame from `_yf_window_fallback`.
+        bars = (
+            _yf_window_fallback(ticker, None, None)
+            if provider_tag == "schwab_api"
+            else window
+        )
         return (bars, provider_tag)
 
     price_cache.set_ladder_fetcher(_quote_hook)
@@ -3405,14 +3423,14 @@ def _step_charts(*, cfg, lease: Lease, eval_run_id: int, data_asof: str,
 
     def _refresh_one(*, ticker: str, surface: str,
                      pipeline_run_id: int | None, pattern_class: str | None,
-                     bytes_: bytes) -> None:
+                     bytes_: bytes, source_data_hash: str) -> None:
         try:
             chart_render = ChartRender(
                 id=None,
                 ticker=ticker,
                 surface=surface,
                 chart_svg_bytes=bytes_,
-                source_data_hash="step_charts_v1",
+                source_data_hash=source_data_hash,
                 rendered_at=_now_iso,
                 data_asof_date=data_asof,
                 pipeline_run_id=pipeline_run_id,
@@ -3469,6 +3487,7 @@ def _step_charts(*, cfg, lease: Lease, eval_run_id: int, data_asof: str,
             ticker=ticker, surface="watchlist_row",
             pipeline_run_id=lease.run_id, pattern_class=None,
             bytes_=svg_bytes,
+            source_data_hash=compute_chart_source_hash(bars),
         )
 
     # ticker_detail surface — Phase 13 T-T4.SB.3 (OQ-5.3 LOCK): pre-gen
@@ -3510,6 +3529,7 @@ def _step_charts(*, cfg, lease: Lease, eval_run_id: int, data_asof: str,
                 ticker=ticker, surface="position_detail",
                 pipeline_run_id=None, pattern_class=None,
                 bytes_=svg_bytes,
+                source_data_hash=compute_chart_source_hash(bars),
             )
 
     # market_weather surface — cfg.rs.benchmark_ticker per Codex R2 MAJOR #4
@@ -3567,6 +3587,7 @@ def _step_charts(*, cfg, lease: Lease, eval_run_id: int, data_asof: str,
                 ticker=benchmark_ticker, surface="market_weather",
                 pipeline_run_id=lease.run_id, pattern_class=None,
                 bytes_=svg_bytes,
+                source_data_hash=compute_chart_source_hash(display_bars),
             )
 
     # End-of-step summary. Denominator is classifier attempts (success +
diff --git a/swing/web/chart_jit.py b/swing/web/chart_jit.py
index cb0bfac8..ff5e265e 100644
--- a/swing/web/chart_jit.py
+++ b/swing/web/chart_jit.py
@@ -41,6 +41,7 @@ from swing.data.repos.chart_renders import (
     refresh_chart_render,
 )
 from swing.web.charts import (
+    compute_chart_source_hash,
     render_market_weather_svg,
     render_position_detail_svg,
     render_ticker_detail_svg,
@@ -196,13 +197,22 @@ def get_or_render_surface(
         )
         return None
 
+    # Phase 16 Arc 3 (3c): stamp a content-derived hash (bar count + first/last
+    # asof_date) instead of the static ``source_data_hash`` default so the cache
+    # row's provenance changes on data growth. Falls back to the passed literal
+    # only if hashing fails (defensive — never block the write-through).
+    try:
+        computed_hash = compute_chart_source_hash(bars)
+    except Exception:  # noqa: BLE001 — provenance is best-effort
+        computed_hash = source_data_hash
+
     try:
         chart_render = ChartRender(
             id=None,
             ticker=ticker,
             surface=surface,
             chart_svg_bytes=svg_bytes,
-            source_data_hash=source_data_hash,
+            source_data_hash=computed_hash,
             rendered_at=datetime.now(UTC).strftime(
                 "%Y-%m-%dT%H:%M:%SZ",
             ),
diff --git a/swing/web/charts.py b/swing/web/charts.py
index af6e75c1..4bd83064 100644
--- a/swing/web/charts.py
+++ b/swing/web/charts.py
@@ -74,6 +74,32 @@ except ImportError as exc:  # pragma: no cover - install gate
 
 logger = logging.getLogger(__name__)
 
+
+def compute_chart_source_hash(bars: Any) -> str:
+    """Content-derived value for ``chart_renders.source_data_hash``.
+
+    Phase 16 Arc 3 (3c): the pipeline + JIT write sites previously stamped a
+    STATIC literal (``"step_charts_v1"`` / ``"chart_jit_v1"``) which does not
+    encode the underlying data, so the field never changed when a chart's bar
+    history grew (e.g. a sparse 16-bar XMAX thumbnail vs a rich 207-bar render)
+    or its window shifted. Keying on (bar count + first/last asof_date) makes
+    the value honest: data growth → a different hash. VALUE-only change (no
+    schema). The field remains a provenance/audit tag today; an honest value is
+    the substrate for any future read-time hash-compare invalidation.
+
+    ``bars`` is the legacy yfinance-shape frame (DatetimeIndex + capitalized
+    OHLCV) the renderers consume. None / empty → a stable sentinel.
+    """
+    if bars is None or len(bars) == 0:
+        return "bars=0"
+    n = len(bars)
+    first = bars.index[0]
+    last = bars.index[-1]
+    first_iso = first.date().isoformat() if hasattr(first, "date") else str(first)
+    last_iso = last.date().isoformat() if hasattr(last, "date") else str(last)
+    return f"bars={n};first={first_iso};last={last_iso}"
+
+
 # Process-wide matplotlib render lock. charts.py renders through pyplot GLOBAL
 # state (matplotlib.pyplot as plt, mpf.plot, plt.subplots, plt.close) which is
 # NOT thread-safe and has no other serialization. Every top-level web render
diff --git a/tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py b/tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py
new file mode 100644
index 00000000..3d4ff056
--- /dev/null
+++ b/tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py
@@ -0,0 +1,195 @@
+"""Phase 16 / Arc 3 — the XMAX watchlist-thumbnail divergence root-cause fix.
+
+Sibling of ``test_yf_window_fallback_returns_full_archive.py`` (the Phase 13
+T1.SB0 Codex R3 Major #1 fix), but for the **Schwab-SUCCESS** path of the
+pipeline ladder's ``_bars_hook``.
+
+Root cause (hypothesis (a), confirmed): when ``_install_pipeline_marketdata_caches``
+installs the Schwab market-data ladder on the pipeline's OhlcvCache, the
+``_bars_hook`` returned the freshly-fetched Schwab window VERBATIM on the
+schwab_api success path (``bars = window.to_dataframe()``). For a ticker whose
+Schwab listing history is short (XMAX = 16 daily Schwab bars vs a 1260-row
+legacy archive; TDAY = 138 vs 1260), ``to_dataframe()`` yields only that short
+window. ``OhlcvCache._fetch_bars_window`` then slices ``[cutoff, end]`` but
+cannot recover history absent from the returned frame, so the pipeline-rendered
+watchlist THUMBNAIL drew ~16 sparse points while the web ``ticker_detail`` path
+(no ladder → ``read_or_fetch_archive`` → 1260-row legacy archive → ~207 bars)
+stayed rich. This VIOLATES the standing "cache hooks must return the FULL
+archive; consumers slice" contract.
+
+The yfinance-fallback path was ALREADY fixed (R3 Major #1) to return the full
+archive via ``read_or_fetch_archive``; the Schwab-success path was not. This
+file pins the symmetric fix and the discriminating convergence regression: the
+ladder (thumbnail) path and the no-ladder (ticker_detail) path must yield the
+SAME bar count for a ticker with a partial Schwab Shape-A file + a full legacy
+archive.
+"""
+from __future__ import annotations
+
+from pathlib import Path
+from types import SimpleNamespace
+from unittest.mock import MagicMock
+
+import pandas as pd
+
+from swing.data.db import ensure_schema
+from swing.integrations.schwab.models import (
+    OhlcvBar,
+    SchwabPriceHistoryWindow,
+)
+
+
+def _make_pipeline_cfg(db_path: Path, tmp_path: Path):
+    """SimpleNamespace cfg shape consumed by ``_install_pipeline_marketdata_caches``
+    + ``_bars_hook`` + ``OhlcvCache`` (mirrors
+    tests/pipeline/test_bars_hook_requests_daily_frequency.py:_make_pipeline_cfg)."""
+    paths_ns = SimpleNamespace(
+        db_path=Path(db_path),
+        prices_cache_dir=tmp_path / "prices-cache",
+    )
+    paths_ns.prices_cache_dir.mkdir(parents=True, exist_ok=True)
+    schwab_ns = SimpleNamespace(
+        environment="production",
+        marketdata_ladder_enabled=True,
+    )
+    integrations_ns = SimpleNamespace(schwab=schwab_ns)
+    web_ns = SimpleNamespace(
+        ohlcv_cache_ttl_seconds=3600,
+        max_concurrent_ohlcv_fetches=4,
+        circuit_breaker_cooldown_seconds=60,
+        db_busy_timeout_ms=30000,
+    )
+    archive_ns = SimpleNamespace(archive_history_days=1260)
+    return SimpleNamespace(
+        paths=paths_ns,
+        integrations=integrations_ns,
+        web=web_ns,
+        archive=archive_ns,
+    )
+
+
+def _full_legacy_archive(end: pd.Timestamp, n_rows: int = 1200) -> pd.DataFrame:
+    """The real legacy ``{TICKER}.parquet`` in-memory shape: DatetimeIndex +
+    capitalized OHLCV columns, ~5y of business days."""
+    idx = pd.bdate_range(end=end, periods=n_rows)
+    closes = [100.0 + i * 0.05 for i in range(n_rows)]
+    return pd.DataFrame(
+        {
+            "Open": [c - 0.05 for c in closes],
+            "High": [c + 0.30 for c in closes],
+            "Low": [c - 0.30 for c in closes],
+            "Close": closes,
+            "Volume": [1_000_000 + i for i in range(n_rows)],
+        },
+        index=idx,
+    )
+
+
+def _short_schwab_window(ticker: str, end: pd.Timestamp, n_days: int = 16):

exec
/bin/bash -lc 'rg -n "_bars_hook|_install_pipeline_marketdata_caches|_step_charts|_refresh_one|compute_chart_source_hash|source_data_hash|chart_jit_v1|step_charts_v1|chart_renders|get_or_render_surface|read_or_fetch_archive|write_window|PriceCache" swing tests .codex-diff.txt' in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 660ms:
tests/trades/test_advisory.py:617:    DB-sourced §4.A.bis advisory under PriceCache degradation.
swing/integrations/schwab/models.py:407:      last_price: primary consumed field; written to PriceCache via ladder.
swing/integrations/schwab/models.py:622:        ``swing.data.ohlcv_archive.write_window``.
swing/integrations/schwab/models.py:626:        the Schwab success path in ``swing/pipeline/runner.py:_bars_hook``.
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:11:Post-fix: `_yf_window_fallback` calls ``read_or_fetch_archive`` directly,
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:38:    # _bars_hook opens a DB connection per call — schema must be ready.
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:69:    from swing.pipeline.runner import _install_pipeline_marketdata_caches
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:75:    def _stub_read_or_fetch_archive(ticker, *, end_date, cache_dir, archive_history_days):
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:82:        "swing.pipeline.runner.read_or_fetch_archive", _stub_read_or_fetch_archive,
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:103:    # fetch_window_via_ladder` inside `_install_pipeline_marketdata_caches`;
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:109:        # Phase 13 T1.SB0 gate-fix (T-GF2): _bars_hook now passes
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:122:    # Construct a mock Schwab client so _install_pipeline_marketdata_caches
tests/pipeline/test_yf_window_fallback_returns_full_archive.py:125:    price_cache, ohlcv_cache, _audit_conn = _install_pipeline_marketdata_caches(
swing/web/view_models/patterns/review_form.py:471:    Per plan §G.3 Step 4: read OHLCV via ``read_or_fetch_archive``
swing/web/view_models/patterns/review_form.py:478:        from swing.data.ohlcv_archive import read_or_fetch_archive
swing/web/view_models/patterns/review_form.py:482:        df = read_or_fetch_archive(
swing/integrations/schwab/marketdata_ladder.py:103:    ``swing.data.ohlcv_archive.write_window``.
swing/integrations/schwab/marketdata_ladder.py:129:        ``_yf_download_window`` → ``read_or_fetch_archive`` legacy path);
swing/integrations/schwab/marketdata_ladder.py:137:    ``write_window`` call against ``None`` (which ``write_window``'s
swing/integrations/schwab/marketdata_ladder.py:192:        from swing.data.ohlcv_archive import write_window
swing/integrations/schwab/marketdata_ladder.py:193:        write_window(ticker, df, provider, cache_dir=cache_dir)
swing/web/view_models/patterns/exemplars.py:195:    the exemplars surface we want the most recent chart_renders row
swing/web/view_models/patterns/exemplars.py:201:        "SELECT chart_svg_bytes FROM chart_renders "
swing/web/view_models/patterns/exemplars.py:218:    Cache-hit path: reads bytes from chart_renders via the substrate
swing/web/view_models/patterns/exemplars.py:226:    ALSO writes the live bytes through to chart_renders via the
swing/web/view_models/patterns/exemplars.py:264:                from swing.data.repos.chart_renders import (
swing/web/view_models/patterns/exemplars.py:318:                            source_data_hash="exemplar_live_render",
tests/pipeline/test_step_pattern_detect_temporal_extension.py:50:    row = conn.execute("SELECT surface, pattern_class FROM chart_renders "
swing/web/view_models/open_positions_row.py:32:from swing.web.price_cache import PriceCache, PriceSnapshot
swing/web/view_models/open_positions_row.py:95:    *, trade: Trade, cfg: Config, cache: PriceCache, executor,
swing/web/view_models/open_positions_row.py:194:    # even when PriceCache is degraded; run the price-independent subset
swing/web/view_models/open_positions_row.py:258:    # -zones SVG, read from the existing `chart_renders` cache (run-agnostic; the
swing/web/view_models/open_positions_row.py:269:    cache: PriceCache | None = None, executor=None, ohlcv_cache=None,
swing/web/view_models/open_positions_row.py:369:    from swing.data.repos.chart_renders import get_cached_chart_svg
tests/web/test_weather_trend_state.py:3:(pipeline `_step_charts` + the interactive `POST /dashboard/weather-chart/refresh`
tests/web/test_weather_trend_state.py:28:from swing.data.repos.chart_renders import refresh_chart_render
tests/web/test_weather_trend_state.py:36:# Pipeline-site harness (reuses the _step_charts substrate shape).
tests/web/test_weather_trend_state.py:109:def _run_step_charts(*, cfg, run_id, eval_run_id, ohlcv_cache):
tests/web/test_weather_trend_state.py:111:    from swing.pipeline.runner import _step_charts
tests/web/test_weather_trend_state.py:113:    _step_charts(
tests/web/test_weather_trend_state.py:152:    _run_step_charts(
tests/web/test_weather_trend_state.py:163:    "undefined" (NOT "n/a"), logs a WARNING, and `_step_charts` completes
tests/web/test_weather_trend_state.py:176:        _run_step_charts(
tests/web/test_weather_trend_state.py:232:                source_data_hash="hash-w",
tests/web/test_weather_trend_state.py:307:    from swing.web.chart_jit import get_or_render_surface
tests/web/test_weather_trend_state.py:330:        get_or_render_surface(
tests/web/test_weather_trend_state.py:360:    _run_step_charts(
tests/web/test_watchlist_row_no_lightning_glyph.py:21:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_watchlist_row_no_lightning_glyph.py:35:        PriceCache, "get_many",
tests/web/test_watchlist_row_no_lightning_glyph.py:40:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_watchlist_row_no_lightning_glyph.py:41:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_chart_jit_option_a_invariant.py:7:``chart_renders`` keyed on the SAME ``pipeline_run_id``. When a fresher
tests/web/test_chart_jit_option_a_invariant.py:29:from swing.web.chart_jit import get_or_render_surface
tests/web/test_chart_jit_option_a_invariant.py:70:    chart_renders rows (one per run_id); the OLD run_id's row is
tests/web/test_chart_jit_option_a_invariant.py:94:        bytes_v100 = get_or_render_surface(
tests/web/test_chart_jit_option_a_invariant.py:115:        bytes_v101 = get_or_render_surface(
tests/web/test_chart_jit_option_a_invariant.py:128:        "SELECT pipeline_run_id, chart_svg_bytes FROM chart_renders "
tests/web/test_watchlist_pivot_column.py:19:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_watchlist_pivot_column.py:27:    """Patch PriceCache.get_many so the dashboard / watchlist rendering does
tests/web/test_watchlist_pivot_column.py:42:        PriceCache, "get_many",
tests/web/test_watchlist_pivot_column.py:47:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_watchlist_pivot_column.py:48:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_base_layout_vm_recent_multi_leg_field.py:308:    """Stub PriceCache.get_many to return empty dict (no executor needed)."""
tests/web/test_base_layout_vm_recent_multi_leg_field.py:309:    from swing.web.price_cache import PriceCache
tests/web/test_base_layout_vm_recent_multi_leg_field.py:311:        PriceCache, "get_many",
tests/web/test_base_layout_vm_recent_multi_leg_field.py:314:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_base_layout_vm_recent_multi_leg_field.py:315:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_base_layout_vm_recent_multi_leg_field.py:320:    from swing.web.price_cache import PriceCache
tests/web/test_base_layout_vm_recent_multi_leg_field.py:335:    cache = PriceCache(cfg=cfg)
tests/web/test_base_layout_vm_recent_multi_leg_field.py:402:    from swing.web.price_cache import PriceCache
tests/web/test_base_layout_vm_recent_multi_leg_field.py:420:    cache = PriceCache(cfg=cfg)
tests/web/test_chart_jit.py:21:from swing.web.chart_jit import get_or_render_surface
tests/web/test_chart_jit.py:68:            "INSERT INTO chart_renders "
tests/web/test_chart_jit.py:70:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_chart_jit.py:78:def test_get_or_render_surface_cache_hit_returns_cached_bytes(
tests/web/test_chart_jit.py:87:    result = get_or_render_surface(
tests/web/test_chart_jit.py:98:def test_get_or_render_surface_cache_miss_renders_via_ohlcv_and_writes_through(
tests/web/test_chart_jit.py:110:        result = get_or_render_surface(
tests/web/test_chart_jit.py:121:        "SELECT chart_svg_bytes FROM chart_renders "
tests/web/test_chart_jit.py:130:def test_get_or_render_surface_stamps_content_derived_source_data_hash(
tests/web/test_chart_jit.py:134:    ``source_data_hash`` (bar count + first/last asof_date) instead of the
tests/web/test_chart_jit.py:135:    static ``chart_jit_v1`` default, so the cached row's provenance changes on
tests/web/test_chart_jit.py:137:    from swing.web.charts import compute_chart_source_hash
tests/web/test_chart_jit.py:148:        get_or_render_surface(
tests/web/test_chart_jit.py:158:        "SELECT source_data_hash FROM chart_renders "
tests/web/test_chart_jit.py:164:    assert row[0] != "chart_jit_v1", (
tests/web/test_chart_jit.py:167:    assert row[0] == compute_chart_source_hash(bars)
tests/web/test_chart_jit.py:171:def test_get_or_render_surface_fetches_min_calendar_days_for_ma200(
tests/web/test_chart_jit.py:183:        get_or_render_surface(
tests/web/test_chart_jit.py:195:def test_get_or_render_surface_returns_none_on_empty_ohlcv(
tests/web/test_chart_jit.py:200:    result = get_or_render_surface(
tests/web/test_chart_jit.py:210:        "SELECT 1 FROM chart_renders WHERE ticker = 'UCTT'"
tests/web/test_chart_jit.py:215:def test_get_or_render_surface_returns_none_on_empty_dataframe_ohlcv(
tests/web/test_chart_jit.py:221:    result = get_or_render_surface(
tests/web/test_chart_jit.py:229:        "SELECT 1 FROM chart_renders WHERE ticker = 'UCTT'"
tests/web/test_chart_jit.py:234:def test_get_or_render_surface_returns_none_on_ohlcv_exception(
tests/web/test_chart_jit.py:240:    result = get_or_render_surface(
tests/web/test_chart_jit.py:249:def test_get_or_render_surface_returns_none_on_empty_render_bytes(
tests/web/test_chart_jit.py:271:        result = get_or_render_surface(
tests/web/test_chart_jit.py:282:        "SELECT ticker, chart_svg_bytes FROM chart_renders "
tests/web/test_chart_jit.py:290:def test_get_or_render_surface_cache_collision_renderer_called_once(
tests/web/test_chart_jit.py:306:        r1 = get_or_render_surface(
tests/web/test_chart_jit.py:313:        r2 = get_or_render_surface(
tests/web/test_chart_jit.py:325:        "SELECT COUNT(*) FROM chart_renders "
tests/web/test_chart_jit.py:357:        bytes_v100 = get_or_render_surface(
tests/web/test_chart_jit.py:376:        bytes_v101 = get_or_render_surface(
tests/web/test_chart_jit.py:387:        "SELECT pipeline_run_id, chart_svg_bytes FROM chart_renders "
tests/web/test_chart_jit.py:398:def test_get_or_render_surface_treats_empty_cached_bytes_as_miss(
tests/web/test_chart_jit.py:404:    Plant a chart_renders row directly via raw SQL with
tests/web/test_chart_jit.py:407:    that skips the dataclass). Call ``get_or_render_surface``. Assert
tests/web/test_chart_jit.py:411:    Pre-fix: ``get_or_render_surface`` returned the empty bytes
tests/web/test_chart_jit.py:419:            "INSERT INTO chart_renders "
tests/web/test_chart_jit.py:421:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_chart_jit.py:428:        "SELECT length(chart_svg_bytes) FROM chart_renders "
tests/web/test_chart_jit.py:443:        result = get_or_render_surface(
tests/web/test_chart_jit.py:457:        "SELECT chart_svg_bytes FROM chart_renders "
tests/web/test_chart_jit.py:485:        get_or_render_surface(
tests/web/test_chart_jit.py:522:    r1 = get_or_render_surface(
tests/web/test_chart_jit.py:533:        "SELECT chart_svg_bytes, pipeline_run_id FROM chart_renders "
tests/web/test_chart_jit.py:541:    r2 = get_or_render_surface(
tests/web/test_watchlist_jit_anchor_r2.py:34:from swing.data.repos.chart_renders import refresh_chart_render
tests/web/test_watchlist_jit_anchor_r2.py:37:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_watchlist_jit_anchor_r2.py:61:        PriceCache, "get_many",
tests/web/test_watchlist_jit_anchor_r2.py:66:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_watchlist_jit_anchor_r2.py:67:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_watchlist_jit_anchor_r2.py:132:                source_data_hash="t-anchor-r2",
tests/research/test_pattern_cohort_evaluator_reader.py:9:  - Test 5 (§E.7 + §F.5): defensive read_or_fetch_archive signature lock +
tests/research/test_pattern_cohort_evaluator_reader.py:208:        "read_or_fetch_archive",
tests/research/test_pattern_cohort_evaluator_reader.py:251:# L2 LOCK Test 5 (defensive): read_or_fetch_archive signature lock per §E.7
tests/research/test_pattern_cohort_evaluator_reader.py:254:def test_read_or_fetch_archive_has_no_prefer_source_kwarg():
tests/research/test_pattern_cohort_evaluator_reader.py:262:    from swing.data.ohlcv_archive import read_or_fetch_archive
tests/research/test_pattern_cohort_evaluator_reader.py:263:    params = inspect.signature(read_or_fetch_archive).parameters
tests/web/test_watchlist_expand_single_pipeline_anchor.py:28:from swing.data.repos.chart_renders import refresh_chart_render
tests/web/test_watchlist_expand_single_pipeline_anchor.py:31:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_watchlist_expand_single_pipeline_anchor.py:55:        PriceCache, "get_many",
tests/web/test_watchlist_expand_single_pipeline_anchor.py:60:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_watchlist_expand_single_pipeline_anchor.py:61:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_watchlist_expand_single_pipeline_anchor.py:127:                source_data_hash="t-anchor",
tests/web/test_watchlist_expand_single_pipeline_anchor.py:169:    # Plant chart_renders rows for runs 101 + 102 (run 102 planted via
tests/web/test_chart_source_hash.py:1:"""Phase 16 / Arc 3 (3c) — content-derived ``chart_renders.source_data_hash``.
tests/web/test_chart_source_hash.py:3:The prior write sites stamped a STATIC literal (``"step_charts_v1"`` /
tests/web/test_chart_source_hash.py:4:``"chart_jit_v1"``) which does not encode the underlying data, so the audit
tests/web/test_chart_source_hash.py:6:``compute_chart_source_hash`` keys the value on (bar count + first/last
tests/web/test_chart_source_hash.py:13:from swing.web.charts import compute_chart_source_hash
tests/web/test_chart_source_hash.py:34:    assert compute_chart_source_hash(a) == compute_chart_source_hash(b)
tests/web/test_chart_source_hash.py:43:    assert compute_chart_source_hash(sparse) != compute_chart_source_hash(rich)
tests/web/test_chart_source_hash.py:50:    assert compute_chart_source_hash(earlier) != compute_chart_source_hash(later)
tests/web/test_chart_source_hash.py:55:    assert compute_chart_source_hash(empty) == compute_chart_source_hash(empty)
tests/web/test_chart_source_hash.py:56:    assert compute_chart_source_hash(empty) != compute_chart_source_hash(
tests/web/test_chart_source_hash.py:62:    h = compute_chart_source_hash(_frame("2026-06-08", 207))
tests/web/test_chart_source_hash.py:63:    assert h not in ("step_charts_v1", "chart_jit_v1")
tests/web/test_base_layout_nav.py:21:from swing.web.price_cache import PriceCache
tests/web/test_base_layout_nav.py:39:    """Spin up a TestClient with PriceCache neutralized so GET / is fast +
tests/web/test_base_layout_nav.py:42:        PriceCache, "get_many",
tests/web/test_base_layout_nav.py:45:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_watchlist_expand_collapse_preserves_thumbnail.py:22:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_watchlist_expand_collapse_preserves_thumbnail.py:33:        PriceCache, "get_many",
tests/web/test_watchlist_expand_collapse_preserves_thumbnail.py:38:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_watchlist_expand_collapse_preserves_thumbnail.py:39:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_watchlist_expand_collapse_preserves_thumbnail.py:52:            "INSERT INTO chart_renders "
tests/web/test_watchlist_expand_collapse_preserves_thumbnail.py:54:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_chart_scope_t5_reason_split.py:5:  - the per-ticker yfinance fetch raised in `_step_charts`
tests/web/test_base_layout_compat.py:32:    from swing.web.price_cache import PriceCache
tests/web/test_base_layout_compat.py:34:        PriceCache, "get_many",
tests/web/test_base_layout_compat.py:37:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_base_layout_compat.py:38:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_base_layout_compat.py:108:    from swing.web.price_cache import PriceCache
tests/web/test_base_layout_compat.py:115:        PriceCache, "get_many",
tests/web/test_base_layout_compat.py:118:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_base_layout_compat.py:119:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_base_layout_compat.py:134:    from swing.web.price_cache import PriceCache
tests/web/test_base_layout_compat.py:141:        PriceCache, "get_many",
tests/web/test_base_layout_compat.py:144:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_base_layout_compat.py:145:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/pipeline/test_step_charts_pregen_scope_reduction.py:3:Per plan §B.3 Sub-task 3E: `_step_charts` pre-gen scope reduced from
tests/pipeline/test_step_charts_pregen_scope_reduction.py:10:  `_step_charts` writes ≤5 watchlist_row rows + 0 ticker_detail +
tests/pipeline/test_step_charts_pregen_scope_reduction.py:128:def _run_step_charts(*, cfg, run_id, eval_run_id):
tests/pipeline/test_step_charts_pregen_scope_reduction.py:130:    from swing.pipeline.runner import _step_charts
tests/pipeline/test_step_charts_pregen_scope_reduction.py:134:    _step_charts(
tests/pipeline/test_step_charts_pregen_scope_reduction.py:140:def test_step_charts_pregen_scope_reduced_to_top5_no_ticker_detail(
tests/pipeline/test_step_charts_pregen_scope_reduction.py:161:    _run_step_charts(
tests/pipeline/test_step_charts_pregen_scope_reduction.py:168:            "SELECT surface, COUNT(*) FROM chart_renders "
tests/web/test_charts.py:816:    """Two get_or_render_surface calls with identical bars -> byte-identical
tests/web/test_charts.py:822:    from swing.web.chart_jit import get_or_render_surface
tests/web/test_charts.py:838:    r1 = get_or_render_surface(
tests/web/test_charts.py:844:    r2 = get_or_render_surface(
tests/web/test_charts.py:852:        "SELECT COUNT(*) FROM chart_renders WHERE surface='ticker_detail' "
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:1:"""Phase 13 T1.SB0 — wiring discriminating test for ``_step_charts``.
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:3:Plan §G.0 T-T1.SB0.3 acceptance: ``_step_charts`` no longer invokes the legacy
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:7:Pre-implementation: ``_step_charts`` calls ``fetcher.get(...)`` → AttributeError.
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:8:Post-implementation: ``_step_charts`` calls ``ohlcv_cache.get_or_fetch(...)`` →
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:28:from swing.pipeline.runner import _step_charts
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:87:    """Stub exposing ONLY ``get_or_fetch`` — pre-implementation ``_step_charts``
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:139:def test_step_charts_calls_ohlcv_cache_get_or_fetch_not_legacy_fetcher_get(
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:142:    """Plan §G.0 T-T1.SB0.3 acceptance: ``_step_charts`` invokes
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:145:    Spy stub exposes ONLY ``get_or_fetch``; pre-implementation ``_step_charts``
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:149:    ``get_or_fetch`` per surface during the chart_renders write-through
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:167:    _step_charts(
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:174:    # from _step_charts; JIT-render at expand time instead. So each A+
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:204:def test_step_charts_handles_get_or_fetch_value_error_as_fetcher_failed(
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:208:    ``get_or_fetch`` raises ``ValueError``, ``_step_charts`` catches it +
tests/pipeline/test_step_charts_ohlcv_cache_wiring.py:227:    _step_charts(
tests/pipeline/test_step_charts_fetch_window.py:12:from swing.pipeline.runner import _step_charts
tests/pipeline/test_step_charts_fetch_window.py:17:from tests.pipeline.test_step_charts_ohlcv_cache_wiring import (
tests/pipeline/test_step_charts_fetch_window.py:30:def test_step_charts_market_weather_window_and_bars(tmp_path, monkeypatch):
tests/pipeline/test_step_charts_fetch_window.py:58:    _step_charts(
tests/research/test_counting_price_fetcher.py:6:vs misses, mirroring both branches of `read_or_fetch_archive`'s
tests/web/test_dashboard_hypothesis_template.py:56:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_dashboard_hypothesis_template.py:58:        PriceCache, "get_many",
tests/web/test_dashboard_hypothesis_template.py:66:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_hypothesis_template.py:67:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/pipeline/test_step_charts_classification.py:1:"""Pipeline tests for `_step_charts` classification persistence (Phase 3).
tests/pipeline/test_step_charts_classification.py:129:def test_step_charts_happy_path_persists_classification_row(
tests/pipeline/test_step_charts_classification.py:197:            "_step_charts must persist a pattern_classifications row for "
tests/pipeline/test_step_charts_classification.py:248:def test_step_charts_too_few_bars_persists_pattern_none(
tests/pipeline/test_step_charts_classification.py:278:def test_step_charts_classifier_exception_persists_null_pattern_with_error(
tests/web/test_view_models/test_watchlist_sort_surfaces.py:102:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_sort_surfaces.py:104:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_view_models/test_watchlist_sort_surfaces.py:106:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:107:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:116:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_sort_surfaces.py:122:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:139:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_sort_surfaces.py:145:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:226:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_sort_surfaces.py:232:    monkeypatch.setattr(PriceCache, "refresh_all", fake_refresh_all)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:233:    monkeypatch.setattr(PriceCache, "reset_circuit_breaker", lambda self: None)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:234:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_view_models/test_watchlist_sort_surfaces.py:236:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:237:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:257:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_sort_surfaces.py:327:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist_sort_surfaces.py:373:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_sort_surfaces.py:431:    cache = PriceCache(cfg)
tests/web/test_app_smoke.py:38:    from swing.web.price_cache import PriceCache
tests/web/test_app_smoke.py:41:    assert isinstance(app.state.price_cache, PriceCache)
tests/web/test_dashboard_detail_button.py:16:from swing.web.price_cache import PriceCache
tests/web/test_dashboard_detail_button.py:41:        PriceCache, "get_many",
tests/web/test_dashboard_detail_button.py:44:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_detail_button.py:45:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/pipeline/test_step_charts_chart_renders_write_through.py:1:"""Phase 13 T2.SB6c task T-A.6c.2 §1.5.1 amendment — pipeline-side chart_renders
tests/pipeline/test_step_charts_chart_renders_write_through.py:2:write-through tests for `_step_charts`.
tests/pipeline/test_step_charts_chart_renders_write_through.py:7:    / market_weather) — exactly 1 chart_renders row exists per active ticker
tests/pipeline/test_step_charts_chart_renders_write_through.py:11:  - 1 DELETE-then-INSERT idempotency test — second `_step_charts` invocation
tests/pipeline/test_step_charts_chart_renders_write_through.py:30:from swing.data.repos.chart_renders import (
tests/pipeline/test_step_charts_chart_renders_write_through.py:32:    list_chart_renders,
tests/pipeline/test_step_charts_chart_renders_write_through.py:39:# Fixtures: a minimal pipeline-run substrate for `_step_charts` to write into.
tests/pipeline/test_step_charts_chart_renders_write_through.py:170:def _run_step_charts(*, cfg, run_id, eval_run_id, ohlcv_cache):
tests/pipeline/test_step_charts_chart_renders_write_through.py:171:    """Acquire the lease for the seeded pipeline_run + invoke `_step_charts`.
tests/pipeline/test_step_charts_chart_renders_write_through.py:173:    Returns nothing — the side-effect is `chart_renders` rows written.
tests/pipeline/test_step_charts_chart_renders_write_through.py:176:    from swing.pipeline.runner import _step_charts
tests/pipeline/test_step_charts_chart_renders_write_through.py:180:    _step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:191:def test_step_charts_writes_through_chart_renders_for_watchlist_row_surface(
tests/pipeline/test_step_charts_chart_renders_write_through.py:194:    """§1.5.1 — `_step_charts` writes a chart_renders row per watchlist
tests/pipeline/test_step_charts_chart_renders_write_through.py:203:    _run_step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:219:def test_step_charts_does_not_pregen_ticker_detail_surface_post_t_t4_sb_3(
tests/pipeline/test_step_charts_chart_renders_write_through.py:222:    """Phase 13 T-T4.SB.3 (OQ-5.3 LOCK): `_step_charts` no longer pre-gens
tests/pipeline/test_step_charts_chart_renders_write_through.py:234:    _run_step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:247:        "_step_charts must NOT pre-gen ticker_detail (OQ-5.3 LOCK); "
tests/pipeline/test_step_charts_chart_renders_write_through.py:252:def test_step_charts_writes_through_chart_renders_for_position_detail_surface(
tests/pipeline/test_step_charts_chart_renders_write_through.py:264:    _run_step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:279:def test_step_charts_writes_through_chart_renders_for_market_weather_surface(
tests/pipeline/test_step_charts_chart_renders_write_through.py:287:    _run_step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:311:def test_step_charts_handles_transient_empty_chart_bytes_gracefully(
tests/pipeline/test_step_charts_chart_renders_write_through.py:344:    _run_step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:380:def test_step_charts_chart_renders_write_through_is_idempotent(pipeline_db):
tests/pipeline/test_step_charts_chart_renders_write_through.py:381:    """§1.5.1 — invoking `_step_charts` twice with identical input replaces
tests/pipeline/test_step_charts_chart_renders_write_through.py:396:    _run_step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:402:        first = list_chart_renders(
tests/pipeline/test_step_charts_chart_renders_write_through.py:422:    _run_step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:428:        second = list_chart_renders(
tests/pipeline/test_step_charts_chart_renders_write_through.py:445:def test_step_charts_stamps_content_derived_source_data_hash(pipeline_db):
tests/pipeline/test_step_charts_chart_renders_write_through.py:446:    """Phase 16 Arc 3 (3c): `_step_charts` MUST stamp a content-derived
tests/pipeline/test_step_charts_chart_renders_write_through.py:447:    ``source_data_hash`` (bar count + first/last asof_date) on the persisted
tests/pipeline/test_step_charts_chart_renders_write_through.py:448:    chart_renders row, NOT the legacy static literal ``"step_charts_v1"``.
tests/pipeline/test_step_charts_chart_renders_write_through.py:451:    persisted hash must equal ``compute_chart_source_hash`` of that frame and
tests/pipeline/test_step_charts_chart_renders_write_through.py:452:    encode its bar count. Pre-fix the row carried ``"step_charts_v1"``.
tests/pipeline/test_step_charts_chart_renders_write_through.py:454:    from swing.web.charts import compute_chart_source_hash
tests/pipeline/test_step_charts_chart_renders_write_through.py:465:    _run_step_charts(
tests/pipeline/test_step_charts_chart_renders_write_through.py:472:        rows = list_chart_renders(
tests/pipeline/test_step_charts_chart_renders_write_through.py:479:    persisted = rows[0].source_data_hash
tests/pipeline/test_step_charts_chart_renders_write_through.py:480:    assert persisted != "step_charts_v1", (
tests/pipeline/test_step_charts_chart_renders_write_through.py:481:        "source_data_hash is still the static literal; 3c not applied"
tests/pipeline/test_step_charts_chart_renders_write_through.py:483:    assert persisted == compute_chart_source_hash(bars)
tests/pipeline/test_step_charts_chart_renders_write_through.py:487:def test_step_charts_populates_three_pregen_surfaces_in_one_run(pipeline_db):
tests/pipeline/test_step_charts_chart_renders_write_through.py:488:    """§1.5.1 amended by Phase 13 T-T4.SB.3 OQ-5.3 LOCK: `_step_charts`
tests/pipeline/test_step_charts_chart_renders_write_through.py:492:    ``test_step_charts_does_not_pregen_ticker_detail_surface_post_t_t4_sb_3``.
tests/pipeline/test_step_charts_chart_renders_write_through.py:503:    _run_step_charts(
tests/web/test_app_marketdata_ladder_wiring.py:90:def test_bars_hook_passes_explicit_daily_kwargs(seeded_db, monkeypatch, tmp_path):
tests/research/test_aplus_v2_ohlcv_reader.py:7:  - Test 4 (defensive): read_or_fetch_archive signature lock
tests/research/test_aplus_v2_ohlcv_reader.py:8:  - Test 5 (defensive): V2 module-set grep for read_or_fetch_archive
tests/research/test_aplus_v2_ohlcv_reader.py:422:# L2 LOCK Test 4 (defensive): read_or_fetch_archive signature lock (plan §K.4)
tests/research/test_aplus_v2_ohlcv_reader.py:425:def test_read_or_fetch_archive_has_no_prefer_source_kwarg():
tests/research/test_aplus_v2_ohlcv_reader.py:428:    read_or_fetch_archive, this test fires + flags that V2's read-only
tests/research/test_aplus_v2_ohlcv_reader.py:433:    from swing.data.ohlcv_archive import read_or_fetch_archive
tests/research/test_aplus_v2_ohlcv_reader.py:434:    params = inspect.signature(read_or_fetch_archive).parameters
tests/research/test_aplus_v2_ohlcv_reader.py:436:        "Production read_or_fetch_archive now has a prefer_source kwarg; "
tests/research/test_aplus_v2_ohlcv_reader.py:444:# L2 LOCK Test 5 (defensive): V2 module-set grep for read_or_fetch_archive (plan §K.5)
tests/research/test_aplus_v2_ohlcv_reader.py:447:def test_v2_module_set_does_NOT_import_read_or_fetch_archive():  # noqa: N802
tests/research/test_aplus_v2_ohlcv_reader.py:449:    V2 modules MUST NOT import read_or_fetch_archive per OQ-16 LOCK.
tests/research/test_aplus_v2_ohlcv_reader.py:458:        assert "read_or_fetch_archive" not in text, (
tests/research/test_aplus_v2_ohlcv_reader.py:460:            f"read_or_fetch_archive (per OQ-16 LOCK + spec §F.1)"
tests/web/test_dashboard_bundle3_advisories.py:22:from swing.web.price_cache import PriceCache
tests/web/test_dashboard_bundle3_advisories.py:88:    """Yield (app, db_path). PriceCache stubbed with two open-trade snapshots."""
tests/web/test_dashboard_bundle3_advisories.py:101:    monkeypatch.setattr(PriceCache, "get_many", _stub_get_many)
tests/web/test_dashboard_bundle3_advisories.py:102:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_bundle3_advisories.py:103:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_dashboard_bundle3_advisories.py:168:    """Codex R1 Major #2 — §4.A.bis must fire even when PriceCache yields
tests/web/test_dashboard_bundle3_advisories.py:173:        PriceCache, "get_many",
tests/web/test_dashboard_bundle3_advisories.py:176:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: True)
tests/web/test_dashboard_bundle3_advisories.py:177:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_daily_management_timeline_legacy_union.py:23:from swing.web.price_cache import PriceCache
tests/web/test_daily_management_timeline_legacy_union.py:50:        PriceCache, "get_many",
tests/web/test_daily_management_timeline_legacy_union.py:53:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_daily_management_timeline_legacy_union.py:54:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist_eval_scoping.py:32:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_eval_scoping.py:37:    monkeypatch.setattr(PriceCache, "get_many", fake_get_many)
tests/web/test_view_models/test_watchlist_eval_scoping.py:38:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist_eval_scoping.py:39:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist_eval_scoping.py:59:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_eval_scoping.py:118:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist_eval_scoping.py:145:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_eval_scoping.py:174:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist_eval_scoping.py:186:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist_eval_scoping.py:225:    cache = PriceCache(cfg)
tests/web/test_daily_management_timeline.py:28:from swing.web.price_cache import PriceCache
tests/web/test_daily_management_timeline.py:131:        PriceCache, "get_many",
tests/web/test_daily_management_timeline.py:134:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_daily_management_timeline.py:135:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_daily_management_tile.py:28:from swing.web.price_cache import PriceCache
tests/web/test_daily_management_tile.py:127:        PriceCache, "get_many",
tests/web/test_daily_management_tile.py:130:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_daily_management_tile.py:131:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_daily_management_tile.py:471:    # Inject live current_price via PriceCache.get_many monkeypatch.
tests/web/test_daily_management_tile.py:480:        PriceCache, "get_many",
tests/web/test_daily_management_tile.py:485:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_daily_management_tile.py:486:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist.py:13:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_watchlist.py:32:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist.py:69:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_watchlist.py:88:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist.py:105:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist.py:141:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist.py:158:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist.py:177:        PriceCache, "get_many",
tests/web/test_view_models/test_watchlist.py:180:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist.py:181:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist.py:212:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist.py:258:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist.py:284:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist.py:305:        PriceCache, "get_many",
tests/web/test_view_models/test_watchlist.py:308:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist.py:309:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist.py:349:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist.py:428:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist.py:456:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist.py:497:        PriceCache, "get_many",
tests/web/test_view_models/test_watchlist.py:500:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist.py:501:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist.py:530:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist.py:558:        PriceCache, "get_many",
tests/web/test_view_models/test_watchlist.py:561:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist.py:562:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist.py:583:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_watchlist.py:600:        PriceCache, "get_many",
tests/web/test_view_models/test_watchlist.py:603:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist.py:604:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist.py:660:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_watchlist.py:703:        PriceCache, "get_many",
tests/web/test_view_models/test_watchlist.py:711:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist.py:712:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist.py:714:    cache = PriceCache(cfg)
tests/web/test_view_models/test_watchlist.py:755:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_watchlist.py:798:        PriceCache, "get_many",
tests/web/test_view_models/test_watchlist.py:806:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_watchlist.py:807:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_watchlist.py:809:    cache = PriceCache(cfg)
tests/web/test_hyprec_thumbnail.py:4:fake app.state.ohlcv_cache). Render-direct: NO chart_renders write (L5).
tests/web/test_hyprec_thumbnail.py:50:def _count_chart_renders(cfg, *, surface: str, ticker: str) -> int:
tests/web/test_hyprec_thumbnail.py:54:            "SELECT COUNT(*) FROM chart_renders WHERE surface=? AND ticker=?",
tests/web/test_hyprec_thumbnail.py:87:def test_hyprec_thumbnail_does_not_write_chart_renders(seeded_db):
tests/web/test_hyprec_thumbnail.py:92:    assert _count_chart_renders(cfg, surface="watchlist_row", ticker="NVDA") == 0
tests/web/test_hyprec_thumbnail.py:93:    assert _count_chart_renders(cfg, surface="watchlist_row", ticker="NVDA") == 0
tests/web/test_detection_chart_capture.py:52:        "SELECT surface, pattern_class, pipeline_run_id FROM chart_renders "
tests/web/test_detection_chart_capture.py:97:        "SELECT COUNT(*) FROM chart_renders WHERE ticker='AAA' "
tests/web/test_view_models/test_trade_entry_form_classification.py:218:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_trade_entry_form_classification.py:249:        PriceCache, "get_many",
tests/web/test_view_models/test_trade_entry_form_classification.py:252:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_trade_entry_form_classification.py:253:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_trade_entry_form_classification.py:255:    cache = PriceCache(cfg)
tests/web/test_dashboard_update_today_badge.py:31:from swing.web.price_cache import PriceCache
tests/web/test_dashboard_update_today_badge.py:97:    """Stand up a dashboard app + DB with PriceCache stubbed (no network)."""
tests/web/test_dashboard_update_today_badge.py:99:        PriceCache, "get_many",
tests/web/test_dashboard_update_today_badge.py:102:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_update_today_badge.py:103:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_dashboard_update_today_badge.py:136:    cache = PriceCache(cfg.web)
tests/web/test_dashboard_update_today_badge.py:164:    cache = PriceCache(cfg.web)
tests/web/test_dashboard_update_today_badge.py:182:    cache = PriceCache(cfg.web)
tests/pipeline/test_runner_export_advisories.py:54:    keeps PriceCache._last_close fresh for rotated-out tickers."""
tests/web/test_view_models/test_trades.py:15:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_trades.py:34:    cache = PriceCache(cfg)
tests/web/test_view_models/test_trades.py:63:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_trades.py:81:    cache = PriceCache(cfg)
tests/web/test_view_models/test_trades.py:205:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_trades.py:207:    cache = PriceCache(cfg)
tests/web/test_view_models/test_trades.py:312:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_trades.py:321:    cache = PriceCache(cfg)
tests/web/test_view_models/test_trades.py:362:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_trades.py:369:    cache = PriceCache(cfg)
tests/web/test_dashboard_integration.py:53:    from swing.web.price_cache import PriceCache
tests/web/test_dashboard_integration.py:54:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_dashboard_integration.py:56:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_integration.py:69:    from swing.web.price_cache import PriceCache
tests/web/test_dashboard_integration.py:70:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_dashboard_integration.py:72:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_integration.py:84:    from swing.web.price_cache import PriceCache
tests/web/test_dashboard_integration.py:85:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_dashboard_integration.py:87:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: True)
tests/web/test_dashboard_integration.py:90:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: fake_until)
tests/web/test_dashboard_integration.py:124:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_dashboard_integration.py:140:        PriceCache, "get_many",
tests/web/test_dashboard_integration.py:148:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_integration.py:149:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_dashboard_integration.py:168:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_dashboard_integration.py:182:        PriceCache, "get_many",
tests/web/test_dashboard_integration.py:190:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_integration.py:191:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_dashboard_integration.py:209:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_dashboard_integration.py:225:        PriceCache, "get_many",
tests/web/test_dashboard_integration.py:233:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_dashboard_integration.py:234:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/pipeline/test_runner_chart_targets.py:7:- After `_step_charts` runs, `pipeline_chart_targets` has one row per ticker
tests/pipeline/test_runner_chart_targets.py:14:Task 5 (chart-scope policy v2) extends this file with direct `_step_charts`
tests/pipeline/test_runner_chart_targets.py:51:from swing.pipeline.runner import _step_charts, run_pipeline_internal
tests/pipeline/test_runner_chart_targets.py:148:def test_step_charts_writes_chart_targets_with_ok_status(
tests/pipeline/test_runner_chart_targets.py:195:def test_step_charts_records_fetcher_failed(tmp_path: Path, monkeypatch):
tests/pipeline/test_runner_chart_targets.py:216:    # Phase 13 T1.SB0: `_step_charts` consumes OHLCV via
tests/pipeline/test_runner_chart_targets.py:254:def test_step_charts_records_too_few_bars(tmp_path: Path, monkeypatch):
tests/pipeline/test_runner_chart_targets.py:292:def test_step_charts_dedupes_aplus_then_tag_aware(
tests/pipeline/test_runner_chart_targets.py:347:# Task 5 — chart-scope policy v2 — direct `_step_charts` invocation context.
tests/pipeline/test_runner_chart_targets.py:356:# directly and call `_step_charts` in isolation.
tests/pipeline/test_runner_chart_targets.py:477:def _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:485:    """Set up the minimum DB state for a direct `_step_charts` invocation:
tests/pipeline/test_runner_chart_targets.py:490:    Returns a context the caller can pass to `_step_charts(...)`.
tests/pipeline/test_runner_chart_targets.py:538:    # Build a stub for any ticker so _step_charts doesn't fetcher_fail before
tests/pipeline/test_runner_chart_targets.py:539:    # reaching the classifier. Phase 13 T1.SB0: _step_charts now consumes
tests/pipeline/test_runner_chart_targets.py:596:def test_step_charts_emits_three_tier_targets_with_correct_sources(
tests/pipeline/test_runner_chart_targets.py:614:    ctx = _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:631:    _step_charts(
tests/pipeline/test_runner_chart_targets.py:645:def test_step_charts_dedup_precedence_aplus_wins_over_open_position(
tests/pipeline/test_runner_chart_targets.py:654:    ctx = _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:659:    _step_charts(
tests/pipeline/test_runner_chart_targets.py:669:def test_step_charts_dedup_precedence_open_position_wins_over_tag_aware(
tests/pipeline/test_runner_chart_targets.py:676:    ctx = _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:685:    _step_charts(
tests/pipeline/test_runner_chart_targets.py:695:def test_step_charts_dedup_ticker_in_all_three_tiers_records_aplus(
tests/pipeline/test_runner_chart_targets.py:702:    ctx = _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:711:    _step_charts(
tests/pipeline/test_runner_chart_targets.py:721:def test_step_charts_canonicalizes_ticker_case_before_dedup(
tests/pipeline/test_runner_chart_targets.py:732:    ctx = _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:737:    _step_charts(
tests/pipeline/test_runner_chart_targets.py:748:def test_step_charts_open_position_pivot_from_trades_entry_price(
tests/pipeline/test_runner_chart_targets.py:756:    red-herring. Run _step_charts; capture pivot/stop passed to render_chart.
tests/pipeline/test_runner_chart_targets.py:761:    ctx = _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:771:    _step_charts(
tests/pipeline/test_runner_chart_targets.py:782:def test_step_charts_tag_aware_filter_intersection_limit(
tests/pipeline/test_runner_chart_targets.py:796:    ctx = _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:808:    _step_charts(
tests/pipeline/test_runner_chart_targets.py:963:def test_step_charts_tag_aware_uses_shared_sort_key(
tests/pipeline/test_runner_chart_targets.py:967:    `_tag_aware_sort_key` directly. Assert `_step_charts` writes
tests/pipeline/test_runner_chart_targets.py:1020:    ctx = _make_step_charts_ctx(
tests/pipeline/test_runner_chart_targets.py:1025:    _step_charts(
tests/pipeline/test_runner_chart_step_walltime.py:3:Instruments `_step_charts` with a wall-time timer that brackets all chart-step
tests/pipeline/test_runner_chart_step_walltime.py:32:from swing.pipeline.runner import _step_charts
tests/pipeline/test_runner_chart_step_walltime.py:106:    """Returns ample bars for any ticker so _step_charts doesn't fetcher_fail.
tests/pipeline/test_runner_chart_step_walltime.py:108:    Phase 13 T1.SB0 wiring: _step_charts now consumes OHLCV via
tests/pipeline/test_runner_chart_step_walltime.py:121:    """Build the minimal DB state for a direct `_step_charts` invocation.
tests/pipeline/test_runner_chart_step_walltime.py:187:def test_step_charts_logs_warning_when_walltime_above_soft_budget(
tests/pipeline/test_runner_chart_step_walltime.py:201:        _step_charts(
tests/pipeline/test_runner_chart_step_walltime.py:224:def test_step_charts_logs_error_when_walltime_above_hard_budget(
tests/pipeline/test_runner_chart_step_walltime.py:238:        _step_charts(
tests/pipeline/test_runner_chart_step_walltime.py:271:def test_step_charts_no_log_when_walltime_under_soft_budget(
tests/pipeline/test_runner_chart_step_walltime.py:285:        _step_charts(
tests/pipeline/test_runner_chart_step_walltime.py:304:def test_step_charts_pipeline_runs_charts_status_unchanged_on_overrun(
tests/pipeline/test_runner_chart_step_walltime.py:309:    After _step_charts completes, query pipeline_chart_targets.chart_status
tests/pipeline/test_runner_chart_step_walltime.py:320:        _step_charts(
tests/pipeline/test_runner_chart_step_walltime.py:335:        "pipeline_chart_targets must have at least one row after _step_charts "
tests/web/test_view_models/test_tag_aware_sort_key.py:4:Byte-identity invariant: `_sort_watchlist` and `_step_charts` (Task 5) both
tests/web/test_market_weather_live_state.py:5:pipeline _step_charts), not a stubbed current_stage (Codex R1 Major #5)."""
tests/web/test_market_weather_live_state.py:128:def test_pipeline_step_charts_market_weather_uses_structural_stage(
tests/web/test_market_weather_live_state.py:131:    from tests.pipeline.test_step_charts_ohlcv_cache_wiring import (
tests/web/test_market_weather_live_state.py:137:    from swing.pipeline.runner import _step_charts
tests/web/test_market_weather_live_state.py:172:    _step_charts(
tests/cli/test_diagnose_prune_chart_cache.py:4:Per plan §B.3 Sub-task 3G: deletes chart_renders rows older than
tests/cli/test_diagnose_prune_chart_cache.py:30:            "INSERT INTO chart_renders "
tests/cli/test_diagnose_prune_chart_cache.py:32:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/cli/test_diagnose_prune_chart_cache.py:80:    assert "Deleted 1 chart_renders rows" in result.output
tests/cli/test_diagnose_prune_chart_cache.py:85:            "SELECT ticker FROM chart_renders ORDER BY ticker"
tests/cli/test_diagnose_prune_chart_cache.py:120:            "SELECT COUNT(*) FROM chart_renders"
tests/cli/test_diagnose_prune_chart_cache.py:150:    assert "Deleted 0 chart_renders rows" in result.output
tests/web/test_open_positions_thumbnail.py:35:    from swing.web.price_cache import PriceCache
tests/web/test_open_positions_thumbnail.py:37:        PriceCache, "get_many",
tests/web/test_open_positions_thumbnail.py:40:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_open_positions_thumbnail.py:41:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
swing/web/view_models/dashboard.py:38:from swing.web.price_cache import PriceCache, PriceSnapshot
swing/web/view_models/dashboard.py:382:    # from the chart_renders cache (surface='market_weather') when a
swing/web/view_models/dashboard.py:388:    # from the chart_renders cache (surface='watchlist_row' for
swing/web/view_models/dashboard.py:482:    *, cfg: Config, cache: PriceCache, executor,
swing/web/view_models/dashboard.py:616:    # route populates this via the PriceCache+executor batch `get_many`
swing/web/view_models/dashboard.py:621:    # chart_renders cache for the operator-facing hyp-rec expansion
swing/web/view_models/dashboard.py:640:    cache: PriceCache | None = None,
swing/web/view_models/dashboard.py:724:    # path; brief §0.3 #4 "PriceCache+executor pattern"). A synchronous
swing/web/view_models/dashboard.py:741:    # Phase 13 T2.SB6c T-A.6c.2 Gap A.1 — consult chart_renders cache for
swing/web/view_models/dashboard.py:745:    from swing.data.repos.chart_renders import get_cached_chart_svg
swing/web/view_models/dashboard.py:755:    # rotated into A+ AFTER pipeline completed; or _step_charts scope
swing/web/view_models/dashboard.py:772:        from swing.web.chart_jit import get_or_render_surface
swing/web/view_models/dashboard.py:773:        ticker_detail_chart_svg_bytes = get_or_render_surface(
swing/web/view_models/dashboard.py:836:    *, cfg: Config, cache: PriceCache, executor, ohlcv_cache=None,
swing/web/view_models/dashboard.py:871:            # the T2.SB6a substrate cache (chart_renders table). All cache
swing/web/view_models/dashboard.py:877:                from swing.data.repos.chart_renders import (
swing/web/view_models/dashboard.py:891:            # from chart_renders cache (T2.SB6a substrate verbatim).
swing/web/view_models/dashboard.py:893:                from swing.data.repos.chart_renders import (
swing/web/view_models/dashboard.py:1200:        # must fire even when PriceCache is degraded. Run the price-
swing/web/view_models/dashboard.py:1403:                # (post-partial-exit) and the live PriceCache snapshot.
swing/web/view_models/dashboard.py:1438:                    # missing (PriceCache degraded path) or the trade has
swing/web/view_models/dashboard.py:1655:    Shared between _sort_watchlist (web view-model) and _step_charts
tests/pipeline/test_runner.py:134:    previously got no candidate row — so PriceCache._last_close() fell back to
tests/pipeline/test_runner.py:204:            "that aren't in the finviz CSV, so PriceCache._last_close sees "
tests/web/test_open_positions_expand_route.py:55:    """Insert a `chart_renders` row, surface='position_detail',
tests/web/test_open_positions_expand_route.py:59:    from swing.data.repos.chart_renders import insert_chart_render
tests/web/test_open_positions_expand_route.py:63:        chart_svg_bytes=svg, source_data_hash="hash-pd",
tests/web/test_open_positions_expand_route.py:70:    from swing.web.price_cache import PriceCache
tests/web/test_open_positions_expand_route.py:71:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_open_positions_expand_route.py:73:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_open_positions_expand_chart.py:52:    """Insert a `chart_renders` row with surface='position_detail' for the
tests/web/test_open_positions_expand_chart.py:57:    from swing.data.repos.chart_renders import insert_chart_render
tests/web/test_open_positions_expand_chart.py:61:        chart_svg_bytes=svg, source_data_hash="hash-pd",
tests/web/test_ohlcv_cache_cold_start_today_aligned.py:83:    real_helper = archive_mod.read_or_fetch_archive
tests/web/test_ohlcv_cache_cold_start_today_aligned.py:92:    monkeypatch.setattr(ohlcv_mod, "read_or_fetch_archive", counting_helper)
tests/web/test_price_cache_ladder_integration.py:1:"""PriceCache + OhlcvCache integration with Schwab market-data ladder (T-C.4).
tests/web/test_price_cache_ladder_integration.py:98:# PriceCache + ladder integration
tests/web/test_price_cache_ladder_integration.py:105:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache_ladder_integration.py:108:    cache = PriceCache(cfg)
tests/web/test_price_cache_ladder_integration.py:130:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache_ladder_integration.py:133:    cache = PriceCache(cfg)
tests/web/test_price_cache_ladder_integration.py:152:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache_ladder_integration.py:155:    cache = PriceCache(cfg)
tests/web/test_price_cache_ladder_integration.py:182:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache_ladder_integration.py:185:    cache = PriceCache(cfg)
tests/web/test_price_cache_ladder_integration.py:279:    fetcher NOT invoked. Mirrors PriceCache discipline."""
tests/web/test_price_cache_ladder_integration.py:341:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_price_cache_ladder_integration.py:350:    cache = PriceCache(sandbox_cfg)
tests/web/test_ohlcv_cache.py:115:    """Mirrors PriceCache sliding-window: >50% failures in window → breaker
tests/web/test_ohlcv_cache.py:367:    real_helper = archive_mod.read_or_fetch_archive
tests/web/test_ohlcv_cache.py:376:    monkeypatch.setattr(ohlcv_mod, "read_or_fetch_archive", counting_helper)
tests/web/test_ohlcv_cache.py:410:        raise AssertionError("read_or_fetch_archive must NOT be called on a warm hit")
tests/web/test_ohlcv_cache.py:412:    monkeypatch.setattr(ohlcv_mod, "read_or_fetch_archive", boom)
tests/pipeline/test_pipeline_schwab_client_env_vars.py:400:    `_construct_pipeline_schwab_client` into `_install_pipeline_marketdata_caches`
tests/web/test_view_models/test_open_positions_row.py:128:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_open_positions_row.py:137:    cache = PriceCache(cfg)
tests/web/test_view_models/test_open_positions_row.py:162:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_open_positions_row.py:175:    cache = PriceCache(cfg)
tests/web/test_view_models/test_open_positions_row.py:195:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_open_positions_row.py:212:    cache = PriceCache(cfg)
tests/web/test_view_models/test_open_positions_row.py:231:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_open_positions_row.py:238:    cache = PriceCache(cfg)
tests/web/test_view_models/test_open_positions_row.py:256:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_open_positions_row.py:263:    cache = PriceCache(cfg)
tests/web/test_view_models/test_open_positions_row.py:294:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_open_positions_row.py:314:        PriceCache, "get_many",
tests/web/test_view_models/test_open_positions_row.py:323:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:1:"""PriceCache — TTL, market-hours, timeout fallback, circuit breaker."""
tests/web/test_price_cache.py:41:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:45:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:65:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:69:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:79:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:83:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:98:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:100:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:108:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:114:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:137:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:143:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:166:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:170:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:194:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:196:    a = PriceCache(cfg)
tests/web/test_price_cache.py:197:    b = PriceCache(cfg)
tests/web/test_price_cache.py:215:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:218:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:245:    the PriceCache always fall back to last-close, even during live
tests/web/test_price_cache.py:248:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:251:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:282:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:285:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:302:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:305:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:321:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:324:    cache = PriceCache(cfg)
tests/web/test_price_cache.py:337:    from swing.web.price_cache import PriceCache
tests/web/test_price_cache.py:341:    cache = PriceCache(cfg)
swing/web/trade_charts.py:4:writes chart_renders (closed-trade charts are neither run-bound nor safely
swing/web/trade_charts.py:14:from swing.data.ohlcv_archive import read_or_fetch_archive
swing/web/trade_charts.py:51:    Returns FULL archive rows <= window_end (read_or_fetch_archive semantics)
swing/web/trade_charts.py:57:    df = read_or_fetch_archive(
swing/web/trade_charts.py:92:    no-coverage. Render-direct; no chart_renders write.
tests/web/test_theme_toggle.py:30:from swing.web.price_cache import PriceCache
tests/web/test_theme_toggle.py:35:        PriceCache, "get_many",
tests/web/test_theme_toggle.py:38:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/pipeline/test_ohlcv_cache_shape_parity.py:10:``swing/pipeline/runner.py:1323`` inside ``_step_charts``.
tests/pipeline/test_ohlcv_cache_shape_parity.py:16:The test monkeypatches ``read_or_fetch_archive`` at BOTH module-level import
tests/pipeline/test_ohlcv_cache_shape_parity.py:45:    Mirrors the shape returned by ``read_or_fetch_archive`` — the source-of-truth
tests/pipeline/test_ohlcv_cache_shape_parity.py:83:    def _stub_read_or_fetch_archive(ticker, *, end_date, cache_dir, archive_history_days):
tests/pipeline/test_ohlcv_cache_shape_parity.py:98:        "swing.prices.read_or_fetch_archive", _stub_read_or_fetch_archive
tests/pipeline/test_ohlcv_cache_shape_parity.py:101:        "swing.web.ohlcv_cache.read_or_fetch_archive",
tests/pipeline/test_ohlcv_cache_shape_parity.py:102:        _stub_read_or_fetch_archive,
tests/pipeline/test_ohlcv_cache_shape_parity.py:149:    classifier/render_chart contract at the existing ``_step_charts`` callsite.
tests/pipeline/test_ohlcv_cache_shape_parity.py:164:        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub, raising=False,
tests/pipeline/test_ohlcv_cache_shape_parity.py:188:    behavior so ``_step_charts``'s ``except Exception`` produces
tests/pipeline/test_ohlcv_cache_shape_parity.py:192:    ``_step_charts`` line 1322-1330 contract.
tests/pipeline/test_ohlcv_cache_shape_parity.py:205:        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub_empty, raising=False,
tests/pipeline/test_ohlcv_cache_shape_parity.py:239:        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub, raising=False,
tests/pipeline/test_ohlcv_cache_shape_parity.py:288:        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub, raising=False,
tests/web/test_trade_charts_window.py:23:    # read_or_fetch_archive returns rows <= end_date (production semantics);
tests/web/test_trade_charts_window.py:30:    monkeypatch.setattr(tc, "read_or_fetch_archive", _fake)
tests/web/test_trade_charts_window.py:43:    monkeypatch.setattr(tc, "read_or_fetch_archive", lambda *a, **k: _archive(days))
tests/web/test_trade_charts_window.py:50:    monkeypatch.setattr(tc, "read_or_fetch_archive", lambda *a, **k: None)
tests/web/test_routes/test_patterns_review_data_completeness.py:324:    Anchored at today's last-completed-session so ``read_or_fetch_archive``
tests/web/test_routes/test_watchlist_template_thumbnail.py:18:from swing.data.repos.chart_renders import refresh_chart_render
tests/web/test_routes/test_watchlist_template_thumbnail.py:54:    inline SVG thumbnail bytes pulled from chart_renders cache."""
tests/web/test_routes/test_watchlist_template_thumbnail.py:64:                source_data_hash="h",
tests/web/test_routes/test_watchlist_template_thumbnail.py:89:            # No chart_renders row planted -> cache miss.
tests/web/test_trade_charts_thumbnail.py:71:    monkeypatch.setattr(tc, "read_or_fetch_archive", planted_archive)
tests/web/test_trade_charts_thumbnail.py:79:    monkeypatch.setattr(tc, "read_or_fetch_archive", lambda *a, **k: None)
tests/web/test_trade_charts_thumbnail.py:86:    monkeypatch.setattr(tc, "read_or_fetch_archive", planted_archive)
tests/web/test_trade_charts_thumbnail.py:96:    monkeypatch.setattr(tc, "read_or_fetch_archive", planted_archive)
tests/pipeline/test_ohlcv_cache_concurrent_fetch_no_race.py:81:        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub_read,
tests/pipeline/test_ohlcv_cache_concurrent_fetch_no_race.py:161:        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub_read,
tests/web/test_routes/test_watchlist_route.py:32:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_route.py:33:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_watchlist_route.py:35:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_watchlist_route.py:47:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_route.py:48:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_watchlist_route.py:72:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_route.py:73:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_watchlist_route.py:75:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_watchlist_route.py:94:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_route.py:95:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_watchlist_route.py:97:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_watchlist_route.py:132:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_route.py:133:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_watchlist_route.py:162:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_route.py:163:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_watchlist_route.py:165:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_trade_charts_render.py:69:    monkeypatch.setattr(tc, "read_or_fetch_archive", planted_archive)
tests/web/test_trade_charts_render.py:77:    monkeypatch.setattr(tc, "read_or_fetch_archive", lambda *a, **k: None)
tests/web/test_trade_charts_render.py:84:    monkeypatch.setattr(tc, "read_or_fetch_archive", planted_archive)
tests/web/test_trade_charts_render.py:95:    monkeypatch.setattr(tc, "read_or_fetch_archive", planted_archive)
tests/pipeline/test_ohlcv.py:93:    monkeypatch.setattr(mod, "read_or_fetch_archive", fake_helper)
tests/pipeline/test_ohlcv.py:119:    monkeypatch.setattr(mod, "read_or_fetch_archive",
tests/pipeline/test_ohlcv.py:139:    monkeypatch.setattr(mod, "read_or_fetch_archive", boom)
tests/pipeline/test_ohlcv.py:154:    monkeypatch.setattr(mod, "read_or_fetch_archive", lambda *a, **kw: None)
tests/pipeline/test_ohlcv.py:177:    monkeypatch.setattr(mod, "read_or_fetch_archive", fake_helper)
tests/evaluation/test_pe_origin_ladder.py:51:    # column on this table (that is chart_renders).
tests/web/test_state_badge_partial.py:53:    """Stub PriceCache so the dashboard renders without network calls."""
tests/web/test_state_badge_partial.py:54:    from swing.web.price_cache import PriceCache
tests/web/test_state_badge_partial.py:56:        PriceCache, "get_many",
tests/web/test_state_badge_partial.py:59:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_watchlist_pin_route.py:21:from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_pin_route.py:61:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_watchlist_pin_route.py:63:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_trades_integration.py:28:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_trades_integration.py:49:        PriceCache, "get_many",
tests/web/test_trades_integration.py:56:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_trades_integration.py:108:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_trades_integration.py:137:        PriceCache, "get_many",
tests/web/test_trades_integration.py:144:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_trades_integration.py:190:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_trades_integration.py:209:        PriceCache, "get_many",
tests/web/test_trades_integration.py:251:    from swing.web.price_cache import PriceCache
tests/web/test_trades_integration.py:299:    monkeypatch.setattr(PriceCache, "market_hours_now", lambda self: True)
tests/web/test_trades_integration.py:302:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_patterns_exemplars_enhanced.py:20:from swing.data.repos.chart_renders import refresh_chart_render
tests/web/test_routes/test_patterns_exemplars_enhanced.py:98:def test_get_patterns_exemplars_chart_consumed_from_chart_renders_cache_when_available(
tests/web/test_routes/test_patterns_exemplars_enhanced.py:106:            # Plant pipeline_runs row FIRST (FK target for chart_renders).
tests/web/test_routes/test_patterns_exemplars_enhanced.py:117:                source_data_hash="h",
tests/web/test_routes/test_patterns_exemplars_enhanced.py:137:def test_get_patterns_exemplars_handles_missing_chart_renders_gracefully(
tests/web/test_routes/test_patterns_exemplars_enhanced.py:335:    pipeline has not yet populated the chart_renders cache.
tests/web/test_routes/test_patterns_exemplars_enhanced.py:397:# call `refresh_chart_render` to populate the `chart_renders` cache so a
tests/web/test_routes/test_patterns_exemplars_enhanced.py:419:def test_cache_miss_render_writes_through_to_chart_renders_cache(
tests/web/test_routes/test_patterns_exemplars_enhanced.py:423:    chart_renders via `refresh_chart_render` so the next build is a hit.
tests/web/test_routes/test_patterns_exemplars_enhanced.py:426:    (renderer stubbed); second call must serve from chart_renders (renderer
tests/web/test_routes/test_patterns_exemplars_enhanced.py:431:    from swing.data.repos.chart_renders import list_chart_renders
tests/web/test_routes/test_patterns_exemplars_enhanced.py:466:        # chart_renders cache row written.
tests/web/test_routes/test_patterns_exemplars_enhanced.py:467:        rows = list_chart_renders(conn, surface="theme2_annotated")
tests/web/test_routes/test_patterns_exemplars_enhanced.py:495:    from swing.data.repos.chart_renders import list_chart_renders
tests/web/test_routes/test_patterns_exemplars_enhanced.py:515:                source_data_hash="hg",
tests/web/test_routes/test_patterns_exemplars_enhanced.py:546:        rows = list_chart_renders(conn, surface="theme2_annotated")
tests/web/test_routes/test_patterns_exemplars_enhanced.py:563:    from swing.data.repos.chart_renders import list_chart_renders
tests/web/test_routes/test_patterns_exemplars_enhanced.py:589:        rows = list_chart_renders(conn, surface="theme2_annotated")
tests/web/test_view_models/test_hyp_recs_sort_neutrality.py:31:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_hyp_recs_sort_neutrality.py:42:    """Stub PriceCache.get_many so the builders don't fetch live prices."""
tests/web/test_view_models/test_hyp_recs_sort_neutrality.py:51:    monkeypatch.setattr(PriceCache, "get_many", get_many)
tests/web/test_view_models/test_hyp_recs_sort_neutrality.py:52:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_hyp_recs_sort_neutrality.py:53:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_hyp_recs_sort_neutrality.py:151:    cache = PriceCache(cfg)
tests/web/test_view_models/test_hyp_recs_sort_neutrality.py:188:    cache = PriceCache(cfg)
tests/web/test_view_models/test_hyp_recs_sort_neutrality.py:281:    cache = PriceCache(cfg)
tests/pipeline/test_observe_remediation.py:11:    write_window,
tests/pipeline/test_observe_remediation.py:47:            write_window(ticker, pd.DataFrame(
tests/pipeline/test_observe_remediation.py:69:    write_window("AAPL", pd.DataFrame([["2026-06-04", 10, 11, 9, 10.5, 100]],
tests/web/test_routes/test_watchlist_pin_render.py:20:from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_pin_render.py:60:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_watchlist_pin_render.py:62:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_hyp_recs_jit_fallback.py:71:            "SELECT data_asof_date FROM chart_renders "
tests/web/test_view_models/test_hyp_recs_jit_fallback.py:128:                "INSERT INTO chart_renders "
tests/web/test_view_models/test_hyp_recs_jit_fallback.py:130:                "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
swing/web/templates/trades/detail.html.j2:62:   chart_renders cache (surface='position_detail'; pipeline_run_id IS
tests/web/test_routes/test_watchlist_pattern_tag_render.py:18:from swing.web.price_cache import PriceCache
tests/web/test_routes/test_watchlist_pattern_tag_render.py:27:    """Stub PriceCache so the route doesn't try to fetch prices."""
tests/web/test_routes/test_watchlist_pattern_tag_render.py:29:        PriceCache, "get_many",
tests/web/test_routes/test_watchlist_pattern_tag_render.py:32:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_watchlist_pattern_tag_render.py:33:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_topbar_cross_vm_consistency.py:154:    """Minimal PriceCache stand-in for build_watchlist on an empty universe."""
tests/web/test_routes/test_open_positions_expand.py:154:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_open_positions_expand.py:155:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_open_positions_expand.py:157:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_open_positions_expand.py:233:    SB3 `position_detail` SVG (read from the chart_renders cache) instead of
tests/web/test_routes/test_open_positions_expand.py:242:    from swing.data.repos.chart_renders import insert_chart_render
tests/web/test_routes/test_open_positions_expand.py:252:                source_data_hash="hash-pd",
tests/web/test_routes/test_open_positions_expand.py:654:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_open_positions_expand.py:669:        PriceCache, "get_many",
tests/web/test_routes/test_open_positions_expand.py:672:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_open_positions_expand.py:673:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_open_positions_expand.py:705:    advisory message content. Stub PriceCache + OhlcvCache so trail_10ma
tests/web/test_routes/test_open_positions_expand.py:711:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_open_positions_expand.py:716:    # PriceCache: live price 100.0 (matches trade entry).
tests/web/test_routes/test_open_positions_expand.py:718:        PriceCache, "get_many",
tests/web/test_routes/test_open_positions_expand.py:726:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_open_positions_expand.py:770:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_open_positions_expand.py:799:        PriceCache, "get_many",
tests/web/test_routes/test_open_positions_expand.py:807:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:72:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:81:    monkeypatch.setattr(PriceCache, "get_many", get_many)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:82:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:83:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:89:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:101:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:131:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:142:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:157:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:168:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:178:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:184:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:194:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:207:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:227:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:237:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:261:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:345:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:379:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:402:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:520:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:548:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:636:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_hypothesis_recommendations.py:672:    cache = PriceCache(cfg)
tests/web/test_view_models/test_hyp_recs_expansion_vm.py:545:    """A.AC.2 — when builder is called with `cache=<PriceCache-like>` AND
tests/web/test_view_models/test_hyp_recs_expansion_vm.py:549:    PriceCache+executor pattern. Discriminating: a stub cache returning
swing/diagnostics/prune_chart_cache.py:1:"""Phase 13 T-T4.SB.3 (Item 5; OQ-5.1 R4 LOCK) — chart_renders cache prune helper.
swing/diagnostics/prune_chart_cache.py:3:Operator-locked manual prune CLI for `chart_renders` rows older than a
swing/diagnostics/prune_chart_cache.py:17:def prune_chart_renders_older_than(
swing/diagnostics/prune_chart_cache.py:20:    """DELETE `chart_renders` rows with ``rendered_at`` older than
swing/diagnostics/prune_chart_cache.py:25:    ``swing/data/repos/chart_renders.py``).
swing/diagnostics/prune_chart_cache.py:36:        "DELETE FROM chart_renders WHERE rendered_at < ?",
tests/web/test_routes/test_watchlist_jit_integration.py:17:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_watchlist_jit_integration.py:34:        PriceCache, "get_many",
tests/web/test_routes/test_watchlist_jit_integration.py:39:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_watchlist_jit_integration.py:40:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_watchlist_jit_integration.py:53:            "INSERT INTO chart_renders "
tests/web/test_routes/test_watchlist_jit_integration.py:55:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:26:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:31:    monkeypatch.setattr(PriceCache, "get_many", fake_get_many)
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:32:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:33:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:41:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:104:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:125:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:158:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:170:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4_candidates_eval_scoping.py:213:    cache = PriceCache(cfg)
tests/web/test_routes/test_metrics_routes.py:113:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_metrics_routes.py:115:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_metrics_routes.py:117:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:76:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:82:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:110:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:129:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:155:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:180:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:216:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:222:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:285:    # Patch PriceCache.get_many to return a canned live price.
tests/web/test_view_models/test_dashboard.py:286:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:289:        PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:297:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:298:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:300:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:321:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard.py:328:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:330:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:331:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:333:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:353:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:375:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:406:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:412:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:432:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard.py:448:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:467:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:485:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:508:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:520:        PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:525:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:526:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:564:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:586:        PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:591:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:592:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:629:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:647:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:675:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:698:        PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:703:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:704:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:730:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:736:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:760:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard.py:777:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:791:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:815:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:846:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:858:        PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:863:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:864:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:896:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:926:        PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:931:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:932:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:957:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard.py:976:        PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:979:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:980:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:1067:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard.py:1087:        PriceCache, "get_many",
tests/web/test_view_models/test_dashboard.py:1090:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard.py:1091:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard.py:1093:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard.py:1153:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_dashboard.py:1184:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_t4.py:82:    """Stub PriceCache/OhlcvCache so build_dashboard doesn't try real I/O.
tests/web/test_view_models/test_dashboard_t4.py:84:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4.py:89:    monkeypatch.setattr(PriceCache, "get_many", fake_get_many)
tests/web/test_view_models/test_dashboard_t4.py:90:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_dashboard_t4.py:91:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_dashboard_t4.py:105:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4.py:142:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_t4.py:160:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4.py:186:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_t4.py:203:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4.py:222:    cache = PriceCache(cfg)
tests/web/test_view_models/test_dashboard_t4.py:234:    are always charted by `_step_charts`). Bug 7's reported symptom
tests/web/test_view_models/test_dashboard_t4.py:240:    from swing.web.price_cache import PriceCache
tests/web/test_view_models/test_dashboard_t4.py:274:    cache = PriceCache(cfg)
tests/web/test_routes/test_trade_entry_sector_industry_tamper.py:37:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trade_entry_sector_industry_tamper.py:42:        PriceCache, "get_many",
tests/web/test_routes/test_trade_entry_sector_industry_tamper.py:50:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trade_entry_sector_industry_tamper.py:51:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_trade_entry_intent.py:19:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trade_entry_intent.py:25:        PriceCache, "get_many",
tests/web/test_routes/test_trade_entry_hypothesis_thread.py:27:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trade_entry_hypothesis_thread.py:135:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trade_entry_hypothesis_thread.py:180:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trade_entry_hypothesis_thread.py:260:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trade_entry_hypothesis_thread.py:339:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_view_models/test_build_hyp_recs_section.py:30:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_view_models/test_build_hyp_recs_section.py:34:    """Stub PriceCache.get_many so the builder doesn't fetch live prices."""
tests/web/test_view_models/test_build_hyp_recs_section.py:43:    monkeypatch.setattr(PriceCache, "get_many", get_many)
tests/web/test_view_models/test_build_hyp_recs_section.py:44:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_view_models/test_build_hyp_recs_section.py:45:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_view_models/test_build_hyp_recs_section.py:178:    cache = PriceCache(cfg)
tests/web/test_view_models/test_build_hyp_recs_section.py:220:    cache = PriceCache(cfg)
tests/web/test_view_models/test_build_hyp_recs_section.py:267:    cache = PriceCache(cfg)
tests/web/test_routes/test_trade_entry_chart_pattern.py:29:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trade_entry_chart_pattern.py:37:    """Stub PriceCache so the route doesn't try to fetch prices."""
tests/web/test_routes/test_trade_entry_chart_pattern.py:39:        PriceCache, "get_many",
tests/web/test_routes/test_trade_entry_chart_pattern.py:42:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trade_entry_chart_pattern.py:43:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_trade_entry_chart_pattern.py:161:    """Stub PriceCache to return a usable snapshot for AAPL POST flows."""
tests/web/test_routes/test_trade_entry_chart_pattern.py:163:        PriceCache, "get_many",
tests/web/test_routes/test_trade_entry_chart_pattern.py:171:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trade_entry_chart_pattern.py:172:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
swing/data/db.py:32:#   5 new tables (pattern_exemplars + chart_renders + pattern_evaluations +
swing/data/db.py:47:#   atomic rename of the chart_renders.surface old detail token to
swing/data/db.py:169:# tables (pattern_exemplars + chart_renders + pattern_evaluations +
swing/data/db.py:180:        "chart_renders",
swing/data/db.py:201:# chart_renders.surface enum value via a single-table rebuild -- NO new tables
swing/data/db.py:1103:    real production v22 DB about to receive migration 0023 (chart_renders
tests/web/test_routes/test_hyp_recs_expand_route.py:21:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_hyp_recs_expand_route.py:26:    """Stub PriceCache.get_many so the route doesn't try to fetch prices."""
tests/web/test_routes/test_hyp_recs_expand_route.py:35:    monkeypatch.setattr(PriceCache, "get_many", get_many)
tests/web/test_routes/test_hyp_recs_expand_route.py:36:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_hyp_recs_expand_route.py:37:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_hyp_recs_expand_route.py:587:# PriceCache+executor `get_many` path threaded from the route handler.
tests/web/test_routes/test_hyp_recs_expand_route.py:593:    """Stub PriceCache.get_many (batch path used by build_hyp_recs_expanded)
tests/web/test_routes/test_hyp_recs_expand_route.py:606:    monkeypatch.setattr(PriceCache, "get_many", _get_many)
tests/web/test_routes/test_hyp_recs_expand_route.py:607:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_hyp_recs_expand_route.py:608:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_hyp_recs_expand_route.py:650:        f"expand route must call PriceCache.get_many(['NVDA'], ...) exactly "
tests/web/test_routes/test_hyp_recs_expand_route.py:694:    """A.AC.6 — when PriceCache.get_many returns an empty dict (no live,
tests/web/test_routes/test_hyp_recs_expand_route.py:1039:    """Stub PriceCache.get_many to return empty dict so snap is None.
tests/web/test_routes/test_hyp_recs_expand_route.py:1047:    monkeypatch.setattr(PriceCache, "get_many", get_many)
tests/web/test_routes/test_hyp_recs_expand_route.py:1048:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_hyp_recs_expand_route.py:1049:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
swing/cli.py:5430:    help="Delete chart_renders rows whose rendered_at is older than N days.",
swing/cli.py:5434:    chart_renders cache.
swing/cli.py:5436:    Deletes ``chart_renders`` rows whose ``rendered_at`` is older than
swing/cli.py:5443:        prune_chart_renders_older_than,
swing/cli.py:5453:                deleted = prune_chart_renders_older_than(
swing/cli.py:5465:        # chart_renders table on a legacy schema).
swing/cli.py:5469:    click.echo(f"Deleted {deleted} chart_renders rows older than "
tests/web/test_routes/test_exit_post_audit_columns.py:42:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_exit_post_audit_columns.py:47:        PriceCache, "get_many",
tests/web/test_routes/test_exit_post_audit_columns.py:56:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_exit_post_audit_columns.py:57:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_exit_form_auto_fill.py:41:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_exit_form_auto_fill.py:46:        PriceCache, "get_many",
tests/web/test_routes/test_exit_form_auto_fill.py:55:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_exit_form_auto_fill.py:56:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_exit_form_auto_fill.py:515:    cache = PriceCache.__new__(PriceCache)  # bypass __init__
swing/data/repos/chart_renders.py:1:"""``chart_renders`` repo — Phase 13 T2.SB1 task T-A.1.1b.
swing/data/repos/chart_renders.py:9:``idx_chart_renders_run_bound`` + ``idx_chart_renders_position_detail`` +
swing/data/repos/chart_renders.py:10:``idx_chart_renders_theme2_annotated`` (SQLite NULL-distinct defense per
swing/data/repos/chart_renders.py:26:    "source_data_hash",
swing/data/repos/chart_renders.py:42:        source_data_hash=row[6],
swing/data/repos/chart_renders.py:51:    """Insert one ``chart_renders`` row; return new id.
swing/data/repos/chart_renders.py:59:        INSERT INTO chart_renders
swing/data/repos/chart_renders.py:61:             chart_svg_bytes, source_data_hash, rendered_at, data_asof_date)
swing/data/repos/chart_renders.py:70:            chart_render.source_data_hash,
swing/data/repos/chart_renders.py:82:        f"SELECT {_SELECT_COLUMNS_SQL} FROM chart_renders WHERE id = ?",
swing/data/repos/chart_renders.py:90:def list_chart_renders(
swing/data/repos/chart_renders.py:100:    """List chart_renders filtered by optional ticker / surface /
swing/data/repos/chart_renders.py:129:        f"SELECT {_SELECT_COLUMNS_SQL} FROM chart_renders"
swing/data/repos/chart_renders.py:167:    ``tests/data/test_chart_renders_repo.py`` (Phase 8 ``cfacbc5``
swing/data/repos/chart_renders.py:190:        "SELECT chart_svg_bytes FROM chart_renders WHERE "
swing/data/repos/chart_renders.py:223:            "DELETE FROM chart_renders WHERE ticker = ? AND surface = ? "
swing/data/repos/chart_renders.py:234:            "DELETE FROM chart_renders WHERE ticker = ? AND surface = ? "
swing/data/repos/chart_renders.py:241:            "DELETE FROM chart_renders WHERE ticker = ? AND surface = ? "
tests/web/test_routes/test_trades_route.py:85:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:103:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:139:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:157:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:197:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:215:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:222:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:255:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:282:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:289:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:356:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:360:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:367:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:478:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:482:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:489:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:570:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:587:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:611:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_trades_route.py:626:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:648:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:664:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:711:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:727:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:733:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:758:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:774:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:780:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:802:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_trades_route.py:818:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:867:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:883:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:911:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:927:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1025:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1041:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1063:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_trades_route.py:1079:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1166:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_trades_route.py:1181:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1239:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1257:        PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1264:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:1328:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1346:        PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1353:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:1404:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1422:        PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1429:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:1481:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1507:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1602:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1618:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1658:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1675:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1715:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1732:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1738:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:1773:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1790:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1832:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1849:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:1854:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:1978:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:1994:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:2017:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:2033:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:2321:    """Make PriceCache.get_many return live snapshots for any requested ticker."""
tests/web/test_routes/test_trades_route.py:2323:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:2325:        PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:2333:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:2548:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:2588:        PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:2596:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:2774:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:2792:        PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:2800:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:2861:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:2879:        PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:2887:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:3004:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_trades_route.py:3008:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:3044:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_trades_route.py:3048:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:3137:    """Monkeypatch PriceCache so tests don't hit yfinance."""
tests/web/test_routes/test_trades_route.py:3139:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:3141:        PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:3149:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_route.py:3817:    Bypasses ``record_entry`` because that path requires PriceCache and a
tests/web/test_routes/test_trades_route.py:4170:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:4174:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:4199:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:4203:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_trades_route.py:4365:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_trades_route.py:4369:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_entry_post_pipeline_drift_pin.py:95:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_entry_post_pipeline_drift_pin.py:97:        PriceCache, "get_many",
tests/web/test_routes/test_entry_post_pipeline_drift_pin.py:105:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_trades_detail_chart.py:4:  - TradeDetailVM populated from chart_renders cache (surface='position_detail',
tests/web/test_routes/test_trades_detail_chart.py:19:from swing.data.repos.chart_renders import refresh_chart_render
tests/web/test_routes/test_trades_detail_chart.py:47:    pulled from chart_renders (surface='position_detail';
tests/web/test_routes/test_trades_detail_chart.py:57:                source_data_hash="h",
tests/web/test_routes/test_trades_detail_chart.py:107:                source_data_hash="h",
tests/web/test_routes/test_entry_post_audit_columns.py:30:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/web/test_routes/test_entry_post_audit_columns.py:35:        PriceCache, "get_many",
tests/web/test_routes/test_entry_post_audit_columns.py:43:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_entry_post_audit_columns.py:44:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_entry_form_auto_fill.py:26:from swing.web.price_cache import PriceCache
tests/web/test_routes/test_entry_form_auto_fill.py:31:        PriceCache, "get_many",
tests/web/test_routes/test_entry_form_auto_fill.py:34:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_entry_form_auto_fill.py:35:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_entry_form_auto_fill.py:354:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_entry_form_auto_fill.py:356:    cache = PriceCache.__new__(PriceCache)  # bypass __init__
tests/integrations/test_schwab_window_ladder_daily_kwargs.py:5:Root cause established at T-GF1 (commit d440578): `_bars_hook` at
tests/integrations/test_schwab_window_ladder_daily_kwargs.py:175:    behavior change. Only the `_bars_hook` callsite at
tests/integrations/test_schwab_window_ladder_daily_kwargs.py:176:    swing/pipeline/runner.py:_install_pipeline_marketdata_caches is updated
tests/integrations/test_schwab_window_ladder_daily_kwargs.py:202:    # `_bars_hook` callsite by passing the daily kwargs explicitly).
tests/pipeline/test_daily_management_step.py:108:    # at swing.pipeline.runner.read_or_fetch_archive is the only fetch now.
tests/pipeline/test_daily_management_step.py:110:        "swing.pipeline.runner.read_or_fetch_archive",
tests/pipeline/test_daily_management_step.py:366:        "swing.pipeline.runner.read_or_fetch_archive",
tests/pipeline/test_daily_management_step.py:437:    monkeypatch.setattr("swing.pipeline.runner.read_or_fetch_archive", boom)
tests/pipeline/test_daily_management_step.py:519:        "swing.pipeline.runner.read_or_fetch_archive", lambda *a, **kw: None,
tests/pipeline/test_daily_management_step.py:560:        "swing.pipeline.runner.read_or_fetch_archive", lambda *a, **kw: df_no_asof,
tests/pipeline/test_daily_management_step.py:582:    read_or_fetch_archive is lease-free today, but the guard must be structural so
tests/pipeline/test_daily_management_step.py:592:    monkeypatch.setattr("swing.pipeline.runner.read_or_fetch_archive", revoke_warm)
tests/web/test_routes/test_dashboard_route.py:39:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_dashboard_route.py:40:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_dashboard_route.py:42:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
swing/data/ohlcv_archive.py:159:    # here -- the one function ALL Shape-A writers funnel through (write_window,
swing/data/ohlcv_archive.py:160:    # read_or_fetch_archive, _backward_compat_rename) -- so none can bypass it.
swing/data/ohlcv_archive.py:282:    BOTH read_or_fetch_archive AND warm_archives_batch with the SAME
swing/data/ohlcv_archive.py:340:    using the EXACT read_or_fetch_archive predicates (Arc 6 §4.1). Local I/O
swing/data/ohlcv_archive.py:370:        # mirroring read_or_fetch_archive's needs_full_refresh arms.
swing/data/ohlcv_archive.py:508:    the serial read_or_fetch_archive incremental-gap branch (lines ~277-281):
swing/data/ohlcv_archive.py:529:        # routes the ticker to WarmReport.fallback; the serial read_or_fetch_archive
swing/data/ohlcv_archive.py:576:    so the serial read_or_fetch_archive loops all hit the cache-hit branch
swing/data/ohlcv_archive.py:586:    the serial write path (`read_or_fetch_archive` + `_write_archive_atomic`'s
swing/data/ohlcv_archive.py:715:def read_or_fetch_archive(
swing/data/ohlcv_archive.py:806:# `read_or_fetch_archive` above; the one-shot `_backward_compat_rename`
swing/data/ohlcv_archive.py:820:    discipline at `read_or_fetch_archive`).
swing/data/ohlcv_archive.py:825:def write_window(
swing/data/ohlcv_archive.py:862:                f"write_window expects pd.DataFrame, got {type(window).__name__}"
swing/data/ohlcv_archive.py:868:                f"write_window expects pd.DataFrame, got {type(window).__name__}")
swing/data/ohlcv_archive.py:878:            log.warning("write_window: failed to read existing %s (%s)", path, exc)
swing/data/ohlcv_archive.py:933:        ticker: ticker symbol; will be uppercased to match write_window.
swing/data/ohlcv_archive.py:1052:    persisting via `write_window`. The underscore-prefixed alias is
swing/data/ohlcv_archive.py:1055:    Legacy archives produced by ``read_or_fetch_archive`` /
swing/data/ohlcv_archive.py:1064:        ``write_window`` writer pattern from ``map_price_history_to_window``.
swing/data/ohlcv_archive.py:1130:    IN PLACE because ``read_or_fetch_archive`` (consumed by
swing/data/ohlcv_archive.py:1140:    ``read_or_fetch_archive`` have been refactored to consume the Shape A
swing/data/ohlcv_archive.py:1197:        # (Codex R2 Major #1 copy-not-move) — read_or_fetch_archive still
swing/data/ohlcv_archive.py:1232:        #   - read_or_fetch_archive consumers read legacy directly (so
tests/web/test_routes/test_dashboard_chart_integration.py:24:from swing.data.repos.chart_renders import refresh_chart_render
tests/web/test_routes/test_dashboard_chart_integration.py:52:        source_data_hash="hash-w",
tests/web/test_routes/test_dashboard_chart_integration.py:161:                source_data_hash="h",
tests/web/test_routes/test_dashboard_chart_integration.py:169:    class _StubPriceCache:
tests/web/test_routes/test_dashboard_chart_integration.py:177:        cfg=cfg, cache=_StubPriceCache(), executor=None, ohlcv_cache=None,
tests/web/test_routes/test_dashboard_chart_integration.py:217:                source_data_hash="h",
tests/web/test_routes/test_dashboard_chart_integration.py:225:    class _StubPriceCache:
tests/web/test_routes/test_dashboard_chart_integration.py:233:        cfg=cfg, cache=_StubPriceCache(), executor=None, ohlcv_cache=None,
tests/web/test_routes/test_dashboard_chart_integration.py:369:        from swing.data.repos.chart_renders import get_cached_chart_svg
tests/web/test_routes/test_dashboard_chart_integration.py:392:    """Production-shape DataFrame matching read_or_fetch_archive output:
tests/web/test_routes/test_dashboard_chart_integration.py:575:    chart_renders row written (spec section 5.6 example #1)."""
tests/web/test_routes/test_dashboard_chart_integration.py:587:            "SELECT COUNT(*) FROM chart_renders "
tests/pipeline/test_daily_management_fence_hoist.py:47:    """EXACT pre-fix: read_or_fetch_archive runs inside compute_* under the held
tests/pipeline/test_daily_management_fence_hoist.py:103:    monkeypatch.setattr("swing.data.ohlcv_archive.read_or_fetch_archive", spying_warm)
tests/pipeline/test_daily_management_fence_hoist.py:104:    monkeypatch.setattr("swing.pipeline.runner.read_or_fetch_archive", spying_warm)
tests/web/test_routes/test_recommendations_detail_chart.py:4:  - VM field populated from chart_renders cache (surface='ticker_detail').
tests/web/test_routes/test_recommendations_detail_chart.py:23:from swing.data.repos.chart_renders import refresh_chart_render
tests/web/test_routes/test_recommendations_detail_chart.py:77:    pulled from chart_renders (surface='ticker_detail', run-bound key)."""
tests/web/test_routes/test_recommendations_detail_chart.py:86:                source_data_hash="h",
tests/web/test_routes/test_recommendations_detail_chart.py:134:                source_data_hash="h",
swing/data/models.py:26:# chart_renders.pattern_class. Re-exported from `swing/patterns/__init__.py`
swing/data/models.py:96:# chart_renders.surface enum (5 values per spec §3.2; v23 renamed the old
swing/data/models.py:1989:    """One row of ``chart_renders`` (migration 0020; spec §3.2).
swing/data/models.py:2001:    source_data_hash: str
swing/data/models.py:2026:                    "chart_renders cross-column CHECK violated: "
swing/data/models.py:2033:                    "chart_renders cross-column CHECK violated: "
swing/data/models.py:2046:                    "chart_renders cache key shape violated: "
swing/data/models.py:2056:                "chart_renders cache key shape violated: "
swing/data/models.py:2069:                "chart_renders chart_svg_bytes must be non-empty per "
tests/pipeline/test_chart_bytes_parity_through_ohlcv_cache.py:93:    monkeypatch.setattr("swing.prices.read_or_fetch_archive", _stub_read)
tests/pipeline/test_chart_bytes_parity_through_ohlcv_cache.py:95:        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub_read,
tests/web/test_routes/test_pipeline_route.py:353:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_pipeline_route.py:355:    orig_refresh = PriceCache.refresh_all
tests/web/test_routes/test_pipeline_route.py:356:    orig_reset = PriceCache.reset_circuit_breaker
tests/web/test_routes/test_pipeline_route.py:363:    monkeypatch.setattr(PriceCache, "refresh_all", wrapped_refresh)
tests/web/test_routes/test_pipeline_route.py:364:    monkeypatch.setattr(PriceCache, "reset_circuit_breaker", wrapped_reset)
tests/web/test_routes/test_pipeline_route.py:365:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_pipeline_route.py:367:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_pipeline_route.py:390:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_pipeline_route.py:392:    monkeypatch.setattr(PriceCache, "refresh_all", lambda self, tickers: None)
tests/web/test_routes/test_pipeline_route.py:393:    monkeypatch.setattr(PriceCache, "reset_circuit_breaker", lambda self: None)
tests/web/test_routes/test_pipeline_route.py:394:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_pipeline_route.py:396:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_pipeline_route.py:417:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_pipeline_route.py:420:    orig_reset = PriceCache.reset_circuit_breaker
tests/web/test_routes/test_pipeline_route.py:421:    orig_refresh = PriceCache.refresh_all
tests/web/test_routes/test_pipeline_route.py:431:    monkeypatch.setattr(PriceCache, "reset_circuit_breaker", wrapped_reset)
tests/web/test_routes/test_pipeline_route.py:432:    monkeypatch.setattr(PriceCache, "refresh_all", wrapped_refresh)
tests/web/test_routes/test_pipeline_route.py:433:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_pipeline_route.py:461:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_pipeline_route.py:471:    monkeypatch.setattr(PriceCache, "refresh_all", lambda self, tickers: None)
tests/web/test_routes/test_pipeline_route.py:472:    monkeypatch.setattr(PriceCache, "reset_circuit_breaker", lambda self: None)
tests/web/test_routes/test_pipeline_route.py:473:    monkeypatch.setattr(PriceCache, "get_many",
tests/web/test_routes/test_pipeline_route.py:475:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_pipeline_route.py:820:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_pipeline_route.py:867:    monkeypatch.setattr(PriceCache, "refresh_all", capturing_refresh_all)
tests/web/test_routes/test_pipeline_route.py:869:        PriceCache, "get_many",
tests/web/test_routes/test_pipeline_route.py:872:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/web/test_routes/test_pipeline_route.py:873:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/web/test_routes/test_pipeline_route.py:874:    monkeypatch.setattr(PriceCache, "reset_circuit_breaker", lambda self: None)
tests/web/test_routes/test_phase13_t2_sb6c_t_a_6c_4.py:1427:    from swing.web.price_cache import PriceCache
tests/web/test_routes/test_phase13_t2_sb6c_t_a_6c_4.py:1429:    cache = PriceCache(cfg)
tests/integrations/test_schwab_token_redaction_audit.py:1396:    `write_window` (T-C.2) MUST NOT contain the sentinel anywhere.
tests/integrations/test_schwab_token_redaction_audit.py:1406:    from swing.data.ohlcv_archive import write_window
tests/integrations/test_schwab_token_redaction_audit.py:1461:    write_window("XYZ", df, "schwab_api", cache_dir=cache_dir)
swing/web/chart_jit.py:39:from swing.data.repos.chart_renders import (
swing/web/chart_jit.py:44:    compute_chart_source_hash,
swing/web/chart_jit.py:67:# ``_step_charts`` invocation pattern + spec §C.5 line 449 thumbnail design).
swing/web/chart_jit.py:73:def get_or_render_surface(
swing/web/chart_jit.py:82:    source_data_hash: str = "chart_jit_v1",
swing/web/chart_jit.py:117:    # Step 2: fetch OHLCV via cache (per ``_step_charts._bars_or_none`` precedent).
swing/web/chart_jit.py:161:            # (the two live sites -- pipeline _step_charts + the weather
swing/web/chart_jit.py:201:    # asof_date) instead of the static ``source_data_hash`` default so the cache
swing/web/chart_jit.py:205:        computed_hash = compute_chart_source_hash(bars)
swing/web/chart_jit.py:207:        computed_hash = source_data_hash
swing/web/chart_jit.py:215:            source_data_hash=computed_hash,
tests/pipeline/test_briefing_advisory_compose.py:8:  - No live PriceCache → ``current_price`` sourced from
tests/pipeline/test_briefing_advisory_compose.py:13:    ``_step_charts`` already populated (``swing.data.ohlcv_archive``).
swing/web/templates/patterns/exemplars.html.j2:47:            chart_renders for this exemplar.
swing/web/charts.py:78:def compute_chart_source_hash(bars: Any) -> str:
swing/web/charts.py:79:    """Content-derived value for ``chart_renders.source_data_hash``.
swing/web/charts.py:82:    STATIC literal (``"step_charts_v1"`` / ``"chart_jit_v1"``) which does not
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:5:pipeline ladder's ``_bars_hook``.
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:7:Root cause (hypothesis (a), confirmed): when ``_install_pipeline_marketdata_caches``
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:9:``_bars_hook`` returned the freshly-fetched Schwab window VERBATIM on the
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:16:(no ladder → ``read_or_fetch_archive`` → 1260-row legacy archive → ~207 bars)
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:21:archive via ``read_or_fetch_archive``; the Schwab-success path was not. This
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:43:    """SimpleNamespace cfg shape consumed by ``_install_pipeline_marketdata_caches``
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:44:    + ``_bars_hook`` + ``OhlcvCache`` (mirrors
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:45:    tests/pipeline/test_bars_hook_requests_daily_frequency.py:_make_pipeline_cfg)."""
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:107:def test_bars_hook_schwab_success_returns_full_archive_not_short_window(
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:114:    (here injected via a stubbed ``read_or_fetch_archive``). The pipeline ladder
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:118:    Pre-fix: ``_bars_hook`` returns ``window.to_dataframe()`` (16 rows) → the
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:122:    Post-fix: ``_bars_hook`` re-reads the FULL archive after the ladder
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:126:    from swing.pipeline.runner import _install_pipeline_marketdata_caches
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:136:    def _stub_read_or_fetch_archive(ticker, *, end_date, cache_dir, archive_history_days):
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:146:        "swing.pipeline.runner.read_or_fetch_archive", _stub_read_or_fetch_archive,
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:149:        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub_read_or_fetch_archive,
tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:168:    _price, ladder_cache, audit_conn = _install_pipeline_marketdata_caches(
swing/web/app.py:31:from swing.web.price_cache import PriceCache
swing/web/app.py:317:        from swing.data.ohlcv_archive import read_or_fetch_archive
swing/web/app.py:319:        return read_or_fetch_archive(
swing/web/app.py:326:    def _bars_hook(ticker: str):
swing/web/app.py:348:    ohlcv_cache.set_ladder_bars_fetcher(_bars_hook)
swing/web/app.py:411:    app.state.price_cache = PriceCache(cfg)
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:6:--   - Theme 1 chart rendering: chart_renders cache.
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:21:--   2. CREATE TABLE chart_renders (9 cols + cross-column CHECK + 3 partial
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:170:-- 2. chart_renders (Theme 1 cache; spec §3.2)
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:176:CREATE TABLE chart_renders (
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:192:    source_data_hash TEXT NOT NULL,
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:209:CREATE UNIQUE INDEX idx_chart_renders_run_bound
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:210:    ON chart_renders(ticker, surface, pipeline_run_id)
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:214:CREATE UNIQUE INDEX idx_chart_renders_position_detail
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:215:    ON chart_renders(ticker, surface)
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:219:CREATE UNIQUE INDEX idx_chart_renders_theme2_annotated
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:220:    ON chart_renders(ticker, surface, pipeline_run_id, pattern_class)
tests/pipeline/test_bars_hook_requests_daily_frequency.py:7:T-GF2.2 (this file): the `_bars_hook` closure installed by
tests/pipeline/test_bars_hook_requests_daily_frequency.py:8:`swing/pipeline/runner.py:_install_pipeline_marketdata_caches` MUST pass
tests/pipeline/test_bars_hook_requests_daily_frequency.py:44:from swing.pipeline.runner import _install_pipeline_marketdata_caches
tests/pipeline/test_bars_hook_requests_daily_frequency.py:50:    its own connection inside _bars_hook, so we just need the path)."""
tests/pipeline/test_bars_hook_requests_daily_frequency.py:77:    """Minimal cfg shape consumed by `_install_pipeline_marketdata_caches` +
tests/pipeline/test_bars_hook_requests_daily_frequency.py:78:    `_bars_hook`. Mirrors the SimpleNamespace pattern used in
tests/pipeline/test_bars_hook_requests_daily_frequency.py:82:    ladder consult during `_install_pipeline_marketdata_caches`.
tests/pipeline/test_bars_hook_requests_daily_frequency.py:125:def test_bars_hook_invokes_ladder_with_daily_period_frequency_kwargs(
tests/pipeline/test_bars_hook_requests_daily_frequency.py:136:    Pre-fix: _bars_hook calls fetch_window_via_ladder(ticker, start=None,
tests/pipeline/test_bars_hook_requests_daily_frequency.py:142:    Post-fix: _bars_hook passes period_type='year', period=5,
tests/pipeline/test_bars_hook_requests_daily_frequency.py:157:    # _install_pipeline_marketdata_caches (closure captures the name). Patch
tests/pipeline/test_bars_hook_requests_daily_frequency.py:164:    price_cache, ohlcv_cache, _audit_conn = _install_pipeline_marketdata_caches(
tests/pipeline/test_bars_hook_requests_daily_frequency.py:171:        "_install_pipeline_marketdata_caches must install ladder bars fetcher"
tests/pipeline/test_bars_hook_requests_daily_frequency.py:174:    # Invoke the installed _bars_hook closure.
tests/pipeline/test_bars_hook_requests_daily_frequency.py:183:    # The critical invariant — _bars_hook must request DAILY bars
tests/pipeline/test_bars_hook_requests_daily_frequency.py:186:        f"_bars_hook MUST pass period_type='year' to override Schwab's "
tests/pipeline/test_bars_hook_requests_daily_frequency.py:192:        f"_bars_hook MUST pass period=5 (5 years of daily bars matching "
tests/pipeline/test_bars_hook_requests_daily_frequency.py:196:        f"_bars_hook MUST pass frequency_type='daily' to get daily bars "
tests/pipeline/test_bars_hook_requests_daily_frequency.py:200:        f"_bars_hook MUST pass frequency=1 (only valid value for "
tests/pipeline/test_bars_hook_requests_daily_frequency.py:207:def test_bars_hook_production_path_returns_daily_shaped_frame_no_duplicate_dates(
tests/pipeline/test_bars_hook_requests_daily_frequency.py:284:    price_cache, ohlcv_cache, _audit_conn = _install_pipeline_marketdata_caches(
tests/pipeline/test_bars_hook_requests_daily_frequency.py:291:    # Production-path invocation matches _step_charts at
swing/data/migrations/0011_pipeline_chart_targets_source_taxonomy.sql:7:-- After this migration, _step_charts writes 'tag_aware_top_n' for the watchlist
swing/data/migrations/0024_phase15_b7_failure_mode.sql:4:-- NON-rebuild migration (contrast 0023's chart_renders rebuild, forced by an
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:2:-- atomically rename the chart_renders.surface enum value
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:9:-- v22 chart_renders DDL; the table body + 3 partial indexes are recreated
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:16:CREATE TABLE chart_renders_new (
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:32:    source_data_hash TEXT NOT NULL,
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:47:INSERT INTO chart_renders_new (id, ticker, surface, pipeline_run_id,
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:48:        pattern_class, chart_svg_bytes, source_data_hash, rendered_at,
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:52:        pipeline_run_id, pattern_class, chart_svg_bytes, source_data_hash,
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:54:    FROM chart_renders;
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:55:DROP TABLE chart_renders;
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:56:ALTER TABLE chart_renders_new RENAME TO chart_renders;
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:57:CREATE UNIQUE INDEX idx_chart_renders_run_bound
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:58:    ON chart_renders(ticker, surface, pipeline_run_id)
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:60:CREATE UNIQUE INDEX idx_chart_renders_position_detail
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:61:    ON chart_renders(ticker, surface)
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:63:CREATE UNIQUE INDEX idx_chart_renders_theme2_annotated
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:64:    ON chart_renders(ticker, surface, pipeline_run_id, pattern_class)
swing/data/migrations/0022_phase14_temporal_log.sql:44:        REFERENCES chart_renders(id) ON DELETE SET NULL,  -- AUDIT LINKAGE to ephemeral run-scoped chart cache; NULL on render-fail or later refresh
tests/integrations/test_schwab_marketdata_ladder.py:638:    populated window; ladder MUST persist via `write_window` to
tests/integrations/test_schwab_marketdata_ladder.py:641:    Pre-fix: ladder returns the window but never calls `write_window` —
tests/integrations/test_schwab_marketdata_ladder.py:741:    before we'd attempt; defense-in-depth empty-write guard in write_window).
tests/integrations/test_schwab_marketdata_ladder.py:793:    + future ladder readers). The legacy `read_or_fetch_archive` path
swing/prices.py:10:from swing.data.ohlcv_archive import read_or_fetch_archive
swing/prices.py:56:        df = read_or_fetch_archive(
tests/integrations/test_schwab_marketdata.py:204:    Pre-fix: method did not exist; ``_bars_hook`` in
swing/patterns/labeling_bars.py:8:`read_or_fetch_archive` weekly-refresh archive): labeling windows are
tests/data/test_write_window_completed_day.py:1:# tests/data/test_write_window_completed_day.py
tests/data/test_write_window_completed_day.py:12:    write_window,
tests/data/test_write_window_completed_day.py:32:    EVERY write path (write_window, read_or_fetch_archive, _backward_compat_
tests/data/test_write_window_completed_day.py:44:    """Shape-agnostic: a DatetimeIndex frame (the read_or_fetch_archive shape)
tests/data/test_write_window_completed_day.py:60:    write_window("AAPL", win, "schwab_api", cache_dir=tmp_path)
tests/data/test_write_window_completed_day.py:70:    atomic strip DISABLED, then prove write_window cleans it.)"""
tests/data/test_write_window_completed_day.py:82:    write_window("AAPL", _frame([]), "schwab_api", cache_dir=tmp_path)
tests/data/test_write_window_completed_day.py:93:    write_window("AAPL", seed, "schwab_api", cache_dir=tmp_path)
tests/data/test_write_window_completed_day.py:94:    write_window("AAPL", None, "schwab_api", cache_dir=tmp_path)
tests/data/test_write_window_completed_day.py:118:    """Codex R1 MAJOR #2: write_window with empty incoming strips a pre-existing
tests/data/test_write_window_completed_day.py:131:    write_window("AAPL", None, "schwab_api", cache_dir=tmp_path)
swing/pipeline/runner.py:23:# Phase 13 T2.SB6c T-A.6c.2 §1.5.1 amendment — chart_renders cache
swing/pipeline/runner.py:28:from swing.data.ohlcv_archive import read_or_fetch_archive, warm_archives_batch
swing/pipeline/runner.py:31:from swing.data.repos.chart_renders import refresh_chart_render
swing/pipeline/runner.py:112:    compute_chart_source_hash,
swing/pipeline/runner.py:237:    ``_install_pipeline_marketdata_caches`` route through the T-C.1 wrappers
swing/pipeline/runner.py:312:def _install_pipeline_marketdata_caches(
swing/pipeline/runner.py:315:    """Construct PriceCache + OhlcvCache + install Schwab-market-data ladder hooks.
swing/pipeline/runner.py:327:      2. Otherwise: construct ``PriceCache(cfg)`` + ``OhlcvCache(cfg)``;
swing/pipeline/runner.py:340:    the same ``schwab_client`` is shared by both PriceCache + OhlcvCache
swing/pipeline/runner.py:348:    Consumer surfaces (post-Phase-13 T1.SB0): PriceCache is consumed by
swing/pipeline/runner.py:350:    ``_step_charts`` via ``ohlcv_cache.get_or_fetch(...)`` for chart-target
swing/pipeline/runner.py:362:    from swing.web.price_cache import PriceCache
swing/pipeline/runner.py:364:    price_cache = PriceCache(cfg)
swing/pipeline/runner.py:370:    # SYNCHRONOUSLY on this thread (PriceCache.get / OhlcvCache.get_or_fetch run
swing/pipeline/runner.py:371:    # the hook inline -- the abandonable PriceCache.get_many executor path is
swing/pipeline/runner.py:399:        # PriceCache `set_ladder_fetcher` contract: (price, provider).
swing/pipeline/runner.py:421:        # archive history via `read_or_fetch_archive` directly, NOT
swing/pipeline/runner.py:431:        bars = read_or_fetch_archive(
swing/pipeline/runner.py:439:    def _bars_hook(ticker: str):
swing/pipeline/runner.py:487:        # while the web `ticker_detail` path (no ladder → read_or_fetch_archive)
swing/pipeline/runner.py:494:        # → read_or_fetch_archive), so this unifies both provider paths.
swing/pipeline/runner.py:505:    ohlcv_cache.set_ladder_bars_fetcher(_bars_hook)
swing/pipeline/runner.py:518:    Phase 11 Sub-bundle C T-C.6 — invoke ``PriceCache.get(ticker)`` for each
swing/pipeline/runner.py:534:    Failures here MUST NOT abort the pipeline — best-effort. The PriceCache's
swing/pipeline/runner.py:542:            # PriceCache.get hits the ladder hook (which uses surface='pipeline')
swing/pipeline/runner.py:611:    # _install_pipeline_marketdata_caches when a Schwab client exists). Init to
swing/pipeline/runner.py:823:            # into `_step_charts`. When `_install_pipeline_marketdata_caches`
swing/pipeline/runner.py:825:            # is constructed here so `_step_charts` has a uniform consumer
swing/pipeline/runner.py:827:            # `read_or_fetch_archive` directly (identical shape to the
swing/pipeline/runner.py:831:            price_cache, ohlcv_cache, audit_conn = _install_pipeline_marketdata_caches(
swing/pipeline/runner.py:913:            # _step_watchlist / _step_recommendations / _step_charts.
swing/pipeline/runner.py:947:            # _step_charts. Both are failure-tolerant per plan §3.4.4.
swing/pipeline/runner.py:1014:                chart_paths = _step_charts(
swing/pipeline/runner.py:1420:    # in candidates.close. PriceCache._last_close reads that table; without
swing/pipeline/runner.py:1461:    # open-trade ticker via the (optional) installed PriceCache. When
swing/pipeline/runner.py:1556:        # Preserve the ticker's close so PriceCache._last_close returns a
swing/pipeline/runner.py:3101:    # across _step_pattern_detect / _step_charts / _step_pattern_observe, so
swing/pipeline/runner.py:3204:def _step_charts(*, cfg, lease: Lease, eval_run_id: int, data_asof: str,
swing/pipeline/runner.py:3399:    # chart_renders cache so downstream surfaces (dashboard / watchlist /
swing/pipeline/runner.py:3419:                "chart_renders write-through: ohlcv fetch failed for %s: %s",
swing/pipeline/runner.py:3424:    def _refresh_one(*, ticker: str, surface: str,
swing/pipeline/runner.py:3426:                     bytes_: bytes, source_data_hash: str) -> None:
swing/pipeline/runner.py:3433:                source_data_hash=source_data_hash,
swing/pipeline/runner.py:3452:                "chart_renders write-through: refresh failed for "
swing/pipeline/runner.py:3461:    # JIT'd at request time via swing/web/chart_jit.py:get_or_render_surface.
swing/pipeline/runner.py:3486:        _refresh_one(
swing/pipeline/runner.py:3490:            source_data_hash=compute_chart_source_hash(bars),
swing/pipeline/runner.py:3498:    # builder now JIT-fall-back to swing/web/chart_jit.py:get_or_render_surface
swing/pipeline/runner.py:3528:            _refresh_one(
swing/pipeline/runner.py:3532:                source_data_hash=compute_chart_source_hash(bars),
swing/pipeline/runner.py:3586:            _refresh_one(
swing/pipeline/runner.py:3590:                source_data_hash=compute_chart_source_hash(display_bars),
swing/pipeline/runner.py:3648:      * No live ``PriceCache``. ``current_price`` is sourced from the open-
swing/pipeline/runner.py:3650:        ``_step_evaluate`` per CLAUDE.md "PriceCache._last_close only sees
swing/pipeline/runner.py:3658:        per-ticker archive (``swing.data.ohlcv_archive``) ``_step_charts``
swing/pipeline/runner.py:3666:    correction): re-reading the archive consults ``read_or_fetch_archive``,
swing/pipeline/runner.py:3672:    ``_step_charts`` lines 728-752); the archive is freshly written within
swing/pipeline/runner.py:3683:    gap-fill yfinance call. Acceptance precedent matches ``_step_charts``
swing/pipeline/runner.py:3721:            # says "PriceCache degraded; OHLCV fetch failed; etc."). Pre-fix
swing/pipeline/runner.py:4017:    # per-ticker OHLCV archive _step_charts already populated → no new
swing/pipeline/runner.py:4206:            # read_or_fetch_archive is lease-free (touches no lease/lock), so it
swing/pipeline/runner.py:4211:                archive_df = read_or_fetch_archive(
swing/pipeline/runner.py:4221:                # downgrade a revoke to warm_raised+skip. read_or_fetch_archive is
tests/data/test_v23_migration.py:1:"""Phase 14 Sub-bundle 3 (T-3.1) v23 chart_renders.surface rename migration.
tests/data/test_v23_migration.py:36:    "source_data_hash, rendered_at, data_asof_date"
tests/data/test_v23_migration.py:48:        f"INSERT INTO chart_renders ({_CHART_RENDER_COLS}) "
tests/data/test_v23_migration.py:69:CREATE TABLE chart_renders_new (id INTEGER PRIMARY KEY);
tests/data/test_v23_migration.py:95:def _chart_renders_ddl(conn) -> str:
tests/data/test_v23_migration.py:98:            "SELECT sql FROM sqlite_schema WHERE name='chart_renders'"
tests/data/test_v23_migration.py:103:        "AND tbl_name='chart_renders' AND sql IS NOT NULL ORDER BY name"
tests/data/test_v23_migration.py:118:def test_v23_migration_renames_existing_chart_renders_rows(tmp_path):
tests/data/test_v23_migration.py:121:        "SELECT id FROM chart_renders WHERE ticker='AAPL'"
tests/data/test_v23_migration.py:126:        "SELECT surface FROM chart_renders WHERE id=?", (row_id,)
tests/data/test_v23_migration.py:134:        "SELECT id FROM chart_renders WHERE ticker='AAPL'"
tests/data/test_v23_migration.py:152:        "JOIN chart_renders cr ON cr.id = pde.chart_render_id "
tests/data/test_v23_migration.py:161:    before = _normalize(_chart_renders_ddl(conn))
tests/data/test_v23_migration.py:163:    after = _normalize(_chart_renders_ddl(conn))
tests/data/test_v23_migration.py:164:    # The post-rebuild table is RENAMEd back to chart_renders, so normalized
tests/data/test_v23_migration.py:200:    assert "chart_renders" in PHASE14_SB3_PRE_MIGRATION_EXPECTED_TABLES
tests/data/test_v23_migration.py:225:        "SELECT surface FROM chart_renders WHERE ticker='AAPL'"
tests/data/test_v23_migration.py:253:            source_data_hash="h",
swing/pipeline/ohlcv.py:3:`fetch_daily_bars` is now a thin adapter over `swing.data.ohlcv_archive.read_or_fetch_archive`
swing/pipeline/ohlcv.py:16:from swing.data.ohlcv_archive import read_or_fetch_archive
swing/pipeline/ohlcv.py:30:    Now archive-aware: consults `swing.data.ohlcv_archive.read_or_fetch_archive`
swing/pipeline/ohlcv.py:44:    df = read_or_fetch_archive(
tests/integrations/test_schwab_exit_auto_fill_e2e.py:61:from swing.web.price_cache import PriceCache
tests/integrations/test_schwab_exit_auto_fill_e2e.py:159:        PriceCache, "get_many",
tests/integrations/test_schwab_exit_auto_fill_e2e.py:162:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/integrations/test_schwab_exit_auto_fill_e2e.py:163:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/data/test_v20_migration.py:103:    "chart_renders",
tests/data/test_v20_migration.py:149:    chart_renders_cols = {
tests/data/test_v20_migration.py:151:        for r in conn.execute("PRAGMA table_info(chart_renders)").fetchall()
tests/data/test_v20_migration.py:160:        "source_data_hash",
tests/data/test_v20_migration.py:163:        assert required in chart_renders_cols, (
tests/data/test_v20_migration.py:164:            f"chart_renders missing column {required!r}"
tests/data/test_v20_migration.py:221:        "idx_chart_renders_run_bound",
tests/data/test_v20_migration.py:222:        "idx_chart_renders_position_detail",
tests/data/test_v20_migration.py:223:        "idx_chart_renders_theme2_annotated",
tests/data/test_v20_migration.py:278:    # chart_renders surface CHECK.
tests/data/test_v20_migration.py:279:    cr_sql = _table_sql(conn, "chart_renders")
tests/data/test_v20_migration.py:493:        source_data_hash="hash",
tests/data/test_v20_migration.py:506:            source_data_hash="hash",
tests/data/test_v20_migration.py:520:            source_data_hash="hash",
swing/trades/daily_management.py:493:    ``archive_df`` (the runner warms ``read_or_fetch_archive`` OUTSIDE the fence)
swing/pipeline/detection_chart_capture.py:14:from swing.data.repos.chart_renders import refresh_chart_render
swing/pipeline/detection_chart_capture.py:37:            chart_svg_bytes=svg, source_data_hash="detection_capture_v1",
tests/integrations/test_schwab_entry_auto_fill_e2e.py:40:from swing.web.price_cache import PriceCache
tests/integrations/test_schwab_entry_auto_fill_e2e.py:65:        PriceCache, "get_many",
tests/integrations/test_schwab_entry_auto_fill_e2e.py:68:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/integrations/test_schwab_entry_auto_fill_e2e.py:69:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
swing/trades/advisory.py:349:    (PriceCache degraded; OHLCV fetch failed; etc.) so DB-sourced advisories
swing/trades/advisory.py:356:    silently dropped §4.A.bis under PriceCache degradation. Per brief §0.3 #13
swing/web/price_cache.py:51:class PriceCache:
swing/web/price_cache.py:89:        by existing PriceCache tests that don't exercise the ladder).
tests/integrations/test_cli_schwab_fetch_verify_marketdata.py:9:into PriceCache/OhlcvCache, and it does NOT write to the OHLCV archive.
swing/web/ohlcv_cache.py:3:Spec §3.2, §4.3, §4.4. Mirrors PriceCache's shape for callers; internals follow
swing/web/ohlcv_cache.py:4:PriceCache's sliding-window breaker (see swing/web/price_cache.py:207-219).
swing/web/ohlcv_cache.py:28:from swing.data.ohlcv_archive import read_or_fetch_archive
swing/web/ohlcv_cache.py:126:        # Sliding window of recent outcomes (True = failure). Matches PriceCache.
swing/web/ohlcv_cache.py:180:        surface for ``_step_charts`` + downstream Theme 2 detectors (T2.SB2,
swing/web/ohlcv_cache.py:190:        legacy ``_step_charts`` line-1323 callsite contract.
swing/web/ohlcv_cache.py:199:        ``read_or_fetch_archive`` is consulted directly + sliced. Either path
swing/web/ohlcv_cache.py:200:        inherits the ``read_or_fetch_archive`` shape (capitalized OHLCV +
swing/web/ohlcv_cache.py:206:        ``_step_charts`` line 1322-1330 ``except Exception`` clause produces
swing/web/ohlcv_cache.py:284:            bars = read_or_fetch_archive(
swing/web/templates/dashboard.html.j2:6:     chart_renders bytes inline; placeholder when no recent pipeline
swing/web/templates/dashboard.html.j2:17:        populate the chart_renders cache.
swing/web/routes/watchlist.py:13:from swing.web.chart_jit import get_or_render_surface
swing/web/routes/watchlist.py:100:        return get_or_render_surface(
swing/web/routes/watchlist.py:132:    (``swing/web/chart_jit.py:get_or_render_surface``) so the collapse
tests/data/test_repo_caller_tx_contract_invariant.py:10:(pattern_exemplars + pattern_evaluations + chart_renders +
tests/data/test_repo_caller_tx_contract_invariant.py:29:    "swing/data/repos/chart_renders.py",
swing/web/routes/trades.py:2669:    shared cache. Read-only: zero domain writes (only ``read_or_fetch_archive``
swing/web/templates/partials/hypothesis_recommendations_expanded.html.j2:78:       chart_renders cache (surface='ticker_detail'). Rendered ahead of
swing/web/templates/partials/open_positions_expanded.html.j2:39:        risk/reward-zones SVG (read from the chart_renders cache, mirroring the
swing/web/routes/journal.py:86:    zero domain writes (only ``read_or_fetch_archive`` inside the renderer).
swing/web/routes/journal.py:184:    domain writes (only ``read_or_fetch_archive`` inside the renderer).
swing/web/routes/recommendations.py:239:    candidate row. RENDER-DIRECT (no chart_renders write; L5) -- fetches bars
swing/web/routes/recommendations.py:241:    NOT chart_jit.get_or_render_surface (which writes the cache). Three
swing/web/routes/dashboard.py:18:from swing.data.repos.chart_renders import refresh_chart_render
swing/web/routes/dashboard.py:65:    """Invalidate the chart_renders cache row for the benchmark ticker's
swing/web/routes/dashboard.py:165:            source_data_hash="manual_refresh",
tests/data/test_ohlcv_archive_warm.py:77:def test_read_or_fetch_archive_full_refresh_uses_shared_predicate(tmp_path, monkeypatch):
tests/data/test_ohlcv_archive_warm.py:108:    mod.read_or_fetch_archive("AAPL", end_date=FIXED, cache_dir=tmp_path, archive_history_days=1260)
tests/data/test_ohlcv_archive_warm.py:390:    """End-to-end: warm fills the gap, then read_or_fetch_archive does NO fetch."""
tests/data/test_ohlcv_archive_warm.py:411:    df = mod.read_or_fetch_archive("GAP1", end_date=FIXED, cache_dir=tmp_path,
tests/data/test_ohlcv_archive_warm.py:496:    mod.read_or_fetch_archive("DEEP", end_date=FIXED, cache_dir=serial_dir,
swing/web/routes/patterns.py:103:            from swing.data.ohlcv_archive import read_or_fetch_archive
swing/web/routes/patterns.py:106:            return read_or_fetch_archive(
tests/data/test_ohlcv_archive_trailing_nan.py:157:    out = mod.read_or_fetch_archive("AAA", end_date=FIXED, cache_dir=tmp_path,
tests/data/test_ohlcv_archive_trailing_nan.py:186:    mod.read_or_fetch_archive("AAA", end_date=FIXED, cache_dir=tmp_path,
tests/data/test_ohlcv_archive_trailing_nan.py:213:    mod.read_or_fetch_archive("AAA", end_date=FIXED, cache_dir=tmp_path,
tests/data/test_ohlcv_archive_trailing_nan.py:238:    out = mod.read_or_fetch_archive("AAA", end_date=FIXED, cache_dir=tmp_path,
tests/data/test_ohlcv_archive_trailing_nan.py:355:    mod.read_or_fetch_archive("AAA", end_date=FIXED, cache_dir=serial_dir,
tests/data/test_ohlcv_archive_shape_a.py:5:- ``write_window`` writes Schwab + yfinance parquet files independently.
tests/data/test_ohlcv_archive_shape_a.py:6:- ``write_window`` empty-window guard (no clobber on empty df) — both
tests/data/test_ohlcv_archive_shape_a.py:41:    shape consumed by ``write_window`` from
tests/data/test_ohlcv_archive_shape_a.py:73:# ---------- write_window: per-provider independence --------------------------
tests/data/test_ohlcv_archive_shape_a.py:76:def test_write_window_creates_per_provider_files_independently(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:79:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:84:    write_window("AAPL", schwab_df, "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:88:    write_window("AAPL", yf_df, "yfinance", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:100:def test_write_window_ticker_is_normalized_to_uppercase(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:103:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:105:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:114:# ---------- write_window: empty-window guard ---------------------------------
tests/data/test_ohlcv_archive_shape_a.py:117:def test_write_window_empty_dataframe_does_not_create_file(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:119:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:121:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:130:def test_write_window_none_window_does_not_create_file(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:132:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:134:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:140:def test_write_window_non_dataframe_raises_typeerror(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:152:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:154:    with pytest.raises(TypeError, match=r"write_window expects pd\.DataFrame"):
tests/data/test_ohlcv_archive_shape_a.py:155:        write_window("AAPL", "not a df", "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:161:def test_write_window_empty_does_not_clobber_existing_file(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:169:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:172:    write_window("AAPL", populated, "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:181:    write_window("AAPL", pd.DataFrame(), "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:196:# ---------- Codex R2 Major #2: write_window merge-by-asof_date semantics -----
tests/data/test_ohlcv_archive_shape_a.py:199:def test_write_window_merges_preserves_rows_outside_incoming_window(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:203:    [2026-01-01, 2026-04-10]. Call `write_window(ticker, small_window_df,
tests/data/test_ohlcv_archive_shape_a.py:213:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:224:    write_window("AAPL", existing_df, "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:236:    write_window("AAPL", small_window, "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:241:        f"Codex R2 Major #2: write_window OVERWROTE the archive — "
tests/data/test_ohlcv_archive_shape_a.py:260:def test_write_window_merge_new_wins_on_overlapping_dates(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:264:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:266:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:273:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:293:def test_write_window_merge_is_sorted_ascending_by_asof_date(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:296:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:299:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:306:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:323:def test_write_window_merge_idempotent_double_write(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:326:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:329:    write_window("AAPL", df, "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:330:    write_window("AAPL", df, "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:341:def test_write_window_empty_window_with_existing_file_still_no_op(tmp_path):
tests/data/test_ohlcv_archive_shape_a.py:346:    Pre-existing test ``test_write_window_empty_does_not_clobber_existing_file``
tests/data/test_ohlcv_archive_shape_a.py:350:    from swing.data.ohlcv_archive import write_window
tests/data/test_ohlcv_archive_shape_a.py:353:    write_window("AAPL", populated, "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:359:    write_window("AAPL", pd.DataFrame(), "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:377:    from swing.data.ohlcv_archive import resolve_ohlcv_window, write_window
tests/data/test_ohlcv_archive_shape_a.py:381:    write_window("AAPL", schwab_df, "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:382:    write_window("AAPL", yf_df, "yfinance", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:398:    from swing.data.ohlcv_archive import resolve_ohlcv_window, write_window
tests/data/test_ohlcv_archive_shape_a.py:400:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:417:    from swing.data.ohlcv_archive import resolve_ohlcv_window, write_window
tests/data/test_ohlcv_archive_shape_a.py:419:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:439:    from swing.data.ohlcv_archive import resolve_ohlcv_window, write_window
tests/data/test_ohlcv_archive_shape_a.py:441:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:447:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:488:    from swing.data.ohlcv_archive import resolve_ohlcv_window, write_window
tests/data/test_ohlcv_archive_shape_a.py:490:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:517:    **LEAVING the legacy file IN PLACE**. ``read_or_fetch_archive`` still
tests/data/test_ohlcv_archive_shape_a.py:537:        "(read_or_fetch_archive consumers) would break. Must be copy-not-move."
tests/data/test_ohlcv_archive_shape_a.py:560:    (``read_or_fetch_archive`` reads ``{TICKER}.parquet`` directly via
tests/data/test_ohlcv_archive_shape_a.py:575:    # Plant legacy DatetimeIndex file (matches what read_or_fetch_archive
tests/data/test_ohlcv_archive_shape_a.py:796:    ``write_window(ticker, empty_df, 'schwab_api')`` to "clear" Schwab —
tests/data/test_ohlcv_archive_shape_a.py:802:    from swing.data.ohlcv_archive import resolve_ohlcv_window, write_window
tests/data/test_ohlcv_archive_shape_a.py:805:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:815:    write_window("AAPL", pd.DataFrame(), "schwab_api", cache_dir=tmp_path)
tests/data/test_ohlcv_archive_shape_a.py:827:    write_window(
tests/data/test_ohlcv_archive_shape_a.py:919:    write_window). Post-fix: still short-circuit the date-column work but
tests/data/test_ohlcv_archive_shape_a.py:1236:    Under V1 copy-not-move semantics, ``read_or_fetch_archive`` is the
tests/data/test_ohlcv_archive_shape_a.py:1255:    # read_or_fetch_archive writes). Different close value to detect winner.
tests/data/test_ohlcv_archive_nonbreakage.py:6:def test_read_or_fetch_archive_signature_unchanged():
tests/data/test_ohlcv_archive_nonbreakage.py:8:    fetch-hoist must not change read_or_fetch_archive's parameters.
tests/data/test_ohlcv_archive_nonbreakage.py:14:    sig = inspect.signature(archive.read_or_fetch_archive)
tests/integrations/schwab/test_pipeline_shared_audit_conn.py:9:    """Minimal stand-in so _install_pipeline_marketdata_caches builds hooks.
tests/integrations/schwab/test_pipeline_shared_audit_conn.py:42:    price_cache, ohlcv_cache, audit_conn = runner._install_pipeline_marketdata_caches(
tests/integrations/schwab/test_pipeline_shared_audit_conn.py:56:    price_cache, ohlcv_cache, audit_conn = runner._install_pipeline_marketdata_caches(
tests/data/test_ohlcv_archive.py:1:"""Tests for swing.data.ohlcv_archive.read_or_fetch_archive.
tests/data/test_ohlcv_archive.py:72:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:119:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:161:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:189:    # Arc 6 §5: read_or_fetch_archive now routes through the staggered
tests/data/test_ohlcv_archive.py:211:    mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:253:        mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:272:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:291:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:319:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:346:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:384:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:426:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:476:    mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:510:    result = mod.read_or_fetch_archive(
tests/data/test_ohlcv_archive.py:551:    result = mod.read_or_fetch_archive(
tests/data/test_read_or_fetch_archive_completed_day.py:1:# tests/data/test_read_or_fetch_archive_completed_day.py
tests/data/test_read_or_fetch_archive_completed_day.py:7:from swing.data.ohlcv_archive import read_or_fetch_archive
tests/data/test_read_or_fetch_archive_completed_day.py:12:    barrier (Task C1) strips it before persist. OLD: read_or_fetch_archive
tests/data/test_read_or_fetch_archive_completed_day.py:14:    archive holds no row > cutoff. NOTE: read_or_fetch_archive writes the
tests/data/test_read_or_fetch_archive_completed_day.py:28:    read_or_fetch_archive("AAPL", end_date=date(2026, 6, 4),
tests/data/repos/test_chart_renders_repo.py:1:"""Phase 13 T2.SB1 T-A.1.1b — chart_renders repo CRUD discriminating tests.
tests/data/repos/test_chart_renders_repo.py:16:from swing.data.repos import chart_renders as repo
tests/data/repos/test_chart_renders_repo.py:43:        "source_data_hash": "deadbeef",
tests/data/repos/test_chart_renders_repo.py:62:        "source_data_hash FROM chart_renders WHERE id = ?",
tests/data/repos/test_chart_renders_repo.py:96:def test_list_chart_renders_paginates_correctly(
tests/data/repos/test_chart_renders_repo.py:99:    """list_chart_renders + filters + pagination work."""
tests/data/repos/test_chart_renders_repo.py:110:    all_rows = repo.list_chart_renders(conn)
tests/data/repos/test_chart_renders_repo.py:113:    watchlist_rows = repo.list_chart_renders(conn, surface="watchlist_row")
tests/data/repos/test_chart_renders_repo.py:116:    run_rows = repo.list_chart_renders(conn, pipeline_run_id=pipeline_run_id)
tests/data/repos/test_chart_renders_repo.py:119:    first_two = repo.list_chart_renders(conn, limit=2, offset=0)
tests/data/repos/test_chart_renders_repo.py:121:    next_two = repo.list_chart_renders(conn, limit=2, offset=2)
tests/data/repos/test_chart_renders_repo.py:133:    assert repo.list_chart_renders(conn, ticker="ROLLED_BACK") == []
tests/data/repos/test_chart_renders_repo.py:141:def test_chart_renders_run_bound_cache_one_row_per_ticker_surface_run(
tests/data/repos/test_chart_renders_repo.py:162:    rows = repo.list_chart_renders(
tests/data/repos/test_chart_renders_repo.py:170:def test_chart_renders_position_detail_cache_no_pipeline_run_id_unique_per_ticker(
tests/data/repos/test_chart_renders_repo.py:188:    rows = repo.list_chart_renders(
tests/data/repos/test_chart_renders_repo.py:196:def test_chart_renders_theme2_annotated_cache_unique_per_ticker_surface_run_pattern_class(
tests/data/repos/test_chart_renders_repo.py:214:    rows = repo.list_chart_renders(
tests/data/repos/test_chart_renders_repo.py:222:def test_chart_renders_session_anchor_read_write_alignment_no_false_miss(
tests/data/repos/test_chart_renders_repo.py:257:def test_chart_renders_cache_invalidation_atomic_delete_then_insert(
tests/data/repos/test_chart_renders_repo.py:283:    rows = repo.list_chart_renders(
tests/data/repos/test_chart_renders_repo.py:323:    rows = repo.list_chart_renders(conn, ticker="ROLL")
tests/data/repos/test_chart_renders_repo.py:347:                source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:364:            source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:383:            source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:410:            source_data_hash="x", rendered_at="2024-01-02T00:00:00",
tests/data/repos/test_chart_renders_repo.py:416:    rows = repo.list_chart_renders(conn, ticker="GOOD")
tests/integration/test_phase9_full_happy_path.py:41:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/integration/test_phase9_full_happy_path.py:406:    # PriceCache shims so the entry-form code path sees a live price.
tests/integration/test_phase9_full_happy_path.py:408:        PriceCache, "get_many",
tests/integration/test_phase9_full_happy_path.py:416:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/integration/test_phase9_full_happy_path.py:417:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/integration/test_phase13_t3_sb2_exit_auto_fill_e2e.py:44:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/integration/test_phase13_t3_sb2_exit_auto_fill_e2e.py:102:        PriceCache, "get_many",
tests/integration/test_phase13_t3_sb2_exit_auto_fill_e2e.py:110:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/integration/test_phase13_t3_sb2_exit_auto_fill_e2e.py:111:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/integration/test_phase8_pipeline_walkthrough.py:19:    (``swing.pipeline.runner.read_or_fetch_archive``): after the 2026-06-07
tests/integration/test_phase8_pipeline_walkthrough.py:137:      * ``_step_charts``  — mpl rendering skipped.
tests/integration/test_phase8_pipeline_walkthrough.py:200:        "swing.pipeline.runner.read_or_fetch_archive",
tests/integration/test_phase8_pipeline_walkthrough.py:207:    monkeypatch.setattr("swing.pipeline.runner._step_charts", lambda **kw: {})
tests/integration/test_phase14_temporal_log_e2e.py:67:        "SELECT length(chart_svg_bytes) FROM chart_renders WHERE id = ?",
tests/integration/test_schwab_full_happy_path.py:58:      PriceCache/OhlcvCache + does NOT write OHLCV archive — verified
tests/integration/test_phase9_end_to_end.py:569:    from swing.web.price_cache import PriceCache, PriceSnapshot
tests/integration/test_phase9_end_to_end.py:628:        PriceCache, "get_many",
tests/integration/test_phase9_end_to_end.py:636:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/integration/test_phase9_end_to_end.py:637:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/integration/test_pipeline_marketdata_ladder_integration.py:4:`_step_charts` callsites consume the cache layer's ladder integration
tests/integration/test_pipeline_marketdata_ladder_integration.py:43:    _install_pipeline_marketdata_caches,
tests/integration/test_pipeline_marketdata_ladder_integration.py:81:    PriceCache reads."""
tests/integration/test_pipeline_marketdata_ladder_integration.py:190:    price_cache, _, _audit_conn = _install_pipeline_marketdata_caches(
tests/integration/test_pipeline_marketdata_ladder_integration.py:195:    # Force market-hours so PriceCache attempts a live fetch through the
tests/integration/test_pipeline_marketdata_ladder_integration.py:245:    price_cache, _, _audit_conn = _install_pipeline_marketdata_caches(
tests/integration/test_pipeline_marketdata_ladder_integration.py:251:    # Mock the yfinance fallback at the PriceCache._fetch_live_price level
tests/integration/test_pipeline_marketdata_ladder_integration.py:285:    """PriceCache TTL provides the test-#4 hit-rate property: invoking
tests/integration/test_pipeline_marketdata_ladder_integration.py:302:    price_cache, _, _audit_conn = _install_pipeline_marketdata_caches(
tests/integration/test_pipeline_marketdata_ladder_integration.py:377:    price_cache, ohlcv_cache, audit_conn = _install_pipeline_marketdata_caches(
tests/integration/test_phase13_t3_sb1_entry_auto_fill_e2e.py:35:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/integration/test_phase13_t3_sb1_entry_auto_fill_e2e.py:54:        PriceCache, "get_many",
tests/integration/test_phase13_t3_sb1_entry_auto_fill_e2e.py:62:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/integration/test_phase13_t3_sb1_entry_auto_fill_e2e.py:63:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/integration/test_phase13_t4_sb_closer_e2e.py:10:     `watchlist_row` chart_renders cache row exists).
tests/integration/test_phase13_t4_sb_closer_e2e.py:12:     <svg ...> chart bytes when the `ticker_detail` chart_renders cache
tests/integration/test_phase13_t4_sb_closer_e2e.py:39:from swing.data.repos.chart_renders import refresh_chart_render
tests/integration/test_phase13_t4_sb_closer_e2e.py:42:from swing.web.price_cache import PriceCache, PriceSnapshot
tests/integration/test_phase13_t4_sb_closer_e2e.py:135:      - 2 chart_renders cache rows for the latest run anchor:
tests/integration/test_phase13_t4_sb_closer_e2e.py:200:                source_data_hash="h-wl",
tests/integration/test_phase13_t4_sb_closer_e2e.py:212:                source_data_hash="h-hr",
tests/integration/test_phase13_t4_sb_closer_e2e.py:225:    pattern: provide a deterministic in-memory PriceCache snapshot so
tests/integration/test_phase13_t4_sb_closer_e2e.py:233:        PriceCache, "get_many",
tests/integration/test_phase13_t4_sb_closer_e2e.py:238:    monkeypatch.setattr(PriceCache, "is_degraded", lambda self: False)
tests/integration/test_phase13_t4_sb_closer_e2e.py:239:    monkeypatch.setattr(PriceCache, "degraded_until", lambda self: None)
tests/integration/test_phase13_t4_sb_closer_e2e.py:288:            "chart_renders cache bytes"
tests/integration/test_phase13_t4_sb_closer_e2e.py:302:            "the watchlist_row chart_renders cache bytes (thumbnail "
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:4:rows + chart_renders cache + walk the operator workflow through all 5
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:32:from swing.data.repos.chart_renders import refresh_chart_render
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:49:    """Plant pipeline run + 2 pattern_evaluations + watchlist + chart_renders
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:145:                source_data_hash="h",
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:156:                source_data_hash="h",

codex
The diff adds one production behavior change in the ladder and one provenance-field change for chart renders, plus focused tests. I’m now checking the actual cache and render call paths to see whether the tested behavior matches production wiring and whether the new hash is used for reads.
exec
/bin/bash -lc "sed -n '430,520p' swing/pipeline/runner.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
        # than sufficient for any V1 chart-window requirement.
        bars = read_or_fetch_archive(
            ticker,
            end_date=last_completed_session(_dt.now()),
            cache_dir=cfg.paths.prices_cache_dir,
            archive_history_days=cfg.archive.archive_history_days,
        )
        return bars

    def _bars_hook(ticker: str):
        # OhlcvCache `set_ladder_bars_fetcher` contract:
        # (bars_df_or_none, provider). Window-ladder returns
        # (window_or_bars, provider_tag); we pass through verbatim since
        # both sides accept a pandas-DataFrame-or-None shape.
        #
        # Phase 13 T1.SB0 gate-fix (T-GF2.2): pass EXPLICIT daily-bar
        # period/frequency kwargs. Without them, schwabdev defaults to
        # periodType=day, period=10, frequencyType=minute, frequency=1 -
        # returning ~3000 1-minute intraday candles per ticker instead of
        # daily bars (Schwab API table at
        # `reference/schwabdev/api-calls.md:425-435`). The intraday output
        # contaminates the chart-step render with duplicate-DatetimeIndex
        # timestamps + per-minute volume scale + 00:00 x-axis labels - the
        # operator-witnessed S3 regression 2026-05-18 PM (CVGI). Recon at
        # `docs/phase13-t1-sb0-gate-fix-recon.md` section 2. Selected
        # daily-window tuple `(year, 5, daily, 1)` matches
        # `cfg.archive.archive_history_days` ~ 1260 trading days ~ 5y. The
        # CLI verify path at `swing/cli_schwab.py:1100-1111` uses
        # `(month, 1, daily, 1)` - both are valid daily-bar specs; the
        # year/5 choice maximizes archive coverage in one call so the
        # Shape A parquet's first write captures full history.
        # OQ-C: use the single shared serialized audit connection (closed by the
        # run finally); audit_service._AUDIT_WRITE_LOCK serializes its writes.
        window, provider_tag = fetch_window_via_ladder(
            ticker,
            start=None, end=None,
            cfg=cfg,
            schwab_client=schwab_client,
            yfinance_fallback_fn=_yf_window_fallback,
            conn=audit_conn,
            surface="pipeline",
            pipeline_run_id=pipeline_run_id,
            period_type="year",
            period=5,
            frequency_type="daily",
            frequency=1,
        )
        # Full-archive-return contract (CLAUDE.md: "cache hooks must return the
        # FULL archive; consumers slice"). Phase 16 Arc 3 fix — the symmetric
        # sibling of the Phase 13 R3 Major #1 yfinance-fallback fix.
        #
        # On the schwab_api SUCCESS path `window` is ONLY the freshly-fetched
        # Schwab sub-window. For a ticker whose Schwab listing history is short
        # (XMAX = 16 daily Schwab bars; TDAY = 138 — vs a ~1260-row legacy
        # archive), `window.to_dataframe()` returned just those bars, so
        # `OhlcvCache._fetch_bars_window` sliced an already-short frame and the
        # pipeline-rendered watchlist THUMBNAIL drew a handful of sparse points
        # while the web `ticker_detail` path (no ladder → read_or_fetch_archive)
        # stayed rich for the SAME data_asof_date. The ladder has ALREADY
        # persisted the fetched Schwab bars to the Shape-A archive (audit +
        # provenance preserved by `fetch_window_via_ladder`); here we return the
        # FULL archive — the SAME read the no-ladder/ticker_detail path uses —
        # so both surfaces converge on identical bar counts. The yfinance
        # fallback path already returns the full archive (`_yf_window_fallback`
        # → read_or_fetch_archive), so this unifies both provider paths.
        # schwab_api → re-read the full archive (above); yfinance → `window` is
        # already the full archive frame from `_yf_window_fallback`.
        bars = (
            _yf_window_fallback(ticker, None, None)
            if provider_tag == "schwab_api"
            else window
        )
        return (bars, provider_tag)

    price_cache.set_ladder_fetcher(_quote_hook)
    ohlcv_cache.set_ladder_bars_fetcher(_bars_hook)

    return price_cache, ohlcv_cache, audit_conn


def _warm_pipeline_marketdata(
    *,
    cfg,
    price_cache,
    held_tickers: list[str],
) -> None:
    """Warm the marketdata ladder for open-trade tickers under pipeline lease.

    Phase 11 Sub-bundle C T-C.6 — invoke ``PriceCache.get(ticker)`` for each
    open-trade ticker so the ladder fires + writes ``surface='pipeline'``
    audit rows under production. Sandbox short-circuit at the ladder layer

exec
/bin/bash -lc "sed -n '1,270p' swing/web/chart_jit.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
"""JIT cache-miss chart-render hook (Phase 13 T4.SB Item 5; T-T4.SB.3).

Architecture LOCK per spec §B.5:
  - ``swing/web/chart_scope.py`` LOCKED read-only (does NOT invoke JIT).
  - JIT invocation lives at route handlers / VM builders that carry the
    necessary dependency context (``conn``, ``ohlcv_cache``, surface-
    specific render kwargs).
  - Cache key shape preserved: run-bound surfaces (``watchlist_row``,
    ``ticker_detail``, ``market_weather``) write pipeline_run_id non-NULL;
    ``position_detail`` writes NULL.
  - F6 construction-barrier defense: ``ChartRender(...)`` construction
    raises on empty bytes; helper catches + returns None + WARN-logs.
  - Renderer-kwargs uniformity LOCK across callsites for cache-collision
    avoidance (per Codex R4 M#3): both ticker_detail callers (hyp-recs
    route + watchlist expanded) pass identical kwargs.

Cumulative gotchas applied:
- Expansion #10 sub-discipline (a) architecture-location: NEW module with
  explicit dependency-injection signature; chart_scope.py LOCKED read-only.
- Expansion #10 sub-discipline (c) renderer-kwargs uniformity for cache
  collision avoidance — both ticker_detail callsites pass
  ``pattern_evaluation=None``; both watchlist_row callsites pass identical
  ma_lines.
- F6 transient-empty defense at construction barrier — ``ChartRender(...)``
  raises ValueError on empty bytes; we catch and return None so the cache
  row stays preserved.
- T2.SB6a Codex R1 CRITICAL #1 semantic contract: run-bound surfaces
  require pipeline_run_id non-NULL; position_detail requires NULL. The
  ``ChartRender.__post_init__`` validator enforces this.
"""
from __future__ import annotations

import logging
import sqlite3
from collections.abc import Callable
from datetime import UTC, datetime

from swing.data.models import ChartRender
from swing.data.repos.chart_renders import (
    get_cached_chart_svg,
    refresh_chart_render,
)
from swing.web.charts import (
    compute_chart_source_hash,
    render_market_weather_svg,
    render_position_detail_svg,
    render_ticker_detail_svg,
    render_watchlist_thumbnail_svg,
)
from swing.web.ohlcv_cache import MIN_CALENDAR_DAYS_FOR_MA200

logger = logging.getLogger(__name__)


# Renderer registry — module-level so tests can mock individual surface
# renderers via ``mod._RENDERERS["ticker_detail"] = MagicMock(...)`` +
# restore via ``importlib.reload(mod)`` in finally block.
_RENDERERS: dict[str, Callable] = {
    "ticker_detail": render_ticker_detail_svg,
    "market_weather": render_market_weather_svg,
    "position_detail": render_position_detail_svg,
    "watchlist_row": render_watchlist_thumbnail_svg,
}


# Conservative default for watchlist thumbnail MA overlays (mirrors current
# ``_step_charts`` invocation pattern + spec §C.5 line 449 thumbnail design).
# Renderer-kwargs uniformity LOCK across both callsites (pipeline pre-gen +
# route JIT) per Codex R4 M#3.
_WATCHLIST_THUMBNAIL_MA_LINES: list[int] = [20, 50]


def get_or_render_surface(
    *,
    conn: sqlite3.Connection,
    ohlcv_cache,
    surface: str,
    ticker: str,
    pipeline_run_id: int | None,
    pattern_class: str | None = None,
    data_asof_date: str,
    source_data_hash: str = "chart_jit_v1",
    **renderer_kwargs,
) -> bytes | None:
    """Return cached SVG bytes if present; otherwise live-render via the
    surface's matplotlib helper, write-through cache, and return bytes.

    Returns None on render-failure / OHLCV-missing / construction-barrier
    rejection. Caller emits chart-unavailable banner per spec §B.5 OQ-5.5.
    """
    # Step 1: cache read.
    #
    # Codex R1 MAJOR #2 — F6 transient-empty defense extended to the
    # cache-HIT path. A bad legacy / future-writer-bypass row whose
    # ``chart_svg_bytes`` is zero-length BLOB (schema only enforces
    # NOT NULL, not non-empty) MUST be treated as a cache miss so the
    # helper falls through to JIT render + write-through replaces the
    # empty row. Without this guard, ``cached is not None`` succeeded
    # at the construction-barrier was bypassed — operator stays blank.
    cached = get_cached_chart_svg(
        conn,
        surface=surface,
        ticker=ticker,
        pipeline_run_id=pipeline_run_id,
        pattern_class=pattern_class,
    )
    if cached is not None and len(cached) > 0:
        return cached
    if cached is not None:
        # cached == b"" → log + fall through to render path.
        logger.warning(
            "chart_jit: cache row for %s/%s/%s has empty bytes; "
            "re-rendering",
            surface, ticker, pipeline_run_id,
        )

    # Step 2: fetch OHLCV via cache (per ``_step_charts._bars_or_none`` precedent).
    try:
        bars = ohlcv_cache.get_or_fetch(
            ticker=ticker, window_days=MIN_CALENDAR_DAYS_FOR_MA200,
        )
    except Exception as exc:  # noqa: BLE001 — log + degrade
        logger.warning(
            "chart_jit ohlcv_cache failure for %s: %s", ticker, exc,
        )
        return None
    if bars is None or len(bars) == 0:
        return None

    renderer = _RENDERERS.get(surface)
    if renderer is None:
        logger.warning("chart_jit: no renderer for surface=%s", surface)
        return None

    # Step 3: render. Renderer-kwargs match the actual signatures at
    # ``swing/web/charts.py:render_*``:
    #   - render_watchlist_thumbnail_svg(*, ticker, bars, ma_lines)
    #   - render_ticker_detail_svg(*, ticker, bars, pattern_evaluation)
    #   - render_market_weather_svg(*, bars, trend_template_state)
    #   - render_position_detail_svg(*, ticker, bars, trade, fills,
    #                                  current_stop)
    # Uniformity LOCK: for ticker_detail, both callsites pass
    # pattern_evaluation=None (V1). For watchlist_row, both callsites
    # pass the SAME ma_lines (cache-collision avoidance).
    try:
        if surface == "ticker_detail":
            svg_bytes = renderer(
                ticker=ticker, bars=bars,
                pattern_evaluation=renderer_kwargs.get("pattern_evaluation"),
            )
        elif surface == "watchlist_row":
            svg_bytes = renderer(
                ticker=ticker, bars=bars,
                ma_lines=renderer_kwargs.get(
                    "ma_lines", _WATCHLIST_THUMBNAIL_MA_LINES,
                ),
            )
        elif surface == "market_weather":
            # Phase 14 SB3 T-3.4 (§C.4a): honest dead/defensive default. No
            # production caller routes market_weather through the JIT today
            # (the two live sites -- pipeline _step_charts + the weather
            # refresh handler -- compute the real state via current_stage and
            # pass it explicitly). A future SB4 caller MUST compute + pass the
            # real trend_template_state; the default below is deliberately a
            # non-committal placeholder, not a fabricated stage label.
            svg_bytes = renderer(
                bars=bars,
                trend_template_state=renderer_kwargs.get(
                    "trend_template_state", "undefined",
                ),
            )
        elif surface == "position_detail":
            svg_bytes = renderer(
                ticker=ticker, bars=bars,
                trade=renderer_kwargs["trade"],
                fills=renderer_kwargs["fills"],
                current_stop=renderer_kwargs.get("current_stop"),
            )
        else:
            return None
    except Exception as exc:  # noqa: BLE001 — log + degrade
        logger.warning(
            "chart_jit render failure for %s/%s: %s",
            surface, ticker, exc,
        )
        return None

    # Step 4: F6 construction-barrier defense — ChartRender(...) raises
    # on empty bytes per CLAUDE.md F6 lesson; catch + WARN + return None
    # (existing cache row not blanked). Also catches semantic contract
    # violations (cache key shape per T2.SB6a Codex R1 CRITICAL #1).
    if not svg_bytes:
        logger.warning(
            "chart_jit renderer returned empty bytes for %s/%s; "
            "preserving existing cache row",
            surface, ticker,
        )
        return None

    # Phase 16 Arc 3 (3c): stamp a content-derived hash (bar count + first/last
    # asof_date) instead of the static ``source_data_hash`` default so the cache
    # row's provenance changes on data growth. Falls back to the passed literal
    # only if hashing fails (defensive — never block the write-through).
    try:
        computed_hash = compute_chart_source_hash(bars)
    except Exception:  # noqa: BLE001 — provenance is best-effort
        computed_hash = source_data_hash

    try:
        chart_render = ChartRender(
            id=None,
            ticker=ticker,
            surface=surface,
            chart_svg_bytes=svg_bytes,
            source_data_hash=computed_hash,
            rendered_at=datetime.now(UTC).strftime(
                "%Y-%m-%dT%H:%M:%SZ",
            ),
            data_asof_date=data_asof_date,
            pipeline_run_id=pipeline_run_id,
            pattern_class=pattern_class,
        )
    except ValueError as exc:
        logger.warning(
            "chart_jit construction-barrier rejected %s/%s: %s",
            surface, ticker, exc,
        )
        return None

    # Step 5: write-through cache. Repo function uses caller-tx contract
    # (no conn.commit() in repo); we wrap in a transaction so the
    # DELETE-then-INSERT atomic refresh commits.
    try:
        with conn:
            refresh_chart_render(conn, chart_render)
    except Exception as exc:  # noqa: BLE001 — degrade + still return bytes
        logger.warning(
            "chart_jit write-through failure for %s/%s: %s",
            surface, ticker, exc,
        )
    return svg_bytes

exec
/bin/bash -lc "sed -n '1,180p' swing/data/repos/chart_renders.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
"""``chart_renders`` repo — Phase 13 T2.SB1 task T-A.1.1b.

Minimum CRUD (insert / get_by_id / list) per plan §G.1 T-A.1.1b acceptance.
Caller-tx contract (NO ``conn.commit()`` in repo) + NO ``INSERT OR REPLACE``
per plan §A.15 LOCK (cache invalidation uses DELETE-then-INSERT atomic
refresh wrapped in caller's ``BEGIN IMMEDIATE`` per §C.2 / §A.15).

Cache uniqueness is enforced via 3 partial unique indexes per spec §3.2:
``idx_chart_renders_run_bound`` + ``idx_chart_renders_position_detail`` +
``idx_chart_renders_theme2_annotated`` (SQLite NULL-distinct defense per
Codex R1 M#3 + R2 M#5 closure).
"""
from __future__ import annotations

import sqlite3

from swing.data.models import ChartRender

_CHART_COLUMNS: tuple[str, ...] = (
    "id",
    "ticker",
    "surface",
    "pipeline_run_id",
    "pattern_class",
    "chart_svg_bytes",
    "source_data_hash",
    "rendered_at",
    "data_asof_date",
)

_SELECT_COLUMNS_SQL: str = ", ".join(_CHART_COLUMNS)


def _row_to_chart_render(row: tuple) -> ChartRender:
    return ChartRender(
        id=row[0],
        ticker=row[1],
        surface=row[2],
        pipeline_run_id=row[3],
        pattern_class=row[4],
        chart_svg_bytes=row[5],
        source_data_hash=row[6],
        rendered_at=row[7],
        data_asof_date=row[8],
    )


def insert_chart_render(
    conn: sqlite3.Connection, chart_render: ChartRender,
) -> int:
    """Insert one ``chart_renders`` row; return new id.

    Caller-tx contract: NO ``conn.commit()``. Partial unique indexes
    enforce one cache row per surface-class cache key; caller decides
    DELETE-then-INSERT atomic refresh per §C.2 cache invalidation pattern.
    """
    cur = conn.execute(
        """
        INSERT INTO chart_renders
            (ticker, surface, pipeline_run_id, pattern_class,
             chart_svg_bytes, source_data_hash, rendered_at, data_asof_date)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?)
        """,
        (
            chart_render.ticker,
            chart_render.surface,
            chart_render.pipeline_run_id,
            chart_render.pattern_class,
            chart_render.chart_svg_bytes,
            chart_render.source_data_hash,
            chart_render.rendered_at,
            chart_render.data_asof_date,
        ),
    )
    return int(cur.lastrowid)


def get_chart_render_by_id(
    conn: sqlite3.Connection, chart_render_id: int,
) -> ChartRender | None:
    row = conn.execute(
        f"SELECT {_SELECT_COLUMNS_SQL} FROM chart_renders WHERE id = ?",
        (chart_render_id,),
    ).fetchone()
    if row is None:
        return None
    return _row_to_chart_render(row)


def list_chart_renders(
    conn: sqlite3.Connection,
    *,
    ticker: str | None = None,
    surface: str | None = None,
    pipeline_run_id: int | None = None,
    pattern_class: str | None = None,
    limit: int | None = None,
    offset: int = 0,
) -> list[ChartRender]:
    """List chart_renders filtered by optional ticker / surface /
    pipeline_run_id / pattern_class. Ordered by (id ASC) for deterministic
    pagination.
    """
    where_clauses: list[str] = []
    params: list[object] = []
    if ticker is not None:
        where_clauses.append("ticker = ?")
        params.append(ticker)
    if surface is not None:
        where_clauses.append("surface = ?")
        params.append(surface)
    if pipeline_run_id is not None:
        where_clauses.append("pipeline_run_id = ?")
        params.append(pipeline_run_id)
    if pattern_class is not None:
        where_clauses.append("pattern_class = ?")
        params.append(pattern_class)

    where_sql = ""
    if where_clauses:
        where_sql = " WHERE " + " AND ".join(where_clauses)

    limit_sql = ""
    if limit is not None:
        limit_sql = " LIMIT ? OFFSET ?"
        params.extend([limit, offset])

    rows = conn.execute(
        f"SELECT {_SELECT_COLUMNS_SQL} FROM chart_renders"
        f"{where_sql} ORDER BY id ASC{limit_sql}",
        tuple(params),
    ).fetchall()
    return [_row_to_chart_render(r) for r in rows]


# ---------------------------------------------------------------------------
# Phase 13 T2.SB6 T-A.6.2 — cache-consumer helpers (spec §C.2 LOCK).
# ---------------------------------------------------------------------------


def get_cached_chart_svg(
    conn: sqlite3.Connection,
    *,
    ticker: str,
    surface: str,
    pipeline_run_id: int | None = None,
    pattern_class: str | None = None,
    min_data_asof_date: str | None = None,
) -> bytes | None:
    """Return the cached SVG bytes for the canonical chart cache key, or None.

    Per spec §C.2 cache key shape LOCK:
      - Run-bound surfaces (``watchlist_row``, ``ticker_detail``,
        ``market_weather``): key on ``(ticker, surface, pipeline_run_id)``.
      - ``position_detail`` surface: key on ``(ticker, surface)`` with
        ``pipeline_run_id IS NULL``.
      - ``theme2_annotated`` surface: key on
        ``(ticker, surface, pipeline_run_id, pattern_class)``.

    Per spec §A.13 session-anchor read/write predicate alignment LOCK:
    ``min_data_asof_date`` is the staleness predicate; pass
    ``last_completed_session(now()).isoformat()`` (backward-looking; same
    function the writer uses) so the read returns rows whose
    ``data_asof_date >= min_data_asof_date``. If ``min_data_asof_date`` is
    None, no staleness filter applies (return the most-recent row
    regardless of asof). Discriminating round-trip test pattern lives at
    ``tests/data/test_chart_renders_repo.py`` (Phase 8 ``cfacbc5``
    precedent).
    """
    where_clauses = ["ticker = ?", "surface = ?"]
    params: list[object] = [ticker, surface]

    if pipeline_run_id is None:
        where_clauses.append("pipeline_run_id IS NULL")
    else:
        where_clauses.append("pipeline_run_id = ?")
        params.append(pipeline_run_id)

    if pattern_class is None:
        where_clauses.append("pattern_class IS NULL")

exec
/bin/bash -lc "sed -n '3360,3605p' swing/pipeline/runner.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
            classifier_errors += 1
            classification = FlagClassificationResult(
                detected=False, confidence=0.0, pattern=None,
                pole_start_date=None, pole_end_date=None,
                flag_start_date=None, flag_end_date=None,
                pole_high=None, flag_low=None, pivot=None,
                components={"error": repr(exc)},
            )

        pattern_overlay = PatternOverlay.from_classification(classification)

        path = render_chart(
            ticker=ticker, ohlcv=ohlcv, pivot=pivot, stop=stop,
            output_path=staging.path / f"{ticker}.png",
            pattern_overlay=pattern_overlay,
        )
        if path is not None:
            out_paths[ticker] = path
            chart_status = "ok"
        else:
            # render_chart returns None when the (post-tail) frame has fewer
            # than MIN_BARS rows — the spec §8 deferred "too_few_bars" case.
            chart_status = "too_few_bars"

        # Single fenced write per ticker — chart_status update + classification
        # row commit together so a partial-failure leaves a structurally
        # consistent state.
        with lease.fenced_write() as conn:
            update_chart_target_status(
                conn, pipeline_run_id=lease.run_id, ticker=ticker,
                chart_status=chart_status,
            )
            insert_classification(
                conn, pipeline_run_id=lease.run_id, ticker=ticker,
                result=classification,
                computed_at=_dt.now().isoformat(timespec="seconds"),
            )

    # Phase 13 T2.SB6c T-A.6c.2 §1.5.1 amendment — write-through to the
    # chart_renders cache so downstream surfaces (dashboard / watchlist /
    # trade detail / hyp-rec expansion) can serve charts inline without
    # re-rendering at request time. 4 surfaces:
    #   - watchlist_row: per active watchlist ticker (pipeline_run_id non-NULL)
    #   - ticker_detail: per A+ candidate (pipeline_run_id non-NULL)
    #   - position_detail: per open trade (pipeline_run_id IS NULL per v20 §3.2)
    #   - market_weather: cfg.rs.benchmark_ticker (pipeline_run_id non-NULL,
    #     per Codex R2 MAJOR #4 closure)
    # F6 transient-empty defense lives at the ChartRender __post_init__
    # construction barrier (T2.SB6a R1 MAJOR #2 LOCK): a renderer returning
    # b"" raises ValueError which we catch + WARN-log + continue.
    _now_iso = _dt.now().isoformat(timespec="seconds")

    def _bars_or_none(ticker: str):
        try:
            return ohlcv_cache.get_or_fetch(
                ticker=ticker, window_days=MIN_CALENDAR_DAYS_FOR_MA200,
            )
        except Exception as exc:  # noqa: BLE001 - per-ticker isolation
            log.warning(
                "chart_renders write-through: ohlcv fetch failed for %s: %s",
                ticker, exc,
            )
            return None

    def _refresh_one(*, ticker: str, surface: str,
                     pipeline_run_id: int | None, pattern_class: str | None,
                     bytes_: bytes, source_data_hash: str) -> None:
        try:
            chart_render = ChartRender(
                id=None,
                ticker=ticker,
                surface=surface,
                chart_svg_bytes=bytes_,
                source_data_hash=source_data_hash,
                rendered_at=_now_iso,
                data_asof_date=data_asof,
                pipeline_run_id=pipeline_run_id,
                pattern_class=pattern_class,
            )
        except ValueError as exc:
            log.warning(
                "F6 transient empty chart skipped: ticker=%s surface=%s err=%s",
                ticker, surface, exc,
            )
            return
        try:
            with lease.fenced_write() as conn:
                refresh_chart_render(conn, chart_render)
        except LeaseRevokedError:
            raise
        except Exception as exc:  # noqa: BLE001 - per-surface isolation
            log.warning(
                "chart_renders write-through: refresh failed for "
                "ticker=%s surface=%s: %s",
                ticker, surface, exc,
            )

    # watchlist_row surface — per data-eligible dashboard-top-5 watchlist
    # ticker. Phase 13 T-T4.SB.3 (OQ-5.3 LOCK): pre-gen scope reduced
    # from top-N (`chart_top_n_watch` = 10) to dashboard-top-5
    # visible-by-default. Out-of-scope (positions 6-10) thumbnails are
    # JIT'd at request time via swing/web/chart_jit.py:get_or_render_surface.
    # The dashboard renders only the top-5 by default — pre-genning the
    # full 10 burned ~50% of chart-step wall time on rows operators
    # rarely opened.
    #
    # Renderer-kwargs uniformity LOCK: ma_lines=[20, 50] matches the
    # JIT helper's default (per Codex R4 M#3 cache-collision avoidance).
    # NOTE: this changes the kwargs from the prior [50, 150, 200] —
    # operator-facing change accepted per plan §B.3 Sub-task 3E.
    _pregen_watchlist_top_n = 5
    for w in tag_aware_top_n[:_pregen_watchlist_top_n]:
        ticker = w.ticker.upper()
        bars = _bars_or_none(ticker)
        if bars is None or bars.empty:
            continue
        try:
            svg_bytes = render_watchlist_thumbnail_svg(
                ticker=ticker, bars=bars, ma_lines=[20, 50],
            )
        except Exception as exc:  # noqa: BLE001 - per-ticker isolation
            log.warning(
                "render_watchlist_thumbnail_svg failed for %s: %s",
                ticker, exc,
            )
            continue
        _refresh_one(
            ticker=ticker, surface="watchlist_row",
            pipeline_run_id=lease.run_id, pattern_class=None,
            bytes_=svg_bytes,
            source_data_hash=compute_chart_source_hash(bars),
        )

    # ticker_detail surface — Phase 13 T-T4.SB.3 (OQ-5.3 LOCK): pre-gen
    # DROPPED from chart-step. The hyp-recs expanded surface is operator-
    # opened on-demand from the dashboard hyp-recs card; pre-genning all
    # A+ tickers wastes ~one render per ticker, most never opened. The
    # /hyp-recs/{ticker}/expand route + build_hyp_recs_expanded VM
    # builder now JIT-fall-back to swing/web/chart_jit.py:get_or_render_surface
    # on cache miss + write-through.

    # position_detail surface — per open trade. Run-agnostic key shape:
    # pipeline_run_id IS NULL per v20 §3.2 LOCK so the dashboard reader
    # (which doesn't anchor on a specific run for open positions) finds
    # the row.
    if open_trades:
        from swing.data.repos.fills import list_fills_for_trade
        for tr in open_trades:
            ticker = tr.ticker.upper()
            bars = _bars_or_none(ticker)
            if bars is None or bars.empty:
                continue
            _conn = connect(cfg.paths.db_path)
            try:
                trade_fills = list_fills_for_trade(_conn, tr.id)
            finally:
                _conn.close()
            try:
                svg_bytes = render_position_detail_svg(
                    ticker=ticker, bars=bars, trade=tr,
                    fills=trade_fills, current_stop=tr.current_stop,
                )
            except Exception as exc:  # noqa: BLE001 - per-ticker isolation
                log.warning(
                    "render_position_detail_svg failed for %s: %s",
                    ticker, exc,
                )
                continue
            _refresh_one(
                ticker=ticker, surface="position_detail",
                pipeline_run_id=None, pattern_class=None,
                bytes_=svg_bytes,
                source_data_hash=compute_chart_source_hash(bars),
            )

    # market_weather surface — cfg.rs.benchmark_ticker per Codex R2 MAJOR #4
    # closure. Dashboard reader at T2.SB6b reads via the SAME ticker;
    # divergence here silently invisibles the chart.
    #
    # F-2 (Phase 14 close-out follow-on): compute the trend state LIVE from a
    # wide (>= MIN_CALENDAR_DAYS_FOR_TREND_TEMPLATE) benchmark fetch via
    # structural_stage. current_stage read PERSISTED candidate_criteria for the
    # benchmark, which is NOT in the evaluated set -> always 'undefined'. Display
    # the narrower MIN_CALENDAR_DAYS_FOR_MA200 window (legibility unchanged);
    # the wider compute window is sliced down (anchored on the frame's own last
    # bar, cache-lag-safe).
    benchmark_ticker = cfg.rs.benchmark_ticker.upper()
    try:
        compute_bars = ohlcv_cache.get_or_fetch(
            ticker=benchmark_ticker,
            window_days=MIN_CALENDAR_DAYS_FOR_TREND_TEMPLATE,
        )
    except Exception as exc:  # noqa: BLE001 - per-ticker isolation
        log.warning(
            "market_weather benchmark fetch failed for %s: %s",
            benchmark_ticker, exc,
        )
        compute_bars = None
    if compute_bars is not None and not compute_bars.empty:
        # Fail-soft: a structural_stage error must NEVER abort the charts step.
        try:
            closes = compute_bars["Close"]
            if getattr(closes, "ndim", 1) == 2:
                closes = closes.iloc[:, 0]
            weather_state = structural_stage(
                closes, rising_period=cfg.trend_template.rising_ma_period_days,
            )
        except Exception as exc:  # noqa: BLE001 - fail-soft, never abort step
            log.warning(
                "market_weather structural_stage failed for %s: %s",
                benchmark_ticker, exc,
            )
            weather_state = "undefined"
        display_bars = slice_recent_calendar_days(
            compute_bars, window_days=MIN_CALENDAR_DAYS_FOR_MA200,
        )
        try:
            svg_bytes = render_market_weather_svg(
                bars=display_bars, trend_template_state=weather_state,
            )
        except Exception as exc:  # noqa: BLE001 - per-ticker isolation
            log.warning(
                "render_market_weather_svg failed for %s: %s",
                benchmark_ticker, exc,
            )
        else:
            _refresh_one(
                ticker=benchmark_ticker, surface="market_weather",
                pipeline_run_id=lease.run_id, pattern_class=None,
                bytes_=svg_bytes,
                source_data_hash=compute_chart_source_hash(display_bars),
            )

    # End-of-step summary. Denominator is classifier attempts (success +
    # error), NOT len(targets) — fetcher_failed tickers never reached the
    # classifier and are not in the classifier-health denominator. Spec §3.3
    # contract: `flag_classifier: {ok}/{total} ok, {errors} errors` where
    # totals are classifier-attempt-scoped (Codex R1 Major 1 fix).
    classifier_attempts = classifier_success + classifier_errors
    log.info(
        f"flag_classifier: {classifier_success}/{classifier_attempts} ok, "
        f"{classifier_errors} errors"
    )
    # Spec §A "Timer-boundary specification": timer ends after the last
    # per-ticker fenced_write for chart_status. promote_staging runs
    # OUTSIDE the timer because it's a separate concern (artifact-promote,

exec
/bin/bash -lc "sed -n '180,380p' swing/data/repos/chart_renders.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
        where_clauses.append("pattern_class IS NULL")
    else:
        where_clauses.append("pattern_class = ?")
        params.append(pattern_class)

    if min_data_asof_date is not None:
        where_clauses.append("data_asof_date >= ?")
        params.append(min_data_asof_date)

    sql = (
        "SELECT chart_svg_bytes FROM chart_renders WHERE "
        + " AND ".join(where_clauses)
        + " ORDER BY id DESC LIMIT 1"
    )
    row = conn.execute(sql, tuple(params)).fetchone()
    if row is None:
        return None
    return bytes(row[0]) if row[0] is not None else None


def refresh_chart_render(
    conn: sqlite3.Connection, chart_render: ChartRender,
) -> int:
    """DELETE-then-INSERT atomic refresh of one cache row.

    Per §A.15 LOCK (no ``INSERT OR REPLACE`` on audit-trail tables) +
    §C.2 cache invalidation pattern (atomic DELETE-then-INSERT wrapped in
    caller's ``BEGIN IMMEDIATE``).

    Caller-tx contract: the function does NOT call ``conn.commit()``; the
    caller is responsible for transaction discipline (typically
    ``BEGIN IMMEDIATE`` + ``COMMIT`` / ``ROLLBACK``).

    The DELETE key shape mirrors the partial unique indexes:
      - ``theme2_annotated``:
        ``(ticker, surface, pipeline_run_id, pattern_class)``.
      - run-bound non-theme2: ``(ticker, surface, pipeline_run_id)`` with
        ``pipeline_run_id IS NOT NULL`` predicate.
      - ``position_detail``: ``(ticker, surface)`` with
        ``pipeline_run_id IS NULL`` predicate.
    """
    if chart_render.surface == "theme2_annotated":
        conn.execute(
            "DELETE FROM chart_renders WHERE ticker = ? AND surface = ? "
            "AND pipeline_run_id = ? AND pattern_class = ?",
            (
                chart_render.ticker,
                chart_render.surface,
                chart_render.pipeline_run_id,
                chart_render.pattern_class,
            ),
        )
    elif chart_render.surface == "position_detail":
        conn.execute(
            "DELETE FROM chart_renders WHERE ticker = ? AND surface = ? "
            "AND pipeline_run_id IS NULL",
            (chart_render.ticker, chart_render.surface),
        )
    else:
        # Run-bound: watchlist_row / ticker_detail / market_weather.
        conn.execute(
            "DELETE FROM chart_renders WHERE ticker = ? AND surface = ? "
            "AND pipeline_run_id = ?",
            (
                chart_render.ticker,
                chart_render.surface,
                chart_render.pipeline_run_id,
            ),
        )
    return insert_chart_render(conn, chart_render)

exec
/bin/bash -lc "sed -n '1,260p' swing/web/ohlcv_cache.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
"""OhlcvCache — TTL-cached daily-bar bundles with sliding-window circuit breaker.

Spec §3.2, §4.3, §4.4. Mirrors PriceCache's shape for callers; internals follow
PriceCache's sliding-window breaker (see swing/web/price_cache.py:207-219).

Key semantics:
- Keyed by uppercase ticker (normalization at cache boundary; DB stores upper).
- TTL from cfg.web.ohlcv_cache_ttl_seconds (default 3600s).
- Bundle fields default None → SMA rules silently no-op per spec §6.
- get_many_bundles records one sliding-window outcome per requested ticker
  (success if bundle produced, failure if deadline miss OR fetch raised).
- Semaphore-bounded executor submissions (cfg.web.max_concurrent_ohlcv_fetches).
"""
from __future__ import annotations

import collections
import logging
import threading
import time
from collections.abc import Callable, Sequence
from concurrent.futures import Executor, Future, wait
from dataclasses import dataclass
from datetime import datetime as _dt
from datetime import timedelta
from typing import TYPE_CHECKING, Any

from swing.config import Config
from swing.data.ohlcv_archive import read_or_fetch_archive
from swing.evaluation.dates import last_completed_session
from swing.pipeline import ohlcv as ohlcv_mod

if TYPE_CHECKING:
    import pandas as pd

log = logging.getLogger(__name__)

# Phase 14 close-out (A-1): a 200-trading-bar SMA200 needs ~200 * 365/252 ~=
# 290 calendar days of lookback; use 300 with margin. CALENDAR-day window
# (get_or_fetch's window_days is a calendar lookback). Shared by the pipeline
# benchmark/chart fetch + the dashboard weather refresh + the JIT path so the
# 200-MA on any MA200-bearing surface has enough bars. Monotonic-safe (more
# bars only; consumers slice).
MIN_CALENDAR_DAYS_FOR_MA200 = 300

# Phase 14 close-out follow-on (F-2): the market-weather trend classification
# needs the structural Trend-Template checks INCLUDING TT3 (200MA rising over
# rising_ma_period_days=21 bars), i.e. >= 200 + 21 = 221 trading bars. 221 bars
# ~= 221 * 365/252 ~= 320 calendar days; add margin -> 390 (~= 250-260 trading
# bars). This is the COMPUTE window (decoupled from the 300-day DISPLAY/MA200
# window). Monotonic-safe (more bars only; consumers slice).
MIN_CALENDAR_DAYS_FOR_TREND_TEMPLATE = 390


def slice_recent_calendar_days(bars: pd.DataFrame, *, window_days: int) -> pd.DataFrame:
    """Slice a fetched bar frame to its most recent ``window_days`` calendar
    days so a wider compute fetch can be displayed at the narrower (prior)
    window without a second fetch.

    Codex R1 Major #4: anchor ``end`` on the FRAME'S OWN last bar
    (``bars.index[-1].date()``), NOT last_completed_session(now()). Re-slicing
    a cached/lagging 390-day frame against a fresh now() could drop the frame's
    last available bar (or empty the display) on a stale archive. Anchoring on
    the frame's last bar yields exactly the last ``window_days`` of the
    AVAILABLE frame, robust to cache lag. Returns the input unchanged if empty.
    """
    if bars is None or bars.empty:
        return bars
    end = bars.index[-1].date()
    cutoff = end - timedelta(days=window_days)
    return bars.loc[bars.index.date >= cutoff]


@dataclass(frozen=True)
class OhlcvBundle:
    """SMA10/20/50 + previous close + ADR% from a single daily-bar fetch.
    All fields are None if the fetch failed or the bar history was
    insufficient for the given period. `fetched_at` is a monotonic
    timestamp (time.monotonic()).

    3e.8 Bundle 2 — ``adr_pct`` added for §4.D parabolic-trim advisory.
    Default None so any code that constructs OhlcvBundle without supplying
    it (e.g., older test fixtures, hand-built bundles) continues to work
    with the rule silently no-opping.

    Schwab Sub-bundle C T-C.4 — ``provider`` provenance tag added (mirrors
    ``PriceSnapshot.provider`` shape). None (legacy / not set) | 'schwab_api'
    (ladder returned Schwab-success path) | 'yfinance' (ladder yfinance
    fallback path OR pre-ladder direct yfinance fetch). DISTINCT from any
    TTL-state / freshness field; documents data origin only.
    """
    sma10: float | None
    sma20: float | None
    sma50: float | None
    previous_close: float | None
    fetched_at: float
    adr_pct: float | None = None
    provider: str | None = None

    def __post_init__(self) -> None:
        if self.provider is not None:
            if not isinstance(self.provider, str):
                raise TypeError(
                    "OhlcvBundle.provider must be str or None; got "
                    f"{type(self.provider).__name__}"
                )
            if self.provider not in ("schwab_api", "yfinance"):
                raise ValueError(
                    "OhlcvBundle.provider must be one of None | "
                    f"'schwab_api' | 'yfinance'; got {self.provider!r}"
                )

    @classmethod
    def empty(cls, fetched_at: float) -> OhlcvBundle:
        return cls(None, None, None, None, fetched_at, None, None)


class OhlcvCache:
    """TTL-cached OhlcvBundle store + circuit breaker. Thread-safe. Spec §3.2."""

    def __init__(self, cfg: Config):
        self._cfg = cfg
        self._ttl = cfg.web.ohlcv_cache_ttl_seconds
        self._sema = threading.Semaphore(cfg.web.max_concurrent_ohlcv_fetches)
        self._lock = threading.Lock()
        self._store: dict[str, tuple[OhlcvBundle, float]] = {}
        # Sliding window of recent outcomes (True = failure). Matches PriceCache.
        self._failure_window: collections.deque[bool] = collections.deque(maxlen=20)
        self._degraded_until: float | None = None
        # Schwab Sub-bundle C T-C.4: optional ladder-aware bars fetcher. When
        # None (default), the worker uses the legacy `ohlcv_mod.fetch_daily_bars`
        # path (yfinance via archive). When set, the worker invokes the
        # callable which MUST return `(bars_df_or_none, provider_tag)` where
        # `provider_tag` is 'schwab_api' or 'yfinance'. The provider tag is
        # stamped onto the resulting OhlcvBundle. Sandbox short-circuit + auth
        # fall-through live INSIDE the ladder; cache stays env-agnostic.
        self._ladder_bars_fetcher: (
            Callable[[str], tuple[Any, str]] | None
        ) = None
        # Phase 13 T1.SB0 (plan §G.0): separate store for the DataFrame-returning
        # `get_or_fetch` surface. Keyed by `(ticker_upper, window_days)`; values
        # are `(DataFrame, fetched_at_monotonic)`. Separate lock from `self._lock`
        # so chart-bars traffic does not contend with dashboard bundle traffic
        # (recon §4.B).
        self._bars_lock = threading.Lock()
        self._bars_store: dict[tuple[str, int], tuple[Any, float]] = {}
        # Pool-widening (2026-06-04): truthful fetch-vs-hit telemetry at the
        # real fetch boundary (Codex R1 MAJOR #5 -- candidate membership is a
        # PROXY, not the truth). ``fetch_window`` counts get_or_fetch calls that
        # consulted the archive/network (the binding observe-load cost across
        # the ~90-session window, since each fresh nightly process starts cold);
        # ``in_memory_hit`` counts TTL cache hits. drain_telemetry() returns +
        # zeroes the counts at the observe-step boundary.
        self._telemetry_lock = threading.Lock()
        self._telemetry = {"in_memory_hit": 0, "fetch_window": 0}

    def set_ladder_bars_fetcher(
        self,
        fetcher: Callable[[str], tuple[Any, str]] | None,
    ) -> None:
        """Install (or clear) a ladder-aware bars-fetch callable.

        ``fetcher`` must accept the ticker symbol and return
        ``(bars_df_or_none, provider: str)`` where ``provider`` is
        ``'schwab_api'`` or ``'yfinance'``. When set, the cache's worker
        invokes this callable instead of ``ohlcv_mod.fetch_daily_bars``;
        the returned ``bars`` are fed through the same SMA / ADR
        computation pipeline and the provider tag is stamped onto the
        resulting ``OhlcvBundle``.

        Passing ``None`` reverts to the legacy yfinance-only worker path
        (used by existing OhlcvCache tests that don't exercise the ladder).
        """
        self._ladder_bars_fetcher = fetcher

    def get_or_fetch(self, *, ticker: str, window_days: int = 180) -> pd.DataFrame:
        """Return daily bars for ``ticker`` over a calendar-day lookback window.

        Phase 13 T1.SB0 (plan §G.0): closes the Phase 11 Sub-bundle C R1 M#5
        ACCEPT-WITH-RATIONALE V1 deferral by exposing a DataFrame-returning
        surface for ``_step_charts`` + downstream Theme 2 detectors (T2.SB2,
        T2.SB3) + T3.SB3 review auto-fill.

        Shape contract: ``pd.DataFrame`` indexed by date (``DatetimeIndex``) with
        capitalized columns ``Open / High / Low / Close / Volume``. Matches
        ``PriceFetcher.get``'s shape (recon §3 parity table).

        Window semantics: ``window_days`` is a calendar-day lookback ending at
        ``last_completed_session(now())`` — backward-looking, NOT forward-looking
        (CLAUDE.md session-anchor read/write mismatch family). Matches the
        legacy ``_step_charts`` line-1323 callsite contract.

        Caching: TTL-keyed by ``(ticker.upper(), window_days)`` in
        ``self._bars_store`` (separate from the bundle ``self._store`` so
        dashboard bundle traffic + chart bars traffic do not contend on lock
        acquisition). TTL inherited from ``cfg.web.ohlcv_cache_ttl_seconds``.

        Routing: when ``set_ladder_bars_fetcher`` is installed, the ladder hook
        is invoked + its DataFrame sliced to the calendar-day window. Otherwise
        ``read_or_fetch_archive`` is consulted directly + sliced. Either path
        inherits the ``read_or_fetch_archive`` shape (capitalized OHLCV +
        DatetimeIndex). Sandbox short-circuit lives inside the ladder; this
        method is env-agnostic.

        Empty-result semantics: raises ``ValueError("No data for {ticker}")`` —
        matches ``PriceFetcher.get``'s raise-on-empty contract so the
        ``_step_charts`` line 1322-1330 ``except Exception`` clause produces
        ``chart_status='fetcher_failed'`` unchanged.

        Concurrency: synchronous + thread-safe under ``self._bars_lock``. The
        lock is NOT held during the fetch call (prevents serialization through
        the lock + I/O). Two threads racing on the same ``(ticker, window_days)``
        each fetch independently + each write; last-writer-wins. Both fetches
        see the same archive-source so values are identical; no data corruption.
        Per-key in-flight dedup is a V2 candidate (recon §4.B).
        """
        ticker_upper = ticker.upper()
        key = (ticker_upper, int(window_days))
        now = time.monotonic()
        with self._bars_lock:
            hit = self._bars_store.get(key)
            if hit is not None and (now - hit[1]) <= self._ttl:
                # Defensive copy on read (Codex R3 Minor #1 fix 2026-05-18):
                # ``get_or_fetch`` is substrate for downstream detector +
                # auto-fill consumers. Returning the stored frame by
                # reference would let one consumer mutating columns/rows
                # corrupt the cache value observed by later consumers
                # within the TTL window. Copy-on-read is the cheapest
                # safety net; copy-on-write at store-time is also defended
                # below for the same reason.
                with self._telemetry_lock:
                    self._telemetry["in_memory_hit"] += 1
                return hit[0].copy()
        with self._telemetry_lock:
            self._telemetry["fetch_window"] += 1
        bars = self._fetch_bars_window(
            ticker=ticker_upper, window_days=int(window_days),
        )
        if bars is None or bars.empty:
            raise ValueError(f"No data for {ticker_upper}")
        with self._bars_lock:
            # Store a copy so a caller mutating their returned frame after
            # this call cannot reach back into the cached value.
            self._bars_store[key] = (bars.copy(), time.monotonic())
        return bars

    def drain_telemetry(self) -> dict[str, int]:
        """Return + zero the fetch-vs-hit counters since the last drain."""
        with self._telemetry_lock:
            out = dict(self._telemetry)
            self._telemetry = {"in_memory_hit": 0, "fetch_window": 0}
            return out

    def _fetch_bars_window(
        self, *, ticker: str, window_days: int,
    ) -> pd.DataFrame | None:
        """Internal: fetch a calendar-day window of daily bars; returns
        ``DataFrame`` or ``None``. Caller maps ``None → ValueError`` so the
        public ``get_or_fetch`` matches ``PriceFetcher.get``'s raise-on-empty
        contract.


exec
/bin/bash -lc "sed -n '1,260p' swing/integrations/schwab/marketdata_ladder.py" in /mnt/c/Users/rwsmy/swing-arc3
exec
/bin/bash -lc "sed -n '600,660p' swing/integrations/schwab/models.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
            if prior is not None and bar.asof_date < prior:
                raise ValueError(
                    f"SchwabPriceHistoryWindow.bars must be sorted by "
                    f"asof_date ascending; saw {prior!r} before "
                    f"{bar.asof_date!r}"
                )
            prior = bar.asof_date

    def to_dataframe(self):
        """Convert bars to a DataFrame matching the legacy yfinance in-memory
        shape consumed by the OHLCV cache + chart-step downstream code.

        Returns a DataFrame with:
          - DatetimeIndex (named ``Date``) parsed from ``OhlcvBar.asof_date``;
          - CAPITALIZED OHLCV columns (``Open``/``High``/``Low``/``Close``/
            ``Volume``) matching what ``swing/pipeline/ohlcv.py:compute_smas``
            and ``swing/web/ohlcv_cache.py`` consume from the legacy
            ``_yf_download_window`` shape.

        This is the in-memory shape, NOT the Shape A on-disk shape. Use
        ``swing.integrations.schwab.marketdata_ladder._schwab_window_to_shape_a_df``
        (or the inline equivalent in the ladder) when persisting via
        ``swing.data.ohlcv_archive.write_window``.

        Codex R1 Major #4: previously the OhlcvCache ladder hook called
        ``window.to_dataframe()`` but this method did not exist, breaking
        the Schwab success path in ``swing/pipeline/runner.py:_bars_hook``.
        Empty bars list → empty DataFrame with the canonical column set
        (defensive; mapper raises before this for empty Schwab responses).
        """
        import pandas as pd  # lazy: keep models.py lightweight

        if not self.bars:
            return pd.DataFrame(
                columns=["Open", "High", "Low", "Close", "Volume"],
            )

        idx = pd.to_datetime([bar.asof_date for bar in self.bars])
        idx.name = "Date"
        return pd.DataFrame(
            {
                "Open": [bar.open for bar in self.bars],
                "High": [bar.high for bar in self.bars],
                "Low": [bar.low for bar in self.bars],
                "Close": [bar.close for bar in self.bars],
                "Volume": [bar.volume for bar in self.bars],
            },
            index=idx,
        )


__all__ = [
    "OhlcvBar",
    "SchwabAccountResponse",
    "SchwabExecutionLeg",
    "SchwabOrderResponse",
    "SchwabPriceHistoryWindow",
    "SchwabQuoteResponse",
    "SchwabTransactionResponse",
    "_SCHWAB_ORDER_INSTRUCTIONS",
    "_SCHWAB_ORDER_STATUSES",

 succeeded in 0ms:
"""Schwab market-data ladder fetcher (Phase 11 Sub-bundle C T-C.3).

Two ladder functions, one per call-shape:

  - `fetch_quote_via_ladder(ticker, *, cfg, schwab_client,
    yfinance_fallback_fn, conn, surface, pipeline_run_id=None)`
    → `tuple[PriceSnapshot, str]`
  - `fetch_window_via_ladder(ticker, *, start, end, cfg, schwab_client,
    yfinance_fallback_fn, conn, surface, pipeline_run_id=None)`
    → `tuple[SchwabPriceHistoryWindow, str]`

The second tuple element is the provenance tag: 'schwab_api' on a Schwab
success path, 'yfinance' on a yfinance fallback path.

Per plan §H.6.1 + §H.6.2 + dispatch brief T-C.3:

  1. **Sandbox / disabled short-circuit:** if
     ``cfg.integrations.schwab.environment != 'production'`` OR
     ``not cfg.integrations.schwab.marketdata_ladder_enabled`` →
     invoke ``yfinance_fallback_fn`` directly; return with provider tag
     ``'yfinance'``. NO schwabdev call attempted. NO audit row written
     (pipeline-internal silent-skip per spec §3.6.3 + Sub-bundle B
     forward-binding lesson #2 surface-aware advisory audit).
  2. **Production + ladder enabled + schwab_client is None:** treated as
     fall-through to yfinance with WARNING log (per dispatch brief §0.5
     pre-emption #7 — caller couldn't construct a client).
  3. **Production + ladder enabled + schwab_client provided:** invoke the
     T-C.1 wrapper (which owns audit-lifecycle + transport-debug suppression
     + signature-hash + mapper). On success → wrap or pass-through with
     provider='schwab_api'.
  4. **On T-C.1 wrapper raising SchwabAuthError / SchwabRateLimitError /
     SchwabApiError:** log warning; invoke ``yfinance_fallback_fn``; return
     with provider tag ``'yfinance'``. Audit row already written by the
     T-C.1 wrapper (status='error'/'auth_failed'/'rate_limited').
  5. **Empty-bars handling:** mapper at T-C.1 raises synthetic
     ``SchwabApiError(204, "empty bars")`` per plan §H.6.4. Ladder catches
     via the ``SchwabApiError`` except clause; falls back to yfinance.
     parquet UNCHANGED (T-C.2 empty-write guard provides defense-in-depth).
  6. **Partial-response on quotes:** mapper at T-C.1 returns
     dict-of-successful-symbols. Ladder receives this dict; if requested
     ticker absent → treats as failure → falls back to yfinance.

**`PriceSnapshot.provider` field addition (Option A)** — per dispatch brief
§0.5 pre-emption #4, the `provider: str | None = None` field was added to
`swing.web.price_cache.PriceSnapshot` IN THIS TASK so the ladder can stamp
provenance at construction time; T-C.4 cache-integration consumes the field
naturally without dataclass extension. Banked as plan deviation.

**Reads but does NOT write `schwab_api_calls`:** all audit rows for the
ladder's Schwab-attempted path are written by the T-C.1 wrappers
(`get_quotes_batch` / `get_price_history`). The ladder itself writes
nothing — sandbox/disabled short-circuits and yfinance-fallback paths are
silent per spec §3.6.3.
"""
from __future__ import annotations

import logging
import sqlite3
from collections.abc import Callable
from datetime import datetime
from pathlib import Path
from typing import Any

from swing.integrations.schwab.client import (
    SchwabApiError,
    SchwabAuthError,
    SchwabRateLimitError,
)
from swing.integrations.schwab.marketdata import (
    get_price_history,
    get_quotes_batch,
)
from swing.integrations.schwab.models import (
    SchwabPriceHistoryWindow,
    SchwabQuoteResponse,
)
from swing.web.price_cache import PriceSnapshot

log = logging.getLogger(__name__)


def _resolve_cache_dir(cfg: Any) -> Path | None:
    """Best-effort lookup of ``cfg.paths.prices_cache_dir``.

    Codex R1 Major #3: persisting Schwab/yfinance window data to the Shape A
    archive requires a cache directory. Production cfg has
    ``cfg.paths.prices_cache_dir`` available; minimal test cfgs (SimpleNamespace)
    may not. Return None when unavailable so the ladder gracefully skips
    persistence in test scenarios that don't exercise the archive layer.
    """
    paths = getattr(cfg, "paths", None)
    if paths is None:
        return None
    cache_dir = getattr(paths, "prices_cache_dir", None)
    if cache_dir is None:
        return None
    return Path(cache_dir)


def _schwab_window_to_shape_a_df(window: SchwabPriceHistoryWindow):
    """Convert a ``SchwabPriceHistoryWindow`` to a Shape A DataFrame
    (``asof_date`` ISO string column + lowercase OHLCV columns) suitable for
    ``swing.data.ohlcv_archive.write_window``.

    Distinct from ``SchwabPriceHistoryWindow.to_dataframe()`` (M#4) which
    returns the IN-MEMORY legacy yfinance shape (DatetimeIndex + capitalized
    OHLCV) consumed by ``compute_smas`` + chart-step downstream code.
    """
    import pandas as pd

    return pd.DataFrame(
        {
            "asof_date": [bar.asof_date for bar in window.bars],
            "open": [bar.open for bar in window.bars],
            "high": [bar.high for bar in window.bars],
            "low": [bar.low for bar in window.bars],
            "close": [bar.close for bar in window.bars],
            "volume": [bar.volume for bar in window.bars],
        }
    )


def _yfinance_window_to_shape_a_df(window: Any):
    """Best-effort conversion of an arbitrary yfinance-fallback return value
    to a Shape A DataFrame.

    The yfinance fallback for the window ladder may return any of:
      - DatetimeIndex DataFrame with capitalized OHLCV columns (from
        ``_yf_download_window`` → ``read_or_fetch_archive`` legacy path);
      - already-Shape-A DataFrame (some test fixtures);
      - ``SchwabPriceHistoryWindow`` (some test fixtures use a Schwab dataclass
        as the fallback return — see ``test_04_window_production_path_empty_bars``);
      - None or empty.

    Returns None when conversion is infeasible (e.g., empty window, can't
    detect date column, non-DataFrame). Callers MUST guard the
    ``write_window`` call against ``None`` (which ``write_window``'s
    empty-window guard already handles).
    """
    if window is None:
        return None
    # SchwabPriceHistoryWindow path — use the same converter as Schwab success.
    if isinstance(window, SchwabPriceHistoryWindow):
        if not window.bars:
            return None
        return _schwab_window_to_shape_a_df(window)
    # DataFrame path — normalize via the archive's public helper
    # (Codex R2 Minor #1: was `_normalize_legacy_dataframe` private import).
    try:
        import pandas as pd

        from swing.data.ohlcv_archive import normalize_legacy_dataframe
    except ImportError:  # pragma: no cover — defensive
        return None
    if not isinstance(window, pd.DataFrame):
        return None
    if window.empty:
        return None
    try:
        return normalize_legacy_dataframe(window)
    except (ValueError, KeyError, AttributeError):
        return None


def _persist_window_to_archive(
    ticker: str,
    window: Any,
    provider: str,
    cache_dir: Path | None,
) -> None:
    """Best-effort persistence of a window to the Shape A archive.

    Codex R1 Major #3 — the ladder previously dropped Schwab-fetched bars on
    the floor; subsequent reads via ``resolve_ohlcv_window`` saw no
    schwab_api archive content. Persisting here ensures Schwab-sourced bars
    survive cross-call/cross-process via the archive layer.

    Failures are LOGGED but never propagated — write failures must not abort
    the ladder's primary contract (return the window to the caller).
    """
    if cache_dir is None or window is None:
        return
    if provider == "schwab_api" and isinstance(window, SchwabPriceHistoryWindow):
        df = _schwab_window_to_shape_a_df(window)
    elif provider == "yfinance":
        df = _yfinance_window_to_shape_a_df(window)
    else:  # pragma: no cover — defensive
        return
    if df is None:
        return
    try:
        from swing.data.ohlcv_archive import write_window
        write_window(ticker, df, provider, cache_dir=cache_dir)
    except Exception as exc:
        log.warning(
            "fetch_window_via_ladder: archive persistence failed for "
            "%s/%s: %s (continuing)",
            ticker, provider, type(exc).__name__,
        )


# ============================================================================
# Helpers
# ============================================================================


def _validate_ticker(ticker: Any) -> str:
    """Defense-in-depth (dispatch brief T-C.3 test #10): reject empty,
    None, non-string tickers BEFORE branching into env/ladder logic."""
    if ticker is None:
        raise TypeError("ticker must be non-empty str; got None")
    if not isinstance(ticker, str):
        raise TypeError(
            f"ticker must be non-empty str; got {type(ticker).__name__}"
        )
    if not ticker:
        raise ValueError("ticker must be non-empty str; got ''")
    return ticker


def _is_ladder_active(cfg: Any) -> bool:
    """Per plan §H.6.1 + §H.6.2 short-circuit predicate.

    Returns True iff env == 'production' AND
    marketdata_ladder_enabled == True. Any other case (sandbox, disabled
    flag, missing attrs) returns False → yfinance-only path.
    """
    integrations = getattr(cfg, "integrations", None)
    if integrations is None:
        return False
    schwab_cfg = getattr(integrations, "schwab", None)
    if schwab_cfg is None:
        return False
    env = getattr(schwab_cfg, "environment", None)
    enabled = getattr(schwab_cfg, "marketdata_ladder_enabled", None)
    return env == "production" and bool(enabled)


def _build_price_snapshot_from_quote(
    ticker: str, quote: SchwabQuoteResponse,
) -> PriceSnapshot:
    """Construct a PriceSnapshot stamped with provider='schwab_api'.

    asof = now() (the quote's `quote_time` is informational; ladder consumers
    treat asof as fetch-time for TTL bookkeeping); is_stale=False;
    source='live' (preserves existing TTL-state field semantics per plan
    pseudocode); provider='schwab_api'.
    """
    return PriceSnapshot(
        ticker=ticker,
        price=float(quote.last_price),
        asof=datetime.now(),
        is_stale=False,
        source="live",
        provider="schwab_api",
    )


# ============================================================================
# Quote ladder

codex
So far, the ladder still persists the Schwab window before returning, and the quote ladder/source files named in the constraints are not touched in the diff. The main question is now the fallback read semantics after a successful Schwab fetch: whether it can return `None` or stale legacy data in cases where only Schwab data exists, and whether that is worse than the old behavior.
exec
/bin/bash -lc "sed -n '260,380p' swing/integrations/schwab/marketdata_ladder.py" in /mnt/c/Users/rwsmy/swing-arc3
exec
/bin/bash -lc "sed -n '260,390p' swing/web/ohlcv_cache.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:

        Mirrors ``swing.prices.PriceFetcher.get`` semantics — ``end`` anchors at
        ``last_completed_session(now())`` (backward-looking, matches the
        CLAUDE.md session-anchor gotcha family), and the slice cuts on
        calendar-day cutoff. Belt-and-suspenders in-progress-bar strip mirrors
        ``swing.pipeline.ohlcv.fetch_daily_bars`` (CLAUDE.md yfinance gotcha).
        """
        end = last_completed_session(_dt.now())
        cutoff = end - timedelta(days=window_days)

        if self._ladder_bars_fetcher is not None:
            try:
                result = self._ladder_bars_fetcher(ticker)
            except Exception as exc:  # noqa: BLE001 — safety boundary
                log.warning(
                    "ohlcv ladder bars fetch raised for %s: %s", ticker, exc,
                )
                return None
            if result is None:
                return None
            bars = result[0] if isinstance(result, tuple) else result
            if bars is None or bars.empty:
                return None
        else:
            bars = read_or_fetch_archive(
                ticker,
                end_date=end,
                cache_dir=self._cfg.paths.prices_cache_dir,
                archive_history_days=self._cfg.archive.archive_history_days,
            )
            if bars is None or bars.empty:
                return None

        # Belt-and-suspenders in-progress bar strip (CLAUDE.md yfinance gotcha).
        # Inequality matters: `end` here is `last_completed_session(now())`
        # (backward-looking), so the archive's last legitimate bar can equal
        # `end`. Only strict-greater (a stray partial bar past the last
        # completed session) is partial-data. Using `>=` would over-strip on
        # every read. Distinct from `fetch_daily_bars` which anchors on
        # `action_session_for_run(now())` (forward-looking) and correctly uses
        # `>=`. Preserves shape parity with `PriceFetcher.get` (which performs
        # no strip; archive helper handles partial-bar avoidance).
        if bars.index[-1].date() > end:
            bars = bars.iloc[:-1]
        if bars.empty:
            return None
        sliced = bars.loc[
            (bars.index.date >= cutoff) & (bars.index.date <= end)
        ]
        if sliced.empty:
            return None
        return sliced

    # ---------- public API ----------

    def get_many_bundles(
        self, tickers: Sequence[str], *,
        deadline_seconds: float, executor: Executor,
    ) -> dict[str, OhlcvBundle]:
        """Return {ticker: OhlcvBundle} for every requested ticker.

        Cache-hits served from memory. Misses dispatched to `executor` under
        the semaphore. Any ticker that doesn't complete before
        `deadline_seconds` receives `OhlcvBundle.empty()` (not cached).

        Spec §4.3: each ticker's outcome (success if bundle produced, failure
        if deadline miss OR fetch raised) is recorded in the sliding window.

        Degraded short-circuit (R2 Major 2 resolution, scoped by R4 Major 1):
        when the breaker is tripped, skip executor submission for MISSES only —
        warm cache hits are still served normally. This preserves the 1-hour
        TTL value: a transient yfinance outage does not blank already-cached
        SMA advisories during cooldown. `is_degraded()` auto-clears the window
        after the cooldown expires so recovery can proceed.
        """
        # Normalize + deduplicate (spec §4.1).
        normalized = list(dict.fromkeys(t.upper() for t in tickers))

        now = time.monotonic()
        out: dict[str, OhlcvBundle] = {}
        to_fetch: list[str] = []
        hits_with_data = 0
        hits_empty_sentinel = 0

        # Cache scan — serve warm hits regardless of breaker state (R4 Major 1
        # resolution: valid cached bundles stay valid during cooldown).
        with self._lock:
            for t in normalized:
                hit = self._store.get(t)
                if hit is not None and (now - hit[1]) <= self._ttl:
                    out[t] = hit[0]
                    bundle = hit[0]
                    has_data = any(
                        v is not None for v in (
                            bundle.sma10, bundle.sma20, bundle.sma50, bundle.previous_close,
                        )
                    )
                    if has_data:
                        hits_with_data += 1
                    else:
                        hits_empty_sentinel += 1
                else:
                    to_fetch.append(t)

        # Ternary breaker accounting for cache hits:
        # - Data-hit: success (counts in window).
        # - Empty-sentinel hit: neutral (skips window) — R3 Major 1 fix.
        for _ in range(hits_with_data):
            self._record_outcome(success=True)
        for _ in range(hits_empty_sentinel):
            self._record_neutral()

        if not to_fetch:
            self._maybe_trip_breaker()
            return out

        # Degraded short-circuit — applies ONLY to misses (R4 Major 1 fix):
        # warm cache already served above, so during cooldown we just skip
        # fresh fetches for the remaining misses. These skips are NEUTRAL —
        # they don't pad the breaker window, same as sentinel hits.
        if self.is_degraded():
            for t in to_fetch:
                out[t] = OhlcvBundle.empty(fetched_at=time.monotonic())
                self._record_neutral()
            return out

        # Dispatch misses.
        futures: dict[Future, str] = {}
        for t in to_fetch:
            fut = executor.submit(self._fetch_bundle_worker, t)
            futures[fut] = t

 succeeded in 0ms:
# Quote ladder
# ============================================================================


def fetch_quote_via_ladder(
    ticker: str,
    *,
    cfg: Any,
    schwab_client: Any | None,
    yfinance_fallback_fn: Callable[[str], PriceSnapshot],
    conn: sqlite3.Connection,
    surface: str,
    pipeline_run_id: int | None = None,
) -> tuple[PriceSnapshot, str]:
    """Quote-fetch ladder: Schwab → yfinance fallback.

    Returns ``(entry, provider_tag)`` where:
      - ``entry`` is a ``PriceSnapshot`` instance.
      - ``provider_tag`` is ``'schwab_api'`` on Schwab success or
        ``'yfinance'`` on fallback.

    The ``yfinance_fallback_fn`` callable receives the ticker only and MUST
    return a ``PriceSnapshot`` (provider field optional — ladder does NOT
    re-stamp on the yfinance return path; caller-supplied provider is
    preserved).

    Raises:
        TypeError / ValueError on invalid ticker input (defense-in-depth).
    """
    ticker = _validate_ticker(ticker)

    # Sandbox / disabled short-circuit (plan §H.6.1 LOCK; ZERO audit row).
    if not _is_ladder_active(cfg):
        entry = yfinance_fallback_fn(ticker)
        return (entry, "yfinance")

    # Production + ladder enabled but client wasn't constructed → fallback.
    if schwab_client is None:
        log.warning(
            "fetch_quote_via_ladder: schwab_client is None in production; "
            "falling back to yfinance for %s",
            ticker,
        )
        entry = yfinance_fallback_fn(ticker)
        return (entry, "yfinance")

    # Attempt Schwab. The T-C.1 wrapper owns _suppress_transport_debug_logs +
    # audit-lifecycle + signature-hash + mapper.
    try:
        mapped = get_quotes_batch(
            schwab_client,
            conn,
            [ticker],
            surface=surface,
            environment="production",
            pipeline_run_id=pipeline_run_id,
        )
    except (SchwabAuthError, SchwabRateLimitError, SchwabApiError) as exc:
        log.warning(
            "Schwab market-data quote fetch failed for %s "
            "(%s); falling back to yfinance",
            ticker, type(exc).__name__,
        )
        entry = yfinance_fallback_fn(ticker)
        return (entry, "yfinance")
    except Exception as exc:
        # Any unexpected exception from the wrapper is treated as a failure
        # → yfinance fallback. Log the exception class+message (NOT exc_info:
        # traceback frames bypass message-level redaction) so a silent degrade
        # (e.g. an audit `database is locked`) is diagnosable.
        log.warning(
            "fetch_quote_via_ladder: unexpected error from T-C.1 wrapper for "
            "%s: %s: %s; falling back to yfinance",
            ticker, type(exc).__name__, exc,
        )
        entry = yfinance_fallback_fn(ticker)
        return (entry, "yfinance")

    # Partial-response (plan §E.4): if requested ticker absent from mapper
    # output → fall back to yfinance. T-C.1 wrapper already wrote audit row
    # reflecting partial-success / all-failed disposition.
    quote = mapped.get(ticker)
    if quote is None:
        log.warning(
            "fetch_quote_via_ladder: ticker %s absent from Schwab response "
            "(partial-response per §E.4); falling back to yfinance",
            ticker,
        )
        entry = yfinance_fallback_fn(ticker)
        return (entry, "yfinance")

    entry = _build_price_snapshot_from_quote(ticker, quote)
    return (entry, "schwab_api")


# ============================================================================
# Window ladder
# ============================================================================


def fetch_window_via_ladder(
    ticker: str,
    *,
    start: datetime | int | None,
    end: datetime | int | None,
    cfg: Any,
    schwab_client: Any | None,
    yfinance_fallback_fn: Callable[
        [str, datetime | int | None, datetime | int | None],
        SchwabPriceHistoryWindow,
    ],
    conn: sqlite3.Connection,
    surface: str,
    pipeline_run_id: int | None = None,
    period_type: str | None = None,
    period: int | None = None,
    frequency_type: str | None = None,
    frequency: int | None = None,
) -> tuple[SchwabPriceHistoryWindow, str]:
    """Window-fetch ladder: Schwab → yfinance fallback.


exec
/bin/bash -lc "sed -n '1,260p' swing/data/ohlcv_archive.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
"""Per-ticker incremental OHLCV archive.

Source-of-truth for archive-aware reads consumed by `swing.prices.PriceFetcher`,
`swing.pipeline.ohlcv.fetch_daily_bars`, and (via the latter) `swing.web.ohlcv_cache.OhlcvCache`.

Schema:
- `{cache_dir}/{TICKER}.parquet` — full retained history, indexed by date,
  with OHLCV columns. Sliced by callers as needed.
- `{cache_dir}/{TICKER}.meta.json` — sidecar metadata, currently
  `{"last_full_refresh_date": "YYYY-MM-DD"}`. Additive shape; future fields
  may join (e.g., last-incremental-fetch timestamp).

Coherence policy (per OHLCV archive consolidation plan locked decision §2.2):
1. New ticker (no archive on disk) → full-history fetch (start = today -
   `_calendar_window_for_trading_days(archive_history_days)`; end = today
   (`_last_completed_session_today`); the helper post-trims to last
   `archive_history_days` rows).
2. Weekly full-refresh: if (today - last_full_refresh_date).days >= 7 →
   full-history fetch.
3. Otherwise incremental: if latest_stored_bar < today → fetch
   (latest+1, today). Caller's end_date controls the return slice ONLY,
   never the fetch upper bound — so historical callers never truncate the
   archive.
4. Else cache hit → return archive slice ≤ end_date with NO yfinance call.

Atomicity: all writes go to a `tempfile.NamedTemporaryFile`-style temp file
in the destination directory, then `os.replace` to the final path. Avoids
the cross-device-link gotcha (CLAUDE.md) and ensures readers never observe
partial writes.

yfinance gotchas (CLAUDE.md): all calls go through `yf.download` with
`threads=False` (NOT `yf.Ticker.history()`); MultiIndex columns are squeezed
defensively.
"""
from __future__ import annotations

import contextlib
import functools
import json
import logging
import math
import os
import tempfile
import zlib
from dataclasses import dataclass, field
from datetime import date, datetime, timedelta
from pathlib import Path

import pandas as pd
import yfinance as yf

log = logging.getLogger(__name__)

# Shape A persistence (Schwab API Sub-bundle C T-C.2 / plan §A.8 + §H.6.3):
# parquet-per-(ticker, provider). LOWER integer = HIGHER priority under
# `min()`-by-precedence selection in `resolve_ohlcv_window`. Mirrors the
# Phase 9 Sub-bundle C `_SOURCE_PRECEDENCE` pattern at
# `swing/data/repos/account_equity_snapshots.py` (schwab_api > yfinance).
_SOURCE_PRECEDENCE_MARKET_DATA: dict[str, int] = {
    "schwab_api": 0,
    "yfinance": 1,
}

# Arc 6 warm-batch tunables (module constants, NOT config — benchmark-pinned).
DEFAULT_CHUNK_SIZE = 75          # benchmark sweeps 50-100; §8 reliability-constrained
GAP_DEEP_BAND_TRADING_DAYS = 30  # gaps staler than this collapse into ONE deep band


@dataclass
class WarmReport:
    """Lightweight result of warm_archives_batch — counts + the fallback list.
    Carries NO DB rows and NO schema (Arc 6 §6). `degraded` is True whenever
    any ticker fell through to the serial path (per-ticker miss or whole-chunk
    failure), so _step_evaluate can decide whether to emit a #27 warning."""
    cache_hit: int = 0
    gap: int = 0
    deep_gap: int = 0
    full_refresh: int = 0
    chunks_attempted: int = 0
    chunk_failures: int = 0
    fallback: list[str] = field(default_factory=list)
    wall_seconds: float = 0.0
    dry_run: bool = False
    # Arc 8: trailing NaN-OHLC bars dropped at the warm write barrier (#27 audit).
    trailing_nan_trimmed: int = 0

    @property
    def degraded(self) -> bool:
        return bool(self.fallback) or self.chunk_failures > 0


def _archive_paths(cache_dir: Path, ticker: str) -> tuple[Path, Path]:
    return (cache_dir / f"{ticker}.parquet"), (cache_dir / f"{ticker}.meta.json")


def _read_meta(meta_path: Path) -> dict:
    if not meta_path.exists():
        return {}
    try:
        return json.loads(meta_path.read_text())
    except (json.JSONDecodeError, OSError) as exc:
        log.warning("corrupted meta %s: %s — treating as missing", meta_path, exc)
        return {}


def _write_meta_atomic(meta_path: Path, meta: dict) -> None:
    cache_dir = meta_path.parent
    fd, tmp_name = tempfile.mkstemp(
        dir=str(cache_dir), prefix=f"{meta_path.stem}.", suffix=".meta.json.tmp"
    )
    os.close(fd)
    tmp_path = Path(tmp_name)
    try:
        tmp_path.write_text(json.dumps(meta))
        os.replace(tmp_path, meta_path)
    except Exception:
        if tmp_path.exists():
            with contextlib.suppress(OSError):
                tmp_path.unlink()
        raise


def _read_archive(parquet_path: Path) -> pd.DataFrame | None:
    if not parquet_path.exists():
        return None
    return pd.read_parquet(parquet_path)


def _strip_incomplete_sessions(df: pd.DataFrame, cutoff_iso: str) -> pd.DataFrame:
    """Drop archive rows dated AFTER the last completed session (cutoff_iso =
    ISO YYYY-MM-DD). Shape-agnostic: Shape-A frames carry an `asof_date` column
    (string OR Timestamp -- both normalized to date before comparison so a
    `2026-06-04 00:00:00` value cannot lexically out-sort the cutoff day);
    legacy/yfinance frames are DatetimeIndex'd (compare `index.date`). Returns a
    new frame (may be empty).

    Codex R1 MAJOR #1: this is the single write-barrier chokepoint, so it FAILS
    CLOSED -- a NON-EMPTY frame carrying neither recognized shape raises (it
    must never silently persist a possibly-`> cutoff` row). An EMPTY frame of
    any shape passes through unchanged (nothing to strip)."""
    cutoff = date.fromisoformat(cutoff_iso)
    if "asof_date" in df.columns:
        asof = pd.to_datetime(df["asof_date"]).dt.date
        return df[asof <= cutoff]
    if isinstance(df.index, pd.DatetimeIndex):
        return df[df.index.date <= cutoff]
    if len(df) == 0:
        return df
    raise ValueError(
        "_strip_incomplete_sessions: unrecognized frame shape (no 'asof_date' "
        "column and non-DatetimeIndex index); refusing to persist to avoid "
        "leaking a row dated after the last completed session"
    )


def _write_archive_atomic(parquet_path: Path, df: pd.DataFrame) -> None:
    # Completed-day write barrier (L2, the single chokepoint): no archive write
    # path may persist a row dated after the last completed session. Applied
    # here -- the one function ALL Shape-A writers funnel through (write_window,
    # read_or_fetch_archive, _backward_compat_rename) -- so none can bypass it.
    df = _strip_incomplete_sessions(df, _last_completed_session_today().isoformat())
    cache_dir = parquet_path.parent
    fd, tmp_name = tempfile.mkstemp(
        dir=str(cache_dir), prefix=f"{parquet_path.stem}.", suffix=".parquet.tmp"
    )
    os.close(fd)
    tmp_path = Path(tmp_name)
    try:
        df.to_parquet(tmp_path)
        os.replace(tmp_path, parquet_path)
    except Exception:
        if tmp_path.exists():
            with contextlib.suppress(OSError):
                tmp_path.unlink()
        raise


def _squeeze_multiindex(df: pd.DataFrame) -> pd.DataFrame:
    if isinstance(df.columns, pd.MultiIndex):
        df = df.copy()
        df.columns = df.columns.get_level_values(0)
    return df


def _calendar_window_for_trading_days(trading_days: int) -> int:
    """Convert trading-day retention to a calendar-day yfinance window.

    Codex R1 Critical 1 + R2 Critical 1 resolution: 1260 trading days is
    NOT 1260 calendar days (~3.45y) AND the old `n * 7 / 5 + 14` heuristic
    is too tight for a full 5-year retention target; multi-year holiday
    clustering eats more than a 14-day buffer. The locked
    retention is "5 years" (spec §2.5), so the conversion uses the actual
    market-calendar ratio: ~252 trading days per ~365.25 calendar days,
    plus a 30-day buffer for holiday clustering. For default 1260 trading
    days: ceil(1260 * 365.25 / 252) + 30 = 1857 calendar days (~5.08y).
    Caller truncates the returned DataFrame to last `trading_days` rows
    post-fetch so the archive doesn't bloat with the buffer days.
    """
    return int(math.ceil(trading_days * 365.25 / 252)) + 30


def _trim_trailing_ragged(df: pd.DataFrame) -> tuple[pd.DataFrame, int]:
    """Arc 8 — drop trailing rows where ANY of Open/High/Low/Close is NaN.

    Iterates from the END, removing rows while the newest remaining row has a
    NaN in any OHLC field; stops at the first clean row. Returns the trimmed
    frame plus the number of rows dropped.

    Scope (LOCKED, run-#99 evidence base):
    - Guards ONLY the incoming TRAILING bar(s) — the run-#99 class is yfinance
      returning the newest bar with NaN `Close` while O/H/L/V are present (the
      adjusted-close derivation artifact). Trimming it leaves the meta/archive
      stale so the next fetch retries a settled bar (the F6-transient posture).
    - INTERIOR ragged rows are PRESERVED — the Phase-15 bad-bar-accept posture
      for HISTORICAL bars is explicitly unchanged; this barrier never reaches
      past the first clean trailing row.
    - Volume-NaN ALONE does NOT trim (legitimately volume-less bars exist).
    """
    ohlc = [c for c in ("Open", "High", "Low", "Close") if c in df.columns]
    if df.empty or not ohlc:
        return df, 0
    n = len(df)
    cut = n
    while cut > 0 and bool(df.iloc[cut - 1][ohlc].isna().any()):
        cut -= 1
    if cut == n:
        return df, 0
    return df.iloc[:cut], n - cut


def _yf_download_window(ticker: str, *, start: date, end: date) -> pd.DataFrame:
    """Wrap yf.download with the project's gotcha-resistant kwargs.
    `start` is inclusive, `end` is exclusive in yfinance — we always pass
    `end + 1 day` to make the call site's `end_date` semantics inclusive.
    """
    df = yf.download(
        ticker,
        start=start,
        end=end + timedelta(days=1),
        progress=False,
        auto_adjust=False,
        actions=False,
        threads=False,
    )
    if df is None or df.empty:
        return pd.DataFrame()
    df = _squeeze_multiindex(df)
    keep_cols = [c for c in ("Open", "High", "Low", "Close", "Volume") if c in df.columns]
    df = df[keep_cols]
    if hasattr(df.index, "tz") and df.index.tz is not None:
        df.index = df.index.tz_localize(None)
    # Arc 8 trailing-bar NaN-Close barrier — covers BOTH serial branches (the
    # full-refresh tail->write @~709 and the gap concat->write @~722). A frame
    # trimmed to empty composes with the existing `if fetched.empty` /
    # `if not gap.empty` guards (no write, meta stays stale, retry next call).
    trimmed_df, n_trimmed = _trim_trailing_ragged(df)
    if n_trimmed:
        dropped = [d.date().isoformat() for d in df.index[len(trimmed_df):]]
        log.warning(
            "serial trailing-ragged trim (%s): dropped %d trailing NaN-OHLC "

exec
/bin/bash -lc "sed -n '260,620p' swing/data/ohlcv_archive.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
            "serial trailing-ragged trim (%s): dropped %d trailing NaN-OHLC "
            "bar(s) %s (retry next fetch)", ticker, n_trimmed, dropped,
        )
    return trimmed_df


def _last_completed_session_today() -> date:
    """Return the most recent completed NYSE session as of now. Used as the
    'today' anchor for weekly-refresh and partial-bar-strip semantics.
    Imports lazily to avoid circular references at module load time."""
    from swing.evaluation.dates import last_completed_session
    return last_completed_session(datetime.now())


def _full_refresh_due(
    ticker: str,
    last_full_refresh: date,
    today_session: date,
    *,
    stagger_enabled: bool,
) -> bool:
    """PURE predicate — the single source of full-refresh-due truth, called by
    BOTH read_or_fetch_archive AND warm_archives_batch with the SAME
    stagger_enabled value (resolved once by _full_refresh_stagger_enabled).

    `last_full_refresh` is a real date; callers with no parseable meta MUST
    NOT call this (they are full-refresh-due unconditionally via the
    archive-missing / meta-missing arms of the classifier).

    stagger_enabled=False  -> exact legacy `days_since >= 7` cliff.
    stagger_enabled=True   -> fire on the ticker's own crc32 bucket day once
    `>= 7` due, with a `>= 13` hard ceiling bounding worst-case staleness.
    crc32 (NOT Python hash()) for cross-process determinism — hash(str) is
    randomized by PYTHONHASHSEED.
    """
    days_since_full = (today_session - last_full_refresh).days
    if not stagger_enabled:
        return days_since_full >= 7
    bucket = zlib.crc32(ticker.encode()) % 7
    day_idx = today_session.toordinal() % 7
    return (days_since_full >= 7 and bucket == day_idx) or (days_since_full >= 13)


def _load_archive_config_for_stagger() -> bool:
    """Read [archive].stagger_full_refresh from the tracked project config.

    Isolated so tests can monkeypatch the config read without touching disk.
    Lazy import avoids any import cycle (swing.config imports only stdlib).
    """
    from swing.config import Config
    return Config.from_defaults().archive.stagger_full_refresh


@functools.lru_cache(maxsize=1)
def _full_refresh_stagger_enabled() -> bool:
    """Single source of the stagger kill-switch, cached at module level.

    Returns True (stagger ON) if the config is unreadable for any reason —
    the safe default that prevents the weekly storm. Cached for the process
    lifetime: the nightly pipeline (a fresh process) always reads current
    config; a long-lived `swing web` server holds the value until restart
    (call `_full_refresh_stagger_enabled.cache_clear()` to force a re-read,
    or restart the server — Arc 6 §5 R3 Minor #2).
    """
    try:
        return bool(_load_archive_config_for_stagger())
    except Exception:  # noqa: BLE001 — any failure -> safe default
        log.warning("could not resolve [archive].stagger_full_refresh; defaulting to True")
        return True


def _classify_warm_cohorts(
    tickers: list[str],
    *,
    cache_dir: Path,
    today_session: date,
    archive_history_days: int,
    stagger_enabled: bool,
) -> dict:
    """Bucket each ticker into cache-hit / gap-bands / deep-gap / full-refresh
    using the EXACT read_or_fetch_archive predicates (Arc 6 §4.1). Local I/O
    only — reads archive + meta from disk, performs ZERO yf.download calls.

    Returns a dict:
      {"cache_hit": [t...],
       "gap_bands": {latest_date: [t...], ...},   # near-current bands
       "deep_gap": [t...],                          # collapsed deep band
       "full_refresh": [t...]}
    """
    cache_dir = Path(cache_dir)
    cache_hit: list[str] = []
    gap_bands: dict[date, list[str]] = {}
    deep_gap: list[str] = []
    full_refresh: list[str] = []

    for raw in tickers:
        ticker = raw.upper()
        parquet_path, meta_path = _archive_paths(cache_dir, ticker)
        archive = _read_archive(parquet_path)
        meta = _read_meta(meta_path)

        last_full_refresh: date | None = None
        last_full_str = meta.get("last_full_refresh_date")
        if last_full_str:
            try:
                last_full_refresh = date.fromisoformat(last_full_str)
            except ValueError:
                last_full_refresh = None

        # Archive-missing / empty / meta-missing -> full-refresh (NO bucket gate),
        # mirroring read_or_fetch_archive's needs_full_refresh arms.
        if archive is None or archive.empty or last_full_refresh is None:
            full_refresh.append(ticker)
            continue
        if _full_refresh_due(ticker, last_full_refresh, today_session,
                             stagger_enabled=stagger_enabled):
            full_refresh.append(ticker)
            continue

        latest_stored = archive.index.max().date()
        if latest_stored >= today_session:
            cache_hit.append(ticker)
            continue

        # Gap cohort — band by latest_stored; collapse very-stale into deep band.
        # Codex R1 Major #3: measure staleness in TRADING days (spec §4.2 locks the
        # collapse at "> 30 trading days"), NOT calendar days through
        # _calendar_window_for_trading_days (which adds a +30 fetch buffer -> ~74
        # calendar days ~= 53 trading days, far looser than the spec). bdate_range
        # counts business days (a holiday-agnostic trading-session proxy, same order
        # of approximation as the existing helper).
        gap_sessions = pd.bdate_range(
            latest_stored + timedelta(days=1), today_session
        ).size
        if gap_sessions > GAP_DEEP_BAND_TRADING_DAYS:
            deep_gap.append(ticker)
        else:
            gap_bands.setdefault(latest_stored, []).append(ticker)

    return {
        "cache_hit": cache_hit,
        "gap_bands": gap_bands,
        "deep_gap": deep_gap,
        "full_refresh": full_refresh,
    }


def _chunk_tickers(tickers: list[str], *, chunk_size: int) -> list[list[str]]:
    """Split into chunks of chunk_size, folding a trailing lone remnant into the
    previous chunk so no size-1 chunk is sent (the single-ticker-remnant shape
    is still handled by _extract_ticker_subframe — this just avoids it where we
    can). Returns [] for empty input."""
    if not tickers:
        return []
    chunks = [tickers[i:i + chunk_size] for i in range(0, len(tickers), chunk_size)]
    if len(chunks) > 1 and len(chunks[-1]) == 1:
        lone = chunks.pop()
        chunks[-1] = chunks[-1] + lone
    return chunks


def _extract_ticker_subframe(frame: pd.DataFrame, ticker: str) -> pd.DataFrame | None:
    """Extract + validate one ticker's OHLCV subframe from a group_by='ticker'
    batch response (Arc 6 §4.3 validation ladder). Returns the normalized
    subframe ([Open,High,Low,Close,Volume], tz-stripped) on success, or None on
    ANY gate failure (-> caller routes the ticker to fallback). Wrapped so a
    malformed shape degrades to None rather than crashing the chunk."""
    try:
        ticker = ticker.upper()
        # (a) subframe present. Flat (non-MultiIndex) frame == single-ticker remnant.
        if isinstance(frame.columns, pd.MultiIndex):
            level0 = {str(c).upper(): c for c in frame.columns.get_level_values(0)}
            if ticker not in level0:
                return None
            sub = frame[level0[ticker]]
            if isinstance(sub, pd.Series):  # degenerate single-column
                return None
        else:
            sub = frame  # flat remnant -> already this ticker's OHLCV
        sub = sub.copy()
        # case-insensitive column resolution
        col_map = {str(c).lower(): c for c in sub.columns}
        # (b) required OHLCV columns present
        required = ["open", "high", "low", "close", "volume"]
        if not all(r in col_map for r in required):
            return None
        keep = sub[[col_map[r] for r in required]]
        keep.columns = ["Open", "High", "Low", "Close", "Volume"]  # canonical (Adj Close dropped)
        # (c) non-empty after dropna(how="all") — F6: present-but-all-NaN -> fallback
        keep = keep.dropna(how="all")
        if keep.empty:
            return None
        # (d) index parseable to DatetimeIndex
        if not isinstance(keep.index, pd.DatetimeIndex):
            keep.index = pd.to_datetime(keep.index)
        if getattr(keep.index, "tz", None) is not None:
            keep.index = keep.index.tz_localize(None)
        return keep
    except Exception:  # noqa: BLE001 — any unforeseen shape error -> fallback
        return None


def _fetch_chunk(
    chunk: list[str], *, start: date, end: date,
) -> tuple[dict[str, pd.DataFrame], list[str], bool]:
    """Fetch ONE chunk with a single multi-ticker yf.download (threads=True,
    group_by='ticker'), mirroring _yf_download_window's kwargs + the inclusive-end
    `+1 day` convention. threads=True is the Arc-6 spec §8 stretch lever,
    OPERATOR-AUTHORIZED 2026-06-10 after the 6c cold gate measured threads=False
    at 207.5s warm (evaluate 220s vs the <=90s target); the serial-fallback
    safety net (proven live, run #99) bounds the rate-limit downside to a slower
    night, never a broken one. The SINGLE-ticker path (_yf_download_window)
    keeps threads=False per the original gotcha. Revert = this one kwarg.
    Returns (extracted, failed, chunk_failed):
      - extracted: {ticker: valid_subframe}
      - failed: tickers that did not extract (per-ticker miss OR whole-chunk)
      - chunk_failed: True ONLY when the whole call failed (yf.download raised, or
        an empty/None response) — a WHOLE-CHUNK download failure (Arc 6 §6). A
        VALID response in which every ticker is present-but-all-NaN sets
        chunk_failed=False (those are per-ticker misses), so the #27
        chunk_failures counter is not corrupted (Codex R1 Major #6)."""
    try:
        raw = yf.download(
            chunk, start=start, end=end + timedelta(days=1),
            group_by="ticker", threads=True, progress=False,
            auto_adjust=False, actions=False,
        )
    except Exception as exc:  # noqa: BLE001 — whole chunk -> serial fallback
        log.warning("warm chunk yf.download failed (%d tickers -> fallback): %s",
                    len(chunk), exc)
        return {}, list(chunk), True
    if raw is None or raw.empty:
        return {}, list(chunk), True
    extracted: dict[str, pd.DataFrame] = {}
    failed: list[str] = []
    for t in chunk:
        sub = _extract_ticker_subframe(raw, t)
        if sub is None:
            failed.append(t.upper())
        else:
            extracted[t.upper()] = sub
    return extracted, failed, False


def _merge_gap_subframe(
    cache_dir: Path, ticker: str, sub: pd.DataFrame, *, archive_history_days: int,
) -> None:
    """Merge a gap subframe into the existing archive — data-content-identical to
    the serial read_or_fetch_archive incremental-gap branch (lines ~277-281):
    concat, dedup keep='last', sort, tail(N), atomic write. NO meta write (gap).

    Codex R1 Critical #1 (deep-band overlap): the deep-gap band collapses tickers
    with DIFFERENT latest_stored into ONE wide window, so a ticker's subframe can
    carry rows AT-OR-BEFORE its own latest_stored (rows another, staler band member
    needed). The serial gap branch fetches ONLY `[latest+1, today]`, so it NEVER
    rewrites a pre-existing archived bar. To match that outcome we slice the
    incoming sub to `index.date > latest_stored` before concat — dropping the
    overlap so existing bars are untouched (a re-fetch of an old bar yfinance may
    have re-stated must NOT overwrite the archived value; that is the serial
    behavior and the #26-temporal-mutation parity requirement). Harmless for
    ordinary bands (all members share latest_stored → zero overlap)."""
    parquet_path, _ = _archive_paths(Path(cache_dir), ticker.upper())
    archive = _read_archive(parquet_path)
    if archive is None or archive.empty:
        # Codex R1 Critical #2: a gap-classified ticker had a non-empty archive at
        # classify time; if it vanished by merge time (TOCTOU), writing the gap
        # sub ALONE would truncate retained history AND — with a surviving recent
        # meta — let the serial path serve the stub as a cache-hit, masking the
        # loss. Pure accelerator: write NOTHING and raise so _warm_one_window
        # routes the ticker to WarmReport.fallback; the serial read_or_fetch_archive
        # (archive-missing -> full-refresh) stays authoritative and self-heals.
        raise RuntimeError(
            f"gap-classified {ticker} archive missing/empty at merge time; "
            "routing to serial fallback (no truncated write)"
        )
    latest_stored = archive.index.max().date()
    # Drop any incoming row dated <= latest_stored so existing bars are never
    # overwritten (byte-parity with the serial `[latest+1, today]` gap fetch).
    fresh = sub.loc[sub.index.date > latest_stored]
    # Arc 8 (Codex R1 MAJOR): the trailing-ragged trim can leave a sub with ONLY
    # overlap rows (<= latest_stored) — e.g. a deep-gap band whose only post-
    # latest_stored row was today's ragged bar. With nothing fresh, the serial
    # gap path no-ops (its `[latest+1, today]` fetch trims to empty -> no write);
    # match that here so warm leaves the archive untouched too (trim-to-empty
    # parity by construction; no needless rewrite).
    if fresh.empty:
        return
    combined = pd.concat([archive, fresh])
    combined = combined[~combined.index.duplicated(keep="last")].sort_index()
    combined = combined.tail(archive_history_days)
    _write_archive_atomic(parquet_path, combined)


def _write_full_refresh_subframe(
    cache_dir: Path, ticker: str, sub: pd.DataFrame, *,
    today_session: date, archive_history_days: int,
) -> None:
    """Write a full-refresh subframe — data-content-identical to the serial
    full-refresh branch (lines ~266-268): tail(N), atomic write, THEN write meta."""
    parquet_path, meta_path = _archive_paths(Path(cache_dir), ticker.upper())
    fetched = sub.tail(archive_history_days)
    _write_archive_atomic(parquet_path, fetched)
    _write_meta_atomic(meta_path, {"last_full_refresh_date": today_session.isoformat()})


def warm_archives_batch(
    tickers: list[str],
    *,
    cache_dir: Path,
    archive_history_days: int,
    end_date: date,
    chunk_size: int = DEFAULT_CHUNK_SIZE,
    inter_chunk_pause_s: float = 0.0,
    dry_run: bool = False,
) -> WarmReport:
    """Pre-warm per-ticker archives with batched multi-ticker yf.download calls
    so the serial read_or_fetch_archive loops all hit the cache-hit branch
    (Arc 6 §3). PURE ACCELERATOR: any per-ticker or per-chunk failure routes to
    `WarmReport.fallback`; the serial path re-fetches those. Never raises for a
    data problem.

    SESSION ANCHOR (Codex R1 Critical #2 — clarified). The warm has NO return
    slice (it returns a WarmReport, not bars), so `end_date` is accepted for
    signature parity with the spec §3 contract but is intentionally UNUSED for
    the write anchor: archives are always warmed to
    `today_session = _last_completed_session_today()`, the SAME source function
    the serial write path (`read_or_fetch_archive` + `_write_archive_atomic`'s
    completed-day strip) uses. The §4.1 invariant is "same SOURCE FUNCTION", not
    a single value threaded through `_write_archive_atomic` (a Shape-A shared
    writer outside this arc's carve-out). Resolving `today_session` once here and
    passing it to the classifier + writers means the warm introduces NO new
    divergence surface beyond what already exists — the three serial fetch loops
    in `_step_evaluate` each call `_last_completed_session_today()` independently
    today. A session-boundary race remains theoretically possible (warm vs serial
    crossing ~16:00 ET mid-run) but is identical in kind to that pre-existing
    inter-loop race and acceptable for a single-operator nightly that runs well
    after the close. Callers MUST pass `end_date` (signature contract); pass
    `last_completed_session(run_now)` from the runner for documentation symmetry.

    dry_run=True: classify + return cohort counts with ZERO yf.download calls.
    """
    import time
    started = time.monotonic()
    cache_dir = Path(cache_dir)
    cache_dir.mkdir(parents=True, exist_ok=True)
    today_session = _last_completed_session_today()
    stagger_enabled = _full_refresh_stagger_enabled()

    deduped = sorted({t.upper() for t in tickers})
    cohorts = _classify_warm_cohorts(
        deduped, cache_dir=cache_dir, today_session=today_session,
        archive_history_days=archive_history_days, stagger_enabled=stagger_enabled,
    )
    gap_count = sum(len(v) for v in cohorts["gap_bands"].values())
    report = WarmReport(
        cache_hit=len(cohorts["cache_hit"]),
        gap=gap_count,
        deep_gap=len(cohorts["deep_gap"]),
        full_refresh=len(cohorts["full_refresh"]),
        dry_run=dry_run,
    )

exec
/bin/bash -lc "rg -n \"def read_or_fetch_archive|def write_window|def resolve_ohlcv_window|_backward_compat|schwab_api\\.parquet|provider\" swing/data/ohlcv_archive.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
55:# parquet-per-(ticker, provider). LOWER integer = HIGHER priority under
160:    # read_or_fetch_archive, _backward_compat_rename) -- so none can bypass it.
715:def read_or_fetch_archive(
806:# `read_or_fetch_archive` above; the one-shot `_backward_compat_rename`
811:# both per-provider files (if present), filters to the caller's
815:def _shape_a_path(cache_dir: Path, ticker: str, provider: str) -> Path:
816:    """Return the Shape A parquet path for `(ticker, provider)`.
822:    return cache_dir / f"{ticker.upper()}.{provider}.parquet"
825:def write_window(
828:    provider: str,
852:    path = _shape_a_path(cache_dir, ticker, provider)
913:def resolve_ohlcv_window(
920:    """Resolve the OHLCV window for `ticker` across both provider parquets.
922:    Reads `{TICKER}.schwab_api.parquet` AND `{TICKER}.yfinance.parquet` from
941:        provenance dict mapping `asof_date` -> winning provider name).
952:        _backward_compat_rename(ticker_u, cache_dir=cache_dir)
954:    # Read both providers' parquet files (if present), accumulate rows by
955:    # (asof_date, provider). Reading returns a fresh DataFrame so we keep
956:    # provider attribution on a per-row basis.
958:    for provider in ("schwab_api", "yfinance"):
959:        path = _shape_a_path(cache_dir, ticker_u, provider)
967:            rows_by_date.setdefault(asof, {})[provider] = row
969:    # Merge: filter to [start, end] then pick lowest-precedence provider.
976:        winner_provider = min(
980:        merged_rows.append(candidates[winner_provider])
981:        provenance[asof_date] = winner_provider
999:    causing the ``_backward_compat_rename`` default branch (Shape A wins)
1066:    Codex R1 Major #2 fix: prior to this helper, ``_backward_compat_rename``
1125:def _backward_compat_rename(ticker: str, *, cache_dir: Path) -> None:

exec
/bin/bash -lc "sed -n '900,1320p' swing/data/ohlcv_archive.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:

    frames = [f for f in (existing, incoming)
              if f is not None and "asof_date" in f.columns]
    if not frames:
        return
    union = pd.concat(frames) if len(frames) > 1 else frames[0]
    merged = union.drop_duplicates(subset=["asof_date"], keep="last")
    merged = merged.sort_values("asof_date").reset_index(drop=True)
    # The atomic writer applies the > cutoff strip; an all-partial union writes
    # clean (no > cutoff survives), a valid union preserves <= cutoff history.
    _write_archive_atomic(path, merged)


def resolve_ohlcv_window(
    ticker: str,
    *,
    start: str,
    end: str,
    cache_dir: Path,
) -> tuple[pd.DataFrame, dict[str, str]]:
    """Resolve the OHLCV window for `ticker` across both provider parquets.

    Reads `{TICKER}.schwab_api.parquet` AND `{TICKER}.yfinance.parquet` from
    `cache_dir` (whichever are present), filters to ISO-date window
    `[start, end]` (inclusive on both ends), and selects the highest-
    priority row per `asof_date` per `_SOURCE_PRECEDENCE_MARKET_DATA`.

    Codex R1 Minor #4: the window filter (`start <= asof_date <= end`) is
    applied AFTER reading both parquets but BEFORE winner selection so
    out-of-range rows neither pollute the merge decision nor leak into the
    return value.

    Args:
        ticker: ticker symbol; will be uppercased to match write_window.
        start: ISO `YYYY-MM-DD` window start (inclusive).
        end: ISO `YYYY-MM-DD` window end (inclusive).
        cache_dir: archive directory; need not exist.

    Returns:
        Tuple of (DataFrame indexed 0..n-1 with `asof_date` column +
        OHLCV columns sorted ascending by `asof_date`,
        provenance dict mapping `asof_date` -> winning provider name).
        Empty DataFrame + empty dict when no rows match.
    """
    cache_dir = Path(cache_dir)
    ticker_u = ticker.upper()

    # Codex R1 Major #1: wire one-shot legacy migration into the read path so
    # existing {TICKER}.parquet archives are automatically migrated to Shape
    # A on FIRST read. Idempotent — subsequent invocations no-op when the
    # legacy file is absent (case 3: new-only).
    if cache_dir.exists():
        _backward_compat_rename(ticker_u, cache_dir=cache_dir)

    # Read both providers' parquet files (if present), accumulate rows by
    # (asof_date, provider). Reading returns a fresh DataFrame so we keep
    # provider attribution on a per-row basis.
    rows_by_date: dict[str, dict[str, pd.Series]] = {}
    for provider in ("schwab_api", "yfinance"):
        path = _shape_a_path(cache_dir, ticker_u, provider)
        if not path.exists():
            continue
        df = pd.read_parquet(path)
        if df.empty or "asof_date" not in df.columns:
            continue
        for _, row in df.iterrows():
            asof = str(row["asof_date"])
            rows_by_date.setdefault(asof, {})[provider] = row

    # Merge: filter to [start, end] then pick lowest-precedence provider.
    merged_rows: list[pd.Series] = []
    provenance: dict[str, str] = {}
    for asof_date in sorted(rows_by_date.keys()):
        if not (start <= asof_date <= end):
            continue
        candidates = rows_by_date[asof_date]
        winner_provider = min(
            candidates.keys(),
            key=lambda p: _SOURCE_PRECEDENCE_MARKET_DATA.get(p, 99),
        )
        merged_rows.append(candidates[winner_provider])
        provenance[asof_date] = winner_provider

    if not merged_rows:
        return pd.DataFrame(), {}

    merged_df = pd.DataFrame(merged_rows).reset_index(drop=True)
    return merged_df, provenance


_OHLCV_LOWER_NAMES = frozenset({"open", "high", "low", "close", "volume"})


def _file_mtime_ns(path: Path) -> int:
    """Return file mtime in nanoseconds when available, else seconds * 1e9.

    Codex R4 Minor #2: on filesystems with coarse-timestamp resolution
    (FAT32, older NFS without sub-second precision, post-rsync), a real
    legacy refresh can tie with a Shape A file's mtime at second-level,
    causing the ``_backward_compat_rename`` default branch (Shape A wins)
    to fire when the legacy refresh should have won. Prefer
    ``st_mtime_ns`` (nanosecond precision; NTFS + modern POSIX
    filesystems support it) over ``st_mtime`` (seconds; float, FAT32
    coarse). The graceful fallback to ``st_mtime * 1_000_000_000``
    preserves V1 best-effort posture on platforms whose
    ``os.stat_result`` does not carry ``st_mtime_ns``.
    """
    stat = path.stat()
    if hasattr(stat, "st_mtime_ns"):
        return stat.st_mtime_ns
    return int(stat.st_mtime * 1_000_000_000)


def _normalize_ohlcv_column_case(df: pd.DataFrame) -> pd.DataFrame:
    """Map any-case OHLCV column names to lowercase Shape A names.

    Returns a renamed view (or the unchanged frame if no rename needed).

    Codex R3 Minor #1: factored out so the public
    ``normalize_legacy_dataframe`` short-circuit branch (frames that
    already have ``asof_date``) still normalizes mixed-shape OHLCV
    casing rather than passing capitalized names through untouched.

    Codex R4 Minor #1: restored case-insensitive matching. The R3 Minor
    #1 implementation pinned an exact title-case keyset (``Open``,
    ``High``, ``Low``, ``Close``, ``Volume``), which narrowed the prior
    case-insensitive normalization. Mixed-casing frames carrying e.g.
    ``CLOSE`` (uppercase), ``open`` (already lowercase mixed with
    capitalized siblings), or other casing variants from V2 broker
    integrations would leak through untouched. Non-OHLCV columns
    (e.g. ``Volume_DAILY``) are preserved as-is — only columns whose
    ``col.lower()`` is in the canonical OHLCV name set are renamed,
    and only when the lowercase form differs from the original.
    """
    rename_dict: dict[str, str] = {}
    for col in df.columns:
        if (
            isinstance(col, str)
            and col.lower() in _OHLCV_LOWER_NAMES
            and col != col.lower()
        ):
            rename_dict[col] = col.lower()
    return df.rename(columns=rename_dict) if rename_dict else df


def normalize_legacy_dataframe(df: pd.DataFrame) -> pd.DataFrame:
    """Convert a legacy DatetimeIndex archive into Shape A.

    **Public helper (promoted from `_normalize_legacy_dataframe` per Codex
    R2 Minor #1).** Cross-module callers (e.g. the Schwab market-data ladder
    at `swing/integrations/schwab/marketdata_ladder.py`) consume this to
    normalize yfinance-shaped fallback windows into Shape A before
    persisting via `write_window`. The underscore-prefixed alias is
    preserved for backward compatibility with existing test imports.

    Legacy archives produced by ``read_or_fetch_archive`` /
    ``_yf_download_window`` carry a DatetimeIndex (date-like, may be named
    ``Date`` after ``reset_index``) plus capitalized OHLCV columns
    (``Open``/``High``/``Low``/``Close``/``Volume``). Shape A (consumed by
    ``resolve_ohlcv_window``) requires:

      - an ``asof_date`` column (ISO ``YYYY-MM-DD`` string) — NOT an index;
      - lowercase OHLCV column names (``open``/``high``/``low``/``close``/
        ``volume``) matching ``_mk_window`` in tests + the
        ``write_window`` writer pattern from ``map_price_history_to_window``.

    Codex R1 Major #2 fix: prior to this helper, ``_backward_compat_rename``
    only renamed the legacy file without normalizing — Shape A's reader
    (``resolve_ohlcv_window``) requires an ``asof_date`` column and skips
    files without one at lines 360-362, so a real legacy archive became
    INVISIBLE post-rename. Idempotent on already-Shape-A frames.

    Codex R3 Minor #1 fix: a frame may carry ``asof_date`` AND capitalized
    OHLCV columns (mixed shape — e.g. a partial migration). The pre-R3
    short-circuit returned such a frame unchanged, leaking capitalized
    columns into Shape A consumers. Now we ALWAYS apply lowercase OHLCV
    column normalization, whether or not the date column needs work.
    """
    if "asof_date" in df.columns:
        # Already has asof_date — but still normalize OHLCV column casing
        # so mixed-shape frames (asof_date + capitalized OHLCV) emerge as
        # full Shape A.
        return _normalize_ohlcv_column_case(df)

    normalized = df.reset_index()
    # `reset_index` promotes the index to a column; the legacy yfinance shape
    # names it `Date` (or sometimes `index` for an unnamed DatetimeIndex). Find
    # the most date-like column and rename it `asof_date`.
    date_col: str | None = None
    for candidate in ("Date", "date", "index", "Datetime", "datetime"):
        if candidate in normalized.columns:
            date_col = candidate
            break
    if date_col is None:
        # Last-ditch: scan for the first column whose dtype is datetime-like.
        for col in normalized.columns:
            if pd.api.types.is_datetime64_any_dtype(normalized[col]):
                date_col = col
                break
    if date_col is None:
        raise ValueError(
            "normalize_legacy_dataframe: cannot identify date column; "
            f"available columns: {list(normalized.columns)!r}"
        )

    normalized = normalized.rename(columns={date_col: "asof_date"})
    # Coerce to ISO date string (mirrors `OhlcvBar.asof_date` + Shape A
    # convention used elsewhere in this module).
    normalized["asof_date"] = (
        pd.to_datetime(normalized["asof_date"]).dt.date.astype(str)
    )

    # Normalize OHLCV column case to lowercase to match Shape A. Shared
    # with the asof_date short-circuit branch (Codex R3 Minor #1).
    normalized = _normalize_ohlcv_column_case(normalized)

    return normalized.reset_index(drop=True)


# Codex R2 Minor #1: backward-compat alias. Existing tests + the Schwab
# market-data ladder import the underscore-prefixed name; preserve it so we
# don't churn unrelated call sites. New code should use the public name.
_normalize_legacy_dataframe = normalize_legacy_dataframe


def _backward_compat_rename(ticker: str, *, cache_dir: Path) -> None:
    """One-shot **non-destructive** replication of legacy `{TICKER}.parquet`
    → Shape A `{TICKER}.yfinance.parquet`.

    Per Codex R2 Major #1 — V1 LEAVES the legacy `{TICKER}.parquet` file
    IN PLACE because ``read_or_fetch_archive`` (consumed by
    ``swing/prices.py``, ``swing/pipeline/ohlcv.py``, and
    ``swing/trades/daily_management.py``) reads ONLY the legacy path. A
    destructive migration (unlink legacy / orphan-rename) would leave V1
    chart-rendering, daily-management ATR computation, and PriceFetcher
    callers without any archive on disk — they would refetch from yfinance
    on every read, defeating the cache. The plan §H.6.3 "rename" terminology
    is preserved for stability of the symbol; semantics are **copy** in V1.

    **V1 → V2 transition path:** when all consumers of
    ``read_or_fetch_archive`` have been refactored to consume the Shape A
    resolver (``resolve_ohlcv_window``), V2 can drop the legacy parquet via
    ``os.remove(old_path)`` in a one-shot cleanup pass. See V2 candidate
    banked in the M#1 R2 fix commit body.

    Per plan §H.6.3 + Codex R1 Major #6 + R1 Major #2 + R2 Minor #1 +
    R2 Major #1 + R3 Major #1 — handles 4 cases without data loss:

    1. **old-only:** `{TICKER}.parquet` exists, `{TICKER}.yfinance.parquet`
       absent → NORMALIZE legacy DatetimeIndex/capitalized shape to Shape A
       (asof_date column + lowercase OHLCV) via
       ``normalize_legacy_dataframe``, write to
       ``{TICKER}.yfinance.parquet``. **LEGACY FILE LEFT IN PLACE** (Codex
       R2 Major #1; both files coexist during V1 read-path co-existence).
       Codex R1 Major #2 fix: previously a bare ``os.replace`` would have
       left the post-rename file invisible to ``resolve_ohlcv_window``.
    2. **both-exist:** MERGE-PRESERVING-BOTH with **mtime-based freshness
       winner** (Codex R3 Major #1). Read both, normalize the legacy one
       first, then concat-dedupe on ``asof_date`` keyed on which file is
       fresher by ``Path.stat().st_mtime``:

         - If legacy ``{TICKER}.parquet`` mtime > Shape A mtime → legacy
           rows win on conflict (concat order ``[new_df, old_df]`` with
           ``keep='last'``). Closes the "yfinance corrections (e.g.,
           post-split adjustment) refreshed legacy but Shape A retains
           stale snapshot" window — legacy is the canonical archive under
           V1 copy-not-move, so a fresher legacy must propagate forward.
         - Otherwise (Shape A fresher or tied) → Shape A rows win on
           conflict (concat order ``[old_df, new_df]`` with
           ``keep='last'``). This was the pre-R3 default and remains the
           tie-breaker.

       Trade-off: mtime-based pick assumes the filesystem preserves mtime
       correctly. Edge cases like rsync/git checkout zeroing mtimes are
       banked as V2 candidate; in V1 copy-not-move mode the worst-case
       outcome of a wrong mtime read is "Shape A retains its own value"
       — the same posture we had before R3, never worse.
       **LEGACY FILE LEFT IN PLACE** (Codex R2 Major #1; quarantine/orphan
       step removed because both files coexist during V1).
    3. **new-only:** already migrated; no-op.
    4. **neither:** no historical data; no-op.

    Idempotent: invoking twice on the same ticker drives the post-state to
    case 2 (both-exist) on the second call, which merges the legacy file
    with the existing Shape A file (no-op if both already contain the same
    content) without losing rows.
    """
    cache_dir = Path(cache_dir)
    ticker_u = ticker.upper()
    old_path = cache_dir / f"{ticker_u}.parquet"
    new_path = cache_dir / f"{ticker_u}.yfinance.parquet"

    old_exists = old_path.exists()
    new_exists = new_path.exists()

    if old_exists and not new_exists:
        # Read + normalize + write to Shape A path. **LEGACY FILE LEFT IN PLACE**
        # (Codex R2 Major #1 copy-not-move) — read_or_fetch_archive still
        # consumes the legacy path under V1.
        old_df = pd.read_parquet(old_path)
        normalized = normalize_legacy_dataframe(old_df)
        _write_archive_atomic(new_path, normalized)
        return

    if old_exists and new_exists:
        # MERGE-PRESERVING-BOTH — preserve every row across both files.
        # Normalize legacy first. **LEGACY FILE LEFT IN PLACE** (Codex R2
        # Major #1 copy-not-move) — no quarantine/orphan; both files coexist.
        old_df = pd.read_parquet(old_path)
        new_df = pd.read_parquet(new_path)
        old_df = normalize_legacy_dataframe(old_df)
        new_df = normalize_legacy_dataframe(new_df)  # idempotent on Shape A

        # Codex R3 Major #1: mtime-based freshness winner. Under V1
        # copy-not-move, the legacy parquet may have been refreshed more
        # recently than Shape A (yfinance refresh post-split, etc.). In
        # that case legacy rows MUST win on `asof_date` conflicts —
        # otherwise Shape A retains stale values forever and yfinance
        # corrections never propagate into the resolver's read path.
        # `Path.stat().st_mtime` is the standard freshness signal; ties
        # fall through to Shape-A-wins (the pre-R3 default, preserved
        # as the tie-breaker for backward compatibility).
        #
        # V1 trade-off note (Codex R4 M#1 ACCEPT-WITH-RATIONALE):
        # File-level mtime is a coarse signal — it cannot distinguish
        # per-row freshness. Consequence: if legacy is touched by a
        # partial refresh, the file-level mtime check lets legacy win
        # for EVERY overlapping asof_date, including rows the refresh
        # did not touch (so Shape A's newer-of-truth values for those
        # rows get rolled back). This is the inverse failure of pre-R3
        # (Shape-A-always-wins → stale Shape A when legacy is refreshed).
        # Under V1, the impact is BOUNDED because:
        #   - read_or_fetch_archive consumers read legacy directly (so
        #     Shape A merge state does not affect their reads).
        #   - Shape A consumers (Sub-bundle C ladder + Phase 10 metrics
        #     if any) see a deterministic merge state that may diverge
        #     from per-row truth.
        # V2 resolves both directions by adding a per-row `recorded_at`
        # column to both archives; the merge then picks per-row winner
        # by `recorded_at` rather than file-level mtime. Tracked as V2
        # candidate in the Sub-bundle C return report (per-row
        # `recorded_at` column already banked from R3 M#1 review).
        #
        # Codex R4 Minor #2: use `_file_mtime_ns` (nanosecond precision
        # where the OS / filesystem supports it) so a real legacy
        # refresh that lands within the same wall-clock second as the
        # Shape A write still wins on conflict. Coarse-timestamp
        # filesystems (FAT32 / older NFS / post-rsync) fall back to
        # the float-mtime-times-1e9 approximation — V1 best-effort.
        try:
            old_mtime = _file_mtime_ns(old_path)
            new_mtime = _file_mtime_ns(new_path)
        except OSError:  # pragma: no cover — defensive
            old_mtime = 0
            new_mtime = 0
        if old_mtime > new_mtime:
            # Legacy fresher → legacy rows win on conflict.
            merged = pd.concat([new_df, old_df]).drop_duplicates(
                subset=["asof_date"], keep="last"
            )
        else:
            # Shape A fresher or tied → Shape A rows win on conflict.
            merged = pd.concat([old_df, new_df]).drop_duplicates(
                subset=["asof_date"], keep="last"
            )
        merged = merged.sort_values("asof_date").reset_index(drop=True)

        # Idempotency optimization: if the existing Shape A file already
        # contains every row that the merge produces (i.e. the legacy file's
        # asof_dates are a subset of the new file's), skip the rewrite to
        # preserve mtime + avoid spurious churn. Compare by content hash.
        try:
            existing_sorted = new_df.sort_values("asof_date").reset_index(drop=True)
            if existing_sorted.equals(merged):
                return
        except (ValueError, TypeError):  # pragma: no cover — defensive
            pass

        _write_archive_atomic(new_path, merged)
        return

    # Cases 3 + 4: new-only or neither — no-op.
    return

exec
/bin/bash -lc "sed -n '250,380p' .codex-diff.txt" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
+            "Open": [c - 0.05 for c in closes],
+            "High": [c + 0.30 for c in closes],
+            "Low": [c - 0.30 for c in closes],
+            "Close": closes,
+            "Volume": [1_000_000 + i for i in range(n_rows)],
+        },
+        index=idx,
+    )
+
+
+def _short_schwab_window(ticker: str, end: pd.Timestamp, n_days: int = 16):
+    """A SHORT Schwab window — the real XMAX shape (16 daily Schwab bars).
+    Derived from the real ``XMAX.schwab_api.parquet`` schema (asof_date +
+    lowercase OHLCV, here as OhlcvBar rows)."""
+    days = pd.bdate_range(end=end, periods=n_days)
+    bars = [
+        OhlcvBar(
+            asof_date=d.date().isoformat(),
+            open=50.0 + i,
+            high=51.0 + i,
+            low=49.0 + i,
+            close=50.5 + i,
+            volume=10_000 + i,
+        )
+        for i, d in enumerate(days)
+    ]
+    return SchwabPriceHistoryWindow(ticker=ticker, bars=bars, provider="schwab_api")
+
+
+def test_bars_hook_schwab_success_returns_full_archive_not_short_window(
+    tmp_path: Path, monkeypatch,
+):
+    """Hypothesis (a) fix + discriminating convergence regression.
+
+    The ladder stub returns a 16-bar Schwab window (the real XMAX shape) on the
+    schwab_api SUCCESS path; a 1200-row legacy archive is the on-disk truth
+    (here injected via a stubbed ``read_or_fetch_archive``). The pipeline ladder
+    (thumbnail) path and the bare no-ladder (ticker_detail) path must converge on
+    the SAME bar count over a 300-day window.
+
+    Pre-fix: ``_bars_hook`` returns ``window.to_dataframe()`` (16 rows) → the
+    ladder path's ``get_or_fetch`` returns ~16 bars while the no-ladder path
+    returns ~207 → divergence; ``len(ladder_df) > 60`` FAILS.
+
+    Post-fix: ``_bars_hook`` re-reads the FULL archive after the ladder
+    fetch+persist → both paths return identical counts; ``len(ladder_df) > 60``
+    passes and the two counts are equal.
+    """
+    from swing.pipeline.runner import _install_pipeline_marketdata_caches
+    from swing.web.ohlcv_cache import OhlcvCache
+
+    db_path = tmp_path / "arc3.db"
+    ensure_schema(db_path).close()
+    cfg = _make_pipeline_cfg(db_path, tmp_path)
+
+    end = pd.Timestamp("2026-06-08")
+    legacy = _full_legacy_archive(end=end, n_rows=1200)
+
+    def _stub_read_or_fetch_archive(ticker, *, end_date, cache_dir, archive_history_days):
+        return legacy.loc[legacy.index.date <= end_date].copy()
+
+    def _stub_session(_now_dt):
+        return end.date()
+
+    # The full archive read used by BOTH the post-fix ladder hook (runner
+    # binding) and the no-ladder path (ohlcv_cache binding). Stubbed identically
+    # so any count divergence is attributable to the hook, not the data source.
+    monkeypatch.setattr(
+        "swing.pipeline.runner.read_or_fetch_archive", _stub_read_or_fetch_archive,
+    )
+    monkeypatch.setattr(
+        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub_read_or_fetch_archive,
+    )
+    monkeypatch.setattr(
+        "swing.pipeline.runner.last_completed_session", _stub_session,
+    )
+    monkeypatch.setattr(
+        "swing.web.ohlcv_cache.last_completed_session", _stub_session,
+    )
+
+    # Ladder stub: a SCHWAB SUCCESS returning only the short 16-bar window.
+    def _stub_ladder_schwab_success(ticker, **_kwargs):
+        return (_short_schwab_window(ticker, end=end, n_days=16), "schwab_api")
+
+    monkeypatch.setattr(
+        "swing.integrations.schwab.marketdata_ladder.fetch_window_via_ladder",
+        _stub_ladder_schwab_success,
+    )
+
+    # Ladder (thumbnail) path.
+    _price, ladder_cache, audit_conn = _install_pipeline_marketdata_caches(
+        cfg, MagicMock(), pipeline_run_id=1,
+    )
+    assert ladder_cache is not None
+    assert ladder_cache._ladder_bars_fetcher is not None
+    ladder_df = ladder_cache.get_or_fetch(ticker="XMAX", window_days=300)
+
+    # No-ladder (ticker_detail) path — same cfg, NO ladder installed.
+    plain_cache = OhlcvCache(cfg)
+    plain_df = plain_cache.get_or_fetch(ticker="XMAX", window_days=300)
+
+    # Post-fix: the ladder/thumbnail path is NOT capped at the 16-bar Schwab
+    # window — it reflects the full archive sliced to 300 calendar days.
+    assert len(ladder_df) > 60, (
+        f"ladder bars hook returned too few rows ({len(ladder_df)}); the "
+        f"schwab_api success path truncated to the short Schwab window instead "
+        f"of returning the full archive (full-archive-return contract)."
+    )
+    # Discriminating convergence regression: the thumbnail (ladder) and
+    # ticker_detail (no-ladder) inputs MUST be the same bar count for a ticker
+    # with a partial Schwab Shape-A file + a full legacy archive.
+    assert len(ladder_df) == len(plain_df), (
+        f"thumbnail (ladder) path {len(ladder_df)} bars != ticker_detail "
+        f"(no-ladder) path {len(plain_df)} bars — surfaces diverge for the same "
+        f"data_asof_date."
+    )
+    if audit_conn is not None:
+        audit_conn.close()
diff --git a/tests/pipeline/test_step_charts_chart_renders_write_through.py b/tests/pipeline/test_step_charts_chart_renders_write_through.py
index f9cf7940..0dcba8f2 100644
--- a/tests/pipeline/test_step_charts_chart_renders_write_through.py
+++ b/tests/pipeline/test_step_charts_chart_renders_write_through.py
@@ -442,6 +442,48 @@ def test_step_charts_chart_renders_write_through_is_idempotent(pipeline_db):
 # ===========================================================================
 
 
+def test_step_charts_stamps_content_derived_source_data_hash(pipeline_db):
+    """Phase 16 Arc 3 (3c): `_step_charts` MUST stamp a content-derived
+    ``source_data_hash`` (bar count + first/last asof_date) on the persisted
+    chart_renders row, NOT the legacy static literal ``"step_charts_v1"``.
+

exec
/bin/bash -lc "sed -n '380,560p' .codex-diff.txt" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
+
+    Discriminator: the watchlist_row stub returns a known 130-bar frame; the
+    persisted hash must equal ``compute_chart_source_hash`` of that frame and
+    encode its bar count. Pre-fix the row carried ``"step_charts_v1"``.
+    """
+    from swing.web.charts import compute_chart_source_hash
+
+    cfg, run_id, eval_run_id = pipeline_db
+    conn = connect(cfg.paths.db_path)
+    try:
+        with conn:
+            _seed_watchlist_row(conn, ticker="HSH1")
+    finally:
+        conn.close()
+
+    bars = _make_bars(periods=130)
+    _run_step_charts(
+        cfg=cfg, run_id=run_id, eval_run_id=eval_run_id,
+        ohlcv_cache=_StubOhlcvCache(frames={"HSH1": bars}),
+    )
+
+    conn = connect(cfg.paths.db_path)
+    try:
+        rows = list_chart_renders(
+            conn, ticker="HSH1", surface="watchlist_row",
+            pipeline_run_id=run_id,
+        )
+    finally:
+        conn.close()
+    assert len(rows) == 1
+    persisted = rows[0].source_data_hash
+    assert persisted != "step_charts_v1", (
+        "source_data_hash is still the static literal; 3c not applied"
+    )
+    assert persisted == compute_chart_source_hash(bars)
+    assert "bars=130" in persisted
+
+
 def test_step_charts_populates_three_pregen_surfaces_in_one_run(pipeline_db):
     """§1.5.1 amended by Phase 13 T-T4.SB.3 OQ-5.3 LOCK: `_step_charts`
     pre-gens THREE surfaces (market_weather + watchlist_row +
diff --git a/tests/web/test_chart_jit.py b/tests/web/test_chart_jit.py
index 8fa5d96d..5b0ad7ca 100644
--- a/tests/web/test_chart_jit.py
+++ b/tests/web/test_chart_jit.py
@@ -127,6 +127,47 @@ def test_get_or_render_surface_cache_miss_renders_via_ohlcv_and_writes_through(
     assert bytes(cached[0]) == b"<svg>rendered</svg>"
 
 
+def test_get_or_render_surface_stamps_content_derived_source_data_hash(
+    conn: sqlite3.Connection, pipeline_run_id: int,
+) -> None:
+    """Phase 16 Arc 3 (3c): the JIT write-through stamps a content-derived
+    ``source_data_hash`` (bar count + first/last asof_date) instead of the
+    static ``chart_jit_v1`` default, so the cached row's provenance changes on
+    data growth."""
+    from swing.web.charts import compute_chart_source_hash
+
+    bars = _planted_bars_df()
+    ohlcv_cache = MagicMock()
+    ohlcv_cache.get_or_fetch.return_value = bars
+    import swing.web.chart_jit as mod
+
+    mod._RENDERERS["ticker_detail"] = MagicMock(
+        return_value=b"<svg>rendered</svg>",
+    )
+    try:
+        get_or_render_surface(
+            conn=conn, ohlcv_cache=ohlcv_cache,
+            surface="ticker_detail", ticker="UCTT",
+            pipeline_run_id=pipeline_run_id,
+            data_asof_date="2026-05-22",
+        )
+    finally:
+        importlib.reload(mod)
+
+    row = conn.execute(
+        "SELECT source_data_hash FROM chart_renders "
+        "WHERE surface = 'ticker_detail' AND ticker = 'UCTT' "
+        "  AND pipeline_run_id = ?",
+        (pipeline_run_id,),
+    ).fetchone()
+    assert row is not None
+    assert row[0] != "chart_jit_v1", (
+        "JIT write-through still stamps the static literal; 3c not applied"
+    )
+    assert row[0] == compute_chart_source_hash(bars)
+    assert "bars=60" in row[0]
+
+
 def test_get_or_render_surface_fetches_min_calendar_days_for_ma200(
     conn: sqlite3.Connection, pipeline_run_id: int,
 ) -> None:
diff --git a/tests/web/test_chart_source_hash.py b/tests/web/test_chart_source_hash.py
new file mode 100644
index 00000000..be938955
--- /dev/null
+++ b/tests/web/test_chart_source_hash.py
@@ -0,0 +1,63 @@
+"""Phase 16 / Arc 3 (3c) — content-derived ``chart_renders.source_data_hash``.
+
+The prior write sites stamped a STATIC literal (``"step_charts_v1"`` /
+``"chart_jit_v1"``) which does not encode the underlying data, so the audit
+field never changes when the chart's bar history grows or its window shifts.
+``compute_chart_source_hash`` keys the value on (bar count + first/last
+asof_date) so data growth yields a different hash.
+"""
+from __future__ import annotations
+
+import pandas as pd
+
+from swing.web.charts import compute_chart_source_hash
+
+
+def _frame(end: str, n: int) -> pd.DataFrame:
+    idx = pd.bdate_range(end=pd.Timestamp(end), periods=n)
+    closes = [100.0 + i for i in range(n)]
+    return pd.DataFrame(
+        {
+            "Open": closes,
+            "High": [c + 1 for c in closes],
+            "Low": [c - 1 for c in closes],
+            "Close": closes,
+            "Volume": [1000 + i for i in range(n)],
+        },
+        index=idx,
+    )
+
+
+def test_identical_frames_produce_identical_hash():
+    a = _frame("2026-06-08", 120)
+    b = _frame("2026-06-08", 120)
+    assert compute_chart_source_hash(a) == compute_chart_source_hash(b)
+
+
+def test_data_growth_changes_hash():
+    """The XMAX symptom substrate: a sparse frame and a rich frame for the
+    same ticker MUST hash differently so the cached row is not silently
+    treated as unchanged."""
+    sparse = _frame("2026-06-08", 16)
+    rich = _frame("2026-06-08", 207)
+    assert compute_chart_source_hash(sparse) != compute_chart_source_hash(rich)
+
+
+def test_window_shift_changes_hash():
+    """Same bar count, later last session → different hash."""
+    earlier = _frame("2026-06-01", 120)
+    later = _frame("2026-06-08", 120)
+    assert compute_chart_source_hash(earlier) != compute_chart_source_hash(later)
+
+
+def test_empty_frame_is_stable_and_distinct():
+    empty = pd.DataFrame(columns=["Open", "High", "Low", "Close", "Volume"])
+    assert compute_chart_source_hash(empty) == compute_chart_source_hash(empty)
+    assert compute_chart_source_hash(empty) != compute_chart_source_hash(
+        _frame("2026-06-08", 1)
+    )
+
+
+def test_hash_is_not_the_old_static_literal():
+    h = compute_chart_source_hash(_frame("2026-06-08", 207))
+    assert h not in ("step_charts_v1", "chart_jit_v1")

codex
The new reproduction test does exercise the installed `_bars_hook` through `OhlcvCache.get_or_fetch`, but it replaces both `read_or_fetch_archive` bindings and the ladder call. That makes it discriminating for the old `window.to_dataframe()` bug, but it does not cover “Schwab-only, no legacy archive” behavior or archive resolver parity.
exec
/bin/bash -lc "sed -n '700,900p' swing/data/ohlcv_archive.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
            try:
                if cohort == "gap":
                    _merge_gap_subframe(cache_dir, ticker, sub,
                                        archive_history_days=archive_history_days)
                else:
                    _write_full_refresh_subframe(
                        cache_dir, ticker, sub, today_session=today_session,
                        archive_history_days=archive_history_days)
            except Exception as exc:  # noqa: BLE001 — per-ticker merge fault -> fallback
                log.warning("warm merge failed for %s -> fallback: %s", ticker, exc)
                report.fallback.append(ticker)
        if inter_chunk_pause_s and i < len(chunks) - 1:
            time.sleep(inter_chunk_pause_s)


def read_or_fetch_archive(
    ticker: str,
    *,
    end_date: date,
    cache_dir: Path,
    archive_history_days: int,
) -> pd.DataFrame | None:
    """Read the per-ticker archive, refreshing from yfinance as needed,
    return rows ≤ end_date.

    Args:
        ticker: ticker symbol; used as the archive filename stem.
        end_date: caller's window end (inclusive). Used ONLY for the return
            slice — the on-disk archive always extends to
            `_last_completed_session_today()` regardless. Historical callers
            receive a slice; they do not truncate the archive.
        cache_dir: archive directory (typically `cfg.paths.prices_cache_dir`).
            Must already exist.
        archive_history_days: full-history fetch window (typically
            `cfg.archive.archive_history_days`).

    Returns:
        DataFrame indexed by date with OHLCV columns; rows ≤ end_date.
        None if yfinance returns empty (delisted / invalid ticker / no history).
    """
    cache_dir = Path(cache_dir)
    ticker = ticker.upper()
    cache_dir.mkdir(parents=True, exist_ok=True)
    parquet_path, meta_path = _archive_paths(cache_dir, ticker)

    today = _last_completed_session_today()

    archive = _read_archive(parquet_path)
    meta = _read_meta(meta_path)

    last_full_refresh: date | None = None
    last_full_str = meta.get("last_full_refresh_date")
    if last_full_str:
        try:
            last_full_refresh = date.fromisoformat(last_full_str)
        except ValueError:
            last_full_refresh = None

    if archive is None or archive.empty or last_full_refresh is None:
        needs_full_refresh = True
    else:
        needs_full_refresh = _full_refresh_due(
            ticker, last_full_refresh, today,
            stagger_enabled=_full_refresh_stagger_enabled(),
        )

    if needs_full_refresh:
        full_calendar_days = _calendar_window_for_trading_days(archive_history_days)
        full_start = today - timedelta(days=full_calendar_days)
        fetched = _yf_download_window(ticker, start=full_start, end=today)
        if fetched.empty:
            # Codex R2 Major 2 — established ticker + transient empty upstream:
            # do NOT overwrite archive or meta. Fall back to the existing archive
            # so callers don't see a transient yfinance hiccup as data absence.
            # Meta stays stale so the next call retries the weekly refresh.
            if archive is not None and not archive.empty:
                return archive.loc[archive.index.date <= end_date]
            return None
        fetched = fetched.tail(archive_history_days)
        _write_archive_atomic(parquet_path, fetched)
        _write_meta_atomic(meta_path, {"last_full_refresh_date": today.isoformat()})
        return fetched.loc[fetched.index.date <= end_date]

    assert archive is not None
    latest_stored: date = archive.index.max().date()
    if latest_stored < today:
        gap_start = latest_stored + timedelta(days=1)
        gap = _yf_download_window(ticker, start=gap_start, end=today)
        if not gap.empty:
            combined = pd.concat([archive, gap])
            combined = combined[~combined.index.duplicated(keep="last")].sort_index()
            combined = combined.tail(archive_history_days)
            _write_archive_atomic(parquet_path, combined)
            archive = combined

    return archive.loc[archive.index.date <= end_date]


# ---------------------------------------------------------------------------
# Shape A persistence (Schwab API Sub-bundle C T-C.2 / plan §H.6.3)
# ---------------------------------------------------------------------------
#
# Files: `{cache_dir}/{TICKER}.{PROVIDER}.parquet` with an explicit
# `asof_date` column (ISO `YYYY-MM-DD` string per
# `swing.integrations.schwab.models.OhlcvBar.asof_date`) plus OHLCV columns.
# Coexists with the legacy `{TICKER}.parquet` shape consumed by
# `read_or_fetch_archive` above; the one-shot `_backward_compat_rename`
# migrates the legacy file to `{TICKER}.yfinance.parquet`.
#
# `resolve_ohlcv_window` is the read path consumed by the Sub-bundle C
# ladder (`swing/integrations/schwab/marketdata_ladder.py` — T-C.3); reads
# both per-provider files (if present), filters to the caller's
# [start, end] window, then picks the highest-priority row per asof_date.


def _shape_a_path(cache_dir: Path, ticker: str, provider: str) -> Path:
    """Return the Shape A parquet path for `(ticker, provider)`.

    Ticker is uppercased here so callers can pass lowercase without
    silently splitting the cache (mirrors the existing helper's
    discipline at `read_or_fetch_archive`).
    """
    return cache_dir / f"{ticker.upper()}.{provider}.parquet"


def write_window(
    ticker: str,
    window: pd.DataFrame | None,
    provider: str,
    *,
    cache_dir: Path,
) -> None:
    """Atomically write a Shape A window, merging with any pre-existing rows by
    `asof_date` (keep='last'). The hard completed-day strip lives in
    `_write_archive_atomic` (every write inherits it); this function adds the
    merge + the M3 guarantee (rewrite existing on an empty incoming so a
    pre-existing on-disk partial is stripped) + F6 (never blank valid history
    on a transient empty fetch).

    Merge semantics: the incoming window is concatenated with the existing
    parquet (if any), deduped on `asof_date` with `keep='last'` (NEW rows win
    on conflict), sorted ascending, then atomically written back -- preserving
    rows OUTSIDE the fetched window (e.g. a 1260-row archive is not clobbered
    by a 60-row ladder fetch).

    F6 (CLAUDE.md "External-API empty-result must be treated as transient"): an
    empty/None incoming window NEVER blanks valid (<= cutoff) history. It only
    triggers a rewrite when the on-disk file still carries a > cutoff partial
    (M3) -- the rewrite re-runs the atomic strip and drops that partial.
    """
    cache_dir = Path(cache_dir)
    cache_dir.mkdir(parents=True, exist_ok=True)
    path = _shape_a_path(cache_dir, ticker, provider)
    cutoff_iso = _last_completed_session_today().isoformat()

    # --- normalize incoming to a DataFrame-or-None ---
    incoming: pd.DataFrame | None = window
    if incoming is not None and not isinstance(incoming, pd.DataFrame):
        try:
            is_empty = len(incoming) == 0
        except TypeError:
            raise TypeError(
                f"write_window expects pd.DataFrame, got {type(window).__name__}"
            ) from None
        if is_empty:
            incoming = None
        else:
            raise TypeError(
                f"write_window expects pd.DataFrame, got {type(window).__name__}")
    if isinstance(incoming, pd.DataFrame) and incoming.empty:
        incoming = None

    # --- read existing ---
    existing: pd.DataFrame | None = None
    if path.exists():
        try:
            existing = pd.read_parquet(path)
        except (OSError, ValueError) as exc:  # pragma: no cover - defensive
            log.warning("write_window: failed to read existing %s (%s)", path, exc)
            existing = None

    if incoming is None and existing is None:
        return

    # Empty incoming: the only work is M3 -- strip a pre-existing on-disk
    # partial. SHAPE-AGNOSTIC (Codex R1 MAJOR #2): handle a DatetimeIndex/legacy
    # `existing` too, not just Shape-A. F6: an empty fetch NEVER blanks valid
    # (<= cutoff) history -- rewrite ONLY when a > cutoff partial is present.
    if incoming is None:
        assert existing is not None  # the both-None case returned above
        if len(_strip_incomplete_sessions(existing, cutoff_iso)) == len(existing):
            return  # no partial -> no-op
        _write_archive_atomic(path, existing)  # the barrier strips the partial
        return

    # legacy REPLACE for non-Shape-A incoming (dedup needs the asof_date key);
    # the atomic strip still fires inside _write_archive_atomic.
    if "asof_date" not in incoming.columns:
        _write_archive_atomic(path, incoming)
        return


exec
/bin/bash -lc "rg -n \"get_cached_chart_svg\\(|min_data_asof_date|source_data_hash\" swing tests | head -n 200" in /mnt/c/Users/rwsmy/swing-arc3
exec
/bin/bash -lc 'rg -n "class ChartRender|def __post_init__" swing/data/models.py' in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
304:    def __post_init__(self) -> None:
360:    def __post_init__(self) -> None:
763:    def __post_init__(self) -> None:
1087:    def __post_init__(self) -> None:
1214:    def __post_init__(self) -> None:
1442:    def __post_init__(self) -> None:
1498:    def __post_init__(self) -> None:
1593:    def __post_init__(self) -> None:
1785:    def __post_init__(self) -> None:
1979:    def __post_init__(self) -> None:
1988:class ChartRender:
2007:    def __post_init__(self) -> None:
2097:    def __post_init__(self) -> None:
2128:    def __post_init__(self) -> None:
2174:    def __post_init__(self) -> None:
2215:    def __post_init__(self) -> None:

exec
/bin/bash -lc "rg -n \"source_data_hash\" swing tests | sed -n '1,220p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 1175ms:
swing/web/view_models/watchlist.py:226:                    svg = get_cached_chart_svg(
swing/pipeline/runner.py:3426:                     bytes_: bytes, source_data_hash: str) -> None:
swing/pipeline/runner.py:3433:                source_data_hash=source_data_hash,
swing/pipeline/runner.py:3490:            source_data_hash=compute_chart_source_hash(bars),
swing/pipeline/runner.py:3532:                source_data_hash=compute_chart_source_hash(bars),
swing/pipeline/runner.py:3590:                source_data_hash=compute_chart_source_hash(display_bars),
swing/web/view_models/trades.py:1901:            position_chart_svg_bytes = get_cached_chart_svg(
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:32:    source_data_hash TEXT NOT NULL,
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:48:        pattern_class, chart_svg_bytes, source_data_hash, rendered_at,
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:52:        pipeline_run_id, pattern_class, chart_svg_bytes, source_data_hash,
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:192:    source_data_hash TEXT NOT NULL,
swing/pipeline/detection_chart_capture.py:37:            chart_svg_bytes=svg, source_data_hash="detection_capture_v1",
swing/web/view_models/patterns/exemplars.py:318:                            source_data_hash="exemplar_live_render",
swing/web/view_models/open_positions_row.py:370:    position_chart_svg_bytes = get_cached_chart_svg(
tests/web/test_weather_trend_state.py:232:                source_data_hash="hash-w",
tests/web/test_watchlist_jit_anchor_r2.py:132:                source_data_hash="t-anchor-r2",
swing/data/repos/chart_renders.py:26:    "source_data_hash",
swing/data/repos/chart_renders.py:42:        source_data_hash=row[6],
swing/data/repos/chart_renders.py:61:             chart_svg_bytes, source_data_hash, rendered_at, data_asof_date)
swing/data/repos/chart_renders.py:70:            chart_render.source_data_hash,
swing/data/repos/chart_renders.py:141:def get_cached_chart_svg(
swing/data/repos/chart_renders.py:148:    min_data_asof_date: str | None = None,
swing/data/repos/chart_renders.py:161:    ``min_data_asof_date`` is the staleness predicate; pass
swing/data/repos/chart_renders.py:164:    ``data_asof_date >= min_data_asof_date``. If ``min_data_asof_date`` is
swing/data/repos/chart_renders.py:185:    if min_data_asof_date is not None:
swing/data/repos/chart_renders.py:187:        params.append(min_data_asof_date)
tests/web/test_watchlist_expand_single_pipeline_anchor.py:127:                source_data_hash="t-anchor",
tests/web/test_watchlist_expand_collapse_preserves_thumbnail.py:54:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
swing/data/models.py:2001:    source_data_hash: str
tests/cli/test_diagnose_prune_chart_cache.py:32:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_chart_source_hash.py:1:"""Phase 16 / Arc 3 (3c) — content-derived ``chart_renders.source_data_hash``.
swing/web/view_models/dashboard.py:746:    ticker_detail_chart_svg_bytes = get_cached_chart_svg(
swing/web/view_models/dashboard.py:880:                dashboard_weather_chart_svg_bytes = get_cached_chart_svg(
swing/web/view_models/dashboard.py:897:                    svg = get_cached_chart_svg(
swing/web/view_models/dashboard.py:907:                    svg = get_cached_chart_svg(
tests/data/test_v23_migration.py:36:    "source_data_hash, rendered_at, data_asof_date"
tests/data/test_v23_migration.py:253:            source_data_hash="h",
tests/data/test_v20_migration.py:160:        "source_data_hash",
tests/data/test_v20_migration.py:493:        source_data_hash="hash",
tests/data/test_v20_migration.py:506:            source_data_hash="hash",
tests/data/test_v20_migration.py:520:            source_data_hash="hash",
tests/web/test_chart_jit.py:70:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_chart_jit.py:130:def test_get_or_render_surface_stamps_content_derived_source_data_hash(
tests/web/test_chart_jit.py:134:    ``source_data_hash`` (bar count + first/last asof_date) instead of the
tests/web/test_chart_jit.py:158:        "SELECT source_data_hash FROM chart_renders "
tests/web/test_chart_jit.py:421:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_view_models/test_hyp_recs_jit_fallback.py:130:                "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_open_positions_expand_route.py:63:        chart_svg_bytes=svg, source_data_hash="hash-pd",
tests/web/test_open_positions_expand_chart.py:8:ZERO-WRITE: the only DB read is `get_cached_chart_svg(... pipeline_run_id=None)`
tests/web/test_open_positions_expand_chart.py:61:        chart_svg_bytes=svg, source_data_hash="hash-pd",
swing/web/routes/dashboard.py:165:            source_data_hash="manual_refresh",
swing/web/chart_jit.py:82:    source_data_hash: str = "chart_jit_v1",
swing/web/chart_jit.py:100:    cached = get_cached_chart_svg(
swing/web/chart_jit.py:201:    # asof_date) instead of the static ``source_data_hash`` default so the cache
swing/web/chart_jit.py:207:        computed_hash = source_data_hash
swing/web/chart_jit.py:215:            source_data_hash=computed_hash,
tests/web/test_routes/test_watchlist_template_thumbnail.py:64:                source_data_hash="h",
swing/web/charts.py:79:    """Content-derived value for ``chart_renders.source_data_hash``.
tests/web/test_routes/test_patterns_exemplars_enhanced.py:117:                source_data_hash="h",
tests/web/test_routes/test_patterns_exemplars_enhanced.py:515:                source_data_hash="hg",
tests/web/test_routes/test_trades_detail_chart.py:57:                source_data_hash="h",
tests/web/test_routes/test_trades_detail_chart.py:107:                source_data_hash="h",
tests/web/test_routes/test_watchlist_jit_integration.py:55:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_routes/test_open_positions_expand.py:252:                source_data_hash="hash-pd",
tests/data/repos/test_chart_renders_repo.py:43:        "source_data_hash": "deadbeef",
tests/data/repos/test_chart_renders_repo.py:62:        "source_data_hash FROM chart_renders WHERE id = ?",
tests/data/repos/test_chart_renders_repo.py:240:    out = repo.get_cached_chart_svg(
tests/data/repos/test_chart_renders_repo.py:243:        min_data_asof_date="2024-02-01",
tests/data/repos/test_chart_renders_repo.py:249:    stale = repo.get_cached_chart_svg(
tests/data/repos/test_chart_renders_repo.py:252:        min_data_asof_date="2024-02-02",
tests/data/repos/test_chart_renders_repo.py:295:    out = repo.get_cached_chart_svg(
tests/data/repos/test_chart_renders_repo.py:347:                source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:364:            source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:383:            source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:410:            source_data_hash="x", rendered_at="2024-01-02T00:00:00",
tests/web/test_routes/test_recommendations_detail_chart.py:86:                source_data_hash="h",
tests/web/test_routes/test_recommendations_detail_chart.py:134:                source_data_hash="h",
tests/web/test_routes/test_dashboard_chart_integration.py:52:        source_data_hash="hash-w",
tests/web/test_routes/test_dashboard_chart_integration.py:161:                source_data_hash="h",
tests/web/test_routes/test_dashboard_chart_integration.py:217:                source_data_hash="h",
tests/web/test_routes/test_dashboard_chart_integration.py:370:        svg = get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:209:        svg = get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:240:        svg = get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:270:        svg = get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:293:        svg = get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:351:        svg_good = get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:355:        svg_empty = get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:445:def test_step_charts_stamps_content_derived_source_data_hash(pipeline_db):
tests/pipeline/test_step_charts_chart_renders_write_through.py:447:    ``source_data_hash`` (bar count + first/last asof_date) on the persisted
tests/pipeline/test_step_charts_chart_renders_write_through.py:479:    persisted = rows[0].source_data_hash
tests/pipeline/test_step_charts_chart_renders_write_through.py:481:        "source_data_hash is still the static literal; 3c not applied"
tests/pipeline/test_step_charts_chart_renders_write_through.py:510:        assert get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:515:        assert get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:520:        assert get_cached_chart_svg(
tests/pipeline/test_step_charts_chart_renders_write_through.py:525:        assert get_cached_chart_svg(
tests/integration/test_phase13_t4_sb_closer_e2e.py:200:                source_data_hash="h-wl",
tests/integration/test_phase13_t4_sb_closer_e2e.py:212:                source_data_hash="h-hr",
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:145:                source_data_hash="h",
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:156:                source_data_hash="h",

 succeeded in 1194ms:
tests/data/test_v23_migration.py:36:    "source_data_hash, rendered_at, data_asof_date"
tests/data/test_v23_migration.py:253:            source_data_hash="h",
tests/data/test_v20_migration.py:160:        "source_data_hash",
tests/data/test_v20_migration.py:493:        source_data_hash="hash",
tests/data/test_v20_migration.py:506:            source_data_hash="hash",
tests/data/test_v20_migration.py:520:            source_data_hash="hash",
tests/integration/test_phase13_t4_sb_closer_e2e.py:200:                source_data_hash="h-wl",
tests/integration/test_phase13_t4_sb_closer_e2e.py:212:                source_data_hash="h-hr",
swing/web/view_models/patterns/exemplars.py:318:                            source_data_hash="exemplar_live_render",
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:145:                source_data_hash="h",
tests/integration/test_phase13_t2_sb6b_closed_loop_e2e.py:156:                source_data_hash="h",
tests/cli/test_diagnose_prune_chart_cache.py:32:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_weather_trend_state.py:232:                source_data_hash="hash-w",
tests/web/test_chart_source_hash.py:1:"""Phase 16 / Arc 3 (3c) — content-derived ``chart_renders.source_data_hash``.
tests/pipeline/test_step_charts_chart_renders_write_through.py:445:def test_step_charts_stamps_content_derived_source_data_hash(pipeline_db):
tests/pipeline/test_step_charts_chart_renders_write_through.py:447:    ``source_data_hash`` (bar count + first/last asof_date) on the persisted
tests/pipeline/test_step_charts_chart_renders_write_through.py:479:    persisted = rows[0].source_data_hash
tests/pipeline/test_step_charts_chart_renders_write_through.py:481:        "source_data_hash is still the static literal; 3c not applied"
tests/web/test_watchlist_jit_anchor_r2.py:132:                source_data_hash="t-anchor-r2",
tests/web/test_watchlist_expand_single_pipeline_anchor.py:127:                source_data_hash="t-anchor",
tests/web/test_watchlist_expand_collapse_preserves_thumbnail.py:54:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_chart_jit.py:70:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_chart_jit.py:130:def test_get_or_render_surface_stamps_content_derived_source_data_hash(
tests/web/test_chart_jit.py:134:    ``source_data_hash`` (bar count + first/last asof_date) instead of the
tests/web/test_chart_jit.py:158:        "SELECT source_data_hash FROM chart_renders "
tests/web/test_chart_jit.py:421:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/web/test_open_positions_expand_route.py:63:        chart_svg_bytes=svg, source_data_hash="hash-pd",
tests/web/test_open_positions_expand_chart.py:61:        chart_svg_bytes=svg, source_data_hash="hash-pd",
tests/web/test_view_models/test_hyp_recs_jit_fallback.py:130:                "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
tests/data/repos/test_chart_renders_repo.py:43:        "source_data_hash": "deadbeef",
tests/data/repos/test_chart_renders_repo.py:62:        "source_data_hash FROM chart_renders WHERE id = ?",
tests/data/repos/test_chart_renders_repo.py:347:                source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:364:            source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:383:            source_data_hash="x", rendered_at="2024-01-01T00:00:00",
tests/data/repos/test_chart_renders_repo.py:410:            source_data_hash="x", rendered_at="2024-01-02T00:00:00",
tests/web/test_routes/test_watchlist_template_thumbnail.py:64:                source_data_hash="h",
tests/web/test_routes/test_patterns_exemplars_enhanced.py:117:                source_data_hash="h",
tests/web/test_routes/test_patterns_exemplars_enhanced.py:515:                source_data_hash="hg",
tests/web/test_routes/test_open_positions_expand.py:252:                source_data_hash="hash-pd",
tests/web/test_routes/test_watchlist_jit_integration.py:55:            "chart_svg_bytes, source_data_hash, rendered_at, data_asof_date) "
swing/web/chart_jit.py:82:    source_data_hash: str = "chart_jit_v1",
swing/web/chart_jit.py:201:    # asof_date) instead of the static ``source_data_hash`` default so the cache
swing/web/chart_jit.py:207:        computed_hash = source_data_hash
swing/web/chart_jit.py:215:            source_data_hash=computed_hash,
swing/web/charts.py:79:    """Content-derived value for ``chart_renders.source_data_hash``.
tests/web/test_routes/test_trades_detail_chart.py:57:                source_data_hash="h",
tests/web/test_routes/test_trades_detail_chart.py:107:                source_data_hash="h",
tests/web/test_routes/test_dashboard_chart_integration.py:52:        source_data_hash="hash-w",
tests/web/test_routes/test_dashboard_chart_integration.py:161:                source_data_hash="h",
tests/web/test_routes/test_dashboard_chart_integration.py:217:                source_data_hash="h",
tests/web/test_routes/test_recommendations_detail_chart.py:86:                source_data_hash="h",
tests/web/test_routes/test_recommendations_detail_chart.py:134:                source_data_hash="h",
swing/pipeline/detection_chart_capture.py:37:            chart_svg_bytes=svg, source_data_hash="detection_capture_v1",
swing/pipeline/runner.py:3426:                     bytes_: bytes, source_data_hash: str) -> None:
swing/pipeline/runner.py:3433:                source_data_hash=source_data_hash,
swing/pipeline/runner.py:3490:            source_data_hash=compute_chart_source_hash(bars),
swing/pipeline/runner.py:3532:                source_data_hash=compute_chart_source_hash(bars),
swing/pipeline/runner.py:3590:                source_data_hash=compute_chart_source_hash(display_bars),
swing/data/repos/chart_renders.py:26:    "source_data_hash",
swing/data/repos/chart_renders.py:42:        source_data_hash=row[6],
swing/data/repos/chart_renders.py:61:             chart_svg_bytes, source_data_hash, rendered_at, data_asof_date)
swing/data/repos/chart_renders.py:70:            chart_render.source_data_hash,
swing/web/routes/dashboard.py:165:            source_data_hash="manual_refresh",
swing/data/models.py:2001:    source_data_hash: str
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:32:    source_data_hash TEXT NOT NULL,
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:48:        pattern_class, chart_svg_bytes, source_data_hash, rendered_at,
swing/data/migrations/0023_phase14_sb3_chart_surface_rename.sql:52:        pipeline_run_id, pattern_class, chart_svg_bytes, source_data_hash,
swing/data/migrations/0020_phase13_charts_patterns_autofill_usability.sql:192:    source_data_hash TEXT NOT NULL,

exec
/bin/bash -lc "sed -n '210,245p' swing/web/view_models/watchlist.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
            unresolved = count_unresolved_material(conn)
            recent_multi_leg = count_recent_multi_leg_auto_corrections(conn)
            banner_resolve_link = (
                fetch_first_pending_ambiguity_resolve_link_path(conn)
            )
            # Arc 7 Task 6 — registry for the per-row cohort-hint preview.
            registry = list_hypotheses(conn)
            # Phase 13 T2.SB6b T-A.6.6 — pull cached watchlist row chart
            # bytes from the chart_renders cache (T2.SB6a substrate
            # verbatim). Empty dict when no pipeline run / no cache rows.
            watchlist_chart_svg_bytes: dict[str, bytes] = {}
            if pipeline_run_id is not None:
                from swing.data.repos.chart_renders import (
                    get_cached_chart_svg,
                )
                for r in rows:
                    svg = get_cached_chart_svg(
                        conn, ticker=r.ticker,
                        surface="watchlist_row",
                        pipeline_run_id=pipeline_run_id,
                    )
                    if svg is not None:
                        watchlist_chart_svg_bytes[r.ticker] = svg
    finally:
        conn.close()
    by_ticker = {c.ticker: c for c in candidates}
    flag_tags = _flag_tags(by_ticker)
    pattern_tags = _pattern_tags(
        classifications,
        display_threshold=cfg.web.flag_pattern_display_threshold,
    )
    # Arc 7 Task 6 — per-ticker cohort-hint preview (read-only).
    cohort_hints: dict[str, str] = {}
    for r in rows:
        hint = cohort_hint_for(by_ticker.get(r.ticker), registry)
        if hint:

exec
/bin/bash -lc "sed -n '1988,2035p' swing/data/models.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
class ChartRender:
    """One row of ``chart_renders`` (migration 0020; spec §3.2).

    Theme 1 chart cache; ``surface`` references _CHART_SURFACE_VALUES.
    Cross-column CHECK: ``surface='theme2_annotated'`` requires both
    ``pattern_class`` AND ``pipeline_run_id`` non-NULL; all other surfaces
    require ``pattern_class`` NULL (mirrors schema CHECK + closes Codex R2
    M#5 SQLite NULL-distinct defense).
    """
    id: int | None
    ticker: str
    surface: str  # one of _CHART_SURFACE_VALUES
    chart_svg_bytes: bytes
    source_data_hash: str
    rendered_at: str
    data_asof_date: str
    pipeline_run_id: int | None = None
    pattern_class: str | None = None

    def __post_init__(self) -> None:
        if self.surface not in _CHART_SURFACE_VALUES:
            raise ValueError(
                "surface must be one of "
                f"{_CHART_SURFACE_VALUES}, got {self.surface!r}"
            )
        if (
            self.pattern_class is not None
            and self.pattern_class not in DETECTOR_PATTERN_CLASSES
        ):
            raise ValueError(
                "pattern_class must be None or one of "
                f"{DETECTOR_PATTERN_CLASSES}, got {self.pattern_class!r}"
            )

        # Cross-column CHECK per spec §3.2.
        if self.surface == "theme2_annotated":
            if self.pattern_class is None or self.pipeline_run_id is None:
                raise ValueError(
                    "chart_renders cross-column CHECK violated: "
                    "surface='theme2_annotated' requires both pattern_class "
                    "AND pipeline_run_id non-NULL"
                )
        else:
            if self.pattern_class is not None:
                raise ValueError(
                    "chart_renders cross-column CHECK violated: "
                    f"pattern_class must be NULL when surface={self.surface!r}"
                )

exec
/bin/bash -lc "sed -n '730,760p' swing/web/view_models/dashboard.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
    # omits it (e.g., unit tests exercising other code paths) we skip the
    # fetch entirely and leave current_price None.
    current_price = None
    if cache is not None and executor is not None:
        prices = cache.get_many(
            [ticker],
            deadline_seconds=cfg.web.price_fetch_deadline_seconds,
            executor=executor,
        )
        current_price = prices.get(ticker)

    # Phase 13 T2.SB6c T-A.6c.2 Gap A.1 — consult chart_renders cache for
    # the ticker detail surface (surface='ticker_detail', run-bound key).
    # Reuses the T2.SB6a substrate `get_cached_chart_svg` verbatim (L7 +
    # L17 LOCK).
    from swing.data.repos.chart_renders import get_cached_chart_svg
    ticker_detail_chart_svg_bytes = get_cached_chart_svg(
        conn,
        ticker=ticker,
        surface="ticker_detail",
        pipeline_run_id=binding.run_id,
    )

    # Phase 13 T-T4.SB.3 (Item 5) — JIT cache-miss fallback. When the
    # pipeline's pre-gen did not populate this surface (e.g., the ticker
    # rotated into A+ AFTER pipeline completed; or _step_charts scope
    # narrowed per OQ-5.3), live-render via the JIT helper + write-through.
    #
    # Gating per R3 LOCK + plan §B.3 Sub-task 3C.1:
    #   - ticker_detail_chart_svg_bytes is None (cache miss)
    #   - ohlcv_cache is not None (caller provided)

exec
/bin/bash -lc "sed -n '880,915p' swing/web/view_models/dashboard.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
                dashboard_weather_chart_svg_bytes = get_cached_chart_svg(
                    conn,
                    ticker=cfg.rs.benchmark_ticker,
                    surface="market_weather",
                    pipeline_run_id=pipeline_run_id,
                )
            recs = list_for_session(
                conn, action_session, evaluation_run_id=pipeline_eval_id,
            )
            watchlist = list_active_watchlist(conn)
            # Phase 13 T2.SB6b T-A.6.6 — populate per-ticker chart SVG bytes
            # from chart_renders cache (T2.SB6a substrate verbatim).
            if pipeline_run_id is not None:
                from swing.data.repos.chart_renders import (
                    get_cached_chart_svg,
                )
                for entry in watchlist:
                    svg = get_cached_chart_svg(
                        conn, ticker=entry.ticker,
                        surface="watchlist_row",
                        pipeline_run_id=pipeline_run_id,
                    )
                    if svg is not None:
                        watchlist_chart_svg_bytes_by_ticker[entry.ticker] = (
                            svg
                        )
                for trade in open_trades:
                    svg = get_cached_chart_svg(
                        conn, ticker=trade.ticker,
                        surface="position_detail",
                        pipeline_run_id=None,
                    )
                    if svg is not None:
                        position_chart_svg_bytes_by_ticker[trade.ticker] = (
                            svg
                        )

codex
I found one real regression risk: the success path now discards the Schwab window as a return value and relies on the legacy yfinance archive read. That fixes XMAX/TDAY when a legacy archive exists, but it can turn a valid Schwab success into no bars when there is no legacy archive and yfinance cannot populate one. I’m checking line numbers and whether there are any additional read-time cache invalidation claims contradicted by the code.
exec
/bin/bash -lc "nl -ba swing/web/chart_jit.py | sed -n '95,125p;196,210p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
    95	    # ``chart_svg_bytes`` is zero-length BLOB (schema only enforces
    96	    # NOT NULL, not non-empty) MUST be treated as a cache miss so the
    97	    # helper falls through to JIT render + write-through replaces the
    98	    # empty row. Without this guard, ``cached is not None`` succeeded
    99	    # at the construction-barrier was bypassed — operator stays blank.
   100	    cached = get_cached_chart_svg(
   101	        conn,
   102	        surface=surface,
   103	        ticker=ticker,
   104	        pipeline_run_id=pipeline_run_id,
   105	        pattern_class=pattern_class,
   106	    )
   107	    if cached is not None and len(cached) > 0:
   108	        return cached
   109	    if cached is not None:
   110	        # cached == b"" → log + fall through to render path.
   111	        logger.warning(
   112	            "chart_jit: cache row for %s/%s/%s has empty bytes; "
   113	            "re-rendering",
   114	            surface, ticker, pipeline_run_id,
   115	        )
   116	
   117	    # Step 2: fetch OHLCV via cache (per ``_step_charts._bars_or_none`` precedent).
   118	    try:
   119	        bars = ohlcv_cache.get_or_fetch(
   120	            ticker=ticker, window_days=MIN_CALENDAR_DAYS_FOR_MA200,
   121	        )
   122	    except Exception as exc:  # noqa: BLE001 — log + degrade
   123	        logger.warning(
   124	            "chart_jit ohlcv_cache failure for %s: %s", ticker, exc,
   125	        )
   196	            surface, ticker,
   197	        )
   198	        return None
   199	
   200	    # Phase 16 Arc 3 (3c): stamp a content-derived hash (bar count + first/last
   201	    # asof_date) instead of the static ``source_data_hash`` default so the cache
   202	    # row's provenance changes on data growth. Falls back to the passed literal
   203	    # only if hashing fails (defensive — never block the write-through).
   204	    try:
   205	        computed_hash = compute_chart_source_hash(bars)
   206	    except Exception:  # noqa: BLE001 — provenance is best-effort
   207	        computed_hash = source_data_hash
   208	
   209	    try:
   210	        chart_render = ChartRender(

exec
/bin/bash -lc "nl -ba swing/pipeline/runner.py | sed -n '490,515p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
   490	        # provenance preserved by `fetch_window_via_ladder`); here we return the
   491	        # FULL archive — the SAME read the no-ladder/ticker_detail path uses —
   492	        # so both surfaces converge on identical bar counts. The yfinance
   493	        # fallback path already returns the full archive (`_yf_window_fallback`
   494	        # → read_or_fetch_archive), so this unifies both provider paths.
   495	        # schwab_api → re-read the full archive (above); yfinance → `window` is
   496	        # already the full archive frame from `_yf_window_fallback`.
   497	        bars = (
   498	            _yf_window_fallback(ticker, None, None)
   499	            if provider_tag == "schwab_api"
   500	            else window
   501	        )
   502	        return (bars, provider_tag)
   503	
   504	    price_cache.set_ladder_fetcher(_quote_hook)
   505	    ohlcv_cache.set_ladder_bars_fetcher(_bars_hook)
   506	
   507	    return price_cache, ohlcv_cache, audit_conn
   508	
   509	
   510	def _warm_pipeline_marketdata(
   511	    *,
   512	    cfg,
   513	    price_cache,
   514	    held_tickers: list[str],
   515	) -> None:

exec
/bin/bash -lc "nl -ba swing/data/repos/chart_renders.py | sed -n '141,205p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
   141	def get_cached_chart_svg(
   142	    conn: sqlite3.Connection,
   143	    *,
   144	    ticker: str,
   145	    surface: str,
   146	    pipeline_run_id: int | None = None,
   147	    pattern_class: str | None = None,
   148	    min_data_asof_date: str | None = None,
   149	) -> bytes | None:
   150	    """Return the cached SVG bytes for the canonical chart cache key, or None.
   151	
   152	    Per spec §C.2 cache key shape LOCK:
   153	      - Run-bound surfaces (``watchlist_row``, ``ticker_detail``,
   154	        ``market_weather``): key on ``(ticker, surface, pipeline_run_id)``.
   155	      - ``position_detail`` surface: key on ``(ticker, surface)`` with
   156	        ``pipeline_run_id IS NULL``.
   157	      - ``theme2_annotated`` surface: key on
   158	        ``(ticker, surface, pipeline_run_id, pattern_class)``.
   159	
   160	    Per spec §A.13 session-anchor read/write predicate alignment LOCK:
   161	    ``min_data_asof_date`` is the staleness predicate; pass
   162	    ``last_completed_session(now()).isoformat()`` (backward-looking; same
   163	    function the writer uses) so the read returns rows whose
   164	    ``data_asof_date >= min_data_asof_date``. If ``min_data_asof_date`` is
   165	    None, no staleness filter applies (return the most-recent row
   166	    regardless of asof). Discriminating round-trip test pattern lives at
   167	    ``tests/data/test_chart_renders_repo.py`` (Phase 8 ``cfacbc5``
   168	    precedent).
   169	    """
   170	    where_clauses = ["ticker = ?", "surface = ?"]
   171	    params: list[object] = [ticker, surface]
   172	
   173	    if pipeline_run_id is None:
   174	        where_clauses.append("pipeline_run_id IS NULL")
   175	    else:
   176	        where_clauses.append("pipeline_run_id = ?")
   177	        params.append(pipeline_run_id)
   178	
   179	    if pattern_class is None:
   180	        where_clauses.append("pattern_class IS NULL")
   181	    else:
   182	        where_clauses.append("pattern_class = ?")
   183	        params.append(pattern_class)
   184	
   185	    if min_data_asof_date is not None:
   186	        where_clauses.append("data_asof_date >= ?")
   187	        params.append(min_data_asof_date)
   188	
   189	    sql = (
   190	        "SELECT chart_svg_bytes FROM chart_renders WHERE "
   191	        + " AND ".join(where_clauses)
   192	        + " ORDER BY id DESC LIMIT 1"
   193	    )
   194	    row = conn.execute(sql, tuple(params)).fetchone()
   195	    if row is None:
   196	        return None
   197	    return bytes(row[0]) if row[0] is not None else None
   198	
   199	
   200	def refresh_chart_render(
   201	    conn: sqlite3.Connection, chart_render: ChartRender,
   202	) -> int:
   203	    """DELETE-then-INSERT atomic refresh of one cache row.
   204	
   205	    Per §A.15 LOCK (no ``INSERT OR REPLACE`` on audit-trail tables) +

exec
/bin/bash -lc "nl -ba swing/web/charts.py | sed -n '74,100p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
    74	
    75	logger = logging.getLogger(__name__)
    76	
    77	
    78	def compute_chart_source_hash(bars: Any) -> str:
    79	    """Content-derived value for ``chart_renders.source_data_hash``.
    80	
    81	    Phase 16 Arc 3 (3c): the pipeline + JIT write sites previously stamped a
    82	    STATIC literal (``"step_charts_v1"`` / ``"chart_jit_v1"``) which does not
    83	    encode the underlying data, so the field never changed when a chart's bar
    84	    history grew (e.g. a sparse 16-bar XMAX thumbnail vs a rich 207-bar render)
    85	    or its window shifted. Keying on (bar count + first/last asof_date) makes
    86	    the value honest: data growth → a different hash. VALUE-only change (no
    87	    schema). The field remains a provenance/audit tag today; an honest value is
    88	    the substrate for any future read-time hash-compare invalidation.
    89	
    90	    ``bars`` is the legacy yfinance-shape frame (DatetimeIndex + capitalized
    91	    OHLCV) the renderers consume. None / empty → a stable sentinel.
    92	    """
    93	    if bars is None or len(bars) == 0:
    94	        return "bars=0"
    95	    n = len(bars)
    96	    first = bars.index[0]
    97	    last = bars.index[-1]
    98	    first_iso = first.date().isoformat() if hasattr(first, "date") else str(first)
    99	    last_iso = last.date().isoformat() if hasattr(last, "date") else str(last)
   100	    return f"bars={n};first={first_iso};last={last_iso}"

exec
/bin/bash -lc "nl -ba swing/integrations/schwab/marketdata_ladder.py | sed -n '370,460p'" in /mnt/c/Users/rwsmy/swing-arc3
exec
/bin/bash -lc "nl -ba tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py | sed -n '120,190p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
   370	    ],
   371	    conn: sqlite3.Connection,
   372	    surface: str,
   373	    pipeline_run_id: int | None = None,
   374	    period_type: str | None = None,
   375	    period: int | None = None,
   376	    frequency_type: str | None = None,
   377	    frequency: int | None = None,
   378	) -> tuple[SchwabPriceHistoryWindow, str]:
   379	    """Window-fetch ladder: Schwab → yfinance fallback.
   380	
   381	    Returns ``(window, provider_tag)`` where:
   382	      - ``window`` is a ``SchwabPriceHistoryWindow`` instance (or whatever
   383	        the yfinance fallback emits; callers should treat it as a window-
   384	        shaped object).
   385	      - ``provider_tag`` is ``'schwab_api'`` on Schwab success or
   386	        ``'yfinance'`` on fallback.
   387	
   388	    The ``yfinance_fallback_fn`` callable receives ``(ticker, start, end)``
   389	    and MUST return a window-shaped object.
   390	
   391	    Empty-bars handling (plan §E.5 + §H.6.4): the T-C.1 mapper raises
   392	    ``SchwabApiError(204, "empty bars")`` when Schwab returns
   393	    ``{"empty": true, "candles": []}``. The ladder catches via the
   394	    ``SchwabApiError`` except clause; falls back to yfinance; the T-C.1
   395	    wrapper's audit row carries ``error_message='empty bars (transient)'``.
   396	    Parquet write at T-C.2 is unchanged (empty-write guard provides
   397	    defense-in-depth).
   398	
   399	    **Phase 13 T1.SB0 gate-fix (T-GF2): period/frequency kwarg forwarding.**
   400	    When the caller wants daily bars (chart-step / detector / archive use
   401	    cases), it MUST pass ``period_type="year", period=5,
   402	    frequency_type="daily", frequency=1`` (or equivalent month/daily combo)
   403	    — otherwise schwabdev defaults to ``periodType=day, period=10,
   404	    frequencyType=minute, frequency=1``, returning ~3000 1-minute intraday
   405	    candles instead of daily bars (recon at
   406	    ``docs/phase13-t1-sb0-gate-fix-recon.md`` §2.B). When all four are
   407	    ``None`` (legacy contract), Schwab applies its server-side defaults —
   408	    preserved for backward compatibility with any caller that genuinely
   409	    wants the default intraday minute-bar behavior.
   410	
   411	    Raises:
   412	        TypeError / ValueError on invalid ticker input.
   413	    """
   414	    ticker = _validate_ticker(ticker)
   415	    cache_dir = _resolve_cache_dir(cfg)
   416	
   417	    if not _is_ladder_active(cfg):
   418	        window = yfinance_fallback_fn(ticker, start, end)
   419	        # Codex R1 Major #3 — persist yfinance window to Shape A archive
   420	        # so subsequent resolve_ohlcv_window calls see the rows.
   421	        _persist_window_to_archive(ticker, window, "yfinance", cache_dir)
   422	        return (window, "yfinance")
   423	
   424	    if schwab_client is None:
   425	        log.warning(
   426	            "fetch_window_via_ladder: schwab_client is None in production; "
   427	            "falling back to yfinance for %s",
   428	            ticker,
   429	        )
   430	        window = yfinance_fallback_fn(ticker, start, end)
   431	        _persist_window_to_archive(ticker, window, "yfinance", cache_dir)
   432	        return (window, "yfinance")
   433	
   434	    try:
   435	        schwab_window = get_price_history(
   436	            schwab_client,
   437	            conn,
   438	            ticker,
   439	            period_type=period_type,
   440	            period=period,
   441	            frequency_type=frequency_type,
   442	            frequency=frequency,
   443	            start_dt=start,
   444	            end_dt=end,
   445	            surface=surface,
   446	            environment="production",
   447	            pipeline_run_id=pipeline_run_id,
   448	        )
   449	    except (SchwabAuthError, SchwabRateLimitError, SchwabApiError) as exc:
   450	        log.warning(
   451	            "Schwab price_history failed for %s (%s); falling back to "
   452	            "yfinance",
   453	            ticker, type(exc).__name__,
   454	        )
   455	        window = yfinance_fallback_fn(ticker, start, end)
   456	        _persist_window_to_archive(ticker, window, "yfinance", cache_dir)
   457	        return (window, "yfinance")
   458	    except Exception as exc:
   459	        # Log the exception class+message (NOT exc_info: traceback frames bypass
   460	        # message-level redaction) so a silent degrade is diagnosable.

 succeeded in 0ms:
   120	    returns ~207 → divergence; ``len(ladder_df) > 60`` FAILS.
   121	
   122	    Post-fix: ``_bars_hook`` re-reads the FULL archive after the ladder
   123	    fetch+persist → both paths return identical counts; ``len(ladder_df) > 60``
   124	    passes and the two counts are equal.
   125	    """
   126	    from swing.pipeline.runner import _install_pipeline_marketdata_caches
   127	    from swing.web.ohlcv_cache import OhlcvCache
   128	
   129	    db_path = tmp_path / "arc3.db"
   130	    ensure_schema(db_path).close()
   131	    cfg = _make_pipeline_cfg(db_path, tmp_path)
   132	
   133	    end = pd.Timestamp("2026-06-08")
   134	    legacy = _full_legacy_archive(end=end, n_rows=1200)
   135	
   136	    def _stub_read_or_fetch_archive(ticker, *, end_date, cache_dir, archive_history_days):
   137	        return legacy.loc[legacy.index.date <= end_date].copy()
   138	
   139	    def _stub_session(_now_dt):
   140	        return end.date()
   141	
   142	    # The full archive read used by BOTH the post-fix ladder hook (runner
   143	    # binding) and the no-ladder path (ohlcv_cache binding). Stubbed identically
   144	    # so any count divergence is attributable to the hook, not the data source.
   145	    monkeypatch.setattr(
   146	        "swing.pipeline.runner.read_or_fetch_archive", _stub_read_or_fetch_archive,
   147	    )
   148	    monkeypatch.setattr(
   149	        "swing.web.ohlcv_cache.read_or_fetch_archive", _stub_read_or_fetch_archive,
   150	    )
   151	    monkeypatch.setattr(
   152	        "swing.pipeline.runner.last_completed_session", _stub_session,
   153	    )
   154	    monkeypatch.setattr(
   155	        "swing.web.ohlcv_cache.last_completed_session", _stub_session,
   156	    )
   157	
   158	    # Ladder stub: a SCHWAB SUCCESS returning only the short 16-bar window.
   159	    def _stub_ladder_schwab_success(ticker, **_kwargs):
   160	        return (_short_schwab_window(ticker, end=end, n_days=16), "schwab_api")
   161	
   162	    monkeypatch.setattr(
   163	        "swing.integrations.schwab.marketdata_ladder.fetch_window_via_ladder",
   164	        _stub_ladder_schwab_success,
   165	    )
   166	
   167	    # Ladder (thumbnail) path.
   168	    _price, ladder_cache, audit_conn = _install_pipeline_marketdata_caches(
   169	        cfg, MagicMock(), pipeline_run_id=1,
   170	    )
   171	    assert ladder_cache is not None
   172	    assert ladder_cache._ladder_bars_fetcher is not None
   173	    ladder_df = ladder_cache.get_or_fetch(ticker="XMAX", window_days=300)
   174	
   175	    # No-ladder (ticker_detail) path — same cfg, NO ladder installed.
   176	    plain_cache = OhlcvCache(cfg)
   177	    plain_df = plain_cache.get_or_fetch(ticker="XMAX", window_days=300)
   178	
   179	    # Post-fix: the ladder/thumbnail path is NOT capped at the 16-bar Schwab
   180	    # window — it reflects the full archive sliced to 300 calendar days.
   181	    assert len(ladder_df) > 60, (
   182	        f"ladder bars hook returned too few rows ({len(ladder_df)}); the "
   183	        f"schwab_api success path truncated to the short Schwab window instead "
   184	        f"of returning the full archive (full-archive-return contract)."
   185	    )
   186	    # Discriminating convergence regression: the thumbnail (ladder) and
   187	    # ticker_detail (no-ladder) inputs MUST be the same bar count for a ticker
   188	    # with a partial Schwab Shape-A file + a full legacy archive.
   189	    assert len(ladder_df) == len(plain_df), (
   190	        f"thumbnail (ladder) path {len(ladder_df)} bars != ticker_detail "

exec
/bin/bash -lc "nl -ba swing/data/ohlcv_archive.py | sed -n '715,785p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
   715	def read_or_fetch_archive(
   716	    ticker: str,
   717	    *,
   718	    end_date: date,
   719	    cache_dir: Path,
   720	    archive_history_days: int,
   721	) -> pd.DataFrame | None:
   722	    """Read the per-ticker archive, refreshing from yfinance as needed,
   723	    return rows ≤ end_date.
   724	
   725	    Args:
   726	        ticker: ticker symbol; used as the archive filename stem.
   727	        end_date: caller's window end (inclusive). Used ONLY for the return
   728	            slice — the on-disk archive always extends to
   729	            `_last_completed_session_today()` regardless. Historical callers
   730	            receive a slice; they do not truncate the archive.
   731	        cache_dir: archive directory (typically `cfg.paths.prices_cache_dir`).
   732	            Must already exist.
   733	        archive_history_days: full-history fetch window (typically
   734	            `cfg.archive.archive_history_days`).
   735	
   736	    Returns:
   737	        DataFrame indexed by date with OHLCV columns; rows ≤ end_date.
   738	        None if yfinance returns empty (delisted / invalid ticker / no history).
   739	    """
   740	    cache_dir = Path(cache_dir)
   741	    ticker = ticker.upper()
   742	    cache_dir.mkdir(parents=True, exist_ok=True)
   743	    parquet_path, meta_path = _archive_paths(cache_dir, ticker)
   744	
   745	    today = _last_completed_session_today()
   746	
   747	    archive = _read_archive(parquet_path)
   748	    meta = _read_meta(meta_path)
   749	
   750	    last_full_refresh: date | None = None
   751	    last_full_str = meta.get("last_full_refresh_date")
   752	    if last_full_str:
   753	        try:
   754	            last_full_refresh = date.fromisoformat(last_full_str)
   755	        except ValueError:
   756	            last_full_refresh = None
   757	
   758	    if archive is None or archive.empty or last_full_refresh is None:
   759	        needs_full_refresh = True
   760	    else:
   761	        needs_full_refresh = _full_refresh_due(
   762	            ticker, last_full_refresh, today,
   763	            stagger_enabled=_full_refresh_stagger_enabled(),
   764	        )
   765	
   766	    if needs_full_refresh:
   767	        full_calendar_days = _calendar_window_for_trading_days(archive_history_days)
   768	        full_start = today - timedelta(days=full_calendar_days)
   769	        fetched = _yf_download_window(ticker, start=full_start, end=today)
   770	        if fetched.empty:
   771	            # Codex R2 Major 2 — established ticker + transient empty upstream:
   772	            # do NOT overwrite archive or meta. Fall back to the existing archive
   773	            # so callers don't see a transient yfinance hiccup as data absence.
   774	            # Meta stays stale so the next call retries the weekly refresh.
   775	            if archive is not None and not archive.empty:
   776	                return archive.loc[archive.index.date <= end_date]
   777	            return None
   778	        fetched = fetched.tail(archive_history_days)
   779	        _write_archive_atomic(parquet_path, fetched)
   780	        _write_meta_atomic(meta_path, {"last_full_refresh_date": today.isoformat()})
   781	        return fetched.loc[fetched.index.date <= end_date]
   782	
   783	    assert archive is not None
   784	    latest_stored: date = archive.index.max().date()
   785	    if latest_stored < today:

exec
/bin/bash -lc "nl -ba swing/web/ohlcv_cache.py | sed -n '265,310p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
   265	        ``swing.pipeline.ohlcv.fetch_daily_bars`` (CLAUDE.md yfinance gotcha).
   266	        """
   267	        end = last_completed_session(_dt.now())
   268	        cutoff = end - timedelta(days=window_days)
   269	
   270	        if self._ladder_bars_fetcher is not None:
   271	            try:
   272	                result = self._ladder_bars_fetcher(ticker)
   273	            except Exception as exc:  # noqa: BLE001 — safety boundary
   274	                log.warning(
   275	                    "ohlcv ladder bars fetch raised for %s: %s", ticker, exc,
   276	                )
   277	                return None
   278	            if result is None:
   279	                return None
   280	            bars = result[0] if isinstance(result, tuple) else result
   281	            if bars is None or bars.empty:
   282	                return None
   283	        else:
   284	            bars = read_or_fetch_archive(
   285	                ticker,
   286	                end_date=end,
   287	                cache_dir=self._cfg.paths.prices_cache_dir,
   288	                archive_history_days=self._cfg.archive.archive_history_days,
   289	            )
   290	            if bars is None or bars.empty:
   291	                return None
   292	
   293	        # Belt-and-suspenders in-progress bar strip (CLAUDE.md yfinance gotcha).
   294	        # Inequality matters: `end` here is `last_completed_session(now())`
   295	        # (backward-looking), so the archive's last legitimate bar can equal
   296	        # `end`. Only strict-greater (a stray partial bar past the last
   297	        # completed session) is partial-data. Using `>=` would over-strip on
   298	        # every read. Distinct from `fetch_daily_bars` which anchors on
   299	        # `action_session_for_run(now())` (forward-looking) and correctly uses
   300	        # `>=`. Preserves shape parity with `PriceFetcher.get` (which performs
   301	        # no strip; archive helper handles partial-bar avoidance).
   302	        if bars.index[-1].date() > end:
   303	            bars = bars.iloc[:-1]
   304	        if bars.empty:
   305	            return None
   306	        sliced = bars.loc[
   307	            (bars.index.date >= cutoff) & (bars.index.date <= end)
   308	        ]
   309	        if sliced.empty:
   310	            return None

codex
The chart hash is definitely not part of any read predicate: cached SVG reads only key by ticker/surface/run/pattern and optionally `data_asof_date`. So the hash write improves audit/provenance, but it does not itself invalidate or re-render stale cached rows.
exec
/bin/bash -lc 'rg -n "''^diff --git" .codex-diff.txt' in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
1:diff --git a/swing/pipeline/runner.py b/swing/pipeline/runner.py
94:diff --git a/swing/web/chart_jit.py b/swing/web/chart_jit.py
130:diff --git a/swing/web/charts.py b/swing/web/charts.py
167:diff --git a/tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py b/tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py
368:diff --git a/tests/pipeline/test_step_charts_chart_renders_write_through.py b/tests/pipeline/test_step_charts_chart_renders_write_through.py
421:diff --git a/tests/web/test_chart_jit.py b/tests/web/test_chart_jit.py
473:diff --git a/tests/web/test_chart_source_hash.py b/tests/web/test_chart_source_hash.py

exec
/bin/bash -lc "sed -n '80,125p' swing/pipeline/runner.py" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
# (a function-local import would shadow the module attribute the patch
# targets).
from swing.pipeline.detection_chart_capture import (
    render_and_capture_detection_chart,
)
from swing.pipeline.finviz_schema import reject_csv, validate_csv
from swing.pipeline.finviz_select import AmbiguousInboxError, NoFilesError, select_csv
from swing.pipeline.heartbeat import Heartbeat
from swing.pipeline.lease import (
    ConcurrentRunBlockedError,
    Lease,
    acquire_lease,
)
from swing.pipeline.ohlcv import compute_adr_pct as _compute_adr_pct
from swing.pipeline.ohlcv import compute_smas as _compute_smas
from swing.pipeline.ohlcv import previous_close as _previous_close
from swing.pipeline.recovery import sweep_stale_artifacts
from swing.pipeline.staging import StagingDir, promote_staging
from swing.prices import PriceFetcher
from swing.recommendations.build import BuildContext, build_recommendations
from swing.rendering.briefing import BriefingInputs, build_briefing_view_model
from swing.rendering.charts import (
    ChartingUnavailableError,
    PatternOverlay,
    render_chart,
)
from swing.rendering.exporter import export_briefing
from swing.rendering.view_models import AdvisorySuggestionVM
from swing.trades.advisory import AdvisoryContext, compute_all_suggestions
from swing.trades.equity import current_equity, sizing_equity
from swing.watchlist.service import compute_watchlist_changes
from swing.web.charts import (
    compute_chart_source_hash,
    render_market_weather_svg,
    render_position_detail_svg,
    render_watchlist_thumbnail_svg,
)

log = logging.getLogger(__name__)


# C.10 migration helper (was: list_all_exits shim from repos/trades.py).
# current_equity() is duck-typed on .realized_pnl; we only need a list of
# objects exposing that attribute. Mirrors the per-module _ExitShape
# pattern in web view models and review_log; dies in the future cleanup
# phase when equity.py refactors to consume Fill directly.

exec
/bin/bash -lc 'rg -n "marketdata_ladder|fetch_window_via_ladder" swing/pipeline/runner.py | head' in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
319:    environment`` AND ``cfg.integrations.schwab.marketdata_ladder_enabled``
357:    from swing.integrations.schwab.marketdata_ladder import (
359:        fetch_window_via_ladder,
463:        window, provider_tag = fetch_window_via_ladder(
490:        # provenance preserved by `fetch_window_via_ladder`); here we return the

exec
/bin/bash -lc "nl -ba swing/pipeline/runner.py | sed -n '345,365p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
   345	        client. ``audit_conn`` is the single shared serialized audit-writer
   346	        connection (OQ-C); the caller MUST close it in the run ``finally``.
   347	
   348	    Consumer surfaces (post-Phase-13 T1.SB0): PriceCache is consumed by
   349	    ``_step_evaluate`` (open-trade-ticker warm); OhlcvCache is consumed by
   350	    ``_step_charts`` via ``ohlcv_cache.get_or_fetch(...)`` for chart-target
   351	    OHLCV (closes the Phase 11 Sub-bundle C R1 M#5 V1 deferral). Both caches
   352	    share the same ``schwab_client`` via captured closures.
   353	    """
   354	    if schwab_client is None:
   355	        return None, None, None
   356	
   357	    from swing.integrations.schwab.marketdata_ladder import (
   358	        fetch_quote_via_ladder,
   359	        fetch_window_via_ladder,
   360	    )
   361	    from swing.web.ohlcv_cache import OhlcvCache
   362	    from swing.web.price_cache import PriceCache
   363	
   364	    price_cache = PriceCache(cfg)
   365	    ohlcv_cache = OhlcvCache(cfg)

exec
/bin/bash -lc 'rg -n "provider_tag|_fetch_bundle_worker|compute_smas|_ladder_bars_fetcher" swing/web/ohlcv_cache.py' in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
132:        # callable which MUST return `(bars_df_or_none, provider_tag)` where
133:        # `provider_tag` is 'schwab_api' or 'yfinance'. The provider tag is
136:        self._ladder_bars_fetcher: (
156:    def set_ladder_bars_fetcher(
173:        self._ladder_bars_fetcher = fetcher
197:        Routing: when ``set_ladder_bars_fetcher`` is installed, the ladder hook
270:        if self._ladder_bars_fetcher is not None:
272:                result = self._ladder_bars_fetcher(ticker)
389:            fut = executor.submit(self._fetch_bundle_worker, t)
470:    def _fetch_bundle_worker(self, ticker: str) -> tuple[OhlcvBundle, bool]:
483:        via `set_ladder_bars_fetcher`, the worker delegates to it; the
489:            provider_tag: str | None = None
491:                if self._ladder_bars_fetcher is not None:
492:                    bars, provider_tag = self._ladder_bars_fetcher(ticker)
507:            smas = ohlcv_mod.compute_smas(bars, [10, 20, 50])
517:                provider=provider_tag,

exec
/bin/bash -lc "nl -ba swing/web/ohlcv_cache.py | sed -n '470,520p'" in /mnt/c/Users/rwsmy/swing-arc3
 succeeded in 0ms:
   470	    def _fetch_bundle_worker(self, ticker: str) -> tuple[OhlcvBundle, bool]:
   471	        """Worker: acquire semaphore, fetch bars, build bundle. Returns
   472	        (bundle, is_source_healthy).
   473	
   474	        `is_source_healthy=False` ONLY when the fetch raised (source-level
   475	        failure, e.g. yfinance down, network error). `True` means the fetch
   476	        completed successfully — an empty result is per-ticker data absence
   477	        (delisted symbol, bad ticker, no history) which is an operator issue,
   478	        not a source issue, and MUST NOT trip the global breaker.
   479	
   480	        Pure return — does NOT touch self._store (R1 Critical 1).
   481	
   482	        Schwab Sub-bundle C T-C.4: when a ladder bars fetcher is installed
   483	        via `set_ladder_bars_fetcher`, the worker delegates to it; the
   484	        returned provider tag is stamped onto the resulting OhlcvBundle.
   485	        Otherwise the legacy `ohlcv_mod.fetch_daily_bars` path runs (provider
   486	        tag remains None for backward compatibility).
   487	        """
   488	        with self._sema:
   489	            provider_tag: str | None = None
   490	            try:
   491	                if self._ladder_bars_fetcher is not None:
   492	                    bars, provider_tag = self._ladder_bars_fetcher(ticker)
   493	                else:
   494	                    bars = ohlcv_mod.fetch_daily_bars(
   495	                        ticker,
   496	                        n_bars=60,
   497	                        cache_dir=self._cfg.paths.prices_cache_dir,
   498	                        archive_history_days=self._cfg.archive.archive_history_days,
   499	                    )
   500	            except Exception as exc:
   501	                log.warning("ohlcv fetch raised for %s: %s", ticker, exc)
   502	                return OhlcvBundle.empty(fetched_at=time.monotonic()), False
   503	            now = time.monotonic()
   504	            if bars is None:
   505	                # Empty result (ticker has no data) — healthy from cache's view.
   506	                return OhlcvBundle.empty(fetched_at=now), True
   507	            smas = ohlcv_mod.compute_smas(bars, [10, 20, 50])
   508	            prev = ohlcv_mod.previous_close(bars)
   509	            adr_pct = ohlcv_mod.compute_adr_pct(bars, lookback=20)
   510	            return OhlcvBundle(
   511	                sma10=smas.get(10),
   512	                sma20=smas.get(20),
   513	                sma50=smas.get(50),
   514	                previous_close=prev,
   515	                fetched_at=now,
   516	                adr_pct=adr_pct,
   517	                provider=provider_tag,
   518	            ), True
   519	
   520	    def _record_outcome(self, *, success: bool) -> None:

codex
1. [MAJOR] [swing/pipeline/runner.py:497](/mnt/c/Users/rwsmy/swing-arc3/swing/pipeline/runner.py:497) — Schwab-success now depends on a yfinance/legacy archive read and can drop valid Schwab data. `fetch_window_via_ladder` persists the Schwab Shape-A file, but `_bars_hook` then returns `_yf_window_fallback(...)`, which calls `read_or_fetch_archive`; that reader only uses `{TICKER}.parquet` and yfinance, not `{TICKER}.schwab_api.parquet` ([swing/data/ohlcv_archive.py:743](/mnt/c/Users/rwsmy/swing-arc3/swing/data/ohlcv_archive.py:743), [swing/data/ohlcv_archive.py:766](/mnt/c/Users/rwsmy/swing-arc3/swing/data/ohlcv_archive.py:766)). For XMAX/TDAY with a full legacy archive, counts converge. For a Schwab-only ticker or a yfinance-empty/new listing, old behavior returned the Schwab window, while new behavior returns `None`, causing `OhlcvCache.get_or_fetch` to fail ([swing/web/ohlcv_cache.py:280](/mnt/c/Users/rwsmy/swing-arc3/swing/web/ohlcv_cache.py:280)). This regresses the “no legacy archive, only Schwab data” case.

2. [MINOR] [swing/pipeline/runner.py:502](/mnt/c/Users/rwsmy/swing-arc3/swing/pipeline/runner.py:502) — `provider_tag` is now misleading on the Schwab-success bars path. The hook returns bars from `_yf_window_fallback` but still returns provider `"schwab_api"`. `OhlcvCache` documents that tag as the provenance stamped onto the resulting bundle ([swing/web/ohlcv_cache.py:482](/mnt/c/Users/rwsmy/swing-arc3/swing/web/ohlcv_cache.py:482), [swing/web/ohlcv_cache.py:517](/mnt/c/Users/rwsmy/swing-arc3/swing/web/ohlcv_cache.py:517)). After this change, SMA/ADR values may be computed from legacy/yfinance archive data while reported as Schwab-sourced.

3. [MINOR] [swing/web/chart_jit.py:100](/mnt/c/Users/rwsmy/swing-arc3/swing/web/chart_jit.py:100) — The 3c `source_data_hash` change does not cause read-time invalidation or “re-render on data growth.” `get_or_render_surface` returns any nonempty cached SVG before fetching bars or computing the hash ([swing/web/chart_jit.py:107](/mnt/c/Users/rwsmy/swing-arc3/swing/web/chart_jit.py:107)), and `get_cached_chart_svg` filters only by ticker/surface/run/pattern and optional `data_asof_date`, never `source_data_hash` ([swing/data/repos/chart_renders.py:170](/mnt/c/Users/rwsmy/swing-arc3/swing/data/repos/chart_renders.py:170)). So this is provenance-only unless another writer refreshes the row.

4. [MINOR] [swing/web/charts.py:95](/mnt/c/Users/rwsmy/swing-arc3/swing/web/charts.py:95) — `compute_chart_source_hash` is not a content hash despite the name. Two frames with the same row count and first/last dates but different OHLCV values or different interior sessions collide. That is acceptable only as a coarse provenance token; it is not safe as a future invalidation key without including row dates and/or values.

5. [NIT] [swing/web/chart_jit.py:204](/mnt/c/Users/rwsmy/swing-arc3/swing/web/chart_jit.py:204) — The broad `except Exception` around `compute_chart_source_hash` silently falls back to the static literal. Since the helper is local and simple, a failure would likely indicate a real shape bug worth logging. As written, a provenance regression can be hidden during JIT writes.

6. [NIT] [tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:136](/mnt/c/Users/rwsmy/swing-arc3/tests/pipeline/test_bars_hook_schwab_success_returns_full_archive.py:136) — The reproduction test is discriminating for the exact old `window.to_dataframe()` truncation because it exercises the installed hook via `OhlcvCache.get_or_fetch`, but it stubs both `read_or_fetch_archive` bindings and the ladder. It does not cover real Shape-A persistence/readback, nor the Schwab-only/no-legacy regression in finding 1.

7. [NIT] `.codex-diff.txt:1` — No hard-constraint violation found in the diff: no schema migration files, `swing/trades/`, or `swing/data/ohlcv_archive.py` edits; the quote ladder is untouched; `write_window`/Shape-A persistence remains in `fetch_window_via_ladder`.
tokens used
79,279
